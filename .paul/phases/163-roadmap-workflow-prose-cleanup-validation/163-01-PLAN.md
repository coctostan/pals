---
phase: 163-roadmap-workflow-prose-cleanup-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kernel/workflows/roadmap-management.md
  - kernel/workflows/plan-phase.md
  - kernel/workflows/discuss-phase.md
  - kernel/workflows/create-milestone.md
  - tests/pi-end-to-end-validation.sh
autonomous: true
---

<objective>
## Goal
Trim the remaining high-frequency roadmap/planning workflow prose so the warm-path shared workflows stay authoritative but carry less duplicated explanation, then lock the preserved contract into validation.

## Purpose
Phase 160 recommended workflow-first cleanup in three bounded waves: wrappers, read discipline, then surrounding prose reduction. Phase 161 slimmed the Pi shells and Phase 162 narrowed roadmap reads. This plan completes the first-pass workflow cleanup by reducing the remaining narrative duplication around those warm planning/status routes without redesigning `ROADMAP.md`, weakening `.paul/*` authority, or touching guided-marker semantics.

## Output
- Slimmed warm-path shared workflow prose in the roadmap/planning route cluster
- Preserved slice-based roadmap reads, collaboration semantics, review menus, and single-next routing behavior
- Updated Pi validation coverage for the installed shared-workflow contract
- Reinstalled Pi surface plus passing Pi and cross-harness validation
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md
.paul/SPECIAL-FLOWS.md

## Prior Work (only if genuinely needed)
.paul/phases/162-workflow-read-discipline-reduction/162-01-SUMMARY.md
.paul/phases/160-findings-recommendation-package/context-reduction-recommendations-ranked.md
.paul/phases/159-workflow-bloat-audit/159-workflow-bloat-report.md
.paul/phases/159-workflow-bloat-audit/workflow-read-surface-inventory.md

## Source Files
kernel/workflows/roadmap-management.md
kernel/workflows/plan-phase.md
kernel/workflows/discuss-phase.md
kernel/workflows/create-milestone.md
tests/pi-end-to-end-validation.sh
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: TODD(100) → no standard test framework detected via hook commands; existing regression proof remains shell-based under `tests/` | IRIS(150) → no TODO/FIXME/HACK/XXX markers found in the candidate workflow and validation files | DAVE(200) → no CI config detected in repo root, so no CI coverage-gap guidance applies to this workflow-doc phase | DOCS(200) → the targeted shared workflows are themselves canonical documentation surfaces, and the durable proof should live in `tests/pi-end-to-end-validation.sh` rather than duplicate prose elsewhere | RUBY(250) → `kernel/workflows/roadmap-management.md` and `kernel/workflows/plan-phase.md` are still large at **452 / 376** lines, and `tests/pi-end-to-end-validation.sh` is already **880** lines, so cleanup and validation edits must stay localized and semantically bounded.

[dispatch] pre-plan enforcement: DEAN(50) → SKIPPED (no supported package manager or audit target detected in this repo)

[dispatch] post-plan: TODD(100) → 0 modifications (no standard TDD candidates detected for the targeted workflow markdown and shell validation file)
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | Plan / apply / unify lifecycle work | ✓ |
| /carl | required | Automatic session-boundary handling during execution | ✓ |
| TODD | required | During APPLY if validation sequencing or regression handling becomes relevant | ✓ |
| WALT | required | During APPLY / UNIFY quality gating and history updates | ✓ |

**BLOCKING:** Required lifecycle flows remain in force for APPLY even though this plan targets shared workflow markdown and validation rather than product runtime code.
</skills>

<acceptance_criteria>
## AC-1: Warm-path workflow prose is thinner without losing route semantics
```gherkin
Given the shared roadmap and planning workflows updated in Phase 162
When Phase 163 is implemented
Then `roadmap-management.md`, `plan-phase.md`, `discuss-phase.md`, and `create-milestone.md` remove nearby duplicated explanation while preserving slice-based roadmap reads, collaboration behavior, review prompts, and route-specific safety semantics.
```

## AC-2: Preservation targets remain explicit after cleanup
```gherkin
Given the workflow-first reduction guardrails from Phases 159-162
When the prose cleanup is complete
Then guided markers, `.paul/*` authority, `PROJECT.md`-first / `PRD.md`-selective planning, exact-one status routing, and one-question-at-a-time milestone behavior all remain clearly represented in the edited workflows.
```

## AC-3: The slimmer shared-workflow contract is deployed and regression-proofed
```gherkin
Given the updated repo-source workflows and validation script
When the Pi install surface is redeployed and the regression suites are run
Then the installed shared workflows reflect the slimmer contract and both Pi validation and cross-harness validation pass.
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Trim roadmap-management and plan-phase to one strong statement per behavior</name>
  <files>kernel/workflows/roadmap-management.md, kernel/workflows/plan-phase.md</files>
  <action>
    Reduce duplicated warm-path prose in the status/planning core without changing behavior:
    - in `roadmap-management.md`, keep the current-milestone roadmap window, git-aware routing table, and exact-one next-action semantics, but remove nearby restatements that do not add unique meaning
    - in `plan-phase.md`, keep the state-first / target-phase-detail read model, collaboration override semantics, exploratory vs direct-requirements planning mode, review menu, and required state-update step, but compress repeated explanation around those rules
    - prefer deleting or tightening duplicated narrative over inventing new abstractions, indirection, or references that would make execution less explicit
    - keep one strong authority statement where needed; do not repeat the same `.paul/*` or additive-lifecycle explanation in multiple nearby places
    Avoid: changing command outputs, guided-marker strings, git-routing precedence, module-dispatch order, or the required PLAN review/state-update behavior.
  </action>
  <verify>grep -q 'current milestone section' kernel/workflows/roadmap-management.md && grep -q 'exactly ONE' kernel/workflows/roadmap-management.md && grep -q 'target phase detail' kernel/workflows/plan-phase.md && grep -q 'planning.default_collaboration' kernel/workflows/plan-phase.md && grep -q 'Would you like to see the plan?' kernel/workflows/plan-phase.md && test "$(wc -l < kernel/workflows/roadmap-management.md)" -lt 452 && test "$(wc -l < kernel/workflows/plan-phase.md)" -lt 376</verify>
  <done>AC-1 and AC-2 satisfied for the core status/planning workflows: the files are slimmer while preserving slice-based reads, collaboration controls, and route-safe planning/status behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the same prose cleanup discipline to discuss-phase and create-milestone</name>
  <files>kernel/workflows/discuss-phase.md, kernel/workflows/create-milestone.md</files>
  <action>
    Tighten the remaining planning-adjacent workflow prose so these routes stay explicit but less repetitive:
    - in `discuss-phase.md`, preserve the selected-phase-detail roadmap model, `PROJECT.md`-first / `PRD.md`-selective brief behavior, collaboration-depth prompts, and exploratory vs direct-requirements distinction while trimming nearby duplicated narration
    - in `create-milestone.md`, preserve the insertion-slice roadmap read model, planning-posture carry-through, review menu, and one-question-at-a-time milestone prompts while removing repeated wording that does not change the workflow contract
    - keep route-local semantics explicit where they actually shape behavior; remove only the repeated surrounding prose
    Avoid: broadening into `discuss-milestone.md`, reopening structural `ROADMAP.md` redesign, or changing milestone/phase creation semantics outside this warm planning cluster.
  </action>
  <verify>grep -q 'selected phase detail' kernel/workflows/discuss-phase.md && grep -q 'direct-requirements' kernel/workflows/discuss-phase.md && grep -q 'one question at a time' kernel/workflows/create-milestone.md && grep -q 'Would you like to see the plan before I update ROADMAP.md?' kernel/workflows/create-milestone.md && test "$(wc -l < kernel/workflows/discuss-phase.md)" -lt 286 && test "$(wc -l < kernel/workflows/create-milestone.md)" -lt 279</verify>
  <done>AC-1 and AC-2 satisfied for the discussion/milestone routes: the files remain explicit about planning posture and milestone behavior while carrying less duplicated prose.</done>
</task>

<task type="auto">
  <name>Task 3: Add durable validation for the slimmer shared-workflow contract and run regression suites</name>
  <files>tests/pi-end-to-end-validation.sh</files>
  <action>
    Make the reduced workflow contract durable and verify it from the installed Pi surface:
    - add or refine Pi validation checks that assert the installed copies of `roadmap-management.md`, `plan-phase.md`, `discuss-phase.md`, and `create-milestone.md` still contain the preserved slice-based read, review-menu, and route-behavior markers after cleanup
    - keep the validation semantic (presence of critical markers / absence of removed scaffolding where appropriate), not brittle exact line counts
    - redeploy the Pi install surface from repo source, then run Pi validation and cross-harness validation
    - if validation fails, fix only the workflow or validation-surface issues exposed by this phase; do not expand into wrapper-shell work, structural artifact redesign, or module-dispatch compression
    Avoid: editing installed runtime copies directly under `~/.pi/agent/...`; all fixes must stay in repo-source files.
  </action>
  <verify>PALS_DRIVER=pi bash install.sh && bash tests/pi-end-to-end-validation.sh && bash tests/cross-harness-validation.sh</verify>
  <done>AC-3 satisfied: the slimmer shared-workflow contract is deployed from repo sources and both validation suites pass.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- `.paul/STATE.md` or `.paul/ROADMAP.md` structure beyond normal lifecycle bookkeeping for this plan flow
- Guided workflow markers, git-routing precedence, or canonical `/skill:paul-*` ownership semantics
- `kernel/workflows/apply-phase.md`, `kernel/workflows/unify-phase.md`, `kernel/workflows/resume-project.md`, or module-dispatch contracts in this phase
- `PROJECT.md` compact-brief role and the `PRD.md` selective-read model

## SCOPE LIMITS
- This phase is about bounded prose cleanup in the warm roadmap/planning workflow cluster, not structural `ROADMAP.md` compaction
- Do not redesign `STATE.md`, introduce hot/cold artifact splits, or add Pi-owned memory/telemetry
- Do not compress module-dispatch protocol in this phase; that remains a later, higher-risk candidate
- Do not broaden validation into a new harness; extend the existing Pi validation surface and reuse the current regression suites
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `kernel/workflows/roadmap-management.md` still preserves current-milestone slicing, git-aware routing, and exact-one next-action guidance while shrinking below its pre-phase size
- [ ] `kernel/workflows/plan-phase.md` still preserves target-phase-detail reads, collaboration overrides, review-menu behavior, and required state updates while shrinking below its pre-phase size
- [ ] `kernel/workflows/discuss-phase.md` and `kernel/workflows/create-milestone.md` still preserve selected-phase/insertion-slice reads, planning posture semantics, and one-question-at-a-time milestone behavior while shrinking below pre-phase size
- [ ] `tests/pi-end-to-end-validation.sh` asserts the installed shared-workflow contract semantically rather than by exact line counts
- [ ] `PALS_DRIVER=pi bash install.sh` completes successfully
- [ ] `bash tests/pi-end-to-end-validation.sh` passes
- [ ] `bash tests/cross-harness-validation.sh` passes
- [ ] All acceptance criteria are met
</verification>

<success_criteria>
- All 3 tasks completed
- The warm roadmap/planning workflow files are materially slimmer without losing preserved route semantics
- Validation covers the installed shared-workflow contract for the cleanup targets
- Pi install + Pi validation + cross-harness validation all pass
</success_criteria>

<output>
After completion, create `.paul/phases/163-roadmap-workflow-prose-cleanup-validation/163-01-SUMMARY.md`
</output>
