---
phase: 283-arch-advisory-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/arch/module.yaml
  - modules/arch/references/architecture-principles.md
  - modules/arch/references/architecture-patterns.md
  - modules/arch/references/architecture-drift.md
  - modules/arch/references/architecture-onboarding.md
  - tests/pi-end-to-end-validation.sh
  - tests/cross-harness-validation.sh
autonomous: true
---

<objective>
## Goal
Add optional, non-blocking ARCH side-effect-boundary advisory guidance for v2.65 and guard the installed ARCH markers across Pi and cross-harness validation.

## Purpose
Phase 283 closes v2.65 by deciding to use the existing ARCH surface for lightweight advisory language rather than creating a new functional-programming module or blocking gate. The guidance should help agents notice boundary drift such as domain logic reading env/time/filesystem/network/process state directly or controllers mixing orchestration, persistence, and business rules inline, while preserving project conventions and local architecture.

## Output
- ARCH module metadata and references mention functional-first side-effect-boundary drift as advisory-only guidance.
- Pi and cross-harness validation scripts assert installed ARCH resources and modules.yaml preserve the Phase 283 markers.
- APPLY verification evidence is ready for `283-01-SUMMARY.md` and milestone closure during UNIFY.
</objective>

<context>
## Project Context
.paul/STATE.md — Phase 283 of 283 is ready to plan; Phases 279–282 are complete.
.paul/ROADMAP.md — v2.65 current milestone; Phase 283 focuses on ARCH Advisory + Closure.
.paul/PROJECT.md — v2.65 must add practical “functional-first, not functional-only” guidance without overriding project conventions.

## Prior Work
.paul/phases/279-functional-first-guidance-contract/279-01-SUMMARY.md — established `docs/PALS-FUNCTIONAL-FIRST-AGENT-GUIDANCE-CONTRACT.md` as the authoritative implementation boundary and named Phase 283 as optional ARCH advisory scope.
.paul/phases/281-brownfield-convention-detection/281-01-SUMMARY.md — added local-idiom and effect-boundary detection context that ARCH guidance should preserve.
.paul/phases/282-todd-ruby-guidance/282-01-SUMMARY.md — completed TODD/RUBY guidance and confirmed Phase 283 can plan from the v2.65 contract boundary.

## Source Files
docs/PALS-FUNCTIONAL-FIRST-AGENT-GUIDANCE-CONTRACT.md — authoritative Phase 283 allowed scope and non-goals.
modules/arch/module.yaml — installed ARCH hook metadata and refs.
modules/arch/references/architecture-principles.md — ARCH advisory/blocking posture and principles.
modules/arch/references/architecture-patterns.md — boundary rules and structural metrics.
modules/arch/references/architecture-drift.md — drift labels and post-apply detection guidance.
modules/arch/references/architecture-onboarding.md — onboarding-aware architecture brief heuristics.
tests/pi-end-to-end-validation.sh — Pi installed-resource validation.
tests/cross-harness-validation.sh — cross-harness installed-resource validation.
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan enforcement: DEAN PASS — planned files are markdown/YAML/shell validation resources only; no dependency manifests or dependency changes in scope.
[dispatch] pre-plan advisory: ARCH — planned files are ARCH module metadata/references plus validation shell scripts; no app source architecture pattern is in scope. Use the Phase 279 contract to add optional side-effect-boundary advisory language only; no blocking ARCH gate.
[dispatch] pre-plan advisory: TODD — no test framework/spec files detected; this plan is `execute`, not TDD, because it changes module guidance text and validation marker checks.
[dispatch] pre-plan advisory: DOCS — documentation/module-instruction scope; related authority doc is `docs/PALS-FUNCTIONAL-FIRST-AGENT-GUIDANCE-CONTRACT.md`.
[dispatch] pre-plan advisory: IRIS — no TODO/FIXME/HACK/XXX markers found in planned files.
[dispatch] pre-plan advisory: DAVE — no CI config changes planned; validation remains shell-script based (`tests/pi-end-to-end-validation.sh`, `tests/cross-harness-validation.sh`, artifact consistency).
[dispatch] pre-plan advisory: RUBY — docs/module-instruction and validation-shell scope is not code debt; no preliminary debt indicators.
[dispatch] pre-plan advisory: CODI skipped — markdown/YAML/shell-only phase; no explicit TS/JS selectors or stable codegraph seeds.
[dispatch] pre-plan advisory: GABE/DANA/LUKE/ARIA/OMAR/REED/VERA/PETE/SETH — skipped; no API, data, UI, runtime observability/resilience/privacy/performance/security implementation paths in scope.

## Post-Plan Module Dispatch
[dispatch] post-plan: TODD — plan remains `execute`; no TDD restructure needed for docs/module-instruction and validation-shell scope.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | PLAN/APPLY/UNIFY lifecycle management | ✓ |
| /carl | required | Session boundary monitoring | automatic |
| TODD | required | APPLY test/verification discipline | via module dispatch |
| WALT | required | APPLY quality gates and validation | via module dispatch |

**APPLY note:** Required lifecycle/module skills must remain active; no extra user-loaded specialized flow is required for this docs/module-instruction/test-shell plan.
</skills>

<acceptance_criteria>
## AC-1: ARCH Side-Effect-Boundary Advisory Guidance
```gherkin
Given ARCH reports architecture context or drift findings
When in-scope code shows side-effect boundary drift such as direct env/time/filesystem/network/process access in domain logic or route/controller code mixing orchestration, persistence, and business rules
Then ARCH guidance surfaces the finding as advisory evidence and preserves project conventions without creating a default functional-style blocking gate
```

## AC-2: ARCH References Preserve Functional-First Constraints
```gherkin
Given ARCH references describe architecture principles, patterns, drift, and onboarding context
When Phase 283 guidance is added
Then the references mention functional-first / Pure Core, Explicit Effects only as lightweight advisory language and explicitly avoid broad rewrites, forced functional architecture, or overriding local idioms
```

## AC-3: Installed Resources Preserve ARCH Phase 283 Markers
```gherkin
Given Pi and cross-harness installers copy module resources
When validation runs after APPLY
Then installed ARCH resources and generated modules.yaml contain the Phase 283 markers for side-effect-boundary advisory guidance and advisory-only functional-first posture
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Add side-effect-boundary advisory language to ARCH metadata and references</name>
  <files>modules/arch/module.yaml, modules/arch/references/architecture-principles.md, modules/arch/references/architecture-patterns.md, modules/arch/references/architecture-drift.md, modules/arch/references/architecture-onboarding.md</files>
  <action>
    Update existing ARCH guidance within its advisory architecture semantics:
    - In `modules/arch/module.yaml`, add concise hook metadata markers for side-effect boundary drift, Pure Core, Explicit Effects, and functional-first advisory posture while preserving the existing “PLAN/APPLY/UNIFY owns blocking response” language.
    - In `architecture-principles.md`, add a principle or advisory note that ARCH may surface side-effect boundary drift when it affects testability, coupling, or maintainability, but must not force a functional architecture.
    - In `architecture-patterns.md`, add side-effect boundary examples to the component/boundary guidance, including domain logic reading env/clock/filesystem/network/process state directly, controllers mixing orchestration/persistence/business rules inline, and hidden dependencies.
    - In `architecture-drift.md`, add a drift category or common drift label for side-effect boundary drift with evidence requirements from in-scope changed files or resolved imports only.
    - In `architecture-onboarding.md`, add onboarding brief language that records local side-effect placement and local idioms only when sourced from in-scope evidence or provided docs.
    Avoid: new hooks, new modules, runtime source rewrites, default blocking ARCH gates for functional style, broad architecture conversion, dependency/tooling changes, or language that overrides project conventions.
  </action>
  <verify>Native grep/read checks show `side-effect boundary`, `Pure Core, Explicit Effects`, `advisory`, and `local idioms` (or equivalent project-convention-preserving wording) in ARCH files; `modules/arch/module.yaml` contains installed metadata markers for Phase 283 side-effect-boundary advisory guidance.</verify>
  <done>AC-1 and AC-2 satisfied: ARCH guidance surfaces side-effect boundary drift as advisory evidence and preserves functional-first constraints without adding blocking semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Add installed-resource validation markers and run verification</name>
  <files>tests/pi-end-to-end-validation.sh, tests/cross-harness-validation.sh</files>
  <action>
    Add validation assertions near the existing v2.65 installed-resource marker checks:
    - Pi validation should assert installed ARCH references preserve Phase 283 side-effect-boundary advisory markers and installed `modules.yaml` preserves ARCH hook metadata markers.
    - Cross-harness validation should assert both Claude Code and Pi installed ARCH references/modules.yaml preserve the same markers.
    - Prefer existing `tap_file_contains_all` patterns and group checks around ARCH resources: `architecture-principles.md`, `architecture-patterns.md`, `architecture-drift.md`, `architecture-onboarding.md`, and modules.yaml as needed.
    - Run focused native marker checks plus `git diff --check`, shell syntax checks, artifact consistency, and the two broader validation suites.
    Avoid: weakening existing checks, changing inherited CODI baseline semantics, adding external dependencies, or treating inherited Pi CODI marker failures as Phase 283 regressions when new ARCH markers pass.
  </action>
  <verify>`git diff --check`; `bash -n tests/pi-end-to-end-validation.sh tests/cross-harness-validation.sh`; `bash tests/helpers/artifact_consistency.sh`; `bash tests/cross-harness-validation.sh`; `bash tests/pi-end-to-end-validation.sh`; focused marker checks for all planned ARCH files and validation scripts.</verify>
  <done>AC-3 satisfied and verification evidence is ready for `283-01-SUMMARY.md` and v2.65 milestone closure.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- `docs/PALS-FUNCTIONAL-FIRST-AGENT-GUIDANCE-CONTRACT.md` — authoritative contract from Phase 279; cite it but do not rewrite it.
- `kernel/workflows/map-codebase.md` and `kernel/templates/codebase/*` — Phase 281 scope already complete.
- `modules/todd/*` and `modules/ruby/*` — Phase 282 scope already complete.
- Runtime source code, package manifests, dependency locks, CI configs, and generated `.paul/codebase/` outputs.

## SCOPE LIMITS
- No new modules, hooks, dependencies, linters, formatters, or runtime tooling.
- No default blocking ARCH gate for functional style or side-effect boundaries.
- No broad architectural rewrites or source-code refactors.
- Preserve project-specific conventions and local idioms as authoritative.
- Keep changes to ARCH module resources and validation marker checks named in this plan.
</boundaries>

<verification>
Before declaring plan complete:
- [ ] ARCH files contain side-effect-boundary advisory guidance, Pure Core, Explicit Effects markers, and project-convention/local-idiom preservation language.
- [ ] `tests/pi-end-to-end-validation.sh` contains Pi installed-resource marker assertions for ARCH Phase 283 markers.
- [ ] `tests/cross-harness-validation.sh` contains Claude Code + Pi marker assertions for ARCH Phase 283 markers.
- [ ] `git diff --check` result captured.
- [ ] `bash -n tests/pi-end-to-end-validation.sh tests/cross-harness-validation.sh` result captured.
- [ ] `bash tests/helpers/artifact_consistency.sh` result captured.
- [ ] `bash tests/cross-harness-validation.sh` result captured.
- [ ] `bash tests/pi-end-to-end-validation.sh` result captured.
- [ ] All acceptance criteria met.
</verification>

<success_criteria>
- ARCH guidance surfaces side-effect-boundary drift as advisory, source-backed evidence.
- Guidance is functional-first, not functional-only; project conventions and local idioms remain authoritative.
- No new blocking gate, runtime refactor, dependency, module, or hook is introduced.
- Installed-resource validation guards the new ARCH markers across Pi and cross-harness installs.
- v2.65 is ready for milestone closure during UNIFY.
</success_criteria>

<output>
After completion, create `.paul/phases/283-arch-advisory-closure/283-01-SUMMARY.md` with changed files, validation results, module dispatch outcomes, inherited baseline notes, milestone closure status, and any deviations from this plan.
</output>
