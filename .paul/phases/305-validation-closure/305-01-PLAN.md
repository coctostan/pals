---
phase: 305-validation-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/PALS-SHARED-RUNTIME-HELPERS-CONTRACT.md
  - tests/cross-harness-validation.sh
autonomous: true
---

<objective>
## Goal
Close the v2.72 Shared Runtime Helper Extraction milestone by (1) running the full validation gate fresh against the extracted state, (2) recording the contract closure on `docs/PALS-SHARED-RUNTIME-HELPERS-CONTRACT.md` (a `## Status After Phase 305` section that aggregates the Phase 303–304 evidence per the established closure-record convention), and (3) adding the `+1` cross-harness milestone closure guardrail that pins the v2.72 shared invariant across docs/contract/lifecycle so future edits cannot silently regress it.

## Purpose
Serves the Core Value (minimal kernel, modular runtime surfaces) by sealing the v2.72 boundary set in Phase 303 and executed in Phase 304. The contract promised that Phase 305 validates the result; the milestone's durability depends on a written closure record plus a regression guardrail, mirroring the proven v2.56–v2.62 sibling-extraction closure phases (245/248/252/256/260/264/267). Executes against `docs/PALS-SHARED-RUNTIME-HELPERS-CONTRACT.md` (Phase 303, PR #223) and the Phase 304 extraction (PR #224).

## Output
- Fresh full validation gate confirmed green (Pi e2e 343/343, cross-harness, artifact consistency PASS, `git diff --check` clean) against the extracted state.
- `## Status After Phase 305` closure record on the contract aggregating Phase 303–304 evidence (PRs #223/#224, helper set, importer count, acyclic-graph result, validation counts).
- One `+1` cross-harness closure guardrail (the v2.72 shared-invariant closure block) pinning MILESTONES.md v2.72 markers — the 8th application of the closure-guardrail recipe.
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md

## Prior Work
# Phase 304 SUMMARY (.paul/phases/304-extraction-import-repoint/304-01-SUMMARY.md):
#   - shared-runtime-helpers.ts leaf created (8 symbols, Node-only); 9 importers repointed; acyclic one-way graph; zero behavior change.
#   - Validation at APPLY: Pi e2e 343/343, cross-harness 253/253, artifact consistency PASS. PR #224 squash 11c8112f.
# docs/PALS-SHARED-RUNTIME-HELPERS-CONTRACT.md:
#   - Source Authority §: "Phase 305 validates the result (build/typecheck, Pi e2e, cross-harness, artifact consistency) against this written spec."
#   - Current ## Status: Phase 303 authored; Phase 304/305 pending. This plan updates it to record closure.

## Source Files
docs/PALS-SHARED-RUNTIME-HELPERS-CONTRACT.md
tests/cross-harness-validation.sh
tests/pi-end-to-end-validation.sh   # read-only this phase; already carries the Phase 304 leaf TAP block (#252)
.paul/MILESTONES.md                  # closure-guardrail target (asserted by the new cross-harness block; not edited by a task)
</context>

<module_dispatch>
## Pre-Plan Module Dispatch

[dispatch] pre-plan advisory: modules.yaml NOT FOUND — WARNING (PALS kernel-source repo; no installed runtime registry at repo root. TODD/IRIS/DAVE/DOCS/CODI/RUBY not dispatchable.) Manual notes: docs/shell-validation-only phase — no new TS/JS source selectors → no CODI seeds (CODI skips cleanly). TODD not applicable (no code units; behavior preservation already enforced by the Phase 304 leaf TAP block + existing suites). DOCS (manual): keep the contract `## Status After Phase 305` record, the MILESTONES.md v2.72 closure markers, and the new cross-harness closure-guardrail literals mutually consistent (Phase 263 lesson: closure records under-enumerate marker drift).
[dispatch] pre-plan enforcement: modules.yaml NOT FOUND — WARNING (DEAN not dispatched; no installed registry. This phase changes one doc + one shell harness and adds NO dependencies, so dependency-audit risk is nil.)
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | All PLAN/APPLY/UNIFY lifecycle work (active) | ● loaded |
| /carl | required | Automatic session-boundary monitoring (passive Pi extension) | ● loaded |
| WALT | required | After implementation — quality/validation gate before complete | ○ (APPLY gate) |

**Note:** This is a validation + closure-record phase with **no source behavior change** — the only edits are a doc Status section and one appended cross-harness closure-guardrail block. TODD is not applicable (no code units to test-first; behavior preservation is already guaranteed by Phase 304's byte-identical relocation + the leaf TAP block). WALT runs as the APPLY post-task/post-apply validation gate. Milestone close/tag/archive is **not** part of this plan — it runs via `/paul:milestone` after this phase's UNIFY (v2.71 Phase 302 precedent).

## Skill Invocation Checklist
- [x] /paul loaded (active lifecycle)
- [x] /carl loaded (passive Pi extension)
- [ ] WALT — APPLY validation gate; confirm at APPLY
</skills>

<acceptance_criteria>

## AC-1: Full validation gate green against the extracted state
```gherkin
Given the v2.72 shared-runtime-helpers extraction is merged (Phase 304, PR #224)
When the full validation gate is run fresh from a clean working tree
Then `bash tests/pi-end-to-end-validation.sh` passes with `# Failed: 0` (total >= 343, including the Phase 304 leaf-extraction TAP block)
And `bash tests/cross-harness-validation.sh` passes with `# Failed: 0` (total = prior 253 + the one net-new v2.72 closure-guardrail assertion)
And `bash tests/helpers/artifact_consistency.sh` reports PASS
And `git diff --check` exits clean
```

## AC-2: Contract closure recorded per the established convention
```gherkin
Given the contract's ## Status section currently reads "Phase 304/305 pending"
When the contract is updated with a ## Status After Phase 305 closure record
Then docs/PALS-SHARED-RUNTIME-HELPERS-CONTRACT.md carries a "## Status After Phase 305" section stating the contract is closed for v2.72
And it cites the helper set (7 functions + PalsStateSnapshot), the new leaf basename shared-runtime-helpers.ts, the 9 repointed importers, the acyclic one-way result (no cycle; leaf imports Node only), and the validation counts (Pi e2e 343, cross-harness post-closure total)
And it references both PRs (#223 contract, #224 extraction) and notes milestone close/tag runs via /paul:milestone after UNIFY
And the pre-existing Purpose/Doctrine/Helper Inventory/Importer Map/Authority Boundaries/Leaf-Cycle Rules sections are preserved verbatim (only the Status area changes)
```

## AC-3: v2.72 closure guardrail pins the shared invariant; scope bounded
```gherkin
Given the cross-harness closure-guardrail recipe (8th application: Phases 245/248/252/256/260/264/267 → 305)
When a new "Phase 305 shared runtime helper extraction closure (v2.72 Shared Invariant)" tap_file_contains_all block is appended before the SUMMARY block in tests/cross-harness-validation.sh
Then it asserts .paul/MILESTONES.md names the v2.72 milestone, all three phases (303/304/305), the new leaf basename shared-runtime-helpers.ts, the orchestrator pals-hooks.ts, and the next-milestone candidate marker
And `bash tests/cross-harness-validation.sh` passes with the new assertion (total = 253 + 1 = 254)
And `git diff main --name-only` (non-.paul/) shows only docs/PALS-SHARED-RUNTIME-HELPERS-CONTRACT.md and tests/cross-harness-validation.sh — no source, no other harness, no installer changes
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Record the contract closure (## Status After Phase 305) and verify the full validation gate fresh</name>
  <files>docs/PALS-SHARED-RUNTIME-HELPERS-CONTRACT.md</files>
  <action>
    Record the v2.72 closure on the authoritative contract, then prove the extracted state is green.

    1. Replace the contract's current `## Status` block (the 3-line "Phase 303 authored … pending Phase 305") with a `## Status After Phase 305` closure record. Model wording/shape on the established closure-record convention (docs/PI-NATIVE-COMMAND-ROUTING-EXTRACTION-CONTRACT.md `## Status After Phase 262`). The record MUST state:
       - Contract closed for v2.72.
       - Phase 304 extracted the 7 pure/derived helpers (`compactWhitespace`, `escapeRegExp`, `selectBoundedLines`, `readFileOr`, `getFileFreshness`, `parsePalsState`, `extractLoopSignature`) + the `PalsStateSnapshot` type into the dependency leaf `drivers/pi/extensions/shared-runtime-helpers.ts`, bodies byte-for-byte identical.
       - All 9 importers repointed (pals-hooks.ts + 8 siblings); stay-put symbols (`ActivationState`, `MAX_WIDGET_MODULE_DETAILS`, `MAX_VISIBLE_MODULES`, `RECENT_MODULE_ACTIVITY_LOOKBACK`, `collectRecentAssistantTexts`) preserved on `./pals-hooks`.
       - Acyclic one-way result: orchestrator/siblings → leaf → Node only; the leaf needed NO type-only back-import (PalsStateSnapshot moved WITH the helpers), unlike S6/S7/S8.
       - Validation: Pi e2e 343/343 (incl. the new bounded leaf-extraction TAP block), cross-harness post-closure total (254/254 after this phase's guardrail), artifact consistency PASS, `git diff --check` clean.
       - PRs: #223 (contract, squash `ee5a9010`), #224 (extraction, squash `11c8112f`).
       - Milestone close, archive, and tag `v2.72` run via `/paul:milestone` after this phase's UNIFY (not in this plan).
       Keep it a single coherent `## Status After Phase 305` section. Preserve the existing `## Status` heading text only if convention prefers retitling — prefer replacing the stale 3 lines with the closure section so there is one current status record.
    - Avoid and WHY: do NOT alter the Purpose, Source Authority numbered list, Doctrine, Helper Inventory tables, Importer Map, Allowed Responsibilities, Authority Boundaries, or Leaf/Cycle-Avoidance Rules — they are the authoritative spec and must stay verbatim; only the trailing Status area changes (AC-2).

    2. Run the full validation gate fresh and confirm green (this is the AC-1 evidence; capture exact counts):
       - `bash tests/pi-end-to-end-validation.sh`  → `# Failed: 0`, total >= 343
       - `bash tests/cross-harness-validation.sh`  → `# Failed: 0` (will be 254 after Task 2; 253 if run before Task 2)
       - `bash tests/helpers/artifact_consistency.sh` → PASS
       - `git diff --check` → clean
  </action>
  <verify>grep -Fq 'Status After Phase 305' docs/PALS-SHARED-RUNTIME-HELPERS-CONTRACT.md && grep -Fq 'shared-runtime-helpers.ts' docs/PALS-SHARED-RUNTIME-HELPERS-CONTRACT.md && grep -Fq '#224' docs/PALS-SHARED-RUNTIME-HELPERS-CONTRACT.md && grep -Eq 'closed' docs/PALS-SHARED-RUNTIME-HELPERS-CONTRACT.md && bash tests/pi-end-to-end-validation.sh 2>&1 | grep -q '# Failed: 0' && bash tests/helpers/artifact_consistency.sh 2>&1 | grep -q PASS && git diff --check</verify>
  <done>AC-1 (Pi e2e + artifact consistency + diff-check green) and AC-2 (contract carries the ## Status After Phase 305 closure record with helper set, leaf, importer count, acyclic result, validation counts, both PRs, and the milestone-close-via-/paul:milestone note; spec sections preserved verbatim) satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Append the v2.72 cross-harness closure guardrail and confirm both suites green</name>
  <files>tests/cross-harness-validation.sh</files>
  <action>
    Add the `+1` milestone closure guardrail — the 8th application of the closure-guardrail recipe (Phases 245/248/252/256/260/264/267 → 305). Model it byte-for-byte on the existing Phase 264 and Phase 267 closure blocks (tests/cross-harness-validation.sh ~lines 2005–2046): a single `tap_file_contains_all` against `$REPO_ROOT/.paul/MILESTONES.md`, inserted AFTER the Phase 267 closure block and BEFORE the next CATEGORY / SUMMARY block so the TAP total self-increments by exactly 1 (253 → 254).

    The new block:
    - Comment header banner matching the existing closure blocks' style, naming "Phase 305 shared runtime helper extraction closure (v2.72 Shared Invariant)" and explaining the guardrail pins the v2.72 milestone, its three phases, the new leaf basename, the orchestrator, and the next-milestone candidate so the shared invariant across docs/contract/lifecycle does not regress.
    - `tap_file_contains_all` with description `"Phase 305 shared runtime helper extraction closure (v2.72 Shared Invariant)"`, file `"$REPO_ROOT/.paul/MILESTONES.md"`, and these literal markers (verify each is actually present in MILESTONES.md first; copy exact text):
        * the v2.72 milestone name as it appears in MILESTONES.md (e.g. 'v2.72 Shared Runtime Helper Extraction')
        * 'Phase 303'
        * 'Phase 304'
        * 'Phase 305'
        * 'shared-runtime-helpers.ts'
        * 'pals-hooks.ts'
        * the next-milestone candidate marker (e.g. 'validation-suite decomposition')
      If any marker is not yet present in MILESTONES.md (because the milestone-completion summary row is written later by /paul:milestone), choose only markers that ARE present now in the Current/Future Milestone Pointer + completed-history region, OR adjust the asserted strings to the exact text present — the block must PASS at APPLY time. Prefer markers guaranteed present after the Phase 304 transition (the pointer line already names v2.72 + Phases 303-305; ROADMAP names the candidate). Read MILESTONES.md first and reconcile.
    - Use the existing `tap_file_contains_all` helper exactly as the neighboring blocks do (same indentation, same line-continuation style). Do NOT alter the SUMMARY block, the PASS/FAIL counters, or any existing assertion.

    Then run BOTH suites and reconcile counts:
    - `bash tests/cross-harness-validation.sh` → `# Failed: 0`, total 254 (253 + 1).
    - `bash tests/pi-end-to-end-validation.sh` → `# Failed: 0`, total >= 343 (unchanged by this task).
    - `bash tests/helpers/artifact_consistency.sh` → PASS; `git diff --check` clean.
    - Avoid and WHY: do not assert a marker string that is not literally in MILESTONES.md — the TAP would fail; read the file and copy exact literals (Phase 263 lesson: closure records under-enumerate marker drift). Do not edit MILESTONES.md from this task (the markers must already exist from the Phase 304 transition / will be reinforced by /paul:milestone); if a required marker is genuinely absent, narrow the asserted set to present markers rather than editing lifecycle artifacts here.
  </action>
  <verify>grep -Fq 'Phase 305 shared runtime helper extraction closure (v2.72 Shared Invariant)' tests/cross-harness-validation.sh && bash tests/cross-harness-validation.sh 2>&1 | grep -q '# Failed: 0' && bash tests/cross-harness-validation.sh 2>&1 | grep -q '# Total:  254' && bash tests/pi-end-to-end-validation.sh 2>&1 | grep -q '# Failed: 0' && bash tests/helpers/artifact_consistency.sh 2>&1 | grep -q PASS && git diff --check</verify>
  <done>AC-3 satisfied: the v2.72 closure-guardrail block is appended (8th recipe application), cross-harness passes at 254/254, Pi e2e stays green at >=343, artifact consistency PASS, diff clean, and changed non-.paul/ files = exactly the contract + cross-harness suite.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- The contract's authoritative spec sections (Purpose, Source Authority, Doctrine, Helper Inventory, Importer Map, Allowed Responsibilities, Authority Boundaries, Leaf/Cycle-Avoidance Rules, Out of Scope) — only the trailing Status area becomes the `## Status After Phase 305` closure record.
- `drivers/pi/extensions/*.ts` — the extraction is complete and merged (Phase 304); no source changes in this phase.
- `tests/pi-end-to-end-validation.sh` — already carries the Phase 304 leaf TAP block; this phase only READS it for the validation gate (no edits expected).
- `drivers/pi/install.sh` and any installer — no install-surface changes.
- Existing cross-harness checks, TAP-count logic, and the SUMMARY block — only APPEND the one new closure block.
- `.paul/*` lifecycle artifacts except the standard PLAN/STATE/ROADMAP/SUMMARY lifecycle writes performed by the workflow. Do not hand-edit MILESTONES.md from a task.

## SCOPE LIMITS
- No milestone close, archive, or `v2.72` tag here — that runs via `/paul:milestone` after this phase's UNIFY (v2.71 Phase 302 precedent).
- No source behavior changes, no new helpers, no new authority markers.
- No new contract/template documents; no new tooling or dependencies.
- No validation-suite decomposition (separately ranked future milestone — the standing next-milestone candidate).
</boundaries>

<verification>
Before declaring plan complete:
- [ ] docs/PALS-SHARED-RUNTIME-HELPERS-CONTRACT.md carries `## Status After Phase 305` with helper set, leaf basename, 9-importer count, acyclic result, validation counts, PRs #223/#224, and the /paul:milestone note; spec sections preserved verbatim.
- [ ] tests/cross-harness-validation.sh has the appended v2.72 closure block (8th recipe application); SUMMARY/counters untouched otherwise.
- [ ] `bash tests/pi-end-to-end-validation.sh` → `# Failed: 0` (total >= 343).
- [ ] `bash tests/cross-harness-validation.sh` → `# Failed: 0` (total 254).
- [ ] `bash tests/helpers/artifact_consistency.sh` → PASS; `git diff --check` clean.
- [ ] `git diff main --name-only` (non-.paul/) = exactly docs/PALS-SHARED-RUNTIME-HELPERS-CONTRACT.md + tests/cross-harness-validation.sh.
- [ ] All acceptance criteria met.
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass (both suites green, artifact consistency PASS, diff clean)
- v2.72 contract closure recorded on the authoritative document per the established convention
- The shared invariant is regression-guarded (8th closure-guardrail recipe application)
- Scope bounded to one doc + one validation harness; milestone close deferred to /paul:milestone
</success_criteria>

<output>
After completion, create `.paul/phases/305-validation-closure/305-01-SUMMARY.md`
</output>
