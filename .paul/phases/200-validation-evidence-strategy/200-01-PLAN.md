---
phase: 200-validation-evidence-strategy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/PI-NATIVE-VALIDATION-EVIDENCE-STRATEGY.md
autonomous: true
---

<objective>
## Goal

Create a source-cited validation and evidence strategy for v2.46 that classifies existing and future checks by support purpose, defines command-output evidence requirements, and gives Phase 201 enough validation constraints to propose executable Pi-native implementation milestones safely.

## Purpose

Phase 200 is implementation planning, not implementation hardening. It must turn the Phase 199 sequencing/dependency plan into validation strategy before build milestones begin, especially for Pi-supported runtime checks, shared invariants, frozen legacy parity, runtime lens checks, guided UI safety, helper delegation checks, and GitHub Flow safety.

## Output

Create `docs/PI-NATIVE-VALIDATION-EVIDENCE-STRATEGY.md` with:
- source inputs and planning-only boundary,
- validation-class taxonomy,
- current validation-suite classification map,
- future workstream evidence requirements,
- command-output and durable artifact evidence rules,
- Phase 201 handoff notes and readiness checklist.
</objective>

<context>
## Project Context

- `.paul/PROJECT.md` — current product posture: Pi-supported runtime, artifact-first lifecycle truth, GitHub Flow safety, module evidence, parent-owned APPLY, and v2.46 implementation-planning focus.
- `.paul/STATE.md` — current lifecycle position: Phase 200 ready to plan after Phase 199 completion.
- `.paul/ROADMAP.md` — current v2.46 milestone route and Phase 200 scope.
- `pals.json` — project GitHub Flow, guided workflow, module, and planning defaults.

## Direct Source Inputs

- `docs/PI-NATIVE-SEQUENCING-DEPENDENCY-PLAN.md` — primary Phase 199 handoff for validation/evidence questions, validation-class dependencies, Phase 201 roadmap inputs, and readiness criteria.
- `.paul/phases/199-sequencing-dependency-plan/199-01-SUMMARY.md` — immediate prior phase outcome and decisions.
- `docs/PI-NATIVE-IMPLEMENTATION-WORKSTREAMS.md` — Phase 198 workstream map with validation/evidence needs per candidate workstream.
- `docs/PI-NATIVE-IMPLEMENTATION-FINDINGS.md` — Phase 197 source for validation classification and legacy cleanup cautions.
- `docs/PI-NATIVE-ARCHITECTURE-PROPOSAL.md` — v2.45 validation classes, evidence future APPLY/UNIFY must preserve, and legacy-surface guidance.
- `docs/PI-NATIVE-CONTEXT-EFFICIENCY-ARCHITECTURE.md` — validation redesign requirements, read/citation contract, and rejected designs.
- `docs/PI-RUNTIME-CAPABILITY-MAP.md` — runtime capability classifications and validation/CI surfacing notes.
- `tests/pi-end-to-end-validation.sh` — current Pi-supported runtime and shared-invariant validation script; source for classification mapping only.
- `tests/cross-harness-validation.sh` — current cross-harness parity/shared-invariant validation script; source for classification mapping only.
- `docs/PI-VALIDATION-REPORT.md` and `docs/VALIDATION-REPORT.md` — historical report examples; sources for report freshness and classification guidance only.

## Planned Source Files

- `docs/PI-NATIVE-VALIDATION-EVIDENCE-STRATEGY.md` — new Phase 200 planning artifact to be created during APPLY.
</context>

<module_dispatch>
## Pre-Plan Module Dispatch

[dispatch] pre-plan advisory: modules.yaml loaded from installed PALS registry (kernel_version 2.0.0).

| Module | Result |
|---|---|
| TODD | No repo-local unit test framework covers this new planning document; use marker greps plus existing Pi and cross-harness validation scripts. Plan remains `execute`, not TDD. |
| IRIS | Planned file does not yet exist; no TODO/FIXME/HACK/XXX findings in planned file scope. |
| DAVE | No CI config detected in repository; preserve warning, but do not broaden this planning-doc phase into CI generation. |
| DOCS | Documentation catalog includes Phase 197-199, v2.45/v2.46 source docs, validation reports, and validation scripts; planned output is a docs artifact and should cite nearby source docs/scripts. |
| CODI | Skipped — no extractable TS/JS symbols or source selectors in this markdown-only phase scope. |
| RUBY | Planned artifact is documentation; keep classification tables bounded and source-cited so validation strategy remains reviewable. |
| ARIA / LUKE / GABE / DANA / OMAR / PETE / REED / VERA | No UI, API, data, observability, performance, resilience, or privacy-sensitive implementation files are in planned scope. |
| ARCH | Repository structure is documentation/kernel/driver/module/test oriented; planned docs file is in the expected documentation layer. No architectural concerns in planned files. |

[dispatch] pre-plan enforcement: DEAN checked package-manager manifests; no root dependency manifest or lockfile found, so no audit command applies and no dependency block is raised. SETH found no planned security-sensitive source files; no security block is raised.

## Post-Plan Module Dispatch

[dispatch] post-plan: TODD reviewed plan type. No existing tests cover this docs-only planned artifact; plan remains `execute` rather than TDD.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | PLAN/APPLY/UNIFY lifecycle management | ✓ |
| /carl | required | Automatic session-boundary monitoring | automatic |
| TODD | required | APPLY test/validation discipline | module dispatch |
| WALT | required | APPLY/UNIFY quality validation | module dispatch |

Required lifecycle skills are active through the current `/skill:paul-plan` workflow and installed module dispatch.
</skills>

<acceptance_criteria>
## AC-1: Validation source inputs and planning boundary are explicit
```gherkin
Given Phase 200 is a planning phase after Phase 199 sequencing
When `docs/PI-NATIVE-VALIDATION-EVIDENCE-STRATEGY.md` is created
Then it cites relevant source inputs and states that Phase 200 does not approve runtime, workflow, validation-script, dependency, GitHub Flow, or legacy-surface changes
```

## AC-2: Current validation checks are classified by purpose
```gherkin
Given current Pi and cross-harness validation suites mix runtime checks, shared invariants, parity, and guardrails
When the validation/evidence strategy analyzes them
Then it includes a validation-class taxonomy and maps current suite sections into Pi-supported runtime, shared invariant, frozen legacy parity, runtime lens, guided UI safety, helper delegation, GitHub Flow safety, and artifact/process categories
```

## AC-3: Future implementation evidence requirements are actionable
```gherkin
Given Phase 201 must propose executable build milestones
When Phase 200 completes
Then the strategy defines command-output evidence, durable artifact evidence, per-workstream validation requirements, and readiness criteria without rewriting validation scripts or choosing final build scope
```

## AC-4: Validation remains green
```gherkin
Given this plan creates documentation and should not change runtime behavior
When APPLY completes
Then marker checks pass and the existing Pi plus cross-harness validation suites remain green
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Create the validation source map and boundary</name>
  <files>docs/PI-NATIVE-VALIDATION-EVIDENCE-STRATEGY.md</files>
  <action>
    Create `docs/PI-NATIVE-VALIDATION-EVIDENCE-STRATEGY.md` with a concise title, status table, purpose, planning-only boundary, source-input table, and validation strategy principles.
    Include at minimum these source paths: `docs/PI-NATIVE-SEQUENCING-DEPENDENCY-PLAN.md`, `docs/PI-NATIVE-IMPLEMENTATION-WORKSTREAMS.md`, `docs/PI-NATIVE-IMPLEMENTATION-FINDINGS.md`, `docs/PI-NATIVE-ARCHITECTURE-PROPOSAL.md`, `docs/PI-NATIVE-CONTEXT-EFFICIENCY-ARCHITECTURE.md`, `docs/PI-RUNTIME-CAPABILITY-MAP.md`, `tests/pi-end-to-end-validation.sh`, `tests/cross-harness-validation.sh`, `docs/PI-VALIDATION-REPORT.md`, and `docs/VALIDATION-REPORT.md`.
    State explicitly that this artifact provides validation/evidence strategy only and does not approve runtime, workflow, validation-script, dependency, GitHub Flow, or legacy-surface changes.
    Preserve `.paul/*` authority, PLAN/APPLY/UNIFY ownership, module evidence, GitHub Flow gates, parent-owned APPLY, citation/freshness/full-read fallback, command-output validation truth, and no hidden Pi state/telemetry/auto-approval.
  </action>
  <verify>grep -n "Source Inputs\|Planning-Only Boundary\|Validation Strategy Principles\|PI-NATIVE-SEQUENCING-DEPENDENCY-PLAN\|pi-end-to-end-validation\|cross-harness-validation" docs/PI-NATIVE-VALIDATION-EVIDENCE-STRATEGY.md</verify>
  <done>AC-1 satisfied: the validation/evidence artifact exists, cites source inputs, and preserves the planning-only boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Classify current validation suites and evidence classes</name>
  <files>docs/PI-NATIVE-VALIDATION-EVIDENCE-STRATEGY.md</files>
  <action>
    Add a validation-class taxonomy covering Pi-supported runtime checks, shared invariant checks, frozen legacy parity checks, runtime lens checks, guided UI safety checks, helper delegation checks, GitHub Flow safety checks, and artifact/process checks.
    Map current validation-suite sections from `tests/pi-end-to-end-validation.sh` and `tests/cross-harness-validation.sh` into those classes using the section names observed during planning.
    Include a table for stale-report handling that treats `docs/PI-VALIDATION-REPORT.md` and `docs/VALIDATION-REPORT.md` as historical examples, not current validation truth.
    Include preservation rules that prevent weakening shared invariants, deleting parity checks, or letting Pi summaries replace command output before an approved future implementation plan.
  </action>
  <verify>grep -n "Validation Class Taxonomy\|Current Suite Classification\|Pi-Supported Runtime\|Shared Invariant\|Frozen Legacy Parity\|Runtime Lens\|Guided UI Safety\|Helper Delegation\|GitHub Flow Safety\|Historical Validation Reports" docs/PI-NATIVE-VALIDATION-EVIDENCE-STRATEGY.md</verify>
  <done>AC-2 satisfied: current validation checks are classified by support purpose and evidence role.</done>
</task>

<task type="auto">
  <name>Task 3: Define future evidence requirements and Phase 201 handoff</name>
  <files>docs/PI-NATIVE-VALIDATION-EVIDENCE-STRATEGY.md</files>
  <action>
    Add future implementation evidence requirements for artifact-slice contract hardening, guided workflow evidence hardening, workflow/resource capsule design, delegated APPLY packet/report compression, Pi-native validation classification work, and legacy retention/install-surface cleanup.
    Include command-output evidence rules for APPLY/UNIFY, durable artifact evidence requirements for PLAN/SUMMARY/STATE/ROADMAP, and a Phase 201 handoff section that tells the final implementation-plan proposal which validation gates and readiness checks must be present.
    Add a validation readiness checklist for promoting a future workstream into a build milestone.
    Run marker checks plus existing Pi and cross-harness validation scripts.
  </action>
  <verify>grep -n "Future Workstream Evidence Requirements\|Command-Output Evidence\|Durable Artifact Evidence\|Phase 201 Handoff\|Validation Readiness Checklist" docs/PI-NATIVE-VALIDATION-EVIDENCE-STRATEGY.md && bash tests/pi-end-to-end-validation.sh && bash tests/cross-harness-validation.sh</verify>
  <done>AC-3 and AC-4 satisfied: Phase 201 handoffs are actionable and validation remains green.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE

- Do not edit `tests/pi-end-to-end-validation.sh` or `tests/cross-harness-validation.sh`.
- Do not edit `docs/PI-VALIDATION-REPORT.md` or `docs/VALIDATION-REPORT.md` during APPLY; treat them as historical source inputs only.
- Do not edit `drivers/pi/extensions/pals-hooks.ts`, `drivers/pi/extensions/README.md`, `drivers/pi/skill-map.md`, Pi installer behavior, shared workflows, module manifests, installed runtime copies, dependency manifests, GitHub Flow behavior, Claude Code wrappers, Agent SDK surfaces, or portability docs.
- Do not modify `.paul/STATE.md`, `.paul/ROADMAP.md`, `.paul/QUALITY-HISTORY.md`, or `.paul/CODI-HISTORY.md` during APPLY tasks except through normal UNIFY/transition workflow steps after implementation is complete.

## SCOPE LIMITS

- This plan creates a validation/evidence strategy documentation artifact only.
- Do not implement artifact-slice hardening, workflow capsules, guided UI changes, helper-agent packet changes, validation-script classification changes, validation-script rewrites, report rewrites, or legacy cleanup.
- Do not choose the final implementation milestone roadmap; Phase 201 owns final implementation-plan proposal.
- Do not add dependencies, telemetry, hidden Pi memory, persistent runtime caches, auto-approval, helper-owned APPLY, merge-gate bypass, lifecycle ledger, or CI generation.
</boundaries>

<verification>
Before declaring APPLY complete:
- [ ] `grep -n "Source Inputs\|Planning-Only Boundary\|Validation Strategy Principles\|Validation Class Taxonomy\|Current Suite Classification\|Future Workstream Evidence Requirements\|Phase 201 Handoff\|Validation Readiness Checklist" docs/PI-NATIVE-VALIDATION-EVIDENCE-STRATEGY.md`
- [ ] `grep -n "Pi-Supported Runtime\|Shared Invariant\|Frozen Legacy Parity\|Runtime Lens\|Guided UI Safety\|Helper Delegation\|GitHub Flow Safety\|Command-Output Evidence\|Durable Artifact Evidence" docs/PI-NATIVE-VALIDATION-EVIDENCE-STRATEGY.md`
- [ ] `bash tests/pi-end-to-end-validation.sh`
- [ ] `bash tests/cross-harness-validation.sh`
- [ ] All acceptance criteria are satisfied.
</verification>

<success_criteria>
- `docs/PI-NATIVE-VALIDATION-EVIDENCE-STRATEGY.md` exists and is concise, source-cited, and clearly planning-only.
- Current validation-suite sections are classified by support purpose and evidence role.
- Future implementation workstreams have actionable command-output and durable-artifact evidence requirements.
- Phase 201 has validation/evidence readiness inputs without Phase 200 prematurely choosing final build scope.
- No runtime, workflow, validation-script, dependency, legacy-surface, report, or lifecycle-authority behavior changes are made.
- Existing validation remains green.
</success_criteria>

<output>
After APPLY completion, create `.paul/phases/200-validation-evidence-strategy/200-01-SUMMARY.md` documenting changed files, acceptance-criteria results, validation output, module reports, deviations, decisions, and follow-on recommendations for Phase 201.
</output>
