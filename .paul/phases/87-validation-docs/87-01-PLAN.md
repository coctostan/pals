---
phase: 87-validation-docs
plan: 01
type: execute
wave: 1
depends_on: ["86-01"]
files_modified:
  - kernel/references/git-strategy.md
  - README.md
autonomous: true
---

<objective>
## Goal
Verify that all GitHub Flow behavioral contracts from the Phase 84 design doc are correctly implemented across the 6 modified workflow files, then update the git-strategy reference and README to document the new capabilities.

## Purpose
Phases 84–86 added GitHub Flow enforcement (preflight/postflight, merge gate, lifecycle awareness) across 6 workflows. This final phase ensures correctness and makes the new behavior discoverable through updated documentation.

## Output
- Verification report confirming all design contracts are implemented
- Updated `kernel/references/git-strategy.md` with workflow modes, new config schema, and behavioral contracts
- Updated `README.md` Configuration section with git workflow documentation
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md

## Prior Work
.paul/phases/84-research-config-schema/github-flow-config-design.md (the design doc — source of truth for contracts)
.paul/phases/84-research-config-schema/84-01-SUMMARY.md
.paul/phases/85-core-loop-enforcement/85-01-SUMMARY.md
.paul/phases/86-lifecycle-awareness/86-01-SUMMARY.md

## Source Files
kernel/workflows/apply-phase.md
kernel/workflows/unify-phase.md
kernel/workflows/transition-phase.md
kernel/workflows/init-project.md
kernel/workflows/resume-project.md
kernel/workflows/pause-work.md
kernel/references/git-strategy.md
README.md
</context>

<acceptance_criteria>

## AC-1: Design Contract Verification
```gherkin
Given the github-flow-config-design.md defines behavioral contracts for 6 workflows
When each contract is checked against the actual workflow file
Then every contract from Section 4 is present and correctly implemented, with zero missing or contradictory behavior
```

## AC-2: git-strategy.md Updated
```gherkin
Given the current git-strategy.md documents only the old 5-field config (remote, branching, auto_push, auto_pr, ci_checks)
When the reference is updated
Then it documents all 3 workflow modes (github-flow, legacy, none), the full 11-field config schema, the 3-tier resolution logic, backward compatibility, and key behavioral patterns (preflight, merge gate, lifecycle awareness)
```

## AC-3: README Configuration Section Updated
```gherkin
Given the README.md Configuration section shows only module config in pals.json
When the section is updated
Then it includes git workflow configuration with the 3 modes, key fields, and a brief explanation of GitHub Flow enforcement behavior
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Verify Design Contracts Against Implementation</name>
  <files>
    .paul/phases/84-research-config-schema/github-flow-config-design.md,
    kernel/workflows/apply-phase.md,
    kernel/workflows/unify-phase.md,
    kernel/workflows/transition-phase.md,
    kernel/workflows/init-project.md,
    kernel/workflows/resume-project.md,
    kernel/workflows/pause-work.md
  </files>
  <action>
    Read Section 4 ("Behavioral Contract Per Workflow") from github-flow-config-design.md.
    For each of the 6 workflow contracts, verify:

    1. **apply-phase.md** — `github_flow_preflight` step exists with: branch validation, behind-base check, stale-base fetch, dirty-tree check. Post-APPLY: auto_push, auto_pr via `gh pr create`, PR URL recording, CI check surfacing. Legacy/none = no-op.

    2. **unify-phase.md** — `github_flow_merge_gate` step exists with 6 sequential gates: PR exists → CI checks → reviews → merged → base sync → branch cleanup. Strict-order routing. Legacy/none = no gate.

    3. **transition-phase.md** — 3-tier workflow resolution (workflow > branching fallback > none). GitHub-flow: commit to feature branch, PR-first ordering, merge via `gh pr merge`, CI blocking, `delete_branch_on_merge`, `GIT_BASE_BRANCH` replaces hardcoded `main`. Legacy = current behavior.

    4. **init-project.md** — GitHub Flow as 3-option choice (GitHub Flow / Feature branch / Direct to main), `gh` CLI validation, 10 config fields set, 3 pals.json templates. Legacy/none defaults preserved.

    5. **resume-project.md** — `check_git_state` step with 3-tier resolution, git/PR/CI state collection, git-aware routing table, conditional Git State display. Legacy/none = no change.

    6. **pause-work.md** — Git State section in handoff template, 3-tier workflow resolution, github-flow auto-commit to feature branch, none mode skips commit.

    Write a verification report as `.paul/phases/87-validation-docs/verification-report.md` with a contract-by-contract PASS/FAIL table and any discrepancies found.
  </action>
  <verify>The verification report exists and every contract row shows PASS or has a documented discrepancy with explanation.</verify>
  <done>AC-1 satisfied: All 6 workflow contracts verified against implementation with PASS/FAIL evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Update git-strategy.md Reference</name>
  <files>kernel/references/git-strategy.md</files>
  <action>
    Rewrite the `<configuration>` section of git-strategy.md to document the new GitHub Flow system:

    1. **Workflow Modes** — Document the 3 modes (github-flow, legacy, none) with one-liner descriptions and behavioral summary for each.

    2. **Config Schema** — Replace the old 5-field table with the full schema table covering all fields: workflow, remote, base_branch, merge_method, auto_push, auto_pr, ci_checks, delete_branch_on_merge, update_branch_when_behind, require_pr_before_next_phase, require_reviews. Include type, default, and which workflows read each field.

    3. **3-Tier Resolution** — Document the config resolution logic: `workflow` field > `branching` fallback > `"none"`.

    4. **Backward Compatibility** — Note that existing `branching`-based configs are auto-detected as `workflow: "legacy"` with zero behavior change.

    5. **Key Behavioral Patterns** — Brief descriptions of: preflight (apply-phase), postflight (apply-phase), merge gate (unify-phase), lifecycle awareness (init/resume/pause).

    Keep all other sections (commit points, commit formats, example log, anti-patterns, rationale) unchanged.
    Do NOT rewrite sections that are not about configuration/workflow modes.
  </action>
  <verify>grep for "workflow" and "github-flow" and "merge_gate\|merge.gate" in git-strategy.md to confirm new content is present. Verify old `branching` field is documented as legacy fallback, not removed.</verify>
  <done>AC-2 satisfied: git-strategy.md documents all 3 workflow modes, full config schema, resolution logic, backward compat, and behavioral patterns.</done>
</task>

<task type="auto">
  <name>Task 3: Update README Configuration Section</name>
  <files>README.md</files>
  <action>
    Update the `## Configuration` section of README.md to include git workflow configuration:

    1. After the existing modules JSON block and before the integrations/preferences section, add a **Git Workflow** subsection.

    2. Show a brief pals.json example with `"git"` section using `workflow: "github-flow"` and the key fields.

    3. Document the 3 modes in a compact table:
       - `github-flow` — strict enforcement (branch validation, auto-PR, merge gate blocks next phase)
       - `legacy` — advisory git ops (current behavior, feature-per-phase branching)
       - `none` — no git operations

    4. Add one sentence: "GitHub Flow enforcement is configured during `/paul:init` or by editing `pals.json` directly. See `kernel/references/git-strategy.md` for full documentation."

    Keep the existing module config, integrations, and preferences content unchanged.
    Do NOT modify any other README section.
  </action>
  <verify>grep for "github-flow" and "Git Workflow" in README.md to confirm new content. Verify existing module config block is unchanged.</verify>
  <done>AC-3 satisfied: README Configuration section includes git workflow modes, example config, and pointer to full docs.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- kernel/workflows/*.md (all 6 workflow files are verified, not modified)
- .paul/phases/84-research-config-schema/* (design artifacts are read-only)
- .paul/phases/85-core-loop-enforcement/* (prior summaries read-only)
- .paul/phases/86-lifecycle-awareness/* (prior summaries read-only)
- pals.json (project config is not modified)

## SCOPE LIMITS
- This plan verifies and documents — no behavioral changes to any workflow
- No new config fields or workflow features
- README changes limited to Configuration section only
- git-strategy.md changes limited to configuration/workflow documentation sections

</boundaries>

<verification>
Before declaring plan complete:
- [ ] Verification report exists at `.paul/phases/87-validation-docs/verification-report.md` with all contracts checked
- [ ] `git-strategy.md` contains "github-flow", "legacy", "none" workflow mode documentation
- [ ] `git-strategy.md` config table has 11 fields (not just the old 5)
- [ ] `README.md` Configuration section mentions git workflow modes
- [ ] No workflow files were modified (read-only verification)
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All 6 design contracts verified PASS (or discrepancies documented with explanation)
- git-strategy.md fully documents the v2.12 GitHub Flow system
- README.md Configuration section is updated for discoverability
- Zero changes to workflow implementation files
</success_criteria>

<output>
After completion, create `.paul/phases/87-validation-docs/87-01-SUMMARY.md`
</output>
