---
phase: 281-brownfield-convention-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kernel/workflows/map-codebase.md
  - kernel/templates/codebase/architecture.md
  - kernel/templates/codebase/conventions.md
  - tests/pi-end-to-end-validation.sh
  - tests/cross-harness-validation.sh
autonomous: true
---

<objective>
## Goal
Teach codebase mapping to detect and preserve brownfield functional/style signals so future planning can follow local idioms instead of imposing a new functional architecture.

## Purpose
Phase 281 implements the brownfield-detection slice of `docs/PALS-FUNCTIONAL-FIRST-AGENT-GUIDANCE-CONTRACT.md`: map-codebase should notice mutation vs immutable style, pure helper/data transformation patterns, side-effect boundaries, service/controller/repository layering, error handling, dependency injection, and class/function/module/framework preferences while keeping the output descriptive and project-specific.

## Output
- `kernel/workflows/map-codebase.md` asks Explore agents to identify functional-first-relevant brownfield convention signals.
- `kernel/templates/codebase/ARCHITECTURE.md` includes a Brownfield Functional/Effect Boundaries section for local idioms and side-effect placement.
- `kernel/templates/codebase/CONVENTIONS.md` includes a Brownfield Functional Style Signals section for pure helpers, mutation/immutability, state/dependency passing, and architecture-style preference.
- Pi and cross-harness validation scripts assert the installed workflow/templates preserve the new markers.
</objective>

<context>
## Project Context
.paul/STATE.md — Phase 281 ready to plan after Phase 280 completion and merge.
.paul/ROADMAP.md — v2.65 current milestone; Phase 281 focuses on brownfield convention detection in map-codebase.
.paul/PROJECT.md — v2.65 uses contract-over-research authority and preserves project-specific conventions.

## Prior Work
.paul/phases/279-functional-first-guidance-contract/279-01-SUMMARY.md — established the v2.65 contract authority.
.paul/phases/280-agent-convention-surface/280-01-SUMMARY.md — added generated AGENTS/init Engineering Principles and marker checks.

## Source Files
- docs/PALS-FUNCTIONAL-FIRST-AGENT-GUIDANCE-CONTRACT.md — Phase 281 allowed scope and signal list.
- docs/FUNCTIONAL-FIRST-AGENT-GUIDANCE-RESEARCH.md — provenance/background examples for map-codebase.
- kernel/workflows/map-codebase.md — Explore-agent prompts and aggregation instructions.
- kernel/templates/codebase/architecture.md — ARCHITECTURE.md output template.
- kernel/templates/codebase/conventions.md — CONVENTIONS.md output template.
- tests/pi-end-to-end-validation.sh — Pi installed-resource marker checks.
- tests/cross-harness-validation.sh — cross-harness installed-resource marker checks.
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan enforcement: DEAN PASS — no dependency manifests or dependency changes planned; no audit-blocking evidence produced.
[dispatch] pre-plan advisory: TODD — no `*.test.*`/`*.spec.*` files detected; shell validation scripts are in scope for marker assertions, not TDD conversion.
[dispatch] pre-plan advisory: ARCH — instruction/template/test-shell scope only; no runtime architecture boundary changes planned.
[dispatch] pre-plan advisory: SETH — planned text/shell files do not implement secrets/auth/injection paths; inspect only for accidental secret-like literals during APPLY.
[dispatch] pre-plan advisory: IRIS — readable planned files have no new TODO/FIXME/HACK/XXX scope; review marker checks remain advisory.
[dispatch] pre-plan advisory: GABE/DANA/LUKE/ARIA — skipped; no API, data model/migration, or UI files in scope.
[dispatch] pre-plan advisory: DAVE — no CI config present; validation remains local shell-script based (`tests/pi-end-to-end-validation.sh`, `tests/cross-harness-validation.sh`).
[dispatch] pre-plan advisory: DOCS — documentation/instruction-template scope; controlling related doc is `docs/PALS-FUNCTIONAL-FIRST-AGENT-GUIDANCE-CONTRACT.md`.
[dispatch] pre-plan advisory: OMAR/REED/VERA/PETE/RUBY — no observability/resilience/privacy/performance/runtime-debt implementation paths in scope; docs/template changes should remain descriptive.
[dispatch] pre-plan advisory: CODI skipped — no explicit TS/JS selectors or stable code seeds in this markdown/shell plan.

## Post-Plan Module Dispatch
[dispatch] post-plan: TODD PASS — plan type is `execute`; no TDD candidates covering the planned files, so no RED-GREEN-REFACTOR rewrite suggested.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | PLAN/APPLY/UNIFY lifecycle management | ✓ |
| /carl | required | Session boundary monitoring | automatic |
| TODD | required | APPLY test/verification discipline | via module dispatch |
| WALT | required | APPLY quality gates and validation | via module dispatch |

**APPLY note:** Required lifecycle/module skills must remain active; no extra user-loaded specialized flow is required for this workflow/template/test-shell plan.
</skills>

<acceptance_criteria>
## AC-1: Map-Codebase Detects Functional-First-Relevant Brownfield Signals
```gherkin
Given `kernel/workflows/map-codebase.md` spawns Explore agents for architecture and conventions
When map-codebase analyzes an existing project
Then agent prompts request source-backed observations about mutation vs immutability, pure helpers/data transformations, side-effect boundaries, service/controller/repository layering, error handling, dependency injection, and class/function/module/framework preferences without prescribing a new style
```

## AC-2: Codebase Templates Preserve Local Idioms
```gherkin
Given generated `.paul/codebase/ARCHITECTURE.md` and `.paul/codebase/CONVENTIONS.md` documents
When map-codebase writes brownfield findings
Then the templates include explicit sections for local functional/effect-boundary signals, style preferences, and “preserve local idioms” guidance rather than requiring functional architecture adoption
```

## AC-3: Validation Guards Installed Brownfield Detection Markers
```gherkin
Given Pi and cross-harness validation scripts install or inspect PALS runtime resources
When validation runs after APPLY
Then both scripts assert the installed map-codebase workflow and codebase templates contain the brownfield functional/style signal markers
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Add functional-style signal detection to map-codebase prompts</name>
  <files>kernel/workflows/map-codebase.md</files>
  <action>
    Update the Architecture + Structure and Conventions + Testing Explore prompts to include a bounded "Brownfield functional-first signals" / "Local idioms to preserve" subsection.
    Require source-backed findings with file paths for: mutation-heavy vs immutable style, pure helper/data transformation patterns, side effects near boundaries, service/repository/controller layering, error handling style, dependency/state passing style, and whether the repo prefers classes, functions, modules, hooks, or framework-specific patterns.
    Update collection/write instructions so ARCHITECTURE.md and CONVENTIONS.md receive these findings.
    Avoid: telling agents to recommend functional rewrites, requiring adoption of pure functions, changing generated AGENTS/init text, or touching TODD/RUBY/ARCH guidance.
  </action>
  <verify>Native grep/read checks show `Brownfield functional-first signals`, `mutation-heavy vs immutable style`, `pure helper`, `side effects`, and `preserve local idioms` markers in `kernel/workflows/map-codebase.md`.</verify>
  <done>AC-1 satisfied: map-codebase explicitly detects local functional/style signals without imposing style.</done>
</task>

<task type="auto">
  <name>Task 2: Add brownfield signal sections to codebase templates</name>
  <files>kernel/templates/codebase/architecture.md, kernel/templates/codebase/conventions.md</files>
  <action>
    Add a compact `## Brownfield Functional/Effect Boundaries` section to the ARCHITECTURE.md file template and good example.
    Add a compact `## Brownfield Functional Style Signals` section to the CONVENTIONS.md file template and good example.
    Sections must capture descriptive findings: local idioms to preserve, mutation/immutability style, pure helper/data transformation patterns, side-effect boundary placement, error/dependency/state-passing style, and class/function/module/framework preference.
    Include language that generated guidance should preserve local idioms and should not require converting the repo to a functional architecture.
    Avoid: expanding templates broadly, adding new generated documents, or editing `.paul/codebase/` generated outputs.
  </action>
  <verify>Native grep/read checks show the new brownfield section headings and core signal markers in both template files.</verify>
  <done>AC-2 satisfied: generated codebase documentation has explicit slots for local functional/style idioms.</done>
</task>

<task type="auto">
  <name>Task 3: Add installed-resource validation markers and run verification</name>
  <files>tests/pi-end-to-end-validation.sh, tests/cross-harness-validation.sh</files>
  <action>
    Add TAP marker assertions near existing installed workflow/template resource checks.
    Pi validation should assert installed `workflows/map-codebase.md`, `templates/codebase/architecture.md`, and `templates/codebase/conventions.md` contain the new brownfield functional/style signal markers.
    Cross-harness validation should assert both Claude Code and Pi installed resources contain the same markers.
    Run focused marker checks, shell syntax checks, and the two validation suites.
    Avoid: weakening existing counts/checks, adding external dependencies, or treating inherited CODI/artifact-consistency baseline failures as Phase 281 regressions when the new markers pass.
  </action>
  <verify>`bash -n tests/pi-end-to-end-validation.sh tests/cross-harness-validation.sh`; `bash tests/pi-end-to-end-validation.sh`; `bash tests/cross-harness-validation.sh`; focused marker checks for all planned files.</verify>
  <done>AC-3 satisfied and verification evidence is ready for `281-01-SUMMARY.md`.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- docs/PALS-FUNCTIONAL-FIRST-AGENT-GUIDANCE-CONTRACT.md — authoritative contract from Phase 279; cite it but do not rewrite it.
- kernel/templates/AGENTS.md and generated init AGENTS text — Phase 280 scope already complete unless a validation-only reference is needed.
- modules/todd/* and modules/ruby/* — Phase 282 scope.
- modules/arch/* — Phase 283 optional advisory/closure scope.
- pals.json dependency/module configuration — no new module or gate for functional style.

## SCOPE LIMITS
- No new dependencies, linters, formatters, CI config, generated `.paul/codebase/` outputs, or runtime tooling.
- Detection must be descriptive and source-backed; do not require brownfield repos to adopt a new architecture.
- Keep changes to map-codebase workflow, codebase templates, and installed-resource marker validation.
- Validation changes are limited to marker-preservation checks for this phase’s installed resources.
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `kernel/workflows/map-codebase.md` contains brownfield functional/style signal instructions.
- [ ] `kernel/templates/codebase/architecture.md` contains `## Brownfield Functional/Effect Boundaries` and core markers.
- [ ] `kernel/templates/codebase/conventions.md` contains `## Brownfield Functional Style Signals` and core markers.
- [ ] `tests/pi-end-to-end-validation.sh` contains Pi installed-resource marker assertions for workflow/templates.
- [ ] `tests/cross-harness-validation.sh` contains cross-harness marker assertions for both installed drivers.
- [ ] `bash -n tests/pi-end-to-end-validation.sh tests/cross-harness-validation.sh` result captured.
- [ ] `bash tests/pi-end-to-end-validation.sh` result captured.
- [ ] `bash tests/cross-harness-validation.sh` result captured.
- [ ] All acceptance criteria met.
</verification>

<success_criteria>
- Map-codebase detects functional-first-relevant brownfield signals and records local idioms.
- Generated ARCHITECTURE/CONVENTIONS templates provide explicit slots for those signals.
- No guidance imposes a functional architecture or overrides project conventions.
- Installed-resource validation guards the workflow/template markers.
- Phase 282/283 module surfaces remain untouched.
</success_criteria>

<output>
After completion, create `.paul/phases/281-brownfield-convention-detection/281-01-SUMMARY.md` with changed files, validation results, module dispatch outcomes, and any deviations from this plan.
</output>
