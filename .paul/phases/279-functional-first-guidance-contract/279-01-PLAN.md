---
phase: 279-functional-first-guidance-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/PALS-FUNCTIONAL-FIRST-AGENT-GUIDANCE-CONTRACT.md
  - docs/FUNCTIONAL-FIRST-AGENT-GUIDANCE-RESEARCH.md
autonomous: true
---

<objective>
## Goal
Create a bounded implementation contract for v2.65 Functional-First Agent Guidance that converts the research note into durable scope, doctrine, non-goals, affected surfaces, validation expectations, and phase handoff guidance.

## Purpose
Phase 279 must prevent the rest of v2.65 from becoming broad or dogmatic. A contract gives later phases an authoritative boundary: functional-first, not functional-only; project conventions remain authoritative; existing PALS surfaces should be extended before considering new modules or blocking gates.

## Output
- New contract document: `docs/PALS-FUNCTIONAL-FIRST-AGENT-GUIDANCE-CONTRACT.md`
- Contract references the source research note, defines contract-vs-research authority, and maps later milestone phases to allowed scope.
</objective>

<context>
## Project Context
.paul/STATE.md — Current Position, Loop Position, and Session Continuity windows confirm Phase 279 is ready to plan.
.paul/ROADMAP.md — v2.65 current milestone and Phase 279 detail define the phase scope.

## Source Brief
`docs/FUNCTIONAL-FIRST-AGENT-GUIDANCE-RESEARCH.md` — source research and design thinking for this contract.

## Authority Hierarchy
After Phase 279 UNIFY, `docs/PALS-FUNCTIONAL-FIRST-AGENT-GUIDANCE-CONTRACT.md` is the authoritative v2.65 implementation boundary. `docs/FUNCTIONAL-FIRST-AGENT-GUIDANCE-RESEARCH.md` remains background/provenance; if the two conflict, the contract wins.

## Current Phase
Phase 279: Functional-First Guidance Contract
Focus: Convert the research note into a bounded implementation contract covering doctrine, non-goals, affected surfaces, and validation expectations.
</context>

<module_dispatch>
## Pre-Plan Module Dispatch

[dispatch] pre-plan advisory: files_modified=`docs/PALS-FUNCTIONAL-FIRST-AGENT-GUIDANCE-CONTRACT.md`, `docs/FUNCTIONAL-FIRST-AGENT-GUIDANCE-RESEARCH.md`; docs-only contract/provenance scope; CODI seeds=none because no explicit TS/JS selectors.
[dispatch] pre-plan advisory: TODD skipped — no test files found for docs-only scope; plan type remains `execute`.
[dispatch] pre-plan advisory: IRIS checked source brief for TODO/FIXME/HACK/XXX markers — none found.
[dispatch] pre-plan advisory: DAVE WARN — no CI config found in bounded discovery; no CI generation included because this plan is docs-only and CI work is out of scope.
[dispatch] pre-plan advisory: DOCS documentation-only scope — drift check not applicable; new contract is itself documentation output.
[dispatch] pre-plan advisory: CODI skipped-no-symbols — markdown-only plan with no bounded TS/JS identifiers.
[dispatch] pre-plan advisory: RUBY skipped — documentation-only scope is not code debt.
[dispatch] pre-plan advisory: ARCH/SETH/OMAR/PETE/REED/ARIA/LUKE/GABE/DANA skipped — no in-scope source, UI, API, data, observability, resilience, privacy, performance, security, or deploy files.
[dispatch] pre-plan enforcement: DEAN WARN/PASS — `drivers/agent-sdk/requirements.txt` exists, but `pip-audit` is unavailable; no parseable critical/high counts produced and no dependency files are modified by this plan, so no planning block is raised.

## Post-Plan Module Dispatch

[dispatch] post-plan: TODD skipped — docs-only `execute` plan; no RED/GREEN/REFACTOR restructuring needed.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | PLAN/APPLY/UNIFY lifecycle work | ✓ paul-plan loaded |
| /carl | required | Session boundary monitoring | automatic |
| TODD | required | Before/during code implementation | n/a docs-only execute plan |
| WALT | required | After implementation validation | to run during APPLY/UNIFY validation |

</skills>

<acceptance_criteria>
## AC-1: Contract Captures Doctrine and Boundaries
```gherkin
Given the Phase 279 source brief exists at docs/FUNCTIONAL-FIRST-AGENT-GUIDANCE-RESEARCH.md
When the contract document is created
Then it states the preferred doctrine, explains “functional-first, not functional-only,” and records non-goals that prevent mandatory FP, OOP-to-FP rewrites, new-module-by-default scope, default blocking gates, or convention override.
```

## AC-2: Contract Maps v2.65 Surfaces to Later Phases
```gherkin
Given v2.65 includes phases 280 through 283
When the contract document is reviewed
Then it maps allowed work for AGENTS/init, map-codebase, TODD, RUBY, and optional ARCH advisory surfaces without assigning implementation details to Phase 279.
```

## AC-3: Contract Defines Validation and Handoff Expectations
```gherkin
Given later phases will mutate workflow/module guidance
When Phase 279 completes
Then the contract identifies validation expectations, source authority, and handoff notes that later PLANs can cite without re-reading the full research discussion.
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Create functional-first guidance contract</name>
  <files>docs/PALS-FUNCTIONAL-FIRST-AGENT-GUIDANCE-CONTRACT.md, docs/FUNCTIONAL-FIRST-AGENT-GUIDANCE-RESEARCH.md</files>
  <action>
    Create a new contract document from `docs/FUNCTIONAL-FIRST-AGENT-GUIDANCE-RESEARCH.md`.
    Include these sections:
    - Purpose and source authority.
    - Doctrine: “Functional-first, not functional-only” and “Pure Core, Explicit Effects.”
    - Practical principles: pure logic, immutable-by-default, explicit inputs/outputs, functional core/imperative shell, readability over clever FP style, explicit state modeling.
    - PALS integration rules: use existing AGENTS/init, map-codebase, TODD, RUBY, and optional ARCH surfaces before considering a new module.
    - Non-goals and hard boundaries.
    - Phase map for 280–283 with allowed scope and deferred decisions.
    - Authority hierarchy: contract is authoritative after Phase 279 UNIFY; research note remains provenance/background; contract wins if they conflict.
    Avoid turning the contract into implementation patches for later phases; Phase 279 creates the boundary, not the downstream edits.
  </action>
  <verify>Read `docs/PALS-FUNCTIONAL-FIRST-AGENT-GUIDANCE-CONTRACT.md` and confirm it contains headings for Doctrine, Integration Rules, Non-Goals, Phase Scope Map, and Validation Expectations.</verify>
  <done>AC-1 and AC-2 satisfied: the contract states the doctrine/non-goals and maps later v2.65 surfaces without implementing them.</done>
</task>

<task type="auto">
  <name>Task 2: Validate contract usability and lifecycle fit</name>
  <files>docs/PALS-FUNCTIONAL-FIRST-AGENT-GUIDANCE-CONTRACT.md, docs/FUNCTIONAL-FIRST-AGENT-GUIDANCE-RESEARCH.md</files>
  <action>
    Review the new contract for PLAN/APPLY usability:
    - Later phases must be able to cite it as authoritative scope.
    - The contract must distinguish required rules from advisory guidance.
    - The contract must state that it supersedes the research note for v2.65 implementation after Phase 279 UNIFY, while the research note remains provenance/background.
    - The ARCH side-effect-boundary work must remain optional/deferred until Phase 283 decides it.
    - Validation expectations must mention artifact consistency, `git diff --check`, and relevant Pi/cross-harness checks for downstream phases.
    - Keep wording non-dogmatic and compatible with brownfield projects that prefer OOP/class-based styles.
    Make only contract-document edits needed to satisfy those checks.
  </action>
  <verify>Run `git diff --check` and `bash tests/helpers/artifact_consistency.sh`; additionally inspect the contract headings/phase map by reading the file.</verify>
  <done>AC-3 satisfied: validation and handoff expectations are explicit, downstream phases have bounded scope, and repository validation is clean.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- Do not edit `kernel/templates/AGENTS.md` in Phase 279.
- Do not edit `kernel/workflows/init-project.md` in Phase 279.
- Do not edit `kernel/workflows/map-codebase.md` in Phase 279.
- Do not edit TODD, RUBY, ARCH, or other module files in Phase 279.
- Do not add a new PALS module/pal.
- Do not change validation scripts except if artifact consistency unexpectedly requires a lifecycle-only repair, and stop for user approval first.

## SCOPE LIMITS
- Phase 279 is documentation/contract creation only.
- Functional programming guidance must remain practical and non-mandatory.
- Project-specific conventions remain authoritative over default guidance.
- No new dependency, CI, package, runtime, or install-surface changes.
- Do not materially rewrite the research note during APPLY; it is included in scope as a preserved source artifact, with only typo/link fixes allowed if needed.
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `docs/PALS-FUNCTIONAL-FIRST-AGENT-GUIDANCE-CONTRACT.md` exists.
- [ ] Contract includes Doctrine, Integration Rules, Non-Goals, Phase Scope Map, and Validation Expectations sections.
- [ ] Contract includes an authority hierarchy that makes the contract authoritative over the research note after Phase 279 UNIFY.
- [ ] `git diff --check` passes.
- [ ] `bash tests/helpers/artifact_consistency.sh` passes.
- [ ] All acceptance criteria are met.
</verification>

<success_criteria>
- Phase 279 creates exactly one new contract document as the intended new APPLY output and preserves the research note as in-scope provenance.
- The contract can guide phases 280–283 without reopening the research discussion.
- No downstream implementation surfaces are mutated in this phase.
- No mandatory FP, OOP rewrite, default blocking gate, or new-module requirement is introduced.
</success_criteria>

<output>
After completion, create `.paul/phases/279-functional-first-guidance-contract/279-01-SUMMARY.md` with:
- Contract path and short summary.
- Any deviations from this plan.
- Validation command results.
- Module execution reports for WALT/CODI/RUBY/SKIP or applicable modules.
- Next-phase recommendation for Phase 280.
</output>
