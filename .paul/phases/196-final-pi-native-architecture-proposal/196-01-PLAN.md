---
phase: 196-final-pi-native-architecture-proposal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/PI-NATIVE-ARCHITECTURE-PROPOSAL.md
autonomous: true
---

<objective>
## Goal

Produce the final v2.45 Pi-native architecture proposal that synthesizes Phases 192-195 into one actionable design, migration plan, implementation roadmap, explicit non-goals, and follow-on milestone recommendation set.

## Purpose

v2.45 has established Pi as the supported runtime, mapped Pi runtime capabilities, designed context-efficiency guardrails, and validated the first artifact-slice spike. This plan closes the milestone by turning those findings into a durable proposal that can guide implementation work without moving lifecycle authority out of `.paul/*` artifacts.

## Output

- Create `docs/PI-NATIVE-ARCHITECTURE-PROPOSAL.md`.
- The proposal must include:
  - source inputs and decision summary,
  - final target architecture and authority boundaries,
  - promoted/rejected Phase 195 spike recommendations,
  - migration and implementation roadmap,
  - validation strategy for Pi-supported runtime plus shared invariants,
  - explicit non-goals and frozen legacy retention guidance,
  - recommended follow-on milestones.
</objective>

<context>
## Project Context

- `.paul/PROJECT.md` — Pi is the supported runtime; `.paul/*` artifacts remain lifecycle truth; PLAN/APPLY/UNIFY, module evidence, GitHub Flow, and parent-owned APPLY remain current guarantees.
- `.paul/ROADMAP.md` — Phase 196 scope is the final Pi-native architecture proposal, migration plan, implementation roadmap, explicit non-goals, and follow-on recommendations.
- `.paul/STATE.md` — Phase 196 is ready to plan after Phase 195 completed and PR #109 merged.
- `pals.json` — GitHub Flow is enabled; planning default collaboration is `high`; REV PR review is disabled.

## Planning Posture

- Collaboration level: `high` from `pals.json` project default.
- Planning mode: `direct-requirements` because Phase 196 has a specific roadmap output, with high-collaboration synthesis of assumptions, alternatives, and risks.
- Review path: normal 4-option plan review menu after PLAN creation.

## Prior Work Inputs

- `docs/PI-NATIVE-SUPPORT-INVENTORY.md` — Phase 192 support-tier source: Pi active, Claude Code / Agent SDK frozen legacy, artifact authority preserved.
- `docs/PI-RUNTIME-CAPABILITY-MAP.md` — Phase 193 capability inventory and lifecycle mapping for Pi primitives.
- `docs/PI-NATIVE-CONTEXT-EFFICIENCY-ARCHITECTURE.md` — Phase 194 context-efficiency architecture, read/citation contract, validation redesign requirements, and Phase 196 open decisions.
- `.paul/phases/195-exploratory-pi-native-spikes/195-01-SUMMARY.md` — Phase 195 artifact-slice spike results and validated pattern candidate.

## Source Files

- `docs/PI-NATIVE-ARCHITECTURE-PROPOSAL.md` — new final proposal output.

## CODI Seed Candidates

No code symbols are in scope. This is a markdown-only architecture proposal; CODI should skip cleanly.
</context>

<module_dispatch>
## Pre-Plan Module Dispatch

[dispatch] pre-plan advisory: modules.yaml loaded from installed PALS registry (kernel_version 2.0.0); executed enabled advisory modules TODD, IRIS, DAVE, DOCS, CODI, and RUBY.

### TODD

| Framework | Test Files | Status |
|---|---:|---|
| shell validation | `tests/pi-end-to-end-validation.sh`, `tests/cross-harness-validation.sh` | Primary validation path for this documentation/architecture phase |
| JS test framework | 0 project test framework markers | No TDD conversion recommended for this docs-only proposal |

TODD note: repository-local validation is shell/TAP based; no RED/GREEN/REFACTOR task structure is needed for a single architecture proposal document.

### IRIS

| File | Pattern | Count |
|---|---:|---:|
| `docs/PI-NATIVE-ARCHITECTURE-PROPOSAL.md` | TODO/FIXME/HACK/XXX | 0 (new file; no existing anti-pattern signatures) |

IRIS: No anti-pattern signatures in planned files.

### DAVE

| File | Platform | Status |
|---|---|---|
| none detected | CI/CD config | WARN: no repository CI config found; use local validation commands in this plan |

DAVE warning: No CI detected. Add `.github/workflows/ci.yml` in a future infrastructure plan if desired; this proposal must not broaden into CI generation.

### DOCS

| Planned File | Related Docs | Status |
|---|---|---|
| `docs/PI-NATIVE-ARCHITECTURE-PROPOSAL.md` | `docs/PI-NATIVE-SUPPORT-INVENTORY.md`, `docs/PI-RUNTIME-CAPABILITY-MAP.md`, `docs/PI-NATIVE-CONTEXT-EFFICIENCY-ARCHITECTURE.md`, `.paul/phases/195-exploratory-pi-native-spikes/195-01-SUMMARY.md` | New docs output with clear source inputs |

DOCS: 19 repository docs detected under `docs/`; planned file is itself a documentation artifact and references its source documents.

### CODI

[dispatch] CODI: skipped — no extractable symbols in phase scope

### RUBY

RUBY: No preliminary code debt indicators in planned files. The plan modifies one markdown proposal and should not touch known large-file debt in the Pi extension or validation scripts.

## Pre-Plan Enforcement Dispatch

[dispatch] pre-plan enforcement: DEAN PASS — no dependency manifest or lockfile detected in the repository root, so no package audit command applies and no critical/high vulnerability blocker exists for this plan.

## Post-Plan Module Dispatch

[dispatch] post-plan: TODD reviewed plan type `execute`; no project test framework candidates cover the planned markdown-only file, so no TDD conversion is recommended.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | All PLAN/APPLY/UNIFY lifecycle work | ✓ |
| /carl | required | Automatic session-boundary monitoring | automatic via Pi extension |
| TODD | required | Before and during implementation | via installed module registry |
| WALT | required | After implementation validation | via installed module registry |

## Skill Invocation Checklist

- [x] `/skill:paul-plan` loaded for this PLAN.
- [x] CARL remains automatic in Pi extension hooks.
- [x] TODD pre-plan dispatch recorded above.
- [ ] WALT will run during APPLY/UNIFY validation.
</skills>

<acceptance_criteria>

## AC-1: Final architecture proposal synthesizes v2.45 evidence

```gherkin
Given Phases 192-195 produced support-tier, capability-map, context-efficiency, and artifact-slice evidence
When `docs/PI-NATIVE-ARCHITECTURE-PROPOSAL.md` is created
Then it cites those source inputs and summarizes the final Pi-native architecture direction without reopening settled artifact-authority decisions.
```

## AC-2: Authority boundaries and non-goals are explicit

```gherkin
Given Pi runtime assistance can reduce context cost but must not become lifecycle authority
When the final proposal describes target architecture, migration, and follow-on work
Then it explicitly preserves `.paul/*` truth, PLAN/APPLY/UNIFY, module evidence, GitHub Flow gates, parent-owned APPLY, full-read fallback, and no hidden Pi state or telemetry.
```

## AC-3: Implementation roadmap and validation strategy are actionable

```gherkin
Given v2.45 must route future work after architecture closure
When the proposal defines migration and implementation sequencing
Then it recommends concrete follow-on milestones, retention/removal classifications for frozen legacy surfaces, and validation categories for Pi-supported runtime plus shared invariants.
```

## AC-4: Existing validation remains green

```gherkin
Given this plan is a documentation/architecture proposal
When local Pi and cross-harness validation run
Then both validation suites pass without code, workflow, driver, dependency, or GitHub Flow behavior changes.
```
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Create the final Pi-native architecture proposal skeleton and synthesis</name>
  <files>docs/PI-NATIVE-ARCHITECTURE-PROPOSAL.md</files>
  <action>
    Create `docs/PI-NATIVE-ARCHITECTURE-PROPOSAL.md` with a clear final-proposal structure:
    - title and status block for v2.45 final proposal,
    - source inputs table citing Phase 192-195 artifacts,
    - executive decision summary,
    - final target architecture model showing authority flow from `.paul/*` artifacts and workflow evidence up to Pi presentation/context surfaces,
    - concise synthesis of what Phases 192, 193, 194, and 195 proved.

    Preserve the core value framing: minimal kernel, modular pals, universal drivers. Do not imply Pi runtime state, widgets, context injection, helper agents, or UI surfaces own lifecycle truth.
  </action>
  <verify>grep -n "Source Inputs\|Executive Decision Summary\|Target Architecture\|Phase 195" docs/PI-NATIVE-ARCHITECTURE-PROPOSAL.md</verify>
  <done>AC-1 and AC-2 satisfied: proposal exists, cites source inputs, and states the final architecture direction with artifact authority intact.</done>
</task>

<task type="auto">
  <name>Task 2: Add migration roadmap, recommendations, and explicit non-goals</name>
  <files>docs/PI-NATIVE-ARCHITECTURE-PROPOSAL.md</files>
  <action>
    Expand the proposal with actionable future-routing sections:
    - promoted Phase 195 pattern(s), especially activation-gated artifact-slice loading,
    - deferred or rejected options such as hidden Pi state, telemetry-owned context, auto-approval, merge-gate bypasses, helper-owned APPLY, and broad workflow-summary replacement,
    - migration sequence from current Pi adapter behavior to follow-on implementation milestones,
    - retention/removal guidance for Claude Code wrappers, Agent SDK surfaces, portability docs, and cross-harness validation,
    - implementation roadmap with recommended milestone names or themes.

    Keep recommendations concrete enough that a future `/paul:milestone` or `/paul:plan` can use them directly. Avoid editing actual legacy surfaces, workflows, validation scripts, drivers, or installers in this plan.
  </action>
  <verify>grep -n "Promoted Patterns\|Migration Roadmap\|Implementation Roadmap\|Non-Goals\|Legacy Surface" docs/PI-NATIVE-ARCHITECTURE-PROPOSAL.md</verify>
  <done>AC-2 and AC-3 satisfied: proposal provides explicit non-goals, legacy-surface guidance, and concrete follow-on implementation routing.</done>
</task>

<task type="auto">
  <name>Task 3: Add validation strategy and verify the proposal</name>
  <files>docs/PI-NATIVE-ARCHITECTURE-PROPOSAL.md</files>
  <action>
    Finalize the proposal with validation and readiness sections:
    - define validation classes: Pi-supported runtime checks, shared invariant checks, frozen legacy parity checks, runtime lens checks, guided UI safety checks, helper-agent delegation checks,
    - state what evidence future APPLY/UNIFY must preserve in PLAN/SUMMARY/STATE,
    - add a final readiness checklist for moving from architecture to implementation.

    Then run validation commands. This task must not change `tests/pi-end-to-end-validation.sh`, `tests/cross-harness-validation.sh`, `drivers/**`, `kernel/**`, `modules/**`, dependency manifests, GitHub Flow behavior, or `.paul/*` lifecycle artifacts outside normal APPLY/UNIFY writes.
  </action>
  <verify>grep -n "Validation Strategy\|Pi-supported runtime\|Shared invariant\|Frozen legacy\|Readiness Checklist" docs/PI-NATIVE-ARCHITECTURE-PROPOSAL.md && bash tests/pi-end-to-end-validation.sh && bash tests/cross-harness-validation.sh</verify>
  <done>AC-3 and AC-4 satisfied: proposal includes validation strategy/readiness checks and both validation suites pass.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE

- Do not edit `drivers/pi/extensions/pals-hooks.ts`, `drivers/pi/extensions/README.md`, `drivers/pi/skill-map.md`, or Pi installer behavior in this plan.
- Do not edit `tests/pi-end-to-end-validation.sh` or `tests/cross-harness-validation.sh` in this plan.
- Do not edit shared workflow semantics in `kernel/workflows/**`, module manifests in `modules/**`, or installed runtime copies under `~/.pi/**`.
- Do not delete, archive, or move Claude Code, Agent SDK, portability, or cross-harness files during this plan.
- Do not add dependencies, package manifests, telemetry, persistent Pi memory, hidden state, or a new lifecycle ledger.
- Do not change GitHub Flow behavior, branch/PR/CI policy, merge gates, or PR automation.

## SCOPE LIMITS

- This plan produces the final architecture proposal only; implementation milestones come later.
- It may recommend future retention/removal actions but must not perform them.
- It may recommend validation redesign but must not rewrite validation scripts.
- It may promote the Phase 195 artifact-slice pattern but must not add more context-loading code.
- Full authoritative reads remain mandatory for edits, lifecycle decisions, ambiguous/stale/contested facts, GitHub Flow gates, and approved PLAN execution.
</boundaries>

<verification>
Before declaring APPLY complete:
- [ ] `grep -n "Source Inputs\|Executive Decision Summary\|Target Architecture\|Phase 195" docs/PI-NATIVE-ARCHITECTURE-PROPOSAL.md`
- [ ] `grep -n "Promoted Patterns\|Migration Roadmap\|Implementation Roadmap\|Non-Goals\|Legacy Surface" docs/PI-NATIVE-ARCHITECTURE-PROPOSAL.md`
- [ ] `grep -n "Validation Strategy\|Pi-supported runtime\|Shared invariant\|Frozen legacy\|Readiness Checklist" docs/PI-NATIVE-ARCHITECTURE-PROPOSAL.md`
- [ ] `bash tests/pi-end-to-end-validation.sh`
- [ ] `bash tests/cross-harness-validation.sh`
- [ ] All acceptance criteria met.
</verification>

<success_criteria>
- `docs/PI-NATIVE-ARCHITECTURE-PROPOSAL.md` exists and is usable as the final v2.45 proposal.
- Proposal synthesizes Phases 192-195 and preserves all artifact-first lifecycle guardrails.
- Proposal includes concrete migration, implementation-roadmap, validation, legacy-surface, non-goal, and follow-on milestone recommendations.
- No source code, workflow semantics, validation scripts, dependency manifests, GitHub Flow behavior, or installed runtime copies are changed.
- Pi and cross-harness validation pass.
</success_criteria>

<output>
After completion, create `.paul/phases/196-final-pi-native-architecture-proposal/196-01-SUMMARY.md` with:
- proposal contents and key decisions,
- validation command output,
- module execution reports for APPLY/UNIFY,
- any deviations from the no-code/no-workflow-change boundary,
- final recommendation for completing v2.45 and opening follow-on implementation milestones.
</output>
