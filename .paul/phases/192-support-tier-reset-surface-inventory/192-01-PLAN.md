---
phase: 192-support-tier-reset-surface-inventory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/HARNESS-PORTABILITY.md
  - docs/PORTABILITY-ROADMAP.md
  - docs/PI-NATIVE-SUPPORT-INVENTORY.md
autonomous: true
---

<objective>
## Goal
Declare Claude Code support unsupported/frozen for the active product direction and produce a bounded inventory of cross-harness assumptions, wrappers, prompts, docs, and validation expectations that future Pi-native architecture phases can use.

## Purpose
v2.45 shifts PALS toward Pi-native runtime assistance. Before mapping Pi extension capabilities or designing context-efficiency changes, the project needs an explicit support-tier reset and a concrete list of surfaces that currently imply Claude Code or cross-harness parity.

## Output
- `docs/PI-NATIVE-SUPPORT-INVENTORY.md` created with the support-tier decision, inventory categories, classification, and follow-up implications.
- `README.md` updated so public positioning no longer claims active Claude Code / Agent SDK parity.
- `docs/HARNESS-PORTABILITY.md` and `docs/PORTABILITY-ROADMAP.md` updated to mark portability material as historical/frozen reference rather than active product direction.
</objective>

<context>
## Project Context
- `.paul/PROJECT.md` — v2.45 is active; `.paul/*` remains lifecycle truth; Claude Code compatibility is unsupported/frozen.
- `.paul/ROADMAP.md` — Phase 192 scope: support-tier reset + surface inventory.
- `.paul/STATE.md` — current loop is ready for first PLAN.
- `pals.json` — project default collaboration is `high`; planning mode carried from discussion is `direct-requirements`.

## Source / Documentation Files
- `README.md` — currently advertises Claude Code, Pi, and Agent SDK support; contains install and architecture claims that need support-tier reset.
- `docs/HARNESS-PORTABILITY.md` — currently describes cross-harness portability as an active goal.
- `docs/PORTABILITY-ROADMAP.md` — currently describes a portability roadmap as active work.
- `docs/PI-NATIVE-SUPPORT-INVENTORY.md` — new inventory artifact for this phase.

## Inventory Targets to Inspect During APPLY
- `kernel/commands/paul/*.md` — Claude-style command wrappers and allowed-tool assumptions.
- `drivers/pi/**` — active Pi surfaces and skills.
- `drivers/agent-sdk/**` — Agent SDK support surface to classify as unsupported/frozen unless explicitly retained later.
- `tests/pi-end-to-end-validation.sh` and `tests/cross-harness-validation.sh` — validation expectations to classify for future redesign.
- Existing docs mentioning Claude, cross-harness portability, harness portability, Agent SDK, or universal drivers.
</context>

<module_dispatch>
## Pre-Plan Module Dispatch

[dispatch] pre-plan advisory: modules.yaml loaded from installed PALS registry (kernel_version 2.0.0); required advisory cohort evaluated for planned documentation files.

- TODD: No `*.test.*`, `*.spec.*`, `__tests__`, or package test framework detected for this docs/inventory plan; no TDD conversion candidate.
- IRIS: No TODO/FIXME/HACK/XXX markers found in `README.md`, `docs/HARNESS-PORTABILITY.md`, or `docs/PORTABILITY-ROADMAP.md`.
- DAVE: No CI config detected (`.github/workflows`, `.gitlab-ci.yml`, Dockerfile, docker-compose, Jenkinsfile absent). Advisory only; CI generation is out of scope for Phase 192.
- DOCS: Planned changes are documentation-first and all modified files are documentation files; no nearby-doc drift risk for source files.
- CODI: skipped — no extractable symbols in phase scope; planned files are markdown documentation and no bounded TS/JS source selectors are in scope.
- RUBY: No preliminary code debt indicators in planned files; documentation-only plan.

[dispatch] pre-plan enforcement: DEAN PASS/WARN — no dependency manifest is in `files_modified`; existing `drivers/agent-sdk/requirements.txt` is not modified. `python3 -m pip_audit -r drivers/agent-sdk/requirements.txt -f json` is unavailable in this environment (`No module named pip_audit`), so no dependency-audit block applies to this documentation-only plan.

## Post-Plan Module Dispatch

[dispatch] post-plan: TODD evaluated standard `execute` plan; no tests cover planned documentation surfaces and no TDD restructuring is suggested.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | All planning, applying, and unifying work | ✓ |
| /carl | required | Automatic session boundary monitoring | automatic |
| TODD | required | Before/during code implementation; advisory only for this docs plan | ✓ via module dispatch |
| WALT | required | After implementation validation | pending APPLY |

**BLOCKING:** Required skills must be loaded or active before APPLY proceeds. For this documentation/inventory plan, `/paul` is active and module dispatch evidence is recorded above.
</skills>

<acceptance_criteria>
## AC-1: Support Tier Reset Is Explicit
```gherkin
Given README and portability docs currently describe PALS as cross-harness
When Phase 192 APPLY completes
Then the active product support statement clearly says Pi is the supported runtime and Claude Code / Agent SDK compatibility is unsupported or frozen
```

## AC-2: Cross-Harness Surface Inventory Exists
```gherkin
Given the repository contains docs, drivers, command wrappers, skills, and validation scripts
When Phase 192 APPLY completes
Then `docs/PI-NATIVE-SUPPORT-INVENTORY.md` lists the major cross-harness/Claude/Agent SDK surfaces, classifies them, and names their follow-up implication for later v2.45 phases
```

## AC-3: Historical Portability Docs Are Reclassified Safely
```gherkin
Given previous portability docs contain useful artifact-first reasoning
When those docs are updated
Then they are marked as historical/frozen reference without deleting useful historical design context
```

## AC-4: Artifact-First Truth Is Preserved
```gherkin
Given v2.45 may become Pi-native
When support-tier reset docs are written
Then they preserve `.paul/*` artifacts as authoritative lifecycle truth and avoid making Pi extension state hidden authoritative project state
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Create Pi-native support inventory artifact</name>
  <files>docs/PI-NATIVE-SUPPORT-INVENTORY.md</files>
  <action>
    Create a new markdown inventory document with these sections:
    - `# Pi-Native Support Inventory`
    - `## Support Tier Decision` stating Pi is the active runtime and Claude Code / Agent SDK are unsupported/frozen for current product direction.
    - `## Classification Legend` with at least: `active-pi`, `frozen-legacy`, `historical-reference`, `future-decision`, `validation-redesign`.
    - `## Surface Inventory` as a table with columns: `Surface`, `Path(s)`, `Current role`, `Classification`, `Follow-up implication`.
    - Include rows for README/install messaging, `kernel/commands/paul/*.md`, `drivers/pi/**`, `drivers/agent-sdk/**`, portability docs, Pi validation, cross-harness validation, module dispatch references, and `.paul/*` lifecycle artifacts.
    - `## Guardrails for Later Phases` preserving artifact-first truth, module evidence, GitHub Flow safety, parent-owned APPLY, and no hidden Pi-authoritative lifecycle state.
    - `## Recommended Next Questions` feeding Phase 193 capability mapping and Phase 194 context-efficiency design.
    Do not remove or rewrite source code in this task.
  </action>
  <verify>test -f docs/PI-NATIVE-SUPPORT-INVENTORY.md && grep -n "Support Tier Decision\|Surface Inventory\|Guardrails for Later Phases" docs/PI-NATIVE-SUPPORT-INVENTORY.md</verify>
  <done>AC-2 and AC-4 satisfied: inventory exists, classifies surfaces, and preserves artifact-first constraints.</done>
</task>

<task type="auto">
  <name>Task 2: Update active public support messaging</name>
  <files>README.md</files>
  <action>
    Update README support claims so the active product positioning no longer says PALS is built for Claude Code, Pi, and Agent SDK equally.
    Required changes:
    - Add or update an early support-status paragraph stating Pi is the supported runtime for current development.
    - State Claude Code and Agent SDK compatibility are unsupported/frozen historical surfaces, not active compatibility targets.
    - Update Quick Start / install language so it points users to Pi-first use and does not promise active Claude Code slash-command support.
    - Preserve the `.paul/*` artifact-first lifecycle truth and parent-owned APPLY statements.
    - Avoid broad rewrites of module descriptions unrelated to support tier.
  </action>
  <verify>grep -n "Pi.*supported runtime\|unsupported/frozen\|Claude Code" README.md</verify>
  <done>AC-1 and AC-4 satisfied: README states current support tier without weakening lifecycle authority.</done>
</task>

<task type="auto">
  <name>Task 3: Reclassify portability docs as historical/frozen reference</name>
  <files>docs/HARNESS-PORTABILITY.md, docs/PORTABILITY-ROADMAP.md</files>
  <action>
    Update both portability docs with a short, prominent status note near the top.
    Required content:
    - The documents are historical/frozen reference for prior cross-harness design thinking.
    - They are no longer the active product roadmap for current PALS development.
    - Pi-native architecture work should consult `docs/PI-NATIVE-SUPPORT-INVENTORY.md` for current support-tier classification.
    - Preserve useful artifact-first and hidden-state warnings as historical constraints; do not delete large sections in this phase.
  </action>
  <verify>grep -n "historical/frozen\|PI-NATIVE-SUPPORT-INVENTORY\|active product roadmap" docs/HARNESS-PORTABILITY.md docs/PORTABILITY-ROADMAP.md</verify>
  <done>AC-3 and AC-4 satisfied: portability docs are safely reclassified and linked to the new inventory.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- Do not delete `kernel/commands/paul/*.md`, `drivers/agent-sdk/**`, or validation scripts in this phase.
- Do not modify Pi extension runtime behavior in `drivers/pi/extensions/pals-hooks.ts`.
- Do not change module dispatch semantics, GitHub Flow gates, APPLY delegation rules, or `.paul/*` lifecycle truth.
- Do not update archive files except through normal PLAN/APPLY/UNIFY lifecycle artifacts.

## SCOPE LIMITS
- This plan is support-tier declaration and inventory only.
- Do not implement Pi-native command routing, compaction hooks, custom tools, or UI dialogs yet; those belong to later v2.45 phases.
- Do not remove Claude Code or Agent SDK files yet; classify and defer removal/retention decisions.
- Do not redesign validation scripts yet; record validation expectations and follow-up implications only.
</boundaries>

<verification>
Before declaring APPLY complete:
- [ ] `test -f docs/PI-NATIVE-SUPPORT-INVENTORY.md`
- [ ] `grep -n "Support Tier Decision\|Surface Inventory\|Guardrails for Later Phases" docs/PI-NATIVE-SUPPORT-INVENTORY.md`
- [ ] `grep -n "unsupported/frozen\|Pi.*supported runtime" README.md docs/HARNESS-PORTABILITY.md docs/PORTABILITY-ROADMAP.md`
- [ ] `grep -n "artifact" docs/PI-NATIVE-SUPPORT-INVENTORY.md README.md docs/HARNESS-PORTABILITY.md docs/PORTABILITY-ROADMAP.md`
- [ ] `git diff -- README.md docs/HARNESS-PORTABILITY.md docs/PORTABILITY-ROADMAP.md docs/PI-NATIVE-SUPPORT-INVENTORY.md` reviewed for accidental broad deletion.
</verification>

<success_criteria>
- Pi is clearly documented as the current supported runtime.
- Claude Code and Agent SDK are explicitly unsupported/frozen rather than active compatibility targets.
- Major cross-harness surfaces are inventoried and classified for future v2.45 planning.
- Historical portability reasoning is preserved but no longer presented as the active roadmap.
- `.paul/*` artifact authority and lifecycle guardrails remain intact.
</success_criteria>

<output>
After completion, create `.paul/phases/192-support-tier-reset-surface-inventory/192-01-SUMMARY.md` with:
- Files changed.
- Support-tier decision recorded.
- Inventory highlights.
- Deferred decisions for Phases 193–196.
- Verification commands and results.
</output>
