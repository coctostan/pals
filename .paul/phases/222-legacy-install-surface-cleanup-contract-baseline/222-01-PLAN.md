---
phase: 222-legacy-install-surface-cleanup-contract-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/PI-NATIVE-LEGACY-RETENTION-INSTALL-SURFACE-CLEANUP-CONTRACT.md
autonomous: true
---

<objective>
## Goal
Create the Phase 222 baseline contract for v2.52 Legacy Retention / Install-Surface Cleanup before any frozen legacy or install-surface removal, relocation, archival, or source-only conversion occurs.

## Purpose
v2.51 completed validation classification so cleanup can finally be planned safely. Phase 222 must convert that classification into an executable cleanup contract: exact candidate surfaces, retention/removal decision rules, protected shared invariants, command-output proof requirements, failure conditions, and Phase 223 handoff. This keeps PALS aligned with the core value — minimal kernel, modular pals, universal drivers — without removing historical surfaces that still protect lifecycle semantics.

## Output
Create `docs/PI-NATIVE-LEGACY-RETENTION-INSTALL-SURFACE-CLEANUP-CONTRACT.md` as a baseline-only contract. The contract must authorize Phase 223 planning inputs but must not itself change runtime behavior, installers, validation scripts, dependency manifests, CI, GitHub Flow automation, helper delegation, lifecycle authority, or legacy surfaces.
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md

## Prior Work
.paul/phases/221-evidence-reconciliation-milestone-closure/221-01-SUMMARY.md
.paul/phases/218-validation-classification-baseline/218-01-SUMMARY.md
.paul/phases/219-runtime-validation-suite-classification-hardening/219-01-SUMMARY.md
.paul/phases/220-documentation-report-surfacing/220-01-SUMMARY.md

## Source Files
README.md
docs/PI-NATIVE-VALIDATION-CLASSIFICATION-CONTRACT.md
docs/PI-NATIVE-IMPLEMENTATION-WORKSTREAMS.md
docs/PI-NATIVE-SEQUENCING-DEPENDENCY-PLAN.md
docs/PI-NATIVE-SUPPORT-INVENTORY.md
docs/PI-NATIVE-LEGACY-RETENTION-INSTALL-SURFACE-CLEANUP-CONTRACT.md
tests/pi-end-to-end-validation.sh
tests/cross-harness-validation.sh
drivers/claude-code/driver.yaml
drivers/agent-sdk/driver.yaml
drivers/pi/driver.yaml
</context>

<module_dispatch>
## Pre-Plan Module Dispatch

[dispatch] pre-plan advisory: ARCH, TODD, IRIS, ARIA, LUKE, DANA, GABE, DAVE, DOCS, OMAR, PETE, REED, VERA, CODI, RUBY
- ARCH: Detected repository shape is a flat/docs-and-driver framework repository: `docs/`, `drivers/`, `kernel/`, `modules/`, `tests/`, `.paul/`. Planned source output is one docs contract, which is in the expected docs layer. No circular dependencies identified. Existing validation-suite size debt remains out of scope.
- TODD: Shell validation suites are the relevant proof path. No conventional package-manager test framework was detected in `package.json`, `Makefile`, or `Cargo.toml`; this plan will use existing `bash tests/pi-end-to-end-validation.sh` and `bash tests/cross-harness-validation.sh` evidence.
- IRIS: Planned source file is new, so no TODO/FIXME/HACK/XXX findings exist yet. APPLY must avoid adding such markers.
- ARIA/LUKE: No UI files planned; skipping UI/accessibility/UX checks.
- DANA/GABE/VERA: No data, migration, API, schema, or privacy implementation files planned; skipping data/API/privacy checks.
- DAVE: No CI config edits planned. Existing CI discovery was not a planned surface; no pipeline generation is required for a docs-only contract baseline.
- DOCS: Documentation catalog exists under `docs/`, `README.md`, `drivers/*/README.md`, `kernel/*`, and `.paul/*`. Planned output is a new docs contract near related Pi-native contracts.
- OMAR/PETE/REED: No production runtime, route, async I/O, retry, timeout, or performance-sensitive source files planned; skipping observability/performance/resilience implementation checks.
- CODI: skipped — no extractable symbols in phase scope.
- RUBY: Planned file is a docs contract; preliminary debt risk is low. Post-apply should still keep the contract focused and avoid duplicating large historical prose.

[dispatch] pre-plan enforcement: DEAN, SETH
- DEAN: No package-manager lockfile or dependency manifest requiring audit was detected for this docs-only plan; no dependency additions are planned. PASS.
- SETH: Planned source output is a new markdown contract; no existing file to scan and no secrets/auth/runtime security surface is planned. APPLY must not add hardcoded secrets or executable snippets containing credentials. PASS.

## Post-Plan Module Dispatch

[dispatch] post-plan: TODD
- TODD: Existing shell validation suites cover milestone-level proof, but the planned source file is a new docs contract rather than code under a unit-test framework. Keep plan type `execute`; no RED-GREEN-REFACTOR restructuring required.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | Current PLAN/APPLY/UNIFY lifecycle work | ✓ |
| /carl | required | Automatic session-boundary monitoring | active via Pi extension |
| TODD | required | APPLY validation and post-task quality proof | registry loaded |
| WALT | required | APPLY/UNIFY quality gates and validation evidence | registry loaded |

**BLOCKING:** Required skills/modules must remain active before APPLY proceeds.
</skills>

<acceptance_criteria>

## AC-1: Cleanup contract source inputs are explicit
```gherkin
Given v2.52 starts after v2.51 validation classification closure
When the Phase 222 contract is created
Then it names the authoritative source inputs from v2.51 classification, prior implementation-planning artifacts, support-tier inventory, validation suites, driver manifests, README/install docs, and current command-output baselines
```

## AC-2: Retention/removal matrix is safe and class-aware
```gherkin
Given legacy and install surfaces may contain both Frozen Legacy Parity and Shared Invariant evidence
When the contract defines cleanup candidates
Then it separates retain/archive/source-only/remove/defer decisions by validation class and requires shared-invariant preservation before Phase 223 changes any surface
```

## AC-3: Protected boundaries prevent behavior changes
```gherkin
Given Phase 222 is a baseline contract phase
When APPLY executes this plan
Then runtime behavior, installers, validation scripts, dependency manifests, CI config, GitHub Flow automation, helper delegation, lifecycle authority, and actual legacy surfaces remain unchanged
```

## AC-4: Verification and failure conditions are executable
```gherkin
Given Phase 223 will implement cleanup based on this baseline
When the contract is complete
Then it includes marker-check families, command-output verification requirements, count-change reconciliation fields, failure conditions, and a Phase 223 handoff that can be executed without guessing
```

## AC-5: Current validation remains command-output-backed
```gherkin
Given reports and contracts are non-authoritative for pass/fail status
When Phase 222 completes
Then focused marker checks plus `PALS_ROOT="$PWD" bash drivers/pi/install.sh`, `bash tests/pi-end-to-end-validation.sh`, and `bash tests/cross-harness-validation.sh` provide fresh evidence with no expected count change from Pi 203/203 and cross-harness 119/119
```
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Create cleanup contract source inputs and decision matrix</name>
  <files>docs/PI-NATIVE-LEGACY-RETENTION-INSTALL-SURFACE-CLEANUP-CONTRACT.md</files>
  <action>
    Create the new Phase 222 contract with:
    - status table for v2.52 Phase 222 baseline-only scope;
    - purpose and non-authority boundary stating command output remains authoritative;
    - source-input table citing `docs/PI-NATIVE-VALIDATION-CLASSIFICATION-CONTRACT.md`, Phase 218-221 summaries, `docs/PI-NATIVE-IMPLEMENTATION-WORKSTREAMS.md`, `docs/PI-NATIVE-SEQUENCING-DEPENDENCY-PLAN.md`, `docs/PI-NATIVE-SUPPORT-INVENTORY.md`, `README.md`, driver manifests, and both validation suites;
    - cleanup target inventory for Claude Code, Agent SDK, portability docs, legacy install references, cross-harness parity checks, and Pi-supported runtime install surfaces;
    - retention/removal decision matrix that distinguishes `retain`, `archive`, `source-only`, `remove`, and `defer` actions with validation class, shared-invariant risk, evidence required, and Phase 223 eligibility.
    Avoid: changing any source outside this one contract; claiming cleanup is already performed; treating Frozen Legacy Parity as safe to delete without shared-invariant replacement evidence.
  </action>
  <verify>Focused grep finds required markers: `Phase 222`, `baseline-only`, `Frozen Legacy Parity`, `Shared Invariant`, `Pi-Supported Runtime`, `retain`, `archive`, `source-only`, `remove`, `defer`, `command-output truth`, and `Phase 223 handoff` in `docs/PI-NATIVE-LEGACY-RETENTION-INSTALL-SURFACE-CLEANUP-CONTRACT.md`.</verify>
  <done>AC-1 and AC-2 satisfied: source inputs and class-aware cleanup decision matrix are explicit.</done>
</task>

<task type="auto">
  <name>Task 2: Add preservation boundaries, verification requirements, and handoff</name>
  <files>docs/PI-NATIVE-LEGACY-RETENTION-INSTALL-SURFACE-CLEANUP-CONTRACT.md</files>
  <action>
    Extend the contract with:
    - preservation rules for `.paul/*` authority, PLAN/APPLY/UNIFY, module evidence, GitHub Flow safety, parent-owned APPLY, explicit approvals/checkpoints, command-output truth, and archive history;
    - explicit non-goals for runtime behavior, installer changes, validation-script edits, dependency/CI changes, helper-agent semantics, lifecycle authority, and broad report surfacing;
    - marker-check families that Phase 223+ can use for docs/contracts/validation if cleanup changes count or posture;
    - count-change reconciliation fields copied from v2.51 classification needs: old count, new count, reason, validation class affected, shared-invariant preservation rationale, command-output evidence, and cleanup authorization;
    - failure conditions that block cleanup if any shared invariant loses coverage, if stale reports replace commands, if Pi-supported runtime install behavior regresses, or if legacy cleanup exceeds approved scope;
    - Phase 223 handoff with concrete eligible/ineligible change categories.
    Avoid: adding validation-script guardrails in Phase 222; approving deletion of any file in this baseline-only phase.
  </action>
  <verify>Run focused marker grep, `PALS_ROOT="$PWD" bash drivers/pi/install.sh`, `bash tests/pi-end-to-end-validation.sh`, and `bash tests/cross-harness-validation.sh`.</verify>
  <done>AC-3, AC-4, and AC-5 satisfied: boundaries are explicit, handoff is executable, and validation remains command-output-backed with no expected count change.</done>
</task>

<task type="auto">
  <name>Task 3: Record Phase 222 evidence for lifecycle reconciliation</name>
  <files>.paul/phases/222-legacy-install-surface-cleanup-contract-baseline/222-01-SUMMARY.md, .paul/STATE.md, .paul/ROADMAP.md</files>
  <action>
    During UNIFY after APPLY succeeds, create the Phase 222 summary and update lifecycle artifacts with:
    - AC results and command-output evidence;
    - validation count reconciliation showing Pi remains 203/203 and cross-harness remains 119/119 unless an approved deviation occurs;
    - module dispatch reports, including DEAN/SETH/TODD/WALT evidence;
    - GitHub Flow evidence for the Phase 222 PR;
    - Phase 223 readiness and any cleanup targets deferred or blocked.
    Avoid: writing lifecycle artifacts during Task 1 or Task 2 APPLY implementation except as required by the PAUL workflow.
  </action>
  <verify>SUMMARY exists after UNIFY and STATE/ROADMAP route to Phase 223 only after merge-gate evidence is recorded.</verify>
  <done>AC-5 satisfied for lifecycle evidence and Phase 223 routing.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- `drivers/pi/install.sh`
- `drivers/pi/extensions/pals-hooks.ts`
- `drivers/claude-code/**`
- `drivers/agent-sdk/**`
- `kernel/**`
- `modules/**`
- `tests/pi-end-to-end-validation.sh`
- `tests/cross-harness-validation.sh`
- dependency manifests / lockfiles
- CI configuration
- runtime behavior, GitHub Flow automation, helper delegation semantics, and lifecycle authority semantics

## SCOPE LIMITS
- Phase 222 is contract baseline only.
- Do not remove, relocate, archive, source-only convert, or delete any legacy/install surface in this plan.
- Do not weaken or reclassify validation checks in this plan.
- Do not add new dependencies, telemetry, hidden Pi state, Pi-owned lifecycle persistence, auto-approval, UI-only lifecycle decisions, helper-owned APPLY, merge-gate bypass, or validation report authority.
- Validation count changes are not expected; if a count changes unexpectedly, stop and reconcile before proceeding.
</boundaries>

<verification>
Before declaring plan complete:
- [ ] Focused marker grep passes for the new cleanup contract.
- [ ] `PALS_ROOT="$PWD" bash drivers/pi/install.sh` passes.
- [ ] `bash tests/pi-end-to-end-validation.sh` passes with expected Pi 203/203 unless explicitly reconciled.
- [ ] `bash tests/cross-harness-validation.sh` passes with expected cross-harness 119/119 unless explicitly reconciled.
- [ ] `git diff --name-only` shows source changes limited to `docs/PI-NATIVE-LEGACY-RETENTION-INSTALL-SURFACE-CLEANUP-CONTRACT.md` plus normal `.paul/*` lifecycle artifacts.
- [ ] All acceptance criteria met.
</verification>

<success_criteria>
- Phase 222 contract exists and is specific enough for Phase 223 implementation planning.
- Cleanup target decisions are class-aware and preserve shared invariants before any action.
- No runtime, installer, validation-script, dependency, CI, helper, GitHub Flow, lifecycle authority, or actual legacy-surface cleanup changes occur in Phase 222.
- Fresh validation command output remains authoritative and green.
- Phase 223 handoff is explicit.
</success_criteria>

<output>
After completion, create `.paul/phases/222-legacy-install-surface-cleanup-contract-baseline/222-01-SUMMARY.md` with AC results, focused marker evidence, installer verification, Pi/cross-harness validation output, count reconciliation, module reports, GitHub Flow evidence, and Phase 223 readiness.
</output>
