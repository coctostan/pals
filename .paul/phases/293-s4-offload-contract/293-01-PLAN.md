---
phase: 293-s4-offload-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/PI-NATIVE-GUIDED-WORKFLOW-CANONICAL-REPLY-DELIVERY-EXTRACTION-CONTRACT.md
  - docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md
autonomous: true
---

<objective>
## Goal
Create the Phase 293 S4 canonical reply delivery extraction contract and bounded Phase 294 handoff without changing runtime behavior.

## Purpose
S4 is the last non-extracted S* Pi runtime context surface. Before Phase 294 moves code out of `pals-hooks.ts`, Phase 293 must define the allowed boundary, forbidden scope, validation evidence, and authority guardrails so extraction reduces future context load without weakening lifecycle truth or transcript-visible canonical replies.

## Output
- New docs contract: `docs/PI-NATIVE-GUIDED-WORKFLOW-CANONICAL-REPLY-DELIVERY-EXTRACTION-CONTRACT.md`
- Updated modularization contract outcome/handoff section in `docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md`
- No source, installer, test, dependency, CI, or installed-runtime changes in this plan.
</objective>

<context>
## Project Context
.paul/STATE.md — Phase 293 is ready to plan; v2.69 targets S4 runtime context offload first.
.paul/ROADMAP.md — Phase 293 focus: define S4 boundary, allowed/forbidden scope, context-bloat success framing, validation expectations, and authority guardrails.
.paul/PROJECT.md — S5/S1/S2/S3/S7/S6/S8 sibling extraction pattern is durable; only S4 canonical reply delivery remains explicitly deferred as the integration point.

## Prior Work
- `docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md` — S1–S8 inventory; S4 row; sibling-module pattern and prior outcomes.
- `docs/PI-NATIVE-GUIDED-WORKFLOW-EVIDENCE-CONTRACT.md` — governs transcript-visible canonical replies, no auto-approval, no auto-continue, checkpoint blocking, no UI-only lifecycle decisions, and no inferred merge intent.
- `docs/PI-NATIVE-GUIDED-WORKFLOW-DETECTION-EXTRACTION-CONTRACT.md` — S3 detection contract precedent; S4 delivery stayed inline.
- `docs/PI-NATIVE-PALS-CONTEXT-INJECTION-EXTRACTION-CONTRACT.md` — contract + handoff shape precedent for the next extraction phase.
- `docs/PI-NATIVE-LIFECYCLE-UI-EXTRACTION-CONTRACT.md` and `docs/PI-NATIVE-COMMAND-ROUTING-EXTRACTION-CONTRACT.md` — later runtime-coupled extraction precedents.

## Source Files (read-only evidence only)
- `drivers/pi/extensions/pals-hooks.ts` — current S4 owner: `sendCanonicalWorkflowResponse`, `presentGuidedWorkflowMoment`, `loadGuidedWorkflowConfig`, `shouldAutoPresent`, and `pi.sendUserMessage` call path.
- `drivers/pi/extensions/guided-workflow-detection.ts` — S3 detection sibling that must remain separate from S4 delivery.
- `tests/pi-end-to-end-validation.sh` — existing S4/S3/S7/S6/S8 guardrail patterns and future Phase 294 validation target.
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan enforcement: DEAN PASS — no planned dependency manifests or lockfiles in scope; `requirements.txt` exists only under frozen `drivers/agent-sdk/` and is out of plan scope.
[dispatch] pre-plan advisory: ARCH skipped — docs-only scope; no source files modified.
[dispatch] pre-plan advisory: SETH skipped — docs-only contract; no secret/auth/validation source paths modified.
[dispatch] pre-plan advisory: TODD skipped — docs-only contract; no pure input/output implementation or existing test file target selected.
[dispatch] pre-plan advisory: IRIS skipped — planned target docs are new/contract prose or existing contract prose; marker count not execution-relevant.
[dispatch] pre-plan advisory: GABE skipped — no API files in scope.
[dispatch] pre-plan advisory: LUKE/ARIA skipped — no UI files in scope.
[dispatch] pre-plan advisory: DANA skipped — no schema, model, entity, or migration files in scope.
[dispatch] pre-plan advisory: DAVE WARN — no CI workflow config detected; existing repo validation remains shell-script based (`tests/pi-end-to-end-validation.sh`, `tests/cross-harness-validation.sh`) and this docs-only plan must not add CI.
[dispatch] pre-plan advisory: DOCS PASS — documentation-only scope; drift check not applicable, but the new S4 contract must cite existing S3/evidence/modularization contracts.
[dispatch] pre-plan advisory: OMAR/REED/VERA/PETE skipped — docs-only contract; no runtime observability/resilience/privacy/performance paths modified.
[dispatch] pre-plan advisory: CODI skipped — no TS/JS files modified and no explicit seed candidates selected.
[dispatch] pre-plan advisory: RUBY skipped — docs-only contract; no code debt review candidates.

## Post-Plan Module Dispatch
[dispatch] post-plan: TODD PASS — plan type remains `execute`; no TDD conversion suggested because planned changes are docs-only contract artifacts and no implementation/test target is selected.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | PLAN/APPLY/UNIFY lifecycle management | ✓ |
| /carl | required | Session boundary management during lifecycle transitions | automatic |
| TODD | required | Before/during code implementation; skipped here because plan is docs-only | n/a |
| WALT | required | APPLY quality/validation gates | ○ |

**BLOCKING:** Required skills must be satisfied before APPLY proceeds. For this docs-only plan, TODD has no TDD conversion target; WALT validation still applies during APPLY.
</skills>

<acceptance_criteria>
## AC-1: S4 Contract Boundary Defined
```gherkin
Given S4 canonical reply delivery still lives in `drivers/pi/extensions/pals-hooks.ts`
When Phase 293 APPLY creates the S4 contract
Then the contract names the S4-owned identifiers, the allowed Phase 294 sibling target, shared-helper/type disposition, and the exact runtime authority boundaries that must be preserved.
```

## AC-2: Forbidden Scope and Authority Guardrails Preserved
```gherkin
Given S4 mutates the Pi message stream through canonical user-message delivery
When the contract defines Phase 294 extraction constraints
Then it explicitly forbids auto-approval, auto-continue, skipped checkpoints, UI-only lifecycle decisions, inferred merge intent, hidden state, telemetry, `.paul/*` lifecycle writes, installed-runtime edits, and unrelated sibling re-extraction.
```

## AC-3: Phase 294 Handoff Is Verifiable
```gherkin
Given Phase 294 will perform the source extraction
When Phase 293 closes
Then the contract provides grep/TAP/install/validation expectations and the modularization contract records the Phase 293 outcome without claiming command-output counts that were not freshly observed.
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Create S4 extraction contract</name>
  <files>docs/PI-NATIVE-GUIDED-WORKFLOW-CANONICAL-REPLY-DELIVERY-EXTRACTION-CONTRACT.md</files>
  <action>
    Create a docs-only contract modeled on prior S* extraction contracts.
    Include:
    - status/purpose that says Phase 293 changes no runtime behavior and authorizes only a future Phase 294 plan to extract code;
    - evidence base citing the modularization, guided workflow evidence, S3 detection, S7/S6/S8 extraction precedents, `pals-hooks.ts`, and existing validation guardrails;
    - S4 subsystem boundary covering `sendCanonicalWorkflowResponse`, `presentGuidedWorkflowMoment`, `loadGuidedWorkflowConfig`, `shouldAutoPresent`, `pi.sendUserMessage`, `ctx.ui.confirm`, `ctx.ui.select`, notify-only behavior, and `guided_workflow.auto_present` gating;
    - allowed Phase 294 extraction shape for a sibling such as `drivers/pi/extensions/guided-workflow-delivery.ts`, including loader-compat no-op factory, minimal imports, type-only back-imports if needed, and exact preservation of canonical response semantics;
    - forbidden scope and authority guardrails listed in AC-2;
    - Phase 294 handoff with concrete grep/TAP/install/validation expectations and summary count reconciliation discipline.
    Avoid: editing source, tests, installer, dependencies, CI, installed runtime files under `~/.pi`, or `.paul/*` lifecycle artifacts as part of APPLY task work.
  </action>
  <verify>Read the new contract and confirm it contains sections for Purpose, Evidence Base, S4 Subsystem Boundary, Allowed Phase 294 Extraction Shape, Forbidden Scope, Phase 294 Handoff, and Acceptance Checklist.</verify>
  <done>AC-1 and AC-2 satisfied: S4 boundary and guardrails are explicit and source behavior remains unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Surface Phase 293 outcome in modularization contract</name>
  <files>docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md</files>
  <action>
    Add a concise `## Phase 293 Outcome` section near the existing chronological outcome sections.
    State that Phase 293 creates the S4 canonical reply delivery extraction contract, promotes S4 to the Phase 294 bounded extraction target, and preserves all runtime/source/test/install/dependency/CI/lifecycle authority until Phase 294 APPLY.
    Mention that this is a docs-only contract phase and must not report validation counts as changed unless APPLY runs fresh validation and records command output in SUMMARY.
    Avoid rewriting historical S1–S8 inventory prose beyond the minimal new outcome section.
  </action>
  <verify>`grep -n "Phase 293 Outcome\|PI-NATIVE-GUIDED-WORKFLOW-CANONICAL-REPLY-DELIVERY-EXTRACTION-CONTRACT" docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md` finds the new outcome and contract path.</verify>
  <done>AC-3 satisfied: the existing modularization authority points to the S4 contract and Phase 294 handoff without overstating validation evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Validate docs-only contract integrity</name>
  <files>docs/PI-NATIVE-GUIDED-WORKFLOW-CANONICAL-REPLY-DELIVERY-EXTRACTION-CONTRACT.md, docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md</files>
  <action>
    Run focused checks after the docs edits:
    - grep the new contract for S4 identifiers and no-auto/no-hidden-authority guardrails;
    - confirm no source/test/installer/dependency/CI files changed unless an APPLY blocker forced an approved plan revision;
    - run repository validation commands listed below when practical for docs-only closure, and copy exact command output counts into SUMMARY instead of inferred counts.
    Avoid: weakening or deleting existing TAP assertions to make docs pass.
  </action>
  <verify>`git diff --check` plus focused grep checks for `sendCanonicalWorkflowResponse`, `presentGuidedWorkflowMoment`, `loadGuidedWorkflowConfig`, `shouldAutoPresent`, `no auto-approval`, `no auto-continue`, `no skipped checkpoints`, `no UI-only lifecycle decisions`, and `no inferred merge intent` in the new contract.</verify>
  <done>AC-1, AC-2, and AC-3 satisfied: contract and outcome are reviewable, guardrails are grep-visible, and validation evidence is command-output based.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- `drivers/pi/extensions/*.ts`
- `drivers/pi/install.sh`, `drivers/pi/uninstall.sh`, `drivers/pi/driver.yaml`
- `tests/*.sh` and `tests/helpers/*.sh`
- Dependency manifests/locks, CI configs, Dockerfiles
- Installed runtime copies under `~/.pi/agent/extensions/` or `~/.pi/agent/skills/`
- `.paul/SPECIAL-FLOWS.md`

## SCOPE LIMITS
- Phase 293 is contract-only; no S4 extraction is implemented here.
- Do not create `drivers/pi/extensions/guided-workflow-delivery.ts` until Phase 294 APPLY.
- Do not change canonical reply behavior, config defaults, UI prompt behavior, command routing, lifecycle UI, S3 detection, S7 injection, S6 UI, S8 routing, or validation suites in this phase.
- Do not claim validation count changes unless copied from fresh command output during APPLY.
</boundaries>

<verification>
Before declaring APPLY complete:
- [ ] `git diff --check`
- [ ] Focused grep confirms the new S4 contract names all S4 identifiers and guardrails.
- [ ] `bash tests/helpers/artifact_consistency.sh` returns PASS when practical.
- [ ] `bash tests/pi-end-to-end-validation.sh` passes when practical; copy exact TAP counts to SUMMARY.
- [ ] `bash tests/cross-harness-validation.sh` passes when practical; copy exact TAP counts to SUMMARY.
- [ ] `git diff --name-only` contains only the planned docs plus expected `.paul/phases/293-*` PLAN/SUMMARY lifecycle artifacts and lifecycle state updates.
</verification>

<success_criteria>
- New S4 contract exists and is specific enough for Phase 294 APPLY to extract without re-litigating boundaries.
- Existing modularization contract records Phase 293 outcome and points to the new S4 contract.
- No runtime, source, installer, validation, dependency, CI, or installed-runtime behavior changes occur in Phase 293.
- All acceptance criteria are met and verification evidence is copied from actual command output.
</success_criteria>

<output>
After completion, create `.paul/phases/293-s4-offload-contract/293-01-SUMMARY.md` with:
- What was built and changed files.
- Module execution reports and any warnings (including DAVE no-CI advisory if still relevant).
- Exact validation command outputs/counts, or explicit reason any optional long validation was not run.
- Confirmation that Phase 294 is the first source-changing S4 extraction phase.
</output>
