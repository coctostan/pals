---
phase: 287-code-review-workflow-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kernel/workflows/apply-phase.md
  - kernel/workflows/unify-phase.md
  - tests/pi-end-to-end-validation.sh
  - tests/cross-harness-validation.sh
autonomous: true
---

<objective>
## Goal
Integrate optional Plannotator code-review handling into PAUL APPLY and UNIFY workflow instructions, including per-invocation prompts, advisory `CODE-REVIEW-NOTES.md` sidecars, appropriate code-review `diffType` guidance, and marker validation.

## Purpose
Phase 287 completes v2.66 by wiring the Phase 285 Plannotator bridge into code-review workflow semantics without transferring lifecycle authority to Plannotator. APPLY and UNIFY should offer a user-controlled rich review surface for implementation diffs while preserving PAUL-owned tasks, verification, module gates, GitHub Flow, REV, and merge decisions.

## Output
- Updated `kernel/workflows/apply-phase.md` with an optional end-of-APPLY Plannotator code-review step after successful post-apply gates and before GitHub Flow postflight/finalize routing.
- Updated `kernel/workflows/unify-phase.md` with bounded required-reading for `CODE-REVIEW-NOTES.md` and an optional pre-merge Plannotator code-review step before GitHub Flow merge-gate decisions.
- Updated Pi and cross-harness validation marker checks proving the workflow contract: per-invocation prompt, decline zero-overhead, `requestCodeReview`, code-review `diffType` selection, `CODE-REVIEW-NOTES.md`, advisory-only notes, ignored `agentSwitch`, no automatic `/paul:fix`, and no lifecycle/merge authority transfer.
</objective>

<context>
## Project Context
.paul/STATE.md — Phase 287 ready to PLAN after Phase 286 completion.
.paul/ROADMAP.md — Phase 287 focus: edit `/paul:apply` and `/paul:unify` for per-invocation Plannotator code-review prompts, write `CODE-REVIEW-NOTES.md`, keep advisory-only behavior, and add a UNIFY required-reading note.
.paul/PROJECT.md — v2.66 active; Phases 284–286 completed the contract, bridge helper surface, and plan-review workflow integration.

docs/PALS-PLANNOTATOR-UI-INTEGRATION-CONTRACT.md — authoritative for code-review payload/result fields, `CODE-REVIEW-NOTES.md`, opt-in prompt, failure modes, and non-adoption boundaries.

## Prior Work
.paul/phases/284-plannotator-integration-contract/284-01-SUMMARY.md — established `CODE-REVIEW-NOTES.md` as advisory evidence and code-review result consumption policy.
.paul/phases/285-paul-plannotator-bridge/285-01-SUMMARY.md — added `drivers/pi/extensions/plannotator-bridge.ts` with `requestCodeReview`.
.paul/phases/286-plan-review-workflow-integration/286-01-SUMMARY.md — integrated the analogous plan-review prompt, sidecars, and marker validation pattern.

## Source Files
- kernel/workflows/apply-phase.md — target workflow instruction for end-of-APPLY code review prompt, bridge call semantics, sidecar creation, no auto-fix routing, and lifecycle routing.
- kernel/workflows/unify-phase.md — target workflow instruction for reading `CODE-REVIEW-NOTES.md` as advisory context and offering optional pre-merge code review without overriding merge gates.
- tests/pi-end-to-end-validation.sh — Pi-supported runtime/installed workflow marker checks.
- tests/cross-harness-validation.sh — shared workflow marker checks.
- drivers/pi/extensions/plannotator-bridge.ts — bridge helper surface referenced by workflow instructions; not modified by this plan unless marker evidence proves a typo/contract mismatch.
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: TODD — no conventional unit-test rewrite suggested; Phase 287 is workflow-instruction and shell-marker-validation scope. Use existing validation scripts, not TDD, unless APPLY identifies a focused helper-test opportunity.
[dispatch] pre-plan advisory: IRIS — planned files are markdown workflow instructions and shell validation scripts. Review for TODO/FIXME/HACK/XXX markers during APPLY; no preliminary blocker.
[dispatch] pre-plan advisory: DAVE — no CI config changes planned; use local validation scripts and GitHub Flow PR checks as evidence.
[dispatch] pre-plan advisory: DOCS — workflow-instruction scope is process documentation; no separate README/docs update required unless APPLY discovers a discovery-surface drift.
[dispatch] pre-plan advisory: CODI — skipped-no-symbols; no TS/JS source changes are planned and `plannotator-bridge.ts` is referenced but not modified.
[dispatch] pre-plan advisory: RUBY — NOT_APPLICABLE for workflow/reference-only and shell-marker validation scope.
[dispatch] pre-plan advisory: ARCH/ARIA/LUKE/DANA/GABE/OMAR/PETE/REED/SETH/VERA — no app UI/API/data/runtime source changes in scope; preserve optional/advisory absence, no hidden persistence, no unbounded retries, no lifecycle writes.
[dispatch] pre-plan enforcement: DEAN PASS — no dependency manifests/lockfiles or dependency changes in scope.
[dispatch] post-plan: TODD no TDD conversion suggested; execute scope remains workflow-marker validation.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | All planning/applying/unifying lifecycle work | ✓ |
| /carl | required | Session boundary management via Pi runtime | ✓ |
| TODD | required | Before/during implementation; non-TDD workflow-marker scope unless APPLY changes that | ✓ (module dispatch) |
| WALT | required | After implementation for quality/validation gates | ○ (APPLY) |

**BLOCKING:** APPLY must dispatch WALT validation before completion and must record TODD as skipped or applied with evidence.
</skills>

<acceptance_criteria>
## AC-1: APPLY workflow offers opt-in Plannotator code review without changing PAUL authority
```gherkin
Given `/paul:apply` has completed planned tasks and required post-apply gates
When Plannotator code review is available at the APPLY review surface
Then the workflow prompts per invocation, declining emits no event and writes no sidecar, review feedback is advisory only, and Plannotator never approves or blocks APPLY lifecycle work by itself
```

## AC-2: UNIFY workflow reads and records code-review notes without overriding merge gates
```gherkin
Given `CODE-REVIEW-NOTES.md` may exist beside a plan and UNIFY may offer a pre-merge code review
When `/paul:unify` reconciles the plan and reaches merge readiness
Then it reads existing notes only as advisory context, optionally records additional UNIFY review feedback, and never lets Plannotator override SUMMARY, GitHub Flow, REV, WALT/TODD/DEAN, reviews, CI, or merge gates
```

## AC-3: Validation protects Phase 287 workflow markers
```gherkin
Given Pi and cross-harness validation scripts run without requiring Plannotator runtime execution
When the scripts inspect installed/source workflows
Then they prove the APPLY/UNIFY code-review markers: prompt, zero-overhead decline, `requestCodeReview`, appropriate `diffType`, `CODE-REVIEW-NOTES.md`, advisory-only behavior, ignored `agentSwitch`, no auto `/paul:fix`, and no lifecycle/merge authority transfer
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Add optional Plannotator code-review step to APPLY workflow</name>
  <files>kernel/workflows/apply-phase.md</files>
  <action>
    Add a compact step after successful post-apply advisory/enforcement hooks and before GitHub Flow postflight/finalize routing. The step should be named clearly, e.g. `optional_plannotator_apply_code_review`, and must:
    - Require project-level availability plus per-invocation prompt: `Run Plannotator code review? [y/n]`.
    - State that decline is zero overhead: no bridge call, no event emitted, no sidecar written, normal PAUL APPLY routing continues.
    - Use the Phase 285 bridge helper surface by name (`requestCodeReview`) without importing Plannotator internals or adding new installed-runtime writes.
    - Select APPLY review context explicitly: `diffType: "uncommitted"` before APPLY postflight commit/push; include `cwd` and GitHub Flow PR URL only when already available.
    - On unavailable/error/timeout/abandoned, record compact advisory evidence and continue through PAUL-owned routing unless the user explicitly made the review blocking.
    - On approved-with-feedback or changes-requested feedback, write or append `.paul/phases/{phase-slug}/CODE-REVIEW-NOTES.md` with phase/plan, review surface `apply`, ISO timestamp, approval state, feedback, requested `diffType`, optional opaque annotation count/reference, and advisory-only statement.
    - Explicitly state: ignore `agentSwitch`, do not auto-route to `/paul:fix`, and do not let Plannotator approve, block, widen, or change PLAN tasks, acceptance criteria, boundaries, module gates, GitHub Flow, REV, checkpoints, `.paul/STATE.md`, or SUMMARY.
    - Preserve existing module dispatch evidence, GitHub Flow postflight, and APPLY finalize semantics.
    Avoid: editing `drivers/pi/extensions/plannotator-bridge.ts`, changing `pals.json`, adding auto-review, adding workflow side effects outside `.paul/phases/{phase-slug}/CODE-REVIEW-NOTES.md`, or changing APPLY state directly from Plannotator results.
  </action>
  <verify>grep `kernel/workflows/apply-phase.md` for `Run Plannotator code review? [y/n]`, `requestCodeReview`, `diffType: "uncommitted"`, `CODE-REVIEW-NOTES.md`, `agentSwitch`, `/paul:fix`, `advisory`, and `GitHub Flow`; run shell syntax checks after validation edits.</verify>
  <done>AC-1 satisfied: APPLY has explicit opt-in code-review semantics, advisory sidecar output, no auto-fix routing, and non-authority guardrails.</done>
</task>

<task type="auto">
  <name>Task 2: Add UNIFY advisory notes loading and optional code-review step</name>
  <files>kernel/workflows/unify-phase.md</files>
  <action>
    Add two compact UNIFY updates:
    1. Required-reading/context loading: if `.paul/phases/{phase-slug}/CODE-REVIEW-NOTES.md` exists beside the approved plan, UNIFY may read it as advisory context only. The note must state it never overrides SUMMARY reconciliation, tasks, acceptance criteria, module gates, GitHub Flow, REV, CI, reviews, merge gates, or `.paul/*` lifecycle truth.
    2. Optional pre-merge code-review step after SUMMARY finalization and before `github_flow_merge_gate`. The step should be named clearly, e.g. `optional_plannotator_unify_code_review`, and must:
       - Require project-level availability plus per-invocation prompt: `Run Plannotator code review? [y/n]`.
       - State that decline is zero overhead.
       - Use `requestCodeReview` by name without importing Plannotator internals.
       - Select UNIFY review context explicitly: prefer PR URL when available; otherwise use a phase-range/staged review context such as `diffType: "phase-range"` or `diffType: "staged"` according to the current Git state.
       - Write or append `.paul/phases/{phase-slug}/CODE-REVIEW-NOTES.md` with review surface `unify`, ISO timestamp, approval state, feedback, requested diff context, optional opaque annotation count/reference, and advisory-only statement.
       - State that Plannotator feedback does not block merge by itself, does not replace REV, and does not override GitHub Flow CI/review/merge gates; user-requested blocking review must be an explicit PAUL decision/deviation.
    Keep changes compact and local to UNIFY context loading / pre-merge routing; do not add `/paul:annotate`, auto-fix routing, or Plannotator lifecycle authority.
  </action>
  <verify>grep `kernel/workflows/unify-phase.md` for `CODE-REVIEW-NOTES.md`, `advisory context only`, `Run Plannotator code review? [y/n]`, `requestCodeReview`, `diffType: "phase-range"`, `diffType: "staged"`, `PR URL`, `REV`, `merge gate`, and `never overrides`.</verify>
  <done>AC-2 satisfied: UNIFY reads and records code-review notes as advisory evidence without expanding authority or merge readiness.</done>
</task>

<task type="auto">
  <name>Task 3: Add workflow marker validation for Phase 287</name>
  <files>tests/pi-end-to-end-validation.sh, tests/cross-harness-validation.sh</files>
  <action>
    Add focused marker checks in both validation scripts for Phase 287 workflow integration:
    - APPLY workflow contains the per-invocation code-review prompt, decline zero-overhead wording, `requestCodeReview`, APPLY `diffType: "uncommitted"`, `CODE-REVIEW-NOTES.md`, ignored `agentSwitch`, no auto `/paul:fix`, advisory-only notes, and PAUL/GitHub Flow authority wording.
    - UNIFY workflow contains `CODE-REVIEW-NOTES.md` advisory context-only loading, per-invocation code-review prompt, `requestCodeReview`, PR URL / phase-range / staged diff context markers, REV and merge-gate non-override wording, and no lifecycle authority transfer.
    - Keep checks marker-based; do not require Plannotator installation, browser/UI execution, or runtime event dispatch.
    - Preserve existing validation counts and inherited-failure posture; do not fix unrelated CODI/context-budget failures in this phase.
  </action>
  <verify>bash -n tests/pi-end-to-end-validation.sh && bash -n tests/cross-harness-validation.sh && bash tests/helpers/artifact_consistency.sh && bash tests/cross-harness-validation.sh && bash tests/pi-end-to-end-validation.sh</verify>
  <done>AC-3 satisfied: Pi and cross-harness scripts protect Phase 287 workflow markers without requiring Plannotator runtime execution.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- `drivers/pi/extensions/plannotator-bridge.ts` unless a direct marker typo prevents workflow integration validation.
- `pals.json` Plannotator defaults or project-level enablement values.
- PALS module registry (`modules.yaml`) or module manifests.
- Installed runtime copies under `~/.pi/agent/extensions/*` or `~/.pi/agent/skills/pals/*`.
- Plannotator internals, `--plan` mode, `plannotator_submit_plan`, `plannotator.json`, archive/history/note-export behavior, or `/paul:annotate` wrappers.
- Plan-review prompt/sidecar semantics from Phase 286 except where validation adjacency requires adding code-review markers.

## SCOPE LIMITS
- This phase integrates only code-review surfaces for `/paul:apply` and `/paul:unify`.
- `CODE-REVIEW-NOTES.md` is advisory evidence only and lives under `.paul/phases/{phase-slug}/`.
- Declining Plannotator code review must be zero overhead.
- Plannotator feedback may inform user decisions, but does not auto-route to `/paul:fix`, does not replace REV, and does not block APPLY/UNIFY/merge gates by itself.
- No new dependencies, no browser automation, no runtime Plannotator invocation required for validation, and no cross-harness driver support.
</boundaries>

<verification>
Before declaring APPLY complete:
- [ ] `git diff --check`
- [ ] `bash -n tests/pi-end-to-end-validation.sh`
- [ ] `bash -n tests/cross-harness-validation.sh`
- [ ] `bash tests/helpers/artifact_consistency.sh`
- [ ] `bash tests/cross-harness-validation.sh`
- [ ] `bash tests/pi-end-to-end-validation.sh` with inherited failures classified separately from new Phase 287 checks
- [ ] Focused marker grep evidence for code-review prompt, sidecar, `requestCodeReview`, diff context, ignored fields, no auto-fix, advisory APPLY/UNIFY notes, REV/GitHub Flow non-override, and PAUL authority
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- `/paul:apply` workflow instructions include opt-in Plannotator code review with safe decline, advisory `CODE-REVIEW-NOTES.md`, no auto-fix routing, and non-authority guardrails.
- `/paul:unify` workflow instructions read `CODE-REVIEW-NOTES.md` only as advisory context when present and may offer pre-merge code review without overriding REV/GitHub Flow gates.
- Pi and cross-harness validation scripts enforce the new workflow markers.
- No runtime Plannotator installation, workflow auto-approval, lifecycle authority transfer, dependency changes, module-registry changes, or plan-review regression are introduced.
</success_criteria>

<output>
After completion, create `.paul/phases/287-code-review-workflow-integration/287-01-SUMMARY.md` documenting implementation, validation output, inherited validation failures if any, and v2.66 milestone closure handoff notes.
</output>
