---
phase: 217-evidence-reconciliation-milestone-closure
plan: 01
type: execute
wave: 1
depends_on:
  - 216-01
files_modified:
  - docs/PI-NATIVE-DELEGATED-APPLY-PACKET-REPORT-CONTRACT.md
  - tests/pi-end-to-end-validation.sh
  - tests/cross-harness-validation.sh
autonomous: true
---

<objective>
## Goal
Reconcile v2.50 Delegated APPLY Packet/Report Contract evidence against the Phase 214 baseline, Phase 215 runtime hardening, and Phase 216 documentation/validation surfacing, then close the milestone with command-backed validation, GitHub Flow evidence, lifecycle metadata, and a clear handoff to Pi-Native Validation Classification.

## Purpose
Phase 214 defined the delegated APPLY packet/report contract; Phase 215 hardened parent/helper workflow behavior; Phase 216 made the behavior discoverable and validation-protected. Phase 217 closes v2.50 by:
- updating the contract with final evidence baselines and closure requirements,
- adding durable closure marker checks for the final v2.50 helper delegation evidence,
- preserving command-output validation truth, parent-owned APPLY, file-scope checks, fallback, module gates, checkpoints, GitHub Flow safety, and no helper-owned lifecycle authority,
- producing complete UNIFY evidence so the next milestone can start from a clean closure point.

## Output
- `docs/PI-NATIVE-DELEGATED-APPLY-PACKET-REPORT-CONTRACT.md` distinguishes Phase 214/215 historical baseline evidence from Phase 216 closure-entry evidence and records final v2.50 closure expectations.
- Pi and cross-harness validation suites protect v2.50 closure markers, Helper Delegation and Shared Invariant validation-class names, count reconciliation language, command-output truth, parent-owned verification, file-scope checks, fallback, and no helper-owned `.paul/*` lifecycle authority.
- `.paul/phases/217-evidence-reconciliation-milestone-closure/217-01-SUMMARY.md` records AC results, marker grep evidence, TAP totals, validation count reconciliation, module reports, GitHub Flow evidence, decisions/deviations, v2.50 milestone closure, and next milestone handoff.
</objective>

<context>
## Project Context
- `.paul/PROJECT.md` — current v2.50 posture; Phase 217 ready to plan.
- `.paul/ROADMAP.md` — v2.50 phase table; Phase 217 focus.
- `.paul/STATE.md` — lifecycle truth and Phase 217 routing.

## Prior Work
- `docs/PI-NATIVE-DELEGATED-APPLY-PACKET-REPORT-CONTRACT.md` — Phase 214 contract baseline and Phase 217 closure requirements.
- `.paul/phases/214-delegated-apply-packet-report-contract-baseline/214-01-SUMMARY.md` — contract baseline evidence; validation remained Pi 197/197 and cross-harness 112/112.
- `.paul/phases/215-runtime-delegation-packet-report-hardening/215-01-SUMMARY.md` — runtime/helper delegation evidence; validation remained Pi 197/197 and cross-harness 112/112.
- `.paul/phases/216-documentation-validation-surfacing/216-01-SUMMARY.md` — docs/helper-agent/validation surfacing evidence, PR #131 merge evidence, final Phase 216 counts (Pi 199/199, cross-harness 115/115), and Phase 217 handoff.
- `.paul/phases/213-evidence-reconciliation-milestone-closure/213-01-PLAN.md` and SUMMARY — prior milestone closure pattern for reconciling baselines and adding durable closure checks.

## Source Files
- `docs/PI-NATIVE-DELEGATED-APPLY-PACKET-REPORT-CONTRACT.md`
- `tests/pi-end-to-end-validation.sh`
- `tests/cross-harness-validation.sh`

## CODI Seed Candidates
- No TS/JS source selectors in this docs/shell-validation closure plan; CODI may skip cleanly.
</context>

<module_dispatch>
## Pre-Plan Module Dispatch

Planning collaboration level: high (project default from `pals.json`). Planning mode: direct-requirements.

[dispatch] pre-plan advisory: modules.yaml loaded from installed registry; TODD, IRIS, DAVE, DOCS, CODI, RUBY, ARCH, SETH, ARIA, LUKE, DANA, GABE, OMAR, PETE, REED, and VERA scope probes completed; advisory only.

[dispatch] TODD: shell validation suites are in planned scope (`tests/pi-end-to-end-validation.sh`, `tests/cross-harness-validation.sh`); no root package test framework conversion requested. This plan should extend existing shell marker checks and run both suites.

[dispatch] IRIS: no TODO/FIXME/HACK/XXX markers found in planned files.

[dispatch] DAVE: no CI/CD config files discovered in bounded scan; no CI file changes planned.

[dispatch] DOCS: 101 markdown docs found within depth 3; planned doc edit is limited to `docs/PI-NATIVE-DELEGATED-APPLY-PACKET-REPORT-CONTRACT.md`; README/Pi docs/skill-map/helper-agent surfaces were updated in Phase 216 and should not be reopened unless validation proves drift.

[dispatch] ARCH: Detected pattern: modular kernel/driver/module architecture (high confidence). Planned documentation lives in `docs/`; validation additions live in existing shell suites. Advisory: `tests/pi-end-to-end-validation.sh` and `tests/cross-harness-validation.sh` are pre-existing large files; keep additions marker-based and localized near delegated APPLY/helper delegation checks.

[dispatch] SETH: no hardcoded secret patterns found in planned files; no auth/API surface in scope.

[dispatch] ARIA: no UI component files in planned changes — skipping.

[dispatch] LUKE: no UI component/design-token files in planned changes — skipping.

[dispatch] DANA: no data schema/model/migration files in planned changes — skipping.

[dispatch] GABE: no API route/controller/schema files in planned changes — skipping.

[dispatch] OMAR: no logging/health/error-handler observability changes required — docs/shell marker checks only.

[dispatch] PETE: no performance-sensitive runtime source in planned changes; avoid expensive validation scans and keep marker checks focused.

[dispatch] REED: no resilience-relevant runtime source in planned changes — skipping.

[dispatch] VERA: no privacy/PII files or data collection surfaces in scope — skipping.

[dispatch] CODI: skipped — no extractable symbols in phase scope.

[dispatch] RUBY: planned shell validation files are large/pre-existing (`tests/pi-end-to-end-validation.sh` 1585 lines; `tests/cross-harness-validation.sh` 1179 lines); additions should be helper-local/marker-based and avoid broad refactors.

[dispatch] pre-plan enforcement: DEAN PASS — no package manager manifests or lockfiles detected at repo root/maxdepth 2 outside ignored runtime/vendor paths, so dependency audit is not applicable and no critical/high dependency block exists.

[dispatch] post-plan: TODD reviewed plan type `execute`; shell validation suites are the proof path and no TDD conversion is requested.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | PLAN/APPLY/UNIFY lifecycle management | ✓ |
| /carl | required | Session boundary management at lifecycle/session transitions | ✓ |
| TODD | required | Test/validation enforcement during APPLY | ✓ via installed modules.yaml |
| WALT | required | Quality gating and validation during APPLY/UNIFY | ✓ via installed modules.yaml |

**BLOCKING:** Required skills/modules MUST remain loaded before APPLY proceeds.
</skills>

<acceptance_criteria>
## AC-1: Contract records v2.50 closure baselines and evidence chain
```gherkin
Given Phase 214 defined the delegated APPLY packet/report contract, Phase 215 hardened runtime/helper workflow behavior, and Phase 216 surfaced docs/helper-agent/validation markers
When `docs/PI-NATIVE-DELEGATED-APPLY-PACKET-REPORT-CONTRACT.md` is updated
Then it records the Phase 214/215 historical baseline (Pi 197/197 and cross-harness 112/112), the Phase 216/v2.50 closure-entry baseline (Pi 199/199 and cross-harness 115/115), PR #131 merge evidence, and the requirement that future count changes record old count, new count, reason, validation class affected, and shared-invariant preservation rationale
```

## AC-2: Pi validation protects v2.50 Helper Delegation closure markers
```gherkin
Given Phase 216 added docs/helper-agent/validation marker checks for delegated APPLY packet/report guarantees
When `tests/pi-end-to-end-validation.sh` runs after this plan
Then it protects v2.50 closure evidence markers in the contract, including `Helper Delegation`, `Shared Invariant`, Pi 199/199, cross-harness 115/115, parent-owned task packets, structured helper reports, allowed/forbidden file scope, fallback triggers, parent-run official verification, changed-file diff review, no helper-owned `.paul/*` lifecycle writes, command-output truth, and validation count reconciliation language
```

## AC-3: Cross-harness validation protects shared invariant closure markers
```gherkin
Given helper delegation is allowed only as parent-owned APPLY assistance and `.paul/*`, PLAN/APPLY/UNIFY, module evidence, GitHub Flow evidence, checkpoints, fallback judgment, and command output remain shared authority
When `tests/cross-harness-validation.sh` runs after this plan
Then it protects the contract closure markers for `.paul/*` authority, parent-owned APPLY, module evidence, checkpoint blocking, GitHub Flow safety, helper reports as review input only, parent-run official verification, changed-file diff checks, command-output truth, validation classes, and no helper-owned lifecycle authority
```

## AC-4: v2.50 milestone closure produces complete reconciliation evidence
```gherkin
Given Phase 217 is the final v2.50 phase
When APPLY/UNIFY completes
Then `.paul/phases/217-evidence-reconciliation-milestone-closure/217-01-SUMMARY.md` records acceptance-criteria results, focused marker grep output, fresh Pi/cross-harness TAP totals, old/new count reconciliation, changed-file scope review, module reports, GitHub Flow evidence, helper delegation evidence if used, decisions/deviations, v2.50 milestone closure, and next milestone handoff
```

## AC-5: Scope remains evidence reconciliation and milestone closure only
```gherkin
Given runtime delegation hardening and docs/validation surfacing are already complete
When this plan is applied
Then it does not change runtime behavior, installer behavior, dependencies, CI configuration, helper-agent contract surfaces, APPLY workflow semantics, broad validation classification, legacy Claude Code/Agent SDK surfaces, or lifecycle authority semantics outside normal `.paul/*` lifecycle writes
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Reconcile delegated APPLY contract closure baselines</name>
  <files>docs/PI-NATIVE-DELEGATED-APPLY-PACKET-REPORT-CONTRACT.md</files>
  <action>
    Update the contract's status/evidence sections so v2.50 closure is explicit:
    - Record Phase 214/215 historical baseline: Pi 197/197 and cross-harness 112/112.
    - Record Phase 216/v2.50 closure-entry baseline: Pi 199/199 and cross-harness 115/115.
    - Record that PR #131 merged Phase 216 docs/validation surfacing and that local validation plus Socket checks passed.
    - Preserve the command-output evidence requirement that future count changes record old count, new count, reason, validation class affected, and shared-invariant preservation rationale.
    - Preserve parent-owned task packets, structured helper reports, allowed/forbidden file scope, fallback triggers, parent-run official verification, changed-file diff checks, Helper Delegation, Shared Invariant, command-output truth, and no helper-owned `.paul/*` lifecycle authority.
    - Do not rewrite the contract as if Phase 214 implemented runtime/docs behavior; keep Phase 214 as baseline, Phase 215 as runtime hardening, Phase 216 as docs/validation surfacing, and Phase 217 as closure.
  </action>
  <verify>grep -n "197/197\|112/112\|199/199\|115/115\|PR #131\|Helper Delegation\|Shared Invariant\|old count\|new count\|validation class affected\|shared-invariant preservation rationale\|Parent-owned task packet\|structured helper report\|changed-file diff\|command-output truth\|no helper-owned .paul" docs/PI-NATIVE-DELEGATED-APPLY-PACKET-REPORT-CONTRACT.md</verify>
  <done>AC-1 satisfied: contract records historical and closure-entry baselines, PR evidence, validation-class names, and count-change reconciliation requirements.</done>
</task>

<task type="auto">
  <name>Task 2: Add durable v2.50 closure marker checks in Pi and cross-harness validation</name>
  <files>tests/pi-end-to-end-validation.sh, tests/cross-harness-validation.sh</files>
  <action>
    Add focused marker checks near existing delegated APPLY/helper delegation checks:
    - In `tests/pi-end-to-end-validation.sh`, add or extend a `tap_file_contains_all` check that validates the contract's v2.50 closure evidence markers: Pi 199/199, cross-harness 115/115, exact validation class names, packet/report schema markers, fallback, file-scope checks, parent-run official verification, no helper-owned lifecycle authority, and command-output count reconciliation language.
    - In `tests/cross-harness-validation.sh`, add or extend a `tap_file_contains_all` check that validates shared-invariant closure markers: `.paul/*` authority, parent-owned APPLY, module evidence, checkpoint blocking, GitHub Flow safety, helper reports as review input only, parent-run official verification, changed-file diff checks, command-output truth, validation classes, and no helper-owned lifecycle authority.
    - Prefer existing helpers and structural marker checks; avoid broad scans, brittle exact prose, new dependencies, or harness refactors.
    - Capture expected count changes during APPLY for UNIFY reconciliation.
  </action>
  <verify>bash tests/pi-end-to-end-validation.sh && bash tests/cross-harness-validation.sh</verify>
  <done>AC-2 and AC-3 satisfied: both validation suites pass and visibly protect v2.50 delegated APPLY closure markers.</done>
</task>

<task type="auto">
  <name>Task 3: Run final v2.50 closure evidence and scope checks</name>
  <files>docs/PI-NATIVE-DELEGATED-APPLY-PACKET-REPORT-CONTRACT.md, tests/pi-end-to-end-validation.sh, tests/cross-harness-validation.sh</files>
  <action>
    Produce APPLY evidence for UNIFY and milestone closure:
    - Run focused marker grep over planned files for v2.50 closure markers: `197/197`, `112/112`, `199/199`, `115/115`, `PR #131`, `Helper Delegation`, `Shared Invariant`, `Parent-owned task packet`, `structured helper report`, `Allowed files:`, `Forbidden files:`, `Fallback triggers:`, `Parent-run official verification:`, `changed-file diff`, `helper reports as review input`, `command-output truth`, `old count`, `new count`, `validation class affected`, `shared-invariant preservation rationale`, `GitHub Flow safety`, and `no helper-owned .paul`.
    - Run `PALS_ROOT="$PWD" bash drivers/pi/install.sh` to verify install/source consistency where applicable.
    - Run `bash tests/pi-end-to-end-validation.sh` and record TAP totals.
    - Run `bash tests/cross-harness-validation.sh` and record TAP totals.
    - Run `git diff --stat` and confirm scope stayed inside approved contract/validation files plus normal `.paul/*` lifecycle artifacts.
    - Capture old/new validation counts and reason for any count increase in UNIFY.
  </action>
  <verify>grep -n "197/197\|112/112\|199/199\|115/115\|PR #131\|Helper Delegation\|Shared Invariant\|Parent-owned task packet\|structured helper report\|Allowed files:\|Forbidden files:\|Fallback triggers:\|Parent-run official verification:\|changed-file diff\|helper reports as review input\|command-output truth\|old count\|new count\|validation class affected\|shared-invariant preservation rationale\|GitHub Flow safety\|no helper-owned .paul" docs/PI-NATIVE-DELEGATED-APPLY-PACKET-REPORT-CONTRACT.md tests/pi-end-to-end-validation.sh tests/cross-harness-validation.sh && PALS_ROOT="$PWD" bash drivers/pi/install.sh && bash tests/pi-end-to-end-validation.sh && bash tests/cross-harness-validation.sh</verify>
  <done>AC-4 and AC-5 satisfied: closure evidence is command-backed and changed files stay inside approved evidence-reconciliation/milestone-closure scope.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- Do not modify `kernel/workflows/apply-phase.md`; Phase 215 runtime/helper delegation behavior is complete.
- Do not modify `README.md`, `drivers/pi/extensions/README.md`, `drivers/pi/skill-map.md`, or `.pi/agents/pals-implementer.md` unless a failing marker check proves Phase 216 surfacing drift that requires an approved re-plan.
- Do not modify `drivers/pi/extensions/pals-hooks.ts`, installer behavior, installed runtime files outside the repo, dependency manifests/lockfiles, CI workflow configuration, GitHub Flow automation, broad validation classification, or lifecycle authority semantics.
- Do not change PLAN/APPLY/UNIFY workflow semantics, module dispatch semantics, checkpoint behavior, guided workflow canonical replies, or helper-agent task packet/report semantics.
- Do not reopen legacy Claude Code / Agent SDK parity or install-surface cleanup.

## SCOPE LIMITS
- Source scope is evidence reconciliation and closure only: `docs/PI-NATIVE-DELEGATED-APPLY-PACKET-REPORT-CONTRACT.md`, `tests/pi-end-to-end-validation.sh`, and `tests/cross-harness-validation.sh`.
- Lifecycle metadata writes (`.paul/STATE.md`, `.paul/ROADMAP.md`, `.paul/PROJECT.md`, `.paul/MILESTONES.md`, history files, PLAN/SUMMARY) are normal APPLY/UNIFY lifecycle artifacts, not source scope expansion.
- Validation additions must be localized marker checks; no broad harness restructuring or large-file refactor in this phase.
- Delegated APPLY may use repo-local `pals-implementer` only for eligible bounded tasks; parent APPLY remains authoritative for verification, module enforcement, fallback, changed-file scope review, checkpoints, GitHub Flow, and `.paul/*` lifecycle writes.
- Any runtime change, helper-agent contract redesign, dependency/CI change, broad validation classification, legacy cleanup, or lifecycle authority change requires a separate approved plan.
</boundaries>

<verification>
Before declaring APPLY complete:
- [ ] Focused contract/validation grep passes: `grep -n "197/197\|112/112\|199/199\|115/115\|PR #131\|Helper Delegation\|Shared Invariant\|Parent-owned task packet\|structured helper report\|Allowed files:\|Forbidden files:\|Fallback triggers:\|Parent-run official verification:\|changed-file diff\|helper reports as review input\|command-output truth\|old count\|new count\|validation class affected\|shared-invariant preservation rationale\|GitHub Flow safety\|no helper-owned .paul" docs/PI-NATIVE-DELEGATED-APPLY-PACKET-REPORT-CONTRACT.md tests/pi-end-to-end-validation.sh tests/cross-harness-validation.sh`
- [ ] `PALS_ROOT="$PWD" bash drivers/pi/install.sh`
- [ ] `bash tests/pi-end-to-end-validation.sh`
- [ ] `bash tests/cross-harness-validation.sh`
- [ ] `git diff --stat` shows scope stayed inside approved contract/validation files plus normal `.paul/*` lifecycle artifacts.
- [ ] Validation count changes, if any, are captured for UNIFY.
- [ ] GitHub Flow evidence captured: branch, base, ahead/behind, PR URL/state, CI/check state, merge-gate result, base sync, and branch cleanup during UNIFY.
- [ ] v2.50 milestone closure handoff names Pi-Native Validation Classification as the next recommended milestone.
- [ ] All acceptance criteria met.
</verification>

<success_criteria>
- Delegated APPLY packet/report contract records final v2.50 closure baselines and PR #131 evidence.
- Pi validation protects v2.50 Helper Delegation closure markers and passes with command-output evidence.
- Cross-harness validation protects Shared Invariant closure markers and passes with command-output evidence.
- SUMMARY reconciles old/new counts, module reports, helper delegation evidence if used, GitHub Flow evidence, decisions/deviations, and final milestone posture.
- v2.50 closes without runtime, dependency, CI, helper-agent contract, legacy, broad classification, or lifecycle-authority changes.
</success_criteria>

<output>
After completion, create `.paul/phases/217-evidence-reconciliation-milestone-closure/217-01-SUMMARY.md` with acceptance-criteria results, focused marker grep evidence, installer verification, Pi/cross-harness validation output and count reconciliation, module dispatch reports, GitHub Flow evidence, helper delegation evidence if used, decisions/deviations, v2.50 milestone closure, and next milestone handoff.
</output>
