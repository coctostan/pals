---
phase: 127-debug-reproduce-instrument
plan: 02
type: execute
wave: 1
depends_on: ["127-01"]
files_modified: []
autonomous: false
---

<objective>
## Goal
Prove whether the 127-01 fixes actually make modules fire at runtime. Run a real PALS loop, observe dispatch output, and iterate until we have confirmed evidence of module dispatch — or identify what's still broken.

## Purpose
127-01 fixed 7 code patterns based on hypotheses from the v2.23 assessment but never reproduced the failure or confirmed the fixes work. Without runtime evidence, we don't know if modules fire now. This plan closes that gap.

## Output
- Confirmed evidence that modules dispatch (or confirmed evidence of what's still broken)
- If iteration needed: targeted fix + re-test until dispatch is observed
- `.paul/phases/127-debug-reproduce-instrument/127-02-SUMMARY.md`
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/STATE.md

## Prior Work
.paul/phases/127-debug-reproduce-instrument/127-01-SUMMARY.md — fixes applied but not runtime-verified
.paul/references/v223-e2e-assessment.md — original 7-failure analysis

## Source Files
~/.pi/agent/skills/pals/modules.yaml — installed module registry (18 modules)
~/.pi/agent/skills/pals/paul-plan/SKILL.md — installed skill with fixes
~/.pi/agent/skills/pals/workflows/plan-phase.md — installed workflow with fixes
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: 13 modules checked, 0 findings (no source files in scope — test/reproduction plan)
[dispatch] pre-plan enforcement: 3 modules checked, 0 blocks (no source files in scope)
</module_dispatch>

<acceptance_criteria>

## AC-1: Runtime Dispatch Evidence
```gherkin
Given the 127-01 fixes are installed at ~/.pi/agent/skills/pals/
When a PALS lifecycle action (plan, apply, or unify) runs
Then at least one [dispatch] log line appears in the output
And the log line references a real module name from modules.yaml
```

## AC-2: Root Cause Confirmed
```gherkin
Given the v2.23 E2E showed zero module dispatch
When we observe the current runtime behavior
Then we can state with evidence which specific failure(s) caused the zero-dispatch
And whether the 127-01 fixes resolved them
```

## AC-3: Iteration If Needed
```gherkin
Given modules may still not fire after the 127-01 fixes
When dispatch is still absent after the first test
Then we diagnose the actual root cause from the observed behavior
And apply a targeted fix and re-test until dispatch is confirmed
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Test Module Dispatch in This Session</name>
  <files></files>
  <action>
    We are currently IN a PALS session with the fixed installed files. Test dispatch right now:

    1. **Read the installed plan-phase.md pre_plan_advisory_hooks step** and follow it literally against this plan's context:
       - Read `~/.pi/agent/skills/pals/modules.yaml`
       - Resolve modules with `pre-plan` hooks
       - For each: load the listed refs, follow the description
       - Emit the dispatch log line

    2. **Record exactly what happens:**
       - Did we successfully read modules.yaml? (path resolution)
       - Did we find pre-plan hooks? (registry parsing)
       - Did each module produce output or skip? (dispatch execution)
       - Was a `[dispatch]` log line emitted? (logging)

    3. **Compare against v2.23 behavior:**
       - v2.23: zero dispatch evidence across 4 phases
       - Now: what do we observe?

    Note: This plan has no source code files, so most modules will correctly skip ("no UI files", "no API files", etc.). That's FINE — a skip with a log line IS evidence of dispatch. The failure was zero log lines, not zero findings.
  </action>
  <verify>
    Confirm at least one of these is true:
    - A `[dispatch]` log line was emitted during this planning session
    - Module names from modules.yaml appeared in dispatch output
    - Module skip messages ("ARIA: No UI files in planned changes — skipping") appeared
    
    If NONE of the above: dispatch is still broken and we need Task 2.
  </verify>
  <done>AC-1 and AC-2 satisfied: runtime dispatch behavior observed and documented.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Based on Task 1 results — did module dispatch work?</decision>
  <context>Task 1 tested dispatch in a live session. The result determines whether we're done or need to iterate.</context>
  <options>
    <option id="dispatch-confirmed">
      <name>Dispatch confirmed — modules fired (even if they skipped due to no applicable files)</name>
      <pros>Phase 127 is complete. Evidence captured.</pros>
      <cons>None — this is the success path.</cons>
    </option>
    <option id="dispatch-still-broken">
      <name>Dispatch still broken — no evidence of modules being consulted</name>
      <pros>We now have runtime evidence of the actual failure, not just code patterns.</pros>
      <cons>Need to diagnose and fix in Task 3.</cons>
    </option>
  </options>
  <resume-signal>Select: dispatch-confirmed or dispatch-still-broken</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Diagnose and Fix (only if dispatch-still-broken)</name>
  <files></files>
  <action>
    **Only runs if Task 2 selected dispatch-still-broken.**

    1. From the Task 1 observations, identify exactly WHERE dispatch fails:
       - Does the model read modules.yaml at all?
       - Is the path resolution wrong?
       - Does it parse the registry but not follow hook descriptions?
       - Is there a context/instruction ordering problem?

    2. Apply the minimum targeted fix to the identified root cause.

    3. Reinstall: `bash install.sh`

    4. Re-test: repeat Task 1's dispatch attempt with the fix in place.

    5. Iterate until dispatch is confirmed or we've exhausted hypotheses and need human intervention.
  </action>
  <verify>
    After fix: repeat the Task 1 verification checks.
    At least one [dispatch] log or module output must appear.
  </verify>
  <done>AC-3 satisfied: iterated until dispatch confirmed or blocker identified.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- The 127-01 fixes (already committed and PR'd) — build on them, don't revert
- modules/*.yaml source manifests
- pals.json config

## SCOPE LIMITS
- This plan tests and iterates — it does not redesign the module system
- If the root cause is architectural (not fixable with targeted edits), document it and defer to Phase 128
- Maximum 3 fix-and-retest iterations before escalating

</boundaries>

<verification>
Before declaring plan complete:
- [ ] At least one runtime dispatch test was performed (not just grep)
- [ ] Evidence of module dispatch (or confirmed absence) is documented
- [ ] If fixes were needed beyond 127-01, they are committed and installed
- [ ] Root cause is confirmed with evidence, not hypothesized
</verification>

<success_criteria>
- Runtime evidence that modules dispatch (skip messages count as dispatch)
- OR: confirmed root cause of continued failure with targeted fix applied
- Root cause is no longer a hypothesis — it's documented with observed behavior
</success_criteria>

<output>
After completion, create `.paul/phases/127-debug-reproduce-instrument/127-02-SUMMARY.md`
</output>
