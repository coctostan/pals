---
phase: 208-documentation-validation-surfacing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - README.md
  - CHANGELOG.md
  - drivers/pi/extensions/README.md
  - drivers/pi/skill-map.md
  - kernel/references/pals-json-schema.md
  - tests/pi-end-to-end-validation.sh
  - tests/cross-harness-validation.sh
autonomous: true
---

<objective>
## Goal
Surface the Phase 207 guided workflow runtime guarantees in Pi-facing documentation, guided workflow settings documentation, and focused validation markers so reviewers can see that guided workflow UI sends transcript-visible canonical replies only after explicit user action and cannot auto-approve, auto-continue, skip checkpoints, infer merge intent, or become lifecycle authority.

## Purpose
v2.48 must make guided workflow evidence understandable and command-verifiable after runtime hardening. This plan turns the Phase 206 contract plus Phase 207 implementation into user/reviewer-facing docs and validation checks while preserving `.paul/*` artifact truth, PLAN/APPLY/UNIFY ownership, module dispatch evidence, GitHub Flow gates, parent-owned APPLY, command-output validation truth, and full authoritative reads for consequential work.

## Output
- README and Pi driver docs describe the canonical guided workflow reply contract, explicit confirm/select-only delivery, notify-only no-reply behavior, checkpoint blocking, merge-gate routing boundaries, and non-authority of UI state.
- `kernel/references/pals-json-schema.md` documents `guided_workflow.auto_present` as display-only configuration, not approval/checkpoint/lifecycle state.
- Validation scripts include focused markers for Guided UI Safety, Pi-Supported Runtime, Shared Invariant, GitHub Flow Safety, Runtime Lens, Artifact / Process evidence, and command-output truth.
- `CHANGELOG.md` notes the documentation and validation surfacing under Unreleased.
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md

## Prior Work
.paul/phases/207-runtime-guided-reply-hardening/207-01-SUMMARY.md
docs/PI-NATIVE-GUIDED-WORKFLOW-EVIDENCE-CONTRACT.md
drivers/pi/extensions/pals-hooks.ts

## Source Files
.gitignore
README.md
CHANGELOG.md
drivers/pi/extensions/README.md
drivers/pi/skill-map.md
kernel/references/pals-json-schema.md
tests/pi-end-to-end-validation.sh
tests/cross-harness-validation.sh
</context>

<module_dispatch>
## Pre-Plan Module Dispatch

Planning posture: `high` collaboration from `pals.json` project default; direct-requirements mode because `.paul/ROADMAP.md`, the Phase 207 SUMMARY, and `docs/PI-NATIVE-GUIDED-WORKFLOW-EVIDENCE-CONTRACT.md` explicitly define Phase 208 documentation and validation surfacing scope.

[dispatch] pre-plan advisory: modules.yaml loaded from installed PALS registry (kernel_version 2.0.0); Phase 208 scope reviewed for Pi docs, skill-map/settings documentation, and focused validation marker surfacing.

| Module / Cohort | Result |
|-----------------|--------|
| TODD | Existing validation is shell/TAP based. No Jest/Vitest unit-test harness applies. Plan type remains `execute`; APPLY must run focused marker greps plus full Pi and cross-harness validation. |
| IRIS | No TODO/FIXME/HACK/XXX patterns found in planned files. Review flags: docs and markers must not overclaim runtime authority; validation assertions must protect no-auto/no-UI-only/checkpoint/merge-gate boundaries rather than create brittle prose-only checks. |
| DAVE | No CI config was discovered in repo scope; this plan does not add CI/CD files. GitHub Flow evidence remains an APPLY/UNIFY responsibility, not a Pi UI action. |
| DOCS | This is a docs/validation surfacing phase. Planned docs are `README.md`, `drivers/pi/extensions/README.md`, `drivers/pi/skill-map.md`, `kernel/references/pals-json-schema.md`, and `CHANGELOG.md`; tests must verify the new docs markers remain discoverable. |
| CODI | `[dispatch] CODI: skipped — no extractable symbols in phase scope`; planned source selectors are Markdown and shell validation files, not bounded TS/JS source changes. No magic inference was used. |
| RUBY | Source-size awareness: validation scripts are already large (`tests/pi-end-to-end-validation.sh` 1419 lines, `tests/cross-harness-validation.sh` 1063 lines). Keep additions focused and do not refactor validation harness structure in this phase. |
| ARCH | Repository pattern is documentation/kernel/driver/module/test oriented. Planned docs belong in README/driver/kernel references; planned checks belong in tests. Existing test-file size debt is pre-existing and not a block for focused marker additions. |
| ARIA / LUKE / GABE / DANA / OMAR / PETE / REED / VERA | No UI component, API endpoint, data model/migration, observability runtime, performance path, resilience runtime policy, or privacy schema changes are planned. |

[dispatch] pre-plan enforcement: DEAN PASS — no root dependency manifest or lockfile detected, no dependency manifests are planned for modification, and no critical/high vulnerability block is raised. SETH PASS — no hardcoded secret patterns found in planned docs/validation files and no env/auth files are planned. ARCH PASS — planned files are in expected documentation/reference/test layers; no circular dependency risk applies.

## Post-Plan Module Dispatch

[dispatch] post-plan: TODD reviewed plan type; no TDD conversion applied because the work uses existing shell/TAP validation and focused marker checks rather than a JS unit-test harness.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | All PLAN/APPLY/UNIFY lifecycle work | ✓ |
| /carl | required | Automatic session-boundary monitoring at phase boundaries and safety thresholds | automatic |
| TODD | required | APPLY validation/test enforcement | ✓ via module dispatch |
| WALT | required | APPLY/UNIFY quality gating and validation history | ✓ via module dispatch |

**BLOCKING:** Required skills must remain available before APPLY proceeds.
</skills>

<acceptance_criteria>

## AC-1: Pi docs surface the canonical reply contract
```gherkin
Given Phase 207 hardened guided workflow runtime replies
When a user or reviewer reads the Pi-facing docs
Then they can see that guided workflow UI sends exact canonical replies through normal transcript-visible user-message flow only after explicit confirm/select action
```

## AC-2: Guided workflow settings are documented as display-only
```gherkin
Given `guided_workflow.auto_present` toggles whether Pi presents confirm/select UI or notify-only surfaces
When `kernel/references/pals-json-schema.md` describes those settings
Then the docs state that the settings are display behavior only and cannot approve, continue, complete checkpoints, write lifecycle state, or replace artifacts/command output
```

## AC-3: No-auto and no-UI-only boundaries are validation-protected
```gherkin
Given guided workflow UI can surface plan review, APPLY approval, checkpoints, resume-next, continue-to-UNIFY, phase/milestone transition, and merge-gate routing prompts
When validation runs
Then focused checks protect no auto-approval, no auto-continue, no skipped checkpoints, no UI-only lifecycle decisions, notify-only no-reply behavior, canonical send path, and no inferred merge intent markers
```

## AC-4: GitHub Flow and command-output truth remain explicit
```gherkin
Given merge-gate routing may mention branch, PR, CI, review, merge, or cleanup state
When docs and validation describe guided workflow evidence
Then Pi is shown as route assistance only while git/gh command output, PR state, CI state, validation command output, and workflow artifacts remain authoritative
```

## AC-5: Validation stays green and evidence is reconciled
```gherkin
Given this phase changes docs and validation marker scripts
When APPLY completes
Then focused marker greps, `PALS_ROOT="$PWD" bash drivers/pi/install.sh`, `bash tests/pi-end-to-end-validation.sh`, and `bash tests/cross-harness-validation.sh` pass with fresh command-output evidence and no out-of-scope files changed
```
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Surface guided workflow evidence in Pi-facing docs</name>
  <files>README.md, CHANGELOG.md, drivers/pi/extensions/README.md, drivers/pi/skill-map.md, kernel/references/pals-json-schema.md</files>
  <action>
    Update user/reviewer-facing documentation to reflect Phase 207 runtime guarantees without changing runtime behavior:
    - In `README.md`, replace outdated guided workflow language that suggests auto-approval with language about canonical transcript replies, explicit confirm/select user action, notify-only no-reply behavior, checkpoint blocking, and artifact/command-output authority.
    - In `drivers/pi/extensions/README.md`, expand `Guided Workflow UX` to name the moment inventory including `merge-gate-routing`, explain `sendCanonicalWorkflowResponse` / normal user-message delivery, and state no auto-approval, no auto-continue, no skipped checkpoints, no UI-only lifecycle decisions, no inferred merge intent, no hidden Pi state, and no Pi-owned lifecycle/module/validation ledger.
    - In `drivers/pi/skill-map.md`, add concise mapping notes showing guided workflow UX is an adapter-side reply-routing aid, not a standalone skill or lifecycle authority; include canonical reply and full-authoritative-read requirements.
    - In `kernel/references/pals-json-schema.md`, clarify that every `guided_workflow.auto_present.*` key is a display toggle only; `true` can auto-present UI, but neither `true` nor `false` can approve, continue, complete checkpoints, infer merge intent, write lifecycle state, or replace command-output evidence.
    - In `CHANGELOG.md`, add an Unreleased entry for guided workflow documentation and validation surfacing.
    Avoid: editing `drivers/pi/extensions/pals-hooks.ts`, changing `pals.json` defaults, adding dependencies, broad README rewrites, legacy Claude/SDK support changes, workflow/resource capsules, helper packet/report compression, validation classification redesign, or lifecycle-authority changes.
  </action>
  <verify>grep -n "canonical transcript\|explicit confirm/select\|notify-only\|no auto-approval\|no auto-continue\|no skipped checkpoints\|no UI-only lifecycle decisions\|no inferred merge intent\|display-only" README.md drivers/pi/extensions/README.md drivers/pi/skill-map.md kernel/references/pals-json-schema.md CHANGELOG.md</verify>
  <done>AC-1, AC-2, and AC-4 satisfied for Pi-facing docs and settings documentation.</done>
</task>

<task type="auto">
  <name>Task 2: Add focused guided workflow validation markers</name>
  <files>tests/pi-end-to-end-validation.sh, tests/cross-harness-validation.sh</files>
  <action>
    Add focused marker checks that protect the Phase 206/207 guided workflow evidence contract without refactoring the validation harnesses:
    - In `tests/pi-end-to-end-validation.sh`, add Pi-supported runtime and guided UI safety checks that verify docs/runtime markers for the named guided moment inventory, canonical reply send path, explicit confirm/select action, notify-only no-reply behavior, display-only config, no auto-approval, no auto-continue, no skipped checkpoints, no UI-only lifecycle decisions, no inferred merge intent, and no hidden Pi state/Pi-owned ledger language.
    - In `tests/cross-harness-validation.sh`, add shared invariant / GitHub Flow safety / command-output truth checks that verify docs preserve `.paul/*`, PLAN/APPLY/UNIFY, parent-owned APPLY, module evidence, GitHub Flow command evidence, validation command output, full authoritative reads, and no UI-only lifecycle authority.
    - Keep checks structural and marker-family based; prefer `tap_file_contains_all` or small grep groups over fragile long exact prose.
    - Ensure TAP counts increase naturally through the existing dynamic `TOTAL` summary; do not hardcode expected totals.
    Avoid: changing runtime behavior, CI config, dependency manifests, installer semantics, broad validation classification, or unrelated validation families.
  </action>
  <verify>grep -n "Guided UI Safety\|Pi-Supported Runtime\|Shared Invariant\|GitHub Flow Safety\|command-output truth\|canonical reply\|no UI-only lifecycle" tests/pi-end-to-end-validation.sh tests/cross-harness-validation.sh</verify>
  <done>AC-3, AC-4, and AC-5 satisfied for focused validation marker surfacing.</done>
</task>

<task type="auto">
  <name>Task 3: Install and validate documentation surfacing evidence</name>
  <files>.gitignore, README.md, CHANGELOG.md, drivers/pi/extensions/README.md, drivers/pi/skill-map.md, kernel/references/pals-json-schema.md, tests/pi-end-to-end-validation.sh, tests/cross-harness-validation.sh</files>
  <action>
    Verify the docs/validation surfacing without expanding Phase 208 scope:
    - Run the focused marker greps from Tasks 1 and 2 and preserve matched output for SUMMARY evidence.
    - Run `PALS_ROOT="$PWD" bash drivers/pi/install.sh` to confirm installed Pi surfaces still generate correctly after docs/validation changes.
    - Run `bash tests/pi-end-to-end-validation.sh` and record TAP summary counts.
    - Run `bash tests/cross-harness-validation.sh` and record TAP summary counts.
    - Review `git diff --stat` and changed files; source changes outside normal `.paul/*` lifecycle artifacts must stay limited to `.gitignore` workguard-ignore housekeeping plus the approved docs and validation files in this plan.
    Avoid: modifying runtime source, installed runtime copies outside the supported installer path, dependencies, CI, generated reports, workflow/resource capsules, delegated APPLY compression, broad validation classification, legacy cleanup, telemetry, hidden state, helper-owned APPLY, or merge-gate automation.
  </action>
  <verify>grep -n "canonical transcript\|explicit confirm/select\|display-only\|Guided UI Safety\|Shared Invariant\|command-output truth" README.md drivers/pi/extensions/README.md drivers/pi/skill-map.md kernel/references/pals-json-schema.md tests/pi-end-to-end-validation.sh tests/cross-harness-validation.sh && PALS_ROOT="$PWD" bash drivers/pi/install.sh && bash tests/pi-end-to-end-validation.sh && bash tests/cross-harness-validation.sh</verify>
  <done>AC-1 through AC-5 satisfied with install and fresh command-output validation evidence.</done>
</task>
</tasks>

<boundaries>

## DO NOT CHANGE
- `drivers/pi/extensions/pals-hooks.ts` runtime behavior; Phase 207 already completed runtime hardening.
- Installed runtime copies outside the repo except through the supported installer command `PALS_ROOT="$PWD" bash drivers/pi/install.sh`.
- `.paul/*` lifecycle artifacts except normal PLAN/APPLY/UNIFY lifecycle writes by the parent workflow.
- `docs/PI-NATIVE-GUIDED-WORKFLOW-EVIDENCE-CONTRACT.md` contract content unless a contradiction is found and replanning is approved.
- Dependency manifests, lockfiles, CI configuration, generated validation reports, GitHub Flow automation, module registry/source behavior, command wrappers, helper-agent contracts, CARL session-boundary semantics, artifact-slice behavior, Claude Code wrappers, Agent SDK surfaces, portability docs, or frozen legacy parity surfaces.

## SCOPE LIMITS
- Implement only docs/skill-map/settings documentation and focused validation marker surfacing for guided workflow evidence.
- Do not implement workflow/resource capsules, delegated APPLY packet/report compression, broad Pi-native validation classification, legacy cleanup, dependency additions, CI changes, telemetry, hidden state, auto-approval, helper-owned APPLY, or merge-gate automation.
- Do not let Pi UI state mark PLAN approval, APPLY start/completion, checkpoint completion, validation pass/fail, GitHub Flow readiness, merge intent, module dispatch completion, or UNIFY completion without transcript/artifact/command evidence.
- Do not use a helper agent for `.paul/*` lifecycle writes or official verification; parent APPLY remains authoritative.
</boundaries>

<verification>
Before declaring APPLY complete:
- [ ] Focused marker grep over docs proves canonical transcript replies, explicit confirm/select action, notify-only no-reply behavior, display-only `guided_workflow.auto_present`, checkpoint blocking, no auto-approval, no auto-continue, no skipped checkpoints, no UI-only lifecycle decisions, no inferred merge intent, GitHub Flow command evidence, validation command output, and `.paul/*` authority.
- [ ] Focused marker grep over validation scripts proves Guided UI Safety, Pi-Supported Runtime, Shared Invariant, GitHub Flow Safety, Runtime Lens, Artifact / Process, and command-output truth marker families are covered.
- [ ] `PALS_ROOT="$PWD" bash drivers/pi/install.sh` passes.
- [ ] `bash tests/pi-end-to-end-validation.sh` passes with fresh TAP summary counts.
- [ ] `bash tests/cross-harness-validation.sh` passes with fresh TAP summary counts.
- [ ] `git diff --stat` and changed-file review confirm no out-of-scope runtime/dependency/CI/legacy files changed outside `.gitignore`, approved docs/validation files, and normal `.paul/*` lifecycle artifacts.
- [ ] All acceptance criteria are satisfied and ready for SUMMARY evidence.
</verification>

<success_criteria>
- All three tasks completed.
- Pi-facing docs and settings documentation accurately describe guided workflow reply evidence and non-authority boundaries.
- Validation scripts protect the docs/runtime evidence markers without brittle long-prose assertions.
- Installer, Pi validation, and cross-harness validation pass.
- Scope remains limited to `.gitignore` workguard-ignore housekeeping, approved docs and validation files, and normal lifecycle artifacts.
</success_criteria>

<output>
After completion, create `.paul/phases/208-documentation-validation-surfacing/208-01-SUMMARY.md` with acceptance-criteria results, changed-file evidence, validation command output including TAP counts, module reports, GitHub Flow evidence, decisions/deviations, and Phase 209 readiness.
</output>
