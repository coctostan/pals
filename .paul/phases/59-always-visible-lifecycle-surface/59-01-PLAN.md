---
phase: 59-always-visible-lifecycle-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - drivers/pi/extensions/pals-hooks.ts
  - tests/pi-end-to-end-validation.sh
autonomous: true
---

<objective>
## Goal
Implement an always-visible Pi-native PALS lifecycle surface that continuously shows milestone/phase, loop status, and next recommended action from `.paul/STATE.md` without creating Pi-only lifecycle truth.

## Purpose
This phase establishes the first part of the v2.4 Pi Native UX Layer by making PALS feel continuously present in Pi while preserving the artifact-safe architecture and interoperability with Claude Code.

## Output
- Updated Pi extension with persistent lifecycle visibility surfaces
- Validation coverage proving the always-visible lifecycle contract
- Plan summary artifact at `.paul/phases/59-always-visible-lifecycle-surface/59-01-SUMMARY.md`
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md
.paul/phases/55-recommendations-and-roadmap/pi-recommendations-ranked.md
.paul/phases/55-recommendations-and-roadmap/pi-roadmap-now-vs-future.md

## Prior Work (only if genuinely needed)
.paul/phases/56-hook-and-context-strategy/56-01-SUMMARY.md
.paul/phases/57-command-routing-discoverability/57-01-SUMMARY.md
.paul/phases/58-adapter-consistency-cleanup/58-01-SUMMARY.md

## Source Files
drivers/pi/extensions/pals-hooks.ts
drivers/pi/extensions/README.md
tests/pi-end-to-end-validation.sh
/opt/homebrew/lib/node_modules/@mariozechner/pi-coding-agent/docs/extensions.md
/opt/homebrew/lib/node_modules/@mariozechner/pi-coding-agent/docs/tui.md
</context>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | Before APPLY for standard PALS phase execution | ○ |

**BLOCKING:** Required skills MUST be loaded before APPLY proceeds.
Run each skill command or confirm already loaded.

## Skill Invocation Checklist
- [ ] /paul loaded (standard PALS lifecycle flow)
</skills>

<acceptance_criteria>
## AC-1: Persistent lifecycle status is visible in Pi
```gherkin
Given a Pi session in a project with `.paul/STATE.md`
When the PALS Pi extension loads and the project state can be parsed
Then Pi shows an always-visible lifecycle surface with the current milestone/phase, loop status, and next recommended action derived from `.paul/STATE.md`
```

## AC-2: Visibility remains adapter-only and artifact-safe
```gherkin
Given the always-visible lifecycle surface is active
When Pi displays or refreshes that surface
Then the extension reads shared artifacts for lifecycle truth, does not create Pi-only state authority, and does not expand automatic workflow-context injection beyond the established guardrails
```

## AC-3: The lifecycle surface is lightweight and regression-checked
```gherkin
Given the updated extension and validation suite
When the Pi adapter validation runs
Then it verifies the presence of the always-visible lifecycle UI contract and confirms existing command/discovery/context guardrails still hold
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Add a reusable lifecycle snapshot model for Pi UI surfaces</name>
  <files>drivers/pi/extensions/pals-hooks.ts</files>
  <action>
    Refactor the Pi extension so the STATE.md parsing logic can produce a compact lifecycle snapshot suitable for both notifications and persistent UI surfaces.
    - Extend the current parsing beyond phase/loop/next action as needed to include milestone information if it can be extracted reliably from `.paul/STATE.md`
    - Normalize fallback behavior so missing or partial state clears or degrades the persistent UI cleanly rather than leaving stale output behind
    - Keep `.paul/STATE.md` as the only lifecycle truth source for the visible surface
    Avoid: introducing Pi-managed lifecycle persistence, hidden derived state that can outlive artifact changes, or any parsing branch that assumes Claude Code-specific runtime behavior
  </action>
  <verify>grep -n "setStatus\|setWidget\|Milestone\|Next action\|parsePalsState" drivers/pi/extensions/pals-hooks.ts</verify>
  <done>AC-1 satisfied in part: the extension has a reusable artifact-grounded lifecycle snapshot that can drive always-visible Pi surfaces</done>
</task>

<task type="auto">
  <name>Task 2: Render an always-visible PALS lifecycle surface in Pi</name>
  <files>drivers/pi/extensions/pals-hooks.ts</files>
  <action>
    Use documented Pi extension UI primitives to make PALS continuously visible during interactive sessions.
    - Add a persistent footer status via `ctx.ui.setStatus(...)` for a concise always-visible indicator
    - Add a lightweight persistent widget via `ctx.ui.setWidget(...)` when UI is available so milestone/phase, loop state, and next recommended action remain visible without replacing the built-in footer or creating heavy clutter
    - Refresh or clear the status/widget from appropriate extension events so the surface stays aligned with current `.paul/STATE.md` while remaining additive to the existing session_start/context behavior
    - Keep the UI read-only and presentational in this phase; interactive shortcuts belong to Phase 60
    Avoid: replacing Pi’s core footer entirely, duplicating lifecycle semantics in TypeScript, or adding extra automatic context injection sites beyond the single established one
  </action>
  <verify>grep -n "setStatus\|setWidget\|session_start\|before_agent_start\|agent_end\|turn_end" drivers/pi/extensions/pals-hooks.ts</verify>
  <done>AC-1 and AC-2 satisfied: Pi shows a compact always-visible lifecycle surface sourced from shared artifacts and kept within adapter-only guardrails</done>
</task>

<task type="auto">
  <name>Task 3: Extend Pi validation for the always-visible lifecycle UX contract</name>
  <files>tests/pi-end-to-end-validation.sh</files>
  <action>
    Expand the Pi end-to-end validation suite to lock in the new always-visible lifecycle surface without weakening prior guarantees.
    - Add checks that the extension uses `setStatus` and `setWidget` for the PALS lifecycle surface
    - Add checks that milestone/phase, loop, and next-action visibility are represented in the extension source contract
    - Preserve and re-run the existing guardrail checks for canonical command routing and the single automatic context injection site
    Avoid: brittle assertions tied to formatting trivia that will create false positives, or tests that imply Pi UI becomes the lifecycle truth source
  </action>
  <verify>bash tests/pi-end-to-end-validation.sh</verify>
  <done>AC-3 satisfied: validation covers the always-visible lifecycle surface and confirms prior Pi adapter guardrails still pass</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- Shared kernel workflows, templates, and `.paul/*` lifecycle semantics
- Claude Code driver behavior or Claude-specific install/runtime paths
- Phase 60 scope for interactive shortcuts and flow entry points
- Any persistence layer outside existing artifact files

## SCOPE LIMITS
- This plan implements read-only always-visible Pi lifecycle visibility only
- Do not add clickable or command-triggered shortcuts in this phase
- Do not replace Pi’s built-in footer with a custom footer
- Do not broaden automatic workflow-context injection beyond the current single injection-site guardrail
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `grep -n "setStatus\|setWidget" drivers/pi/extensions/pals-hooks.ts` shows persistent UI surfaces were added
- [ ] `grep -n "Milestone\|Phase\|Loop\|Next" drivers/pi/extensions/pals-hooks.ts` confirms lifecycle fields are surfaced
- [ ] `bash tests/pi-end-to-end-validation.sh` passes
- [ ] Always-visible Pi lifecycle UI remains derived from `.paul/STATE.md`
- [ ] All acceptance criteria are met
</verification>

<success_criteria>
- All three tasks completed
- Pi extension exposes a persistent lifecycle status and lightweight widget
- The visible surface shows milestone/phase, loop state, and next action from shared artifacts
- No Pi-only lifecycle truth or extra auto-injection sites are introduced
- Validation passes without regressing existing Pi adapter checks
</success_criteria>

<output>
After completion, create `.paul/phases/59-always-visible-lifecycle-surface/59-01-SUMMARY.md`
</output>
