---
phase: 95-test-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
---

<objective>
## Goal
Open a cmux split pane and launch Pi with kimi-coder model. The Pi session (acting as a human user) will create the pi-verify project from scratch, set up the GitHub repo, and prepare for Phase 96. Orchestrator only observes and records.

## Purpose
This is pure human simulation — Pi does EVERYTHING a real user would do, starting from an empty terminal.

## Output
- cmux split pane opened (right of current)
- Pi launched in test pane with `--model "kimi-coder/kimi-for-coding"`
- Pi creates GitHub repo `coctostan/pi-verify`
- Pi clones pi-extension-template and initializes project
- PALS availability confirmed in test pane
- Orchestrator has captured the surface reference for Phase 96
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md

## Technical Context
cmux CLI: new-split, send, read-screen, identify
model: kimi-coder/kimi-for-coding
template: https://github.com/ayagmar/pi-extension-template
target project: pi-verify (to be created by Pi in test pane)
</context>

<acceptance_criteria>

## AC-1: cmux Pane Ready
```gherkin
Given cmux is available
When new-split right is executed
Then a terminal pane exists and is visible
```

## AC-2: Pi Launched with Correct Model
```gherkin
Given the test pane exists
When Pi is launched with --model "kimi-coder/kimi-for-coding"
Then Pi starts and shows the assistant prompt
```

## AC-3: Pi Creates Project Infrastructure
```gherkin
Given Pi is running in the test pane
When instructed to create pi-verify project
Then Pi executes: gh repo create, git clone template, copy files, push to main
```

## AC-4: PALS Available
```gherkin
Given the project is set up in the test pane
When /paul-status is typed
Then PALS responds (showing no project or project status)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Open cmux split pane</name>
  <files>(no source files)</files>
  <action>
    1. Create right split: `cmux new-split right`
    2. Capture the new surface reference (e.g., surface:18) from output
    3. Store surface ref for Phase 96
    4. Verify pane exists: `cmux list-panes`
  </action>
  <verify>
    - `cmux list-panes` shows 2 panes
    - New pane is visible in UI
  </verify>
  <done>AC-1 satisfied</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>cmux split pane opened and ready</what-built>
  <how-to-verify>
    1. Verify you can see the new terminal pane to the right
    2. Confirm you're ready for me to launch Pi in that pane
  </how-to-verify>
  <resume-signal>Type "approved" to launch Pi in the test pane</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Launch Pi with kimi-coder model</name>
  <files>(no source files)</files>
  <action>
    1. In test pane, launch Pi: `cmux send --surface <ref> "pi --model 'kimi-coder/kimi-for-coding'"`
    2. Wait 5 seconds for Pi to start
    3. Read screen: `cmux read-screen --surface <ref> --lines 20`
    4. Verify Pi started (look for assistant greeting)
    5. Send initial context: `cmux send --surface <ref> "You are testing PALS (Project Automation & Lifecycle System). Create a Pi extension called pi-verify that auto-detects project type and runs verification checks. Start by creating a GitHub repo at coctostan/pi-verify, then clone https://github.com/ayagmar/pi-extension-template and set up the project."`
  </action>
  <verify>
    - Screen shows Pi assistant prompt
    - Pi has context about the task
  </verify>
  <done>AC-2 satisfied</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Pi launched with kimi-coder, given task context</what-built>
  <how-to-verify>
    1. Look at the right pane — you should see Pi running with kimi model
    2. The context about creating pi-verify should be visible
    3. Confirm Pi is responsive before proceeding
  </how-to-verify>
  <resume-signal>Type "approved" to have Pi create the project infrastructure</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Pi creates project infrastructure</name>
  <files>(no source files — all work done by Pi in test pane)</files>
  <action>
    Guide Pi through creating the project:
    
    1. Send: `cmux send --surface <ref> "Create a GitHub repo called coctostan/pi-verify using gh repo create"`
    2. Wait 10 seconds, read screen to see result
    3. Send: `cmux send --surface <ref> "Clone the template: git clone https://github.com/ayagmar/pi-extension-template.git /tmp/pi-template"`
    4. Wait 5 seconds
    5. Send: `cmux send --surface <ref> "Clone your new repo: git clone https://github.com/coctostan/pi-verify.git ~/pi-verify"`
    6. Wait 5 seconds
    7. Send: `cmux send --surface <ref> "Copy template files: cp -r /tmp/pi-template/* ~/pi-verify/ && cp /tmp/pi-template/.gitignore ~/pi-verify/"`
    8. Wait 3 seconds
    9. Send: `cmux send --surface <ref> "cd ~/pi-verify && git add -A && git commit -m 'Initial commit from template' && git push origin main"`
    10. Wait 10 seconds, read screen to verify push succeeded
  </action>
  <verify>
    - `gh repo view coctostan/pi-verify` shows the repo exists
    - Repo has files from template (src/index.ts, package.json, etc.)
  </verify>
  <done>AC-3 satisfied</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Pi created the pi-verify project infrastructure</what-built>
  <how-to-verify>
    1. Check https://github.com/coctostan/pi-verify exists with template files
    2. The Pi session should show the successful push
    3. Confirm ready to test PALS availability
  </how-to-verify>
  <resume-signal>Type "approved" to test PALS in the Pi session</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Verify PALS availability</name>
  <files>(no source files)</files>
  <action>
    1. Send PALS command: `cmux send --surface <ref> "/paul-status"`
    2. Wait 3 seconds
    3. Read screen: `cmux read-screen --surface <ref> --lines 30`
    4. Verify PALS responds (look for "No project found" or project status output)
    5. Record: PALS available = yes/no
  </action>
  <verify>
    - Screen shows PALS output (not "command not found")
  </verify>
  <done>AC-4 satisfied</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- No modifications to pals project source
- No CLI commands executed in orchestrator pane (except cmux)
- All project setup work happens in Pi session (test pane)

## SCOPE LIMITS
- Orchestrator only: open split, launch Pi, send guiding messages, read/verify
- Pi does all real work: gh repo create, git clone, commit, push
- This is a checkpoint-heavy plan — human verifies at each major step

</boundaries>

<verification>
Before declaring plan complete:
- [ ] cmux split pane visible
- [ ] Pi running with kimi-coder model
- [ ] Pi created coctostan/pi-verify repo
- [ ] Pi cloned template and pushed to repo
- [ ] PALS responds to /paul-status
- [ ] Surface reference captured for Phase 96
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- cmux pane operational with Pi running
- pi-verify project exists on GitHub (created by Pi)
- PALS available in test session
- Ready for Phase 96: Pi will run `/paul-init` and full PALS lifecycle
</success_criteria>

<output>
After completion, create `.paul/phases/95-test-infrastructure/95-01-SUMMARY.md`
</output>
