---
phase: 96-init-and-first-loop
plan: 01
type: execute
wave: 1
depends_on: ["95-01"]
files_modified:
  - .paul/phases/96-init-and-first-loop/test-log.md
autonomous: false
---

<objective>
## Goal
Drive Pi in the test pane through project setup, PALS init, and one full PLAN→APPLY→UNIFY loop — building the core pi-verify extension. Record every interaction as pass/fail.

## Purpose
This is the core end-to-end validation. A real AI agent (Kimi for Coding) in Pi will use PALS to build a real extension, exercising init, discuss, milestone, plan, apply, and unify workflows.

## Output
- pi-verify repo created and initialized (by Pi in test pane)
- PALS initialized with GitHub Flow config (by Pi)
- First milestone and phase created (by Pi)
- Core verify command built via PLAN→APPLY→UNIFY (by Pi)
- Test log recording every interaction pass/fail
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/STATE.md

## Prior Work
.paul/phases/95-test-infrastructure/95-01-SUMMARY.md

## Test Pane State
- Surface: surface:23 (pane:22)
- Pi: Kimi for Coding (K2.5), 0% context
- Directory: /Users/maxwellnewman/claude/pi-verify (empty, no git)
- PALS loaded, megapowers disabled
</context>

<acceptance_criteria>

## AC-1: Pi Creates Project
```gherkin
Given Pi is running in the test pane in an empty directory
When the orchestrator sends project setup instructions
Then Pi creates the GitHub repo, clones the template, and commits
```

## AC-2: PALS Init Completes
```gherkin
Given Pi has a git repo with template files
When /paul-init is sent and init questions are answered
Then .paul/ directory exists with STATE.md, ROADMAP.md, PROJECT.md, pals.json
```

## AC-3: First Loop Completes
```gherkin
Given PALS is initialized
When discuss → milestone → plan → apply → unify are driven through Pi
Then a SUMMARY.md exists for the first phase and STATE.md shows loop complete
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Drive Pi to create project and repo</name>
  <files>(test pane — no orchestrator files modified)</files>
  <action>
    Send Pi a natural language prompt via cmux to:
    1. Create GitHub repo: `gh repo create coctostan/pi-verify --public`
    2. Initialize git in current directory
    3. Clone template from https://github.com/ayagmar/pi-extension-template
    4. Copy template files, commit, push
    5. Install dependencies with pnpm

    Orchestrator monitors via read-screen, waits for completion.
    Record: did Pi successfully create the repo and scaffold?
  </action>
  <verify>
    Read screen to confirm Pi completed the setup.
    Check: `cmux read-screen` shows git repo initialized, files committed.
  </verify>
  <done>AC-1 satisfied</done>
</task>

<task type="auto">
  <name>Task 2: Drive Pi through /paul-init</name>
  <files>(test pane — no orchestrator files modified)</files>
  <action>
    1. Send `/paul-init` to Pi via cmux
    2. Monitor screen for PALS init prompts
    3. Answer init questions as they appear:
       - Project description: "Pi extension for smart project verification"
       - Collaboration level: "medium" (or accept default)
       - Modules: accept defaults (all enabled)
       - Git workflow: "GitHub Flow" (option 1)
       - GitHub repo: auto-detect from origin
    4. Wait for init to complete
    5. Record: did each init step succeed? Were questions asked one at a time?
  </action>
  <verify>
    Read screen for "PALS initialized" or similar completion message.
    Verify .paul/ directory created (send `ls .paul/` via Pi or read screen).
  </verify>
  <done>AC-2 satisfied</done>
</task>

<task type="auto">
  <name>Task 3: Drive Pi through first full PALS loop</name>
  <files>.paul/phases/96-init-and-first-loop/test-log.md</files>
  <action>
    Drive Pi through the complete lifecycle:

    a) `/paul-discuss` — milestone discussion
       - Answer: "Build a Pi extension that auto-detects project type and runs verification checks (typecheck, lint, test). Commands: /verify all, /verify test, /verify lint, /verify quick"
       - Accept scope synthesis
       - Approve context file

    b) `/paul-milestone` — create milestone from discussion context
       - Approve milestone creation

    c) `/paul-plan` — plan first phase
       - Approve generated plan

    d) `/paul-apply` — execute the plan
       - Approve execution
       - Monitor Pi building the extension code
       - Answer any checkpoints

    e) `/paul-unify` — reconcile results
       - Approve UNIFY

    After each step, read screen and record:
    - Did PALS route correctly to the next step?
    - Were prompts clear and one-at-a-time?
    - Did Pi (Kimi) understand and execute the PALS workflow?
    - Any errors, hangs, or unexpected behavior?

    Write all results to test-log.md in the orchestrator.
  </action>
  <verify>
    test-log.md exists with pass/fail for each PALS interaction.
    Test pane shows completed loop (PLAN ✓ APPLY ✓ UNIFY ✓).
  </verify>
  <done>AC-3 satisfied</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- No pals kernel/workflow source files modified
- Orchestrator only sends commands and reads output

## SCOPE LIMITS
- One full loop only (second loop in Phase 97)
- Don't fix PALS bugs during this phase — just record them
- If Pi/Kimi gets stuck, record the gap and manually unstick

</boundaries>

<verification>
Before declaring plan complete:
- [ ] pi-verify repo exists on GitHub with template files
- [ ] PALS initialized in test pane project
- [ ] At least one full PLAN→APPLY→UNIFY loop completed
- [ ] test-log.md documents every interaction
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- Pi successfully used PALS to build (or attempt to build) the verify extension
- Every PALS interaction recorded with pass/fail
- Gaps documented for Phase 99 report
</success_criteria>

<output>
After completion, create `.paul/phases/96-init-and-first-loop/96-01-SUMMARY.md`
</output>
