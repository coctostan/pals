---
phase: 231-process-workflow-verbosity-paring
plan: "01"
type: execute
wave: 1
depends_on:
  - "230-01"
files_modified:
  - kernel/references/context-management.md
  - kernel/references/module-dispatch.md
  - kernel/workflows/plan-phase.md
  - kernel/workflows/create-milestone.md
  - kernel/workflows/resume-project.md
  - kernel/workflows/pause-work.md
autonomous: true
---

<objective>
## Goal
Implement the first process/workflow verbosity paring slice by adding shared contextual-verbosity guidance and using it to compact high-frequency PLAN/resume/pause/milestone reply surfaces.

## Purpose
Phase 230 identified repeated banners, option menus, copied template blocks, and no-scope module noise as wasteful when they do not carry evidence. This plan turns that audit into source workflow guidance while preserving lifecycle authority, approval gates, module evidence, validation truth, GitHub Flow safety, parent-owned APPLY, and `.paul/*` artifact truth.

## Output
- Shared contextual verbosity rules in `kernel/references/context-management.md`.
- Compact module-dispatch evidence guidance in `kernel/references/module-dispatch.md`.
- Updated PLAN/milestone/resume/pause workflow reply templates that prefer concise summaries and exactly one next action unless review, blockers, evidence failures, or user request require more detail.
</objective>

<context>
## Project Context
- `.paul/PROJECT.md` — v2.54 current focus and preservation boundaries.
- `.paul/ROADMAP.md` — Phase 231 scope.
- `.paul/STATE.md` — lifecycle state.
- `docs/PALS-CONTEXTUAL-VERBOSITY-AUDIT.md` — Phase 230 source audit and Phase 231 inputs.
- `.paul/phases/230-baseline-verbosity-audit/230-01-SUMMARY.md` — closure evidence and handoff.

## Source Files
- `kernel/references/context-management.md` — best location for shared concise-by-default guidance.
- `kernel/references/module-dispatch.md` — best location for compact skip/evidence-line guidance while preserving one visible dispatch line.
- `kernel/workflows/plan-phase.md` — high-frequency plan review/APPLY routing template.
- `kernel/workflows/create-milestone.md` — high-frequency milestone creation review/next-action template.
- `kernel/workflows/resume-project.md` — high-frequency resume/status routing template and single-next-action contract.
- `kernel/workflows/pause-work.md` — high-frequency session handoff template and resume instruction surface.
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
- `[dispatch] pre-plan advisory: ARCH` planned files are kernel workflow/reference markdown in expected source locations; no architectural boundary violation expected. Keep source/install distinction intact and modify repo source only.
- `[dispatch] pre-plan advisory: TODD` no test files directly cover the planned markdown workflow changes; validation will rely on focused semantic marker checks plus existing Pi/cross-harness shell suites.
- `[dispatch] pre-plan advisory: IRIS` markdown workflow edits should avoid TODO/FIXME/HACK placeholders and preserve explicit blocking/authority markers.
- `[dispatch] pre-plan advisory: DAVE` no CI files in scope; no pipeline changes planned.
- `[dispatch] pre-plan advisory: DOCS` docs/process changes are self-documenting; no README update required in this slice because user-facing docs are best handled in Phase 233 evidence closure.
- `[dispatch] pre-plan advisory: CODI skipped — no extractable symbols in phase scope`; markdown-only workflow/reference scope has no TS/JS source selectors.
- `[dispatch] pre-plan advisory: RUBY` markdown-only process compaction; watch for large workflow surfaces but do not refactor APPLY/UNIFY authority sections in this slice.
- `[dispatch] pre-plan advisory: ARIA/DANA/GABE/LUKE/OMAR/PETE/REED/VERA skipped` no UI, data, API, UX, observability, performance, resilience, or privacy code changes planned.
- `[dispatch] pre-plan enforcement: DEAN PASS/SKIP` no dependency manifests or lockfiles in scope.
- `[dispatch] pre-plan enforcement: SETH PASS` no source or secret-bearing files planned; run focused secret-pattern grep after edits.

## Post-Plan Module Dispatch
- `[dispatch] post-plan: TODD` plan remains `execute`; no TDD restructuring required for markdown workflow/reference edits.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | PLAN/APPLY/UNIFY lifecycle | ✓ |
| /carl | required | Session boundary management | automatic |
| TODD | required | Validation/test enforcement during APPLY | via module dispatch |
| WALT | required | Quality gating during APPLY/UNIFY | via module dispatch |
</skills>

<acceptance_criteria>
## AC-1: Shared contextual verbosity rule exists
```gherkin
Given Phase 230 identified required evidence versus wasteful verbosity
When workflow authors read context-management guidance
Then they see concise-by-default rules that say when to expand and what evidence must stay visible
```

## AC-2: Module evidence remains visible but compact
```gherkin
Given module dispatch evidence is lifecycle proof
When module-dispatch guidance is updated
Then skip/no-scope cases prefer compact evidence lines while preserving warnings, blocking output, and durable summary requirements
```

## AC-3: High-frequency workflow replies are pared
```gherkin
Given plan, milestone, resume, and pause workflows are common user-facing surfaces
When their templates are updated
Then routine output favors concise summaries and one next action without weakening review options, approval gates, or git-aware routing
```

## AC-4: Validation remains green and scope stays bounded
```gherkin
Given workflow/reference guidance changed
When APPLY verification runs
Then focused semantic marker checks, Pi validation, cross-harness validation, and changed-file scope checks pass
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Add shared contextual verbosity and compact dispatch guidance</name>
  <files>kernel/references/context-management.md, kernel/references/module-dispatch.md</files>
  <action>
    Add a concise-by-default contextual verbosity section to `kernel/references/context-management.md` using the Phase 230 audit categories: required evidence, useful context, wasteful/redundant verbosity, deferred complexity.
    Include explicit expansion triggers: blockers, failed/ambiguous verification, enforcement module findings, checkpoint/review decisions, GitHub Flow/CI/merge gates, and direct user request.
    Add compact dispatch-output guidance to `kernel/references/module-dispatch.md`: one visible `[dispatch] ...` line still required, compact skip/no-scope cases preferred, expand only for blocking findings, warnings, failures, or durable summary content.
    Do not remove existing dispatch evidence requirements, module authority rules, or context budget guidance.
  </action>
  <verify>Focused greps find `Contextual Verbosity`, `Concise by default`, `Expansion triggers`, `Required evidence`, `compact skip`, and `[dispatch]` preservation markers in the two reference files.</verify>
  <done>AC-1 and AC-2 satisfied: shared process guidance exists and module evidence remains visible.</done>
</task>

<task type="auto">
  <name>Task 2: Pare high-frequency PLAN/milestone/resume/pause workflow output templates</name>
  <files>kernel/workflows/plan-phase.md, kernel/workflows/create-milestone.md, kernel/workflows/resume-project.md, kernel/workflows/pause-work.md</files>
  <action>
    Update the review/report/route template guidance in these workflows to reference contextual verbosity rules and prefer compact routine output.
    Keep the existing review choices available where a user decision is required, but reduce explanatory filler around them.
    Preserve exact lifecycle gates: explicit APPLY approval, exactly-one-next-action resume routing, GitHub Flow state surfacing, plan review choices, milestone creation artifacts, pause handoff sufficiency, and state/roadmap update requirements.
    Do not edit APPLY/UNIFY workflows, skill wrappers, modules, validation scripts, or runtime code in this plan.
  </action>
  <verify>Focused greps find contextual-verbosity references in all four workflows and still find `exactly ONE`, `Approved, run APPLY`, `Git State`, `ROADMAP.md updated`, `STATE.md updated`, and `Handoff created` guardrails where applicable.</verify>
  <done>AC-3 satisfied: routine workflow replies are pared without weakening required gates.</done>
</task>

<task type="auto">
  <name>Task 3: Verify process paring semantics and scope</name>
  <files>kernel/references/context-management.md, kernel/references/module-dispatch.md, kernel/workflows/plan-phase.md, kernel/workflows/create-milestone.md, kernel/workflows/resume-project.md, kernel/workflows/pause-work.md</files>
  <action>
    Run focused marker checks for new contextual verbosity guidance and preserved lifecycle/evidence guardrails.
    Run `PALS_ROOT="$PWD" bash drivers/pi/install.sh` so installed Pi skill/kernel copies reflect source workflow/reference changes.
    Run `bash tests/pi-end-to-end-validation.sh` and `bash tests/cross-harness-validation.sh`.
    Run changed-file scope checks confirming only the six planned workflow/reference files plus `.paul/*` lifecycle artifacts changed.
    Record results for UNIFY.
  </action>
  <verify>Installer, Pi validation, cross-harness validation, focused marker checks, and Workguard or git scope checks pass.</verify>
  <done>AC-4 satisfied: validation is green and scope remains bounded.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- `kernel/workflows/apply-phase.md`
- `kernel/workflows/unify-phase.md`
- `drivers/pi/skills/*/SKILL.md` (Phase 232 owns skill wrapper paring)
- `modules/**`
- `drivers/pi/extensions/**`
- `tests/**`
- dependency manifests or lockfiles
- GitHub Flow configuration or merge-gate behavior

## SCOPE LIMITS
- This plan is the first Phase 231 slice; it does not need to compact every large workflow.
- Do not weaken lifecycle authority, approvals, checkpoint gates, module dispatch evidence, validation command truth, GitHub Flow safety, parent APPLY authority, or `.paul/*` artifact truth.
- Do not remove existing review paths; make default output concise and expansion conditional.
</boundaries>

<verification>
Before declaring APPLY complete:
- [ ] Focused greps pass for contextual verbosity markers and preserved lifecycle/evidence guardrails.
- [ ] `PALS_ROOT="$PWD" bash drivers/pi/install.sh` passes.
- [ ] `bash tests/pi-end-to-end-validation.sh` passes.
- [ ] `bash tests/cross-harness-validation.sh` passes.
- [ ] Changed-file scope is limited to the six planned source files plus `.paul/*` lifecycle artifacts.
- [ ] All acceptance criteria are met.
</verification>

<success_criteria>
- Shared contextual-verbosity guidance exists.
- Module dispatch guidance supports compact skip/no-scope evidence without hiding required dispatch proof.
- PLAN/milestone/resume/pause workflows default toward concise replies while preserving review, approval, state, handoff, and GitHub Flow semantics.
- Validation remains green.
</success_criteria>

<output>
After completion, create `.paul/phases/231-process-workflow-verbosity-paring/231-01-SUMMARY.md` with acceptance results, verification evidence, changed-file scope, preserved boundaries, and any follow-up inputs for later Phase 231 plans or Phase 232.
</output>
