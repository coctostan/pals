---
phase: 133-greenfield-fast-path
plan: 01
type: execute
wave: 1
depends_on: ["132-01"]
files_modified:
  - kernel/workflows/init-project.md
autonomous: true
---

<objective>
## Goal
Reduce greenfield init to ~8 questions by merging/skipping low-value questions, defaulting modules silently for greenfield, adding grouped module descriptions for brownfield, and creating an explicit default milestone during init.

## Purpose
v2.25 testing (Items 8, 9, 10) showed 17 questions creates onboarding fatigue. By question 10 users just press "skip." The greenfield path should feel fast — get to planning in under 2 minutes.

## Output
Updated `kernel/workflows/init-project.md` with greenfield fast path (~8 questions), explicit milestone creation, and improved module question.
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md
.paul/references/v225-actionable-items.md (Items 8, 9, 10)

## Prior Work
.paul/phases/132-smart-question-gating/132-01-SUMMARY.md (Phase 132 already removed SonarQube + skills questions — Item 12 done)

## Source Files
kernel/workflows/init-project.md
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: TODD(p100) → skip (no tests) | IRIS(p150) → 1 flag: 854 lines | DAVE(p200) → skip | DOCS(p200) → skip | RUBY(p250) → 1 flag: monitor growth
[dispatch] pre-plan enforcement: DEAN(p50) → skip (no lock file)
</module_dispatch>

<acceptance_criteria>

## AC-1: Greenfield Init Asks ~8 Questions
```gherkin
Given a greenfield project with no source files
When init-project runs
Then the user answers approximately 8 questions before artifacts are created
And planning posture defaults to direct-requirements/medium (no question)
And the "problem/opportunity" question is merged into "what are you building"
And the "constraints/questions" step is simplified to a single optional prompt
And modules default to all-enabled with no interactive question
```

## AC-2: Init Creates an Explicit Default Milestone
```gherkin
Given any project completing init
When artifacts are created
Then ROADMAP.md and STATE.md reference "v0.1 Initial Release" as a formally created milestone
And the confirm_and_route display mentions the milestone
And no separate /paul:milestone invocation is needed
```

## AC-3: Module Question Improved for Brownfield
```gherkin
Given a brownfield project completing init
When the module configuration step runs
Then modules are listed with grouped one-liner descriptions by concern
And the user can still toggle with numbers or accept defaults with Enter
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Greenfield fast path — reduce to ~8 questions</name>
  <files>kernel/workflows/init-project.md</files>
  <action>
    Modify the init flow for greenfield projects to reduce interactive questions:

    1. In `determine_planning_posture` step:
       - Add greenfield detection: if `brownfield = false`, skip the exploratory/direct-requirements question and the collaboration level question
       - Default: `planning_mode = "direct-requirements"`, `collaboration_level = "medium"`, `default_collaboration = "medium"`
       - Only ask these questions for brownfield projects (where shaping matters more)
       - Display: "Greenfield project — using direct-requirements mode with medium collaboration."

    2. In `gather_problem_opportunity` step:
       - Merge this into `gather_identity_and_framing` as a combined question
       - New combined Question 1:
         ```
         What are you building, and what problem or opportunity is it addressing?

         (Example: "A CLI tool for managing Docker containers — platform engineers need a safer way to manage container fleets across environments")
         ```
       - Store as both `description` and `problem_opportunity` / `why_now` (parse from the single answer)
       - Delete the separate `gather_problem_opportunity` step (or mark it as brownfield-only)

    3. In `gather_constraints_questions` step:
       - For greenfield: replace the 6-field question with a simpler optional prompt:
         ```
         Any constraints or key decisions to capture? (press Enter to skip)

         (Example: "Must use TypeScript, no external auth services, deploy to Vercel")
         ```
       - Store as `constraints`. Leave `dependencies`, `assumptions`, `open_questions`, `success_signals` as empty/TBD
       - For brownfield: keep the full question (brownfield has more to capture)

    4. In `gather_users_and_needs` step:
       - Keep as-is (already concise, Question 2)

    5. In `gather_scope_shape` step:
       - Keep as-is (already concise, Question 3)

    6. In `gather_project_name` step:
       - Keep as-is (Question 4, often auto-inferred)

    **Resulting greenfield flow (~8 interactions):**
    1. What are you building + why? (merged Q1)
    2. Who are the primary users? (Q2)
    3. Must-have vs deferred? (Q3)
    4. Constraints? (optional, press Enter to skip) (Q4)
    5. Project name confirm (Q5, often auto)
    6. GitHub repo? (Q6)
    7. Git workflow? (Q7, only if GitHub)
    8. Confirm and create

    Avoid: Changing the brownfield flow — it should keep all existing questions.
  </action>
  <verify>Count the interactive questions in the greenfield path by reading each step and checking for greenfield gating. Confirm ~8 questions max for greenfield. Confirm brownfield path retains all original questions.</verify>
  <done>AC-1 satisfied: Greenfield init asks ~8 questions with smart defaults for planning posture and modules.</done>
</task>

<task type="auto">
  <name>Task 2: Add explicit default milestone creation to init</name>
  <files>kernel/workflows/init-project.md</files>
  <action>
    1. In the `create_roadmap_md` step:
       - The template already references "v0.1 Initial Release" — keep this as the formal default milestone
       - Add a comment: "This is the explicitly created default milestone. No separate /paul:milestone needed."

    2. In the `create_state_md` step:
       - Ensure STATE.md references the milestone formally:
         ```
         Milestone: v0.1 Initial Release (created during init)
         ```
       - Update Next action to: "Run /paul:plan to define phases and first plan for v0.1"

    3. In the `confirm_and_route` step:
       - Add the milestone to the confirmation display after the Created block:
         ```
         Milestone: v0.1 Initial Release (default — customize via /paul:milestone)
         ```

    Avoid: Adding a milestone name question — Option 2 from Item 9 (default milestone, no extra question).
  </action>
  <verify>Read `create_roadmap_md`, `create_state_md`, and `confirm_and_route` steps. Confirm all three reference the milestone explicitly. Confirm no new question was added.</verify>
  <done>AC-2 satisfied: Init creates an explicit default milestone with clear references in ROADMAP.md, STATE.md, and the confirmation display.</done>
</task>

<task type="auto">
  <name>Task 3: Improve module question — skip for greenfield, add descriptions for brownfield</name>
  <files>kernel/workflows/init-project.md</files>
  <action>
    In the `configure_modules` step:

    1. Add greenfield gating at the top:
       ```
       **If greenfield (brownfield = false):**
       - All modules enabled by default (silent)
       - Display: "All 18 modules enabled by default."
       - Skip the interactive toggle question
       - Store `module_selections` with all enabled
       - Continue to configure_git
       ```

    2. For brownfield projects, replace the flat numbered list with grouped descriptions:
       ```
       Which PALS modules would you like to enable?
       (All enabled by default — enter numbers to toggle, or press Enter to accept)

       Quality & Testing:
         [1]  TODD  ✓  Enforces test coverage and TDD workflows
         [2]  WALT  ✓  Gates quality (tests, lint, typecheck pass/fail)

       Security & Dependencies:
         [3]  DEAN  ✓  Audits dependencies for known vulnerabilities
         [4]  SETH  ✓  Scans for hardcoded secrets and injection risks

       Code Quality:
         [5]  IRIS  ✓  Reviews code for anti-patterns and smells
         [6]  RUBY  ✓  Flags technical debt and refactoring candidates

       Architecture & APIs:
         [7]  ARCH  ✓  Detects architecture patterns and boundary violations
         [8]  GABE  ✓  Reviews API contracts and conventions

       UI & Accessibility:
         [9]  LUKE  ✓  UI/UX pattern detection and review
         [10] ARIA  ✓  Accessibility assessment and enforcement

       Data & Infrastructure:
         [11] DANA  ✓  Data patterns and migration safety
         [12] DAVE  ✓  CI/CD config validation and deploy checks
         [13] PETE  ✓  Performance pattern detection

       Observability & Resilience:
         [14] OMAR  ✓  Logging and observability review
         [15] REED  ✓  Resilience patterns (timeouts, retries, error handling)

       Privacy & Session:
         [16] VERA  ✓  PII detection and privacy compliance
         [17] CARL  ✓  Session boundary manager (Pi extension)

       Knowledge:
         [18] SKIP  ✓  Captures decisions, rationale, and lessons learned
       ```

    3. Keep the toggle logic unchanged — numbers still toggle modules.

    Avoid: Changing the pals.json template — only the display is changing.
  </action>
  <verify>Read the `configure_modules` step. Confirm: (1) greenfield skips the question silently, (2) brownfield shows grouped descriptions, (3) toggle logic unchanged.</verify>
  <done>AC-3 satisfied: Modules skip silently for greenfield and show grouped descriptions for brownfield.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- kernel/workflows/plan-phase.md
- kernel/workflows/apply-phase.md
- kernel/workflows/unify-phase.md
- Any module files (modules/, references/)
- pals.json structure or template
- PROJECT.md / PRD.md templates (content model unchanged)
- The brownfield flow questions (brownfield keeps full question set)

## SCOPE LIMITS
- Do not add --quick mode (Phase 134)
- Do not change the content of PROJECT.md or PRD.md templates
- Do not change post-init routing (still routes to /paul:plan)
- Module toggle logic stays the same — only the display changes

</boundaries>

<verification>
Before declaring plan complete:
- [ ] Count greenfield questions: should be ~8
- [ ] Confirm brownfield retains all original questions
- [ ] Read `determine_planning_posture` — greenfield defaults silently
- [ ] Read `gather_identity_and_framing` — merged with problem/opportunity
- [ ] Read `gather_constraints_questions` — simplified for greenfield
- [ ] Read `configure_modules` — greenfield skips, brownfield has groups
- [ ] Read `create_roadmap_md` — milestone comment present
- [ ] Read `create_state_md` — milestone reference explicit
- [ ] Read `confirm_and_route` — milestone line present
</verification>

<success_criteria>
- All 3 tasks completed
- Greenfield init ~8 questions (down from ~11 after Phase 132)
- Brownfield flow unchanged except improved module display
- Explicit default milestone in all artifacts
- File stays readable and maintainable
</success_criteria>

<output>
After completion, create `.paul/phases/133-greenfield-fast-path/133-01-SUMMARY.md`
</output>
