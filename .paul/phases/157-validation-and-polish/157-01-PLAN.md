---
phase: 157-validation-and-polish
plan: 01
type: execute
wave: 1
depends_on: ["156-01"]
files_modified: ["tests/pi-end-to-end-validation.sh", "tests/cross-harness-validation.sh", "README.md", "drivers/pi/extensions/README.md", "drivers/pi/skill-map.md", "CHANGELOG.md", ".pi/agents/pals-implementer.md", "drivers/pi/install.sh", "kernel/workflows/apply-phase.md", "drivers/pi/skills/paul-apply/SKILL.md", "drivers/pi/skills/paul-review/SKILL.md", "drivers/pi/extensions/pals-hooks.ts"]
autonomous: true
---

<objective>
## Goal
Validate the PALS-native implementer path in real repo use, harden the automated proof around it, and polish the final Pi operating-model documentation without regressing REV.

## Purpose
Phase 156 added the bounded `pals-implementer` path. Phase 157 needs to prove that the path works as a real APPLY aid, that Pi install/runtime surfaces expose it correctly, that TODD/WALT-style parent authority is preserved, and that REV remains the separate on-demand review path.

## Output
- Stronger Pi validation coverage for agent install, delegated APPLY boundaries, and REV separation
- Updated user-facing docs describing delegated APPLY, Pi agent install boundaries, and final operating model
- Live validation results plus any bounded follow-up polish needed inside the listed file scope
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md

## Prior Work (only if genuinely needed)
.paul/phases/155-pals-implementer-design/pals-implementer-contract.md
.paul/phases/155-pals-implementer-design/prompt-and-module-strategy.md
.paul/phases/155-pals-implementer-design/integration-handoff.md
.paul/phases/156-pals-implementer-integration/156-01-SUMMARY.md

## Source Files
.pi/agents/pals-implementer.md
drivers/pi/install.sh
drivers/pi/extensions/pals-hooks.ts
drivers/pi/extensions/README.md
drivers/pi/skill-map.md
drivers/pi/skills/paul-apply/SKILL.md
drivers/pi/skills/paul-review/SKILL.md
kernel/workflows/apply-phase.md
README.md
CHANGELOG.md
tests/pi-end-to-end-validation.sh
tests/cross-harness-validation.sh
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: TODD(100) → standard test infrastructure not detected from `.test` / `.spec` files or package-manager manifests; repo uses shell validation scripts under `tests/` instead | IRIS(150) → no TODO / FIXME / HACK / XXX signatures found in the planned files | DAVE(200) → no CI/CD config detected in repo root or `.github/workflows/` | DOCS(200) → 88 non-`.paul` markdown files detected; this plan directly updates README / Pi docs / changelog so documentation coverage is in-scope | RUBY(250) → planned files are mostly docs and validation scripts, but `tests/pi-end-to-end-validation.sh` (667 lines) and `tests/cross-harness-validation.sh` (478 lines) are already large; keep changes additive and bounded
[dispatch] pre-plan enforcement: DEAN(50) → skipped gracefully; no supported package-manager audit manifest detected in repo root and no `.paul/dean-baseline.json` exists
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | Core lifecycle execution for this validation/polish phase | ✓ |

**BLOCKING:** Required skills MUST be loaded before APPLY proceeds.
Run each skill command or confirm already loaded.

## Skill Invocation Checklist
- [x] /paul loaded in current session
</skills>

<acceptance_criteria>
## AC-1: Automated validation covers delegated APPLY and REV separation
```gherkin
Given the Pi installer and validation suites in this repo
When Phase 157 is complete
Then the Pi validation suite proves that `pals-implementer` is installed to `~/.pi/agent/agents/`
And the validation coverage distinguishes delegated APPLY (`pals-implementer`) from REV's on-demand `code-reviewer` path
And cross-harness/shared-workflow assertions still pass without implying Pi-only agent installation on non-Pi drivers
```

## AC-2: Live validation proves the operating model end-to-end
```gherkin
Given the repo source for the Pi implementer path
When the installer is run in a temporary HOME and the validation commands are executed
Then the installed Pi runtime contains `pals-implementer.md` in the expected agent directory
And the validation suites pass after the Phase 157 changes
And delegated APPLY evidence remains parent-authoritative while REV behavior stays intact
```

## AC-3: Documentation matches the shipped behavior
```gherkin
Given a user reads the root README and Pi adapter docs
When they look up Pi subagent behavior
Then they can see where Pi skills, extension files, and project-shipped agents are installed
And they can distinguish bounded delegated APPLY from the separate REV review flow
And the changelog records the v2.34 validation/polish outcome without reopening the authority model
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Harden automated proof for delegated APPLY and REV boundaries</name>
  <files>tests/pi-end-to-end-validation.sh, tests/cross-harness-validation.sh</files>
  <action>
    Extend the validation suites so the repo has durable, programmatic proof for the new Pi implementer path.
    - In `tests/pi-end-to-end-validation.sh`, add checks that `drivers/pi/install.sh` installs `.pi/agents/pals-implementer.md` into `~/.pi/agent/agents/`, and that the installed agent still contains the bounded parent-authority contract markers.
    - Add validation assertions that delegated APPLY uses `pals-implementer` while REV remains tied to `/paul:review` and `code-reviewer`; make those assertions text-based and resilient rather than line-number brittle.
    - In `tests/cross-harness-validation.sh`, add only the shared-surface assertions that should hold across installs (for example, shared workflow / Pi skill references that preserve the delegated APPLY vs REV distinction). Do not pretend Claude Code installs Pi agents.
    - Keep the proof additive. Avoid rewriting the existing TAP structure or broadening the suites into unrelated platform checks.
  </action>
  <verify>bash tests/pi-end-to-end-validation.sh && bash tests/cross-harness-validation.sh</verify>
  <done>AC-1 satisfied: automated validation now proves Pi agent install plus delegated APPLY / REV separation.</done>
</task>

<task type="auto">
  <name>Task 2: Document the final Pi subagent operating model</name>
  <files>README.md, drivers/pi/extensions/README.md, drivers/pi/skill-map.md, CHANGELOG.md</files>
  <action>
    Update the human-facing docs so the shipped behavior is explicit and aligned with the Phase 155/156 contract.
    - In `README.md`, document the repo-local `pals-implementer`, the Pi agent install target (`~/.pi/agent/agents/`), the parent-authoritative delegated APPLY model, and the fact that REV remains a separate on-demand review path.
    - In `drivers/pi/extensions/README.md` and `drivers/pi/skill-map.md`, describe the three Pi install surfaces clearly: skills/kernel, extension, and project-shipped agents. Call out that delegated APPLY is bounded and that Pi does not become lifecycle truth.
    - Add a concise `CHANGELOG.md` entry for v2.34 validation/polish covering automated proof, agent installation visibility, and final operating-model documentation.
    - Preserve the current architecture and wording discipline. Do not turn the docs into a generic subagent tutorial or reopen settled design decisions.
  </action>
  <verify>grep -n 'pals-implementer\|agent/agents\|code-reviewer\|/paul:review\|agents\.implementer' README.md drivers/pi/extensions/README.md drivers/pi/skill-map.md CHANGELOG.md</verify>
  <done>AC-3 satisfied: the root and Pi-specific docs clearly describe delegated APPLY, Pi agent install boundaries, and REV separation.</done>
</task>

<task type="auto">
  <name>Task 3: Run live validation and apply bounded follow-up polish</name>
  <files>.pi/agents/pals-implementer.md, drivers/pi/install.sh, kernel/workflows/apply-phase.md, drivers/pi/skills/paul-apply/SKILL.md, drivers/pi/skills/paul-review/SKILL.md, drivers/pi/extensions/pals-hooks.ts, tests/pi-end-to-end-validation.sh, tests/cross-harness-validation.sh, README.md, drivers/pi/extensions/README.md, drivers/pi/skill-map.md, CHANGELOG.md</files>
  <action>
    Use the repo itself to validate the operating model and only apply the smallest follow-up polish that the evidence requires.
    - Run a temporary-HOME Pi install smoke test and confirm `~/.pi/agent/agents/pals-implementer.md` exists after install.
    - Re-run `bash tests/pi-end-to-end-validation.sh` and `bash tests/cross-harness-validation.sh` after all source changes.
    - Perform targeted proof checks for the two subagent paths: delegated APPLY should still reference `pals-implementer` inside the APPLY workflow/skill/extension surfaces, and REV should still reference `code-reviewer` and `/paul:review` without being repurposed.
    - If any validation or doc issue is exposed, make the smallest corrective change only within the listed file scope. Re-run install/validation after each substantive fix.
    - Explicitly avoid unrelated artifact cleanup. Do not modify `.paul/phases/151-research-and-design/rev-design.md` or widen scope into new features, telemetry, or a redesign of delegation semantics.
  </action>
  <verify>TEMP_HOME=$(mktemp -d) && PALS_ROOT=$(pwd) HOME="$TEMP_HOME" bash drivers/pi/install.sh && test -f "$TEMP_HOME/.pi/agent/agents/pals-implementer.md" && bash tests/pi-end-to-end-validation.sh && bash tests/cross-harness-validation.sh && grep -n 'pals-implementer' kernel/workflows/apply-phase.md drivers/pi/skills/paul-apply/SKILL.md drivers/pi/extensions/pals-hooks.ts .pi/agents/pals-implementer.md && grep -n 'code-reviewer\|/paul:review' drivers/pi/skills/paul-review/SKILL.md README.md drivers/pi/extensions/README.md</verify>
  <done>AC-2 satisfied: live install + validation pass, delegated APPLY remains parent-authoritative, and REV remains intact.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- `.paul/*` lifecycle artifacts except the standard Phase 157 planning/unify bookkeeping done by PAUL workflows
- `.paul/phases/151-research-and-design/rev-design.md` (explicitly out of scope unless an unrelated dirty diff already exists and must be reverted)
- Module manifests or unrelated module references (`modules/*`) — this phase validates the implementer path, it does not redesign module contracts
- Unrelated kernel workflows outside `kernel/workflows/apply-phase.md`

## SCOPE LIMITS
- No new subagent types, no runtime-global implementer adoption, and no Pi-owned lifecycle state
- No redesign of REV; only preserve and prove its separate on-demand path
- No broad README rewrite beyond delegated APPLY / Pi install / REV operating-model clarity
- No new test framework adoption; reuse the existing shell validation suites and targeted smoke checks
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `bash tests/pi-end-to-end-validation.sh` passes
- [ ] `bash tests/cross-harness-validation.sh` passes
- [ ] Temporary-HOME Pi install produces `~/.pi/agent/agents/pals-implementer.md`
- [ ] Docs mention delegated APPLY, Pi agent install path, and REV separation
- [ ] No unrelated Phase 151 artifact changes are introduced
- [ ] All acceptance criteria are met
</verification>

<success_criteria>
- Validation proof exists for agent installation, delegated APPLY boundaries, and REV preservation
- Pi operating-model docs match the shipped runtime behavior
- Any issues surfaced by live validation are resolved within the bounded Phase 157 file scope
- The milestone is ready for APPLY using the new delegated path in real work
</success_criteria>

<output>
After completion, create `.paul/phases/157-validation-and-polish/157-01-SUMMARY.md`
</output>
