---
phase: 162-workflow-read-discipline-reduction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kernel/workflows/plan-phase.md
  - kernel/workflows/roadmap-management.md
  - kernel/workflows/discuss-phase.md
  - kernel/workflows/create-milestone.md
autonomous: true
---

<objective>
## Goal
Reduce workflow-side context pressure in the highest-value planning/status/milestone routes by making shared workflow reads more state-first and slice-based instead of reflexively bundling broad `ROADMAP.md` context.

## Purpose
Phase 160 ranked roadmap read-discipline as the strongest next reduction target after wrapper slimming. This plan captures that first shared-workflow pass without redesigning `ROADMAP.md`, weakening `.paul/*` authority, or expanding into broader prose cleanup and validation work that belongs in the next phase.

## Output
- Narrower read-discipline instructions in the core planning and status workflows
- State-first, targeted phase/milestone read guidance in the discussion and milestone-creation workflows
- Installed Pi skill surface refreshed from repo-source workflows plus passing regression checks
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md
.paul/SPECIAL-FLOWS.md

## Prior Work (only if genuinely needed)
.paul/phases/161-wrapper-entrypoint-slimming/161-01-SUMMARY.md
.paul/phases/160-findings-recommendation-package/160-01-SUMMARY.md
.paul/phases/160-findings-recommendation-package/context-reduction-recommendations-ranked.md
.paul/phases/160-findings-recommendation-package/context-bloat-findings-matrix.md
.paul/phases/159-workflow-bloat-audit/159-01-SUMMARY.md
.paul/phases/159-workflow-bloat-audit/workflow-read-surface-inventory.md
.paul/phases/159-workflow-bloat-audit/prompt-shape-duplication-audit.md

## Source Files
kernel/workflows/plan-phase.md
kernel/workflows/roadmap-management.md
kernel/workflows/discuss-phase.md
kernel/workflows/create-milestone.md
install.sh
tests/pi-end-to-end-validation.sh
tests/cross-harness-validation.sh
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: TODD(100) → no standard test framework detected via hook commands; existing regression proof remains shell-based under `tests/` | IRIS(150) → no TODO/FIXME/HACK/XXX markers found in the candidate workflow files | DAVE(200) → no CI config detected in repo root, so no CI coverage-gap guidance applies to this workflow-doc phase | DOCS(200) → the targeted shared workflows are themselves the canonical documentation surface, and related adapter docs already exist in `drivers/pi/skill-map.md`, `drivers/pi/extensions/README.md`, and `README.md`; preserve those surfaces while tightening read discipline | RUBY(250) → candidate workflow files are already large at **374 / 456 / 288 / 285** lines, with `roadmap-management.md` the main debt hotspot, so edits must stay localized and avoid adding new boilerplate.

[dispatch] pre-plan enforcement: DEAN(50) → SKIPPED (no supported package manager or audit target detected in this repo)

[dispatch] post-plan: TODD(100) → 0 modifications (execute plan retained; no standard TDD restructure suggested for shared workflow markdown edits)
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | Plan / apply / unify lifecycle work | ✓ |
| /carl | required | Automatic session-boundary handling during execution | ✓ |
| TODD | required | During APPLY if implementation needs test-first or per-task regression handling | ✓ |
| WALT | required | During APPLY / UNIFY validation and quality gating | ✓ |

**BLOCKING:** Required lifecycle flows remain in force for APPLY even though this plan targets shared workflow markdown rather than runtime product code.
</skills>

<acceptance_criteria>
## AC-1: Planning and status workflows read roadmap context by active slice
```gherkin
Given the shared planning and status workflows that currently pair `STATE.md` with broad `ROADMAP.md` reads
When Phase 162 is implemented
Then `plan-phase.md` and `roadmap-management.md` instruct a state-first flow that reads only the current milestone, target phase, or operation-specific roadmap slice needed for the route instead of reflexively treating the full roadmap as the default bundle.
```

## AC-2: Discussion and milestone-creation workflows keep guidance while narrowing artifact pulls
```gherkin
Given the shared phase-discussion and milestone-creation workflows
When their read discipline is updated in this phase
Then they preserve collaboration posture, review behavior, and milestone/phase framing while using `STATE.md` and compact context first and consulting roadmap detail only where the selected phase or roadmap mutation genuinely requires it.
```

## AC-3: Read-discipline reductions remain authoritative and regression-safe
```gherkin
Given the updated shared workflow files
When the repo-source skill surface is reinstalled and the existing regression suites are run
Then the shared workflows still own lifecycle truth, canonical guided behavior remains stable, and the Pi plus cross-harness validation suites continue to pass.
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Narrow roadmap reads in the core planning and status workflows</name>
  <files>kernel/workflows/plan-phase.md, kernel/workflows/roadmap-management.md</files>
  <action>
    Update the planning and status workflows so they read roadmap context by active window instead of as a broad default bundle:
    - in `plan-phase.md`, keep `STATE.md` as the first authority read, then make `ROADMAP.md` explicitly current-milestone / target-phase scoped for phase selection and scope analysis unless a broader roadmap operation is actually required
    - in `roadmap-management.md`, keep show-status state-first and make roadmap reads explicitly current-milestone / current-phase scoped for reporting; keep add/remove operations clear about when a broader roadmap edit is necessary because the workflow is mutating roadmap structure
    - preserve existing git-aware routing, loop-position routing, review menus, and state-update semantics
    - keep the reduction semantic, not cosmetic: the goal is narrower read discipline, not a general prose rewrite or marker change
    Avoid: changing command outputs, guided marker strings, git-routing tables, or replacing shared `.paul/*` authority with Pi-owned state.
  </action>
  <verify>grep -q 'current milestone section' kernel/workflows/plan-phase.md && grep -q 'target phase detail' kernel/workflows/plan-phase.md && grep -q 'current milestone section' kernel/workflows/roadmap-management.md</verify>
  <done>AC-1 satisfied: planning and status workflows describe state-first, slice-based roadmap reads without changing route behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the same state-first read discipline to phase discussion and milestone creation</name>
  <files>kernel/workflows/discuss-phase.md, kernel/workflows/create-milestone.md</files>
  <action>
    Bring the planning-adjacent discussion and milestone-creation routes into the same narrowed read model:
    - in `discuss-phase.md`, keep `STATE.md` and `PROJECT.md` as the default brief, make `PRD.md` explicitly selective, and scope roadmap reads to the selected phase row/detail after validation rather than a broad bundle up front
    - in `create-milestone.md`, keep milestone-context and state first, then describe roadmap reads in terms of the insertion/update slice needed to create the milestone and derive next phase numbering
    - preserve collaboration-level prompts, exploratory/direct-requirements mode handling, review menus, and one-question-at-a-time milestone behavior
    - keep wording concise and operational; only trim or move prose where it is necessary to make the read discipline explicit
    Avoid: broad cleanup of milestone/discussion prose beyond the read-discipline goal, or changes to milestone-state write semantics.
  </action>
  <verify>grep -q 'selected phase detail' kernel/workflows/discuss-phase.md && grep -q 'next available phase number' kernel/workflows/create-milestone.md && grep -q 'one question at a time' kernel/workflows/create-milestone.md</verify>
  <done>AC-2 satisfied: discussion and milestone-creation flows keep their planning behavior while reducing default artifact pulls.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- drivers/pi/skills/* thin wrappers from Phase 161 (that contract is already settled for this wave)
- drivers/pi/extensions/pals-hooks.ts or parser marker logic
- `.paul/ROADMAP.md` structure, active-window archival strategy, or milestone-history compaction (structural roadmap work belongs later)
- kernel/workflows/apply-phase.md and kernel/workflows/unify-phase.md (module-dispatch compression is a later phase)
- `.paul/*` authority semantics, guided workflow markers, or git-aware routing behavior

## SCOPE LIMITS
- This phase is about shared-workflow read discipline, not broad workflow prose cleanup
- Do not redesign `STATE.md`, split hot/cold artifacts, or introduce Pi-owned memory/telemetry
- Do not expand validation into a new large test harness; use targeted grep checks plus the existing install and regression suites
- Leave `kernel/workflows/discuss-milestone.md`, `kernel/workflows/complete-milestone.md`, and `kernel/workflows/phase-assumptions.md` for later passes unless they become necessary to complete this exact read-discipline change safely
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `kernel/workflows/plan-phase.md` explicitly scopes roadmap reads to the current milestone section and target phase detail
- [ ] `kernel/workflows/roadmap-management.md` explicitly scopes status reads to the current milestone section while preserving roadmap mutation guidance for add/remove operations
- [ ] `kernel/workflows/discuss-phase.md` keeps `PROJECT.md` first, `PRD.md` selective, and phase-level roadmap reads targeted
- [ ] `kernel/workflows/create-milestone.md` stays milestone-context/state first and limits roadmap reads to the slice needed for milestone creation
- [ ] `PALS_DRIVER=pi bash install.sh` completes successfully
- [ ] `bash tests/pi-end-to-end-validation.sh` passes
- [ ] `bash tests/cross-harness-validation.sh` passes
- [ ] All acceptance criteria are met
</verification>

<success_criteria>
- Both tasks completed
- The highest-value shared planning/status/milestone routes now describe narrower roadmap reads without changing lifecycle authority
- Existing guided behavior and collaboration semantics remain intact
- Pi install plus Pi/cross-harness validation stay green after the workflow edits
</success_criteria>

<output>
After completion, create `.paul/phases/162-workflow-read-discipline-reduction/162-01-SUMMARY.md`
</output>
