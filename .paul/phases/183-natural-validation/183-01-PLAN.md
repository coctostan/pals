---
phase: 183-natural-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .paul/phases/183-natural-validation/183-01-SUMMARY.md
  - .paul/ROADMAP.md
  - .paul/STATE.md
autonomous: true
---

<objective>
## Goal
Close v2.42 CODI v0.1 by replacing the previously blocked cross-repo natural-validation plan with a repo-local closeout/reconciliation plan that ships the bounded instrumentation repair as good enough for v0.1.

## Purpose
Phase 182 already repaired the CODI post-unify measurement surface with an early hook-entry trace and `injected-degraded` classification. The original Phase 183 plan attempted to validate that repair through a fresh quark phase, but it was invalid for APPLY because it targeted absolute paths outside this repo and imposed a validation burden the user explicitly does not want. This plan preserves lifecycle honesty by recording the decision to ship despite skipping the fresh quark validation, while keeping all execution inside the pals repo.

## Output
1. `.paul/phases/183-natural-validation/183-01-SUMMARY.md` records the closeout decision, skipped-validation caveat, and final v2.42 verdict.
2. `.paul/ROADMAP.md` marks Phase 183 and v2.42 complete with the ship-now rationale.
3. `.paul/STATE.md` routes the project out of the blocked Phase 183 state and toward milestone completion / next work.
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md
.paul/SPECIAL-FLOWS.md
.paul/phases/183-natural-validation/CONTEXT.md

## Prior Work (genuinely required)
.paul/phases/182-bounded-instrumentation-fix/182-01-SUMMARY.md — confirms the bounded CODI repair shipped and records the original next-phase validation target.
.paul/proposals/v2.42-post-unify-instrumentation-fix.md — defines the original natural-validation success criterion and the no-retroactive-repair invariant.

## Source Files
.paul/phases/183-natural-validation/183-01-SUMMARY.md
.paul/ROADMAP.md
.paul/STATE.md
</context>

<module_dispatch>
## Pre-Plan Module Dispatch

Planning posture used for this run: `low` collaboration + `direct-requirements`, carried from `.paul/phases/183-natural-validation/CONTEXT.md`. The user explicitly requested shipping CODI and avoiding further validation minutiae.

Prepared CODI seeds: none. The executable plan scope is lifecycle markdown only, with no repo-relative `.ts/.tsx/.js/.jsx` source selectors.

**Advisory (non-blocking):**
- `[dispatch] ARCH (p75) → pass with context warning` — repo architecture is markdown/kernel/modules/drivers. Planned files are lifecycle artifacts in expected `.paul/` locations. Existing `ROADMAP.md` / `STATE.md` are large historical artifacts, but this plan edits only targeted lifecycle sections.
- `[dispatch] TODD (p100) → no TDD restructuring` — no relevant test-first surface for markdown-only lifecycle reconciliation; plan remains `execute`.
- `[dispatch] IRIS (p150) → pass` — no actionable TODO/FIXME/HACK/XXX markers are planned in the replacement lifecycle artifacts.
- `[dispatch] ARIA / LUKE / DANA / GABE / OMAR / PETE / REED / VERA → skip` — no UI, data, API, observability, performance, resilience, or privacy code files are in scope.
- `[dispatch] DAVE (p200) → warn` — no CI config was detected at the repo root; CI generation remains out of scope for this lifecycle closeout.
- `[dispatch] DOCS (p200) → pass` — lifecycle documentation is the work product; no runtime docs change is required.
- `[dispatch] CODI (p220) → skipped — no extractable symbols in phase scope` — expected for markdown-only closeout.
- `[dispatch] RUBY (p250) → pass` — no source-code debt surface in planned changes.

### Blast Radius (CODI)

_(no downstream call-sites — markdown-only lifecycle reconciliation has no bounded source selectors)_

**Enforcement (blocking-capable):**
- `[dispatch] DEAN (p50) → skip` — no supported root-level package/audit manifest was detected for this repo-level lifecycle-only plan.
- `[dispatch] SETH (p80) → pass` — no hardcoded secret pattern was found in the planned lifecycle files.
- `[dispatch] ARCH circular-dep → pass` — no runtime dependency graph changes are introduced.

**Post-plan dispatch:**
- `[dispatch] TODD post-plan (p100) → no modifications` — plan type is `execute` and no relevant test candidates cover the planned markdown lifecycle edits.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | Entire PLAN → APPLY → UNIFY lifecycle for Phase 183 closeout | ✓ |
| /carl | required | Session-boundary handling while closing the phase/milestone | ✓ |
| TODD | required | During APPLY if any test-relevant code unexpectedly enters scope | ✓ |
| WALT | required | During APPLY/UNIFY validation and quality-history handling where applicable | ✓ |

**BLOCKING:** Required lifecycle flows remain in force for APPLY. This plan is intentionally markdown-only; if source-code work appears necessary, stop and re-plan.

## Skill Invocation Checklist
- [x] /paul loaded
- [x] /carl active via installed runtime/extension path
- [x] TODD available via installed module overlay
- [x] WALT available via installed module overlay
</skills>

<acceptance_criteria>
## AC-1: Blocked cross-repo validation is replaced with repo-local closeout
```gherkin
Given the prior Phase 183 plan was blocked because it targeted absolute quark paths outside the pals repo
When APPLY executes this replacement plan
Then all executable task file scopes are repo-relative pals paths
And no quark lifecycle artifacts are edited or required for completion
And the SUMMARY records that the planned fresh quark validation was intentionally skipped rather than passed
```

## AC-2: v2.42 ships with an honest CODI v0.1 verdict
```gherkin
Given Phase 182 repaired CODI's bounded post-unify measurement surface
When Phase 183 is reconciled
Then the final verdict says CODI v0.1 is shipping as good enough now
And it states that further validation/refinement is deferred until real usage produces a concrete issue
And it preserves the caveat that no new natural quark validation row was collected in Phase 183
```

## AC-3: Lifecycle state routes out of the blocked phase
```gherkin
Given STATE.md previously routed Phase 183 to re-plan because the invalid cross-repo plan blocked APPLY
When APPLY finishes this replacement plan
Then ROADMAP.md marks Phase 183 complete and v2.42 complete
And STATE.md no longer routes to re-plan Phase 183
And the next action points to milestone completion / next milestone selection rather than quark validation minutiae
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Write the Phase 183 closeout summary with the ship-now decision</name>
  <files>.paul/phases/183-natural-validation/183-01-SUMMARY.md</files>
  <action>
    Create the Phase 183 SUMMARY as the authoritative closeout artifact for the replacement plan.

    Include:
    - the reason for replacing the original plan: cross-repo absolute paths made it invalid for APPLY
    - the user/product decision: CODI v0.1 is considered good enough and additional quark validation minutiae is not worth the time now
    - what Phase 182 already shipped: hook-entry trace and `injected-degraded` classification
    - the integrity caveat: no fresh natural quark validation row was collected, so Phase 183 is a ship decision, not a new empirical pass
    - final routing: v2.42 closes; future CODI work should be driven by real usage signals or concrete defects

    Avoid:
    - claiming the skipped quark validation passed
    - editing or reinterpreting v2.41/quark `112-01` or `113-01` artifacts
    - adding new CODI scope, v0.2 design, or another trial proposal
  </action>
  <verify>grep -n 'ship\|skipped\|good enough\|no fresh natural quark validation\|v2.42' .paul/phases/183-natural-validation/183-01-SUMMARY.md</verify>
  <done>AC-1 and AC-2 satisfied: the summary records the repo-local replacement and honest ship-now verdict.</done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP and STATE to close v2.42 cleanly</name>
  <files>.paul/ROADMAP.md, .paul/STATE.md</files>
  <action>
    Update `.paul/ROADMAP.md`:
    - mark v2.42 status complete
    - mark Phase 183 complete with the completion date
    - replace the Phase 183 plan note with the replacement closeout plan result
    - update the v2.42 outcome/theme language so it says the milestone shipped CODI v0.1 as good enough after the bounded instrumentation fix, while explicitly noting fresh natural validation was skipped by product decision

    Update `.paul/STATE.md`:
    - remove the APPLY-blocked / re-plan-required current position
    - mark v2.42 complete and Phase 183 complete
    - add a decision entry capturing the ship-now choice and the validation caveat
    - set session continuity to route to milestone completion / next milestone discussion, not quark validation

    Avoid:
    - editing source files or CODI module files
    - claiming a new quark row exists
    - deleting historical blocker/deviation context entirely; preserve the history as a resolved planning defect where useful
  </action>
  <verify>grep -n 'v2.42\|Phase 183\|ship\|good enough\|skipped\|Next action' .paul/ROADMAP.md .paul/STATE.md</verify>
  <done>AC-3 satisfied: lifecycle state no longer points to the blocked cross-repo validation plan.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- `modules/codi/module.yaml`
- `modules/codi/references/codi-instrumentation.md`
- Any CODI source/runtime behavior
- Any v2.41 artifact under `.paul/phases/179-*` or `.paul/phases/180-*`
- Any quark repo artifact or CODI history row

## SCOPE LIMITS
- No cross-repo execution from pals.
- No new quark milestone, phase, plan, summary, or CODI ledger row.
- No synthetic CODI probe, second observation window, or controlled self-trial.
- No CODI v0.2 expansion, `symbol_graph`, `trace`, or PLAN-template formalization.
- No broad roadmap cleanup beyond the targeted v2.42/Phase 183 lifecycle updates.
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `.paul/phases/183-natural-validation/183-01-SUMMARY.md` exists and records the ship-now decision plus skipped-validation caveat.
- [ ] `.paul/ROADMAP.md` marks Phase 183 and v2.42 complete.
- [ ] `.paul/STATE.md` no longer says APPLY is blocked or that Phase 183 needs re-plan.
- [ ] No executable task `<files>` entry contains an absolute path.
- [ ] No quark artifacts or CODI source files were modified.
</verification>

<success_criteria>
- Phase 183 closes through repo-local lifecycle reconciliation only.
- v2.42 CODI v0.1 ships as good enough, with the skipped fresh validation stated honestly.
- Future CODI work is deferred until real usage or a concrete defect justifies it.
- The PALS state is unblocked and ready for milestone close / next milestone selection.
</success_criteria>

<output>
After completion, create `.paul/phases/183-natural-validation/183-01-SUMMARY.md`.
</output>