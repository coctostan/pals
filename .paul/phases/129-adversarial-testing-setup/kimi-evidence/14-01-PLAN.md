---
phase: 14-module-bait-implementation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/admin-api.ts
  - src/dashboard-component.tsx
  - src/db-helpers.ts
  - package.json
autonomous: false
---

<objective>
## Goal
Add intentionally problematic code designed to trigger every PALS module (SETH, IRIS, OMAR, GABE, LUKE, ARIA, REED, PETE, DANA, TODD, DEAN) in a single APPLY cycle. This is an adversarial test of PALS module dispatch.

## Purpose
Validate that the PALS two-step dispatch system (advisory modules → enforcement modules) correctly identifies and flags anti-patterns across security, UX, observability, performance, and quality domains.

## Output
- `src/admin-api.ts`: Security-sensitive admin endpoint with hardcoded credentials, eval usage, console.log secrets
- `src/dashboard-component.tsx`: Complex UI component with inline styles, hardcoded colors, accessibility violations
- `src/db-helpers.ts`: Database helpers with N+1 patterns, unbounded queries, no timeouts
- Modified `package.json`: Dependency changes to trigger DEAN audit
</objective>

<context>
## Project Context
- .paul/PROJECT.md: pi-verify is a TypeScript extension for verification checks
- .paul/ROADMAP.md: v1.1 milestone with Phase 14 for adversarial module testing
- Current version: 1.0.0 (shipped)

## Module Targets
| Module | Category | Target File | Anti-Pattern |
|--------|----------|-------------|--------------|
| SETH | Security | admin-api.ts | Hardcoded secrets, eval(), no input validation |
| IRIS | Code Review | admin-api.ts | eval(), magic numbers, god functions |
| OMAR | Observability | admin-api.ts | console.log secrets, empty catch blocks |
| GABE | API Contract | admin-api.ts | No validation, breaking changes |
| LUKE | UX | dashboard-component.tsx | Inline styles, hardcoded colors, god component |
| ARIA | Accessibility | dashboard-component.tsx | div onClick, no alt text, no labels |
| REED | Resilience | dashboard-component.tsx, db-helpers.ts | No error boundaries, no timeouts |
| PETE | Performance | db-helpers.ts | N+1 queries, unbounded queries, nested loops |
| DANA | Data | db-helpers.ts | No timestamps, destructive patterns |
| TODD | Quality | (no tests) | Deliberately skip tests to trigger BLOCK |
| DEAN | Dependencies | package.json | Audit baseline change |

## Constraints
- Code intentionally violates best practices
- This is BAIT code — not production quality
- DO NOT fix the anti-patterns — they are the feature
</context>

<module_dispatch>
## Pre-Plan Module Dispatch

[dispatch] pre-plan advisory: TODD(p100) → inject tdd_candidates, tdd_type | skip (no tests planned)
[dispatch] pre-plan enforcement: DEAN(p50) → inject dep_warnings | baseline recorded
[dispatch] pre-plan advisory: SETH(p80) → inject sec_warnings | hardcoded secrets expected
[dispatch] pre-plan advisory: IRIS(p150) → inject review_flags | eval, magic numbers expected
[dispatch] pre-plan advisory: LUKE(p185) → inject ux_context | UI bait planned
[dispatch] pre-plan advisory: ARIA(p190) → inject a11y_context | a11y violations expected
[dispatch] pre-plan advisory: OMAR(p205) → inject obs_context | console.log secrets expected
[dispatch] pre-plan advisory: GABE(p175) → inject api_context | no validation expected
[dispatch] pre-plan advisory: REED(p210) → inject resilience_context | no timeouts expected
[dispatch] pre-plan advisory: PETE(p225) → inject perf_context | N+1 patterns expected
[dispatch] pre-plan advisory: DANA(p195) → inject data_context | query patterns expected

Note: All advisory modules will flag violations. TODD will BLOCK (no tests). DEAN will audit package.json changes.
</module_dispatch>

<acceptance_criteria>

## AC-1: Admin API Security Bait
```gherkin
Given the admin-api.ts file is created
When SETH, IRIS, OMAR, and GABE modules scan it
Then they should detect hardcoded credentials, eval usage, console.log secrets, and missing validation
```

## AC-2: Dashboard Component UX/ARIA Bait
```gherkin
Given the dashboard-component.tsx file is created
When LUKE, ARIA, and REED modules scan it
Then they should detect inline styles, hardcoded colors, non-semantic HTML, missing alt text, and no error boundary
```

## AC-3: Database Helper Performance Bait
```gherkin
Given the db-helpers.ts file is created
When PETE, DANA, and REED modules scan it
Then they should detect N+1 patterns, unbounded queries, no timeouts, and missing timestamps
```

## AC-4: TODD Test Block
```gherkin
Given no test files are added for the new code
When TODD post-apply hook runs
Then it should BLOCK with "No tests found for new code"
```

## AC-5: DEAN Dependency Audit
```gherkin
Given package.json is modified
When DEAN pre-plan and post-apply hooks run
Then they should detect dependency changes and compare against baseline
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Create Admin API with Security Anti-Patterns</name>
  <files>src/admin-api.ts</files>
  <action>
    Create src/admin-api.ts with intentional security vulnerabilities:

    1. Hardcoded admin credentials (triggers SETH):
       - const ADMIN_PASSWORD = "supersecret123"
       - const API_KEY = "sk-live-abcdef123456"

    2. Eval usage with dynamic input (triggers SETH, IRIS):
       - eval(req.body.command)

    3. Console.log of secrets (triggers OMAR):
       - console.log("Authenticating with:", ADMIN_PASSWORD)
       - console.log("API Key:", API_KEY)

    4. No input validation (triggers GABE):
       - app.post("/admin/exec", (req, res) => { ... })
       - Directly use req.body without validation

    5. Empty catch block (triggers OMAR, REED):
       - try { ... } catch (e) { }

    6. God function with 50+ lines and 6+ parameters (triggers IRIS)

    Export the express router for mounting.
  </action>
  <verify>grep -E "(ADMIN_PASSWORD|API_KEY|eval\(|console\.log.*password|catch.*\{\s*\})" src/admin-api.ts</verify>
  <done>AC-1 satisfied: Security anti-patterns present in admin-api.ts</done>
</task>

<task type="auto">
  <name>Task 2: Create Dashboard Component with UX/Accessibility Anti-Patterns</name>
  <files>src/dashboard-component.tsx</files>
  <action>
    Create src/dashboard-component.tsx with intentional UX/a11y violations:

    1. God component >300 lines (triggers LUKE):
       - Single component handling data fetch, chart rendering, table, modal

    2. Inline styles (triggers LUKE):
       - style={{ margin: '20px', backgroundColor: '#ff0000' }}

    3. Hardcoded colors (triggers LUKE):
       - color: '#00ff00' instead of design tokens

    4. Non-semantic interactive elements (triggers ARIA):
       - <div onClick={...}> instead of <button>
       - <span onClick={...}> for actions

    5. Images without alt text (triggers ARIA):
       - <img src="..." /> without alt prop

    6. Inputs without labels (triggers ARIA):
       - <input type="text" ... /> without associated label

    7. No error boundary (triggers REED):
       - Component throws errors without catch
       - fetch() without error handling

    8. No loading states (triggers LUKE):
       - fetch call without isLoading state

    Export as DashboardComponent.
  </action>
  <verify>grep -E "(style=\{\{|color.*#[0-9a-f]|div.*onClick|img[^>]*>(?!.*alt)|input[^>]*type)" src/dashboard-component.tsx</verify>
  <done>AC-2 satisfied: UX/a11y anti-patterns present in dashboard-component.tsx</done>
</task>

<task type="auto">
  <name>Task 3: Create Database Helpers with Performance Anti-Patterns</name>
  <files>src/db-helpers.ts</files>
  <action>
    Create src/db-helpers.ts with intentional performance/data violations:

    1. N+1 query pattern (triggers PETE):
       - for (const user of users) { await db.query(...) }

    2. Unbounded queries (triggers PETE):
       - SELECT * FROM users (no LIMIT)
       - findAll() without pagination

    3. No timeouts (triggers REED, PETE):
       - fetch() or db.query() without timeout option

    4. Nested loops (triggers PETE):
       - for (a) { for (b) { ... } } patterns

    5. Synchronous I/O in async path (triggers PETE):
       - readFileSync inside request handler

    6. Missing timestamps (triggers DANA):
       - Entity/table without created_at/updated_at

    Export functions: getAllUsers(), getUserDetails(), processBatch().
  </action>
  <verify>grep -E "(for.*await|SELECT \*|findAll\(\)|readFileSync)" src/db-helpers.ts</verify>
  <done>AC-3 satisfied: Performance anti-patterns present in db-helpers.ts</done>
</task>

<task type="auto">
  <name>Task 4: Modify package.json to Trigger DEAN</name>
  <files>package.json</files>
  <action>
    Modify package.json with a benign dependency change to trigger DEAN audit:

    1. Add a devDependency (triggers DEAN comparison):
       - "@types/debug": "^4.1.12"

    2. Or bump a patch version of an existing dependency

    This ensures DEAN pre-plan and post-apply have different baselines to compare.
    Do NOT introduce actual vulnerabilities — just trigger the audit flow.
  </action>
  <verify>git diff package.json | grep -E "(\+.*@types|version.*[0-9])"</verify>
  <done>AC-5 satisfied: package.json modified for DEAN audit</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Phase 14 bait code for adversarial PALS module testing</what-built>
  <how-to-verify>
    1. Review src/admin-api.ts — confirm it contains:
       - Hardcoded ADMIN_PASSWORD and API_KEY
       - eval() with dynamic input
       - console.log of secrets
       - Empty catch block
       - No input validation

    2. Review src/dashboard-component.tsx — confirm it contains:
       - Inline styles (style={{...}})
       - Hardcoded colors (#hex values)
       - <div onClick={...}> (non-semantic)
       - <img> without alt
       - <input> without label

    3. Review src/db-helpers.ts — confirm it contains:
       - for-await pattern (N+1)
       - SELECT * without LIMIT
       - No timeout configuration

    4. Review package.json changes — confirm benign dependency addition

    5. Confirm NO test files were added (TODD should block)
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to APPLY, or describe issues</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- src/index.ts (main export — do not pollute)
- src/verify.ts (core functionality)
- src/detectors.ts (language detection)
- src/config.ts (config system)
- test/*.test.ts (existing tests)
- Any .verifyrc.json or config files

## SCOPE LIMITS
- Code intentionally violates best practices — this is BAIT
- Do NOT fix the anti-patterns — they are the feature
- Do NOT add tests — TODD blocking is intentional
- Do NOT make the code production-ready
- Do NOT add error handling, validation, or security fixes
- Keep changes isolated to the three new bait files + package.json

## MODULE EXPECTATIONS
| Module | Expected Response |
|--------|-------------------|
| SETH | BLOCK on hardcoded secrets, eval |
| IRIS | WARN on eval, magic numbers |
| OMAR | BLOCK on console.log secrets |
| GABE | WARN on no validation |
| LUKE | WARN on inline styles, god component |
| ARIA | BLOCK on missing alt, non-semantic HTML |
| REED | WARN on no timeouts, empty catch |
| PETE | WARN on N+1, unbounded queries |
| DANA | WARN on missing timestamps |
| TODD | BLOCK on no tests |
| DEAN | WARN on dependency changes |

</boundaries>

<verification>
Before declaring plan complete:
- [ ] src/admin-api.ts created with security anti-patterns
- [ ] src/dashboard-component.tsx created with UX/a11y anti-patterns
- [ ] src/db-helpers.ts created with performance anti-patterns
- [ ] package.json modified with dependency change
- [ ] NO test files added for new code
- [ ] All bait code compiles (TypeScript errors acceptable for bait)
- [ ] User has approved the checkpoint
</verification>

<success_criteria>
- All 4 tasks completed
- All verification checks pass
- Code contains intentional anti-patterns for each target module
- TODD will BLOCK (expected behavior)
- User has approved checkpoint
</success_criteria>

<output>
After completion, create `.paul/phases/14-module-bait-implementation/14-01-SUMMARY.md`
</output>
