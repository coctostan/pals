---
phase: 247-documentation-validation-surfacing-v2-57
plan: 01
type: execute
wave: 1
depends_on:
  - phase: 246-s2-workflow-resource-capsule-rendering-extraction
    provides: S2 `workflow-resource-capsule-rendering` extracted to `drivers/pi/extensions/workflow-resource-capsule-rendering.ts` (192 LOC) with the canonical no-op default-exported factory + `No-op Pi extension factory` marker; `pals-hooks.ts` 1415→1273 LOC; helpers preserved/exported; install reports 4 files; sibling-module pattern triple-confirmed (S5+S1+S2); loader-compat invariant promoted to per-extraction AC; Pi `1..221/0`, cross-harness `1..129/0`.
  - phase: 244-documentation-validation-surfacing
    provides: canonical Documentation + Validation Surfacing shape — README/extensions-README/skill-map name the shipped sibling, extraction-target baseline + modularization contract carry a `## Phase N Outcome` subsection, and exactly one localized closure guardrail per validation suite asserts the new surfacing markers; multi-file `tap_file_contains_all` surfacing emits N blocks under one shared title.
files_modified:
  - README.md
  - drivers/pi/extensions/README.md
  - drivers/pi/skill-map.md
  - docs/PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md
  - docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md
  - tests/pi-end-to-end-validation.sh
  - tests/cross-harness-validation.sh
autonomous: true
risk_tier: normal
collaboration_level: high
planning_mode: direct-requirements
---

# Plan 247-01 — Documentation + Validation Surfacing (v2.57 S2 Sibling Extraction)

<objective>

## Goal

Surface Phase 246 outcomes — S2 `workflow-resource-capsule-rendering` is now an installed sibling Pi extension module, the sibling-module pattern is triple-confirmed (S5 + S1 + S2), and the Phase 245 Pi extension loader-compat invariant has been promoted from a closure-phase hotfix to a per-extraction acceptance criterion — through the user-facing docs and the two extraction contract docs that drove the wave; add exactly one localized closure guardrail per validation suite to protect this surfacing.

## Purpose

Close the documentation/validation gap created when Phase 246 shipped the S2 extraction without updating top-level docs (`README.md`, `drivers/pi/extensions/README.md`, `drivers/pi/skill-map.md` mention only `pals-hooks.ts`, `module-activity-parsing.ts`, and `artifact-slice-rendering.ts` — they don't yet name `workflow-resource-capsule-rendering.ts`) and without amending the two extraction contract docs (`PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md` still labels S2 "Still inline in `pals-hooks.ts`" on its candidate table, line 28; `PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md` still describes S2 as a candidate, not a shipped extraction). Phase 248 (Evidence Closure) needs surfaced docs + protected markers before evidence reconciliation can land cleanly.

This plan follows the canonical Documentation + Validation Surfacing shape established by Phases 208/212/216/220/224/244 and ratified by Phase 244 specifically: docs surfacing for the milestone's primary runtime/baseline phase outcome + exactly one localized validation guardrail per suite, with no source/runtime/install/dep/CI changes; multi-file `tap_file_contains_all` surfacing emits N blocks under one shared title rather than collapsing to inline grep.

## Output

- Updated user-facing docs (`README.md`, `drivers/pi/extensions/README.md`, `drivers/pi/skill-map.md`) that name `workflow-resource-capsule-rendering.ts` as a shipped sibling extension module alongside the existing three siblings; source-set/install-surface prose names all four siblings as the current source set.
- Updated `docs/PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md`: candidate-table row for S2 updated from "Still inline in `pals-hooks.ts`" to "Extracted to `drivers/pi/extensions/workflow-resource-capsule-rendering.ts` (Phase 246 / PR #161)"; new `## Phase 246 Outcome` subsection added (placed after the existing `## Phase 243 Outcome` subsection) recording the actual extraction result (S2 extracted, S3 explicitly deferred to v2.58 at the earliest per the conditional-secondary classification, helpers preserved, loader-compat invariant promoted to per-extraction AC, install reports 4 files, Pi 1..221/0, cross-harness 1..129/0); existing approved-wave selection prose preserved verbatim.
- Updated `docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md`: new `## Phase 246 Outcome` subsection (placed after the existing `## Phase 243 Outcome` subsection) recording that S2 has been extracted following the S5 + S1 sibling-module pattern, naming the canonical `loader-compat invariant` (no-op default-exported factory + `No-op Pi extension factory` marker comment) as a per-extraction AC, naming the helpers still exported from `pals-hooks.ts`, and explicitly preserving the existing S1-S8 risk/readiness analysis verbatim.
- Updated `tests/pi-end-to-end-validation.sh`: exactly two new `tap_file_contains_all` blocks under the shared title `Phase 247 sibling extraction surfacing (Pi-supported runtime)` — one asserts the basename `workflow-resource-capsule-rendering.ts` appears in `drivers/pi/extensions/README.md`; one asserts it appears in `drivers/pi/skill-map.md`. Multi-file split mirrors Phase 244 precedent. Pi suite delta: +2 net (`1..221` → `1..223`).
- Updated `tests/cross-harness-validation.sh`: exactly one new `tap_file_contains_all` block titled `Phase 247 sibling extraction surfacing (Shared Invariant)` that asserts the basename `workflow-resource-capsule-rendering.ts` plus the literal phrase `loader-compat invariant` appear in `docs/PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md`. Cross-harness delta: +1 net (`1..129` → `1..130`).
- All four official validation commands pass with command-output evidence; `git diff --check` clean; install surface unchanged at 4 files (no source-set membership change).

</objective>

<context>

## Project Context

- `.paul/STATE.md` — v2.57 active; Phase 247 ready to plan; loop ✓✓✓ for Phase 246; mode `direct-requirements`; collaboration `high`. Last commit on main: `204d3a33` Phase 246 transition.
- `.paul/PROJECT.md` — v2.57 Pi Extension Submodule Extraction Wave 1 of 3 phases complete; Phase 247 is the next planning target.
- `.paul/ROADMAP.md` — Phase 247 row: "Surface the S2 extraction outcome across README, Pi extension docs, skill map, and the extraction-target baseline + modularization contract docs; add localized validation guardrails (Pi e2e + cross-harness) for the new sibling and its loader-compat marker without re-opening contract scope."
- `.paul/phases/246-s2-workflow-resource-capsule-rendering-extraction/246-01-SUMMARY.md` — sibling-module triple-confirmation; loader-compat invariant promotion; helper-import tightening; S3 deferral to v2.58. Patterns Established section cited verbatim.
- `.paul/SPECIAL-FLOWS.md` — /paul, /carl, TODD, WALT required for lifecycle work.

## Prior Work

- `.paul/phases/244-documentation-validation-surfacing/244-01-PLAN.md` and `244-01-SUMMARY.md` — Phase 244 surfaced the S1 extraction; canonical surfacing shape is named there. This plan mirrors it phase-by-phase.
- `docs/PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md` — Phase 242 baseline, Phase 243 outcome already surfaced; needs Phase 246 outcome surfacing without re-litigating selection.
- `docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md` — original S1-S8 inventory + Phase 243 outcome subsection; needs Phase 246 outcome subsection.
- `drivers/pi/extensions/README.md` — names three siblings; current source-set prose says "currently `pals-hooks.ts`, the Phase 239 first-spike S5 extraction `module-activity-parsing.ts`, and the Phase 243 S1 sibling extraction `artifact-slice-rendering.ts`". Needs the Phase 246 S2 sibling added.
- `drivers/pi/skill-map.md` — same source-set prose at line 96; same update required.
- `README.md` line 25 — same source-set prose; same update required.

## Source-set context (read-only orientation; no source edits in this plan)

- `drivers/pi/extensions/pals-hooks.ts` (1273 LOC after Phase 246; helpers exported and preserved).
- `drivers/pi/extensions/module-activity-parsing.ts` (Phase 239 S5 sibling).
- `drivers/pi/extensions/artifact-slice-rendering.ts` (Phase 243 S1 sibling, 169 LOC).
- `drivers/pi/extensions/workflow-resource-capsule-rendering.ts` (Phase 246 S2 sibling, 192 LOC, **target of surfacing**).

## Anchors (verbatim phrases that must be searchable after surfacing)

- `workflow-resource-capsule-rendering.ts` — the new sibling basename; currently absent from README/extensions-README/skill-map.
- `Phase 246 sibling extraction` — anchor noun phrase for closure marker (mirrors `Phase 244 sibling extraction`).
- `loader-compat invariant` — canonical pattern name (Phase 245 introduction, Phase 246 promotion to per-extraction AC).
- `triple-confirmed sibling-module pattern` — emergent pattern phrase for the modularization contract (S5 + S1 + S2 all match).

</context>

<module_dispatch>

## Pre-Plan Module Dispatch

[dispatch] CODI: pre-plan advisory → docs + validation lines only; no symbol-level changes; no impact run required (skip-no-symbols expected at post-unify).

[dispatch] pre-plan advisory: TODD → no spec/unit tests; existing TAP shell suites carry validation. Plan stays `execute`. Test-suite update required: +2 Pi e2e blocks under one shared title (multi-file split, Phase 244 precedent), +1 cross-harness block.

[dispatch] pre-plan advisory: IRIS / RUBY → docs + 2 validation lines per suite; no code complexity, no anti-patterns, no debt added.

[dispatch] pre-plan advisory: DOCS → high signal — README / extensions-README / skill-map / extraction-target baseline / modularization contract all currently mis-state extraction state (only S5 + S1 named; S2 still labeled "Still inline").

[dispatch] pre-plan advisory: DEAN → no dependency surface; PASS.

[dispatch] pre-plan advisory: ARCH / SETH → no boundary or secret surface change; surfacing prose only.

[dispatch] pre-plan advisory: DAVE → no deploy/install surface change. Phase 241 source-set installer auto-covers all 4 siblings; verified 2026-05-02 (Phase 246 install reports 4 files).

[dispatch] pre-plan advisory: WALT → pre-apply baseline expected Pi 221/221, cross-harness 129/129. Post-apply expected: Pi 223/223 (+2; multi-file split mirrors Phase 244), cross-harness 130/130 (+1).

[dispatch] pre-plan enforcement: DEAN p50 → not applicable (no `package.json` change); PASS.

</module_dispatch>

<acceptance_criteria>

**AC-1: Top-level user-facing docs name S2 as a shipped sibling extension module**

Given Phase 246 extracted S2 to `drivers/pi/extensions/workflow-resource-capsule-rendering.ts` and the sibling-module pattern is now triple-confirmed (S5 + S1 + S2),

When a reader scans `README.md`, `drivers/pi/extensions/README.md`, and `drivers/pi/skill-map.md` for the current Pi extension source-set,

Then each of those three files contains the exact basename `workflow-resource-capsule-rendering.ts` at least once, alongside the existing `pals-hooks.ts`, `module-activity-parsing.ts`, and `artifact-slice-rendering.ts` mentions; the source-set/install-surface prose names all four siblings as the current source set rather than naming only the prior three; and the install-count claim (where present) is updated from "3 files" to "4 files" with the Phase 246 reference.

**AC-2: Extraction target baseline reflects Phase 246 outcome without re-opening selection**

Given `docs/PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md` was authored as the Phase 243 handoff baseline and still labels S2 "Still inline in `pals-hooks.ts`" on its candidate table (line 28),

When the candidate table and the post-Phase-243 outcome sections are read after this plan,

Then the candidate-table row for S2 is updated to "Extracted to `drivers/pi/extensions/workflow-resource-capsule-rendering.ts` (Phase 246 / PR #161)" with status "Shipped — sibling-module pattern triple-confirmed"; a new `## Phase 246 Outcome` subsection is added (placed immediately after the existing `## Phase 243 Outcome` subsection) recording the actual extraction result (S2 extracted with the canonical no-op default-exported factory + `No-op Pi extension factory` marker; the loader-compat invariant promoted from Phase 245 closure-phase hotfix to a per-extraction acceptance criterion asserted inline in the extraction TAP guardrail; helpers preserved and still exported from `pals-hooks.ts`; helper-import tightening at extraction time named as canonical sibling-import discipline; S3 explicitly deferred to v2.58 at the earliest per the conditional-secondary classification on line 29; install reports `4 files`; Pi 1..221/0, cross-harness 1..129/0); the existing approved-wave selection prose, the line-29 S3 conditional-secondary classification, and all S4/S6/S7/S8 deferral rationale are preserved verbatim.

**AC-3: Modularization contract surfaces the Phase 246 sibling extraction outcome and the loader-compat invariant promotion**

Given `docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md` already carries a `## Phase 243 Outcome` subsection that records S1 extraction and the default-arg cycle-avoidance pattern,

When the contract is read after this plan,

Then a new `## Phase 246 Outcome` subsection (placed immediately after the existing `## Phase 243 Outcome` subsection) records that S2 has been extracted to `drivers/pi/extensions/workflow-resource-capsule-rendering.ts` following the S5 + S1 sibling-module pattern; names the canonical `loader-compat invariant` (no-op default-exported factory + `No-op Pi extension factory` marker comment) as a per-extraction acceptance criterion that catches Pi extension-loader breakage at extraction time rather than at the next session start; names the `triple-confirmed sibling-module pattern` (S5 Phase 239 + S1 Phase 243 + S2 Phase 246) as ratified rather than provisional; names `helper-import tightening` (omit unreferenced helpers like `escapeRegExp`/`PalsStateSnapshot` at extraction time) as canonical sibling-import discipline; explicitly preserves the existing S1–S8 risk-readiness table, the existing `## Phase 243 Outcome` subsection content, and all surrounding selection rationale verbatim — only outcome surfacing is added.

**AC-4: Validation suites carry exactly the planned localized closure guardrails per suite, protecting the new surfacing markers**

Given Phase 247 surfacing introduces specific anchor phrases that must remain stable through Phase 248 closure,

When `bash tests/pi-end-to-end-validation.sh` and `bash tests/cross-harness-validation.sh` run after this plan,

Then `tests/pi-end-to-end-validation.sh` contains exactly two new `tap_file_contains_all` blocks under the shared title `Phase 247 sibling extraction surfacing (Pi-supported runtime)` — one asserting `workflow-resource-capsule-rendering.ts` in `drivers/pi/extensions/README.md`, one asserting `workflow-resource-capsule-rendering.ts` in `drivers/pi/skill-map.md` (Pi-supported runtime surface; multi-file split per Phase 244 precedent); `tests/cross-harness-validation.sh` contains exactly one new `tap_file_contains_all` block titled `Phase 247 sibling extraction surfacing (Shared Invariant)` asserting `workflow-resource-capsule-rendering.ts` plus the literal phrase `loader-compat invariant` in `docs/PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md` (Shared Invariant surface across docs/contract); both suites run green with Pi `1..223/0` (planned +2) and cross-harness `1..130/0` (planned +1); `bash tests/helpers/artifact_consistency.sh` reports `artifact_consistency_check: PASS`; `git diff --check` is clean.

**AC-5: No source/runtime/install/dependency/CI/lifecycle-authority drift**

Given Plan 247-01 is documentation + validation surfacing only,

When Plan 247-01 APPLY completes,

Then the changed-file set equals exactly: `README.md`, `drivers/pi/extensions/README.md`, `drivers/pi/skill-map.md`, `docs/PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md`, `docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md`, `tests/pi-end-to-end-validation.sh`, `tests/cross-harness-validation.sh`, plus the standard lifecycle artifacts (`.paul/STATE.md`, `.paul/ROADMAP.md`, `.paul/phases/247-documentation-validation-surfacing-v2-57/247-01-PLAN.md`, `.paul/phases/247-documentation-validation-surfacing-v2-57/247-01-SUMMARY.md`); `drivers/pi/extensions/pals-hooks.ts`, `drivers/pi/extensions/module-activity-parsing.ts`, `drivers/pi/extensions/artifact-slice-rendering.ts`, `drivers/pi/extensions/workflow-resource-capsule-rendering.ts`, `drivers/pi/install.sh`, `drivers/pi/uninstall.sh`, `drivers/pi/driver.yaml`, `kernel/`, `modules/`, `package.json`, `tests/helpers/**`, `.github/`, and `pals.json` are NOT modified; no `.paul/*` artifact authority is reassigned; install surface remains `[ok] Pi extensions installed: 4 files`.

</acceptance_criteria>

<tasks>

<task id="1" name="Surface the new S2 sibling across user-facing docs">
<files>
- README.md
- drivers/pi/extensions/README.md
- drivers/pi/skill-map.md
</files>
<action>
1. In `README.md` line 25 (the "Pi is the supported runtime: …" sentence), update the Pi extension siblings list from "currently `pals-hooks.ts` plus the Phase 239 first-spike S5 extraction `module-activity-parsing.ts` and the Phase 243 S1 sibling extraction `artifact-slice-rendering.ts`" to also name "the Phase 246 S2 sibling extraction `workflow-resource-capsule-rendering.ts`". Preserve the surrounding prose verbatim. If line 284 ("Default supported target") names a specific extension count or only `pals-hooks.ts`, leave it untouched (the existing surfacing already generalizes via the source-set rule).
2. In `drivers/pi/extensions/README.md`:
   - Line 198 (the table row) — extend the parenthetical from "currently `pals-hooks.ts`, the Phase 239 first-spike S5 extraction `module-activity-parsing.ts`, and the Phase 243 S1 sibling extension `artifact-slice-rendering.ts` per `docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md`" to also name "the Phase 246 S2 sibling extension `workflow-resource-capsule-rendering.ts`".
   - Line 207 (the source-set paragraph) — extend the parenthetical the same way: keep the existing three sibling references and add ", and the Phase 246 S2 sibling extension `workflow-resource-capsule-rendering.ts`".
   - Verify no other in-file references to a specific extension count (`2 files`, `3 files`) remain stale. If any non-installer prose in this file says "3 files" as a hard-coded count, update to "4 files" with the Phase 246 reference; if only the installer command output is shown verbatim, leave it (it is captured at install time, not asserted as prose).
3. In `drivers/pi/skill-map.md` line 96, apply the same source-set extension to name `workflow-resource-capsule-rendering.ts` alongside the existing three siblings; preserve the surrounding driver-manifest-audit prose verbatim.
4. **Do not modify** any source-file under `drivers/pi/extensions/*.ts`, `drivers/pi/install.sh`, `drivers/pi/uninstall.sh`, or `drivers/pi/driver.yaml`. Do not modify any kernel/module/test-helper files.
</action>
<verify>
- `grep -c "workflow-resource-capsule-rendering.ts" README.md` ≥ 1.
- `grep -c "workflow-resource-capsule-rendering.ts" drivers/pi/extensions/README.md` ≥ 2 (table row + source-set paragraph).
- `grep -c "workflow-resource-capsule-rendering.ts" drivers/pi/skill-map.md` ≥ 1.
- All three files still contain `pals-hooks.ts`, `module-activity-parsing.ts`, and `artifact-slice-rendering.ts` (existing references preserved).
- `git diff --name-only` for this task shows exactly: `README.md`, `drivers/pi/extensions/README.md`, `drivers/pi/skill-map.md`.
</verify>
<done>AC-1 evidence captured.</done>
</task>

<task id="2" name="Surface Phase 246 outcome in the extraction-target baseline">
<files>
- docs/PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md
</files>
<action>
1. Update the candidate-table row for S2 (current line 28: `| S2 \`workflow-resource-capsule-rendering\` | Still inline in \`pals-hooks.ts\` | Approved primary candidate, pairable with S1 if bounded |`) to: `| S2 \`workflow-resource-capsule-rendering\` | Extracted to \`drivers/pi/extensions/workflow-resource-capsule-rendering.ts\` (Phase 246 / PR #161) | Shipped — sibling-module pattern triple-confirmed |`. Do not modify rows for S1, S3, S4, S6, S7, or S8.
2. Add a new `## Phase 246 Outcome` subsection placed **immediately after** the existing `## Phase 243 Outcome` subsection. Section content (in order):
   - Opening sentence: "Phase 246 extracted the S2 `workflow-resource-capsule-rendering` subsystem from `drivers/pi/extensions/pals-hooks.ts` to a new sibling Pi extension module `drivers/pi/extensions/workflow-resource-capsule-rendering.ts` (192 LOC) following the Phase 239 S5 + Phase 243 S1 sibling-module pattern."
   - Bullet: **S2 `workflow-resource-capsule-rendering` extracted** — markers (`WORKFLOW_RESOURCE_CAPSULE_SCHEMA_MARKERS`, `MAX_WORKFLOW_RESOURCE_CAPSULE_CHARS=6_000`, `MAX_WORKFLOW_RESOURCE_CAPSULE_LINES=5`, the capsules banner, the `Fallback: full authoritative read…` line, and the `Authority: Derived aid only…` line) preserved single-defined in the new sibling.
   - Bullet: **Loader-compat invariant promoted to per-extraction acceptance criterion** — Phase 245's closure-phase hotfix (no-op default-exported factory + `No-op Pi extension factory` marker comment) is now asserted inline in the Phase 246 extraction TAP guardrail, catching Pi extension-loader breakage at extraction time rather than at the next session start.
   - Bullet: **Sibling-module pattern triple-confirmed** — S5 (Phase 239) + S1 (Phase 243) + S2 (Phase 246) all follow the same shape: dedicated `drivers/pi/extensions/<name>.ts`, helpers imported from `./pals-hooks`, single-defined exact-marker exports, trailing no-op default-exported factory + marker comment.
   - Bullet: **Helper-import tightening** — extracted siblings import only the helpers they actually reference (e.g., the new S2 sibling imports `compactWhitespace`, `readFileOr`, `getFileFreshness`, `selectBoundedLines` and omits `escapeRegExp`/`PalsStateSnapshot` because they are unreferenced).
   - Bullet: **`pals-hooks.ts` shrank by 142 LOC** (1415 → 1273); shared helpers (`compactWhitespace`, `readFileOr`, `getFileFreshness`, `selectBoundedLines`, `escapeRegExp`, type `PalsStateSnapshot`) remain defined and exported for future S3-S8 sibling extractions.
   - Bullet: **S3 `guided-workflow-detection` is deferred to v2.58 at the earliest** per the line-29 conditional-secondary classification ("larger and contract-sensitive, conditional secondary candidate only after S1/S2 bounds are stable"). Phase 246 stabilizes S2 but does not validate S3 readiness; the baseline says wait, and v2.57 honors that.
   - Bullet: **Validation evidence:** Pi `1..221/0`, cross-harness `1..129/0`, artifact_consistency PASS, install reports `[ok] Pi extensions installed: 4 files`, `git diff --check` clean. PR #161 merged 2026-05-02 (squash + delete-branch).
3. **Do not modify** the existing approved-wave selection prose, the line-29 S3 conditional-secondary classification, the S4/S6/S7/S8 deferral rationale, or any other content outside the targeted candidate-table row and the new subsection.
</action>
<verify>
- `grep -c "Extracted to \`drivers/pi/extensions/workflow-resource-capsule-rendering.ts\` (Phase 246 / PR #161)" docs/PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md` = 1.
- `grep -c "^## Phase 246 Outcome" docs/PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md` = 1.
- `grep -c "loader-compat invariant" docs/PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md` ≥ 1.
- `grep -c "sibling-module pattern triple-confirmed" docs/PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md` ≥ 1.
- `grep -c "Still inline in \`pals-hooks.ts\` | Approved primary candidate, pairable with S1" docs/PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md` = 0 (the old S2 row is replaced; not duplicated).
- `grep -c "S3 \`guided-workflow-detection\`" docs/PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md` ≥ 2 (line-29 classification preserved + new outcome reference).
- `grep -c "## Phase 243 Outcome" docs/PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md` = 1 (existing section preserved).
</verify>
<done>AC-2 evidence captured.</done>
</task>

<task id="3" name="Surface Phase 246 outcome in the modularization contract">
<files>
- docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md
</files>
<action>
1. Add a new `## Phase 246 Outcome` subsection placed **immediately after** the existing `## Phase 243 Outcome` subsection. Section content (in order):
   - Opening sentence: "Phase 246 extracted S2 to `drivers/pi/extensions/workflow-resource-capsule-rendering.ts` following the Phase 239 S5 + Phase 243 S1 sibling-module pattern. The contract-protected `WORKFLOW_RESOURCE_CAPSULE_SCHEMA_MARKERS` strings and the `MAX_WORKFLOW_RESOURCE_CAPSULE_*` caps are preserved single-defined in the new sibling; no marker is duplicated and none was dropped."
   - Bullet: **Loader-compat invariant** — every helper-only Pi extension sibling MUST end with a no-op default-exported factory `export default function (_pi: unknown): void {}` preceded by a JSDoc comment carrying the literal marker phrase `No-op Pi extension factory`. This is a per-extraction acceptance criterion (Phase 246 promotion from Phase 245's closure-phase hotfix). Pi auto-loads every `~/.pi/agent/extensions/*.ts` and rejects modules without a default-exported factory; without the loader-compat marker the sibling fails at session start with `Failed to load extension "...": Extension does not export a valid factory function`.
   - Bullet: **Triple-confirmed sibling-module pattern** — S5 (Phase 239 `module-activity-parsing.ts`) + S1 (Phase 243 `artifact-slice-rendering.ts`) + S2 (Phase 246 `workflow-resource-capsule-rendering.ts`) all match the same shape. The pattern is now ratified rather than provisional. Future S3/S4/S6/S7/S8 extractions can apply this recipe verbatim.
   - Bullet: **Helper-import tightening (canonical sibling-import discipline)** — extracted siblings import only the helpers they actually reference. The new S2 sibling imports `compactWhitespace`, `readFileOr`, `getFileFreshness`, `selectBoundedLines` and deliberately omits `escapeRegExp` and `PalsStateSnapshot` because the moved S2 code does not reference them; this keeps the sibling-import surface honest and the boundary minimal.
   - Bullet: **`pals-hooks.ts` post-extraction state** — 1273 LOC (was 1415; −142). The shared helpers `compactWhitespace`, `readFileOr`, `getFileFreshness`, `selectBoundedLines`, `escapeRegExp`, and the type `PalsStateSnapshot` remain defined and exported, so future S3–S8 extractions can import them without re-litigating the helper boundary.
   - Bullet: **Validation evidence:** Pi `1..221/0`, cross-harness `1..129/0`, artifact_consistency PASS, install reports `[ok] Pi extensions installed: 4 files`, `git diff --check` clean. PR #161 merged 2026-05-02.
2. **Do not modify** the existing S1–S8 risk-readiness table, the existing `## Phase 243 Outcome` subsection content, or any of the surrounding selection rationale. Only add the new `## Phase 246 Outcome` subsection.
</action>
<verify>
- `grep -c "^## Phase 246 Outcome" docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md` = 1.
- `grep -c "^## Phase 243 Outcome" docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md` = 1 (existing section preserved).
- `grep -c "loader-compat invariant" docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md` ≥ 1.
- `grep -c "Triple-confirmed sibling-module pattern" docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md` ≥ 1.
- `grep -c "helper-import tightening" docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md` ≥ 1 (case-insensitive search variant: `grep -ci "helper-import tightening"`).
- `grep -c "workflow-resource-capsule-rendering.ts" docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md` ≥ 1.
- `grep -c "S1 \`artifact-slice-rendering\` extracted" docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md` ≥ 1 (existing Phase 243 outcome preserved).
</verify>
<done>AC-3 evidence captured.</done>
</task>

<task id="4" name="Add Phase 247 closure guardrails (Pi e2e + cross-harness)">
<files>
- tests/pi-end-to-end-validation.sh
- tests/cross-harness-validation.sh
</files>
<action>
1. In `tests/pi-end-to-end-validation.sh`, immediately after the Phase 246 sibling-extraction blocks (the Phase 246 blocks added in Plan 246-01 Task 3, ending around the third Phase 246 block), append exactly two new `tap_file_contains_all` blocks under the shared title `Phase 247 sibling extraction surfacing (Pi-supported runtime)`. Mirror Phase 244's multi-file-split pattern (which uses the same shared title across one block per asserted file):
   - Block A: title `"Phase 247 sibling extraction surfacing (Pi-supported runtime): drivers/pi/extensions/README.md names workflow-resource-capsule-rendering.ts alongside the prior siblings"`, file `"$REPO_ROOT/drivers/pi/extensions/README.md"`, asserts the literal `workflow-resource-capsule-rendering.ts`.
   - Block B: title `"Phase 247 sibling extraction surfacing (Pi-supported runtime): drivers/pi/skill-map.md names workflow-resource-capsule-rendering.ts alongside the prior siblings"`, file `"$REPO_ROOT/drivers/pi/skill-map.md"`, asserts the literal `workflow-resource-capsule-rendering.ts`.
   Add a banner comment matching the Phase 244 / Phase 245 / Phase 246 banner style above the new blocks.
2. In `tests/cross-harness-validation.sh`, immediately after the existing Phase 244 sibling-extraction-surfacing block (ends around line 1260) and **before** the Phase 245 sibling-extraction-wave-closure block, append exactly one new `tap_file_contains_all` block titled `Phase 247 sibling extraction surfacing (Shared Invariant): extraction-target baseline names workflow-resource-capsule-rendering.ts and the loader-compat invariant`, file `"$REPO_ROOT/docs/PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md"`, asserting the literals `workflow-resource-capsule-rendering.ts` and `loader-compat invariant`. Add a banner comment matching the Phase 244 banner style.
3. **Do not modify** existing TAP blocks, helper functions, banners, or unrelated suite structure. Do not touch `tests/helpers/**`. Do not modify the suite header or `# Total:` reconciliation logic — the new blocks adjust the TAP plan automatically.
</action>
<verify>
- `grep -c "Phase 247 sibling extraction surfacing (Pi-supported runtime)" tests/pi-end-to-end-validation.sh` = 2 (one per asserted file under shared title).
- `grep -c "Phase 247 sibling extraction surfacing (Shared Invariant)" tests/cross-harness-validation.sh` = 1.
- `bash tests/pi-end-to-end-validation.sh` exits 0 with `# Total:  223` (planned +2 vs Phase 246 baseline 221).
- `bash tests/cross-harness-validation.sh` exits 0 with `# Total:  130` (planned +1 vs Phase 246 baseline 129).
- `bash tests/helpers/artifact_consistency.sh` reports `artifact_consistency_check: PASS`.
- `git diff --check` clean.
- `git diff --name-only` shows: README.md, drivers/pi/extensions/README.md, drivers/pi/skill-map.md, docs/PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md, docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md, tests/pi-end-to-end-validation.sh, tests/cross-harness-validation.sh — and **no** files under `drivers/pi/extensions/*.ts`, `drivers/pi/install.sh`, `drivers/pi/uninstall.sh`, `drivers/pi/driver.yaml`, `kernel/`, `modules/`, `tests/helpers/`, `package.json`, or `pals.json`.
</verify>
<done>AC-4 + AC-5 evidence captured.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE

- `drivers/pi/extensions/pals-hooks.ts`, `drivers/pi/extensions/module-activity-parsing.ts`, `drivers/pi/extensions/artifact-slice-rendering.ts`, `drivers/pi/extensions/workflow-resource-capsule-rendering.ts` — source files; surfacing is prose-only.
- `drivers/pi/install.sh`, `drivers/pi/uninstall.sh`, `drivers/pi/driver.yaml` — install surface; the source-set rule already covers all 4 siblings.
- `kernel/`, `modules/`, `pals.json`, `package.json`, `tests/helpers/**`, `.github/` — out of scope.
- Existing TAP blocks (Phase 244 surfacing, Phase 245 loader-compat, Phase 245 closure, Phase 246 extraction blocks) — append new blocks; do not edit existing ones.
- Existing `## Phase 243 Outcome` subsection in either contract doc — preserve verbatim; add the new `## Phase 246 Outcome` subsection alongside it.
- Existing approved-wave selection prose, S3/S4/S6/S7/S8 deferral rationale, and risk-readiness tables in both contract docs — preserve verbatim.

## SCOPE LIMITS

- Plan 247-01 is documentation + validation surfacing only. No source/runtime/install/dep/CI/lifecycle-authority changes.
- This plan does not author new contract docs.
- This plan does not aggregate evidence into `MILESTONES.md` / `PROJECT.md` (reserved for Phase 248).
- This plan does not change the install surface count (stays at 4 files; install-count claims in prose are updated to "4 files" but the installer command output is unchanged).

</boundaries>

<verification>

## Overall Completion Checks

1. README.md, drivers/pi/extensions/README.md, drivers/pi/skill-map.md each name `workflow-resource-capsule-rendering.ts` alongside the prior three siblings.
2. docs/PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md candidate-table S2 row updated; new `## Phase 246 Outcome` subsection added; line-29 S3 classification preserved.
3. docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md gains `## Phase 246 Outcome` subsection naming the loader-compat invariant, triple-confirmed sibling-module pattern, and helper-import tightening.
4. tests/pi-end-to-end-validation.sh: +2 blocks under shared title `Phase 247 sibling extraction surfacing (Pi-supported runtime)`; suite green at `1..223`.
5. tests/cross-harness-validation.sh: +1 block titled `Phase 247 sibling extraction surfacing (Shared Invariant)`; suite green at `1..130`.
6. All four official validation commands pass with command-output evidence.
7. Changed-file set matches AC-5 exactly.

</verification>

<success_criteria>

- [ ] AC-1 PASS: top-level user-facing docs name `workflow-resource-capsule-rendering.ts`.
- [ ] AC-2 PASS: extraction-target baseline reflects Phase 246 outcome; line-29 S3 classification preserved.
- [ ] AC-3 PASS: modularization contract names loader-compat invariant + triple-confirmed pattern + helper-import tightening.
- [ ] AC-4 PASS: Pi suite +2 (`1..223`); cross-harness +1 (`1..130`); both green; artifact_consistency PASS; `git diff --check` clean.
- [ ] AC-5 PASS: only the planned files modified; no source/runtime/install/dep/CI drift.
- [ ] SUMMARY.md drafted at `.paul/phases/247-documentation-validation-surfacing-v2-57/247-01-SUMMARY.md` reconciling expected vs actual counts and recording any banner-style or block-placement adaptations.

</success_criteria>

<output>

`.paul/phases/247-documentation-validation-surfacing-v2-57/247-01-SUMMARY.md` (drafted in UNIFY).

</output>
