---
phase: 289-hot-artifact-budget-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .paul/PROJECT.md
  - .paul/ROADMAP.md
  - .paul/archive/project/PROJECT-HISTORY-v2.61-v2.66.md
autonomous: true
---

<objective>
## Goal
Bring `.paul/PROJECT.md` and `.paul/ROADMAP.md` back inside v2.62 hot-artifact budget expectations by archiving completed-milestone detail and preserving only current routing, constraints, compact milestone posture, and archive pointers in the hot artifacts.

## Purpose
Phase 288 cleared inherited CODI marker drift, leaving hot-artifact budget drift as the active v2.67 validation concern. This plan restores the context-diet contract without changing product/runtime behavior, lifecycle authority, module semantics, or validation thresholds.

## Output
- `.paul/PROJECT.md` is below the 25,000-byte v2.62 ceiling while retaining current focus, constraints, active requirements, current milestone posture, latest closed milestone pointer, and archive links.
- `.paul/ROADMAP.md` remains below the 12,000-byte ceiling and 120-line active-window expectation with the active v2.67 milestone intact.
- Completed milestone detail moved out of PROJECT hot path is preserved in a new archive file: `.paul/archive/project/PROJECT-HISTORY-v2.61-v2.66.md`.
</objective>

<context>
## Project Context
.paul/STATE.md — Phase 289 ready to PLAN after Phase 288 UNIFY; remaining Pi e2e failure is PROJECT byte-budget drift.
.paul/ROADMAP.md — Phase 289 focus: bring `.paul/PROJECT.md` and `.paul/ROADMAP.md` back under v2.62 byte-budget expectations through archive/deduplication hygiene while preserving authoritative current routing and archive links.
.paul/PROJECT.md — hot-path model defines v2.62 budgets: PROJECT ≤ 25 KB, ROADMAP ≤ 12 KB / ≤ 120 active-window lines.

## Prior Work
.paul/phases/288-codi-validation-marker-hygiene/288-01-SUMMARY.md — Phase 288 cleared CODI marker drift; Pi e2e now has only PROJECT byte-budget drift remaining, cross-harness remains passing.
docs/PALS-STATE-CONTEXT-DIET-CONTRACT.md — authoritative context-diet contract and archive/compact pattern.

## Baseline Evidence
- `.paul/PROJECT.md`: 28,201 bytes, above the 25,000-byte ceiling.
- `.paul/ROADMAP.md`: 11,337 bytes, currently below the 12,000-byte ceiling but still in Phase 289 hygiene scope for active-window preservation.
- `bash tests/pi-end-to-end-validation.sh` after Phase 288: `334/335`; remaining failure is `Repo PROJECT stays within v2.62 byte budget`.

## Source Files
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/archive/project/PROJECT-HISTORY-v2.61-v2.66.md
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: TODD/WALT context — validation commands are shell/TAP suites; active acceptance target is the existing Pi e2e context-diet byte-ceiling check plus artifact consistency. `tdd_candidates`: none beyond existing TAP validation.
[dispatch] pre-plan advisory: IRIS — No TODO/FIXME/HACK/XXX markers in planned hot artifacts.
[dispatch] pre-plan advisory: DAVE — No CI config files detected or planned.
[dispatch] pre-plan advisory: DOCS — Lifecycle documentation/artifact hygiene scope; archive links must remain explicit in hot artifacts.
[dispatch] pre-plan advisory: CODI — skipped — markdown-only lifecycle artifacts, no TS/JS selectors or extractable symbols.
[dispatch] pre-plan advisory: RUBY — docs/lifecycle-artifact scope; no code-debt candidates.
[dispatch] pre-plan advisory: ARCH/ARIA/DANA/GABE/LUKE/OMAR/PETE/REED/SETH/VERA — skipped/no concerns for markdown-only lifecycle artifacts with no UI/API/data/runtime source changes.
[dispatch] pre-plan enforcement: DEAN PASS — no dependency manifest/lockfile found, so vulnerability audit scope is unavailable and no critical/high dependency delta can be established for this plan.
[dispatch] post-plan: TODD — Plan remains `execute`; existing TAP validation covers the planned artifact-budget behavior, no TDD restructure required.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | PLAN/APPLY/UNIFY lifecycle management | ✓ |
| /carl | required | Session boundary monitoring | automatic |
| TODD | required | Before/during implementation validation | ✓ via module dispatch |
| WALT | required | Post-implementation quality validation | pending APPLY |

**BLOCKING:** Required skills must remain active before APPLY proceeds.
</skills>

<acceptance_criteria>
## AC-1: PROJECT hot artifact is back under budget
```gherkin
Given `.paul/PROJECT.md` is over the v2.62 25 KB hot-artifact byte ceiling
When completed-milestone detail is archived and the hot PROJECT posture is compacted
Then `.paul/PROJECT.md` is at or below 25,000 bytes and still preserves current focus, constraints, active requirements, current v2.67 posture, and archive pointers
```

## AC-2: ROADMAP active routing remains compact and authoritative
```gherkin
Given `.paul/ROADMAP.md` owns current milestone and phase routing
When Phase 289 performs hot-artifact hygiene
Then ROADMAP remains under the 12 KB byte ceiling and 120-line active-window ceiling while preserving v2.67 phases 288-290, Phase 289 routing, and completed-history archive links
```

## AC-3: Archive preserves moved history without behavior changes
```gherkin
Given completed milestone detail is removed from hot PROJECT narrative
When that detail is moved to `.paul/archive/project/PROJECT-HISTORY-v2.61-v2.66.md`
Then no authoritative history is lost, hot artifacts point to the archive, and validation passes without product/runtime, module, or test-threshold changes
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Archive completed milestone detail from PROJECT hot path</name>
  <files>.paul/PROJECT.md, .paul/archive/project/PROJECT-HISTORY-v2.61-v2.66.md</files>
  <action>
    Create `.paul/archive/project/PROJECT-HISTORY-v2.61-v2.66.md` and move the long completed-milestone detail currently bloating `.paul/PROJECT.md` into it, especially v2.61-v2.66 rows whose detail is no longer required in the hot brief.
    Replace those hot PROJECT rows with compact one-line status summaries and explicit archive pointers.
    Preserve current focus, hot-path reading model, active requirements summary, current v2.67 posture, constraints, success metrics, and footer/update metadata.
    Do not delete history; do not change lifecycle authority, module semantics, validation ceilings, or product/runtime source files.
  </action>
  <verify>`nu -c 'ls .paul/PROJECT.md | get size'` reports a PROJECT size below 25 KB, and `.paul/PROJECT.md` contains the new archive pointer.</verify>
  <done>AC-1 and AC-3 satisfied: PROJECT is compact and moved history is preserved in archive.</done>
</task>

<task type="auto">
  <name>Task 2: Keep ROADMAP current window compact and aligned</name>
  <files>.paul/ROADMAP.md</files>
  <action>
    Review `.paul/ROADMAP.md` for Phase 289 routing and active-window budget compliance.
    Keep the v2.67 current milestone table, Phase 288 result, Phase 289 focus, and Phase 290 focus intact.
    Compact any avoidable duplicate completed-milestone narrative if needed, but preserve completed-history archive pointers and latest completed milestone references.
    Do not broaden scope into Phase 290 validation closure or completed-milestone archival outside the active ROADMAP window unless required to stay under budget.
  </action>
  <verify>`nu -c 'ls .paul/ROADMAP.md | get size'` reports ROADMAP below 12 KB, and `bash tests/helpers/artifact_consistency.sh` passes.</verify>
  <done>AC-2 satisfied: ROADMAP remains compact, authoritative, and aligned with Phase 289 routing.</done>
</task>

<task type="auto">
  <name>Task 3: Run hot-artifact validation closure for Phase 289 scope</name>
  <files>.paul/PROJECT.md, .paul/ROADMAP.md, .paul/archive/project/PROJECT-HISTORY-v2.61-v2.66.md</files>
  <action>
    Run the scoped artifact and validation checks after compaction.
    Confirm the prior PROJECT byte-budget failure is cleared.
    Confirm the Phase 288 CODI semantic checks remain passing and no new hot-artifact failures appear.
    If validation exposes unrelated new failures, stop and classify before changing scope.
  </action>
  <verify>`bash tests/helpers/artifact_consistency.sh` passes; `bash tests/pi-end-to-end-validation.sh` passes all checks or has no PROJECT/ROADMAP budget failure; `bash tests/cross-harness-validation.sh` passes or shows no new artifact-budget regression.</verify>
  <done>AC-1, AC-2, and AC-3 satisfied: v2.67 hot-artifact budget hygiene is verified and ready for UNIFY.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- Product/runtime source behavior.
- `tests/pi-end-to-end-validation.sh` validation thresholds, ceilings, or assertions.
- `tests/helpers/artifact_consistency.sh` or context-diet validation helpers.
- Module instruction semantics, CODI authority/advisory behavior, or installed-resource content.
- Phase 288 summary evidence except for normal lifecycle references if required.

## SCOPE LIMITS
- This phase is hot-artifact archive/deduplication hygiene only.
- Do not hide or reclassify validation failures by weakening tests.
- Do not remove archive links or authoritative historical references; move cold detail to archive files instead.
- Do not complete v2.67; Phase 290 owns final full validation closure.
</boundaries>

<verification>
Before declaring APPLY complete:
- [ ] `.paul/PROJECT.md` is at or below 25,000 bytes.
- [ ] `.paul/ROADMAP.md` is at or below 12,000 bytes and the active window remains under 120 lines.
- [ ] `.paul/archive/project/PROJECT-HISTORY-v2.61-v2.66.md` preserves moved completed-milestone detail and is linked from PROJECT.
- [ ] `bash tests/helpers/artifact_consistency.sh` passes.
- [ ] `bash tests/pi-end-to-end-validation.sh` no longer fails PROJECT/ROADMAP hot-artifact budget checks.
- [ ] `bash tests/cross-harness-validation.sh` passes or has no new artifact-budget regression.
- [ ] No product/runtime files changed.
- [ ] All acceptance criteria met.
</verification>

<success_criteria>
- Hot `.paul/PROJECT.md` is back under the v2.62 byte budget without losing history.
- `.paul/ROADMAP.md` remains compact and authoritative for v2.67 routing.
- Moved completed milestone detail is preserved in archive and linked from hot artifacts.
- The inherited PROJECT byte-budget Pi e2e failure is cleared without validation-threshold changes.
</success_criteria>

<output>
After completion, create `.paul/phases/289-hot-artifact-budget-hygiene/289-01-SUMMARY.md` with:
- before/after byte counts for PROJECT and ROADMAP;
- archive file created and archive-link evidence;
- Pi e2e and cross-harness validation results;
- explicit note that Phase 290 remains responsible for final v2.67 validation closure;
- Module execution reports for WALT/SKIP/CODI/RUBY as applicable.
</output>
