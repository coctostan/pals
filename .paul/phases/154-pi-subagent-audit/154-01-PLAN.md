---
phase: 154-pi-subagent-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .paul/phases/154-pi-subagent-audit/pi-subagents-current-state.md
  - .paul/phases/154-pi-subagent-audit/subagent-runtime-behavior.md
  - .paul/phases/154-pi-subagent-audit/pals-implementer-audit-summary.md
autonomous: true
---

<objective>
## Goal
Produce a concrete current-state audit of how PALS uses the `pi-subagents` extension today, with enough evidence to drive a PALS-native implementer design in Phase 155.

## Purpose
This phase turns the user's milestone intent into explicit evidence: what subagents PALS actually uses, how those agents are configured, what prompts/tools/models/extensions they receive, whether PALS modules reach them, and where current behavior diverges from the desired inline-APPLY-like implementer model.

## Output
- `.paul/phases/154-pi-subagent-audit/pi-subagents-current-state.md`
- `.paul/phases/154-pi-subagent-audit/subagent-runtime-behavior.md`
- `.paul/phases/154-pi-subagent-audit/pals-implementer-audit-summary.md`
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md
pals.json
.paul/SPECIAL-FLOWS.md

## Prior Work (only if genuinely needed)
.paul/phases/151-research-and-design/rev-design.md
.paul/phases/153-validation-and-polish/153-01-SUMMARY.md

## Source Files
drivers/pi/skills/paul-review/SKILL.md
drivers/pi/extensions/pals-hooks.ts
kernel/workflows/research.md
kernel/workflows/map-codebase.md
kernel/workflows/unify-phase.md
modules/rev/references/review-prompt.md

## External Runtime Context
The audit should read the current global Pi subagent definitions as runtime evidence:
- `~/.pi/agent/agents/code-reviewer.md`
- `~/.pi/agent/agents/implementer.md`
- `~/.pi/agent/agents/worker.md`
- `~/.pi/agent/agents/spec-reviewer.md`

Use these only as read-only audit inputs. Do not modify them in this plan.
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: TODD(100) → test infrastructure not detected from project manifests; shell validation scripts exist under `tests/` but no Jest/Vitest/Pytest/Go test config was found | IRIS(150) → no anti-pattern signatures found in planned phase artifacts path | DAVE(200) → no CI/CD config detected in repo root or `.github/workflows/` | DOCS(200) → external documentation catalog exists; planned outputs are audit markdown artifacts under `.paul/phases/154-pi-subagent-audit/` | RUBY(250) → docs-only planned files; no preliminary debt indicators

[dispatch] pre-plan enforcement: DEAN(50) → skipped gracefully; no supported package manager/audit manifest detected in repo root and no `.paul/dean-baseline.json` exists
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | Core lifecycle execution for this audit plan | ✓ |

**BLOCKING:** Required skills MUST be loaded before APPLY proceeds.
Run each skill command or confirm already loaded.

## Skill Invocation Checklist
- [x] /paul loaded in current session
</skills>

<acceptance_criteria>
## AC-1: Current `pi-subagents` integration is inventoried
```gherkin
Given the current PALS repo and active Pi runtime agent definitions
When the audit is completed
Then there is an evidence-backed inventory of the live PALS ↔ `pi-subagents` integration points, including prompts, tools, models, extensions, routing paths, and relevant config inputs
```

## AC-2: Runtime behavior and module reach are explained
```gherkin
Given PALS workflows that currently dispatch or reference subagents
When the runtime behavior report is written
Then it explains which workflows invoke which subagent types, how prompts are assembled, and whether enabled PALS modules are inherited, explicitly reloaded, or not propagated into subagents today
```

## AC-3: Phase 155 has explicit design inputs
```gherkin
Given the user's goal of a PALS-native implementer that behaves like inline APPLY with fresh context
When the audit summary is completed
Then it captures the current gaps, protected constraints, unexpected agent-usage findings, and explicit requirements/questions that Phase 155 must resolve before implementation
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Create the current-state `pi-subagents` inventory</name>
  <files>.paul/phases/154-pi-subagent-audit/pi-subagents-current-state.md</files>
  <action>
    Create a current-state inventory document that answers, with file-path evidence:
    - Where PALS currently invokes or references `pi-subagents`
    - Which subagent types are used in repo-controlled workflows today
    - What the current global Pi agent definitions specify for prompts, tools, models, and extensions
    - How `pals.json` currently influences subagent behavior (for example REV model override or absence of an `agents` block)
    - Which behavior is repo-defined vs runtime-global (`~/.pi/agent/agents/*.md`)

    Structure the document so it is directly usable in later design work:
    - Integration points table
    - Agent definitions table
    - Config/routing notes
    - Evidence section with concrete file paths

    Avoid: making design recommendations in this document. This artifact is for current-state facts, not solutions.
  </action>
  <verify>grep -n "Integration Points\|Agent Definitions\|pals.json\|code-reviewer\|implementer" .paul/phases/154-pi-subagent-audit/pi-subagents-current-state.md</verify>
  <done>AC-1 satisfied: current live integration points, agent definitions, and config influences are documented with evidence</done>
</task>

<task type="auto">
  <name>Task 2: Trace runtime behavior and module propagation</name>
  <files>.paul/phases/154-pi-subagent-audit/subagent-runtime-behavior.md</files>
  <action>
    Create a runtime behavior report that traces how PALS actually uses subagents today.

    Cover at minimum:
    - Research/discovery paths (`kernel/workflows/research.md`, `kernel/workflows/map-codebase.md`)
    - REV review paths (`drivers/pi/skills/paul-review/SKILL.md`, `kernel/workflows/unify-phase.md`)
    - Whether repo-controlled PALS currently dispatches `implementer`, `worker`, or `spec-reviewer`
    - If the user-observed `worker` / `implementer` behavior is not repo-controlled, explicitly classify that as out-of-band/runtime-level evidence to be carried forward
    - Whether modules like TODD/WALT are actually injected into subagents, merely enforced in the parent workflow, or only present via separate extensions/prompts

    Include one explicit section named "Module Reach" that distinguishes:
    - parent workflow module dispatch
    - subagent prompt inheritance
    - subagent extension loading
    - absent/unknown behavior

    Avoid: implementing fixes or changing behavior in this phase.
  </action>
  <verify>grep -n "Runtime Paths\|Module Reach\|REV\|research\|implementer\|worker" .paul/phases/154-pi-subagent-audit/subagent-runtime-behavior.md</verify>
  <done>AC-2 satisfied: runtime behavior and current module propagation model are explained with evidence and clear boundaries</done>
</task>

<task type="auto">
  <name>Task 3: Produce the PALS implementer audit summary for Phase 155</name>
  <files>.paul/phases/154-pi-subagent-audit/pals-implementer-audit-summary.md</files>
  <action>
    Create a synthesis artifact that converts the audit into explicit Phase 155 inputs.

    The summary must include:
    - Confirmed findings that matter to a PALS-native implementer design
    - Gaps between current runtime behavior and the desired inline-APPLY-like behavior
    - Constraints that Phase 155 must preserve (`.paul/*` authority, additive-only changes, preserve REV, Pi-only scope, no forced PALS-awareness for every subagent, inline APPLY as reference)
    - A recommendation about generic `worker` / `implementer` usage for now
    - Open questions Phase 155 must answer before implementation

    Keep this document design-facing but still grounded in audit evidence. It should be the clean handoff into the next phase, not a replacement for the detailed audit files.

    Avoid: drafting the new implementer prompt or editing any runtime files in this phase.
  </action>
  <verify>grep -n "Confirmed Findings\|Gaps\|Constraints\|Recommendations\|Open Questions\|Phase 155" .paul/phases/154-pi-subagent-audit/pals-implementer-audit-summary.md</verify>
  <done>AC-3 satisfied: explicit design inputs for the PALS-native implementer phase are captured and traceable to current-state findings</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- drivers/pi/extensions/pals-hooks.ts
- drivers/pi/skills/paul-review/SKILL.md
- kernel/workflows/research.md
- kernel/workflows/map-codebase.md
- kernel/workflows/unify-phase.md
- pals.json
- `~/.pi/agent/agents/*.md` runtime agent definitions

## SCOPE LIMITS
- This plan is audit-only. Do not design or implement the new PALS-native implementer yet.
- Do not change REV behavior, Pi extension behavior, or global agent definitions in this phase.
- Do not redesign the core PALS lifecycle or introduce cross-harness changes.
- Do not attempt to make every subagent PALS-aware in this plan.
</boundaries>

<verification>
Before declaring plan complete:
- [ ] All three audit artifacts exist in `.paul/phases/154-pi-subagent-audit/`
- [ ] Each artifact cites concrete repo and/or runtime file paths as evidence
- [ ] The runtime behavior artifact has an explicit "Module Reach" section
- [ ] The summary artifact includes confirmed findings, gaps, constraints, recommendations, and open questions
- [ ] All acceptance criteria are met
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No source/runtime behavior changed during this audit phase
- Phase 155 has a clean, evidence-backed handoff for implementer design
</success_criteria>

<output>
After completion, create `.paul/phases/154-pi-subagent-audit/154-01-SUMMARY.md`
</output>
