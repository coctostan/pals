---
phase: 68-execution-audit
plan: 01
type: research
wave: 1
depends_on: []
files_modified:
  - .paul/phases/68-execution-audit/module-hook-inventory.md
  - .paul/phases/68-execution-audit/module-execution-audit.md
  - .paul/phases/68-execution-audit/module-gap-report.md
autonomous: true
---

<objective>
## Goal
Produce a codebase-grounded audit that shows which PALS modules are declared, which ones can actually fire today across PLAN/APPLY/UNIFY, where visibility is missing, and which confirmed gaps should feed later fix and UX phases.

## Purpose
The user wants proof, not assumptions: are the additional modules actually doing anything today, or are they mostly invisible bloat? Phase 68 reduces guesswork by separating source-declared intent, installed-registry/runtime reality, and current user-visible surfaces before any wiring fixes, TUI changes, or module recommendations are attempted.

## Output
- `.paul/phases/68-execution-audit/module-hook-inventory.md`
- `.paul/phases/68-execution-audit/module-execution-audit.md`
- `.paul/phases/68-execution-audit/module-gap-report.md`
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md
.paul/SPECIAL-FLOWS.md

## Prior Work (only if genuinely needed)
- `.paul/MILESTONES.md` — reference prior module rollout, observability, and Pi lifecycle milestones for historical intent
- `.paul/milestones/v2.6-ROADMAP.md` — confirms the current milestone boundary and that Phase 68 should start with audit-first work
- Recent state decisions and fixes in `.paul/STATE.md` — especially the installed-registry `hook_details` alignment work from Phase 63 and the Pi lifecycle visibility work from Phases 59-64

## Source Files
- `modules/*/module.yaml` — all source module manifests (`carl`, `dave`, `dean`, `docs`, `iris`, `ruby`, `skip`, `todd`, `walt`)
- `/Users/maxwellnewman/.pi/agent/skills/pals/modules.yaml` — current installed Pi module registry for runtime reality checks
- `kernel/references/module-dispatch.md`
- `docs/REFERENCE-RESOLUTION.md`
- `kernel/workflows/plan-phase.md`
- `kernel/workflows/apply-phase.md`
- `kernel/workflows/unify-phase.md`
- `kernel/templates/SUMMARY.md`
- `drivers/pi/extensions/pals-hooks.ts`
- `drivers/pi/extensions/README.md`
- `tests/cross-harness-validation.sh`
- `tests/pi-end-to-end-validation.sh`

## Notes
- This is an audit-only research plan. Do not change kernel, driver, module, install, or validation behavior during APPLY.
- The repo source does **not** contain `kernel/modules.yaml`; the current Pi install instead exposes the active registry at `~/.pi/agent/skills/pals/modules.yaml`. Treat that distinction as an audit subject, not as something to “fix” in this phase.
- Focus on PLAN/APPLY/UNIFY lifecycle behavior first. Mention fix-loop behavior only if it materially clarifies a confirmed finding.
- The user explicitly wants: inspect the current module invocation paths on branch `feature/67-onboarding-prd-depth`, report what fires and what does not, and then await direction before Phase 69 gap fixes.
</context>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | For the PLAN → APPLY → UNIFY lifecycle work in this audit-only phase | ○ |

**BLOCKING:** Required skills MUST be loaded before APPLY proceeds.
Run each skill command or confirm already loaded.

## Skill Invocation Checklist
- [ ] /paul loaded (run command or confirm)
</skills>

<acceptance_criteria>
## AC-1: Module hook inventory is complete and source-backed
```gherkin
Given the source module manifests, the dispatch contract, and the current installed Pi module registry
When `module-hook-inventory.md` is complete
Then it lists every module, its declared lifecycle hooks or explicit no-hook status, its source-vs-installed registry shape, and any mismatches with file-backed evidence
```

## AC-2: Execution audit distinguishes what is proven, conditional, absent, and invisible
```gherkin
Given the PLAN/APPLY/UNIFY workflows, installed registry expectations, Pi lifecycle surfaces, and SUMMARY reporting template
When `module-execution-audit.md` is complete
Then it shows for each module and lifecycle phase whether execution is confirmed, registry-dependent, intentionally absent, or currently invisible to the user, with citations to the relevant files
```

## AC-3: Findings are severity-ordered and ready for later phases without speculative fixes
```gherkin
Given the completed inventory and execution audit
When `module-gap-report.md` is written
Then it orders confirmed gaps by severity and impact, distinguishes execution gaps from visibility gaps, maps each finding to the appropriate follow-on phase (69, 70, or 71), and explicitly stops for user direction before any fixes begin
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Build the module hook inventory from source manifests and the installed Pi registry</name>
  <files>.paul/phases/68-execution-audit/module-hook-inventory.md</files>
  <action>
    Read all source module manifests under `modules/*/module.yaml`, the dispatch contract in `kernel/references/module-dispatch.md`, the adapter resolution notes in `docs/REFERENCE-RESOLUTION.md`, and the current Pi installed registry at `/Users/maxwellnewman/.pi/agent/skills/pals/modules.yaml`.
    Write one inventory artifact that captures, for every module:
    - whether it participates in PAUL lifecycle hooks at all
    - which hooks are declared in source manifests
    - which hooks appear in the installed Pi registry
    - priorities, refs, and hook descriptions where available
    - whether the module is intentionally outside lifecycle dispatch (for example CARL)
    Include a compact hook matrix covering `pre-plan`, `post-plan`, `pre-apply`, `post-task`, `post-apply`, `pre-unify`, and `post-unify`.
    Highlight source-vs-installed mismatches and notable absences, but do **not** classify them as bugs or propose fixes yet.
    Avoid mixing in workflow-trace conclusions here; keep this artifact focused on declared capability and registry reality.
  </action>
  <verify>test -f .paul/phases/68-execution-audit/module-hook-inventory.md && grep -q "Module Hook Inventory" .paul/phases/68-execution-audit/module-hook-inventory.md && grep -q "todd" .paul/phases/68-execution-audit/module-hook-inventory.md && grep -q "carl" .paul/phases/68-execution-audit/module-hook-inventory.md</verify>
  <done>AC-1 satisfied: every module's declared hook participation and source-vs-installed registry status is captured with evidence</done>
</task>

<task type="auto">
  <name>Task 2: Trace actual PLAN/APPLY/UNIFY execution paths and current module visibility surfaces</name>
  <files>.paul/phases/68-execution-audit/module-execution-audit.md</files>
  <action>
    Read `kernel/workflows/plan-phase.md`, `kernel/workflows/apply-phase.md`, `kernel/workflows/unify-phase.md`, `kernel/templates/SUMMARY.md`, `drivers/pi/extensions/pals-hooks.ts`, `drivers/pi/extensions/README.md`, and the two validation suites.
    Trace how module dispatch is supposed to happen in each lifecycle stage, where it depends on installed registry resolution, and what current user-facing surfaces exist for module visibility.
    For each module across PLAN/APPLY/UNIFY, classify the current state using evidence-backed labels such as:
    - confirmed dispatch path exists in current workflow + installed registry
    - registry-dependent / environment-dependent (declared, but runtime proof depends on install state or external execution)
    - intentionally absent from lifecycle hooks
    - not surfaced to the user in live UX or end reporting
    Explicitly cover whether current Pi lifecycle UI and SUMMARY reporting surfaces mention module participation today, and where that visibility appears absent or partial.
    Stay strict about evidence: if runtime execution cannot be proven from code, registry, or validation surface, mark it as conditional/uncertain rather than assuming it works.
  </action>
  <verify>test -f .paul/phases/68-execution-audit/module-execution-audit.md && grep -q "PLAN / APPLY / UNIFY" .paul/phases/68-execution-audit/module-execution-audit.md && grep -q "Visibility Baseline" .paul/phases/68-execution-audit/module-execution-audit.md</verify>
  <done>AC-2 satisfied: the audit distinguishes execution reality and current user visibility for every module with file-backed evidence</done>
</task>

<task type="auto">
  <name>Task 3: Produce the severity-ordered findings report and handoff for later phases</name>
  <files>.paul/phases/68-execution-audit/module-gap-report.md</files>
  <action>
    Synthesize the completed inventory and execution audit into one ordered findings report.
    Separate findings into clear buckets:
    - confirmed working / already wired
    - confirmed execution gaps
    - confirmed visibility gaps
    - intentional non-hook modules or advisory-only cases
    - items that belong to Phase 71 recommendation work rather than Phase 69 or 70 implementation
    For every confirmed gap, include:
    - severity / impact
    - affected module(s)
    - affected lifecycle phase(s)
    - evidence source(s)
    - why the issue matters to the user concern about invisible bloat or missing execution
    - which later phase should handle it (`69-gap-fixes`, `70-visibility-ux`, or `71-recommendations`)
    End the report with an explicit “await direction before Phase 69” handoff so the milestone follows the user’s audit-first constraint.
    Do **not** prescribe speculative code changes or cleanup work that is not justified by the audit artifacts.
  </action>
  <verify>test -f .paul/phases/68-execution-audit/module-gap-report.md && grep -q "Confirmed Gaps" .paul/phases/68-execution-audit/module-gap-report.md && grep -q "Await Direction" .paul/phases/68-execution-audit/module-gap-report.md</verify>
  <done>AC-3 satisfied: the findings report is severity-ordered, evidence-based, and ready to drive later phases without speculative fixes</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- `kernel/workflows/*` (this phase audits dispatch behavior; it does not implement fixes)
- `modules/*/module.yaml` and installed module registries (record reality; do not rewrite manifests or registry contents)
- `drivers/pi/extensions/*` (no live UX implementation changes yet)
- `kernel/templates/SUMMARY.md` and other shared templates (report current behavior only)
- `tests/*`, `install.sh`, `uninstall.sh`, and adapter/runtime files outside normal `.paul/` lifecycle tracking

## SCOPE LIMITS
- This plan produces audit artifacts only under `.paul/phases/68-execution-audit/`
- Do not implement wiring fixes, TUI integration, end-report changes, or module removals in Phase 68
- Do not invent gaps or fixes not supported by code inspection or installed-registry evidence
- Keep the primary focus on PLAN/APPLY/UNIFY lifecycle behavior; do not let side-loop analysis take over the phase
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `module-hook-inventory.md` exists and covers all modules with source-vs-installed hook evidence
- [ ] `module-execution-audit.md` distinguishes confirmed, conditional, absent, and invisible states across PLAN/APPLY/UNIFY
- [ ] `module-gap-report.md` orders confirmed findings by severity/impact and maps them to Phases 69, 70, or 71
- [ ] No kernel, module, driver, or validation behavior was modified during the audit
- [ ] All acceptance criteria are satisfied
</verification>

<success_criteria>
- All three audit artifacts are created in `.paul/phases/68-execution-audit/`
- The user can answer “what fires, what doesn’t, and what is invisible today?” from the artifacts alone
- Later milestone phases are grounded in confirmed findings rather than assumptions
- The resulting audit is specific enough that Phase 69 and Phase 70 can proceed without redoing the discovery work
</success_criteria>

<output>
After completion, create `.paul/phases/68-execution-audit/68-01-SUMMARY.md`
</output>
