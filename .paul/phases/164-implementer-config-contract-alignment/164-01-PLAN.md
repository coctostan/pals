---
phase: 164-implementer-config-contract-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kernel/workflows/init-project.md
  - kernel/references/pals-json-schema.md
  - tests/pi-end-to-end-validation.sh
  - tests/cross-harness-validation.sh
autonomous: true
---

<objective>
## Goal
Align the canonical `pals.json` contract so explicit `agents.implementer` config is created by init, preserved by migration, documented in the schema reference, and covered by validation that proves the contract exists before delegated APPLY policy expands further.

## Purpose
Phase 156 introduced a narrow runtime/config surface for delegated APPLY, but the canonical init and schema sources are still behind that runtime expectation. This phase closes that contract drift first so later delegation expansion work in Phase 165 builds on an explicit, durable config model instead of repo-local accident.

## Output
A Phase 164 implementation that:
- adds `agents.implementer` defaults to the canonical init/migration-generated `pals.json`
- documents `agents.implementer.enabled` and `agents.implementer.model` in the schema reference
- adds validation coverage that catches future drift in generated config and installed shared assets
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md
.paul/SPECIAL-FLOWS.md

## Prior Work (only if genuinely needed)
.paul/phases/156-pals-implementer-integration/156-01-SUMMARY.md
.paul/handoffs/archive/HANDOFF-2026-03-28-v237-milestone-created.md

## Source Files
kernel/workflows/init-project.md
kernel/references/pals-json-schema.md
kernel/workflows/apply-phase.md
pals.json
tests/pi-end-to-end-validation.sh
tests/cross-harness-validation.sh
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
WARNING: modules.yaml not loaded — no installed module registry was present in this repo checkout, so pre-plan advisory/enforcement dispatch could not run for this planning session.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | Entire PLAN → APPLY → UNIFY lifecycle | ✓ |
| /carl | required | Session-boundary handling during longer execution/review flows | ✓ |
| TODD | required | Before and during implementation changes in APPLY | ✓ |
| WALT | required | After implementation during APPLY/UNIFY verification | ✓ |

**BLOCKING:** Required lifecycle flows remain in force for APPLY. In this environment, `/carl` is provided by the installed Pi extension/runtime path and TODD/WALT are enforced as module overlays during APPLY/UNIFY rather than separate callable skills.
## Skill Invocation Checklist
- [x] /paul loaded
- [x] /carl active via installed runtime/extension path
- [x] TODD available via installed module overlay
- [x] WALT available via installed module overlay
</skills>

<acceptance_criteria>
## AC-1: Init and migration produce the implementer config block
```gherkin
Given a project initialized or migrated through the canonical init workflow
When `pals.json` is created or upgraded to the current schema
Then it includes an explicit `agents.implementer` object with the intended default fields and preserves existing user values elsewhere
```

## AC-2: Schema documentation matches runtime expectations
```gherkin
Given a contributor reads the canonical pals.json schema reference
When they inspect agent-related configuration
Then `agents.implementer.enabled` and `agents.implementer.model` are documented with types, defaults, and migration expectations consistent with the existing runtime contract
```

## AC-3: Validation catches future contract drift
```gherkin
Given the repo validation scripts are run after reinstalling updated shared assets
When they inspect generated/installed PALS surfaces
Then they fail if init/schema/install validation no longer exposes the explicit implementer config contract expected by delegated APPLY
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Align init-project generated and migrated pals.json output with the implementer contract</name>
  <files>kernel/workflows/init-project.md</files>
  <action>
    Update the canonical init workflow so both fresh creation and migration guidance include the explicit `agents.implementer` block already used by this repo's runtime contract.
    - Add `agents.implementer.enabled` and `agents.implementer.model` to the canonical JSON example.
    - Ensure the migration wording clearly says missing agent fields are added without overwriting existing user values.
    - Keep the scope limited to config-contract alignment; do not change delegated APPLY eligibility, parent authority rules, or REV behavior in this phase.
  </action>
  <verify>grep -n '"agents"\|"implementer"' kernel/workflows/init-project.md</verify>
  <done>AC-1 satisfied: the canonical init/migration workflow now emits the explicit implementer config contract.</done>
</task>

<task type="auto">
  <name>Task 2: Document the implementer config in the canonical schema reference</name>
  <files>kernel/references/pals-json-schema.md</files>
  <action>
    Add an `agents` section to the schema reference that documents the implementer config fields and ties them back to migration/default behavior.
    - Describe `agents.implementer.enabled` and `agents.implementer.model` with types, defaults, and since-version metadata.
    - Keep the wording aligned with the existing delegated APPLY contract in `kernel/workflows/apply-phase.md` and current `pals.json`.
    - Do not broaden the schema to new agent roles or redesign config semantics in this phase.
  </action>
  <verify>grep -n '## agents\|agents\.implementer' kernel/references/pals-json-schema.md</verify>
  <done>AC-2 satisfied: the canonical schema reference documents the implementer config fields and migration semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Extend validation to prove the config contract across repo and installed surfaces</name>
  <files>tests/pi-end-to-end-validation.sh, tests/cross-harness-validation.sh</files>
  <action>
    Add or update validation checks so future drift between init/schema/install surfaces is caught automatically.
    - Add assertions that the relevant source or installed shared files include explicit implementer-config markers.
    - Prefer bounded text-based checks that prove the contract without overfitting exact formatting.
    - Reinstall shared assets with `bash drivers/pi/install.sh` before installed-surface assertions that depend on updated workflow/reference files.
    - Keep validation focused on config-contract presence; delegated APPLY behavior expansion belongs to Phase 165 and broader docs refresh belongs to Phase 166.
  </action>
  <verify>bash drivers/pi/install.sh && bash tests/pi-end-to-end-validation.sh && bash tests/cross-harness-validation.sh</verify>
  <done>AC-3 satisfied: automated validation now fails if the implementer config contract disappears from the relevant generated or installed surfaces.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- kernel/workflows/apply-phase.md delegation semantics and eligibility rules
- .pi/agents/pals-implementer.md authority contract
- drivers/pi/extensions/pals-hooks.ts delegated APPLY guidance
- REV routing and `/paul:review` behavior

## SCOPE LIMITS
- Do not expand delegated APPLY coverage in this plan; Phase 165 owns that work.
- Do not perform broad README/CHANGELOG refresh work here unless required for a failing validation assertion; Phase 166 owns broader docs refresh.
- Do not redesign the overall `agents` model beyond the explicit implementer contract already in use.
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `grep -n '"agents"\|"implementer"' kernel/workflows/init-project.md`
- [ ] `grep -n '## agents\|agents\.implementer' kernel/references/pals-json-schema.md`
- [ ] `bash drivers/pi/install.sh`
- [ ] `bash tests/pi-end-to-end-validation.sh`
- [ ] `bash tests/cross-harness-validation.sh`
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All tasks completed
- Canonical init/migration output includes `agents.implementer`
- Schema reference documents the implementer config fields clearly
- Validation coverage proves the contract across the relevant repo/installed surfaces
- No delegated APPLY policy expansion was introduced in this phase
</success_criteria>

<output>
After completion, create `.paul/phases/164-implementer-config-contract-alignment/164-01-SUMMARY.md`
</output>
