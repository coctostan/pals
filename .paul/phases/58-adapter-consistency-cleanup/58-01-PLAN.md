---
phase: 58-adapter-consistency-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - drivers/pi/driver.yaml
  - drivers/pi/uninstall.sh
  - drivers/pi/extensions/README.md
  - drivers/pi/skill-map.md
  - drivers/pi/skills/paul-help/SKILL.md
  - tests/pi-end-to-end-validation.sh
autonomous: true
---

<objective>
## Goal
Remove the remaining Pi adapter drift across manifest, uninstall/install-facing operator text, and skill/help discovery surfaces so the adapter documentation and packaging metadata accurately match the current extension-enabled Pi model.

## Purpose
Phase 57 improved Pi command routing and discoverability. Phase 58 closes the milestone by reconciling the remaining operator-facing and maintenance-facing inconsistencies so the Pi adapter clearly reflects reality without changing shared workflow semantics.

## Output
Aligned Pi driver metadata, installer/uninstaller operator wording, help/discovery text, and validation coverage proving the Pi adapter’s documented shape matches the implemented command + skill + extension model.
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md
.paul/SPECIAL-FLOWS.md

## Prior Work (only if genuinely needed)
.paul/phases/57-command-routing-discoverability/57-01-SUMMARY.md
.paul/phases/56-hook-and-context-strategy/56-01-SUMMARY.md

## Source Files
drivers/pi/driver.yaml
drivers/pi/uninstall.sh
drivers/pi/extensions/README.md
drivers/pi/skill-map.md
drivers/pi/skills/paul-help/SKILL.md
tests/pi-end-to-end-validation.sh
</context>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | Before executing this lifecycle plan | ✓ |

**BLOCKING:** The PAUL lifecycle skill must remain active before APPLY proceeds. No additional specialized Pi-only skill loading is required for this cleanup plan.

## Skill Invocation Checklist
- [x] /paul loaded
</skills>

<acceptance_criteria>
## AC-1: Pi adapter metadata and operator scripts describe the same runtime model
```gherkin
Given a maintainer reviewing the Pi adapter packaging files
When they inspect the driver manifest and install/uninstall-facing script text
Then those files consistently describe Pi as using canonical skills plus an extension-backed command/hook layer rather than an outdated or partial model
```

## AC-2: Pi help and discovery surfaces are internally consistent after the command-layer improvements
```gherkin
Given a Pi user or maintainer reading the adapter’s help/discovery files
When they compare the extension README, skill map, and help skill
Then the files present one coherent explanation of `/paul-*`, `/skill:paul-*`, and the extension’s role with no obvious drift in terminology or responsibilities
```

## AC-3: Validation covers the cleaned-up adapter contract
```gherkin
Given the Pi adapter after the consistency cleanup
When the validation suite is run
Then it checks the updated manifest/script/help expectations and passes without requiring broader runtime or workflow changes
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Align Pi manifest and operator-facing script wording with the extension-enabled adapter model</name>
  <files>drivers/pi/driver.yaml, drivers/pi/uninstall.sh</files>
  <action>
    Reconcile the remaining packaging/operator-facing Pi files with the current adapter reality.
    - Update manifest wording or file lists if needed so the driver metadata clearly reflects the installed Pi surfaces.
    - Update uninstall/operator text so it accurately describes removing both canonical skills and the extension-backed command layer.
    - Keep this as wording/metadata cleanup; do not redesign installer behavior or resource locations in this phase.
  </action>
  <verify>grep -n "description:\|skills\|extension\|command\|hook\|pals-hooks.ts" drivers/pi/driver.yaml drivers/pi/uninstall.sh</verify>
  <done>AC-1 satisfied: manifest and operator scripts consistently describe the Pi adapter runtime model</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile the remaining Pi help and discovery text for consistency</name>
  <files>drivers/pi/extensions/README.md, drivers/pi/skill-map.md, drivers/pi/skills/paul-help/SKILL.md</files>
  <action>
    Tighten the remaining Pi-facing explanation layer so the adapter reads as one coherent system.
    - Normalize terminology around command layer, canonical skills, and extension responsibilities.
    - Remove any lingering wording mismatch left after Phase 57.
    - Keep the command-context guardrail intact: this phase is wording consistency work, not command or context behavior redesign.
  </action>
  <verify>grep -n "canonical\|/paul-\*\|/skill:paul-\*\|extension\|command layer\|skill layer" drivers/pi/extensions/README.md drivers/pi/skill-map.md drivers/pi/skills/paul-help/SKILL.md</verify>
  <done>AC-2 satisfied: Pi help/discovery surfaces present a consistent adapter story</done>
</task>

<task type="auto">
  <name>Task 3: Extend validation to lock in the cleaned-up Pi adapter contract</name>
  <files>tests/pi-end-to-end-validation.sh</files>
  <action>
    Update the Pi validation suite so it protects the cleanup work and catches future documentation/metadata drift.
    - Add or refine structural checks for manifest wording/file coverage and uninstall/help expectations where appropriate.
    - Keep validation repo-local and lightweight.
    - Do not add checks that encourage broader prompt/context injection or Pi-only runtime semantics.
  </action>
  <verify>bash tests/pi-end-to-end-validation.sh</verify>
  <done>AC-3 satisfied: validation asserts the updated Pi adapter contract and passes</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- Shared kernel workflow semantics under `kernel/workflows/` and `kernel/commands/paul/`
- Phase 56 event-model decision artifacts
- The command-routing implementation strategy established in Phase 57 except for small wording alignment where necessary

## SCOPE LIMITS
- Do not redesign Pi event hooks, context injection timing, or command behavior in this plan
- Do not add new Pi UI widgets, selectors, or persistence behavior
- Do not change installer packaging scope beyond consistency/metadata cleanup
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `grep -n "description:\|skills\|extension\|command\|hook\|pals-hooks.ts" drivers/pi/driver.yaml drivers/pi/uninstall.sh`
- [ ] `grep -n "canonical\|/paul-\*\|/skill:paul-\*\|extension\|command layer\|skill layer" drivers/pi/extensions/README.md drivers/pi/skill-map.md drivers/pi/skills/paul-help/SKILL.md`
- [ ] `bash tests/pi-end-to-end-validation.sh`
- [ ] All acceptance criteria are met without changing shared artifact authority or expanding Pi runtime context behavior
</verification>

<success_criteria>
- All 3 tasks completed
- Pi manifest and operator scripts match the implemented adapter model
- Help/discovery surfaces read consistently across extension, skill map, and help skill
- Validation passes with checks that protect against future adapter drift
</success_criteria>

<output>
After completion, create `.paul/phases/58-adapter-consistency-cleanup/58-01-SUMMARY.md`
</output>
