---
phase: 46-harness-agnostic-skills
plan: 02
type: execute
wave: 1
depends_on: ["46-01"]
files_modified:
  - kernel/commands/paul/add-phase.md
  - kernel/commands/paul/apply.md
  - kernel/commands/paul/assumptions.md
  - kernel/commands/paul/complete-milestone.md
  - kernel/commands/paul/consider-issues.md
  - kernel/commands/paul/deploy.md
  - kernel/commands/paul/discuss.md
  - kernel/commands/paul/discuss-milestone.md
  - kernel/commands/paul/discover.md
  - kernel/commands/paul/fix.md
  - kernel/commands/paul/flows.md
  - kernel/commands/paul/init.md
  - kernel/commands/paul/knowledge.md
  - kernel/commands/paul/milestone.md
  - kernel/commands/paul/pause.md
  - kernel/commands/paul/plan-fix.md
  - kernel/commands/paul/plan.md
  - kernel/commands/paul/refactor.md
  - kernel/commands/paul/remove-phase.md
  - kernel/commands/paul/research-phase.md
  - kernel/commands/paul/research.md
  - kernel/commands/paul/resume.md
  - kernel/commands/paul/unify.md
  - kernel/commands/paul/verify.md
  - kernel/workflows/complete-milestone.md
  - kernel/workflows/configure-special-flows.md
  - kernel/workflows/create-milestone.md
  - kernel/workflows/discuss-milestone.md
  - kernel/workflows/discuss-phase.md
  - kernel/workflows/fix-loop.md
  - kernel/workflows/init-project.md
  - kernel/workflows/pause-work.md
  - kernel/workflows/research.md
  - kernel/workflows/resume-project.md
  - kernel/workflows/roadmap-management.md
  - kernel/workflows/transition-phase.md
  - kernel/references/context-management.md
  - kernel/references/plan-format.md
  - kernel/rules/commands.md
  - kernel/rules/style.md
  - kernel/rules/workflows.md
  - kernel/templates/SPECIAL-FLOWS.md
autonomous: true
---

<objective>
## Goal
Apply portable reference conventions (from Plan 46-01) to all remaining kernel files — 40 files across commands, workflows, references, rules, and templates.

## Purpose
Completes the harness-agnostic transformation of the kernel. After this plan, zero `@~/.pals/`, `@.paul/`, or bare `~/.pals/` references remain anywhere in kernel/. Any adapter (Claude Code, Pi, Generic) can resolve kernel/ logical paths.

## Output
- 40 files converted to portable reference convention
- Zero `@` prefixed references in kernel/
- Zero `~/.pals/` install paths in kernel/ (all become `kernel/` logical paths)
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md

## Prior Work (genuine dependency)
.paul/phases/46-harness-agnostic-skills/46-01-SUMMARY.md
docs/REFERENCE-RESOLUTION.md
</context>

<acceptance_criteria>

## AC-1: Commands Use Portable References
```gherkin
Given all 24 files in kernel/commands/paul/
When searched for @~/.pals/, @.paul/, or bare ~/.pals/ patterns
Then zero matches found — all converted to kernel/ logical paths or plain .paul/ paths
```

## AC-2: Remaining Workflows Use Portable References
```gherkin
Given the 12 unconverted files in kernel/workflows/
When searched for @~/.pals/, @.paul/, or bare ~/.pals/ patterns
Then zero matches found — same convention as core loop workflows from 46-01
```

## AC-3: References, Rules, and Templates Use Portable References
```gherkin
Given files in kernel/references/, kernel/rules/, kernel/templates/
When searched for @~/.pals/, @.paul/, or bare ~/.pals/ patterns
Then zero matches found
```

## AC-4: Claude Code Zero-Regression
```gherkin
Given the Claude Code adapter resolves kernel/ to ~/.pals/ at install time
When a user runs /paul:plan or any PAUL command after this change
Then behavior is identical — symlinks and tool dispatch work unchanged
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Convert all command files to portable references</name>
  <files>
    kernel/commands/paul/add-phase.md,
    kernel/commands/paul/apply.md,
    kernel/commands/paul/assumptions.md,
    kernel/commands/paul/complete-milestone.md,
    kernel/commands/paul/consider-issues.md,
    kernel/commands/paul/deploy.md,
    kernel/commands/paul/discuss.md,
    kernel/commands/paul/discuss-milestone.md,
    kernel/commands/paul/discover.md,
    kernel/commands/paul/fix.md,
    kernel/commands/paul/flows.md,
    kernel/commands/paul/init.md,
    kernel/commands/paul/knowledge.md,
    kernel/commands/paul/milestone.md,
    kernel/commands/paul/pause.md,
    kernel/commands/paul/plan-fix.md,
    kernel/commands/paul/plan.md,
    kernel/commands/paul/refactor.md,
    kernel/commands/paul/remove-phase.md,
    kernel/commands/paul/research-phase.md,
    kernel/commands/paul/research.md,
    kernel/commands/paul/resume.md,
    kernel/commands/paul/unify.md,
    kernel/commands/paul/verify.md
  </files>
  <action>
    Apply these mechanical transformations to each file:

    1. `@~/.pals/workflows/X.md` → `kernel/workflows/X.md`
    2. `@~/.pals/templates/X.md` → `kernel/templates/X.md`
    3. `@~/.pals/references/X.md` → `kernel/references/X.md`
    4. `@~/.pals/modules/Y/references/X.md` → `kernel/modules/Y/references/X.md`
    5. `@~/.pals/modules/Y/module.yaml` → `kernel/modules/Y/module.yaml`
    6. `@.paul/X.md` → `.paul/X.md` (remove @ prefix only)
    7. `~/.pals/modules.yaml` → `kernel/modules.yaml` (in hook dispatch prose)
    8. `~/.pals/pals.json` → `kernel/pals.json` (in config references)

    Do NOT change:
    - File behavior or logic
    - Section structure
    - Anything outside reference paths
  </action>
  <verify>
    Run: grep -r '@~/.pals/\|@\.paul/\|~/.pals/' kernel/commands/paul/ | wc -l
    Expected: 0 matches
  </verify>
  <done>AC-1 satisfied: All 24 command files use portable references</done>
</task>

<task type="auto">
  <name>Task 2: Convert remaining workflows, references, rules, and templates</name>
  <files>
    kernel/workflows/complete-milestone.md,
    kernel/workflows/configure-special-flows.md,
    kernel/workflows/create-milestone.md,
    kernel/workflows/discuss-milestone.md,
    kernel/workflows/discuss-phase.md,
    kernel/workflows/fix-loop.md,
    kernel/workflows/init-project.md,
    kernel/workflows/pause-work.md,
    kernel/workflows/research.md,
    kernel/workflows/resume-project.md,
    kernel/workflows/roadmap-management.md,
    kernel/workflows/transition-phase.md,
    kernel/references/context-management.md,
    kernel/references/plan-format.md,
    kernel/rules/commands.md,
    kernel/rules/style.md,
    kernel/rules/workflows.md,
    kernel/templates/SPECIAL-FLOWS.md
  </files>
  <action>
    Apply identical mechanical transformations as Task 1:

    1. `@~/.pals/workflows/X.md` → `kernel/workflows/X.md`
    2. `@~/.pals/templates/X.md` → `kernel/templates/X.md`
    3. `@~/.pals/references/X.md` → `kernel/references/X.md`
    4. `@~/.pals/modules/Y/references/X.md` → `kernel/modules/Y/references/X.md`
    5. `@.paul/X.md` → `.paul/X.md` (remove @ prefix only)
    6. `~/.pals/modules.yaml` → `kernel/modules.yaml`
    7. Any remaining `~/.pals/` → `kernel/` for logical kernel paths

    Do NOT change:
    - File behavior or logic
    - Section structure
    - Anything outside reference paths
  </action>
  <verify>
    Run: grep -r '@~/.pals/\|@\.paul/\|~/.pals/' kernel/workflows/ kernel/references/ kernel/rules/ kernel/templates/ | wc -l
    Expected: 0 matches
  </verify>
  <done>AC-2 and AC-3 satisfied: All remaining kernel files use portable references</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- kernel/workflows/plan-phase.md (converted in 46-01)
- kernel/workflows/apply-phase.md (converted in 46-01)
- kernel/workflows/unify-phase.md (converted in 46-01)
- kernel/templates/PLAN.md (converted in 46-01)
- kernel/templates/SUMMARY.md (converted in 46-01)
- docs/REFERENCE-RESOLUTION.md (convention doc, not a conversion target)
- docs/ARTIFACT-SPEC.md, docs/LIFECYCLE-SPEC.md, docs/HOOK-CONTRACT.md (Phase 45 specs)
- modules/ directory (deferred to Plan 46-03)
- .paul/ directory (project artifacts, not kernel)

## SCOPE LIMITS
- Only reference path conversion — no logic changes
- No new files created
- No module files (modules/*) — that's Plan 46-03
- Do not refactor, restructure, or "improve" any file content

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `grep -r '@~/.pals/' kernel/` returns 0 matches
- [ ] `grep -r '@\.paul/' kernel/` returns 0 matches
- [ ] `grep -r '~/.pals/' kernel/` returns 0 matches (all converted to kernel/ logical paths)
- [ ] No files in boundaries list were modified
- [ ] All 40 files touched, no files missed
</verification>

<success_criteria>
- All 40 files converted to portable reference convention
- Zero @~/.pals/, @.paul/, or bare ~/.pals/ references in kernel/
- No behavioral changes — only path notation updated
- Claude Code zero-regression maintained
</success_criteria>

<output>
After completion, create `.paul/phases/46-harness-agnostic-skills/46-02-SUMMARY.md`
</output>
