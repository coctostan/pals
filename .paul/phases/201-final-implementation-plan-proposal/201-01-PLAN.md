---
phase: 201-final-implementation-plan-proposal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/PI-NATIVE-IMPLEMENTATION-PLAN.md
autonomous: true
---

<objective>
## Goal
Create `docs/PI-NATIVE-IMPLEMENTATION-PLAN.md`, the final v2.46 implementation-plan proposal that converts Phases 197-200 into an executable Pi-native build roadmap with recommended milestone order, acceptance-criteria families, validation/evidence requirements, and protected authority boundaries.

## Purpose
v2.46 is the planning bridge between the v2.45 Pi-native architecture and future build milestones. This plan keeps that bridge docs-only: it proposes what to build next without changing runtime behavior, workflows, validation scripts, dependencies, GitHub Flow behavior, installed runtime copies, or legacy surfaces.

## Output
- `docs/PI-NATIVE-IMPLEMENTATION-PLAN.md` created as the Phase 201 proposal artifact.
- The artifact names the recommended first build milestone, candidate follow-on milestone sequence, per-milestone acceptance/evidence requirements, deferrals, and non-negotiable authority boundaries.
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md

## Prior Work
.paul/phases/200-validation-evidence-strategy/200-01-SUMMARY.md

docs/PI-NATIVE-IMPLEMENTATION-FINDINGS.md
docs/PI-NATIVE-IMPLEMENTATION-WORKSTREAMS.md
docs/PI-NATIVE-SEQUENCING-DEPENDENCY-PLAN.md
docs/PI-NATIVE-VALIDATION-EVIDENCE-STRATEGY.md

## Source Files
docs/PI-NATIVE-IMPLEMENTATION-PLAN.md
</context>

<module_dispatch>
## Pre-Plan Module Dispatch

Planning posture: `high` collaboration from `pals.json` project default; direct-requirements mode because Phase 201 scope is explicit in `.paul/ROADMAP.md` and Phase 197-200 artifacts.

[dispatch] pre-plan advisory: modules.yaml loaded from installed registry; docs-only Phase 201 scope reviewed.

| Module / Cohort | Result |
|-----------------|--------|
| TODD | Repo has shell validation suites under `tests/` and no first-party `*.test.*` / `*.spec.*` files found by repo-scoped search; plan type remains `execute`, not TDD. |
| IRIS | Planned output is a new markdown planning artifact; no source anti-pattern scan applies before file creation. |
| DAVE | No `.github` CI config detected; this docs-only plan does not add CI/CD files. Validation remains local command output from existing shell suites. |
| DOCS | Documentation catalog contains the Phase 197-200 Pi-native planning artifacts; planned output belongs under `docs/` next to related source docs. |
| CODI | Skipped — no extractable TS/JS symbols or bounded source selectors in this markdown-only phase scope. |
| RUBY | Markdown-only planning artifact; no implementation functions, classes, or complexity-bearing source files planned. |
| ARIA / LUKE / GABE / DANA / OMAR / PETE / REED / VERA / ARCH | No UI, API, data, runtime, performance, resilience, privacy, or architecture-sensitive implementation files are planned. |

[dispatch] pre-plan enforcement: DEAN PASS — no root dependency manifest or lockfile detected; no dependency audit applies and no critical/high vulnerability block is raised.

## Post-Plan Module Dispatch

[dispatch] post-plan: TODD reviewed plan type; no TDD conversion suggested for docs-only proposal work.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | All PLAN/APPLY/UNIFY lifecycle work | ✓ |
| /carl | required | Automatic session-boundary monitoring at phase boundaries and safety thresholds | automatic |
| TODD | required | APPLY validation/test enforcement | ✓ via module dispatch |
| WALT | required | APPLY/UNIFY quality gating and validation history | ✓ via module dispatch |

**BLOCKING:** Required skills must remain available before APPLY proceeds.
</skills>

<acceptance_criteria>

## AC-1: Source-grounded roadmap proposal
```gherkin
Given Phase 197-200 planning artifacts exist
When the Phase 201 proposal is created
Then it cites those artifacts as source inputs and derives recommendations from their findings, workstreams, sequencing, and validation/evidence strategy
```

## AC-2: Executable build milestone recommendations
```gherkin
Given v2.46 must hand off to future implementation hardening
When the proposal defines the next build roadmap
Then it names the recommended first build milestone, candidate follow-on sequence, split/merge rationale, dependencies, and explicit deferrals
```

## AC-3: Authority boundaries and non-goals preserved
```gherkin
Given PALS lifecycle truth remains artifact-first and parent-owned
When implementation milestones are proposed
Then the proposal protects `.paul/*` authority, PLAN/APPLY/UNIFY, module evidence, GitHub Flow gates, parent-owned APPLY, explicit approval/checkpoints, full-read fallback, and excludes hidden Pi state, telemetry, auto-approval, UI-only decisions, helper-owned APPLY, merge-gate bypass, dependency additions, and unclassified legacy cleanup
```

## AC-4: Validation and durable evidence requirements are explicit
```gherkin
Given Phase 200 made command output the validation truth
When the proposal defines milestone readiness and acceptance criteria families
Then each proposed milestone includes validation classes, required commands, expected command-output evidence, durable artifact evidence, fallback triggers, and shared-invariant preservation rules
```

## AC-5: Validation remains green
```gherkin
Given the proposal is docs-only
When APPLY verifies the completed artifact
Then required marker checks pass and both `bash tests/pi-end-to-end-validation.sh` and `bash tests/cross-harness-validation.sh` remain green
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Create source-grounded proposal frame</name>
  <files>docs/PI-NATIVE-IMPLEMENTATION-PLAN.md</files>
  <action>
    Create the proposal artifact with:
    - status table for Milestone v2.46 / Phase 201 and a planning-only boundary;
    - purpose explaining that this is the final implementation-plan proposal, not build authorization;
    - source inputs table covering `docs/PI-NATIVE-IMPLEMENTATION-FINDINGS.md`, `docs/PI-NATIVE-IMPLEMENTATION-WORKSTREAMS.md`, `docs/PI-NATIVE-SEQUENCING-DEPENDENCY-PLAN.md`, `docs/PI-NATIVE-VALIDATION-EVIDENCE-STRATEGY.md`, and `.paul/phases/200-validation-evidence-strategy/200-01-SUMMARY.md`;
    - concise synthesis of settled decisions and non-negotiable authority boundaries.
    Avoid copying large prior sections verbatim; cite source files and preserve only the decision/evidence needed for the final roadmap.
  </action>
  <verify>grep -n "Planning-Only Boundary\|Source Inputs\|Non-Negotiable Authority Boundaries\|docs/PI-NATIVE-VALIDATION-EVIDENCE-STRATEGY.md" docs/PI-NATIVE-IMPLEMENTATION-PLAN.md</verify>
  <done>AC-1 and AC-3 satisfied: the proposal is source-grounded, planning-only, and boundary-preserving.</done>
</task>

<task type="auto">
  <name>Task 2: Define executable implementation roadmap</name>
  <files>docs/PI-NATIVE-IMPLEMENTATION-PLAN.md</files>
  <action>
    Add the executable roadmap section with:
    - recommended first build milestone: Artifact-Slice Contract Hardening, unless the artifact explicitly explains a validation-first override;
    - follow-on candidate sequence: Guided Workflow Evidence Hardening, Workflow/Resource Capsule Contract, Delegated APPLY Packet/Report Contract, Pi-Native Validation Classification, then Legacy Retention and Install-Surface Cleanup;
    - per-candidate purpose, likely repo surfaces, entry conditions, exit evidence, dependency notes, and split/merge rationale;
    - explicit deferrals and non-goals for the first build milestone.
    Keep the proposal concrete enough for future `/paul:milestone` or `/paul:plan` work to create bounded build phases without guessing.
  </action>
  <verify>grep -n "Recommended First Build Milestone\|Candidate Build Sequence\|Artifact-Slice Contract Hardening\|Guided Workflow Evidence Hardening\|Legacy Retention" docs/PI-NATIVE-IMPLEMENTATION-PLAN.md</verify>
  <done>AC-2 satisfied: the next build roadmap, sequence, dependencies, and deferrals are explicit.</done>
</task>

<task type="auto">
  <name>Task 3: Add validation, evidence, readiness, and verification gates</name>
  <files>docs/PI-NATIVE-IMPLEMENTATION-PLAN.md</files>
  <action>
    Add validation and readiness sections with:
    - acceptance-criteria families future build plans must include;
    - validation-class requirements for Pi-Supported Runtime, Shared Invariant, Frozen Legacy Parity, Runtime Lens, Guided UI Safety, Helper Delegation, GitHub Flow Safety, and Artifact / Process;
    - command-output evidence requirements for marker greps, `bash tests/pi-end-to-end-validation.sh`, `bash tests/cross-harness-validation.sh`, dependency/security audits when applicable, git/PR/CI evidence, and helper delegation evidence when used;
    - durable artifact evidence requirements for PLAN, APPLY notes/task results, SUMMARY, STATE, ROADMAP, and historical reports;
    - final readiness checklist and handoff instructions for the next build milestone.
    Preserve the rule that validation command output is truth; Pi summaries, widgets, reports, slices, capsules, and helper reports may explain but not replace command evidence.
  </action>
  <verify>grep -n "Validation Classes\|Command-Output Evidence\|Durable Artifact Evidence\|Readiness Checklist\|Next Build Handoff" docs/PI-NATIVE-IMPLEMENTATION-PLAN.md && bash tests/pi-end-to-end-validation.sh && bash tests/cross-harness-validation.sh</verify>
  <done>AC-4 and AC-5 satisfied: validation classes, evidence gates, readiness, handoff, and green validation are present.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Runtime behavior in `drivers/pi/extensions/**`, `drivers/pi/install.sh`, or installed runtime copies.
- Kernel workflows, module source, validation scripts, dependency manifests, GitHub Flow behavior, Claude Code wrappers, Agent SDK surfaces, portability docs, historical validation reports, or legacy install surfaces.
- Existing Phase 197-200 artifacts except for citing them in the new proposal.
- `.paul/*` lifecycle authority, PLAN/APPLY/UNIFY semantics, module evidence requirements, GitHub Flow gates, parent-owned APPLY boundaries, approval/checkpoint requirements, or archive authority.

## SCOPE LIMITS
- Docs-only proposal work: create `docs/PI-NATIVE-IMPLEMENTATION-PLAN.md` only.
- Do not implement artifact slices, guided workflow changes, capsules, helper packet/report compression, validation classification changes, or legacy cleanup in this phase.
- Do not add dependencies, CI configuration, tests, generated reports, or install outputs.
- Do not treat this PLAN or the proposal artifact as build authorization; future implementation still requires normal milestone creation, PLAN approval, APPLY execution, verification, GitHub Flow gates, and UNIFY reconciliation.

</boundaries>

<verification>
Before declaring APPLY complete:
- [ ] `grep -n "Planning-Only Boundary\|Source Inputs\|Recommended First Build Milestone\|Candidate Build Sequence\|Validation Classes\|Command-Output Evidence\|Durable Artifact Evidence\|Readiness Checklist\|Next Build Handoff" docs/PI-NATIVE-IMPLEMENTATION-PLAN.md`
- [ ] `bash tests/pi-end-to-end-validation.sh`
- [ ] `bash tests/cross-harness-validation.sh`
- [ ] `git diff --stat` confirms docs-only product scope plus lifecycle artifacts.
- [ ] All acceptance criteria are met.
</verification>

<success_criteria>
- `docs/PI-NATIVE-IMPLEMENTATION-PLAN.md` exists and is source-cited to Phase 197-200 artifacts.
- The proposal recommends an executable first build milestone and candidate sequence with dependencies, validation classes, evidence, and deferrals.
- Non-negotiable authority boundaries and first-build non-goals are explicit.
- Validation command output remains green and is captured during APPLY/UNIFY.
- No runtime, workflow, validation-script, dependency, GitHub Flow, installed runtime, legacy-surface, or lifecycle-authority behavior changes are introduced.
</success_criteria>

<output>
After completion, create `.paul/phases/201-final-implementation-plan-proposal/201-01-SUMMARY.md` with acceptance-criteria results, verification output, module reports, changed files, decisions, deviations, and recommended next milestone handoff.
</output>
