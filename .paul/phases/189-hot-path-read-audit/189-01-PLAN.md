---
phase: 189-hot-path-read-audit
plan: 01
type: research
wave: 1
depends_on: []
files_modified:
  - .paul/phases/189-hot-path-read-audit/189-01-HOT-PATH-READ-AUDIT.md
autonomous: true
---

<objective>
## Goal
Audit the remaining recurring hot-path read cost in normal PALS PLAN/APPLY/UNIFY usage and identify the highest-value next reductions.

## Purpose
Phase 188 reduced wrapper duplication. Phase 189 should now measure what is still repeatedly read so Phase 190 can simplify active artifacts and Phase 191 can protect the right surfaces with validation.

## Output
A durable audit report at `.paul/phases/189-hot-path-read-audit/189-01-HOT-PATH-READ-AUDIT.md` with measured hot-path surfaces, frequency assumptions, ranked waste candidates, and specific recommendations.
</objective>

<context>
## Project Context
- `.paul/PROJECT.md` — v2.44 context optimization must preserve artifact-first truth, module evidence, GitHub Flow safety, and parent-owned APPLY while reducing recurring read cost.
- `.paul/ROADMAP.md` — Phase 189 focus: measure what PALS reads repeatedly during normal PLAN/APPLY/UNIFY and identify avoidable context load.
- `.paul/STATE.md` — Phase 188 complete; Phase 189 ready for PLAN.
- Planning mode: direct continuation from v2.44 milestone; collaboration level: high.

## Prior Work
- `.paul/phases/188-workflow-prompt-skill-slimming/188-01-SUMMARY.md` — selected prompt/skill wrapper set reduced from 406 to 296 lines while validation stayed green.

## Candidate Hot-Path Surfaces Observed During Planning
| Surface | Lines | Notes |
|---------|-------|-------|
| `drivers/pi/extensions/pals-hooks.ts` | 1331 | Pi adapter and context injection surface; likely hot for activation/context logic. |
| `.paul/ROADMAP.md` | 745 | Current milestone slice is hot, completed history remains present below it. |
| `kernel/workflows/apply-phase.md` | 694 | Core hot workflow. |
| `kernel/workflows/unify-phase.md` | 589 | Core hot workflow plus transition/merge gate routing. |
| `kernel/workflows/plan-phase.md` | 349 | Core hot workflow. |
| `kernel/workflows/create-milestone.md` | 272 | Milestone setup path; warm/hot at milestone boundaries. |
| `kernel/workflows/resume-project.md` | 268 | Resume path; hot at session boundaries. |
| `.paul/PROJECT.md` | 111 | Hot project brief. |
| `.paul/STATE.md` | 90 | Hot lifecycle truth. |
| `.paul/MILESTONES.md` | 75 | Compact index. |
| `.paul/SPECIAL-FLOWS.md` | 41 | Specialized routing. |

## Source Files
- `.paul/STATE.md`
- `.paul/PROJECT.md`
- `.paul/ROADMAP.md`
- `.paul/MILESTONES.md`
- `.paul/SPECIAL-FLOWS.md`
- `kernel/workflows/plan-phase.md`
- `kernel/workflows/apply-phase.md`
- `kernel/workflows/unify-phase.md`
- `kernel/workflows/resume-project.md`
- `kernel/workflows/create-milestone.md`
- `drivers/pi/extensions/pals-hooks.ts`
- `drivers/pi/skills/paul-*.md`
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: ARCH detected repo-organized kernel/drivers/modules/tests structure; planned output is a `.paul/phases` audit artifact, so no architecture boundary risk.

[dispatch] pre-plan advisory: ARIA, DANA, GABE, LUKE, OMAR, PETE, REED, VERA skipped — planned changes are markdown audit documentation with no UI, data, API, observability runtime, performance-sensitive runtime, resilience, or privacy surface.

[dispatch] pre-plan advisory: SETH passed — planned audit artifact does not introduce secrets or auth surface.

[dispatch] pre-plan advisory: TODD found no root test framework; existing shell validation suites remain the relevant verification path.

[dispatch] pre-plan advisory: IRIS found no anti-pattern markers in planned target artifact path.

[dispatch] pre-plan advisory: DAVE found no CI config; CI generation remains out of scope.

[dispatch] pre-plan advisory: DOCS notes this plan creates a phase audit document; no separate docs update is required unless APPLY finds a user-facing docs drift.

[dispatch] CODI: skipped — no extractable symbols in phase scope.

[dispatch] pre-plan advisory: RUBY flags large hot/warm surfaces (`pals-hooks.ts`, `ROADMAP.md`, `apply-phase.md`, `unify-phase.md`) as the likely audit focus.

[dispatch] pre-plan enforcement: DEAN PASS/SKIP — no dependency manifest or lock file is modified by this plan.

[dispatch] post-plan: TODD found no TDD conversion candidate for a documentation-only audit plan.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | PLAN/APPLY/UNIFY lifecycle work | ✓ loaded via `/skill:paul-plan` |
| /carl | required | Automatic session boundary monitoring | automatic |
| TODD | required | During APPLY verification gates | module dispatch |
| WALT | required | During APPLY/UNIFY quality gates | module dispatch |

**BLOCKING:** APPLY must preserve parent-owned verification and module enforcement.
</skills>

<acceptance_criteria>
## AC-1: Hot-Path Inventory
```gherkin
Given normal PALS work repeatedly uses lifecycle artifacts, workflows, wrappers, and Pi context injection
When the audit is complete
Then the report lists the main hot-path surfaces with line/byte counts, likely trigger frequency, and whether each is hot, warm, or cold
```

## AC-2: Avoidable Read-Cost Diagnosis
```gherkin
Given v2.44 aims to reduce recurring context cost without deleting truth
When the report ranks waste candidates
Then each candidate explains the repeated-read cause, the preserved authority/safety requirement, and the recommended reduction pattern
```

## AC-3: Next-Phase Recommendations
```gherkin
Given Phase 190 will simplify artifact structure and Phase 191 will update guardrails
When the audit is complete
Then it provides actionable recommendations mapped to Phase 190, Phase 191, or future milestone work
```

## AC-4: Existing Validation Remains Green
```gherkin
Given this is a documentation/audit plan
When APPLY finishes
Then Pi and cross-harness validation still pass and no source behavior changes are introduced
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Build measured hot-path inventory</name>
  <files>.paul/phases/189-hot-path-read-audit/189-01-HOT-PATH-READ-AUDIT.md</files>
  <action>
    Create the audit report with an inventory section:
    - Measure line and byte counts for hot lifecycle artifacts, core workflows, Pi skill wrappers, relevant command wrappers, and Pi context injection source.
    - Classify each surface as hot, warm, or cold based on normal PLAN/APPLY/UNIFY/resume/milestone usage.
    - Record trigger assumptions explicitly rather than implying telemetry exists.
    - Preserve artifact-first truth: do not suggest deleting archive authority or replacing `.paul/*` with hidden state.
  </action>
  <verify>`test -f .paul/phases/189-hot-path-read-audit/189-01-HOT-PATH-READ-AUDIT.md && grep -q 'Hot-Path Inventory' .paul/phases/189-hot-path-read-audit/189-01-HOT-PATH-READ-AUDIT.md`</verify>
  <done>AC-1 satisfied with measured inventory and classification.</done>
</task>

<task type="auto">
  <name>Task 2: Rank avoidable read-cost candidates</name>
  <files>.paul/phases/189-hot-path-read-audit/189-01-HOT-PATH-READ-AUDIT.md</files>
  <action>
    Add a ranked findings section:
    - Identify the top 5-8 avoidable recurring-read costs.
    - For each finding, include path(s), approximate current cost, why it is repeatedly read, what must be preserved, and a safe reduction pattern.
    - Separate quick wins from larger architectural decisions.
    - Include the strategic concern that Claude Code may become legacy/reference while Pi remains the primary active surface.
  </action>
  <verify>`grep -q 'Ranked Findings' .paul/phases/189-hot-path-read-audit/189-01-HOT-PATH-READ-AUDIT.md && grep -q 'Preserve' .paul/phases/189-hot-path-read-audit/189-01-HOT-PATH-READ-AUDIT.md`</verify>
  <done>AC-2 satisfied with ranked, preservation-aware findings.</done>
</task>

<task type="auto">
  <name>Task 3: Map recommendations and validate no regression</name>
  <files>.paul/phases/189-hot-path-read-audit/189-01-HOT-PATH-READ-AUDIT.md</files>
  <action>
    Finalize the report with recommendations:
    - Map each recommendation to Phase 190, Phase 191, or future milestone/product decision.
    - Add a "Do Not Do" section for unsafe reductions such as removing lifecycle truth, weakening module evidence, hiding state, or skipping GitHub Flow safety.
    - Run existing validation suites to prove the audit did not disturb current behavior.
  </action>
  <verify>`bash tests/pi-end-to-end-validation.sh` and `bash tests/cross-harness-validation.sh` both pass; report contains 'Recommendations' and 'Do Not Do'.</verify>
  <done>AC-3 and AC-4 satisfied with final report and validation evidence.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- Runtime behavior, workflow semantics, module dispatch contracts, GitHub Flow gates, or Pi context injection logic.
- `.paul/archive/*` historical authority.
- Validation scripts, unless an audit-only false assumption requires documenting a test gap; default is no validation-script edits.
- Installed runtime copies outside the repo.

## SCOPE LIMITS
- This plan produces an audit and recommendations, not the Phase 190 simplification implementation.
- Do not compact `.paul/ROADMAP.md`, `.paul/STATE.md`, or workflow files in this plan.
- Do not add telemetry or hidden runtime state to measure reads.
- Do not expand Claude-specific support surface.
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `.paul/phases/189-hot-path-read-audit/189-01-HOT-PATH-READ-AUDIT.md` exists.
- [ ] Report includes measured inventory, ranked findings, recommendations, and unsafe reductions.
- [ ] Recommendations are mapped to Phase 190, Phase 191, or future product decision.
- [ ] `bash tests/pi-end-to-end-validation.sh` passes.
- [ ] `bash tests/cross-harness-validation.sh` passes.
- [ ] All acceptance criteria met.
</verification>

<success_criteria>
- Remaining recurring hot-path read costs are measured and classified.
- The report identifies concrete, preservation-aware reduction opportunities.
- Phase 190 has actionable artifact-structure simplification inputs.
- Current validation remains green.
</success_criteria>

<output>
After completion, create `.paul/phases/189-hot-path-read-audit/189-01-SUMMARY.md` documenting audit findings, recommendations, validation results, module execution reports, and any deferred product decisions.
</output>
