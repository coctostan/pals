---
phase: 193-pi-runtime-capability-mapping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/PI-RUNTIME-CAPABILITY-MAP.md
autonomous: true
---

<objective>
## Goal
Create a Pi runtime capability map that translates the Phase 192 support-tier inventory into concrete Pi primitives, current repo surfaces, lifecycle touchpoints, safe use cases, and follow-up implications for the Phase 194 context-efficiency architecture.

## Purpose
Phase 192 established Pi as the supported runtime and froze Claude Code / Agent SDK as historical surfaces. Phase 193 needs a bounded map of what Pi already exposes through skills, extensions, hooks, command registration, lifecycle UI, guided workflow UX, session boundaries, helper agents, and installed module registry/resource resolution before later phases design context-efficiency changes or run spikes.

## Output
- `docs/PI-RUNTIME-CAPABILITY-MAP.md` created as the current capability mapping artifact.
- The map ties Pi runtime primitives to PLAN/APPLY/UNIFY, module dispatch, GitHub Flow, context injection, handoffs/session continuity, and delegated APPLY helper-agent boundaries.
- The map classifies each capability as usable now, design candidate, guardrail-only, validation redesign input, or out of scope for v2.45.
</objective>

<context>
## Project Context
- `.paul/PROJECT.md` — v2.45 is active; Phase 193 is next; `.paul/*` remains lifecycle truth.
- `.paul/ROADMAP.md` — Phase 193 scope: map PLAN/APPLY/UNIFY, modules, GitHub Flow, context injection, and handoffs onto Pi extension primitives.
- `.paul/STATE.md` — current loop is ready for PLAN for Phase 193.
- `.paul/phases/192-support-tier-reset-surface-inventory/192-01-SUMMARY.md` — prior phase output and deferred items for Phase 193.
- `docs/PI-NATIVE-SUPPORT-INVENTORY.md` — support-tier and surface classification source for this phase.

## Source / Documentation Files to Inspect During APPLY
- `drivers/pi/driver.yaml` — declared Pi driver capabilities: workflow invocation, hooks, state read/write, user interaction, command registration.
- `drivers/pi/extensions/README.md` — documented Pi command model, lifecycle hooks, guided workflow UX, live module visibility, CARL session boundaries, and install surfaces.
- `drivers/pi/skill-map.md` — command/skill/workflow map, path strategy, shortcut entry points, and planning collaboration notes.
- `drivers/pi/extensions/pals-hooks.ts` — implementation source for current Pi extension primitives and safe context-injection behavior.
- `docs/PI-NATIVE-SUPPORT-INVENTORY.md` — active/frozen/future-decision surface inventory from Phase 192.

## Source Selectors / CODI Seeds
The implementation source is read-only context for this documentation plan. Stable symbols observed in `drivers/pi/extensions/pals-hooks.ts`: `parsePalsState`, `syncLifecycleUi`, `buildPalsContextPayload`, `detectGuidedWorkflowMoment`, `carlEvaluatePhaseCompletion`.
</context>

<module_dispatch>
## Pre-Plan Module Dispatch

[dispatch] pre-plan advisory: modules.yaml loaded from installed PALS registry (kernel_version 2.0.0); required advisory cohort evaluated for planned capability-map documentation.

- ARCH: Planned file is a documentation artifact under `docs/`, which is the expected layer for a capability map. Repo shape is modular/driver-based rather than app MVC; no circular dependency risk for this docs-only plan.
- TODD: No repo-local `*.test.*`, `*.spec.*`, or package test framework covers the planned documentation artifact; no TDD restructuring candidate.
- IRIS: Planned file is new; no TODO/FIXME/HACK/XXX markers in the source documentation inspected for this plan.
- DAVE: No CI config detected; CI generation remains out of scope for Phase 193.
- DOCS: Planned change is documentation-first and creates a docs artifact directly related to Pi runtime source/docs; no source/doc drift risk.
- CODI: pre-plan advisory → impact × 5 symbols → 5 resolved, 0 unresolved, 11 total call-sites, injected blast_radius. Advisory only: this plan does not modify TypeScript source.

### Blast Radius (CODI)

**parsePalsState**
```text
drivers/pi/extensions/pals-hooks.ts:1067 carlCreateFreshSession depth:1
drivers/pi/extensions/pals-hooks.ts:1105 carlEvaluatePhaseCompletion depth:1
drivers/pi/extensions/pals-hooks.ts:1138 carlMonitorSafetyCeiling depth:1
drivers/pi/extensions/pals-hooks.ts:1225 maybePresentGuidedWorkflow depth:1
drivers/pi/extensions/pals-hooks.ts:435 syncLifecycleUi depth:1
drivers/pi/extensions/pals-hooks.ts:1055 palsHooks depth:1
```

**syncLifecycleUi**
```text
drivers/pi/extensions/pals-hooks.ts:1055 palsHooks depth:1
```

**buildPalsContextPayload**
```text
drivers/pi/extensions/pals-hooks.ts:1055 palsHooks depth:1
```

**detectGuidedWorkflowMoment**
```text
drivers/pi/extensions/pals-hooks.ts:1225 maybePresentGuidedWorkflow depth:1
drivers/pi/extensions/pals-hooks.ts:1055 palsHooks depth:2
```

**carlEvaluatePhaseCompletion**
```text
drivers/pi/extensions/pals-hooks.ts:1055 palsHooks depth:1
```

- RUBY: No preliminary debt indicators in the planned documentation file; source selectors are read-only context.
- ARIA/LUKE: No UI component files in `files_modified`; skip.
- DANA/GABE/OMAR/PETE/REED/VERA: No data/API/runtime source files in `files_modified`; skip.

[dispatch] pre-plan enforcement: DEAN PASS/WARN — no dependency manifest is in `files_modified`; no dependency-audit block applies to this documentation-only plan.

## Post-Plan Module Dispatch

[dispatch] post-plan: TODD evaluated standard `execute` plan; no tests cover planned documentation surfaces and no TDD restructuring is suggested.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | All planning, applying, and unifying work | ✓ |
| /carl | required | Automatic session boundary monitoring | automatic |
| TODD | required | Before/during code implementation; advisory only for this docs plan | ✓ via module dispatch |
| WALT | required | After implementation validation | pending APPLY |

**BLOCKING:** Required skills must be loaded or active before APPLY proceeds. For this documentation/capability-map plan, `/paul` is active and module dispatch evidence is recorded above.
</skills>

<acceptance_criteria>
## AC-1: Pi Runtime Primitive Inventory Exists
```gherkin
Given Phase 193 needs to map Pi runtime capability before architecture design
When APPLY completes
Then `docs/PI-RUNTIME-CAPABILITY-MAP.md` lists the major Pi runtime primitives and the repo surfaces that implement or document them
```

## AC-2: Lifecycle Mapping Is Explicit
```gherkin
Given PALS must preserve PLAN/APPLY/UNIFY, module evidence, GitHub Flow safety, and parent-owned APPLY
When the capability map is written
Then it maps Pi primitives to PLAN, APPLY, UNIFY, module dispatch, GitHub Flow, handoffs/session continuity, and delegated APPLY boundaries without making Pi state authoritative
```

## AC-3: Capability Classification Feeds Phase 194
```gherkin
Given Phase 194 will design context-efficiency architecture
When Phase 193 completes
Then each mapped capability is classified as usable now, design candidate, guardrail-only, validation redesign input, or out of scope, with Phase 194 implications
```

## AC-4: No Runtime Behavior Changes
```gherkin
Given this phase is mapping and documentation only
When APPLY completes
Then no Pi extension source, skill behavior, module dispatch semantics, GitHub Flow gates, validation scripts, or `.paul/*` lifecycle authority semantics are changed
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Create Pi runtime capability map artifact</name>
  <files>docs/PI-RUNTIME-CAPABILITY-MAP.md</files>
  <action>
    Create a new markdown document with these sections:
    - `# Pi Runtime Capability Map`
    - `## Source Inputs` listing the support inventory, Pi driver manifest, Pi extension README, skill map, and extension source as mapping inputs.
    - `## Capability Classification Legend` with at least: `usable-now`, `design-candidate`, `guardrail-only`, `validation-redesign`, `out-of-scope`.
    - `## Runtime Capability Inventory` as a table with columns: `Capability`, `Repo surface(s)`, `Current behavior`, `Classification`, `Lifecycle implication`.
    Include rows for command registration, canonical skill routing, installed kernel/resource resolution, installed `modules.yaml`, lifecycle status/widget, bounded context injection, guided workflow UX, live module visibility, CARL session boundaries, helper-agent delegation, user interaction, state read/write, shell/tool execution via parent workflows, and validation/CI surfacing.
    Do not modify runtime source code in this task.
  </action>
  <verify>test -f docs/PI-RUNTIME-CAPABILITY-MAP.md && grep -n "Runtime Capability Inventory\|Capability Classification Legend\|Source Inputs" docs/PI-RUNTIME-CAPABILITY-MAP.md</verify>
  <done>AC-1 satisfied: capability-map artifact exists with source inputs, classification legend, and runtime inventory.</done>
</task>

<task type="auto">
  <name>Task 2: Map capabilities to PALS lifecycle surfaces</name>
  <files>docs/PI-RUNTIME-CAPABILITY-MAP.md</files>
  <action>
    Extend the document with:
    - `## Lifecycle Mapping` covering PLAN, APPLY, UNIFY, module dispatch, GitHub Flow, session continuity/handoffs, and delegated APPLY.
    - For each lifecycle surface, state which Pi primitives can help now and which must remain artifact/workflow-owned.
    - Preserve explicit guardrails: `.paul/*` artifacts remain authoritative, module evidence stays visible and persisted, GitHub Flow gates cannot be bypassed by UI automation, and parent APPLY owns verification/fallback/checkpoints.
    - Include at least one table with columns: `Lifecycle surface`, `Useful Pi primitive(s)`, `Allowed assistance`, `Must remain authoritative outside Pi`.
    Avoid adding implementation proposals that belong in Phase 194; this task maps capabilities and guardrails only.
  </action>
  <verify>grep -n "Lifecycle Mapping\|PLAN\|APPLY\|UNIFY\|GitHub Flow\|parent APPLY" docs/PI-RUNTIME-CAPABILITY-MAP.md</verify>
  <done>AC-2 and AC-4 satisfied: lifecycle mapping is explicit and preserves authority boundaries without runtime changes.</done>
</task>

<task type="auto">
  <name>Task 3: Record Phase 194 implications and validation redesign notes</name>
  <files>docs/PI-RUNTIME-CAPABILITY-MAP.md</files>
  <action>
    Add final sections:
    - `## Phase 194 Design Inputs` listing concrete design questions and candidate context-efficiency opportunities derived from the capability map.
    - `## Validation and Redesign Notes` describing what existing Pi validation can keep protecting, which cross-harness checks are frozen-legacy or validation-redesign inputs, and what future tests may need if Phase 194 introduces Pi-assisted context loading or UI/tooling behavior.
    - `## Non-Goals for Phase 193` making clear that this phase does not implement new commands, hooks, tools, UI dialogs, compaction, validation rewrites, or legacy surface removal.
    - `## Recommended Next Questions` for Phase 194.
    Keep the document bounded to mapping and classification; no source edits.
  </action>
  <verify>grep -n "Phase 194 Design Inputs\|Validation and Redesign Notes\|Non-Goals for Phase 193\|Recommended Next Questions" docs/PI-RUNTIME-CAPABILITY-MAP.md</verify>
  <done>AC-3 and AC-4 satisfied: Phase 194 receives actionable inputs while Phase 193 remains mapping-only.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- Do not modify `drivers/pi/extensions/pals-hooks.ts` or any runtime behavior in Phase 193.
- Do not modify `drivers/pi/skills/**`, installed skill wrappers, `kernel/workflows/**`, or `kernel/references/**` in this plan.
- Do not change module dispatch semantics, GitHub Flow gates, APPLY delegation rules, or `.paul/*` lifecycle authority semantics.
- Do not delete or archive Claude Code, Agent SDK, cross-harness validation, or portability files in this phase.
- Do not update validation scripts in this phase; record validation implications only.

## SCOPE LIMITS
- This plan creates a documentation map only.
- Do not implement Pi-native command routing, compaction hooks, custom tools, UI dialogs, or source-code spikes; those belong to later v2.45 phases.
- Do not make final retention/removal decisions for frozen legacy surfaces.
- Do not introduce dependencies or package-manager changes.
</boundaries>

<verification>
Before declaring APPLY complete:
- [ ] `test -f docs/PI-RUNTIME-CAPABILITY-MAP.md`
- [ ] `grep -n "Runtime Capability Inventory\|Capability Classification Legend\|Source Inputs" docs/PI-RUNTIME-CAPABILITY-MAP.md`
- [ ] `grep -n "Lifecycle Mapping\|PLAN\|APPLY\|UNIFY\|GitHub Flow\|parent APPLY" docs/PI-RUNTIME-CAPABILITY-MAP.md`
- [ ] `grep -n "Phase 194 Design Inputs\|Validation and Redesign Notes\|Non-Goals for Phase 193\|Recommended Next Questions" docs/PI-RUNTIME-CAPABILITY-MAP.md`
- [ ] `grep -n "artifact\|\.paul/\|authoritative" docs/PI-RUNTIME-CAPABILITY-MAP.md`
- [ ] `git diff -- docs/PI-RUNTIME-CAPABILITY-MAP.md` reviewed for mapping-only scope.
- [ ] `bash tests/pi-end-to-end-validation.sh`
- [ ] `bash tests/cross-harness-validation.sh`
</verification>

<success_criteria>
- Pi runtime primitives are inventoried and classified.
- PLAN/APPLY/UNIFY, module dispatch, GitHub Flow, session continuity, and delegated APPLY are mapped to allowed Pi assistance vs authoritative artifact/workflow ownership.
- Phase 194 receives concrete design inputs for context-efficiency architecture.
- No runtime source code, validation scripts, legacy surfaces, or lifecycle semantics are changed.
- Validation remains green.
</success_criteria>

<output>
After completion, create `.paul/phases/193-pi-runtime-capability-mapping/193-01-SUMMARY.md` with:
- Files changed.
- Capability-map highlights.
- Lifecycle mapping decisions/guardrails.
- Phase 194 design inputs and validation redesign notes.
- Verification commands and results.
</output>
