---
phase: 194-context-efficiency-architecture
plan: 01
type: research
wave: 1
depends_on: []
files_modified:
  - docs/PI-NATIVE-CONTEXT-EFFICIENCY-ARCHITECTURE.md
autonomous: true
---

<objective>
## Goal
Produce a Pi-native context-efficiency architecture that explains how runtime assistance can reduce repeated prompt, workflow, artifact, and context-loading cost while preserving artifact-first lifecycle truth.

## Purpose
Phase 192 made Pi the supported runtime and Phase 193 mapped current Pi capabilities. Phase 194 now needs an architecture artifact that converts those inputs into a concrete read model, assistance boundaries, validation implications, and Phase 195 spike candidates before any runtime implementation begins.

## Output
- `docs/PI-NATIVE-CONTEXT-EFFICIENCY-ARCHITECTURE.md` created as the Phase 194 architecture design artifact.
- The artifact defines context-cost surfaces, a Pi-native assistance model, explicit authority boundaries, validation redesign requirements, and bounded Phase 195 spike candidates.
- No runtime code, shared workflow semantics, validation scripts, GitHub Flow mechanics, or legacy-support retention/removal decisions are changed in this plan.
</objective>

<context>
## Project Context
- `.paul/PROJECT.md` — v2.45 active requirements, core value, Pi-supported runtime posture, and artifact-first guardrails.
- `.paul/ROADMAP.md` — Phase 194 scope: design runtime assistance that reduces repeated prompt/workflow/artifact/context cost.
- `.paul/STATE.md` — current loop is ready for Phase 194 PLAN; Phase 193 completed with green validation.
- `.paul/SPECIAL-FLOWS.md` — required lifecycle skills and module expectations.

## Prior Work
- `.paul/phases/193-pi-runtime-capability-mapping/193-01-SUMMARY.md` — immediate prior phase summary; confirms Phase 194 inputs and guardrails.
- `docs/PI-RUNTIME-CAPABILITY-MAP.md` — primary capability map and Phase 194 design-input source.
- `docs/PI-NATIVE-SUPPORT-INVENTORY.md` — support-tier source: Pi active, Claude Code / Agent SDK frozen legacy, `.paul/*` authoritative.

## Reference / Design Inputs to Inspect During APPLY
- `kernel/references/context-management.md` — current context brackets, progressive-detail guidance, active ROADMAP window, and plan sizing heuristics.
- `docs/REFERENCE-RESOLUTION.md` — project-artifact vs kernel/module resource resolution convention.
- `drivers/pi/skill-map.md` — Pi command/skill/workflow/resource map, module registry resolution, and helper-agent boundaries.
- `drivers/pi/extensions/README.md` — Pi command model, guided workflow UX, lifecycle hooks, live module visibility, CARL, and installation boundaries.
- `docs/PI-RUNTIME-CAPABILITY-MAP.md` — current source of runtime capability classifications; use this instead of rereading TypeScript implementation unless a specific ambiguity appears.

## Planning Posture
- Collaboration level: `high` from `pals.json` project default.
- Planning mode: direct-requirements. The phase objective is already defined; APPLY should stay close to the Phase 194 design artifact deliverable while surfacing assumptions, alternatives, and risks inside the document.

## Source Selectors / CODI Seeds
No TypeScript/JavaScript source selectors are required for this plan. Phase 194 modifies one markdown architecture artifact and should rely on the Phase 193 capability map for code-derived runtime facts unless APPLY finds a concrete ambiguity.
</context>

<module_dispatch>
## Pre-Plan Module Dispatch

[dispatch] pre-plan advisory: modules.yaml loaded from installed PALS registry (kernel_version 2.0.0); required advisory cohort evaluated for planned Phase 194 architecture documentation.

- ARCH: Planned file is a documentation architecture artifact under `docs/`, which is the expected layer for the deliverable. Repo shape is modular/driver-based; no circular dependency risk for this docs-only research plan.
- TODD: No repo-local `*.test.*` or `*.spec.*` files were detected for the planned documentation artifact; no TDD restructuring candidate.
- IRIS: Planned file is new; no TODO/FIXME/HACK/XXX markers exist in the new target.
- DAVE: No `.github/workflows/` CI config is present in this repo; validation remains local via the existing Pi and cross-harness validation scripts.
- DOCS: Planned change is documentation-first and directly related to existing Pi support/runtime documentation; no source/doc drift is expected because no source files are modified.
- CODI: skipped — no extractable symbols in phase scope. No TS/JS source selectors are needed for this docs-only architecture plan.
- RUBY: No preliminary debt indicators in the planned documentation file; source/runtime debt analysis is out of scope.
- ARIA/LUKE: No UI component files in `files_modified`; skip.
- DANA/GABE/OMAR/PETE/REED/VERA: No data/API/runtime source files in `files_modified`; skip.

[dispatch] pre-plan enforcement: DEAN PASS/WARN — no dependency manifest is in `files_modified`; no dependency-audit block applies to this documentation-only research plan.

## Post-Plan Module Dispatch

[dispatch] post-plan: TODD evaluated standard `research` plan; no tests cover planned documentation surfaces and no TDD restructuring is suggested.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | All planning, applying, and unifying work | ✓ |
| /carl | required | Automatic session boundary monitoring | automatic |
| TODD | required | Before/during code implementation; advisory only for this docs plan | ✓ via module dispatch |
| WALT | required | After implementation validation | pending APPLY |

**BLOCKING:** Required skills must be loaded or active before APPLY proceeds. For this Phase 194 documentation/research plan, `/paul` is active and module dispatch evidence is recorded above.
</skills>

<acceptance_criteria>
## AC-1: Context-Cost Model Exists
```gherkin
Given Phase 194 must reduce repeated prompt, workflow, artifact, and context costs
When APPLY completes
Then `docs/PI-NATIVE-CONTEXT-EFFICIENCY-ARCHITECTURE.md` defines the major context-cost surfaces, current hot/warm/cold read model, and the problem boundaries for Pi-native assistance
```

## AC-2: Pi-Native Assistance Architecture Is Explicit
```gherkin
Given Pi runtime assistance must remain derived, bounded, and artifact-backed
When the architecture artifact is written
Then it maps candidate Pi assistance components to authoritative sources, allowed help, forbidden ownership, evidence destinations, and lifecycle surfaces including PLAN, APPLY, UNIFY, module dispatch, GitHub Flow, session continuity, context injection, and delegated APPLY
```

## AC-3: Phase 195 Spikes and Validation Redesign Are Actionable
```gherkin
Given Phase 195 may run bounded Pi-native spikes
When Phase 194 completes
Then the artifact recommends a prioritized spike backlog and validation redesign checks that can be implemented later without weakening artifact authority, module evidence, GitHub Flow gates, or parent-owned APPLY boundaries
```

## AC-4: Design-Only Boundary Is Preserved
```gherkin
Given this phase is architecture design rather than runtime implementation
When APPLY completes
Then no Pi extension source, skills, shared workflows, validation scripts, dependency manifests, GitHub Flow mechanics, legacy-support surfaces, or `.paul/*` lifecycle authority semantics are changed
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Establish the context-cost model and authority guardrails</name>
  <files>docs/PI-NATIVE-CONTEXT-EFFICIENCY-ARCHITECTURE.md</files>
  <action>
    Create a new markdown document with these sections:
    - `# Pi-Native Context-Efficiency Architecture`
    - `## Source Inputs` citing Phase 193 capability map, Phase 192 support inventory, context-management reference, reference-resolution convention, Pi skill map, and Pi extension README.
    - `## Design Problem` describing repeated prompt, workflow, artifact, module-dispatch, GitHub Flow, handoff/session, and helper-agent review context costs.
    - `## Context-Cost Surface Model` with a table covering hot, warm, cold, and runtime-derived context surfaces; include cost driver, current source, safe reduction path, and authority boundary.
    - `## Non-Negotiable Guardrails` preserving `.paul/*` artifact authority, PLAN/APPLY/UNIFY ownership, visible module evidence, GitHub Flow gates, parent-owned APPLY, explicit user approval, and no hidden Pi state.
    Keep this task descriptive and evidence-backed. Do not propose source-level implementation details yet.
  </action>
  <verify>test -f docs/PI-NATIVE-CONTEXT-EFFICIENCY-ARCHITECTURE.md && grep -n "Source Inputs\|Design Problem\|Context-Cost Surface Model\|Non-Negotiable Guardrails" docs/PI-NATIVE-CONTEXT-EFFICIENCY-ARCHITECTURE.md</verify>
  <done>AC-1 and AC-4 satisfied: the architecture artifact exists with context-cost surfaces and authority guardrails, with no runtime changes.</done>
</task>

<task type="auto">
  <name>Task 2: Design the Pi-native assistance architecture</name>
  <files>docs/PI-NATIVE-CONTEXT-EFFICIENCY-ARCHITECTURE.md</files>
  <action>
    Extend the document with:
    - `## Architecture Overview` describing a layered model: authoritative artifacts, installed workflow/module resources, transient Pi-derived context lenses, guided UI replies, helper-agent task packets, and validation/CI surfacing.
    - `## Assistance Components` as a table with columns: `Component`, `Authoritative source`, `Allowed runtime assistance`, `Forbidden ownership`, `Evidence destination`, and `Lifecycle surfaces`.
    - Include components for artifact-indexed context loading, workflow/resource capsules, module-dispatch evidence lens, guided workflow reply UI, GitHub Flow status lens, CARL/session continuity, helper-agent task packet/report compression, and validation redesign hooks.
    - `## Read Model and Citation Contract` defining which artifact slices can be summarized or tool-addressed, when full markdown reads remain required, and how Pi-assisted outputs must cite source artifact paths.
    - `## Risks and Rejected Designs` explicitly rejecting opaque Pi memory, telemetry-owned state, auto-approval, hidden module ledgers, merge-gate bypass, and helper-agent-owned APPLY.
    Avoid implementation specifics that belong to Phase 195 spikes. Keep the architecture implementable but not source-changing.
  </action>
  <verify>grep -n "Architecture Overview\|Assistance Components\|Read Model and Citation Contract\|Risks and Rejected Designs" docs/PI-NATIVE-CONTEXT-EFFICIENCY-ARCHITECTURE.md && grep -n "PLAN\|APPLY\|UNIFY\|module\|GitHub Flow\|parent APPLY\|\.paul/" docs/PI-NATIVE-CONTEXT-EFFICIENCY-ARCHITECTURE.md</verify>
  <done>AC-2 and AC-4 satisfied: Pi-native assistance is mapped to authoritative sources and forbidden ownership boundaries without runtime changes.</done>
</task>

<task type="auto">
  <name>Task 3: Define Phase 195 spike candidates and validation redesign</name>
  <files>docs/PI-NATIVE-CONTEXT-EFFICIENCY-ARCHITECTURE.md</files>
  <action>
    Add final sections:
    - `## Phase 195 Spike Backlog` with 3-5 bounded candidate spikes, each listing purpose, likely repo files for a future plan, validation proof, and guardrails.
    - `## Recommended Sequence` explaining which spike should run first and why, including what can be deferred to Phase 196 final proposal.
    - `## Validation Redesign Requirements` distinguishing Pi-supported runtime checks from shared invariant checks and frozen legacy parity checks.
    - `## Open Decisions for Phase 196` listing retention/removal and implementation-roadmap decisions that should not be settled in Phase 194.
    - `## Non-Goals for Phase 194` confirming no commands, tools, hooks, dialogs, compaction behavior, validation rewrites, dependency changes, or legacy deletion occur in this phase.
    Make the spike backlog actionable enough that Phase 195 `/paul:plan` can choose one small prototype without re-litigating the whole architecture.
  </action>
  <verify>grep -n "Phase 195 Spike Backlog\|Recommended Sequence\|Validation Redesign Requirements\|Open Decisions for Phase 196\|Non-Goals for Phase 194" docs/PI-NATIVE-CONTEXT-EFFICIENCY-ARCHITECTURE.md</verify>
  <done>AC-3 and AC-4 satisfied: follow-on spikes and validation requirements are actionable, while Phase 194 remains design-only.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- Do not modify `drivers/pi/extensions/pals-hooks.ts`, `drivers/pi/skills/**`, installed Pi runtime files, or helper-agent definitions in Phase 194.
- Do not modify `kernel/workflows/**`, `kernel/references/**`, `kernel/templates/**`, module manifests, or module dispatch semantics in this plan.
- Do not modify `tests/pi-end-to-end-validation.sh`, `tests/cross-harness-validation.sh`, CI configuration, or GitHub Flow mechanics in this phase.
- Do not delete, archive, or redesign Claude Code, Agent SDK, portability, or cross-harness files in this phase.
- Do not change dependency manifests or introduce new packages.

## SCOPE LIMITS
- This plan creates one architecture/design document only.
- Do not implement lifecycle-context tools, extension hooks, command routing, custom Pi UI, compaction behavior, module-ledger behavior, or helper-agent protocol changes; those are Phase 195+ candidates.
- Do not move lifecycle truth from `.paul/*` artifacts, shared workflows, installed `modules.yaml`, git/PR evidence, or SUMMARY/PLAN reports into Pi extension memory or hidden context.
- Do not make final legacy-retention/removal decisions; list options and defer decisions to Phase 196.
</boundaries>

<verification>
Before declaring APPLY complete:
- [ ] `test -f docs/PI-NATIVE-CONTEXT-EFFICIENCY-ARCHITECTURE.md`
- [ ] `grep -n "Source Inputs\|Design Problem\|Context-Cost Surface Model\|Non-Negotiable Guardrails" docs/PI-NATIVE-CONTEXT-EFFICIENCY-ARCHITECTURE.md`
- [ ] `grep -n "Architecture Overview\|Assistance Components\|Read Model and Citation Contract\|Risks and Rejected Designs" docs/PI-NATIVE-CONTEXT-EFFICIENCY-ARCHITECTURE.md`
- [ ] `grep -n "PLAN\|APPLY\|UNIFY\|module\|GitHub Flow\|parent APPLY\|\.paul/" docs/PI-NATIVE-CONTEXT-EFFICIENCY-ARCHITECTURE.md`
- [ ] `grep -n "Phase 195 Spike Backlog\|Recommended Sequence\|Validation Redesign Requirements\|Open Decisions for Phase 196\|Non-Goals for Phase 194" docs/PI-NATIVE-CONTEXT-EFFICIENCY-ARCHITECTURE.md`
- [ ] `grep -n "hidden Pi\|authoritative\|artifact" docs/PI-NATIVE-CONTEXT-EFFICIENCY-ARCHITECTURE.md`
- [ ] `git diff -- docs/PI-NATIVE-CONTEXT-EFFICIENCY-ARCHITECTURE.md` reviewed for design-only scope.
- [ ] `bash tests/pi-end-to-end-validation.sh`
- [ ] `bash tests/cross-harness-validation.sh`
</verification>

<success_criteria>
- Context-cost surfaces are named and tied to current authoritative artifacts/resources.
- Pi-native assistance components are mapped to allowed help, forbidden ownership, evidence destinations, and lifecycle surfaces.
- Phase 195 receives a bounded, prioritized spike backlog with validation proof expectations.
- Validation redesign requirements distinguish Pi-supported runtime checks from shared invariant checks and frozen legacy parity checks.
- No runtime source code, validation scripts, module dispatch semantics, GitHub Flow mechanics, legacy surfaces, dependency manifests, or lifecycle authority semantics are changed.
</success_criteria>

<output>
After completion, create `.paul/phases/194-context-efficiency-architecture/194-01-SUMMARY.md` with:
- Files changed.
- Architecture highlights and context-cost model summary.
- Assistance components and guardrails.
- Phase 195 spike recommendations and validation redesign notes.
- Verification commands and results.
</output>
