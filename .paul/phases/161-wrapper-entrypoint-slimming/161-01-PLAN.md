---
phase: 161-wrapper-entrypoint-slimming
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - drivers/pi/skills/paul-apply/SKILL.md
  - drivers/pi/skills/paul-discuss/SKILL.md
  - drivers/pi/skills/paul-fix/SKILL.md
  - drivers/pi/skills/paul-help/SKILL.md
  - drivers/pi/skills/paul-init/SKILL.md
  - drivers/pi/skills/paul-milestone/SKILL.md
  - drivers/pi/skills/paul-pause/SKILL.md
  - drivers/pi/skills/paul-plan/SKILL.md
  - drivers/pi/skills/paul-resume/SKILL.md
  - drivers/pi/skills/paul-status/SKILL.md
  - drivers/pi/skills/paul-unify/SKILL.md
  - tests/pi-end-to-end-validation.sh
autonomous: true
---

<objective>
## Goal
Slim the core Pi wrapper skill shells so they behave as true command-local entrypoints instead of restating workflow-owned read bundles and process prose.

## Purpose
Phase 159 and Phase 160 established that wrapper-layer repetition is one of the safest workflow-side reduction targets. This plan captures that reduction without changing `.paul/*` authority, guided markers, or shared workflow ownership.

## Output
- Slimmed core Pi wrapper skills for the recurring `/paul-*` entrypoints
- Updated Pi validation coverage for the slimmer wrapper contract
- Reinstalled Pi surface plus passing Pi and cross-harness validation
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md
.paul/SPECIAL-FLOWS.md

## Prior Work (only if genuinely needed)
.paul/phases/160-findings-recommendation-package/160-01-SUMMARY.md
.paul/phases/160-findings-recommendation-package/context-reduction-recommendations-ranked.md
.paul/phases/159-workflow-bloat-audit/159-01-SUMMARY.md
.paul/phases/159-workflow-bloat-audit/workflow-read-surface-inventory.md
.paul/phases/159-workflow-bloat-audit/prompt-shape-duplication-audit.md
.paul/phases/159-workflow-bloat-audit/159-workflow-bloat-report.md

## Source Files
drivers/pi/skills/paul-apply/SKILL.md
drivers/pi/skills/paul-discuss/SKILL.md
drivers/pi/skills/paul-fix/SKILL.md
drivers/pi/skills/paul-help/SKILL.md
drivers/pi/skills/paul-init/SKILL.md
drivers/pi/skills/paul-milestone/SKILL.md
drivers/pi/skills/paul-pause/SKILL.md
drivers/pi/skills/paul-plan/SKILL.md
drivers/pi/skills/paul-resume/SKILL.md
drivers/pi/skills/paul-status/SKILL.md
drivers/pi/skills/paul-unify/SKILL.md
drivers/pi/skill-map.md
drivers/pi/extensions/README.md
tests/pi-end-to-end-validation.sh
install.sh
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: TODD(100) → no standard test framework detected via hook commands; existing regression proof remains shell-based under `tests/` | IRIS(150) → no TODO/FIXME/HACK/XXX markers found in the planned wrapper files | DAVE(200) → no CI config detected in repo root, so no CI coverage gap guidance applies to this plan | DOCS(200) → nearby Pi adapter docs already exist in `drivers/pi/skill-map.md`, `drivers/pi/extensions/README.md`, and `README.md` to anchor wrapper terminology | RUBY(250) → planned skill files are all compact (<100 lines), but `tests/pi-end-to-end-validation.sh` is already 750 lines, so validation edits must stay narrow and section-local.

[dispatch] pre-plan enforcement: DEAN(50) → SKIPPED (no supported package manager or audit target detected in this repo)

[dispatch] post-plan: TODD(100) → 0 modifications (no standard TDD candidates detected for the targeted Pi wrapper markdown and validation files)
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | Plan / apply / unify lifecycle work | ✓ |
| /carl | required | Automatic session-boundary handling during execution | ✓ |
| TODD | required | During APPLY if implementation needs test-first or per-task regression handling | ✓ |
| WALT | required | During APPLY / UNIFY validation and quality gating | ✓ |

**BLOCKING:** Required lifecycle flows remain in force for APPLY even though this plan targets Pi wrapper markdown and validation rather than product runtime code.
</skills>

<acceptance_criteria>
## AC-1: Core Pi wrappers become command-local shells
```gherkin
Given the repo-source Pi skill shells for the recurring `/paul-*` lifecycle entrypoints
When Phase 161 is complete
Then each edited wrapper keeps its canonical workflow reference and route-specific guardrails but no longer repeats large workflow-owned read bundles or step-by-step process prose.
```

## AC-2: High-signal entrypoint semantics remain explicit
```gherkin
Given the slimmed wrapper shells for planning, execution, resume, status, milestone, pause, fix, unify, discuss, init, and help
When those files are reviewed together
Then the unique semantics that matter for safe routing remain explicit, including parent-authoritative APPLY, single-next resume/status behavior, collaboration-aware planning entrypoints, and `/paul-*` versus `/skill:paul-*` guidance.
```

## AC-3: The slimmer wrapper contract is deployed and regression-proofed
```gherkin
Given the updated repo-source wrapper shells and validation script
When the Pi install surface is redeployed and the validation suites are run
Then the installed Pi skills reflect the slimmer wrapper contract and both Pi validation and cross-harness validation pass.
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Slim the core Pi lifecycle wrapper shells to a command-local contract</name>
  <files>drivers/pi/skills/paul-apply/SKILL.md, drivers/pi/skills/paul-discuss/SKILL.md, drivers/pi/skills/paul-fix/SKILL.md, drivers/pi/skills/paul-init/SKILL.md, drivers/pi/skills/paul-milestone/SKILL.md, drivers/pi/skills/paul-pause/SKILL.md, drivers/pi/skills/paul-plan/SKILL.md, drivers/pi/skills/paul-resume/SKILL.md, drivers/pi/skills/paul-status/SKILL.md, drivers/pi/skills/paul-unify/SKILL.md</files>
  <action>
    Rewrite the listed wrapper skills so they act as true command-local deltas:
    - keep the frontmatter, the route purpose, the canonical shared workflow path(s), and only the notes that are unique to that wrapper
    - remove duplicated `Read project state:` lists, repeated artifact bundle prose, and multi-step process descriptions that the shared workflow already owns
    - preserve route-specific semantics that are safety-relevant:
      - `paul-apply`: explicit approval, bounded `pals-implementer` delegation, parent-owned verify/module/state authority
      - `paul-resume` and `paul-status`: authoritative `STATE.md` routing and single-next-action behavior
      - `paul-discuss`, `paul-init`, and `paul-milestone`: collaboration / planning-mode / review-menu / milestone-creation notes where they are command-local
      - `paul-pause`, `paul-fix`, and `paul-unify`: loop semantics and reconciliation expectations that are unique to those wrappers
    - keep wording concise and command-local; do not rewrite the shared kernel workflow semantics here
    Avoid: changing command names, changing canonical workflow filenames, or moving lifecycle truth out of shared workflows and `.paul/*` artifacts.
  </action>
  <verify>for f in drivers/pi/skills/paul-{apply,discuss,fix,init,milestone,pause,plan,resume,status,unify}/SKILL.md; do test -f "$f" || exit 1; grep -qi 'workflow' "$f" || exit 1; ! grep -q 'Read project state:' "$f" || exit 1; done</verify>
  <done>AC-1 satisfied for the core lifecycle wrappers: the shells are slimmer, still route to canonical workflows, and no longer restate workflow-owned read bundles.</done>
</task>

<task type="auto">
  <name>Task 2: Align the help surface and Pi validation script with the slimmer wrapper contract</name>
  <files>drivers/pi/skills/paul-help/SKILL.md, tests/pi-end-to-end-validation.sh</files>
  <action>
    Update the help/validation layer so the slimmer entrypoint model is durable:
    - keep `paul-help` explicit about `/paul-*` convenience wrappers, canonical `/skill:paul-*` entry points, shortcut routing, and module-overlay guidance
    - add or refine Pi validation checks so the core wrapper set still has frontmatter, workflow references, and route-specific guardrails, while no longer depending on duplicated step-by-step read scaffolding
    - prefer semantic checks (presence/absence of key phrases and route contracts) over brittle line-count assertions
    - keep the validation edits narrowly scoped inside the existing skill/discovery sections of `tests/pi-end-to-end-validation.sh`
    Avoid: expanding the validation script into a broader refactor or changing review/config entrypoints in this phase.
  </action>
  <verify>bash tests/pi-end-to-end-validation.sh</verify>
  <done>AC-2 and the validation half of AC-3 are satisfied: help/discovery surfaces still explain the wrapper-vs-canonical model and Pi validation enforces the new slimmer wrapper contract.</done>
</task>

<task type="auto">
  <name>Task 3: Reinstall the Pi surface and run full regression validation</name>
  <files>install.sh, tests/pi-end-to-end-validation.sh, tests/cross-harness-validation.sh</files>
  <action>
    Re-deploy and verify the repo-source wrapper changes:
    - run the repo-root installer with the Pi driver selected so the installed skill tree reflects the edited source wrappers
    - run the Pi end-to-end validation suite after install
    - run the cross-harness validation suite to prove the wrapper-shell slimming did not break portability guarantees
    - if validation fails, fix only the wrapper-shell or validation-surface issues exposed by this phase; do not broaden into workflow read-discipline or artifact redesign work
    Avoid: editing installed runtime copies directly under `~/.pi/agent/...`; all fixes must stay in repo-source files.
  </action>
  <verify>PALS_DRIVER=pi bash install.sh && bash tests/pi-end-to-end-validation.sh && bash tests/cross-harness-validation.sh</verify>
  <done>AC-3 satisfied: the slimmer wrapper contract is deployed from repo sources and both validation suites pass.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- kernel/workflows/* (Phase 162 owns workflow read-discipline changes)
- drivers/pi/extensions/pals-hooks.ts (keep runtime command/guided-flow behavior stable in this wrapper-only pass)
- `.paul/*` authority artifacts except normal lifecycle bookkeeping from this plan flow
- drivers/pi/skills/paul-config/SKILL.md and drivers/pi/skills/paul-review/SKILL.md (leave lower-frequency entrypoints for a later pass if still needed)

## SCOPE LIMITS
- This plan is about Pi wrapper shells and their validation contract, not shared workflow prose cleanup
- Do not redesign `STATE.md`, `ROADMAP.md`, or module-dispatch semantics in this phase
- Do not replace canonical `/skill:paul-*` ownership with Pi-only behavior
- Do not add telemetry, Pi-owned workflow state, or new shortcut semantics
</boundaries>

<verification>
Before declaring plan complete:
- [ ] Repo-source wrapper skills still reference the correct shared workflow(s)
- [ ] The edited core wrappers no longer contain duplicated `Read project state:` scaffolding
- [ ] `drivers/pi/skills/paul-help/SKILL.md` still explains `/paul-*`, canonical `/skill:paul-*`, shortcuts, and module overlays
- [ ] `PALS_DRIVER=pi bash install.sh` completes successfully
- [ ] `bash tests/pi-end-to-end-validation.sh` passes
- [ ] `bash tests/cross-harness-validation.sh` passes
- [ ] All acceptance criteria are met
</verification>

<success_criteria>
- All 3 tasks completed
- Core Pi lifecycle wrappers are materially slimmer without losing route-specific guardrails
- Validation covers the new wrapper contract without brittle line-count coupling
- Pi install + Pi validation + cross-harness validation all pass
</success_criteria>

<output>
After completion, create `.paul/phases/161-wrapper-entrypoint-slimming/161-01-SUMMARY.md`
</output>
