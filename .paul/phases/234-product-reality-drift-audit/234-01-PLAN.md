---
phase: 234-product-reality-drift-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/PALS-MAINTAINABILITY-PRODUCT-REALITY-AUDIT.md
autonomous: true
---

<objective>
## Goal
Create `docs/PALS-MAINTAINABILITY-PRODUCT-REALITY-AUDIT.md`, a grounded audit of PALS maintainability and product-reality risks before any implementation changes in v2.55.

## Purpose
v2.55 should make PALS cheaper to maintain, easier to validate, easier to explain, and harder to drift without weakening lifecycle authority, explicit approvals, module evidence, validation truth, GitHub Flow gates, parent-owned APPLY, delegated APPLY boundaries, `.paul/*` artifact truth, command-output truth, or full authoritative read fallback. Phase 234 establishes the evidence base for Phases 235-241.

## Output
A single docs-first audit artifact with:
- ranked weakness list
- file-size / hotspot inventory
- artifact drift inventory
- validation brittleness inventory
- first-run friction notes
- usefulness-evidence gaps
- recommendations for Phases 235-241
</objective>

<context>
## Project Context
- `.paul/PROJECT.md` — current v2.55 focus, active guarantees, constraints, and project posture.
- `.paul/ROADMAP.md` — active v2.55 milestone and Phase 234 target scope.
- `.paul/STATE.md` — current lifecycle truth and next action.
- `docs/PALS-CRITICAL-REVIEW-NEXT-ROADMAP.md` — source context for the critical review and proposed v2.55 phase map.

## Read-only audit targets
- `.paul/` lifecycle artifacts, especially `STATE.md`, `ROADMAP.md`, `MILESTONES.md`, phase summaries, archive indexes, and handoff/archive conventions.
- `docs/` product and architecture documents, especially README-adjacent onboarding and Pi-native architecture/validation docs.
- `tests/` validation scripts, especially Pi end-to-end and cross-harness suites.
- `drivers/pi/extensions/`, `drivers/*`, `kernel/`, and `modules/` as maintainability/hotspot inventory targets.

## Source Files Modified
- `docs/PALS-MAINTAINABILITY-PRODUCT-REALITY-AUDIT.md`
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: modules.yaml loaded from installed PALS registry; kernel_version 2.0.0 matches pals.json schema_version 2.0.0.

- TODD: No `*.test.*`, `*.spec.*`, or `__tests__` paths detected in the repository-level scan. No root package/Makefile test framework marker detected. Plan remains `execute`; verification relies on focused marker checks plus existing validation shell suites where applicable.
- IRIS: Planned changed file is a new docs audit. No TODO/FIXME/HACK/XXX markers found in `docs/PALS-CRITICAL-REVIEW-NEXT-ROADMAP.md`; planned output should avoid placeholder debt.
- DAVE: No CI/CD config detected from `.github`/GitLab/Docker/Jenkins patterns in the bounded scan. Docs-only audit introduces no deploy changes; record CI absence as product-reality context if relevant, but do not generate CI in this phase.
- DOCS: Docs catalog exists under `docs/`; planned file is documentation and is directly related to the v2.55 roadmap source document. No separate source/doc drift expected for this docs-only audit.
- CODI: skipped — no extractable symbols in phase scope. This plan modifies only a Markdown audit artifact and does not change TS/JS behavior.
- RUBY: No preliminary debt indicators in planned changed files. Phase output should inventory debt/hotspots rather than refactor them.

[dispatch] pre-plan enforcement: DEAN PASS — no root package manager/lockfile was detected; `drivers/agent-sdk/requirements.txt` exists but this plan changes no dependency files. `python3 -m pip_audit -r drivers/agent-sdk/requirements.txt -f json` was unavailable (`No module named pip_audit`), so no dependency audit baseline is created; dependency risk is unchanged by this docs-only plan.

## Post-Plan Module Dispatch
[dispatch] post-plan: TODD reviewed plan type. No detected test coverage for the planned docs artifact; no TDD restructuring suggested.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | PLAN/APPLY/UNIFY lifecycle management | ✓ |
| /carl | required | Session boundary monitoring at phase boundaries/mid-phase | automatic |
| TODD | required | APPLY test/validation discipline | module dispatch recorded |
| WALT | required | APPLY/UNIFY quality gates and validation evidence | module dispatch during APPLY/UNIFY |

**BLOCKING:** Required skills/modules must remain visible in APPLY/UNIFY evidence. No extra user-loaded skill is required before APPLY for this docs-only audit.
</skills>

<acceptance_criteria>
## AC-1: Audit artifact exists with required sections
```gherkin
Given Phase 234 is a docs/audit-first phase
When APPLY completes the plan
Then docs/PALS-MAINTAINABILITY-PRODUCT-REALITY-AUDIT.md exists and contains sections for ranked weaknesses, file-size/hotspots, artifact drift, validation brittleness, first-run friction, usefulness-evidence gaps, and recommendations for Phases 235-241
```

## AC-2: Findings are evidence-backed and bounded
```gherkin
Given the audit is used to guide future implementation phases
When a weakness, hotspot, drift item, or recommendation is listed
Then the item cites a concrete repo artifact, command/check result, or explicit source context instead of relying on unsupported speculation
```

## AC-3: No implementation scope creep
```gherkin
Given Phase 234 is intended to create a grounded audit before changing implementation
When APPLY modifies files
Then the only planned product/source change is the audit document and no runtime, validation-suite, module, driver, or lifecycle behavior is changed
```

## AC-4: Verification protects audit completeness
```gherkin
Given the audit document is the phase output
When verification runs
Then focused marker checks confirm all required audit sections are present, and existing validation commands are run or explicitly scoped with captured rationale if a full suite is not appropriate for docs-only audit work
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Build maintainability and product-reality inventories</name>
  <files>docs/PALS-MAINTAINABILITY-PRODUCT-REALITY-AUDIT.md</files>
  <action>
    Create the audit document and populate the evidence inventory sections before ranking recommendations.
    - Use `docs/PALS-CRITICAL-REVIEW-NEXT-ROADMAP.md` as the framing source, but verify claims against current repo artifacts.
    - Inventory likely maintenance gravity wells using native file discovery/line-count inspection, including `tests/`, `drivers/pi/extensions/`, `kernel/`, `modules/`, `.paul/`, and `docs/`.
    - Inventory validation brittleness by inspecting validation scripts for exact-marker/prose contract patterns and fragile string checks.
    - Inventory artifact drift by comparing hot lifecycle/index artifacts (`.paul/STATE.md`, `.paul/ROADMAP.md`, `.paul/MILESTONES.md`, latest summaries, archive indexes, handoff archive state) for mismatched current milestone/phase pointers.
    - Inventory first-run friction from README/docs/module/driver surfaces visible in the repo; focus on how much a new user must understand before feeling value.
    - Inventory usefulness-evidence gaps using existing PALS evidence docs and summaries; distinguish structural validation evidence from user-value evidence.
    - Avoid fixing issues in this task. Record findings only.
  </action>
  <verify>Focused read/grep checks show the audit document includes the required inventory headings and at least one concrete artifact reference in each inventory section.</verify>
  <done>AC-1 and AC-2 satisfied: required inventories exist and findings are evidence-backed.</done>
</task>

<task type="auto">
  <name>Task 2: Rank weaknesses and map recommendations to Phases 235-241</name>
  <files>docs/PALS-MAINTAINABILITY-PRODUCT-REALITY-AUDIT.md</files>
  <action>
    Extend the audit document with decision-ready synthesis.
    - Add a ranked weakness list with severity/rationale tied to evidence from Task 1.
    - Add a recommendations section for Phases 235-241, mapping each future phase to the highest-value follow-up work and any boundaries discovered by the audit.
    - Call out items explicitly deferred from Phase 234 so APPLY does not silently start implementation.
    - Preserve v2.55 constraints: lifecycle authority, explicit approvals, module evidence, validation truth, GitHub Flow gates, parent-owned APPLY, delegated APPLY boundaries, `.paul/*` artifact truth, command-output truth, and full authoritative read fallback.
  </action>
  <verify>Focused read/grep checks show each Phase 235-241 has a recommendation entry and the document includes a ranked weakness list plus deferred/non-goal notes.</verify>
  <done>AC-1, AC-2, and AC-3 satisfied: recommendations are complete, bounded, and phase-mapped.</done>
</task>

<task type="auto">
  <name>Task 3: Verify audit completeness and scope discipline</name>
  <files>docs/PALS-MAINTAINABILITY-PRODUCT-REALITY-AUDIT.md</files>
  <action>
    Run and record verification for the audit artifact.
    - Run focused marker checks for all required sections and phase recommendation entries.
    - Run `git diff --check` to catch whitespace/conflict-marker issues.
    - Run existing Pi validation and cross-harness validation if practical for docs-only audit work. If a suite is intentionally scoped out, record the exact rationale and the narrower proof used instead.
    - Confirm the changed-file set stays within the planned docs/audit scope plus PAUL lifecycle artifacts generated by APPLY/UNIFY.
  </action>
  <verify>Verification commands/checks are recorded in the eventual SUMMARY.md, including marker checks, `git diff --check`, validation-suite result or explicit scoped rationale, and changed-file review.</verify>
  <done>AC-3 and AC-4 satisfied: audit is complete, scope is controlled, and verification evidence is available for UNIFY.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- Runtime behavior in `drivers/pi/extensions/`, `kernel/`, `modules/`, or driver implementations.
- Validation suite logic in `tests/`.
- Install/uninstall scripts, package/dependency files, or CI configuration.
- Existing lifecycle authority rules, approval gates, module dispatch evidence, GitHub Flow requirements, parent-owned APPLY verification, delegated APPLY boundaries, or `.paul/*` artifact truth.

## SCOPE LIMITS
- This plan creates an audit document only; it does not implement fixes for identified drift, validation brittleness, onboarding friction, or modularization risks.
- Recommendations for Phases 235-241 may be specific, but actual implementation belongs to later plans.
- Do not add telemetry or hidden runtime state as usefulness evidence.
- Do not treat marker/prose validation as fixed merely because it is documented; classify brittleness honestly.
</boundaries>

<verification>
Before declaring APPLY complete:
- [ ] `docs/PALS-MAINTAINABILITY-PRODUCT-REALITY-AUDIT.md` exists.
- [ ] Focused marker checks confirm sections for ranked weaknesses, file-size/hotspots, artifact drift, validation brittleness, first-run friction, usefulness-evidence gaps, and recommendations for Phases 235-241.
- [ ] Each major finding cites a concrete repo artifact, command/check result, or source context.
- [ ] Each Phase 235-241 has a recommendation entry.
- [ ] `git diff --check` passes.
- [ ] Existing Pi validation and cross-harness validation are run, or any docs-only scoped skip is explicitly justified with alternate focused proof.
- [ ] Changed files remain within planned audit/lifecycle scope.
</verification>

<success_criteria>
- The Phase 234 audit artifact is complete enough to guide Phases 235-241 without another discovery pass.
- No runtime, module, driver, validation behavior, dependency, or CI changes are introduced.
- Verification evidence is sufficient for UNIFY to reconcile the phase honestly.
- The audit improves product reality by distinguishing maintainability risk, process weight, onboarding friction, and usefulness-evidence gaps.
</success_criteria>

<output>
After completion, create `.paul/phases/234-product-reality-drift-audit/234-01-SUMMARY.md` with:
- files created/modified
- key findings and ranked weaknesses
- recommendations carried into Phases 235-241
- validation commands/results or scoped rationale
- module execution reports
- deviations from this PLAN, if any
</output>
