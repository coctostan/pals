---
phase: 01-bench-command
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/index.ts
  - test/bench.test.ts
  - README.md
autonomous: true
---

<objective>
## Goal
Create the first usable `/bench` MVP for pi-bench: accept a prompt plus 2+ models, resolve models from config defaults with inline overrides, execute the same benchmark run per model through Pi APIs, and print structured latency/token comparison output.

## Purpose
This delivers the first meaningful product outcome from `PROJECT.md`: helping Pi users compare models inside Pi without leaving the tool.

## Output
- Extension command entrypoint implementing `/bench`
- Benchmark execution flow using assumed Pi APIs
- Automated tests covering command parsing and comparison output behavior
- README usage updated for the MVP command
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md
.paul/PRD.md

## Prior Work (only if genuinely needed)
None.

## Source Files
package.json
README.md
src/index.ts
test/bench.test.ts
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: ARCH(75) → detected minimal flat TypeScript scaffold, no circular dependency evidence | TODD(100) → tdd candidate present because benchmark command behavior is testable and `test/` exists | IRIS(150) → no existing source anti-patterns found in current scope | DAVE(200) → no CI config detected; advisory only, CI remains deferred | DOCS(200) → README exists but is minimal; update recommended when command lands | RUBY(250) → no current debt flags beyond empty scaffold.

[dispatch] pre-plan enforcement: DEAN(50) → skip, no lockfile/audit baseline available, no blocking result | SETH(80) → no hardcoded secret findings in planned files, no block.
</module_dispatch>

<acceptance_criteria>
## AC-1: Bench command accepts prompt and model selection
```gherkin
Given the extension is installed
When the user runs `/bench` with a prompt and at least two models, or relies on configured default models with inline overrides
Then the command resolves the final model list and rejects invalid runs with fewer than two models.
```

## AC-2: Benchmark execution produces structured comparison output
```gherkin
Given `/bench` resolves two or more models
When the command benchmarks the same prompt across those models through the Pi APIs
Then it returns structured comparison output containing one result per model with latency and token usage fields.
```

## AC-3: MVP behavior is documented and test-covered
```gherkin
Given a developer reads the repo after the MVP is implemented
When they inspect automated tests and README usage instructions
Then they can verify command behavior and understand how config defaults and inline model overrides work.
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Define `/bench` command contract and model resolution flow</name>
  <files>src/index.ts, test/bench.test.ts</files>
  <action>
    Implement the extension entrypoint and `/bench` command shape in `src/index.ts`.
    Define the MVP command contract around one prompt plus 2+ models.
    Support both config defaults and inline model overrides, with inline values taking precedence.
    Validate that at least two models are present before execution starts.
    Add tests for argument parsing, default resolution, override precedence, and invalid model-count handling.
    Avoid: adding output-quality scoring, persistence, or CI hooks in this plan, because those are explicitly deferred.
  </action>
  <verify>npm test</verify>
  <done>AC-1 satisfied: command contract, model resolution, and invalid-input handling are implemented and covered by tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement benchmark execution and structured comparison reporting</name>
  <files>src/index.ts, test/bench.test.ts</files>
  <action>
    Add the benchmark runner in `src/index.ts` that executes the same prompt per selected model using the assumed Pi APIs `pi.switchModel` and `pi.sendUserMessage`.
    Capture per-model result data with model identifier, latency, and token usage when available from the API response.
    Format a structured comparison output that is deterministic and testable.
    Mock or stub Pi API interactions in tests so command behavior can be verified without live model calls.
    Avoid: introducing historical storage, subjective quality scoring, or API abstraction layers that are not needed for the MVP.
  </action>
  <verify>npm test</verify>
  <done>AC-2 satisfied: benchmark execution returns structured per-model comparison output with latency and token fields.</done>
</task>

<task type="auto">
  <name>Task 3: Document MVP usage and constraints</name>
  <files>README.md, package.json</files>
  <action>
    Update `README.md` with a concise description of the `/bench` command, example invocation, expected output shape, and the config-default-plus-inline-override model selection behavior.
    Update `package.json` only if script or package metadata needs adjustment to reflect the implemented command/test flow.
    Keep documentation aligned with the actual MVP only.
    Avoid: documenting deferred features as if shipped.
  </action>
  <verify>npm test && npm run check</verify>
  <done>AC-3 satisfied: README explains the shipped MVP and repository checks still pass.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- `.paul/**` except the required summary after APPLY
- `pals.json`
- Deferred scope: output quality scoring, historical tracking, CI integration

## SCOPE LIMITS
- This plan delivers a single-command MVP only.
- Do not add persistence, dashboards, or external services.
- Do not assume unsupported Pi APIs beyond `pi.switchModel` and `pi.sendUserMessage`; if token metadata is partial, surface the available fields without inventing data.
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `npm test` passes
- [ ] `npm run check` passes
- [ ] `/bench` behavior is covered for default models, inline overrides, and invalid fewer-than-2-model runs
- [ ] Structured output includes one row/result per model with latency and token fields
- [ ] README usage matches implemented command behavior
- [ ] All acceptance criteria are met
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced by tests, typecheck, or formatting checks
- The repo now contains a usable `/bench` MVP aligned with the initial release scope
</success_criteria>

<output>
After completion, create `.paul/phases/01-bench-command/01-01-SUMMARY.md`
</output>
