---
phase: 60-interactive-shortcuts-flow-entry-points
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - drivers/pi/extensions/pals-hooks.ts
  - drivers/pi/extensions/README.md
  - drivers/pi/skill-map.md
  - drivers/pi/skills/paul-help/SKILL.md
  - tests/pi-end-to-end-validation.sh
autonomous: true
---

<objective>
## Goal
Add lightweight Pi-native interactive shortcuts and flow entry points from the visible PALS lifecycle surface into existing status, resume, help, and milestone actions without changing workflow semantics or creating Pi-only lifecycle truth.

## Purpose
This phase builds on the always-visible lifecycle surface from Phase 59 so Pi users can move from passive lifecycle visibility into the most relevant existing PALS actions faster, while keeping shared markdown workflows and `/skill:paul-*` entries as the canonical implementation layer.

## Output
- Updated Pi extension with bounded quick-action metadata, shortcut wiring, and lifecycle-surface entry hints
- Updated Pi adapter docs/help text describing shortcut-enabled entry points and canonical routing
- Validation coverage proving the shortcut/entry-point layer remains adapter-only and wrapper-based
- Plan summary artifact at `.paul/phases/60-interactive-shortcuts-flow-entry-points/60-01-SUMMARY.md`
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md

## Prior Work (only if genuinely needed)
.paul/phases/57-command-routing-discoverability/57-01-SUMMARY.md
.paul/phases/58-adapter-consistency-cleanup/58-01-SUMMARY.md
.paul/phases/59-always-visible-lifecycle-surface/59-01-SUMMARY.md

## Source Files
drivers/pi/extensions/pals-hooks.ts
drivers/pi/extensions/README.md
drivers/pi/skill-map.md
drivers/pi/skills/paul-help/SKILL.md
tests/pi-end-to-end-validation.sh
/opt/homebrew/lib/node_modules/@mariozechner/pi-coding-agent/docs/extensions.md
/opt/homebrew/lib/node_modules/@mariozechner/pi-coding-agent/docs/keybindings.md
/opt/homebrew/lib/node_modules/@mariozechner/pi-coding-agent/examples/extensions/plan-mode/index.ts
</context>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | Before APPLY for standard PALS phase execution | ○ |

**BLOCKING:** Required skills MUST be loaded before APPLY proceeds.
Run each skill command or confirm already loaded.

## Skill Invocation Checklist
- [ ] /paul loaded (standard PALS lifecycle flow)
</skills>

<acceptance_criteria>
## AC-1: The visible lifecycle surface offers lightweight action entry points
```gherkin
Given a Pi session in a PALS project with `.paul/STATE.md`
When the PALS Pi extension renders the lifecycle status/widget surface
Then the user can see compact entry hints for the most relevant existing PALS actions without losing the artifact-derived milestone/phase/loop/next-action context
```

## AC-2: Pi-native shortcuts route into existing PALS commands without changing workflow truth
```gherkin
Given the updated Pi extension is loaded
When a user triggers one of the approved shortcut-enabled entry points for status, resume, help, or milestone work
Then Pi routes into the existing `/paul-*` or canonical `/skill:paul-*` flow surfaces rather than inventing new workflow semantics or Pi-owned lifecycle state
```

## AC-3: Shortcut UX remains documented, low-clutter, and regression-checked
```gherkin
Given the updated extension, docs, and validation suite
When the Pi adapter guidance and validation run
Then they describe the shortcut/entry-point layer clearly, verify the expected shortcut and routing contract, and preserve existing artifact-safe and canonical-routing guardrails
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Add a reusable quick-action model for the Pi lifecycle surface</name>
  <files>drivers/pi/extensions/pals-hooks.ts</files>
  <action>
    Refactor the Pi extension so the lifecycle parsing/rendering path can also derive a bounded set of quick actions from current PALS state and existing command surfaces.
    - Introduce a small action model that maps current lifecycle state to compact flow-entry metadata for the approved surfaces: next action, status, resume, help, and milestone
    - Reuse the existing command-wrapper routing pattern so quick actions resolve to already-registered `/paul-*` wrappers and keep `/skill:paul-*` canonical
    - Keep the action model read-only and derived from `.paul/STATE.md` plus static command definitions; it must not persist independent workflow truth in Pi
    - Preserve graceful fallback behavior when state is partial so the visible surface stays informative without becoming noisy or stale
    Avoid: adding a second lifecycle authority, inventing new workflow commands, or broadening the action set into unrelated flows outside the Phase 60 scope
  </action>
  <verify>grep -n "nextAction\|registerCommand\|paul-status\|paul-resume\|paul-help\|paul-milestone" drivers/pi/extensions/pals-hooks.ts</verify>
  <done>AC-1 satisfied in part: the extension has an artifact-grounded quick-action model that points users into approved existing PALS flow surfaces</done>
</task>

<task type="auto">
  <name>Task 2: Wire shortcut-enabled flow entry points into the Pi extension UI</name>
  <files>drivers/pi/extensions/pals-hooks.ts</files>
  <action>
    Use documented Pi extension primitives to turn the visible lifecycle surface into a lightweight action launcher without changing PALS semantics.
    - Update the lifecycle widget/status/notification output so it surfaces compact action hints or labels for the approved entry points while keeping the current milestone/phase/loop/next-action visibility intact
    - Register a small, memorable set of Pi-native keyboard shortcuts for the approved actions and route them through the same extension command helper used by the slash-command wrappers
    - Keep shortcut behavior additive: shortcuts should trigger existing command paths, not bypass skill routing or duplicate workflow logic in TypeScript
    - Ensure the UX stays low-clutter by limiting the number of visible actions and avoiding heavy custom UI overlays for this phase
    Avoid: custom modal workflows, custom tools, replacing the footer/widget surfaces, or adding shortcut-triggered behavior that mutates `.paul/*` outside the existing workflow commands
  </action>
  <verify>grep -n "registerShortcut\|setStatus\|setWidget\|notify\|sendUserMessage" drivers/pi/extensions/pals-hooks.ts</verify>
  <done>AC-1 and AC-2 satisfied: Pi shows lightweight flow-entry hints and keyboard shortcuts that launch the existing PALS command/skill surfaces</done>
</task>

<task type="auto">
  <name>Task 3: Extend Pi docs and validation for shortcut-enabled flow entry points</name>
  <files>drivers/pi/extensions/README.md, drivers/pi/skill-map.md, drivers/pi/skills/paul-help/SKILL.md, tests/pi-end-to-end-validation.sh</files>
  <action>
    Update Pi-facing documentation and regression checks so the new shortcut/entry-point layer is explicit, bounded, and compatible with the canonical routing model.
    - Document which lightweight shortcut-enabled entry points exist and how they relate to `/paul-*` wrappers and canonical `/skill:paul-*` skills
    - Update the help/discovery surfaces so users can learn the quickest path into status, resume, help, milestone, and next-step actions from the visible lifecycle context
    - Expand the Pi validation suite to verify shortcut registration and lifecycle-entry guidance while preserving existing checks for canonical routing, single auto-injection behavior, and artifact-safe lifecycle derivation
    Avoid: documentation that implies the shortcut layer is canonical, or brittle tests tied to exact formatting instead of stable contract markers
  </action>
  <verify>bash tests/pi-end-to-end-validation.sh</verify>
  <done>AC-3 satisfied: the shortcut-enabled Pi entry layer is documented and regression-checked without weakening existing adapter guardrails</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- Shared kernel workflows, templates, and `.paul/*` lifecycle semantics
- Canonical `/skill:paul-*` entry semantics or workflow ownership
- Claude Code driver behavior or cross-harness kernel portability guarantees
- Any persistence layer outside existing artifact files and current Pi session/runtime surfaces

## SCOPE LIMITS
- This plan adds lightweight Pi-native shortcuts and flow entry points only
- Keep the action set bounded to the visible-surface-adjacent flows: next action, status, resume, help, and milestone
- Do not introduce Pi-only lifecycle state, Pi-only workflow branches, or heavy custom overlay UI
- Do not broaden automatic context injection beyond the current single injection-site guardrail
- Do not redesign unrelated command surfaces, installer behavior, or non-Pi drivers in this phase
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `grep -n "registerShortcut\|sendUserMessage\|paul-status\|paul-resume\|paul-help\|paul-milestone" drivers/pi/extensions/pals-hooks.ts` shows the new shortcut/entry-point wiring
- [ ] `grep -n "/paul-\*\|/skill:paul-\*\|shortcut" drivers/pi/extensions/README.md drivers/pi/skill-map.md drivers/pi/skills/paul-help/SKILL.md` confirms updated discovery guidance
- [ ] `bash tests/pi-end-to-end-validation.sh` passes
- [ ] The visible lifecycle surface and all new entry points remain derived from existing artifact state and wrapper routing only
- [ ] All acceptance criteria are met
</verification>

<success_criteria>
- All three tasks completed
- Pi extension exposes compact quick-action hints and a bounded keyboard-shortcut layer for approved PALS flows
- All shortcut-enabled entry points reuse existing `/paul-*` wrappers and preserve `/skill:paul-*` as canonical
- No Pi-only lifecycle truth, extra auto-injection sites, or cross-harness regressions are introduced
- Validation passes with explicit coverage of the shortcut/entry-point contract
</success_criteria>

<output>
After completion, create `.paul/phases/60-interactive-shortcuts-flow-entry-points/60-01-SUMMARY.md`
</output>
