---
phase: 213-evidence-reconciliation-milestone-closure
plan: 01
type: execute
wave: 1
depends_on:
  - 212-01
files_modified:
  - docs/PI-NATIVE-WORKFLOW-RESOURCE-CAPSULE-CONTRACT.md
  - tests/pi-end-to-end-validation.sh
  - tests/cross-harness-validation.sh
autonomous: true
---

<objective>
## Goal
Reconcile v2.49 Workflow/Resource Capsule Contract evidence against the Phase 210 baseline, Phase 211 runtime hardening, and Phase 212 docs/validation surfacing, then close the milestone with command-backed validation, GitHub Flow evidence, lifecycle metadata, and a clear handoff to the next Pi-native build milestone.

## Purpose
Phase 210 defined the workflow/resource capsule contract; Phase 211 implemented runtime capsule loading; Phase 212 made the behavior discoverable and validation-protected. Phase 213 closes v2.49 by:
- updating the contract with final baselines and closure evidence,
- adding durable closure marker checks for the final v2.49 capsule evidence,
- preserving command-output validation truth and full-read/non-authority boundaries,
- producing complete UNIFY evidence so the next milestone can start from a clean closure point.

## Output
- `docs/PI-NATIVE-WORKFLOW-RESOURCE-CAPSULE-CONTRACT.md` distinguishes Phase 210/211 baseline evidence from the Phase 212 closure baseline and records final validation counts.
- Pi and cross-harness validation suites protect v2.49 closure markers, exact validation-class names, command-output truth, and derived-only/non-authority boundaries.
- `.paul/phases/213-evidence-reconciliation-milestone-closure/213-01-SUMMARY.md` records AC results, marker grep evidence, TAP totals, validation count reconciliation, module reports, GitHub Flow evidence, decisions/deviations, v2.49 milestone closure, and next milestone handoff.
</objective>

<context>
## Project Context
- `.paul/PROJECT.md` — current v2.49 posture; Phase 213 ready to plan.
- `.paul/ROADMAP.md` — v2.49 phase table; Phase 213 focus.
- `.paul/STATE.md` — lifecycle truth and Phase 213 routing.

## Prior Work
- `docs/PI-NATIVE-WORKFLOW-RESOURCE-CAPSULE-CONTRACT.md` — Phase 210 contract baseline and Phase 213 closure requirements.
- `.paul/phases/210-workflow-resource-capsule-contract-baseline/210-01-SUMMARY.md` — contract baseline evidence.
- `.paul/phases/211-runtime-capsule-loading-hardening/211-01-SUMMARY.md` — runtime capsule evidence and Phase 212 handoff.
- `.paul/phases/212-documentation-validation-surfacing/212-01-SUMMARY.md` — docs/validation surfacing evidence, PR #127 merge evidence, final Phase 212 counts (Pi 196/196, cross-harness 111/111), and Phase 213 handoff.
- `.paul/phases/209-evidence-reconciliation-milestone-closure/209-01-PLAN.md` and SUMMARY — prior milestone closure pattern for reconciling baselines and adding durable closure checks.

## Source Files
- `docs/PI-NATIVE-WORKFLOW-RESOURCE-CAPSULE-CONTRACT.md`
- `tests/pi-end-to-end-validation.sh`
- `tests/cross-harness-validation.sh`

## CODI Seed Candidates
- No TS/JS source selectors in this docs/shell-validation closure plan; CODI may skip cleanly.
</context>

<module_dispatch>
## Pre-Plan Module Dispatch

Planning collaboration level: high (project default). Planning mode: direct-requirements.

[dispatch] pre-plan advisory: TODD, IRIS, DAVE, DOCS, CODI, RUBY, ARCH, SETH, ARIA, LUKE, DANA, GABE, OMAR, PETE, REED, and VERA scope probes completed; advisory only.

[dispatch] TODD: shell validation suites are in planned scope (`tests/pi-end-to-end-validation.sh`, `tests/cross-harness-validation.sh`); no root package test framework conversion requested. This plan should extend existing shell marker checks and run both suites.

[dispatch] IRIS: no TODO/FIXME/HACK/XXX markers found in planned files.

[dispatch] DAVE: no CI/CD config files discovered in bounded scan; no CI file changes planned.

[dispatch] DOCS: planned doc edit is limited to `docs/PI-NATIVE-WORKFLOW-RESOURCE-CAPSULE-CONTRACT.md`; README/Pi docs/skill-map were surfaced in Phase 212 and should not be reopened unless validation proves drift.

[dispatch] ARCH: Detected pattern: modular kernel/driver/module architecture (high confidence). Planned documentation lives in `docs/`; validation additions live in existing shell suites. Advisory: `tests/pi-end-to-end-validation.sh` and `tests/cross-harness-validation.sh` are pre-existing large files; keep additions marker-based and localized near current workflow/resource capsule checks.

[dispatch] SETH: no hardcoded secret patterns found in planned files; no auth/API surface in scope.

[dispatch] ARIA: no UI component files in planned changes — skipping.

[dispatch] LUKE: no UI component/design-token files in planned changes — skipping.

[dispatch] DANA: no data schema/model/migration files in planned changes — skipping.

[dispatch] GABE: no API route/controller/schema files in planned changes — skipping.

[dispatch] OMAR: no logging/health/error-handler observability changes required — docs/shell marker checks only.

[dispatch] PETE: no performance-sensitive runtime source in planned changes; avoid expensive validation scans and keep marker checks focused.

[dispatch] REED: no resilience-relevant runtime source in planned changes — skipping.

[dispatch] VERA: no privacy/PII files or data collection surfaces in scope — skipping.

[dispatch] CODI: skipped — no TS/JS source selectors in planned Phase 213 docs/shell-validation closure scope.

[dispatch] RUBY: planned shell validation files are large/pre-existing; additions should be helper-local/marker-based and avoid broad refactors.

[dispatch] pre-plan enforcement: DEAN PASS — no package manager manifests or lockfiles detected at repo root/maxdepth 2 outside ignored runtime/vendor paths, so dependency audit is not applicable and no critical/high dependency block exists.

[dispatch] post-plan: TODD reviewed plan type `execute`; shell validation suites are the proof path and no TDD conversion is requested.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | PLAN/APPLY/UNIFY lifecycle management | ✓ |
| /carl | required | Session boundary management at lifecycle/session transitions | ✓ |
| TODD | required | Test/validation enforcement during APPLY | ✓ via installed modules.yaml |
| WALT | required | Quality gating and validation during APPLY/UNIFY | ✓ via installed modules.yaml |

**BLOCKING:** Required skills/modules MUST remain loaded before APPLY proceeds.
</skills>

<acceptance_criteria>
## AC-1: Contract records v2.49 closure baselines and evidence chain
```gherkin
Given Phase 210 defined the workflow/resource capsule contract, Phase 211 hardened runtime loading, and Phase 212 surfaced docs/validation markers
When `docs/PI-NATIVE-WORKFLOW-RESOURCE-CAPSULE-CONTRACT.md` is updated
Then it records the historical Phase 210/211 baseline (Pi 193/193 and cross-harness 109/109), the Phase 212/v2.49 closure baseline (Pi 196/196 and cross-harness 111/111), PR #127 merge evidence, and the requirement that future count changes record old count, new count, reason, validation class affected, and shared-invariant preservation rationale.
```

## AC-2: Pi validation protects v2.49 workflow/resource capsule closure markers
```gherkin
Given Phase 212 added runtime/docs/skill-map marker checks for workflow/resource capsules
When `tests/pi-end-to-end-validation.sh` runs after this plan
Then it protects the v2.49 closure evidence markers in the contract, including `Workflow/Resource Capsule Context — Pi-Supported Runtime`, `Workflow/Resource Capsule Context — Shared Invariant`, Pi 196/196, cross-harness 111/111, source/type/freshness/bounds/fallback/`Derived aid only`, and full authoritative read fallback markers.
```

## AC-3: Cross-harness validation protects shared invariant closure markers
```gherkin
Given workflow/resource capsules are Pi-supported runtime aids but `.paul/*`, PLAN/APPLY/UNIFY, module evidence, GitHub Flow evidence, checkpoints, parent-owned APPLY, and command output remain shared authority
When `tests/cross-harness-validation.sh` runs after this plan
Then it protects the contract closure markers for `.paul/*` authority, STOP/DO NOT SKIP exclusion, checkpoint blocking, module evidence, GitHub Flow safety, parent-owned APPLY, command-output truth, validation classes, and full authoritative read fallback.
```

## AC-4: v2.49 milestone closure produces complete reconciliation evidence
```gherkin
Given Phase 213 is the final v2.49 phase
When APPLY/UNIFY completes
Then `.paul/phases/213-evidence-reconciliation-milestone-closure/213-01-SUMMARY.md` records acceptance-criteria results, focused marker grep output, fresh Pi/cross-harness TAP totals, old/new count reconciliation, changed-file scope review, module reports, GitHub Flow evidence, decisions/deviations, v2.49 milestone closure, and next milestone handoff.
```

## AC-5: Scope remains evidence reconciliation and milestone closure only
```gherkin
Given runtime capsule loading and docs/validation surfacing are already complete
When this plan is applied
Then it does not change runtime behavior, installer behavior, dependencies, CI configuration, helper delegation packet/report behavior, broad validation classification, legacy Claude Code/Agent SDK surfaces, or lifecycle authority semantics outside normal `.paul/*` lifecycle writes.
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Reconcile workflow/resource capsule contract closure baselines</name>
  <files>docs/PI-NATIVE-WORKFLOW-RESOURCE-CAPSULE-CONTRACT.md</files>
  <action>
    Update the contract's status/evidence sections so v2.49 closure is explicit:
    - Record Phase 210/211 historical baseline: Pi 193/193 and cross-harness 109/109.
    - Record Phase 212/v2.49 closure baseline: Pi 196/196 and cross-harness 111/111.
    - Record that PR #127 merged Phase 212 docs/validation surfacing and that local validation plus Socket checks passed.
    - Preserve the command-output evidence requirement that future count changes record old count, new count, reason, validation class affected, and shared-invariant preservation rationale.
    - Preserve full authoritative read fallback, `Derived aid only`, STOP/DO NOT SKIP exclusion, checkpoint/module/GitHub Flow/validation non-authority, and `.paul/*` authority boundaries.
    - Do not rewrite the contract as if Phase 210 implemented runtime/docs behavior; keep Phase 210 as baseline, Phase 211 as runtime hardening, Phase 212 as surfacing, and Phase 213 as closure.
  </action>
  <verify>grep -n "193/193\|109/109\|196/196\|111/111\|PR #127\|Workflow/Resource Capsule Context — Pi-Supported Runtime\|Workflow/Resource Capsule Context — Shared Invariant\|old count\|new count\|validation class affected\|shared-invariant preservation rationale\|Derived aid only\|full authoritative read" docs/PI-NATIVE-WORKFLOW-RESOURCE-CAPSULE-CONTRACT.md</verify>
  <done>AC-1 satisfied: contract records historical and closure baselines, PR evidence, validation-class names, and count-change reconciliation requirements.</done>
</task>

<task type="auto">
  <name>Task 2: Add durable v2.49 closure marker checks in Pi and cross-harness validation</name>
  <files>tests/pi-end-to-end-validation.sh, tests/cross-harness-validation.sh</files>
  <action>
    Add focused marker checks near existing workflow/resource capsule checks:
    - In `tests/pi-end-to-end-validation.sh`, add or extend a `tap_file_contains_all` check that validates the contract's v2.49 closure evidence markers: Pi 196/196, cross-harness 111/111, exact validation class names, schema/fallback/non-authority markers, and command-output count reconciliation language.
    - In `tests/cross-harness-validation.sh`, add or extend a `tap_file_contains_all` check that validates shared-invariant closure markers: `.paul/*` authority, STOP/DO NOT SKIP exclusion, checkpoint blocking, module evidence, GitHub Flow safety, parent-owned APPLY, command-output truth, validation classes, and full authoritative read fallback.
    - Prefer existing helpers and structural marker checks; avoid broad scans, brittle exact prose, new dependencies, or harness refactors.
    - Capture expected count changes during APPLY for UNIFY reconciliation.
  </action>
  <verify>bash tests/pi-end-to-end-validation.sh && bash tests/cross-harness-validation.sh</verify>
  <done>AC-2 and AC-3 satisfied: both validation suites pass and visibly protect v2.49 workflow/resource capsule closure markers.</done>
</task>

<task type="auto">
  <name>Task 3: Run final v2.49 closure evidence and scope checks</name>
  <files>docs/PI-NATIVE-WORKFLOW-RESOURCE-CAPSULE-CONTRACT.md, tests/pi-end-to-end-validation.sh, tests/cross-harness-validation.sh</files>
  <action>
    Produce APPLY evidence for UNIFY and milestone closure:
    - Run focused marker grep over the planned files for v2.49 closure markers: `193/193`, `109/109`, `196/196`, `111/111`, `PR #127`, `Workflow/Resource Capsule Context — Pi-Supported Runtime`, `Workflow/Resource Capsule Context — Shared Invariant`, `Capsule:`, `Source type`, `Freshness:`, `Bounds:`, `Fallback:`, `Derived aid only`, `full authoritative read`, `STOP/DO NOT SKIP`, `checkpoint blocking`, `module evidence`, `GitHub Flow safety`, `parent-owned APPLY`, and `command-output truth`.
    - Run `PALS_ROOT="$PWD" bash drivers/pi/install.sh` to verify install/source consistency where applicable.
    - Run `bash tests/pi-end-to-end-validation.sh` and record TAP totals.
    - Run `bash tests/cross-harness-validation.sh` and record TAP totals.
    - Run `git diff --stat` and confirm scope stayed inside approved contract/validation files plus normal `.paul/*` lifecycle artifacts.
    - Capture old/new validation counts and reason for any count increase in UNIFY.
  </action>
  <verify>grep -n "193/193\|109/109\|196/196\|111/111\|PR #127\|Workflow/Resource Capsule Context — Pi-Supported Runtime\|Workflow/Resource Capsule Context — Shared Invariant\|Capsule:\|Source type\|Freshness:\|Bounds:\|Fallback:\|Derived aid only\|full authoritative read\|STOP/DO NOT SKIP\|checkpoint blocking\|module evidence\|GitHub Flow safety\|parent-owned APPLY\|command-output truth" docs/PI-NATIVE-WORKFLOW-RESOURCE-CAPSULE-CONTRACT.md tests/pi-end-to-end-validation.sh tests/cross-harness-validation.sh && PALS_ROOT="$PWD" bash drivers/pi/install.sh && bash tests/pi-end-to-end-validation.sh && bash tests/cross-harness-validation.sh</verify>
  <done>AC-4 and AC-5 satisfied: closure evidence is command-backed and changed files stay inside approved evidence-reconciliation/milestone-closure scope.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- Do not modify `drivers/pi/extensions/pals-hooks.ts`; Phase 211 runtime behavior is complete.
- Do not modify README/Pi docs/skill-map unless a failing marker check proves Phase 212 surfacing drift that requires an approved re-plan.
- Do not modify installer behavior, installed runtime files outside the repo, dependency manifests/lockfiles, CI workflow configuration, helper agents, delegated APPLY packet/report behavior, broad validation classification, or lifecycle authority semantics.
- Do not change PLAN/APPLY/UNIFY workflow semantics, module dispatch semantics, GitHub Flow gates, checkpoint behavior, or guided workflow canonical reply behavior.
- Do not reopen legacy Claude Code / Agent SDK parity or install-surface cleanup.

## SCOPE LIMITS
- Source scope is evidence reconciliation and closure only: `docs/PI-NATIVE-WORKFLOW-RESOURCE-CAPSULE-CONTRACT.md`, `tests/pi-end-to-end-validation.sh`, and `tests/cross-harness-validation.sh`.
- Lifecycle metadata writes (`.paul/STATE.md`, `.paul/ROADMAP.md`, `.paul/PROJECT.md`, `.paul/MILESTONES.md`, history files, PLAN/SUMMARY) are normal APPLY/UNIFY lifecycle artifacts, not source scope expansion.
- Validation additions must be localized marker checks; no broad harness restructuring or large-file refactor in this phase.
- Any runtime change, contract redesign, dependency/CI change, helper compression, broad validation classification, or legacy cleanup requires a separate approved plan.
</boundaries>

<verification>
Before declaring APPLY complete:
- [ ] Focused contract/validation grep passes: `grep -n "193/193\|109/109\|196/196\|111/111\|PR #127\|Workflow/Resource Capsule Context — Pi-Supported Runtime\|Workflow/Resource Capsule Context — Shared Invariant\|Capsule:\|Source type\|Freshness:\|Bounds:\|Fallback:\|Derived aid only\|full authoritative read\|STOP/DO NOT SKIP\|checkpoint blocking\|module evidence\|GitHub Flow safety\|parent-owned APPLY\|command-output truth" docs/PI-NATIVE-WORKFLOW-RESOURCE-CAPSULE-CONTRACT.md tests/pi-end-to-end-validation.sh tests/cross-harness-validation.sh`
- [ ] `PALS_ROOT="$PWD" bash drivers/pi/install.sh`
- [ ] `bash tests/pi-end-to-end-validation.sh`
- [ ] `bash tests/cross-harness-validation.sh`
- [ ] `git diff --stat` shows scope stayed inside approved contract/validation files plus normal `.paul/*` lifecycle artifacts.
- [ ] Validation count changes, if any, are captured for UNIFY.
- [ ] GitHub Flow evidence captured: branch, base, ahead/behind, PR URL/state, CI/check state, merge-gate result, base sync, and branch cleanup during UNIFY.
- [ ] v2.49 milestone closure handoff names the next recommended Pi-native build milestone.
- [ ] All acceptance criteria met.
</verification>

<success_criteria>
- Workflow/resource capsule contract records final v2.49 closure baselines and PR #127 evidence.
- Pi validation protects v2.49 capsule closure markers and passes with command-output evidence.
- Cross-harness validation protects shared invariant closure markers and passes with command-output evidence.
- SUMMARY reconciles old/new counts, module reports, GitHub Flow evidence, decisions/deviations, and final milestone posture.
- v2.49 closes without runtime, dependency, CI, helper-compression, legacy, broad classification, or lifecycle-authority changes.
</success_criteria>

<output>
After completion, create `.paul/phases/213-evidence-reconciliation-milestone-closure/213-01-SUMMARY.md` with acceptance-criteria results, focused marker grep evidence, installer verification, Pi/cross-harness validation output and count reconciliation, module dispatch reports, GitHub Flow evidence, decisions/deviations, v2.49 milestone closure, and next milestone handoff.
</output>
