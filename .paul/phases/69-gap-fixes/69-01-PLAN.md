---
phase: 69-gap-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kernel/workflows/unify-phase.md
  - kernel/workflows/fix-loop.md
  - kernel/templates/SUMMARY.md
  - docs/HOOK-CONTRACT.md
  - docs/MODULE-SPEC.md
  - docs/ARTIFACT-SPEC.md
  - modules/walt/workflows/apply-phase-quality.md
  - modules/ruby/module.yaml
  - tests/cross-harness-validation.sh
  - tests/pi-end-to-end-validation.sh
autonomous: true
---

<objective>
## Goal
Close the confirmed Phase 68 execution-proof gaps by making module participation durably provable in end-of-loop artifacts and validation, with special focus on the current post-unify reporting hole.

## Purpose
Phase 68 proved the module system is more wired than visible. The next trust-building step is not richer Pi UX yet; it is making sure shared UNIFY/fix reporting and validation can prove which modules contributed, especially for post-unify-only modules whose outputs currently arrive too late to be dependably preserved in `SUMMARY.md`.

## Output
- Updated shared UNIFY/fix workflows that preserve module execution evidence through summary finalization
- Updated `SUMMARY.md` / hook-contract / module-spec guidance for durable module reporting
- Expanded validation that proves the post-unify evidence path exists across installed registries and shared workflow artifacts
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md
.paul/SPECIAL-FLOWS.md

## Prior Work (only if genuinely needed)
- `.paul/phases/68-execution-audit/68-01-SUMMARY.md` — reconciled the audit and recorded the execution-proof-first decision for Phase 69
- `.paul/phases/68-execution-audit/module-gap-report.md` — confirms the high-priority post-unify reporting gap and the validation-proof gap assigned to Phase 69
- `.paul/phases/68-execution-audit/module-execution-audit.md` — explains why current `SUMMARY.md` generation is only partial/conditional for module reporting
- `.paul/phases/11-kernel-extraction/11-02-SUMMARY.md` — original source of the generic `Module Execution Reports` contract that Phase 69 must harden rather than replace

## Source Files
- `kernel/workflows/apply-phase.md`
- `kernel/workflows/unify-phase.md`
- `kernel/workflows/fix-loop.md`
- `kernel/templates/SUMMARY.md`
- `docs/HOOK-CONTRACT.md`
- `docs/MODULE-SPEC.md`
- `docs/ARTIFACT-SPEC.md`
- `modules/walt/workflows/apply-phase-quality.md`
- `modules/ruby/module.yaml`
- `modules/walt/module.yaml`
- `modules/skip/module.yaml`
- `/Users/maxwellnewman/.pi/agent/skills/pals/modules.yaml`
- `tests/cross-harness-validation.sh`
- `tests/pi-end-to-end-validation.sh`

## Notes
- Planning posture for this run: `medium` collaboration fallback, `direct-requirements` mode — Phase 68 already narrowed the problem, so this plan should stay execution-focused.
- Treat installed `modules.yaml` alignment as already proven. Do **not** turn this into registry-generation churn unless validation coverage truly requires a minimal assertion update.
- Keep live Pi lifecycle visibility, status bar/widget updates, and guided UX surfaces out of scope for this phase; those belong to Phase 70.
- Preserve the kernel’s generic module model. The fix should strengthen durable reporting/evidence paths, not hardcode WALT/SKIP/RUBY-specific UX into shared workflows.
</context>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | For the PLAN → APPLY → UNIFY lifecycle work in this gap-fix implementation plan | ○ |

**BLOCKING:** Required skills MUST be loaded before APPLY proceeds.
Run each skill command or confirm already loaded.

## Skill Invocation Checklist
- [ ] /paul loaded (run command or confirm)
</skills>

<acceptance_criteria>
## AC-1: UNIFY and standard fix summaries preserve post-unify module evidence
```gherkin
Given a completed APPLY run or standard fix run with post-apply annotations and post-unify-capable modules available
When the shared reconciliation flow finishes generating its summary artifact
Then the resulting summary has a dependable `Module Execution Reports` path that can include post-unify module contributions and recorded side effects instead of leaving that evidence only in late hook narration
```

## AC-2: Shared template and contract docs describe one durable reporting model
```gherkin
Given the shared `SUMMARY.md` template, hook-contract/spec docs, and WALT post-unify overlay instructions
When a maintainer reads the execution-reporting contract after Phase 69
Then they see one consistent model for how post-apply annotations, post-unify module reports, and side effects persist into summary/history artifacts without relying on Pi-only visibility surfaces
```

## AC-3: Validation proves the module evidence path rather than only registry presence
```gherkin
Given a fresh Claude Code or Pi install generated from this repo
When the validation suites run
Then they fail if post-unify hook metadata is missing or if the shared workflow/template/contract files no longer provide the durable summary/reporting path needed to prove module execution contributions
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Rework shared UNIFY/fix ordering so module execution evidence persists before loop close</name>
  <files>kernel/workflows/unify-phase.md, kernel/workflows/fix-loop.md</files>
  <action>
    Rework the shared UNIFY data path so `SUMMARY.md` creation/finalization no longer strands post-unify-only module outputs after the summary has already been written.
    Make the workflow explicitly preserve three classes of data through summary finalization:
    - accumulated post-apply annotations
    - any pre-unify context that should feed reconciliation
    - post-unify module reports / recorded side effects that must be durably visible after the loop closes

    Mirror the same durable-summary behavior in the standard fix loop so `FIX-SUMMARY` artifacts do not bypass the post-unify evidence path.

    Preserve:
    - non-blocking `post-unify` semantics
    - generic module handling (no hardcoded module-specific sections in the kernel workflow logic)
    - the existing Phase 69 boundary that this is evidence/reporting work, not Phase 70 live UX work

    Avoid:
    - adding Pi widget/status/module activity UI
    - introducing a new hidden telemetry store or Pi-only state source
    - changing install/runtime registry resolution unless the shared proof path truly depends on it
  </action>
  <verify>grep -q 'Module Execution Reports' kernel/workflows/unify-phase.md && grep -q 'post-unify' kernel/workflows/unify-phase.md && grep -q 'summary' kernel/workflows/unify-phase.md && grep -q 'FIX-SUMMARY' kernel/workflows/fix-loop.md && grep -q 'post-unify' kernel/workflows/fix-loop.md</verify>
  <done>AC-1 satisfied: shared UNIFY/fix workflows now preserve a durable path for post-unify module evidence in summary artifacts</done>
</task>

<task type="auto">
  <name>Task 2: Align the shared summary/reporting contract and module overlay guidance</name>
  <files>kernel/templates/SUMMARY.md, docs/HOOK-CONTRACT.md, docs/MODULE-SPEC.md, docs/ARTIFACT-SPEC.md, modules/walt/workflows/apply-phase-quality.md, modules/ruby/module.yaml</files>
  <action>
    Update the shared reporting/template/contract surface so it matches the new durable execution-evidence path from Task 1.

    Specifically:
    - make the `Module Execution Reports` section describe dependable persisted evidence rather than conditional best-effort comments
    - clarify the post-unify return/persistence contract in `docs/HOOK-CONTRACT.md` and `docs/MODULE-SPEC.md` so module authors know what should persist to summaries versus what remains a side effect
    - update `docs/ARTIFACT-SPEC.md` so the optional summary section reflects the real end-of-loop evidence path
    - align WALT’s post-unify overlay instructions with the kernel’s actual finalization order and durable summary/history behavior
    - update any post-unify manifest wording that still describes outdated output semantics (for example, RUBY describing summary additions as generic annotations)

    Avoid:
    - broad README / discovery-surface rewrites reserved for Phase 70
    - reintroducing module-specific hardcoding into shared kernel templates
    - changing advisory-only modules into blockers
  </action>
  <verify>grep -q 'Module Execution Reports' kernel/templates/SUMMARY.md && grep -q 'post-unify' docs/HOOK-CONTRACT.md && grep -q 'post-unify' docs/MODULE-SPEC.md && grep -q 'Module Execution Reports' docs/ARTIFACT-SPEC.md && grep -q 'Quality History Recording' modules/walt/workflows/apply-phase-quality.md && grep -q 'post-unify' modules/ruby/module.yaml</verify>
  <done>AC-2 satisfied: the shared template, hook/spec docs, and module overlay instructions all describe one durable execution-reporting model</done>
</task>

<task type="auto">
  <name>Task 3: Extend validation to prove the durable module execution evidence path</name>
  <files>tests/cross-harness-validation.sh, tests/pi-end-to-end-validation.sh</files>
  <action>
    Strengthen the existing validation suites so they prove more than installed-registry presence.

    Add deterministic checks that fail when:
    - installed registries stop exposing the expected post-unify hook metadata
    - shared workflow/template/contract files regress away from the durable summary/reporting path added in Tasks 1-2
    - Pi-specific validation continues to claim module capability without checking the end-of-loop evidence path

    Keep the proof bounded and stable:
    - prefer source/install contract assertions over flaky live-agent execution attempts
    - reuse the existing temp-install pattern in the validation suites
    - make sure both cross-harness and Pi validation cover the Phase 69 evidence goal explicitly

    Finish by running both suites and resolving any failures caused by the new assertions.
  </action>
  <verify>bash tests/cross-harness-validation.sh && bash tests/pi-end-to-end-validation.sh</verify>
  <done>AC-3 satisfied: validation now proves the durable module evidence path and both suites pass</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- `drivers/pi/extensions/pals-hooks.ts`, `drivers/pi/extensions/README.md`, or other live Pi visibility/discovery surfaces in this plan
- install/uninstall scripts or driver manifests unless a tiny validation-alignment change becomes genuinely unavoidable
- module enablement/selection policy, module priorities, or the installed-registry authority model already validated in Phase 68
- unrelated tracked working tree changes outside the files listed in this plan

## SCOPE LIMITS
- This plan fixes durable execution proof and reporting only; it does **not** implement live module visibility UX (Phase 70)
- Do not redesign the whole hook system or invent a telemetry subsystem just to prove post-unify reporting
- Keep the change generic and shared across harnesses; no Pi-only truth, UI state, or adapter-only reporting path
- If broader recommendation questions emerge (how much telemetry, which modules to merge/remove, richer long-term proof models), note them for Phase 71 instead of solving them here
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `kernel/workflows/unify-phase.md` preserves a durable summary/reporting path for post-apply + post-unify module evidence
- [ ] `kernel/workflows/fix-loop.md` mirrors the durable post-unify reporting path for standard fixes
- [ ] `kernel/templates/SUMMARY.md`, `docs/HOOK-CONTRACT.md`, `docs/MODULE-SPEC.md`, and `docs/ARTIFACT-SPEC.md` describe the same execution-reporting contract
- [ ] `modules/walt/workflows/apply-phase-quality.md` aligns with the kernel’s final post-unify summary/history flow
- [ ] `bash tests/cross-harness-validation.sh` passes
- [ ] `bash tests/pi-end-to-end-validation.sh` passes
- [ ] All acceptance criteria are satisfied
</verification>

<success_criteria>
- Users can point to a durable summary/reporting path showing that post-unify-capable modules contributed, rather than inferring it from late hook narration alone
- Shared contract docs and template guidance no longer over-promise or contradict the actual UNIFY ordering/data path
- Validation now answers the Phase 68 trust question with stronger code-backed proof instead of only registry/UI wiring checks
- Phase 70 can focus on visibility UX on top of a proven evidence path rather than patching an unclear execution contract
</success_criteria>

<output>
After completion, create `.paul/phases/69-gap-fixes/69-01-SUMMARY.md`
</output>
