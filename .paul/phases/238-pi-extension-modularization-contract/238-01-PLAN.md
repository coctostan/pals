---
phase: 238-pi-extension-modularization-contract
plan: 01
type: execute
risk_tier: tiny
wave: 1
depends_on: ["237-01"]
files_modified:
  - docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md
autonomous: true
---

<objective>
## Goal
Establish a docs-only contract baseline for `drivers/pi/extensions/pals-hooks.ts` modularization that defines extraction boundaries, names candidate subsystems, classifies each candidate by risk and extraction readiness, and selects exactly one low-risk pure subsystem as the Phase 239 spike candidate — without changing the runtime, source, validation suites, install surface, or tests.

## Purpose
Phase 234 ranked `drivers/pi/extensions/pals-hooks.ts` (1,684 lines) as the Pi-supported runtime maintainability hotspot (weakness #2). Phase 235 requires that any new safety mechanism preserve `.paul/*` authority and add no hidden state, telemetry, or alternate ledger. Phase 236 preserved validation command-output truth and the safe sourced-helper extraction pattern. Phase 237 added artifact consistency guardrails and proved the durable contract-then-implementation cadence works end-to-end (the guardrail caught real drift mid-loop).

This plan continues that cadence: define the modularization contract before Phase 239 attempts an extraction. It follows the v2.5x baseline-first precedent (Phases 202, 206, 210, 214, 218, 222, 237) and stays docs-only so Phase 239 can do the bounded source spike inside an explicit contract.

## Output
- New `docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md` documenting:
  - Subsystem inventory derived from a read-only inspection of `drivers/pi/extensions/pals-hooks.ts`
  - Classification per candidate (risk, purity, ordering preference, extraction readiness)
  - Selected first-spike candidate for Phase 239 with explicit rationale and rejection reasons for the others
  - Authority/non-goals (no runtime/source/test/install/dependency/CI/lifecycle-authority changes; no hidden state; no telemetry; full authoritative read fallback preserved)
  - Phase 239 handoff inputs (allowed files, validation classes, count-reconciliation expectation, Workguard scope)
- No validation count changes: Pi `210/210` and cross-harness `124/124` remain stable; baselines in lifecycle artifacts are unchanged.
</objective>

<context>
## Project Context
- `.paul/PROJECT.md` — current v2.55 posture: Phases 234-237 complete; Phase 238 should establish the modularization contract before any extraction.
- `.paul/ROADMAP.md` — Phase 238 focus: define extraction boundaries for `drivers/pi/extensions/pals-hooks.ts` and select the first low-risk subsystem to extract.
- `.paul/STATE.md` — Phase 238 ready to plan; `.paul/*` artifacts are authoritative lifecycle truth.
- `.paul/MILESTONES.md` — current pointer correctly names v2.55 Phases 234-237 complete and Phase 238 ready to plan.
- `.paul/phases/237-artifact-consistency-guardrails/237-01-SUMMARY.md` — direct predecessor; preserved Pi 210/210 and cross-harness 124/124, established sourced-helper pattern, demonstrated contract-then-implementation cadence catching real drift.

## Source / Contract Inputs
- `docs/PALS-MAINTAINABILITY-PRODUCT-REALITY-AUDIT.md` — ranks Pi extension monolith as weakness #2; lists candidate boundaries: command routing, activation/slices, capsules, guided workflow, CARL/session boundary, module activity parsing.
- `docs/PALS-RISK-TIERED-LIFECYCLE-CONTRACT.md` — Phase 235 contract; this plan is **tiny / doc-only** tier (single new doc, no runtime/source/test changes); Phase 239 will be **risky** tier when it touches runtime source.
- `docs/PI-NATIVE-ARTIFACT-SLICE-CONTRACT.md` — existing artifact-slice surface; the contract should treat artifact slices as one already-cohesive subsystem candidate.
- `docs/PI-NATIVE-WORKFLOW-RESOURCE-CAPSULE-CONTRACT.md` — existing workflow/resource capsule surface; another subsystem candidate.
- `docs/PI-NATIVE-GUIDED-WORKFLOW-EVIDENCE-CONTRACT.md` — guided workflow detection/canonical-reply surface.
- `docs/PI-NATIVE-DELEGATED-APPLY-PACKET-REPORT-CONTRACT.md` — informs how runtime helpers must remain bounded and parent-owned; modularization must not weaken parent authority.

## Source Files (read-only inventory only)
- `drivers/pi/extensions/pals-hooks.ts` — current monolith (1,684 lines). Read for subsystem boundaries; **not modified by this plan**. The current top-level section comments are:
  - lines 1-15: file header
  - lines 16-66: `// -- Helpers --` (constants block, types, schema markers)
  - lines 67-1234: `// -- CARL Session Boundary Manager constants --` (the bulk: artifact slices, workflow capsules, guided workflow detection, module activity parsing, lifecycle UI, command routing helpers)
  - lines 1235-1329: `// -- CARL helpers and core functions --`
  - lines 1330-1406: `// -- Command definitions --`
  - lines 1407-1684: `// -- Extension entry point --`
- The current section comments understate the diversity inside lines 67-1234; the contract should derive a richer subsystem map from observed top-level functions/constants rather than from comment headers alone.
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: modules.yaml loaded from installed registry; planned files are exactly one new docs file; scope classified as **tiny / doc-only** because no runtime, source, validation, install, dependency, CI, or `.paul/*` authority is touched, and `pals-hooks.ts` is read-only context.
- ARCH: Detected pattern: markdown-first lifecycle repo with shell validation suites and one large Pi runtime extension (high confidence). Planned new doc lives in `docs/` and matches the v2.5x baseline-first precedent (Phases 202, 206, 210, 214, 218, 222, 237). No structural concerns.
- TODD: No conventional `*.test.*`/`*.spec.*` test infrastructure detected; this repo uses executable validation suites. Phase 238 has no executable surface; no `tdd_candidates`.
- IRIS: No TODO/FIXME/HACK/XXX patterns in planned existing files; only one new file is created.
- DOCS: New contract doc is required because Phase 239 needs durable boundary, candidate classification, and first-spike selection before any extraction. Minor doc cross-links may be added in later phases; this plan keeps cross-links inside the new contract only.
- DAVE: No CI configuration discovered or in scope.
- CODI: skipped — no extractable TS/JS symbols in phase scope (markdown-only deliverable; `pals-hooks.ts` is read-only context, not a source edit). `blast_radius=n`.
- RUBY: Preliminary debt flag retained: `drivers/pi/extensions/pals-hooks.ts` is the largest TS hotspot in the repo. This plan does not churn it; it inventories and classifies it for Phase 239.
- ARIA/LUKE: skipped — no UI files in planned changes.
- DANA/GABE/OMAR/PETE/REED/SETH/VERA: no data/API/observability/performance/resilience/security/privacy-sensitive planned files; hardcoded-secret scan found no matches in planned existing files.

[dispatch] pre-plan enforcement: DEAN PASS — no dependency manifests/lockfiles are planned for modification; no dependency audit blocker introduced by this plan.

## Post-Plan Module Dispatch
[dispatch] post-plan: TODD reviewed plan type; no TDD restructuring applied. Phase has no executable acceptance gate; existing official validation commands remain the count-of-record source and must stay green at Pi 210/210 and cross-harness 124/124.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | PLAN/APPLY/UNIFY lifecycle management | ✓ |
| /carl | required | Session boundary monitoring at phase boundaries | automatic |
| TODD | required | Validation/test enforcement during APPLY (no-op for docs-only scope; still dispatched) | via module dispatch |
| WALT | required | Quality gating and validation before completion | via module dispatch |

**BLOCKING:** Required skills/modules must remain active during APPLY. Parent APPLY owns verification and lifecycle writes.
</skills>

<acceptance_criteria>
## AC-1: Contract baseline preserves authority and adds no hidden state
```gherkin
Given Phase 238 targets only a docs-only modularization contract baseline
When `docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md` is created
Then it explicitly states that the runtime extension `drivers/pi/extensions/pals-hooks.ts` is unchanged by this plan, that `.paul/*` and command-output validation remain authoritative, that no telemetry/hidden state/alternate ledger is introduced, that full authoritative read fallback is preserved, and that the contract is a derived planning aid only.
```

## AC-2: Subsystem inventory is source-derived and bounded
```gherkin
Given `drivers/pi/extensions/pals-hooks.ts` is a 1,684-line monolith
When the contract names candidate subsystems
Then each candidate cites observed top-level functions and/or constants in `pals-hooks.ts`, lists 5 to 8 candidates total, and avoids inventing abstractions that do not appear in the source file.
```

## AC-3: Each candidate is classified for Phase 239 readiness
```gherkin
Given Phase 239 is a bounded extraction spike
When the contract classifies each candidate subsystem
Then it records, per candidate: purity (pure / mixed / runtime-coupled), risk tier hint (tiny / normal / risky), extraction-readiness rationale, and any explicit dependency on parent-owned APPLY, GitHub Flow, module evidence, lifecycle authority, or guided workflow canonical replies that constrains extraction order.
```

## AC-4: Exactly one first-spike candidate is selected with explicit rationale
```gherkin
Given Phase 239 should pick the lowest-risk pure subsystem
When the contract selects the Phase 239 spike candidate
Then it names exactly one selected candidate, gives a short reason for selection, and gives a short rejection reason for each non-selected candidate so Phase 239 can plan inside an unambiguous boundary.
```

## AC-5: Phase 239 handoff is explicit and bounded
```gherkin
Given Phase 239 must remain bounded
When the contract finalizes the Phase 239 handoff section
Then it lists allowed file paths for the spike (selected subsystem source location, single new module file location, and Pi extension-only validation guardrail expectations), declares expected validation count change (e.g., 0 or +1 / suite, with a docs-stated cap), and explicitly states that scope expansion requires a separately approved later plan.
```

## AC-6: Validation truth and count baselines are unchanged
```gherkin
Given both official validation suites currently pass Pi `210/210` and cross-harness `124/124`
When this plan's APPLY completes
Then Pi validation still reports `1..210` with `# Passed: 210` and cross-harness still reports `1..124` with `# Passed: 124`; no validation assertion is added, removed, or altered by this plan; lifecycle artifact baselines (`.paul/STATE.md`, `.paul/ROADMAP.md`, `.paul/MILESTONES.md`) match Phase 238 readiness without drift.
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Create extension modularization contract — header, evidence, authority/non-goals</name>
  <files>docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md</files>
  <action>
    Create `docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md` with the following structure (sections may use additional sub-headings as long as required `##` markers exist):

    - YAML/markdown header naming Phase 238 and Plan 238-01.
    - Evidence base section citing `docs/PALS-MAINTAINABILITY-PRODUCT-REALITY-AUDIT.md`, `docs/PALS-RISK-TIERED-LIFECYCLE-CONTRACT.md`, `docs/PI-NATIVE-ARTIFACT-SLICE-CONTRACT.md`, `docs/PI-NATIVE-WORKFLOW-RESOURCE-CAPSULE-CONTRACT.md`, `docs/PI-NATIVE-GUIDED-WORKFLOW-EVIDENCE-CONTRACT.md`, `docs/PI-NATIVE-DELEGATED-APPLY-PACKET-REPORT-CONTRACT.md`, `.paul/STATE.md`, `.paul/ROADMAP.md`, `.paul/phases/237-artifact-consistency-guardrails/237-01-SUMMARY.md`, and `drivers/pi/extensions/pals-hooks.ts`.
    - A section named `## Authority and Non-Goals` stating: this contract is a derived planning aid only; `drivers/pi/extensions/pals-hooks.ts` is unchanged by Phase 238; `.paul/*` and command-output validation remain authoritative; no hidden state; no telemetry; no alternate lifecycle/module/validation ledger; no auto-approval; no auto-continue; no skipped checkpoints; full authoritative read fallback preserved; runtime/install/CI/dependency/GitHub Flow semantics preserved.
    - A section named `## Tier-Aware Usage` recording: Phase 238 lands as **tiny / doc-only** tier; Phase 239 will land as **risky** tier when it touches runtime source.

    Avoid: claiming this document is authoritative over `.paul/*` or runtime; mentioning specific extraction implementation choices that pre-empt Phase 239 plan decisions; introducing schema beyond the explicit subsystem-inventory and Phase 239 handoff fields named in later tasks.
  </action>
  <verify>Focused marker check that the new file contains `Phase 238`, `Plan 238-01`, `## Authority and Non-Goals`, `## Tier-Aware Usage`, `no hidden state`, `no telemetry`, `Derived aid only`, `command-output truth`, `full authoritative read fallback`, and references to `drivers/pi/extensions/pals-hooks.ts`, `docs/PALS-MAINTAINABILITY-PRODUCT-REALITY-AUDIT.md`, and `docs/PALS-RISK-TIERED-LIFECYCLE-CONTRACT.md`.</verify>
  <done>AC-1 satisfied: the contract preserves authority, declares non-goals, and is recorded as tiny / doc-only tier.</done>
</task>

<task type="auto">
  <name>Task 2: Inventory candidate subsystems with source citations and classify each</name>
  <files>docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md</files>
  <action>
    Append two sections to `docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md`:

    1. `## Subsystem Inventory` — between 5 and 8 candidate subsystems derived from a read-only inspection of `drivers/pi/extensions/pals-hooks.ts`. Each candidate must:
       - Have a short identifier (e.g., `artifact-slice-rendering`, `workflow-resource-capsule-rendering`, `guided-workflow-detection`, `module-activity-parsing`, `carl-session-boundary`, `lifecycle-ui`, `command-routing`, `pals-context-injection`).
       - Cite at least two top-level identifiers actually present in the source (e.g., `buildArtifactSlice`, `renderArtifactSlices`, `getArtifactSliceSpecs`; `buildWorkflowResourceCapsule`, `renderWorkflowResourceCapsules`; `detectGuidedWorkflowMoment`, `parseGuidedWorkflowOptions`, `detectExplicitCanonicalResponse`, `sendCanonicalWorkflowResponse`; `parseModuleActivityEntries`, `extractDispatchModuleActivity`, `extractModuleReportActivity`, `extractRecentModuleActivity`; `loadCarlConfig`, `buildCarlBootstrapPrompt`, `extractLoopSignature`; `renderLifecycleStatus`, `renderLifecycleWidget`, `syncLifecycleUi`; `getCommand`, `getQuickActions`, `toWrapperCommand`, `detectCommandSignal`; `buildPalsContextPayload`, `shouldInjectPalsContext`, `keepOnlyLatestPalsContextMessage`).
       - Note whether it owns or merely consumes Pi-specific state.

    2. `## Classification` — for each candidate, record purity (`pure`, `mixed`, `runtime-coupled`), risk tier hint (`tiny`, `normal`, `risky`), extraction-readiness rationale (1-2 sentences), and any explicit dependency on parent-owned APPLY, GitHub Flow gates, module evidence, lifecycle authority, or guided workflow canonical replies that constrains extraction ordering.

    Avoid: inventing subsystems whose names do not appear (directly or as obvious umbrella over named functions) in `drivers/pi/extensions/pals-hooks.ts`; recommending implementation; weakening guided-workflow canonical-reply, parent-APPLY, lifecycle-authority, or merge-gate boundaries.
  </action>
  <verify>Focused marker check that the file contains `## Subsystem Inventory`, `## Classification`, between 5 and 8 candidate identifiers each cited at least twice in the inventory section, and at least one of `purity`, `risk tier`, `extraction-readiness` per candidate in the classification section.</verify>
  <done>AC-2 and AC-3 satisfied: source-derived inventory with bounded count and per-candidate classification.</done>
</task>

<task type="auto">
  <name>Task 3: Select Phase 239 first-spike candidate, record rejections, and write the Phase 239 handoff</name>
  <files>docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md</files>
  <action>
    Append two sections to `docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md`:

    1. `## Phase 239 First-Spike Selection` — name exactly one selected candidate (e.g., `artifact-slice-rendering` or another candidate the inventory shows is purer/lower risk), give a 2-3 sentence selection rationale, and give a 1-2 sentence rejection reason for each non-selected candidate. Selection criteria must include: minimal Pi-API coupling, minimal lifecycle/guided-workflow/merge-gate authority, ability to preserve current behavior with focused validation markers.

    2. `## Phase 239 Handoff` — record:
       - Allowed source file: `drivers/pi/extensions/pals-hooks.ts` (read + minimal edit to delegate to extracted module).
       - Allowed new file location pattern: `drivers/pi/extensions/<selected-subsystem-name>.ts` (or sibling under `drivers/pi/extensions/`).
       - Allowed validation guardrail expectation: at most one localized Pi validation guardrail addition (Pi `210→210` if pure refactor; up to Pi `210→211` if a localized marker is required) and at most one localized cross-harness guardrail addition (cross-harness `124→124` or `124→125`).
       - Explicit `## Out of Scope (Future Approved Plans Only)` for Phase 239: do not refactor non-selected subsystems; do not change `drivers/pi/install.sh`, package manifests, CI config, or `.paul/*` lifecycle authority; do not relax parent-owned APPLY, helper-delegation boundaries, lifecycle/module/validation/GitHub Flow authority, or full authoritative read fallback.

    Avoid: pre-deciding implementation details Phase 239 must own (concrete API shape, file naming, exact line ranges); allowing more than +1 / suite validation count drift; allowing scope expansion without a later approved plan.
  </action>
  <verify>Focused marker check that the file contains `## Phase 239 First-Spike Selection`, `## Phase 239 Handoff`, exactly one selected candidate name appearing in both sections, an `## Out of Scope (Future Approved Plans Only)` heading, mentions of `drivers/pi/extensions/`, and explicit validation count language at most `Pi 210→211` and `cross-harness 124→125`.</verify>
  <done>AC-4 and AC-5 satisfied: explicit Phase 239 first-spike selection plus a bounded handoff.</done>
</task>

<task type="auto">
  <name>Task 4: Run docs-only verification and confirm validation count stability</name>
  <files></files>
  <action>
    Final tiny / doc-only verification:
    - Run focused marker greps for all required `##` headings, authority/non-goal phrases, candidate identifiers, selected candidate, and Phase 239 handoff fields.
    - Run `git diff --check` on the working tree.
    - Run `bash tests/pi-end-to-end-validation.sh` and confirm `1..210`, `# Passed: 210`, `# Failed: 0`.
    - Run `bash tests/cross-harness-validation.sh` and confirm `1..124`, `# Passed: 124`, `# Failed: 0`.
    - Run `bash tests/helpers/artifact_consistency.sh` and confirm `artifact_consistency_check: PASS`.
    - Use Workguard report against the Phase 238 plan snapshot with allowed paths limited to `docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md`, `.paul/STATE.md`, `.paul/ROADMAP.md`, `.paul/MILESTONES.md`, `.paul/phases/238-pi-extension-modularization-contract/**`, `.paul/handoffs/archive/**`, `.paul/HANDOFF-*.md`, `.paul/CODI-HISTORY.md`, `.paul/QUALITY-HISTORY.md`.

    Avoid: editing any source file in `drivers/`, `kernel/`, `modules/`, `tests/`, or `.pi/`; adding any new validation assertion; changing any lifecycle artifact except for Phase 238 plan/SUMMARY/handoff/state metadata.
  </action>
  <verify>Pi validation reports `1..210` and `# Passed: 210`; cross-harness reports `1..124` and `# Passed: 124`; artifact consistency helper PASS; `git diff --check` clean; Workguard `outside_allowed=0` and `over_budget=false`.</verify>
  <done>AC-6 satisfied: validation truth, count baselines, and lifecycle artifact consistency are unchanged after Phase 238 APPLY.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- Do not change runtime behavior in `drivers/pi/extensions/`, `drivers/*`, `kernel/`, or `modules/`. `drivers/pi/extensions/pals-hooks.ts` is read-only context for this plan.
- Do not change install/uninstall scripts, dependency manifests, lockfiles, CI configuration, GitHub Flow behavior, helper-agent authority, module manifests, or PAUL workflow semantics.
- Do not modify validation suites or `tests/helpers/*` files; do not add, remove, rename, or alter any TAP assertion.
- Do not add telemetry, hidden runtime state, product analytics, caches, generated ledgers, or any non-`.paul/*` source of lifecycle truth.
- Do not weaken protected markers for validation truth, lifecycle authority, module blockers, GitHub Flow gates, parent-owned APPLY, delegated APPLY boundaries, full authoritative read fallback, or `.paul/*` authority.
- Do not pre-decide Phase 239 implementation details (concrete API surface for the extracted module, exact internal naming, exact line ranges to move) beyond the bounded handoff fields named in Task 3.

## SCOPE LIMITS
- This plan is limited to creating exactly one new contract document at `docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md` plus the standard lifecycle metadata writes for Phase 238.
- Subsystem candidate count is bounded between 5 and 8.
- Phase 239 first-spike selection must be exactly one candidate.
- Phase 239 validation count handoff is bounded at most Pi `210→211` and cross-harness `124→125`; any larger drift is out of scope and requires a separately approved plan.
- Validation count for Phase 238 itself remains exactly Pi `210/210` and cross-harness `124/124`; this plan does not add validation assertions.
</boundaries>

<verification>
Before declaring APPLY complete:
- [ ] `docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md` exists.
- [ ] All required `##` sections present: `## Authority and Non-Goals`, `## Tier-Aware Usage`, `## Subsystem Inventory`, `## Classification`, `## Phase 239 First-Spike Selection`, `## Phase 239 Handoff`, `## Out of Scope (Future Approved Plans Only)`.
- [ ] Required authority phrases present: `no hidden state`, `no telemetry`, `Derived aid only`, `command-output truth`, `full authoritative read fallback`.
- [ ] Subsystem inventory has between 5 and 8 candidates, each citing at least two top-level identifiers from `drivers/pi/extensions/pals-hooks.ts`.
- [ ] Classification table or list records purity, risk tier hint, extraction-readiness, and ordering constraints per candidate.
- [ ] Phase 239 First-Spike Selection names exactly one selected candidate; non-selected candidates each have a rejection reason.
- [ ] Phase 239 Handoff names allowed source file (`drivers/pi/extensions/pals-hooks.ts`), allowed new file location pattern under `drivers/pi/extensions/`, and bounded count handoff (at most Pi `210→211`, cross-harness `124→125`).
- [ ] `bash tests/pi-end-to-end-validation.sh` passes with `1..210`, `# Passed: 210`, `# Failed: 0`.
- [ ] `bash tests/cross-harness-validation.sh` passes with `1..124`, `# Passed: 124`, `# Failed: 0`.
- [ ] `bash tests/helpers/artifact_consistency.sh` passes (`artifact_consistency_check: PASS`).
- [ ] `git diff --check` passes.
- [ ] Workguard scope report confirms only approved files plus expected `.paul/*` lifecycle artifacts changed.
- [ ] All acceptance criteria met.
</verification>

<success_criteria>
- A durable, source-cited modularization contract exists and is referenced by Phase 239's plan inputs.
- Subsystem inventory is bounded (5-8) and grounded in actually-present top-level identifiers in `drivers/pi/extensions/pals-hooks.ts`.
- Exactly one Phase 239 first-spike candidate is selected, with rejection reasons for the rest.
- Phase 239 handoff is bounded: explicit allowed file scope, bounded validation count drift, explicit out-of-scope set.
- No runtime, source, validation, install, dependency, CI, or GitHub Flow behavior changes are introduced.
- Validation counts remain Pi `210/210` and cross-harness `124/124`; artifact consistency guardrail still passes.
</success_criteria>

<output>
After completion, create `.paul/phases/238-pi-extension-modularization-contract/238-01-SUMMARY.md` documenting AC results, focused marker evidence, validation command output, count reconciliation (no change), Workguard scope proof, module reports, deviations, decisions, and Phase 239 readiness.
</output>
