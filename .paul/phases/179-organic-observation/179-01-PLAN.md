---
phase: 179-organic-observation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .paul/phases/179-organic-observation/179-01-SUMMARY.md
  - .paul/ROADMAP.md
  - .paul/STATE.md
trial_host_repo: /Users/maxwellnewman/claude/quark
trial_host_artifacts:
  - /Users/maxwellnewman/claude/quark/.paul/CODI-HISTORY.md
  - /Users/maxwellnewman/claude/quark/.paul/STATE.md
  - /Users/maxwellnewman/claude/quark/.paul/ROADMAP.md
  - /Users/maxwellnewman/claude/quark/.paul/phases/
autonomous: false
---

<objective>
## Goal
Open the v2.41 natural observation window by routing the user's pre-authored quark patches through one or more normal quark PALS phases under CODI, while preserving the milestone's naturality and trial-integrity constraints.

## Purpose
Phase 178 prepared the instrument but did not generate evidence: quark now has `modules.codi`, a refreshed codegraph, and a seeded `quark/.paul/CODI-HISTORY.md` with zero rows. Phase 179 is the first phase that can answer the unresolved Q2 question from Phase 177: whether CODI's injected blast radius materially shapes real APPLY work when the work naturally touches indexed TypeScript in a repo that actually has structural fan-out. The observation must stay honest — no CODI-source edits, no synthetic rows, no controlled self-trials, and no scope tuning to flatter CODI.

## Output
1. One or more quark PALS phase plans/summaries created from the user's existing quark patches using engineering-logic-first decomposition.
2. `quark/.paul/CODI-HISTORY.md` accrues one row per natural quark UNIFY completed under CODI.
3. `.paul/phases/179-organic-observation/179-01-SUMMARY.md` records the observed row set, per-phase qualitative notes on whether APPLY actually consumed the injected blast radius, and any integrity/exclusion notes needed for Phase 180.
4. `.paul/ROADMAP.md` and `.paul/STATE.md` advance cleanly to Phase 180 once the observation window is complete.
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md
.paul/proposals/v2.41-cross-repo-trial-quark.md

## Prior Work (genuinely required)
.paul/phases/178-codi-cross-repo-install/178-01-SUMMARY.md — confirms quark is CODI-ready, codegraph is refreshed, and `quark/.paul/CODI-HISTORY.md` is seeded with zero rows.
/Users/maxwellnewman/claude/quark/.paul/CODI-HISTORY.md — must remain the authoritative row ledger during observation.
/Users/maxwellnewman/.pi/agent/skills/pals/modules.yaml — shared installed registry used by both pals and quark; CODI hook parity remains by construction.

## Source Files
/Users/maxwellnewman/claude/quark/.paul/CODI-HISTORY.md
/Users/maxwellnewman/claude/quark/.paul/STATE.md
/Users/maxwellnewman/claude/quark/.paul/ROADMAP.md
</context>

<module_dispatch>
## Pre-Plan Module Dispatch

Registry source: `~/.pi/agent/skills/pals/modules.yaml` (kernel_version `2.0.0`; CODI pre-plan priority `220`; CODI post-unify priority `220`).

**Trial-integrity precheck:** pals main is now at merged Phase 178 state, quark still uses the shared installed registry, and `quark/.paul/CODI-HISTORY.md` still contains only the canonical 7-line header with zero data rows. pals `modules/codi/*` remains frozen at SHA `0d667560b65944801faaee270ab920786afa471a` for the duration of v2.41.

**Planning posture used for this run:** `high` collaboration (project default) + `direct-requirements` mode (the milestone shape is already locked by the approved proposal; this plan simply opens the observation window cleanly on `main`).

**Advisory (non-blocking):**
- `[dispatch] ARCH (p75) → pass` — pals-side scope is bounded to observation artifacts and lifecycle tracking; quark-side execution must happen through normal quark phases, not ad hoc commands.
- `[dispatch] TODD (p100) → pass` — the quark phases created during APPLY must respect quark-local TODD requirements wherever the patch work has a real test-first surface.
- `[dispatch] IRIS (p150) → pass` — no anti-pattern signatures or TODO debt are being introduced in the pals-side observation scaffold.
- `[dispatch] DAVE (p200) → skip` — no CI/CD config changes are in scope for the pals-side observation plan.
- `[dispatch] DOCS (p200) → pass` — this PLAN plus the forthcoming SUMMARY are the only pals-side documentation artifacts required in this phase.
- `[dispatch] CODI (p220) → skipped — no extractable symbols in phase scope` — expected for the pals-side observation plan because its explicit source selectors are lifecycle artifacts, not repo-relative TS/JS files. The real CODI evidence must come from the natural quark phase plans created during APPLY.
- `[dispatch] RUBY (p250) → pass` — no in-repo code surface is being modified in pals during planning.
- `[dispatch] LUKE/ARIA/DANA/GABE/OMAR/PETE/REED/VERA → skip` — no applicable UI/data/API/observability/perf/resilience/privacy surfaces in this pals-side observation scaffold.

### Blast Radius (CODI)

_(no downstream call-sites — this phase opens the observation window only; CODI rows must be generated by natural quark phase plans, not by this pals plan artifact)_

**Enforcement (blocking-capable):**
- `[dispatch] DEAN (p50) → skip` — no dependency manifest changes are in scope.
- `[dispatch] SETH (p80) → pass` — no secrets/auth-sensitive edits are introduced by this plan.
- `[dispatch] ARCH circular-dep → pass` — no runtime dependency graph changes are proposed in pals.

**Accumulated context_inject:**
- `trial_integrity` → quark is ready; shared `modules.yaml` parity is intact; history ledger is still at zero rows.
- `naturality_guard` → quark phase decomposition must follow engineering logic first, before any CODI dispatch reads the scope.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | Entire PLAN → APPLY → UNIFY lifecycle for Phase 179 and each quark observation step | ✓ |
| /carl | required | Session-boundary handling across the observation window | ✓ |
| TODD | required | During quark implementation phases whenever the patch work has a test-first surface | ✓ |
| WALT | required | During quark APPLY/UNIFY validation and pals-side Phase 179 reconciliation | ✓ |

**BLOCKING:** Required lifecycle flows remain in force for APPLY. The quark work must run as normal quark PALS phases, not outside the loop.

## Skill Invocation Checklist
- [x] /paul loaded
- [x] /carl active via installed runtime/extension path
- [x] TODD available via installed module overlay for quark phases
- [x] WALT available via installed module overlay
</skills>

<acceptance_criteria>
## AC-1: Phase 179 opens as an observation-only window with the correct integrity constraints
```gherkin
Given Phase 178 already enabled CODI in quark, refreshed quark's codegraph, and seeded `quark/.paul/CODI-HISTORY.md` with zero rows
When this plan is approved and Phase 179 begins
Then the pals-side plan states that Phase 179 is observation-only
And no CODI-source edits in pals are allowed during the milestone
And no synthetic rows or controlled self-trials are allowed to count as live evidence
And the first valid row must come from a natural quark UNIFY under CODI
```

## AC-2: The user's existing quark patches are decomposed into one or more natural quark phases by engineering logic first
```gherkin
Given the user already has quark patches authored before CODI was enabled in quark
When APPLY inspects that patch set and creates one or more quark plans
Then the split is driven by genuine engineering boundaries and dependencies rather than trial convenience
And each quark plan drafts `<context>` and `files_modified` from engineering logic before CODI dispatch runs
And no quark plan is tuned to feed CODI nicer symbols or inflate row count
```

## AC-3: Natural quark UNIFY runs generate the authoritative Phase 179 CODI row set
```gherkin
Given quark's installed CODI hooks are active through the shared registry
When each planned quark phase completes its normal PLAN → APPLY → UNIFY loop
Then `quark/.paul/CODI-HISTORY.md` appends exactly one row per completed natural quark UNIFY
And each new row is traceable to a real quark phase artifact pair (`PLAN.md` / `SUMMARY.md`)
And Phase 179 records whether APPLY actually consumed the injected blast radius for that phase
```

## AC-4: pals closes Phase 179 with a summary that is ready input for Phase 180
```gherkin
Given the natural quark observation work is complete
When Phase 179 reconciles the observed row set
Then `.paul/phases/179-organic-observation/179-01-SUMMARY.md` records the row set, qualitative APPLY-consumption notes, and any integrity exclusions
And `.paul/STATE.md` and `.paul/ROADMAP.md` advance to Phase 180 as the next step
And Phase 180 can consume the Phase 179 summary without reconstructing the observation window from raw quark artifacts
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Inspect the existing quark patch set and create one or more natural quark phase plans</name>
  <files>/Users/maxwellnewman/claude/quark/.paul/STATE.md, /Users/maxwellnewman/claude/quark/.paul/ROADMAP.md, /Users/maxwellnewman/claude/quark/.paul/phases/*/*-PLAN.md</files>
  <action>
    Inspect the user's existing quark patch set as it exists today. Determine whether the work is best represented as one quark phase or multiple quark phases based only on genuine engineering seams: subsystem boundaries, dependency order, risk isolation, or reviewable vertical slices.

    For each resulting quark phase plan:
    - draft `<context>` and `files_modified` from the engineering work first
    - only after that, let CODI dispatch run over that naturally chosen scope
    - preserve the v2.41 integrity invariants: no CODI-source edits, no synthetic setup phases, no controlled self-trials, no row-count inflation by artificial splitting

    If the patch set is trivially one coherent slice, keep it as one quark phase. If it naturally decomposes into multiple phases, each split must be justified in the quark plan itself by real execution logic.

    Do NOT apply quark patches outside the quark PLAN → APPLY → UNIFY loop.
    Do NOT change quark scope wording to feed CODI friendlier identifiers.
  </action>
  <verify>At least one quark PLAN.md exists for the observation work, and its `<context>` / `files_modified` clearly map to real patch files and engineering seams rather than CODI convenience.</verify>
  <done>AC-2 satisfied: the quark work is decomposed naturally and ready to run under CODI.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Approve the proposed decomposition of the quark patch set before execution begins</decision>
  <context>The decomposition determines how many natural rows Phase 179 can legitimately produce. This is the highest-risk place for unconscious trial shaping, so the split should be explicit before APPLY runs the quark phases.</context>
  <options>
    <option id="option-a">
      <name>Approve the proposed quark phase split</name>
      <pros>Preserves momentum and lets APPLY proceed immediately under a documented natural decomposition.</pros>
      <cons>If the split was still too coarse or too fine, that problem carries into the evidence set.</cons>
    </option>
    <option id="option-b">
      <name>Adjust the quark phase split before execution</name>
      <pros>Lets the user correct an engineering-boundary mistake before the evidence window starts.</pros>
      <cons>Adds one more review step before the quark work begins.</cons>
    </option>
  </options>
  <resume-signal>Select: option-a or option-b</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Run the quark phases through normal PLAN → APPLY → UNIFY and reconcile Phase 179 in pals</name>
  <files>/Users/maxwellnewman/claude/quark/.paul/CODI-HISTORY.md, /Users/maxwellnewman/claude/quark/.paul/phases/*/*-SUMMARY.md, .paul/phases/179-organic-observation/179-01-SUMMARY.md, .paul/ROADMAP.md, .paul/STATE.md</files>
  <action>
    Execute each approved quark phase normally. For every completed quark PLAN → APPLY → UNIFY loop in the observation set:
    - preserve quark-local TODD/WALT expectations
    - let the installed CODI pre-plan and post-unify hooks run organically
    - confirm the resulting `quark/.paul/CODI-HISTORY.md` row is tied to a real quark phase artifact pair
    - capture the qualitative observation Phase 177 lacked: whether APPLY actually consumed the injected blast radius, and if so where it mattered (task action, boundary guard, or surprise-file avoidance)

    After all natural quark patch work has landed, write `.paul/phases/179-organic-observation/179-01-SUMMARY.md` in pals. Record:
    - the exact row set produced in quark during Phase 179
    - any exclusions or integrity caveats
    - a per-phase note on whether CODI materially shaped APPLY
    - the clean handoff into Phase 180's unchanged gating rubric

    Then update `.paul/ROADMAP.md` and `.paul/STATE.md` so Phase 179 is complete and Phase 180 becomes the next planned step.

    Do NOT backfill rows manually.
    Do NOT run synthetic quark UNIFY passes just to create data.
    Do NOT modify pals CODI source or quark work outside the user's existing patch set.
  </action>
  <verify>`quark/.paul/CODI-HISTORY.md` row growth matches the number of completed natural quark UNIFY phases in the observation set, and `.paul/phases/179-organic-observation/179-01-SUMMARY.md` records the row set plus qualitative APPLY-consumption notes.</verify>
  <done>AC-1, AC-3, and AC-4 satisfied: the natural observation window runs cleanly and hands Phase 180 a complete evidence package.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- `modules/codi/module.yaml` and `modules/codi/references/*` in pals — frozen for the duration of v2.41.
- `kernel/workflows/plan-phase.md` CODI coupling and the Phase 173/177 gating rubric — both remain unchanged during observation.
- quark scope wording or phase decomposition for the sake of making CODI look better.
- `quark/.paul/CODI-HISTORY.md` by hand — rows must come only from natural quark UNIFY hook execution.

## SCOPE LIMITS
- No controlled self-trials, no synthetic setup phases, and no retroactive CODI runs against older quark phases.
- No pals-side feature work during Phase 179; this phase is observation and reconciliation only.
- No quark changes outside the user's existing patch set and the normal quark lifecycle artifacts those phases create.
- No copying Phase 179 data into Phase 180 artifacts early; Phase 180 performs the formal re-trial after this phase closes.
</boundaries>

<verification>
Before declaring plan complete:
- [ ] At least one quark observation PLAN exists and its scope is justified by engineering logic first.
- [ ] Every completed quark UNIFY in the observation set appends exactly one authoritative row to `quark/.paul/CODI-HISTORY.md`.
- [ ] `.paul/phases/179-organic-observation/179-01-SUMMARY.md` records the observed row set and whether APPLY consumed the injected blast radius per phase.
- [ ] `.paul/ROADMAP.md` and `.paul/STATE.md` advance cleanly to Phase 180 with no trial-integrity violations outstanding.
</verification>

<success_criteria>
- Phase 179 runs entirely on natural quark work that predates CODI enablement.
- The quark row count matches the real engineering decomposition rather than an artificially chosen target.
- The observation window yields a clean, traceable evidence package for Phase 180.
- No CODI-source edits, synthetic rows, or controlled self-trials contaminate the dataset.
</success_criteria>

<output>
After completion, create `.paul/phases/179-organic-observation/179-01-SUMMARY.md`.
</output>
