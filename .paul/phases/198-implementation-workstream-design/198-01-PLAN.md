---
phase: 198-implementation-workstream-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/PI-NATIVE-IMPLEMENTATION-WORKSTREAMS.md
autonomous: true
---

<objective>
## Goal

Create a source-cited implementation workstream design artifact for v2.46 that converts the Phase 197 findings and v2.45 architecture proposal into concrete candidate workstreams, with outputs, repo surfaces, validation expectations, scope boundaries, and readiness status.

## Purpose

Phase 198 is still planning, not implementation hardening. Its job is to make later sequencing and executable implementation-roadmap work possible without prematurely editing Pi runtime, workflow, validation, dependency, GitHub Flow, or legacy-support surfaces.

## Output

Create `docs/PI-NATIVE-IMPLEMENTATION-WORKSTREAMS.md` with:
- source inputs and planning-only status,
- workstream inventory and readiness classification,
- per-workstream expected outputs, likely repo surfaces, validation/evidence needs, authority boundaries, dependencies, and open questions,
- Phase 199 handoff notes for sequencing and dependency planning.
</objective>

<context>
## Project Context

- `.paul/PROJECT.md` — current project posture: Pi-supported runtime, artifact-first lifecycle truth, parent-owned APPLY, GitHub Flow safety, module evidence, and v2.46 implementation-planning focus.
- `.paul/STATE.md` — current lifecycle position: Phase 198 ready to plan after Phase 197 completion.
- `.paul/ROADMAP.md` — current v2.46 milestone route and Phase 198 scope.
- `pals.json` — planning default, GitHub Flow mode, guided workflow and helper-agent settings.

## Direct Source Inputs

- `docs/PI-NATIVE-IMPLEMENTATION-FINDINGS.md` — primary Phase 197 findings triage and Phase 198 handoff source.
- `docs/PI-NATIVE-ARCHITECTURE-PROPOSAL.md` — final v2.45 proposal with promoted patterns, migration roadmap, implementation roadmap, validation strategy, and non-goals.
- `docs/PI-RUNTIME-CAPABILITY-MAP.md` — Pi runtime primitives and lifecycle mappings for command routing, installed resources, context injection, guided UX, module visibility, CARL, helper-agent delegation, and GitHub Flow surfacing.
- `docs/PI-NATIVE-CONTEXT-EFFICIENCY-ARCHITECTURE.md` — context-cost model, assistance components, read/citation contract, spike backlog, recommended sequence, and validation redesign requirements.
- `.paul/phases/197-v245-findings-triage/197-01-SUMMARY.md` — immediate prior phase outcome and key patterns.

## Planned Source Files

- `docs/PI-NATIVE-IMPLEMENTATION-WORKSTREAMS.md` — new Phase 198 planning artifact to be created during APPLY.
</context>

<module_dispatch>
## Pre-Plan Module Dispatch

[dispatch] pre-plan advisory: modules.yaml loaded from installed PALS registry (kernel_version 2.0.0).

| Module | Result |
|---|---|
| TODD | No repo-local unit test framework covers this new planning document; use marker greps plus existing Pi and cross-harness validation scripts. Plan remains `execute`, not TDD. |
| IRIS | Planned file does not yet exist; no TODO/FIXME/HACK/XXX findings in planned file scope. |
| DAVE | No CI config detected in repository; preserve warning, but do not broaden this planning-doc phase into CI generation. |
| DOCS | Documentation catalog includes v2.45/v2.46 source docs; planned output is a docs artifact and should cite nearby source docs. |
| CODI | Skipped — no extractable TS/JS symbols or source selectors in this markdown-only phase scope. |
| RUBY | Planned artifact is documentation; keep it bounded, tabular, and structured to avoid creating large unmaintainable planning prose. |
| ARIA / LUKE / GABE / DANA / OMAR / PETE / REED / VERA | No UI, API, data, observability, performance, resilience, or privacy-sensitive implementation files are in planned scope. |
| ARCH | Repository is organized around `docs/`, `drivers/`, `kernel/`, `modules/`, and `tests/`; planned docs file is in the expected documentation layer. No architectural concerns in planned files. |

[dispatch] pre-plan enforcement: DEAN checked package-manager manifests; no root dependency manifest or lockfile found, so no audit command applies and no dependency block is raised. SETH found no planned security-sensitive source files; no security block is raised.

## Post-Plan Module Dispatch

[dispatch] post-plan: TODD reviewed plan type. No existing tests cover this docs-only planned artifact; plan remains `execute` rather than TDD.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | PLAN/APPLY/UNIFY lifecycle management | ✓ |
| /carl | required | Automatic session-boundary monitoring | automatic |
| TODD | required | APPLY test/validation discipline | module dispatch |
| WALT | required | APPLY/UNIFY quality validation | module dispatch |

Required lifecycle skills are active through the current `/skill:paul-plan` workflow and installed module dispatch.
</skills>

<acceptance_criteria>
## AC-1: Candidate implementation workstreams are source-cited and classified
```gherkin
Given Phase 197 produced classified v2.45 findings and workstream seeds
When `docs/PI-NATIVE-IMPLEMENTATION-WORKSTREAMS.md` is created
Then it cites the relevant source paths and classifies candidate workstreams by purpose, readiness, implementation target, and planning status
```

## AC-2: Each workstream has executable planning detail without authorizing implementation
```gherkin
Given later phases need workstream outputs, repo surfaces, validation expectations, and guardrails
When the workstream artifact describes each candidate workstream
Then each workstream includes expected outputs, likely repo surfaces, validation/evidence needs, preserved authority boundaries, dependencies, and open questions while remaining planning-only
```

## AC-3: Phase 199 sequencing handoff is actionable
```gherkin
Given Phase 199 owns sequencing and dependency planning
When Phase 198 completes
Then the workstream artifact provides dependency notes, sequencing inputs, split/merge cautions, and unresolved decisions for Phase 199 without finalizing the implementation roadmap
```

## AC-4: Validation remains green
```gherkin
Given this plan creates documentation and should not change runtime behavior
When APPLY completes
Then marker checks pass and the existing Pi plus cross-harness validation suites remain green
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Create the implementation workstream design source map</name>
  <files>docs/PI-NATIVE-IMPLEMENTATION-WORKSTREAMS.md</files>
  <action>
    Create `docs/PI-NATIVE-IMPLEMENTATION-WORKSTREAMS.md` with a concise title, status table, purpose, planning-only boundary, and source-input table.
    Include at minimum these source paths: `docs/PI-NATIVE-IMPLEMENTATION-FINDINGS.md`, `docs/PI-NATIVE-ARCHITECTURE-PROPOSAL.md`, `docs/PI-RUNTIME-CAPABILITY-MAP.md`, `docs/PI-NATIVE-CONTEXT-EFFICIENCY-ARCHITECTURE.md`, and `.paul/phases/197-v245-findings-triage/197-01-SUMMARY.md`.
    State explicitly that the artifact defines workstream planning inputs only and does not approve runtime, workflow, validation-script, dependency, GitHub Flow, or legacy-surface changes.
    Keep source claims path-cited; do not copy large sections verbatim from source docs.
  </action>
  <verify>grep -n "Source Inputs\|Planning-Only Boundary\|PI-NATIVE-IMPLEMENTATION-FINDINGS\|PI-NATIVE-ARCHITECTURE-PROPOSAL\|PI-RUNTIME-CAPABILITY-MAP\|PI-NATIVE-CONTEXT-EFFICIENCY-ARCHITECTURE" docs/PI-NATIVE-IMPLEMENTATION-WORKSTREAMS.md</verify>
  <done>AC-1 satisfied: the workstream design artifact exists, cites source inputs, and preserves the planning-only boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Define candidate workstreams with outputs, surfaces, validation, and boundaries</name>
  <files>docs/PI-NATIVE-IMPLEMENTATION-WORKSTREAMS.md</files>
  <action>
    Add a candidate workstream inventory and per-workstream detail sections for:
    - artifact-slice contract hardening,
    - workflow/resource capsule design,
    - guided workflow evidence hardening,
    - delegated APPLY packet/report compression,
    - Pi-native validation classification,
    - legacy retention and install-surface cleanup.
    For each workstream, include: purpose, readiness classification (`implementation-ready`, `needs design`, `depends on validation classification`, or `defer`), expected outputs, likely repo surfaces, validation/evidence requirements, authority boundaries to preserve, dependencies, and open questions.
    Treat artifact-slice contract hardening as the most implementation-ready candidate because Phase 195 already validated the pattern, but do not finalize Phase 199 sequencing.
    Preserve `.paul/*` authority, PLAN/APPLY/UNIFY ownership, module evidence, GitHub Flow gates, parent-owned APPLY, citation/freshness/full-read fallback, and no hidden Pi state/telemetry/auto-approval boundaries across every workstream.
  </action>
  <verify>grep -n "Artifact-Slice Contract Hardening\|Workflow/Resource Capsule Design\|Guided Workflow Evidence Hardening\|Delegated APPLY Packet/Report Compression\|Pi-Native Validation Classification\|Legacy Retention" docs/PI-NATIVE-IMPLEMENTATION-WORKSTREAMS.md && grep -n "Likely Repo Surfaces\|Validation / Evidence\|Authority Boundaries\|Open Questions" docs/PI-NATIVE-IMPLEMENTATION-WORKSTREAMS.md</verify>
  <done>AC-1 and AC-2 satisfied: candidate workstreams are classified and contain executable planning detail without approving implementation.</done>
</task>

<task type="auto">
  <name>Task 3: Add Phase 199 sequencing handoff and validate</name>
  <files>docs/PI-NATIVE-IMPLEMENTATION-WORKSTREAMS.md</files>
  <action>
    Add a Phase 199 handoff section that identifies sequencing inputs, dependency edges, split/merge cautions, risk gates, and unresolved decisions.
    Include notes about likely sequence pressures from v2.45: artifact slices before capsules, guided UI evidence near current Pi behavior, helper-agent packet compression after parent-review/report schema clarity, validation classification before weakening frozen parity, and legacy cleanup last.
    Add a short readiness checklist for when a workstream can move from design to implementation-plan proposal.
    Run marker checks plus existing Pi and cross-harness validation scripts.
  </action>
  <verify>grep -n "Phase 199 Handoff\|Sequencing Inputs\|Dependency Edges\|Split / Merge Cautions\|Risk Gates\|Readiness Checklist" docs/PI-NATIVE-IMPLEMENTATION-WORKSTREAMS.md && bash tests/pi-end-to-end-validation.sh && bash tests/cross-harness-validation.sh</verify>
  <done>AC-3 and AC-4 satisfied: Phase 199 has actionable handoff input and validation remains green.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE

- Do not edit `drivers/pi/extensions/pals-hooks.ts`, `drivers/pi/extensions/README.md`, `drivers/pi/skill-map.md`, or Pi installer behavior.
- Do not edit `tests/pi-end-to-end-validation.sh` or `tests/cross-harness-validation.sh`.
- Do not edit shared workflow semantics, module manifests, installed runtime copies, dependency manifests, GitHub Flow behavior, Claude Code wrappers, Agent SDK surfaces, or portability docs.
- Do not modify `.paul/STATE.md`, `.paul/ROADMAP.md`, `.paul/QUALITY-HISTORY.md`, or `.paul/CODI-HISTORY.md` during APPLY tasks except through normal UNIFY/transition workflow steps after implementation is complete.

## SCOPE LIMITS

- This plan creates a workstream-design documentation artifact only.
- Do not implement artifact-slice hardening, workflow capsules, guided UI changes, helper-agent packet changes, validation classification, or legacy cleanup.
- Do not add dependencies, telemetry, hidden Pi memory, persistent runtime caches, auto-approval, or a lifecycle ledger.
- Do not decide final implementation sequencing; Phase 199 owns sequencing and dependency planning.
- Do not treat runtime slices, future capsules, helper reports, or Pi UI as substitutes for full authoritative reads when facts are stale, ambiguous, contested, or lifecycle-changing.
</boundaries>

<verification>
Before declaring APPLY complete:
- [ ] `grep -n "Source Inputs\|Planning-Only Boundary\|Candidate Workstream Inventory\|Phase 199 Handoff\|Readiness Checklist" docs/PI-NATIVE-IMPLEMENTATION-WORKSTREAMS.md`
- [ ] `grep -n "Artifact-Slice Contract Hardening\|Workflow/Resource Capsule Design\|Guided Workflow Evidence Hardening\|Delegated APPLY Packet/Report Compression\|Pi-Native Validation Classification\|Legacy Retention" docs/PI-NATIVE-IMPLEMENTATION-WORKSTREAMS.md`
- [ ] `bash tests/pi-end-to-end-validation.sh`
- [ ] `bash tests/cross-harness-validation.sh`
- [ ] All acceptance criteria are satisfied.
</verification>

<success_criteria>
- `docs/PI-NATIVE-IMPLEMENTATION-WORKSTREAMS.md` exists and is concise, source-cited, and clearly planning-only.
- Candidate workstreams are classified by readiness and describe outputs, repo surfaces, validation/evidence needs, authority boundaries, dependencies, and open questions.
- Phase 199 has actionable sequencing and dependency-planning inputs without final sequencing decisions being made in Phase 198.
- No runtime, workflow, validation-script, dependency, legacy-surface, or lifecycle-authority behavior changes are made.
- Existing validation remains green.
</success_criteria>

<output>
After APPLY completion, create `.paul/phases/198-implementation-workstream-design/198-01-SUMMARY.md` documenting changed files, acceptance-criteria results, validation output, module reports, deviations, decisions, and follow-on recommendations for Phase 199.
</output>
