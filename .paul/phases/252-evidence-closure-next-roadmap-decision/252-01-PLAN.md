---
phase: 252-evidence-closure-next-roadmap-decision
plan: 01
type: execute
wave: 1
depends_on:
  - phase: 249-s3-extraction-contract-bounded-submodule-plan
    provides: S3 extraction contract and Phase 250 handoff; Pi 223/223; cross-harness 131/131; PR #164.
  - phase: 250-bounded-s3-submodule-extraction
    provides: `drivers/pi/extensions/guided-workflow-detection.ts` sibling extraction; S4 delivery retained in `pals-hooks.ts`; Pi 224/224; cross-harness 131/131; PR #165.
  - phase: 251-documentation-validation-surfacing
    provides: README/Pi docs/skill-map + contract surfacing; localized validation guardrails; Pi 227/227; cross-harness 134/134; PR #166.
files_modified:
  - .paul/MILESTONES.md
  - .paul/PROJECT.md
  - .paul/ROADMAP.md
  - tests/cross-harness-validation.sh
autonomous: true
risk_tier: normal
collaboration_level: high
planning_mode: direct-requirements
review_preference: tbd
---

# Phase 252 Plan 01: Evidence Closure + Next Roadmap Decision (v2.58)

<objective>
## Goal
Close v2.58 Pi Extension Submodule Extraction Wave — S3 Guided Workflow Detection by aggregating Phases 249–251 evidence, marking v2.58 complete in lifecycle artifacts, recording a next-roadmap recommendation for v2.59, and adding one localized shared-invariant closure guardrail.

## Purpose
v2.58 extracted and surfaced the S3 `guided-workflow-detection` sibling while preserving S4 canonical reply delivery in `pals-hooks.ts`, guided workflow authority boundaries, no inferred merge intent, and command-output validation truth. Phase 252 makes the milestone closure durable without reopening runtime TypeScript, installer behavior, dependency/CI surfaces, validation-suite structure, or lifecycle authority. The v2.59 recommendation is recorded for `/paul:milestone`; it does not approve or start the next milestone.

## Output
- `.paul/MILESTONES.md` records v2.58 complete with Phases 249–252, PR #164/#165/#166 plus the Phase 252 PR placeholder/actual PR, validation count reconciliation, and the next-roadmap recommendation.
- `.paul/PROJECT.md` marks v2.58 complete and records a v2.59 recommendation as pending `/paul:milestone` approval.
- `.paul/ROADMAP.md` marks Phase 252/v2.58 complete and moves v2.58 into completed milestone summary while preserving the next-phase route.
- `tests/cross-harness-validation.sh` gains exactly one localized closure guardrail for v2.58 shared milestone evidence, moving cross-harness validation from 134 to 135.
- UNIFY creates `.paul/phases/252-evidence-closure-next-roadmap-decision/252-01-SUMMARY.md` with validation, scope, module, count, GitHub Flow, and next-roadmap evidence.
</objective>

<context>
## Project Context
- `.paul/STATE.md` — Phase 252 ready to plan; v2.58 3 of 4 phases complete; post-Phase-251 baseline: Pi 227/227, cross-harness 134/134, artifact consistency PASS, `git diff --check` clean.
- `.paul/PROJECT.md` — current focus says Phase 252 is ready to plan; active milestone table lists v2.58 as Active (3 of 4 phases).
- `.paul/ROADMAP.md` — current milestone table lists Phase 252 as Ready to plan; Phases 249–251 are complete.
- `.paul/MILESTONES.md` — live status points to v2.58 active with Phase 252 ready to plan.

## Prior Work
- `.paul/phases/249-s3-extraction-contract-bounded-submodule-plan/249-01-SUMMARY.md` — S3 contract evidence: guided-workflow detection contract created, S4/S6/S7/S8 deferrals preserved, Pi 223/223, cross-harness 131/131, PR #164.
- `.paul/phases/250-bounded-s3-submodule-extraction/250-01-SUMMARY.md` — runtime extraction evidence: `guided-workflow-detection.ts` created, `pals-hooks.ts` delegates S3 detection while retaining S4 delivery, install surface 5 files, Pi 224/224, cross-harness 131/131, PR #165.
- `.paul/phases/251-documentation-validation-surfacing/251-01-SUMMARY.md` — surfacing evidence: README/Pi docs/skill-map and contract docs updated, Pi 227/227, cross-harness 134/134, PR #166.

## Source Files
- `.paul/MILESTONES.md`
- `.paul/PROJECT.md`
- `.paul/ROADMAP.md`
- `tests/cross-harness-validation.sh`

## CODI Seed Candidates
No TS/JS source selectors are planned. The terms `guided-workflow-detection`, `pals-context-injection`, `pals-hooks.ts`, and S1–S8 are milestone evidence/recommendation labels in lifecycle artifacts, not planned code changes. CODI may skip cleanly.
</context>

<module_dispatch>
## Pre-Plan Module Dispatch

Planning collaboration level: high (project default from `pals.json`). Planning mode: direct-requirements from STATE/ROADMAP.

- `[dispatch] pre-plan advisory: modules.yaml loaded` from installed PALS registry, kernel version `2.0.0`; `pals.json` schema version matches.
- `[dispatch] pre-plan advisory: TODD` found no repo-local `*.test.*`, `*.spec.*`, or `__tests__` tree. Existing shell validation suites remain the proof path; plan type stays `execute`, not `tdd`.
- `[dispatch] pre-plan advisory: IRIS` scanned planned files for `TODO|FIXME|HACK|XXX`; no findings.
- `[dispatch] pre-plan advisory: DAVE` found no in-scope CI config files; no CI file is modified by this plan. GitHub Flow PR checks remain merge-gate evidence during UNIFY.
- `[dispatch] pre-plan advisory: DOCS` found repo docs under `docs/`; this plan updates lifecycle closure artifacts and one shared validation guardrail, not product/runtime docs.
- `[dispatch] pre-plan advisory: CODI skipped — no extractable symbols in phase scope`; planned files are Markdown lifecycle artifacts plus one shell validation guardrail, not TS/JS source changes.
- `[dispatch] pre-plan advisory: RUBY` noted `tests/cross-harness-validation.sh` is an existing large validation script. Size debt is pre-existing/future validation-architecture scope; this plan must add one localized block only.
- `[dispatch] pre-plan advisory: ARCH` detected the expected flat docs/drivers/kernel/modules/tests lifecycle repo structure. Planned files stay in expected lifecycle/test layers; no circular dependency risk.
- `[dispatch] pre-plan advisory: ARIA/LUKE/DANA/GABE/OMAR/PETE/REED/VERA` skipped — no UI, design-token, data, API, observability, performance, resilience, or privacy implementation surface in planned files.
- `[dispatch] pre-plan enforcement: DEAN PASS/SKIP` no dependency manifest/lockfile is in planned scope; no dependency change is introduced. The frozen legacy `drivers/agent-sdk/requirements.txt` is outside scope.
- `[dispatch] pre-plan enforcement: SETH PASS` no hardcoded-secret patterns were found in planned lifecycle artifacts; no auth/input-validation surface is modified.

## Post-Plan Module Dispatch

- `[dispatch] post-plan: TODD` execute plan is appropriate; no RED/GREEN/REFACTOR restructuring suggested. Shell validation and focused marker checks are the proof path.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | All PLAN/APPLY/UNIFY lifecycle work | ✓ |
| /carl | required | Session-boundary monitoring via Pi extension | active |
| TODD | required | Test/validation enforcement during APPLY | registry-loaded |
| WALT | required | Quality gating and validation during APPLY/UNIFY | registry-loaded |

**BLOCKING:** Required skills/modules must remain active before APPLY proceeds.
</skills>

<acceptance_criteria>
## AC-1: v2.58 closure is durable in MILESTONES.md
```gherkin
Given v2.58 has completed Phase 249 contract, Phase 250 runtime extraction, and Phase 251 documentation/validation surfacing
When `.paul/MILESTONES.md` is read after APPLY
Then the completed milestones table contains a v2.58 row naming Phases 249, 250, 251, and 252; the live status pointer says no active milestone and v2.59 selection is pending via `/paul:milestone`; the closure pointer records `guided-workflow-detection.ts`, preserved S4 `pals-hooks.ts` delivery, validation reconciliation Pi 223→227 (+4), cross-harness 131→135 (+4 after this closure guardrail), install 4→5 (+1), and the footer trail includes `v2.58 milestone closure`.
```

## AC-2: PROJECT.md and ROADMAP.md reflect v2.58 complete and preserve next-roadmap recommendation
```gherkin
Given Phase 252 is the final v2.58 phase
When `.paul/PROJECT.md` and `.paul/ROADMAP.md` are read after APPLY
Then PROJECT marks v2.58 complete (4 of 4 phases, closed via the Phase 252 PR placeholder/actual PR), records a decision row recommending v2.59 default focus S7 `pals-context-injection` as a bounded contract/baseline candidate now that guided-workflow evidence semantics are stable, and preserves alternatives; ROADMAP marks Phase 252 complete, marks the current milestone complete, and adds/updates a completed milestone summary for v2.58 without deleting prior completed-milestone history.
```

## AC-3: Cross-harness closure guardrail is localized and count-reconciled
```gherkin
Given cross-harness validation currently passes 134/134 after Phase 251
When Phase 252 adds closure evidence protection
Then `tests/cross-harness-validation.sh` gains exactly one localized `tap_file_contains_all` block titled `Phase 252 sibling extraction wave closure (v2.58 Shared Invariant)` that asserts `.paul/MILESTONES.md` contains `v2.58 Pi Extension Submodule Extraction Wave`, `Phase 249`, `Phase 250`, `Phase 251`, `Phase 252`, `guided-workflow-detection.ts`, `pals-hooks.ts`, and `v2.59`; the suite plan total moves from `1..134` to `1..135`; the suite passes 135/135 with zero failures.
```

## AC-4: Verification is green and scope remains bounded
```gherkin
Given APPLY has completed the planned closure edits
When final verification runs
Then `PALS_ROOT="$PWD" bash drivers/pi/install.sh` reports 5 Pi extension files, Pi validation remains 227/227, cross-harness validation passes 135/135, artifact_consistency PASSes, `git diff --check` is clean, and changed non-lifecycle files are limited to `tests/cross-harness-validation.sh`.
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Aggregate v2.58 closure evidence in MILESTONES.md</name>
  <files>.paul/MILESTONES.md</files>
  <action>
    Update `.paul/MILESTONES.md` in bounded closure locations only:
    - Add a completed-milestones row after the v2.57 row for `v2.58 Pi Extension Submodule Extraction Wave — S3 Guided Workflow Detection`, completed 2026-05-03, stats `4 phases, 4 plans`, detail naming Phase 249 contract, Phase 250 S3 extraction, Phase 251 surfacing, Phase 252 closure, SUMMARY paths, PRs #164/#165/#166 and the Phase 252 PR placeholder/actual PR.
    - Update the Live Status `Current milestone` row to state no active milestone after v2.58 closure and v2.59 selection pending via `/paul:milestone`, with recorded default recommendation `S7 pals-context-injection contract/baseline` contingent on explicit approval.
    - Add a v2.58 closure paragraph in Current / Future Milestone Pointer: Phase 249 contract, Phase 250 extraction, Phase 251 surfacing, Phase 252 closure; validation reconciliation Pi 223→227 (+4), cross-harness 131→135 (+4 after this closure guardrail), install 4→5 (+1); and v2.59 recommendation with alternatives.
    - Append `updated for v2.58 milestone closure: 2026-05-03` to the footer trail.
    Preserve existing v2.56/v2.57 rows and archive pointers verbatim.
  </action>
  <verify>Focused checks find `v2.58 Pi Extension Submodule Extraction Wave`, `Phase 249`, `Phase 250`, `Phase 251`, `Phase 252`, `guided-workflow-detection.ts`, `pals-hooks.ts`, `v2.58 milestone closure`, and `v2.59` in `.paul/MILESTONES.md`; existing `v2.57 Pi Extension Submodule Extraction Wave` count is preserved.</verify>
  <done>AC-1 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Close v2.58 in PROJECT.md and ROADMAP.md with next-roadmap recommendation</name>
  <files>.paul/PROJECT.md, .paul/ROADMAP.md</files>
  <action>
    Update `.paul/PROJECT.md`:
    - Mark the v2.58 milestone row complete (4 of 4 phases; closed 2026-05-03 via the Phase 252 PR placeholder/actual PR).
    - Update Current focus / Current milestone pointer prose so Phase 252 is no longer described as pending after APPLY.
    - Append one Key Decisions row: `2026-05-03: Close v2.58 S3 guided-workflow-detection extraction wave; recommend v2.59 default focus S7 pals-context-injection contract/baseline as a moderate-risk next candidate now that guided-workflow evidence semantics are stable; alternatives remain S4 canonical reply delivery, S6 lifecycle UI, S8 command routing, local context-cost evidence, demo project preset, and broader validation-script size-debt work requiring separate planning.`
    - Update the footer to Phase 252 closure.

    Update `.paul/ROADMAP.md`:
    - Change current milestone status from In Progress to Complete and Phase 252 status from Ready to plan/Planning to Complete with 2026-05-03 completion date.
    - Add or update a completed milestone row/summary for v2.58 naming Phase 249, 250, 251, 252, PRs #164/#165/#166 and Phase 252 PR placeholder/actual PR, validation/install reconciliation, and the v2.59 recommendation.
    - Preserve existing v2.57/v2.56 completed milestone summaries and archive links.
  </action>
  <verify>Focused checks find `Complete (4 of 4 phases)`, `Phase 252`, `v2.59`, `pals-context-injection`, `guided-workflow-detection.ts`, and Phase 252 closure footer text in `.paul/PROJECT.md`; ROADMAP shows v2.58 complete and Phase 252 complete.</verify>
  <done>AC-2 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Add shared closure guardrail and run official verification</name>
  <files>tests/cross-harness-validation.sh</files>
  <action>
    Add exactly one localized closure `tap_file_contains_all` block to `tests/cross-harness-validation.sh`, immediately before the summary echo block and after the Phase 248/251 sibling extraction blocks:
    - Title: `Phase 252 sibling extraction wave closure (v2.58 Shared Invariant)`.
    - Target: `$REPO_ROOT/.paul/MILESTONES.md`.
    - Required phrases: `v2.58 Pi Extension Submodule Extraction Wave`, `Phase 249`, `Phase 250`, `Phase 251`, `Phase 252`, `guided-workflow-detection.ts`, `pals-hooks.ts`, and `v2.59`.
    - Update the TAP plan total from `1..134` to `1..135` only as required by the added assertion.
    Do not modify `tests/pi-end-to-end-validation.sh`, `tests/helpers/**`, existing Phase 248/251 validation blocks, helper functions, or unrelated test text.

    Run official verification:
    - `PALS_ROOT="$PWD" bash drivers/pi/install.sh`
    - `bash tests/pi-end-to-end-validation.sh`
    - `bash tests/cross-harness-validation.sh`
    - `bash tests/helpers/artifact_consistency.sh`
    - `git diff --check`
    - changed-file scope check against this plan's allowed files plus standard `.paul/*` lifecycle artifacts.
  </action>
  <verify>Cross-harness total is 135 with zero failures; Pi validation remains 227 with zero failures; install reports `[ok] Pi extensions installed: 5 files`; artifact_consistency PASS; `git diff --check` clean; changed non-lifecycle file set is exactly `tests/cross-harness-validation.sh`.</verify>
  <done>AC-3 and AC-4 satisfied.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- Runtime TypeScript behavior, especially `drivers/pi/extensions/guided-workflow-detection.ts`, `drivers/pi/extensions/pals-hooks.ts`, and other sibling runtime modules.
- `drivers/pi/install.sh`, `drivers/pi/uninstall.sh`, and `drivers/pi/driver.yaml`.
- `README.md`, `drivers/pi/extensions/README.md`, `drivers/pi/skill-map.md`, and `docs/*.md` contract docs; Phase 251 already surfaced the S3 outcome.
- `tests/pi-end-to-end-validation.sh` and `tests/helpers/**`.
- Existing Phase 248/251 validation guardrails, TAP helper functions, and unrelated validation classifications.
- `kernel/**`, `modules/**`, `.pi/agents/**`, dependency manifests, lockfiles, CI configuration, GitHub Flow automation, helper-agent contracts, APPLY workflow semantics, guided workflow runtime behavior, module dispatch semantics.
- Existing v2.56/v2.57 milestone rows/summaries except surrounding pointer context required to add v2.58.

## SCOPE LIMITS
- Evidence aggregation + lifecycle artifact closure + one cross-harness shared-invariant guardrail only.
- No runtime/source extraction, S4/S6/S7/S8 implementation, install-surface change, dependency/CI change, validation-helper refactor, validation-suite decomposition, local context-cost measurement, demo project preset, or lifecycle-authority behavior change.
- The v2.59 recommendation is recorded, not approved. Final v2.59 selection happens via `/paul:milestone` after v2.58 closes.
- If APPLY discovers the recommended v2.59 focus is ambiguous or unsupported by closure evidence, stop for guidance rather than inventing a roadmap.
</boundaries>

<verification>
Before declaring APPLY complete:
- [ ] Focused marker checks pass for `.paul/MILESTONES.md`, `.paul/PROJECT.md`, `.paul/ROADMAP.md`, and `tests/cross-harness-validation.sh`.
- [ ] `PALS_ROOT="$PWD" bash drivers/pi/install.sh` reports `[ok] Pi extensions installed: 5 files`.
- [ ] `bash tests/pi-end-to-end-validation.sh` passes with `# Total:  227` and `# Failed: 0`.
- [ ] `bash tests/cross-harness-validation.sh` passes with `# Total:  135` and `# Failed: 0`.
- [ ] `bash tests/helpers/artifact_consistency.sh` reports `artifact_consistency_check: PASS`.
- [ ] `git diff --check` is clean.
- [ ] Changed-file scope is limited to `.paul/MILESTONES.md`, `.paul/PROJECT.md`, `.paul/ROADMAP.md`, `tests/cross-harness-validation.sh`, and normal `.paul/*` lifecycle artifacts including this PLAN/SUMMARY.
- [ ] Workguard report allows `.paul/**` and `tests/cross-harness-validation.sh` with no out-of-scope drift.
</verification>

<success_criteria>
- v2.58 closure evidence is durable in `.paul/MILESTONES.md`, `.paul/PROJECT.md`, and `.paul/ROADMAP.md`.
- The final v2.58 validation/install count reconciliation is explicit: Pi 223→227, cross-harness 131→135, install 4→5.
- The v2.59 recommendation is recorded as a recommendation only and preserves alternatives.
- Cross-harness closure guardrail protects v2.58 milestone evidence with one localized assertion.
- No runtime TypeScript, install/uninstall/manifest, dependency, CI, validation-helper, or lifecycle-authority scope creep occurs.
</success_criteria>

<output>
After completion, create `.paul/phases/252-evidence-closure-next-roadmap-decision/252-01-SUMMARY.md` with AC results, files changed, validation output/count reconciliation, module reports, Workguard scope evidence, GitHub Flow evidence, and any deviations.
</output>
