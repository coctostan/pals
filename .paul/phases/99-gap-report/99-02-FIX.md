---
phase: 99-gap-report
plan: 02
type: fix
wave: 1
depends_on: []
files_modified:
  - kernel/workflows/apply-phase.md
  - kernel/workflows/plan-phase.md
  - kernel/workflows/unify-phase.md
  - kernel/workflows/fix-loop.md
  - kernel/workflows/pause-work.md
  - kernel/workflows/research.md
  - kernel/workflows/resume-project.md
  - kernel/references/module-dispatch.md
autonomous: true
---

<objective>
## Fix
Replace all `kernel/modules.yaml` → `modules.yaml` and `kernel/references/` → `references/` in workflow and reference files. The `kernel/` prefix is a source-repo artifact that doesn't exist in the installed Pi tree, causing all module dispatch to silently no-op.
</objective>

<tasks>

<task type="auto">
  <name>Fix: Remove kernel/ prefix from all module dispatch paths in source + installed files</name>
  <files>8 source files + 10 installed files</files>
  <action>
    1. In all source kernel/workflows/*.md and kernel/references/module-dispatch.md:
       - Replace "kernel/modules.yaml" → "modules.yaml"
       - Replace "kernel/references/" → "references/"
    2. Copy fixed source files to installed location
    3. Also fix installed-only files (tdd-execution.md, tdd-plan-generation.md)
  </action>
  <verify>
    - grep -r "kernel/modules.yaml" kernel/ should return 0
    - grep -r "kernel/modules.yaml" ~/.pi/agent/skills/pals/ should return 0
  </verify>
  <done>Fix verified working</done>
</task>

</tasks>

<verification>
- [ ] Fix applied and verified
- [ ] No regressions introduced
</verification>
