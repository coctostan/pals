---
phase: 239-bounded-pi-extension-extraction-spike
plan: 01
type: execute
wave: 1
depends_on: [238-01]
files_modified:
  - drivers/pi/extensions/pals-hooks.ts
  - drivers/pi/extensions/module-activity-parsing.ts
  - tests/pi-end-to-end-validation.sh
  - tests/cross-harness-validation.sh
  - drivers/pi/extensions/README.md
  - drivers/pi/skill-map.md
autonomous: true
risk_tier: risky
---

# Plan 239-01 — Bounded Pi Extension Extraction Spike (S5 `module-activity-parsing`)

<objective>
**Goal:** Extract the `module-activity-parsing` subsystem (S5) out of `drivers/pi/extensions/pals-hooks.ts` into a single new module file `drivers/pi/extensions/module-activity-parsing.ts`, behavior-preserving.

**Purpose:** Prove the Pi-extension modularization path defined by the Phase 238 contract on the smallest, purest subsystem. Reduce maintainability weakness #2 (1,684-line `pals-hooks.ts`) without changing supported Pi runtime behavior, validation truth, or `.paul/*` lifecycle authority.

**Output:** New module `drivers/pi/extensions/module-activity-parsing.ts` exporting the S5 surface; `drivers/pi/extensions/pals-hooks.ts` importing from the module instead of declaring it inline; one bounded TAP guardrail per validation suite verifying `DISPATCH_MARKER` / `MODULE_REPORTS_HEADER` semantics are preserved; final SUMMARY at `.paul/phases/239-bounded-pi-extension-extraction-spike/239-01-SUMMARY.md`.
</objective>

<context>

**Project files:**
- `.paul/STATE.md` — authoritative lifecycle state; Phase 239 ready to plan.
- `.paul/ROADMAP.md` — Phase 239 focus: extract one pure subsystem and prove modularization path.
- `.paul/PROJECT.md` — `.paul/*` artifact authority, parent-owned APPLY, and `pals-implementer` bounded-delegation rules.
- `.paul/phases/238-pi-extension-modularization-contract/238-01-SUMMARY.md` — direct predecessor; Phase 239 readiness evidence.
- `docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md` — **binding contract for Phase 239**: subsystem inventory, S5 selection, allowed file scope, count caps, forbidden surfaces, required risky-tier evidence.
- `docs/PALS-RISK-TIERED-LIFECYCLE-CONTRACT.md` — risky-tier evidence requirements; non-bypass rules for `.paul/*` authority, no hidden state, no telemetry, command-output truth, full authoritative read fallback.
- `docs/PALS-ARTIFACT-CONSISTENCY-GUARDRAILS.md` — Phase 237 helper; runs automatically inside both validation suites and will catch artifact drift mid-loop.

**Source files (read-only context for PLAN; APPLY edits the source copies):**
- `drivers/pi/extensions/pals-hooks.ts` — supported Pi runtime entry point. S5 surface lives at lines 36–37 (`MAX_VISIBLE_MODULES`, `MAX_WIDGET_MODULE_DETAILS`), 38–39 (`DISPATCH_MARKER`, `MODULE_REPORTS_HEADER`), 166–169 (`ModuleActivityEntry` type), 171–176 (`RecentModuleActivity` type), 569–696 (the eight S5 functions: `compactModuleDetail`, `normalizeModuleEntryName`, `formatModuleStageLabel`, `parseModuleActivityEntries`, `extractDispatchModuleActivity`, `extractModuleReportActivity`, `extractRecentModuleActivity`, `formatModuleEntryList`). Lines may shift by APPLY time; APPLY MUST reread before editing.
- `drivers/pi/extensions/module-activity-parsing.ts` — does not yet exist; created by APPLY.
- `tests/pi-end-to-end-validation.sh` — current baseline `1..210` / `# Passed: 210`. Phase 239 cap: at most `1..211` / `# Passed: 211`.
- `tests/cross-harness-validation.sh` — current baseline `1..124` / `# Passed: 124`. Phase 239 cap: at most `1..125` / `# Passed: 125`.

**Carried decisions (from Phase 238 contract):**
- Selected first-spike candidate is **S5 `module-activity-parsing`** — pure transcript parsing, no Pi mutation, no filesystem I/O, no `.paul/*` writes.
- Cited identifiers that MUST be preserved exactly: `DISPATCH_MARKER`, `MODULE_REPORTS_HEADER`, plus the eight S5 function names listed above and the `ModuleActivityEntry` / `RecentModuleActivity` type names.
- The `compactWhitespace` helper at line 236 is shared with non-S5 subsystems (parsers for STATE.md, command routing, lifecycle UI, etc.) and **stays in `pals-hooks.ts`**; the new module imports it from there.
- The `MAX_VISIBLE_MODULES` and `MAX_WIDGET_MODULE_DETAILS` constants are read by `formatModuleEntryList` callers (`renderModuleActivity`, `renderModuleActivityDetails`) which are S6 lifecycle-UI surface, not S5. The constants stay defined in `pals-hooks.ts`; `extractModuleReportActivity` references `MAX_WIDGET_MODULE_DETAILS` and the new module imports it.

**Blast radius (CODI, advisory):**
- `parseModuleActivityEntries` → `extractDispatchModuleActivity` → `extractRecentModuleActivity` → `syncLifecycleUi` → `palsHooks` (entry point). All call-sites are inside `pals-hooks.ts`. No external consumers.
- `formatModuleEntryList` is called only by `renderModuleActivity` / `renderModuleActivityDetails` (S6, inline). It is part of the S5 export surface so APPLY does not need to relocate the renderers.

</context>

<acceptance_criteria>

**AC-1: Behavior-preserving extraction.**
Given the current S5 surface declared inline in `drivers/pi/extensions/pals-hooks.ts`,
when APPLY moves the eight S5 functions and the `ModuleActivityEntry` / `RecentModuleActivity` types into `drivers/pi/extensions/module-activity-parsing.ts` and re-imports them in `pals-hooks.ts`,
then `bash -n drivers/pi/extensions/pals-hooks.ts` is clean, `bash -n drivers/pi/extensions/module-activity-parsing.ts` is clean, the file no longer declares those eight functions or two types inline, and every existing call-site (`extractDispatchModuleActivity` from `parseModuleActivityEntries`, `extractRecentModuleActivity` calling `extractDispatchModuleActivity` and `extractModuleReportActivity`, `renderModuleActivity` / `renderModuleActivityDetails` calling `formatModuleEntryList`, and `syncLifecycleUi` calling `extractRecentModuleActivity`) resolves to the imported symbol with identical signatures.

**AC-2: Marker preservation.**
Given the constants `DISPATCH_MARKER = "[dispatch]"` and `MODULE_REPORTS_HEADER = "Module Execution Reports"` defined at lines 38–39 of `pals-hooks.ts` before the spike,
when APPLY moves them into the new module (or re-exports them via the new module while keeping single-source-of-truth),
then a focused marker check confirms both string literals appear exactly once across `drivers/pi/extensions/{pals-hooks,module-activity-parsing}.ts` as a `const` definition, and any other reference is an `import` of the same symbol — i.e., no string-literal drift, no duplicate definitions.

**AC-3: Bounded validation guardrail.**
Given the Phase 239 contract cap of at most Pi `210→211` and cross-harness `124→125`,
when APPLY adds **at most one** new TAP assertion per suite covering S5 marker preservation (e.g., asserting that exactly one definition of each marker exists in the module file and that `pals-hooks.ts` imports from `./module-activity-parsing`),
then `bash tests/pi-end-to-end-validation.sh` reports `1..N` and `# Passed: N` with `N` ∈ {210, 211}, `bash tests/cross-harness-validation.sh` reports `1..M` and `# Passed: M` with `M` ∈ {124, 125}, and `# Failed: 0` in both. Pure-refactor zero-drift (210/210 and 124/124) is also acceptable; larger drift is forbidden.

**AC-4: Bounded scope.**
Given the Phase 238 contract's allowed-files list,
when APPLY commits its change set,
then changed paths are a subset of: `drivers/pi/extensions/pals-hooks.ts`, `drivers/pi/extensions/module-activity-parsing.ts`, `tests/pi-end-to-end-validation.sh`, `tests/cross-harness-validation.sh`, `drivers/pi/extensions/README.md`, `drivers/pi/skill-map.md`, and standard `.paul/*` lifecycle metadata (`.paul/STATE.md`, `.paul/ROADMAP.md`, `.paul/MILESTONES.md`, `.paul/phases/239-bounded-pi-extension-extraction-spike/**`, `.paul/handoffs/**`). The Workguard report shows `outside_allowed=0` and `over_budget=false`; `git diff --check` is clean.

**AC-5: Authority and non-bypass preservation.**
Given the risky-tier non-bypass rules from `docs/PALS-RISK-TIERED-LIFECYCLE-CONTRACT.md` and Phase 238's forbidden list,
when APPLY completes,
then no telemetry/hidden runtime state/alternate ledger is added, no `pi.*` API call site is removed or added, no `/paul-*` command/shortcut/canonical-reply behavior changes, parent-owned APPLY remains authoritative, full authoritative read fallback is preserved, command-output validation truth is preserved, `.paul/*` artifact authority is preserved, and `drivers/pi/install.sh`, `drivers/pi/uninstall.sh`, `drivers/pi/driver.yaml`, `drivers/claude-code/**`, `drivers/agent-sdk/**`, `kernel/**`, `modules/**`, `tests/helpers/tap.sh`, `tests/helpers/artifact_consistency.sh`, package manifests, lockfiles, and CI configuration are unchanged.

</acceptance_criteria>

<tasks>

<task id="239-01-T1" title="Extract S5 surface into new module file" eligible_for_delegation="true">
  <files>
    - drivers/pi/extensions/module-activity-parsing.ts (new)
    - drivers/pi/extensions/pals-hooks.ts (edit: remove inline S5 declarations, add import)
  </files>
  <action>
    1. Reread `drivers/pi/extensions/pals-hooks.ts` to confirm current line numbers for the S5 surface (constants `DISPATCH_MARKER`, `MODULE_REPORTS_HEADER`, types `ModuleActivityEntry`, `RecentModuleActivity`, and functions `compactModuleDetail`, `normalizeModuleEntryName`, `formatModuleStageLabel`, `parseModuleActivityEntries`, `extractDispatchModuleActivity`, `extractModuleReportActivity`, `extractRecentModuleActivity`, `formatModuleEntryList`). Treat the line numbers in this plan as advisory and use the live read.
    2. Create `drivers/pi/extensions/module-activity-parsing.ts` containing exactly:
       - `import { compactWhitespace, MAX_WIDGET_MODULE_DETAILS } from "./pals-hooks";` if those symbols cannot be moved without dragging unrelated subsystems (preferred), OR introduce no circular import by inlining a private `compactWhitespace` copy ONLY if a test-driven need surfaces during APPLY (default: import).
       - `export const DISPATCH_MARKER = "[dispatch]";`
       - `export const MODULE_REPORTS_HEADER = "Module Execution Reports";`
       - `export type ModuleActivityEntry = { name: string; detail?: string; };`
       - `export type RecentModuleActivity = { source: "dispatch" | "report"; stage: string; stageLabel: string; entries: ModuleActivityEntry[]; };`
       - `export function compactModuleDetail(...)`, `export function normalizeModuleEntryName(...)`, `export function formatModuleStageLabel(...)`, `export function parseModuleActivityEntries(...)`, `export function extractDispatchModuleActivity(...)`, `export function extractModuleReportActivity(...)`, `export function extractRecentModuleActivity(...)`, `export function formatModuleEntryList(...)` — each with the exact same signature and body as the source copy. No new behavior, no logic drift, no signature change.
    3. In `drivers/pi/extensions/pals-hooks.ts`:
       - Remove the inline `const DISPATCH_MARKER`, `const MODULE_REPORTS_HEADER`, `type ModuleActivityEntry`, `type RecentModuleActivity`, and the eight S5 function declarations.
       - Add a single import: `import { DISPATCH_MARKER, MODULE_REPORTS_HEADER, ModuleActivityEntry, RecentModuleActivity, compactModuleDetail, normalizeModuleEntryName, formatModuleStageLabel, parseModuleActivityEntries, extractDispatchModuleActivity, extractModuleReportActivity, extractRecentModuleActivity, formatModuleEntryList } from "./module-activity-parsing";` (or, if the project's style is path-suffix-aware, use the equivalent matching neighboring import style in the same file).
       - `compactWhitespace`, `MAX_VISIBLE_MODULES`, and `MAX_WIDGET_MODULE_DETAILS` STAY in `pals-hooks.ts`; only `MAX_WIDGET_MODULE_DETAILS` is consumed by the new module and gets exported (or re-exported) so the import in step 2 resolves. Do NOT move them.
       - Do not change any other line in `pals-hooks.ts`.
    4. Confirm by re-reading both files: `wc -l` on `pals-hooks.ts` should drop by approximately the size of the moved declarations (≈ 130 lines); the new module's `wc -l` should be approximately the same magnitude plus a small import header.
  </action>
  <verify>
    - `bash -n drivers/pi/extensions/pals-hooks.ts` exits 0.
    - `bash -n drivers/pi/extensions/module-activity-parsing.ts` exits 0.
    - `grep -c "^const DISPATCH_MARKER\b\|^export const DISPATCH_MARKER\b" drivers/pi/extensions/pals-hooks.ts` reports `0`.
    - `grep -c "^export const DISPATCH_MARKER\b" drivers/pi/extensions/module-activity-parsing.ts` reports `1`.
    - `grep -c "^const MODULE_REPORTS_HEADER\b\|^export const MODULE_REPORTS_HEADER\b" drivers/pi/extensions/pals-hooks.ts` reports `0`.
    - `grep -c "^export const MODULE_REPORTS_HEADER\b" drivers/pi/extensions/module-activity-parsing.ts` reports `1`.
    - `grep -c "^function parseModuleActivityEntries\b\|^export function parseModuleActivityEntries\b" drivers/pi/extensions/pals-hooks.ts` reports `0`.
    - `grep -c "from \"./module-activity-parsing\"" drivers/pi/extensions/pals-hooks.ts` reports `1` (single import line).
    - `git diff --check` clean.
  </verify>
  <done>AC-1, AC-2, AC-5 (S5 surface relocated; markers single-defined; no behavior or `pi.*` change).</done>
</task>

<task id="239-01-T2" title="Add bounded marker-preservation guardrails to validation suites" eligible_for_delegation="true">
  <files>
    - tests/pi-end-to-end-validation.sh
    - tests/cross-harness-validation.sh
  </files>
  <action>
    1. Reread the tail of both validation suites and the assertion-numbering pattern they use (TAP `1..N`, `ok N - description`, `# Passed: N`).
    2. Add **at most one** new TAP assertion to `tests/pi-end-to-end-validation.sh` that asserts the S5 marker contract: e.g., that `drivers/pi/extensions/module-activity-parsing.ts` exists, defines `DISPATCH_MARKER = "[dispatch]"` and `MODULE_REPORTS_HEADER = "Module Execution Reports"` (literal-string match), and that `drivers/pi/extensions/pals-hooks.ts` contains an import from `"./module-activity-parsing"` (single-line literal match). The assertion is a single localized check; do not introduce a new helper script. Update `1..210` → `1..211` only if exactly one assertion is added.
    3. Add the equivalent **at most one** new TAP assertion to `tests/cross-harness-validation.sh` covering the same module marker preservation. Update `1..124` → `1..125` only if exactly one assertion is added.
    4. If APPLY can prove behavior preservation without adding an assertion (pure-refactor outcome), skip step 2 and 3 and leave both suites unchanged — Pi `210/210` and cross-harness `124/124` is an acceptable target. Plan default: add one assertion per suite to make S5 preservation explicit, but record the chosen outcome in the SUMMARY.
    5. Do NOT modify `tests/helpers/tap.sh` or `tests/helpers/artifact_consistency.sh`.
  </action>
  <verify>
    - `bash tests/pi-end-to-end-validation.sh` exits 0 with `1..N` / `# Passed: N` where `N` ∈ {210, 211} and `# Failed: 0`.
    - `bash tests/cross-harness-validation.sh` exits 0 with `1..M` / `# Passed: M` where `M` ∈ {124, 125} and `# Failed: 0`.
    - `bash tests/helpers/artifact_consistency.sh` reports `artifact_consistency_check: PASS`.
    - `git diff tests/helpers/tap.sh tests/helpers/artifact_consistency.sh` is empty.
    - `git diff tests/pi-end-to-end-validation.sh tests/cross-harness-validation.sh` shows ≤ 1 added TAP assertion per file plus the single `1..` plan-line bump (or empty diff for the zero-drift outcome).
  </verify>
  <done>AC-3 (bounded validation count change at or under cap; both suites green).</done>
</task>

<task id="239-01-T3" title="Surface naming for the extracted module (optional, bounded)" eligible_for_delegation="true">
  <files>
    - drivers/pi/extensions/README.md
    - drivers/pi/skill-map.md
  </files>
  <action>
    1. Reread `drivers/pi/extensions/README.md` and `drivers/pi/skill-map.md`.
    2. If either file already enumerates extension surface modules (e.g., a "Files" or "Modules" section), add a short single-line entry naming `module-activity-parsing.ts` and pointing at `docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md` (Phase 239 first-spike). No new section, no rephrased contract narrative, no copied content from the contract doc.
    3. If neither file has a natural insertion point without restructuring, leave both unchanged. The contract permits zero edits here.
    4. Do not rename any other surface, do not change file purposes, do not touch any other doc.
  </action>
  <verify>
    - Diff per file is ≤ 3 added lines OR zero (no edits).
    - `grep -l "module-activity-parsing" drivers/pi/extensions/README.md drivers/pi/skill-map.md` either shows the file(s) chosen for the optional entry or is empty (skip path).
    - `bash tests/pi-end-to-end-validation.sh` and `bash tests/cross-harness-validation.sh` still pass.
  </verify>
  <done>AC-4 partial (changes stay in allowed scope; surface naming optional and bounded).</done>
</task>

</tasks>

<boundaries>

**DO NOT CHANGE (forbidden by Phase 238 contract and risky-tier non-bypass rules):**
- Any non-S5 subsystem: S1 `artifact-slice-rendering`, S2 `workflow-resource-capsule-rendering`, S3 `guided-workflow-detection`, S4 `guided-workflow-canonical-reply`, S6 `lifecycle-ui`, S7 `pals-context-injection`, S8 `command-routing`. Functions, constants, types, and call-sites of those subsystems in `pals-hooks.ts` MUST remain byte-identical except where a single-line import addition is required.
- `drivers/pi/install.sh`, `drivers/pi/uninstall.sh`, `drivers/pi/driver.yaml`.
- `drivers/claude-code/**`, `drivers/agent-sdk/**`.
- `kernel/**`, `modules/**`.
- `tests/helpers/tap.sh`, `tests/helpers/artifact_consistency.sh`.
- `package.json`, lockfiles, CI configuration, dependency versions.
- Any `.paul/*` artifact other than the standard Phase 239 lifecycle metadata writes.
- The `/paul-*` user-facing command set, shortcuts, or canonical replies.
- Parent-owned APPLY authority, helper-delegation boundaries, lifecycle authority, module evidence, GitHub Flow gates, full authoritative read fallback, command-output truth, guided-workflow canonical-reply semantics, no auto-approval, no auto-continue, no skipped checkpoints, no UI-only lifecycle decisions, no inferred merge intent, `.paul/*` artifact authority.

**SCOPE LIMITS:**
- Exactly one new file under `drivers/pi/extensions/`: `module-activity-parsing.ts`. No additional new files anywhere in the repo (other than the Phase 239 SUMMARY and the auto-generated handoff).
- At most one new TAP assertion per validation suite. Pi count cap: `211`. Cross-harness count cap: `125`. Zero-drift (`210` / `124`) is also acceptable.
- No telemetry, hidden runtime state, caches, generated ledgers, alternate validation/module/lifecycle/GitHub Flow ledger, or analytics surface.
- No behavior change. No signature change. No logic drift. Pure relocation of declarations plus a single import line.
- No changes to `compactWhitespace`, `MAX_VISIBLE_MODULES`, the `RecentModuleActivity` shape, or the `ModuleActivityEntry` shape.

</boundaries>

<verification>

**Pre-APPLY (read-only):**
- `bash tests/pi-end-to-end-validation.sh` baseline = `1..210` / `# Passed: 210` / `# Failed: 0`.
- `bash tests/cross-harness-validation.sh` baseline = `1..124` / `# Passed: 124` / `# Failed: 0`.
- `bash tests/helpers/artifact_consistency.sh` = `artifact_consistency_check: PASS`.
- `git status --short` clean before APPLY begins.

**Post-APPLY (parent-run; helper-delegated APPLY does not write `.paul/*` and does not gate on its own validation):**
- `bash -n drivers/pi/extensions/pals-hooks.ts` exits 0.
- `bash -n drivers/pi/extensions/module-activity-parsing.ts` exits 0.
- Marker preservation: `grep -E '^export const DISPATCH_MARKER\s*=\s*"\[dispatch\]"\s*;' drivers/pi/extensions/module-activity-parsing.ts` matches exactly once; `grep -E '^export const MODULE_REPORTS_HEADER\s*=\s*"Module Execution Reports"\s*;' drivers/pi/extensions/module-activity-parsing.ts` matches exactly once; `grep -E '^const DISPATCH_MARKER|^const MODULE_REPORTS_HEADER' drivers/pi/extensions/pals-hooks.ts` matches zero times.
- Import preservation: `grep -E 'from "\./module-activity-parsing"' drivers/pi/extensions/pals-hooks.ts` matches exactly once.
- `bash tests/pi-end-to-end-validation.sh` reports `1..N` / `# Passed: N` / `# Failed: 0`, `N` ∈ {210, 211}.
- `bash tests/cross-harness-validation.sh` reports `1..M` / `# Passed: M` / `# Failed: 0`, `M` ∈ {124, 125}.
- `bash tests/helpers/artifact_consistency.sh` = `artifact_consistency_check: PASS`.
- `git diff --check` clean.
- `git diff --name-only` shows only paths from the AC-4 allowed list.
- Workguard report (allowed paths from AC-4): `outside_allowed=0`, `over_budget=false`.
- Full authoritative read fallback preserved: `pals-hooks.ts` still parses `.paul/STATE.md` directly via `readFileOr` and `parsePalsState` (lines 240–259 region) — the extraction does NOT touch this code path.

**Module dispatch (parent runs on APPLY):**
- `pre-apply`: WALT records baseline; TODD has no test infrastructure for extensions, may emit `TODD: No test infrastructure detected — consider adding tests.` (advisory only; the bounded TAP assertion satisfies risky-tier evidence).
- `post-apply`: WALT compares to baseline (must be no regression); IRIS/RUBY may flag complexity but the move is mechanical; SETH/DEAN/DAVE/DOCS/PETE/REED/VERA/OMAR/LUKE/ARIA/GABE/DANA expect zero new findings; SKIP captures decisions.
- `post-unify`: CODI appends one row to `.paul/CODI-HISTORY.md`; WALT appends one row to `.paul/quality-history.md`; SKIP captures the SUMMARY decisions.

</verification>

<success_criteria>
- All five ACs satisfied.
- `pals-hooks.ts` line count drops by approximately the size of the moved declarations (target: ≈ 130 fewer lines).
- New `drivers/pi/extensions/module-activity-parsing.ts` exists, exports the full S5 surface, contains no behavior change.
- Validation: Pi `210/210` or `211/211`; cross-harness `124/124` or `125/125`; both green; artifact-consistency PASS.
- Parent-run official validation matches reconciled count exactly; helper-delegated APPLY tasks (if used) produce a structured report inspected by the parent before acceptance.
- Workguard scope clean; `git diff --check` clean.
- The Phase 238 contract's required risky-tier evidence is fully present in the Phase 239 SUMMARY.
</success_criteria>

<output>
- `.paul/phases/239-bounded-pi-extension-extraction-spike/239-01-SUMMARY.md` (UNIFY).
- New module: `drivers/pi/extensions/module-activity-parsing.ts`.
- Updated: `drivers/pi/extensions/pals-hooks.ts`, `tests/pi-end-to-end-validation.sh`, `tests/cross-harness-validation.sh`, optionally `drivers/pi/extensions/README.md` and/or `drivers/pi/skill-map.md`.
- Standard `.paul/*` lifecycle metadata: `.paul/STATE.md`, `.paul/ROADMAP.md`, `.paul/MILESTONES.md`, this PLAN, the SUMMARY.
</output>

<skills>
- `/paul` — required (PLAN/APPLY/UNIFY loop authority).
- TODD — required gate; advisory pass acceptable for risky tier when bounded TAP guardrails carry the marker preservation evidence.
- WALT — required gate; baseline-vs-post comparison must show no regression.
- `/carl` — automatic (session boundary monitoring).
</skills>

<module_dispatch>

**Pre-plan advisory (recorded at plan creation, 2026-05-01):**

- `[dispatch] CODI: pre-plan advisory → impact × 3 symbols → 3 resolved, 0 unresolved, 6 total call-sites, injected blast_radius`

### Blast Radius (CODI)

**parseModuleActivityEntries**
```
drivers/pi/extensions/pals-hooks.ts:605:4c99  extractDispatchModuleActivity  behavioral  depth:1
drivers/pi/extensions/pals-hooks.ts:670:5ec6  extractRecentModuleActivity  behavioral  depth:2
drivers/pi/extensions/pals-hooks.ts:740:37f2  syncLifecycleUi  behavioral  depth:3
drivers/pi/extensions/pals-hooks.ts:1408:cdd1 palsHooks  behavioral  depth:4 (entry-point)
```

**extractRecentModuleActivity**
```
drivers/pi/extensions/pals-hooks.ts:740:37f2  syncLifecycleUi  behavioral  depth:1
drivers/pi/extensions/pals-hooks.ts:1408:cdd1 palsHooks  behavioral  depth:2 (entry-point)
```

**formatModuleEntryList**
```
drivers/pi/extensions/pals-hooks.ts:698:048b  renderModuleActivity  behavioral  depth:1
drivers/pi/extensions/pals-hooks.ts:705:4505  renderModuleActivityDetails  behavioral  depth:1
```

**Other pre-plan advisory hooks (summary):**
- `[dispatch] pre-plan advisory: TODD → No `.test.ts` files near `drivers/pi/extensions/`; bounded TAP guardrails in the validation suites carry the marker preservation evidence in lieu of unit tests for this risky-tier spike.`
- `[dispatch] pre-plan advisory: IRIS → No anti-patterns in planned files.`
- `[dispatch] pre-plan advisory: DAVE → CI config unchanged in scope.`
- `[dispatch] pre-plan advisory: DOCS → README/skill-map updates are explicitly bounded and optional; contract doc already exists at `docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md`.`
- `[dispatch] pre-plan advisory: RUBY → Pure relocation; complexity-per-function unchanged. Extraction reduces `pals-hooks.ts` size by ≈ 130 lines, a maintainability improvement.`

**Pre-plan enforcement:**
- `[dispatch] pre-plan enforcement: DEAN → no dependency change in scope; PASS.`

**Post-plan:**
- `[dispatch] post-plan: TODD → No restructure to TDD; existing infrastructure does not cover the extension file. Bounded TAP guardrails accepted as risky-tier evidence.`

</module_dispatch>

<assumptions>
- The S5 line ranges cited in `<context>` are advisory; APPLY rereads `drivers/pi/extensions/pals-hooks.ts` before editing.
- `compactWhitespace` is shared with non-S5 subsystems and stays in `pals-hooks.ts`. The new module imports it. If APPLY discovers a circular-import risk, it may export `compactWhitespace` from `pals-hooks.ts` (single direction) before importing into the new module; the helper itself does not move.
- Both validation suites use the same TAP idiom and `1..N` plan line; APPLY confirms exact bumping mechanics during edit (pure-refactor outcome with zero-drift is also acceptable).
- The Pi extension file is imported only by Pi runtime (not by tests in this repo); no test-side import path needs updating.
- Helper delegation via `pals-implementer` is permitted for Tasks T1, T2, T3 individually but parent APPLY runs official validation, inspects diffs, and writes `.paul/*`. Task T1 is the load-bearing edit; if any helper output deviates from the bounded plan, parent rejects and re-runs locally.
</assumptions>

<alternatives_considered>
1. **Move `compactWhitespace` into the new module too.** Rejected because `compactWhitespace` is consumed by S6 (`renderLifecycleStatus`), `parsePalsState` (S7-adjacent), and command-routing (S8) — moving it would expand scope into non-S5 subsystems and violate the Phase 238 forbidden-list rule of refactoring non-selected subsystems.
2. **Skip the validation guardrail addition (zero-drift outcome) and rely on existing suites alone.** Acceptable per the Phase 238 contract and listed as the alternate AC-3 outcome. Default plan path adds one bounded assertion per suite to make marker preservation explicit and to exercise the proportional safety mechanism the v2.55 milestone is hardening; a pure-refactor zero-drift outcome may be chosen at APPLY time and recorded in the SUMMARY instead.
3. **Extract S1 `artifact-slice-rendering` instead of S5.** Rejected — Phase 238 contract pre-selected S5 with documented selection rationale (lower contract-protected prose surface, smallest authority surface, highest reuse for Phase 240/241 evidence work). Reopening that decision requires a separate approved plan.
4. **Add unit tests under `drivers/pi/extensions/__tests__/` instead of TAP guardrails.** Rejected — `drivers/pi/extensions/` has no existing test harness, package.json/test command, or vitest/jest configuration; introducing one would violate the Phase 238 forbidden surfaces (`package.json`, CI configuration). TAP guardrails in the existing suites carry the same evidence within scope.
</alternatives_considered>

<risks>
- **Circular import** between `pals-hooks.ts` and `module-activity-parsing.ts` if `compactWhitespace` is imported in one direction while a re-export is added in the other. Mitigation: import only goes from new module ← `pals-hooks.ts`; the new module never imports a symbol that itself depends on `module-activity-parsing`. APPLY runs `bash -n` on both files plus a focused `grep` against the new module's import set before considering T1 done.
- **Hidden behavior dependency on declaration order** inside `pals-hooks.ts`. Mitigation: `parseModuleActivityEntries` and friends are pure functions and only consume `compactWhitespace` and `MAX_WIDGET_MODULE_DETAILS`; both are evaluated lazily at call time. Removing them does not change any module-init ordering observable to the Pi runtime.
- **Validation count drift beyond cap.** Mitigation: AC-3 explicitly caps `1..211` / `1..125`; APPLY adds at most one assertion per suite or none at all. The plan documents both acceptable outcomes.
- **Helper-agent delegation drift** (helper edits an out-of-scope file). Mitigation: parent inspects `git diff --name-only` against AC-4's allow list and runs Workguard; any out-of-scope edit blocks acceptance and is repaired locally by the parent.
- **Marker semantics drift via subtle edit** (e.g., trailing whitespace, alternate quote style). Mitigation: AC-2 + the bounded TAP assertion + `git diff --check` together pin the exact literal. Phase 237's artifact-consistency helper provides additional drift detection across `.paul/*`.
</risks>

<deferred>
- Extraction of S1, S2, S3, S4, S6, S7, S8.
- Pi extension test harness / unit-test coverage for `drivers/pi/extensions/**`.
- Dependency or CI changes related to per-driver test infrastructure.
- Conversion of brittle prose checks elsewhere in the validation suites (Phase 236 baseline; later milestones).
- CARL session-boundary surface promotion to its own extracted subsystem.
</deferred>
