---
phase: 206-guided-workflow-evidence-contract-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/PI-NATIVE-GUIDED-WORKFLOW-EVIDENCE-CONTRACT.md
autonomous: true
---

<objective>
## Goal
Create the v2.48 guided workflow evidence contract baseline: inventory the Pi guided workflow moments, define canonical transcript reply/evidence rules, name no-auto-continue and no-UI-only-decision boundaries, and hand off exact runtime/docs/validation requirements for later v2.48 phases.

## Purpose
v2.47 hardened artifact slices as bounded orientation aids. The next Pi-native build milestone should harden guided workflow evidence before broader workflow/resource capsules or helper packet compression so approvals, checkpoints, continue-to-UNIFY, pause/stop, and merge-gate routing stay explicit, auditable, and parent-workflow-owned.

## Output
- New `docs/PI-NATIVE-GUIDED-WORKFLOW-EVIDENCE-CONTRACT.md` contract baseline.
- A named v2.48 handoff for runtime guided reply hardening, docs/skill-map surfacing, and validation/evidence reconciliation.
- Completion evidence in `206-01-SUMMARY.md` after APPLY/UNIFY.
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md
.paul/phases/205-validation-evidence-reconciliation/205-01-SUMMARY.md

## Source Inputs
docs/PI-NATIVE-IMPLEMENTATION-PLAN.md
docs/PI-NATIVE-IMPLEMENTATION-WORKSTREAMS.md
docs/PI-NATIVE-VALIDATION-EVIDENCE-STRATEGY.md
docs/PI-RUNTIME-CAPABILITY-MAP.md
docs/PI-NATIVE-ARCHITECTURE-PROPOSAL.md

## Source Files
drivers/pi/extensions/pals-hooks.ts
drivers/pi/extensions/README.md
drivers/pi/skill-map.md
tests/pi-end-to-end-validation.sh
tests/cross-harness-validation.sh
pals.json
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: TODD, IRIS, DAVE, DOCS, CODI, RUBY, ARCH, SETH, and local scope probes completed; advisory only.

- Planning posture: `high` collaboration from `pals.json` project default; direct-requirements mode using STATE's recommended default milestone and the Phase 201 implementation-plan order.
- TODD: No repo-local test infrastructure detected outside ignored runtime/vendor paths; validation remains shell-suite based. Plan remains `execute`, not `tdd`.
- IRIS: No TODO/FIXME/HACK/XXX markers expected in the new planned doc; planned source change is documentation-only.
- DAVE: No repo-local CI config found outside ignored runtime/vendor paths; do not add CI in this plan.
- DOCS: Documentation catalog exists under `docs/`; the planned contract doc is a new source artifact that will cite the existing implementation-plan, workstream, validation, runtime-capability, and architecture proposal docs.
- CODI: NO MAGIC INFERENCE — only explicit source selectors. Seeds from `drivers/pi/extensions/pals-hooks.ts`: `detectGuidedWorkflowMoment`, `presentGuidedWorkflowMoment`, `sendCanonicalWorkflowResponse`, `loadGuidedWorkflowConfig`, `shouldAutoPresent`.

### Blast Radius (CODI)
**detectGuidedWorkflowMoment**
```text
drivers/pi/extensions/pals-hooks.ts:1355:71df  maybePresentGuidedWorkflow  behavioral  depth:1  [fan-in:1, fan-out:5, roles:none, coverage:untested, co-change:0.00, chain-confidence:0.90]
drivers/pi/extensions/pals-hooks.ts:1185:cdd1  palsHooks  behavioral  depth:2  [fan-in:0, fan-out:21, roles:entry-point, coverage:untested, co-change:0.00, chain-confidence:0.90]
```

**presentGuidedWorkflowMoment**
```text
drivers/pi/extensions/pals-hooks.ts:1355:71df  maybePresentGuidedWorkflow  behavioral  depth:1  [fan-in:1, fan-out:5, roles:none, coverage:untested, co-change:0.00, chain-confidence:0.90]
drivers/pi/extensions/pals-hooks.ts:1185:cdd1  palsHooks  behavioral  depth:2  [fan-in:0, fan-out:21, roles:entry-point, coverage:untested, co-change:0.00, chain-confidence:0.90]
```

**sendCanonicalWorkflowResponse**
```text
drivers/pi/extensions/pals-hooks.ts:971:6b8d  presentGuidedWorkflowMoment  behavioral  depth:1  [fan-in:1, fan-out:2, roles:none, coverage:untested, co-change:0.00, chain-confidence:0.90]
drivers/pi/extensions/pals-hooks.ts:1355:71df  maybePresentGuidedWorkflow  behavioral  depth:2  [fan-in:1, fan-out:5, roles:none, coverage:untested, co-change:0.00, chain-confidence:0.90]
drivers/pi/extensions/pals-hooks.ts:1185:cdd1  palsHooks  behavioral  depth:3  [fan-in:0, fan-out:21, roles:entry-point, coverage:untested, co-change:0.00, chain-confidence:0.90]
```

**loadGuidedWorkflowConfig**
```text
drivers/pi/extensions/pals-hooks.ts:1355:71df  maybePresentGuidedWorkflow  behavioral  depth:1  [fan-in:1, fan-out:5, roles:none, coverage:untested, co-change:0.00, chain-confidence:0.90]
drivers/pi/extensions/pals-hooks.ts:1185:cdd1  palsHooks  behavioral  depth:2  [fan-in:0, fan-out:21, roles:entry-point, coverage:untested, co-change:0.00, chain-confidence:0.90]
```

**shouldAutoPresent**
```text
drivers/pi/extensions/pals-hooks.ts:971:6b8d  presentGuidedWorkflowMoment  behavioral  depth:1  [fan-in:1, fan-out:2, roles:none, coverage:untested, co-change:0.00, chain-confidence:0.90]
drivers/pi/extensions/pals-hooks.ts:1355:71df  maybePresentGuidedWorkflow  behavioral  depth:2  [fan-in:1, fan-out:5, roles:none, coverage:untested, co-change:0.00, chain-confidence:0.90]
drivers/pi/extensions/pals-hooks.ts:1185:cdd1  palsHooks  behavioral  depth:3  [fan-in:0, fan-out:21, roles:entry-point, coverage:untested, co-change:0.00, chain-confidence:0.90]
```

[dispatch] CODI: pre-plan advisory → impact × 5 symbols → 5 resolved, 0 unresolved, 12 total call-sites, injected blast_radius
- RUBY: Planned source change is one new doc. Existing validation scripts remain large, but this plan does not edit them.
- ARCH: Detected pattern: flat / kernel-and-driver repository (medium confidence). New doc belongs in `docs/`; no architectural concern for documentation-only contract baseline.
- SETH: No planned secret-bearing source types; no hardcoded secret risk in a new markdown contract.
- ARIA/LUKE/DANA/GABE/OMAR/PETE/REED/VERA: No UI component, data schema, API route, observability runtime, performance-sensitive code, resilience runtime, or privacy data files are planned.

[dispatch] pre-plan enforcement: DEAN PASS — no package manager manifests or lockfiles detected at repo root/maxdepth 2 outside ignored runtime/vendor paths, so dependency audit is not applicable and no critical/high dependency block exists.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | PLAN/APPLY/UNIFY lifecycle work | ✓ |
| /carl | required | Session boundary monitoring | automatic |
| TODD | required | Before/during implementation; shell-suite validation substitutes for TDD conversion here | via module dispatch |
| WALT | required | APPLY/UNIFY quality validation | via module dispatch |

**BLOCKING:** Required skills MUST be loaded or active before APPLY proceeds.
</skills>

<acceptance_criteria>
## AC-1: Guided workflow moments and canonical replies are inventoried
```gherkin
Given Pi already detects guided workflow moments from shared PALS prompts
When the new contract baseline is reviewed
Then it names the guided moments, prompt markers, allowed UI mode, canonical reply values, and transcript/artifact evidence expected for plan review, APPLY approval, checkpoints, resume-next, continue-to-UNIFY, phase/milestone transition, and merge-gate routing handoff.
```

## AC-2: Explicit intent and no-auto-continue boundaries are contractually protected
```gherkin
Given guided UI is a guardrail-only Pi primitive
When the contract defines safety boundaries
Then it states that Pi UI state alone cannot approve, continue, skip checkpoints, infer merge intent, mark lifecycle work complete, write lifecycle truth, or replace canonical user-message replies.
```

## AC-3: Later runtime/docs/validation phases have executable handoff requirements
```gherkin
Given Guided Workflow Evidence Hardening needs design before runtime hardening
When Phase 206 completes
Then the contract identifies validation classes, likely repo surfaces, marker families, command-output evidence requirements, failure conditions, and Phase 207/208/209 handoff inputs without modifying runtime behavior in this plan.
```

## AC-4: Scope stays limited to contract baseline creation
```gherkin
Given v2.48 starts with a contract-baseline phase
When APPLY completes
Then only the approved guided workflow evidence contract doc is created or modified outside normal `.paul/*` lifecycle artifacts, and no runtime, validation-script, dependency, CI, workflow/resource capsule, helper compression, validation classification, legacy cleanup, or lifecycle-authority behavior changes are introduced.
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Create guided workflow moment inventory and reply contract</name>
  <files>docs/PI-NATIVE-GUIDED-WORKFLOW-EVIDENCE-CONTRACT.md</files>
  <action>
    Create the contract doc with source-cited sections for:
    - Purpose/status and planning-only boundary.
    - Source inputs from Phase 201 implementation plan, workstream design, validation/evidence strategy, runtime capability map, architecture proposal, v2.47 completion summary, and repo-source guided workflow implementation.
    - Guided workflow moment inventory covering at minimum: plan review, APPLY approval, checkpoint decision, checkpoint human verification, checkpoint human action, resume-next, continue-to-UNIFY, phase transition, milestone transition, and merge-gate routing handoff.
    - Canonical reply schema for `yes`, `approved`, `1`/numbered choices, option ids, checkpoint responses, pause/stop choices, and non-continuation choices.
    - Evidence expectations: transcript-visible canonical user messages, PLAN/SUMMARY/STATE artifacts where applicable, and command-output validation truth.
    Avoid writing implementation code or treating the contract as proof that runtime behavior is already hardened.
  </action>
  <verify>grep -n "plan review\|APPLY approval\|checkpoint decision\|continue-to-UNIFY\|canonical reply\|transcript" docs/PI-NATIVE-GUIDED-WORKFLOW-EVIDENCE-CONTRACT.md</verify>
  <done>AC-1 satisfied: the contract names guided moments, reply values, and audit evidence expectations.</done>
</task>

<task type="auto">
  <name>Task 2: Define explicit-intent boundaries and validation handoff</name>
  <files>docs/PI-NATIVE-GUIDED-WORKFLOW-EVIDENCE-CONTRACT.md</files>
  <action>
    Extend the contract with the safety and handoff sections needed by later phases:
    - Non-negotiable authority boundaries: no auto-approval, no auto-continue, no skipped checkpoints, no UI-only lifecycle decisions, no inferred merge intent, no hidden Pi state, no Pi-owned lifecycle/module/validation ledger, and no replacement of `.paul/*` or PLAN/APPLY/UNIFY authority.
    - Validation classes: Guided UI Safety, Pi-Supported Runtime, Shared Invariant, GitHub Flow Safety, Runtime Lens where context injection/status is referenced, and Artifact / Process.
    - Marker-check families later phases should add or protect in `drivers/pi/extensions/pals-hooks.ts`, `drivers/pi/extensions/README.md`, `drivers/pi/skill-map.md`, `pals.json` guided workflow docs/config, and `tests/pi-end-to-end-validation.sh` / `tests/cross-harness-validation.sh`.
    - Failure conditions and explicit deferrals: workflow/resource capsules, delegated APPLY packet/report compression, broad validation classification, legacy cleanup, dependencies, CI, and runtime behavior changes remain out of this phase.
  </action>
  <verify>grep -n "no auto-approval\|no auto-continue\|no UI-only\|Guided UI Safety\|GitHub Flow Safety\|Failure Conditions\|Phase 207" docs/PI-NATIVE-GUIDED-WORKFLOW-EVIDENCE-CONTRACT.md</verify>
  <done>AC-2 and AC-3 satisfied: the contract protects explicit intent and gives later phases executable validation/runtime handoff requirements.</done>
</task>

<task type="auto">
  <name>Task 3: Run final contract evidence and scope checks</name>
  <files>docs/PI-NATIVE-GUIDED-WORKFLOW-EVIDENCE-CONTRACT.md</files>
  <action>
    Produce APPLY evidence for UNIFY:
    - Run focused marker grep over the new contract for guided moment names, canonical reply language, transcript evidence, no-auto/no-UI-only boundaries, validation classes, failure conditions, and deferred scopes.
    - Run current Pi and cross-harness validation suites to prove the docs-only contract baseline did not regress existing validation.
    - Run `git diff --stat` and review changed files to confirm no runtime, validation-script, dependency, CI, capsule/helper, legacy, or lifecycle-authority behavior changed outside normal `.paul/*` lifecycle artifacts.
  </action>
  <verify>grep -n "guided workflow\|canonical reply\|transcript\|no auto-approval\|no auto-continue\|no UI-only\|Guided UI Safety\|Shared Invariant\|Failure Conditions\|workflow/resource capsules\|delegated APPLY" docs/PI-NATIVE-GUIDED-WORKFLOW-EVIDENCE-CONTRACT.md && bash tests/pi-end-to-end-validation.sh && bash tests/cross-harness-validation.sh && git diff --stat</verify>
  <done>AC-4 satisfied: final evidence is command-backed and changed files stay inside approved contract-baseline scope.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- `drivers/pi/extensions/pals-hooks.ts` runtime behavior in Phase 206.
- `tests/pi-end-to-end-validation.sh` and `tests/cross-harness-validation.sh` in Phase 206.
- `drivers/pi/install.sh`, installed runtime copies outside the repo, dependency manifests/lockfiles, and CI configuration.
- Kernel workflow semantics, module source behavior, GitHub Flow automation, lifecycle approval/checkpoint semantics, or `.paul/*` authority outside normal lifecycle artifact updates.
- Claude Code, Agent SDK, portability/frozen legacy parity surfaces.

## SCOPE LIMITS
- This plan creates the guided workflow evidence contract baseline only.
- Do not implement runtime guided workflow behavior changes, workflow/resource capsules, delegated APPLY packet/report compression, broad validation classification, legacy cleanup, telemetry, hidden state, helper-owned APPLY behavior, or merge-gate automation.
- Do not treat Pi UI state as lifecycle truth; every continuation must remain an explicit canonical workflow reply.
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `grep -n "plan review\|APPLY approval\|checkpoint decision\|continue-to-UNIFY\|canonical reply\|transcript" docs/PI-NATIVE-GUIDED-WORKFLOW-EVIDENCE-CONTRACT.md`
- [ ] `grep -n "no auto-approval\|no auto-continue\|no UI-only\|Guided UI Safety\|GitHub Flow Safety\|Failure Conditions\|Phase 207" docs/PI-NATIVE-GUIDED-WORKFLOW-EVIDENCE-CONTRACT.md`
- [ ] `grep -n "guided workflow\|canonical reply\|transcript\|no auto-approval\|no auto-continue\|no UI-only\|Guided UI Safety\|Shared Invariant\|Failure Conditions\|workflow/resource capsules\|delegated APPLY" docs/PI-NATIVE-GUIDED-WORKFLOW-EVIDENCE-CONTRACT.md`
- [ ] `bash tests/pi-end-to-end-validation.sh`
- [ ] `bash tests/cross-harness-validation.sh`
- [ ] `git diff --stat` confirms only the approved contract doc changed outside `.paul/*` lifecycle artifacts.
- [ ] All acceptance criteria met.
</verification>

<success_criteria>
- Guided workflow evidence contract baseline exists and is source-cited.
- Contract names guided moments, canonical replies, transcript/artifact evidence expectations, validation classes, failure conditions, and later-phase handoffs.
- Explicit user intent remains protected: no auto-approval, no auto-continue, no skipped checkpoints, no UI-only lifecycle decisions, no inferred merge intent.
- Current Pi and cross-harness validation remain green.
- No runtime, validation-script, dependency, CI, legacy, workflow/capsule/helper, or lifecycle-authority behavior changes are introduced in this phase.
</success_criteria>

<output>
After completion, create `.paul/phases/206-guided-workflow-evidence-contract-baseline/206-01-SUMMARY.md` with acceptance criteria results, command-output evidence, changed-file scope review, module reports, GitHub Flow evidence, decisions/deviations, and Phase 207 runtime-hardening handoff recommendations.
</output>
