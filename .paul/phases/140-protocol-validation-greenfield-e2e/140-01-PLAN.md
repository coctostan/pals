---
phase: 140-protocol-validation-greenfield-e2e
plan: 01
type: execute
wave: 1
depends_on: ["139-01"]
files_modified:
  - .paul/references/v229-e2e-report.md
autonomous: false
---

<objective>
## Goal
Validate the E2E test protocol (kernel/references/e2e-test-protocol.md) by executing a real greenfield project build through PALS in a cmux pane, monitoring all 8 dimensions, and producing the first structured E2E report.

## Purpose
The protocol was designed in Phase 139 — this phase proves it works under real conditions. We build a genuine webapp (pi-monitor) from scratch using Pi + PALS in a separate pane, follow the "script" at each lifecycle point, and produce an actionable report that feeds into future milestone discussions.

## Output
- `.paul/references/v229-e2e-report.md` — completed E2E report using kernel/templates/E2E-REPORT.md
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md

## Prior Work
.paul/phases/139-protocol-design-report-template/139-01-SUMMARY.md

## Protocol & Template
kernel/references/e2e-test-protocol.md (the protocol to validate)
kernel/templates/E2E-REPORT.md (report template to fill in)

## Reference
.paul/references/v225-actionable-items.md (gold standard for findings format)
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: TODD(p100) → skip | IRIS(p150) → skip | DAVE(p200) → skip | DOCS(p200) → skip | RUBY(p250) → skip (all: no source files in pals repo)
[dispatch] pre-plan enforcement: DEAN(p50) → skip (no package manager in pals repo)
</module_dispatch>

<acceptance_criteria>

## AC-1: E2E Test Run Completes Full Lifecycle
```gherkin
Given a greenfield project (pi-monitor) initialized via Pi + PALS
When the test completes at least 2 full PLAN/APPLY/UNIFY loops
Then the protocol was exercised under real conditions
And real-time observations were captured at each lifecycle point
```

## AC-2: All 8 Monitoring Dimensions Assessed
```gherkin
Given the E2E report is generated
When each dimension section is reviewed
Then all 8 dimensions (A-H) have scores and evidence
And no dimension is left as placeholder/template text
```

## AC-3: Report Produces Actionable Findings
```gherkin
Given the E2E report is complete
When the Itemized Findings section is reviewed
Then at least 1 finding exists with Source/Problem/Evidence/Fix/Risk/Priority/Milestone candidate
And findings are grouped into milestone candidates in the Milestone Mapping table
```

## AC-4: Protocol Validated as Usable
```gherkin
Given the protocol was followed during the test run
When the report reflects on protocol usability
Then the Executive Summary notes whether the protocol was practical to follow
And any protocol improvements are captured as findings
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Set Up Test Environment & Run Init</name>
  <files>.paul/references/v229-e2e-report.md</files>
  <action>
    **Run configuration (record in report header):**
    - Project: pi-monitor (greenfield webapp)
    - Model: anthropic/claude-sonnet-4-6:high
    - Posture: real-dev
    - Focus: broad audit (all 8 dimensions)
    - Scope: full lifecycle (init → 2-3 loops → milestone boundary)

    **Step 1 — Create cmux pane and project:**
    ```bash
    # Create a new workspace/pane for the test
    cmux new-workspace --command "bash"
    # In the new pane:
    cmux send "mkdir -p ~/claude/pi-monitor && cd ~/claude/pi-monitor && git init"
    cmux send-key enter
    ```

    **Step 2 — Start Pi with target model:**
    ```bash
    cmux send "pi --model anthropic/claude-sonnet-4-6:high"
    cmux send-key enter
    ```
    Wait for Pi to start. Verify with `cmux read-screen`.

    **Step 3 — Run PALS init:**
    ```bash
    cmux send "/skill:paul-init"
    cmux send-key enter
    ```

    **Step 4 — Answer init questions as a real user would:**
    Provide these inputs when prompted (wait for each question, read screen between sends):
    - What are you building? → "A webapp dashboard for monitoring multiple Pi coding agent sessions with real-time status updates and notifications"
    - Primary users? → "Developers running multiple Pi sessions simultaneously"
    - Must-have? → "Session status dashboard showing lifecycle state, phase notifications, real-time updates"
    - Deferred? → "User auth, persistent storage, deployment"
    - Project name? → "pi-monitor"
    - GitHub repo? → "no" (local only for this test)
    - Collaboration level? → "medium" (or accept default)
    - For any other questions: accept defaults or answer naturally

    **MONITORING (Dimension E — Init/Onboarding):**
    - Count total questions asked
    - Note which questions were skipped/defaulted
    - Check brownfield detection (should detect greenfield — empty repo)
    - After init completes, verify artifacts:
      ```bash
      cmux send "ls .paul/STATE.md .paul/PROJECT.md .paul/ROADMAP.md pals.json"
      cmux send-key enter
      ```
    - Record observations in the run log format:
      ```
      [Init, question-N] {observation}
      [Init, artifacts] {what was created}
      ```

    **Step 5 — Drive milestone discussion and creation:**
    ```bash
    cmux send "/paul:discuss-milestone"
    cmux send-key enter
    ```
    When asked what to build, describe the webapp features:
    - "Session status dashboard with cards showing project name, milestone, phase, loop position"
    - "Real-time updates via polling or WebSocket"
    - "Browser notifications on state changes"
    - "Next.js app with TypeScript"

    Let the discussion flow naturally. Accept the milestone structure it proposes (should be ~2-3 phases). Then proceed to milestone creation when prompted.

    **Step 6 — Initialize the E2E report:**
    Create `.paul/references/v229-e2e-report.md` by copying the template and filling in the Run Configuration header section with the actual run details. Leave dimension assessments and findings empty for now — they'll be populated during Task 2.
  </action>
  <verify>
    - cmux read-screen shows Pi running in the test pane
    - pi-monitor/.paul/STATE.md exists with a milestone and first phase ready
    - pi-monitor/pals.json exists
    - .paul/references/v229-e2e-report.md exists with Run Configuration filled in
    - Init observation notes captured (question count, artifacts created)
  </verify>
  <done>AC-1 partially satisfied (init complete, lifecycle started). AC-4 partially satisfied (protocol init monitoring exercised).</done>
</task>

<task type="auto">
  <name>Task 2: Execute PALS Loops & Monitor All Dimensions</name>
  <files>.paul/references/v229-e2e-report.md</files>
  <action>
    **Execute at least 2 full PLAN/APPLY/UNIFY loops** by driving Pi in the cmux pane. At each lifecycle point, monitor per the protocol.

    **Loop 1 — Foundation phase:**

    Drive /paul:plan in the test pane:
    ```bash
    cmux send "/paul:plan"
    cmux send-key enter
    ```

    **MONITORING during PLAN (Dimensions A, B, H):**
    - Check for `[dispatch]` pre-plan log lines (Dimension A — Module Dispatch L1)
    - Read the PLAN.md after creation — does it have all sections? (Dimension B — Artifact Quality)
    - Are tasks specific with files + action + verify + done? (Dimension H — Planning Quality)
    - Record: `[Loop 1, plan] {observations}`

    When plan is created and review offered, approve and proceed to apply:
    ```bash
    # Wait for plan creation, then approve
    cmux send "approved"
    cmux send-key enter
    ```

    **MONITORING during APPLY (Dimensions A, C, G):**
    - Watch for GitHub Flow preflight (Dimension D — should skip since no github)
    - Monitor task execution — does the model run commands or describe them? (Dimension G — Model Behavior)
    - Watch for `[dispatch]` post-apply advisory + enforcement (Dimension A — L2/L3)
    - Check for deviation detection (Dimension C — Workflow Compliance)
    - Record: `[Loop 1, apply] {observations}`

    When apply completes:
    ```bash
    cmux send "1"  # or "yes" to continue to UNIFY
    cmux send-key enter
    ```

    **MONITORING during UNIFY (Dimensions A, B, C):**
    - Check SUMMARY.md — reconciliation accurate? Module reports populated? (Dimension B)
    - Watch for post-unify hooks — WALT, SKIP, RUBY (Dimension A — L2)
    - Check STATE.md updated correctly (Dimension C)
    - Record: `[Loop 1, unify] {observations}`

    **Loop 2 — UI/Dashboard phase:**
    Repeat the same pattern: drive /paul:plan → approve → apply → unify.
    Monitor same dimensions at each point.

    **Loop 3 (if time/context allows) — Notifications phase:**
    Same pattern. If context is getting large, note where CARL would trigger and wrap up.

    **Between loops — MONITORING (Dimensions C, F):**
    - Check STATE.md loop position markers (Dimension C)
    - Check ROADMAP.md phase status updates (Dimension C)
    - If pausing between loops, test /paul:pause and /paul:resume (Dimension F)

    **Throughout — collect observations in run log format:**
    ```
    [Loop N, lifecycle-point] Observation
    [Loop N, lifecycle-point] FINDING: {severity} — {description}
    ```

    After all loops complete (or at natural stopping point), read back key artifacts from the test project for evidence:
    ```bash
    cmux send "cat .paul/STATE.md"
    cmux send-key enter
    # capture output
    cmux send "cat .paul/ROADMAP.md"
    cmux send-key enter
    # capture output
    ```

    **Update the E2E report** (.paul/references/v229-e2e-report.md) with:
    - All dimension assessment tables filled in with scores and evidence
    - Cross-model comparison section deleted (single model run)
    - Run log appendix populated with real-time observations
  </action>
  <verify>
    - At least 2 full PLAN/APPLY/UNIFY loops completed in the test pane
    - Observations recorded for all 8 dimensions
    - .paul/references/v229-e2e-report.md dimension assessments populated (not template placeholders)
    - Run log has entries from each lifecycle point
  </verify>
  <done>AC-1 satisfied (full lifecycle exercised). AC-2 partially satisfied (dimensions assessed).</done>
</task>

<task type="auto">
  <name>Task 3: Produce Final E2E Report</name>
  <files>.paul/references/v229-e2e-report.md</files>
  <action>
    Finalize `.paul/references/v229-e2e-report.md`:

    **1. Executive Summary:**
    - Write 3-5 sentence overview of findings
    - Overall health assessment
    - Critical/High item counts

    **2. Verify all dimension assessments are complete:**
    - Each active dimension has a score (pass/partial/fail or percentage)
    - Each has an evidence summary table
    - Delete Cross-Model Comparison section (single model run)
    - Delete Init/Onboarding section only if init was not tested (it was, so keep it)

    **3. Itemize findings:**
    For each issue discovered during monitoring, create a finding section:
    ```markdown
    ### Item N: {Title}

    **Source:** {which loop/lifecycle point surfaced this}
    **Problem:** {specific description}
    **Evidence:** {artifact references, command output, observations}
    **Fix:** {suggested resolution}
    **Risk:** {tradeoffs}
    **Priority:** {Critical/High/Medium/Low}
    **Milestone candidate:** {suggested milestone}
    ```

    **4. Milestone mapping:**
    Group findings into suggested future milestone candidates with themes and priority ordering.

    **5. Phase estimates:**
    Add rough phase breakdown for each milestone candidate.

    **6. Protocol feedback:**
    Add a section at the end of the report evaluating the protocol itself:
    - Was it practical to follow during a real test?
    - Were any dimensions hard to assess?
    - Are any dimensions missing?
    - Suggested protocol improvements (these become findings too)

    **7. Final review:**
    Read the complete report end-to-end. Verify:
    - No template placeholders remain (no `{curly_brace_text}`)
    - All findings have all 7 required fields
    - Milestone mapping accounts for all findings
    - Report is self-contained and actionable
  </action>
  <verify>
    - grep -c '{' .paul/references/v229-e2e-report.md shows zero or only legitimate uses (JSON examples, etc.)
    - All 8 dimension sections have scores (not placeholders)
    - At least 1 itemized finding with full 7-field format
    - Milestone mapping table present with at least 1 entry
    - Executive summary present (not template text)
  </verify>
  <done>AC-2 satisfied (all dimensions assessed). AC-3 satisfied (actionable findings). AC-4 satisfied (protocol validated with feedback).</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- kernel/references/e2e-test-protocol.md (the protocol being tested — don't modify during the test)
- kernel/templates/E2E-REPORT.md (the template — modify only the filled-in copy)
- kernel/workflows/* (no workflow changes)
- kernel/modules.yaml (no module changes)

## SCOPE LIMITS
- The webapp code lives in ~/claude/pi-monitor — it is NOT part of the pals repo
- This plan produces exactly one artifact in the pals repo: .paul/references/v229-e2e-report.md
- Do not attempt to fix PALS issues found during the test — document them as findings
- Target 2-3 loops minimum, do not force more if context is exhausted
- If the test pane hits issues, document them as findings rather than debugging indefinitely

</boundaries>

<verification>
Before declaring plan complete:
- [ ] At least 2 full PLAN/APPLY/UNIFY loops completed in test pane
- [ ] .paul/references/v229-e2e-report.md exists and is complete
- [ ] All 8 dimensions scored with evidence
- [ ] At least 1 itemized finding with full format
- [ ] Milestone mapping present
- [ ] Executive summary written
- [ ] Protocol feedback section included
- [ ] No template placeholders remaining in report
</verification>

<success_criteria>
- E2E test run completed with real greenfield project
- Protocol validated as usable under real conditions
- Report produces actionable findings for future milestones
- All verification checks pass
</success_criteria>

<output>
After completion, create `.paul/phases/140-protocol-validation-greenfield-e2e/140-01-SUMMARY.md`
</output>
