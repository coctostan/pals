---
phase: 274-base-module-dispatch-instruction-rewrite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/MODULE-SPEC.md
  - docs/MODULE-BUILDERS-GUIDE.md
  - kernel/references/module-dispatch.md
autonomous: false
---

<objective>
## Goal
Collaboratively audit and rewrite the core base-module contract surfaces — `docs/MODULE-SPEC.md`, `docs/MODULE-BUILDERS-GUIDE.md`, and `kernel/references/module-dispatch.md` — so they align with the Phase 273 module instruction audit contract on installed registry authority, source-file authority, hook-local refs, advisory/enforcement semantics, compact dispatch evidence, and lifecycle ownership.

## Purpose
Phase 273 created `docs/PALS-MODULE-INSTRUCTION-AUDIT-CONTRACT.md` and handed Phase 274 the shared/base module cohort. This plan starts Phase 274 with the highest-leverage surfaces: the formal module spec, module builder guide, and dispatch reference. These files define how module authors and lifecycle workflows understand manifests, refs, hook behavior, dispatch evidence, and blocking authority. They must be clear, bounded, current, and free of stale ceremony before later phases rewrite individual enforcement/advisory/domain modules.

## Output
A bounded docs/reference rewrite with:
- compact audit of the three approved files and proposed file/section order;
- blocking user approval checkpoint before any product-source edit;
- section-by-section approved edits only;
- re-read verification after each changed file/section;
- final validation evidence and deferred handoff for remaining Phase 274 surfaces (`kernel/references/evidence-discipline.md`, `kernel/references/context-management.md`, `kernel/references/specialized-workflow-integration.md`, and kernel workflow call-site wording).
</objective>

<context>
## Project Context
- `.paul/STATE.md` — current lifecycle truth: Phase 274 ready to plan; PLAN/APPLY/UNIFY loop open.
- `.paul/ROADMAP.md` — Phase 274 scope: collaboratively rewrite kernel/module-dispatch/base-module instructions, including lifecycle boundaries, hook semantics, skip/block evidence, compact dispatch output, and PAUL/CARL separation.
- `.paul/PROJECT.md` — active constraints: PLAN/APPLY/UNIFY remains authoritative; `.paul/*` artifacts remain lifecycle truth; module overlays dispatch from installed `modules.yaml`; kernel workflows stay platform-agnostic; CARL remains architecturally independent from PALS workflow hooks.
- `docs/PALS-MODULE-INSTRUCTION-AUDIT-CONTRACT.md` — Phase 273 contract: Phase 274 must begin with compact audit + proposed file/section order, include blocking approval checkpoint before edits, proceed section-by-section, re-read changed sections, and record SUMMARY evidence.

## Direct Prior Work
- `.paul/phases/273-module-instruction-audit-contract-cohort-map/273-01-SUMMARY.md` — confirms Phase 273 produced the v2.64 contract/cohort map and scoped Phase 274 to shared/base module surfaces.

## Source Files
- `docs/MODULE-SPEC.md` — formal module manifest/lifecycle hook contract, hook data flow, blocking behavior, no-op behavior, filesystem layout, install/runtime expectations.
- `docs/MODULE-BUILDERS-GUIDE.md` — module-authoring guide, manifest examples, hook registration guidance, ref-file guidance, builder workflow.
- `kernel/references/module-dispatch.md` — installed registry resolution, dispatch procedure, hook taxonomy, evidence-line convention, durable evidence destinations, and project-config vs installed-registry boundary.

## Deferred Phase 274 Surfaces
These are in Phase 274 scope but intentionally deferred from this first plan to keep the rewrite bounded:
- `kernel/references/evidence-discipline.md`
- `kernel/references/context-management.md`
- `kernel/references/specialized-workflow-integration.md`
- `kernel/workflows/plan-phase.md`
- `kernel/workflows/apply-phase.md`
- `kernel/workflows/unify-phase.md`
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: modules.yaml loaded from installed PALS registry; kernel_version 2.0.0 matches pals.json schema_version 2.0.0.

- ARCH: Planned files are documentation/reference surfaces under `docs/` and `kernel/references/`, matching the base-module/dispatch instruction layer. No runtime architecture boundary change is planned; watch for lifecycle-authority drift between docs and kernel workflow ownership.
- SETH: No secret/auth/input handling surface is modified. Security relevance is instruction-level only: avoid adding examples that encourage direct home-directory mutation, hidden state, shell-only secrets handling, or unsafe runtime assumptions.
- TODD: Plan remains `execute`, not TDD. No test-first code implementation is planned; validation is focused marker checks plus existing repository validation where applicable.
- IRIS: Review risk is instruction quality: stale `.pals` paths vs Pi-era installed registry wording, possible source-vs-installed authority confusion, vague advisory/enforcement language, and marker-only dispatch evidence. Apply the Phase 273 holistic review questions before edits.
- DAVE: No CI/CD config or deployment surface is modified.
- DOCS: Planned files are public/internal docs and kernel reference docs. README surfacing is not part of this plan unless a later approved Phase 274 plan adds it.
- CODI: skipped — no extractable TS/JS symbols in this markdown/reference-only phase scope.
- RUBY: Preliminary debt is documentation/instruction debt: duplicated hook descriptions, stale install-path language, overlong examples, and possible confusion between workflow call-site authority and shared dispatch mechanics.
- ARIA/GABE/LUKE/DANA/OMAR/PETE/REED/VERA: skipped — no UI/API/data/observability/performance/resilience/privacy runtime files are modified.

[dispatch] pre-plan enforcement: DEAN PASS — no package/dependency files are modified and no root dependency manifest/lockfile is present; dependency risk unchanged.

## Post-Plan Module Dispatch
[dispatch] post-plan: TODD reviewed plan type. No TDD restructuring suggested for this docs/reference rewrite; APPLY should still record validation evidence and WALT quality gate results.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | PLAN/APPLY/UNIFY lifecycle management | ✓ |
| /carl | required | Session boundary monitoring at phase boundaries / mid-phase | automatic |
| TODD | required | APPLY validation discipline | module dispatch recorded |
| WALT | required | APPLY/UNIFY quality gates and validation evidence | module dispatch during APPLY/UNIFY |

**BLOCKING:** Required skills/modules must remain visible in APPLY/UNIFY evidence. No extra user-loaded skill is required before APPLY for this docs/reference rewrite.
</skills>

<acceptance_criteria>

## AC-1: Compact audit and approved rewrite order precede edits
```gherkin
Given Phase 274 must use the Phase 273 collaborative rewrite protocol
When APPLY starts this plan
Then it presents a compact audit of MODULE-SPEC, MODULE-BUILDERS-GUIDE, and module-dispatch plus a proposed file/section order, and no source edit happens before the user approves or revises that order
```

## AC-2: Module spec and builder guide agree on module authority boundaries
```gherkin
Given the module spec and builder guide explain module manifests, refs, hooks, and install/runtime expectations
When their approved sections are rewritten
Then they clearly distinguish source `modules/*` authority from installed `modules.yaml` dispatch authority, preserve `.paul/*` lifecycle truth, require hook-local refs, and avoid stale or harness-specific install-path assumptions unless explicitly contextualized
```

## AC-3: Dispatch reference remains the shared mechanics contract, not lifecycle authority
```gherkin
Given `kernel/references/module-dispatch.md` defines shared dispatch behavior
When its approved sections are rewritten
Then it preserves installed registry authority, advisory vs enforcement taxonomy, compact `[dispatch] ...` evidence, durable evidence destinations, hook-local ref loading, and explicitly leaves workflow-local STOP/DO NOT SKIP behavior with lifecycle call sites
```

## AC-4: Rewrites are section-by-section and semantically verified
```gherkin
Given the user approves a section or file for mutation
When APPLY edits that section
Then APPLY re-reads the changed section, verifies the intended semantics landed, records decision/evidence notes for SUMMARY, and does not perform bulk rewrites or marker-only fixes
```

## AC-5: Plan scope stays documentation/reference-only
```gherkin
Given this plan targets base module docs and the dispatch reference
When APPLY completes
Then only the three planned source files plus routine `.paul/*` lifecycle artifacts changed; no modules, kernel workflows, tests, drivers, dependencies, CI, install scripts, or home-directory installed copies changed
```
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Audit approved base/dispatch surfaces and propose rewrite order</name>
  <files>docs/MODULE-SPEC.md, docs/MODULE-BUILDERS-GUIDE.md, kernel/references/module-dispatch.md</files>
  <action>
    Perform read-only audit first; do not mutate source files in this task.
    - Read only the relevant sections needed to evaluate: manifest/source authority, installed registry authority, hook-local refs, advisory/enforcement semantics, compact dispatch evidence, lifecycle ownership, no-op behavior, and install/runtime path wording.
    - Apply the Phase 273 holistic review prompts: redundant, confusing, stale ceremony, marker-only, contradictory, over-specified, under-specified, shell/harness-specific without need, no longer true, or simply wrong.
    - Produce a compact audit table by file with: section, issue, proposed action, risk, and whether the edit belongs in this plan or should be deferred to a later Phase 274 plan.
    - Propose a concrete file/section edit order. Keep the initial proposed mutation scope to the three planned files only.
    - Do not edit `kernel/workflows/**`, `modules/**`, tests, install scripts, or home-directory installed copies.
  </action>
  <verify>Before checkpoint, confirm the audit names all three planned files, identifies source-vs-installed authority and advisory/enforcement semantics, and proposes a file/section order with any deferred Phase 274 surfaces clearly listed.</verify>
  <done>AC-1 partially satisfied: audit and proposed order are ready for approval before edits.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Approve the Phase 274 Plan 274-01 rewrite order and initial section scope before source edits.</decision>
  <context>Phase 273 requires blocking approval before later module rewrite phases mutate instruction-sensitive sources. This checkpoint prevents bulk rewrites and lets the user narrow or reorder sections after seeing the compact audit.</context>
  <options>
    <option id="approve-order">
      <name>Approve proposed order</name>
      <pros>APPLY can continue with section-by-section edits for the proposed sections in the three planned files.</pros>
      <cons>Any missed section-order issue must be corrected in a later approved checkpoint or plan.</cons>
    </option>
    <option id="revise-order">
      <name>Revise order or section list</name>
      <pros>User can reorder files, remove sections, or add targeted sections from the three planned files before edits.</pros>
      <cons>APPLY must update the planned edit packet and re-confirm the revised sequence before mutating.</cons>
    </option>
    <option id="narrow-scope">
      <name>Narrow this plan</name>
      <pros>Reduces risk if the audit shows the three-file scope is too large.</pros>
      <cons>Remaining Phase 274 work will require one or more follow-up plans.</cons>
    </option>
  </options>
  <resume-signal>Select `approve-order`, `revise-order`, or `narrow-scope`.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Rewrite approved MODULE-SPEC and builder-guide sections</name>
  <files>docs/MODULE-SPEC.md, docs/MODULE-BUILDERS-GUIDE.md</files>
  <action>
    Edit only sections approved at the checkpoint and only after presenting each section's current excerpt, concrete problem, proposed wording/focused diff, affected semantics, and verification expectation.
    - In `docs/MODULE-SPEC.md`, align manifest/source authority, installed-runtime assumptions, hook-local refs, no-op behavior, context injection/annotation flow, blocking cascade wording, and lifecycle ownership with the Phase 273 contract.
    - In `docs/MODULE-BUILDERS-GUIDE.md`, align builder instructions with the updated spec and dispatch reference: per-hook refs, actionable hook descriptions, advisory/enforcement clarity, no direct installed home-directory edits, and current logical installed registry wording.
    - Remove or narrow stale `.pals`/harness-specific language only where the approved section shows it is obsolete or confusing; preserve historical/contextual examples only when clearly labeled.
    - After each approved section edit, re-read the changed section and record whether the intended semantics landed.
    - Do not rewrite unapproved sections just for style or length.
  </action>
  <verify>Focused read/grep checks confirm edited spec/guide sections distinguish source `modules/*` from installed `modules.yaml`, preserve hook-local refs, avoid uncontextualized stale install paths, and keep lifecycle truth with PAUL/STATE/PLAN/APPLY/UNIFY rather than modules.</verify>
  <done>AC-2 and AC-4 satisfied for approved MODULE-SPEC and MODULE-BUILDERS-GUIDE sections.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite approved dispatch-reference sections and validate scope</name>
  <files>kernel/references/module-dispatch.md</files>
  <action>
    Edit only approved `kernel/references/module-dispatch.md` sections after presenting the current excerpt, concrete problem, proposed wording/focused diff, affected lifecycle/module semantics, and verification expectation.
    - Preserve the reference's role as shared mechanics: installed registry resolution, hook-local ref loading, dispatch ordering, compact evidence lines, durable evidence destinations, and guardrails.
    - Clarify advisory vs enforcement semantics without moving workflow-local blocking/override authority out of PLAN/APPLY/UNIFY call sites.
    - Clarify source `modules/*/module.yaml` vs installed `modules.yaml` authority without changing runtime behavior.
    - Record any remaining Phase 274 follow-up surfaces for SUMMARY: evidence/context/specialized references and kernel workflow call-site wording.
    - Run final changed-file scope review and validation.
  </action>
  <verify>Focused read/grep checks confirm dispatch reference contains installed registry authority, advisory/enforcement taxonomy, `[dispatch]` evidence-line requirements, durable evidence destinations, hook-local ref loading, and lifecycle-call-site authority. `git diff --check` is clean. Run `bash tests/helpers/artifact_consistency.sh` and, if no executable/kernel workflow behavior changed, either run Pi/cross-harness suites or record a docs/reference scoped skip with rationale.</verify>
  <done>AC-3, AC-4, and AC-5 satisfied: dispatch reference semantics are aligned and changed-file scope is bounded.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `modules/**` — individual module manifests/references/workflows are handled in Phases 275-277.
- `kernel/workflows/**` — kernel call-site wording is Phase 274 scope but intentionally deferred from this first plan unless a blocker proves a tiny call-site note is required; if so, stop and ask before expanding scope.
- `kernel/references/evidence-discipline.md`, `kernel/references/context-management.md`, `kernel/references/specialized-workflow-integration.md` — Phase 274 shared surfaces deferred to a follow-up plan.
- `tests/**` — semantic guardrail implementation belongs to Phase 278 unless the approved edit introduces a direct existing-test failure that must be repaired.
- `drivers/**`, install/uninstall scripts, package/dependency files, CI configuration, and runtime binaries.
- Installed home-directory copies under `~/.pi/agent/skills/pals/*` or any other runtime installation surface.

## SCOPE LIMITS
- This plan rewrites only approved sections in the three planned docs/reference files.
- Do not change module behavior, hook priorities, registry generation, install behavior, validation suites, or workflow execution logic.
- Do not bulk-rewrite files for tone, formatting, or line count alone.
- Do not treat marker phrases as proof; every semantic claim must tie to source wording or validation evidence.
- If the audit shows the scope is too large, use the checkpoint to narrow rather than editing ahead.
</boundaries>

<verification>
Before declaring APPLY complete:
- [ ] Checkpoint audit/order approval is recorded before any source edit.
- [ ] Focused checks confirm `docs/MODULE-SPEC.md`, `docs/MODULE-BUILDERS-GUIDE.md`, and `kernel/references/module-dispatch.md` preserve source vs installed authority boundaries.
- [ ] Focused checks confirm hook-local refs and compact `[dispatch] ...` evidence remain explicit.
- [ ] Focused checks confirm advisory modules cannot block and enforcement blocking remains evidence-based and workflow-owned.
- [ ] Re-read evidence exists for each changed section/file.
- [ ] `git diff --check` is clean.
- [ ] `bash tests/helpers/artifact_consistency.sh` runs, or any failure is diagnosed and recorded.
- [ ] Pi/cross-harness validation suites are run, or a docs/reference scoped skip is explicitly justified because no executable workflow behavior changed.
- [ ] Changed-file review confirms no files outside the three planned files plus routine `.paul/*` lifecycle artifacts changed.
</verification>

<success_criteria>
- The first Phase 274 plan has an approved compact audit and file/section order before mutation.
- MODULE-SPEC and MODULE-BUILDERS-GUIDE align on source authority, installed dispatch authority, hook-local refs, lifecycle ownership, and current install/runtime wording.
- `kernel/references/module-dispatch.md` remains the shared mechanics contract and does not take over lifecycle call-site authority.
- Future Phase 274 follow-up surfaces are clearly deferred for another plan instead of silently skipped.
- No module/runtime behavior changes in this docs/reference rewrite.
</success_criteria>

<output>
After completion, create `.paul/phases/274-base-module-dispatch-instruction-rewrite/274-01-SUMMARY.md` with:
- files changed and approved sections edited
- audit/order checkpoint outcome
- source-vs-installed authority alignment summary
- advisory/enforcement and dispatch-evidence alignment summary
- deferred Phase 274 surfaces and recommended next plan
- validation commands/results or scoped rationale
- module execution reports
- deviations from this PLAN, if any
</output>
