---
phase: 284-plannotator-integration-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/PALS-PLANNOTATOR-UI-INTEGRATION-CONTRACT.md
autonomous: true
---

<objective>
## Goal

Produce the authoritative integration contract `docs/PALS-PLANNOTATOR-UI-INTEGRATION-CONTRACT.md` that pins exactly what PALS does and does not consume from `@plannotator/pi-extension`, defines the advisory sidecar artifact contracts, documents the non-adoption surface and authority boundaries, and specifies the opt-in mechanism — so Phases 285–287 implement against a fixed contract rather than the upstream README.

## Purpose

Establishes the contract-over-research authority hierarchy (v2.65 lineage) for v2.66 Plannotator Integration. PROJECT.md value: enhance human plan/code review with rich UI annotations while keeping `.paul/*` lifecycle authority intact. The contract is the single source of truth Phases 285 (bridge), 286 (plan-review workflow), and 287 (code-review workflow) implement against; the upstream Plannotator README, source, and event payloads remain provenance/background, not authoritative for PALS behavior.

## Output

- New file: `docs/PALS-PLANNOTATOR-UI-INTEGRATION-CONTRACT.md` (authoritative contract)
- Validated against actual `apps/pi-extension/` source in the `backnotprop/plannotator` repository (Pi >= 0.53.0 required; PALS project on Pi 0.74 satisfies this)
- No source code changes
- No `.paul/*` lifecycle authority changes
- No kernel changes
- No module-registry changes
</objective>

<context>
## Project Context

.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md

## Prior Work

- Phase 279 Plan 279-01 — established the contract-over-research authority hierarchy for v2.65 Functional-First Agent Guidance; same shape reused here (contract is authoritative, external research is provenance).
- v2.63 — section-by-section workflow rewrite pattern; informs the surface area Phases 286–287 will touch (referenced by the contract but not changed in Phase 284).
- v2.65 — installed-resource marker check pattern; informs Phase 285 marker-check planning (referenced by the contract).
- CODI v0.1 — advisory module surface shape (opt-in, advisory, graceful-when-absent); the Plannotator integration mirrors this shape for graceful absence and opt-in.

## Source Files

# No source files modified in this phase. Investigation reads the following EXTERNAL upstream sources
# as provenance for the contract; these are NOT in this repo and NOT modified:
#
# - github.com/backnotprop/plannotator — apps/pi-extension/README.md (already inspected during discussion)
# - github.com/backnotprop/plannotator — apps/pi-extension/package.json (already inspected)
# - github.com/backnotprop/plannotator — apps/pi-extension/src/* (event delivery, result schema, savedPath)
# - github.com/backnotprop/plannotator — apps/hook/, apps/marketing/ (referenced for context only)
</context>

<module_dispatch>
## Pre-Plan Module Dispatch

[dispatch] pre-plan advisory: 16 modules registered for this hook

| Module | Status | Notes |
|--------|--------|-------|
| ARCH   | SKIP   | docs-only; no architecture pattern applicable |
| ARIA   | SKIP   | no UI files in scope |
| CODI   | SKIP   | markdown-only phase; no `codi_seed_candidates` |
| DANA   | SKIP   | no data files in scope |
| DAVE   | SKIP   | no CI/CD/deploy config or source-code build changes |
| DOCS   | N/A    | DOCS: Documentation-only scope — drift check not applicable |
| GABE   | SKIP   | no API endpoint files |
| IRIS   | N/A    | new file; no existing TODO/FIXME/HACK/XXX markers to count |
| LUKE   | SKIP   | no UI files in scope |
| OMAR   | SKIP   | OMAR: skipped — no relevant paths |
| PETE   | SKIP   | no performance-sensitive paths |
| REED   | SKIP   | no resilience-pattern paths |
| RUBY   | N/A    | RUBY: No preliminary debt indicators in planned files. |
| SETH   | SKIP   | markdown-only; no secret literal / authz / injection surface |
| TODD   | SKIP   | no test framework changes; no business logic; markdown-only contract |
| VERA   | SKIP   | no PII fields / privacy markers |

[dispatch] pre-plan enforcement: 1 module registered for this hook

| Module | Status | Notes |
|--------|--------|-------|
| DEAN   | PASS   | no dependency manifest changes in scope |

No blocking findings. Shape matches v2.65 contract-authoring phases (Phase 279 had the same all-SKIP/advisory pattern).
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

<!-- .paul/SPECIAL-FLOWS.md exists; this is a docs/contract phase with no specialized flows triggered. -->

No specialized flows triggered for this phase (contract-authoring is not a SPECIAL-FLOWS-tracked work type).
</skills>

<acceptance_criteria>

## AC-1: Contract file exists at canonical path with authoritative status

```gherkin
Given v2.66 Phase 284 is ready to plan
When Plan 284-01 completes APPLY
Then `docs/PALS-PLANNOTATOR-UI-INTEGRATION-CONTRACT.md` exists
 And the file contains a top-level "Status" or "Authority" section declaring it authoritative for what PALS consumes from `@plannotator/pi-extension`
 And it declares the upstream Plannotator README, source, and event payloads as provenance/background (non-authoritative for PALS behavior)
 And it explicitly references the v2.65 contract-over-research lineage
```

## AC-2: Event surface, result protocol, and async race recovery are pinned

```gherkin
Given the contract is read by a Phase 285 implementer
When they look up "what event channel does PALS emit on" and "what fields does PALS consume from the result"
Then the contract documents:
   - The exact `plannotator:request` channel name and supported action `plan-review` and `code-review` payload shapes that PALS uses
   - The exact `plannotator:review-result` channel name and the result fields PALS consumes: `reviewId`, `approved`, `feedback`, `savedPath?`
   - The async result protocol: emit-listen race recovery via `review-status` query (documented in upstream README) and how PALS handles it
   - A documented timeout / UI-abandoned policy (decision recorded; specific value may be parameterized)
 And both consumed and ignored result fields are listed exhaustively
```

## AC-3: Non-adoption surface, authority hierarchy, advisory sidecars, opt-in mechanism, validation classification, and failure modes are documented

```gherkin
Given the contract is read by any Phase 285–287 implementer or reviewer
When they look up "what PALS does NOT consume" and "where Plannotator content lands" and "how integration is enabled"
Then the contract documents:
   - Non-adoption surface: `--plan` mode, `plannotator_submit_plan` tool, internal phase machine, `plannotator.json` per-phase config, `agentSwitch` result field, `permissionMode` result field — each listed with a one-sentence reason for non-adoption
   - Authority hierarchy: (1) this contract → (2) `.paul/*` artifacts → (3) Plannotator README/source → (4) `*-REVIEW-NOTES.md` and `PLAN-FEEDBACK-{iteration}.md` advisory sidecars
   - Advisory sidecar contracts: file paths under `.paul/phases/N/`, when they are written, by which workflow, what fields they carry, and that they are never lifecycle truth
   - `savedPath` policy: advisory input to a PAUL-driven re-plan; never directly adopted as PLAN.md
   - Opt-in mechanism: `pals.json` `integrations.plannotator.enabled` (project-level enable) AND per-invocation user prompt at each review surface (never auto-launched, never config-driven default-on)
   - Validation classification (Phase 196 / v2.51 taxonomy): helper delegation + Pi-supported runtime; Pi-only milestone; cross-harness retired
   - Failure modes: extension absent → graceful skip + install nudge; user declines per-invocation prompt → zero overhead; UI closed without submitting → documented abandonment behavior
   - Pi version requirement: >= 0.53.0 (project on 0.74; no pinning work needed)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Investigate upstream Plannotator Pi-extension to resolve 8 open contract questions</name>
  <files>(read-only; produces investigation notes consumed by Task 2)</files>
  <action>
    Read the upstream `backnotprop/plannotator` repository (via fetch_content + raw.githubusercontent.com URLs, no clone needed) — specifically `apps/pi-extension/` — to resolve the contract's open questions. Required investigations:

    1. Event delivery mechanism: confirm `plannotator:request` is Pi's extension event bus (vs Node EventEmitter / IPC / other). Read the Pi extension's main source file(s) to identify the import + emit pattern PALS must mirror.
    2. Async result race recovery: confirm `review-status` query pattern semantics — request shape, response shape, when it's safe vs racy.
    3. Extension-presence probe: identify how a peer Pi extension can detect whether `@plannotator/pi-extension` is loaded (e.g., feature flag in Pi's extension registry, presence of a known exported symbol, listener probe). Document the chosen detection strategy and its trade-offs.
    4. Exact result schema: extract the TypeScript types or zod schema for `plannotator:review-result`. Confirm exact field names, optionality, and types for `reviewId`, `approved`, `feedback`, `savedPath`, `agentSwitch`, `permissionMode`. The README is suggestive; the contract must cite source.
    5. `savedPath` payload semantics: is the file at `savedPath` a serialized annotation overlay (deletions + insertions + comments as structured edits), a rendered final markdown, or a Plannotator-internal format? Read the relevant save/serialization code.
    6. Multi-iteration behavior: when PALS re-emits `plan-review` for the same plan after a re-plan, does Plannotator create a fresh `reviewId`, reuse, or surface plan-diff against prior versions? Read the relevant route/handler.
    7. UI-abandoned handling: what happens if the user closes the browser without submitting? Does the result event fire with an "abandoned" status, or does it never fire? Document the actual behavior and PALS's abandonment policy (suggest: PALS sets a configurable timeout; on timeout, treat as `{ approved: false, feedback: "review abandoned (timeout)" }` and route the same as deny — or document why a different policy is correct).
    8. `/plannotator-annotate` namespace check: confirm Plannotator's own `/plannotator-*` command set so the contract can explicitly say "PALS does NOT add `/paul:annotate`; users use `/plannotator-annotate <path>` directly per Plannotator docs."

    Capture findings as a bounded section in the contract draft (do NOT write a separate notes file; investigation evidence lives inline in the contract under a "Source Evidence" subsection per AC-2/AC-3 topic).

    AVOID: cloning the entire upstream repo; reading apps/marketing or apps/codex; speculating where source evidence is unclear. If source is unclear, the contract documents the unknown and recommends a Phase 285-time empirical probe rather than guessing.
  </action>
  <verify>
    grep -E "Source Evidence|raw.githubusercontent.com|apps/pi-extension/src" docs/PALS-PLANNOTATOR-UI-INTEGRATION-CONTRACT.md
    (Should show source-citation evidence inline; at least the 8 investigation questions answered or explicitly marked "unresolved, deferred to Phase 285 empirical probe".)
  </verify>
  <done>AC-2 partially satisfied (event surface, result protocol, async race); investigation evidence cited inline in the contract.</done>
</task>

<task type="auto">
  <name>Task 2: Author the authoritative contract document</name>
  <files>docs/PALS-PLANNOTATOR-UI-INTEGRATION-CONTRACT.md</files>
  <action>
    Create `docs/PALS-PLANNOTATOR-UI-INTEGRATION-CONTRACT.md` with the following sections (in this order):

    1. **Status** — Authoritative for what PALS consumes from `@plannotator/pi-extension` after Phase 284 UNIFY. References v2.65 contract-over-research lineage (Phase 279). Last updated, kernel version, PALS version pointer.

    2. **Scope** — What this contract covers (event surface PALS emits/consumes, sidecar artifact contracts, non-adoption, opt-in, validation classification, failure modes) and what it does NOT cover (Plannotator's internal implementation details, UI behavior beyond what PALS depends on, Plannotator's evolution after this contract is authored).

    3. **Authority Hierarchy** — Numbered list as defined in MILESTONE-CONTEXT.md additional context: (1) this contract → (2) `.paul/*` artifacts → (3) Plannotator README/source → (4) `*-REVIEW-NOTES.md` and `PLAN-FEEDBACK-{iteration}.md` advisory sidecars.

    4. **Consumed Event Surface** — Exact event channel names, action names, payload shapes, and result fields PALS consumes. Includes source-evidence citations from Task 1. Documents `plan-review`, `code-review` request payloads; `plannotator:review-result` response payload; `review-status` query for race recovery.

    5. **Non-Adoption Surface** — Exhaustive list with one-sentence reason each: `--plan` mode, `plannotator_submit_plan` tool, internal phase machine (`idle → planning → executing`), `plannotator.json` per-phase config, `agentSwitch` result field, `permissionMode` result field, Plannotator's `/plannotator-annotate` is used directly without a PALS wrapper (YAGNI per discussion).

    6. **Advisory Sidecar Contracts** — For each of `.paul/phases/N/PLAN-REVIEW-NOTES.md`, `.paul/phases/N/PLAN-FEEDBACK-{iteration}.md`, `.paul/phases/N/CODE-REVIEW-NOTES.md`:
       - Canonical file path pattern
       - Which workflow writes it (PLAN / APPLY / UNIFY)
       - When it is written (approve+feedback / deny / code-review feedback)
       - Schema: feedback text, Plannotator reviewId, ISO timestamp, optional savedPath reference, iteration number where applicable
       - Lifecycle status: advisory evidence only; never lifecycle truth; never read by `.paul/STATE.md` as authoritative

    7. **`savedPath` Policy** — Documented decision: advisory input only. PAUL re-derives PLAN.md from feedback through a re-plan turn; Plannotator-edited file at `savedPath` is read as advisory input alongside `feedback` but never directly becomes PLAN.md. Source-evidence reference for `savedPath` payload semantics.

    8. **Opt-In Mechanism** — Two layers:
       - Project-level: `pals.json` `integrations.plannotator.enabled: boolean` (default `false`); optional `integrations.plannotator.surfaces: ("plan-review"|"code-review"|"both"|"none")` for finer-grained control (default if enabled: `"both"`)
       - Per-invocation: each PAUL workflow that has a review surface asks the user "Run Plannotator review? [y/n]". Never auto-launched. Never config-driven default-on (config can disable, not auto-confirm).
       - Declining either layer costs zero overhead; workflow proceeds exactly as today.

    9. **Validation Classification** — Phase 196 / v2.51 taxonomy: helper delegation + Pi-supported runtime. Pi-only milestone. Cross-harness retired (project decision before v2.66). Pi version requirement >= 0.53.0; project on 0.74.

    10. **Failure Modes** — Extension absent (graceful skip + install nudge, parallel to CODI's missing-codegraph pattern); user declines per-invocation prompt (zero overhead); UI closed without submitting (documented abandonment behavior per Task 1 finding); event-emit-before-listener race (`review-status` recovery path per Task 1 finding).

    11. **Feedback Incorporation Protocol** — When Plannotator returns `{ approved: false, feedback, savedPath? }`:
        - PAUL writes `.paul/phases/N/PLAN-FEEDBACK-{iteration}.md` with feedback text, savedPath contents (if any) as advisory input, ISO timestamp, iteration number
        - PAUL re-enters `/paul:plan` with the feedback file as required reading
        - PAUL regenerates PLAN.md (PALS retains absolute authority; savedPath is never directly adopted)
        - Each iteration gets its own feedback file (audit trail)
        - After approval, feedback files remain as evidence in the phase directory
        - When `{ approved: true, feedback: "..." }` (approve-with-feedback), PAUL writes `PLAN-REVIEW-NOTES.md` instead and proceeds to APPLY; APPLY workflow reads `PLAN-REVIEW-NOTES.md` if present (Phase 286 implements this).

    12. **Architectural Lineage** — Sibling Pi-extension extraction recipe (v2.56–v2.61, 9th ratification of loader-compat invariant in Phase 285); section-by-section workflow rewrite pattern (v2.63 in Phases 286–287); installed-resource marker check pattern (v2.65 in Phase 285); contract-over-research authority hierarchy (v2.65 Phase 279, mirrored here).

    13. **Out of Scope for v2.66** — Mirror MILESTONE-CONTEXT.md out-of-scope list verbatim: non-PAUL markdown plan review, automatic routing to `/paul:fix`, cross-harness driver support, Stage CLI integration, Validation Hygiene (v2.67 candidate), REV/Plannotator merger, `.paul/*` authority modification, adoption of Plannotator's `--plan` mode / phase machine / `plannotator_submit_plan` / `plannotator.json`.

    14. **Source Evidence** — Inline citations under each topic referencing upstream raw GitHub URLs investigated in Task 1. Where source was unclear, mark explicitly "unresolved; deferred to Phase 285 empirical probe" rather than speculating.

    Use Markdown; use the existing `docs/PALS-*-CONTRACT.md` files (e.g., `docs/PALS-FUNCTIONAL-FIRST-AGENT-GUIDANCE-CONTRACT.md` from v2.65) as the style reference for headings, code-block conventions, and section ordering — but only mirror style, do not copy substance.

    AVOID: declaring decisions that should be deferred to Phase 285 (e.g., specific timeout value, specific bridge function signatures — those belong in Phase 285 PLAN); guessing where source is unclear; importing Plannotator's `--plan` mode workflow or `plannotator.json` config schema; adding any change to `.paul/*` artifact authority; adding any new module manifest entry (Plannotator is integration, not module); adding any new `/paul:*` command (per YAGNI decision).
  </action>
  <verify>
    test -f docs/PALS-PLANNOTATOR-UI-INTEGRATION-CONTRACT.md \
      && grep -c "^## " docs/PALS-PLANNOTATOR-UI-INTEGRATION-CONTRACT.md | awk '$1 >= 12 {exit 0} {exit 1}' \
      && grep -E "Status|Authority|Non-Adoption|Advisory Sidecar|savedPath|Opt-In|Validation Classification|Failure Modes|Feedback Incorporation|Architectural Lineage|Out of Scope|Source Evidence" docs/PALS-PLANNOTATOR-UI-INTEGRATION-CONTRACT.md
  </verify>
  <done>AC-1 satisfied; AC-3 satisfied; AC-2 fully satisfied (event surface + result protocol + async race recovery all pinned with source citations).</done>
</task>

<task type="auto">
  <name>Task 3: Cross-check contract against MILESTONE-CONTEXT.md, non-adoption commitments, and v2.65 lineage</name>
  <files>docs/PALS-PLANNOTATOR-UI-INTEGRATION-CONTRACT.md (read; no further edits unless gaps found)</files>
  <action>
    Read the freshly authored contract against (a) `.paul/STATE.md` Resume context section (which captures the discussion commitments) and (b) `.paul/ROADMAP.md` v2.66 phase descriptions. Verify the contract documents:

    1. Every non-adoption item from the discussion: `--plan` mode, `plannotator_submit_plan`, phase machine, `plannotator.json`, `agentSwitch`, `permissionMode`, no PALS-wrapper command for ad-hoc annotation (YAGNI).
    2. Every advisory sidecar from MILESTONE-CONTEXT: `PLAN-REVIEW-NOTES.md`, `PLAN-FEEDBACK-{iteration}.md`, `CODE-REVIEW-NOTES.md`.
    3. The opt-in posture: per-invocation prompt + project-level `pals.json` toggle; never auto-launched; declining is zero overhead.
    4. The authority hierarchy explicitly numbered (4 layers).
    5. The Phase 285–287 hand-off shape: bridge helper signatures named but NOT specified (Phase 285 owns that), workflow edits scoped but NOT drafted (Phases 286–287 own those).
    6. Out-of-scope items verbatim from MILESTONE-CONTEXT.
    7. Pi 0.53.0 minimum + project-on-0.74 fact.
    8. v2.65 contract-over-research lineage reference.

    If any item is missing or contradictory with the MILESTONE-CONTEXT commitments, return to Task 2 and add/fix it. Otherwise, no further edits.

    AVOID: adding NEW commitments that weren't in the discussion (e.g., introducing a new module name, a new sidecar filename, a new pals.json key not previously agreed). The contract pins discussion decisions; it does not expand scope.
  </action>
  <verify>
    # Cross-check non-adoption items
    grep -E "plan mode|plannotator_submit_plan|phase machine|plannotator\.json|agentSwitch|permissionMode" docs/PALS-PLANNOTATOR-UI-INTEGRATION-CONTRACT.md | wc -l | awk '$1 >= 6 {exit 0} {exit 1}'
    # Cross-check sidecar files
    grep -E "PLAN-REVIEW-NOTES\.md|PLAN-FEEDBACK-\{iteration\}\.md|CODE-REVIEW-NOTES\.md" docs/PALS-PLANNOTATOR-UI-INTEGRATION-CONTRACT.md | wc -l | awk '$1 >= 3 {exit 0} {exit 1}'
    # Cross-check opt-in mechanism
    grep -E "integrations\.plannotator\.enabled|per-invocation" docs/PALS-PLANNOTATOR-UI-INTEGRATION-CONTRACT.md
    # Cross-check Pi version + v2.65 lineage
    grep -E "0\.53|0\.74|v2\.65|Phase 279|contract-over-research" docs/PALS-PLANNOTATOR-UI-INTEGRATION-CONTRACT.md
  </verify>
  <done>AC-1, AC-2, AC-3 jointly satisfied; contract is internally consistent with MILESTONE-CONTEXT discussion commitments; no scope expansion beyond Phase 284.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE

- `.paul/*` artifacts (Phase 284 is a docs-only contract authoring phase; STATE/ROADMAP updates happen during `update_state` step, not within plan tasks)
- `kernel/**` (no kernel changes)
- `kernel/modules.yaml` (Plannotator is an integration, not a module; no module-registry entry)
- `pals.json` (the contract DOCUMENTS the `integrations.plannotator.*` opt-in keys but does NOT modify `pals.json` in Phase 284; Phases 285+ may add defaults; per-project enabling is user-driven)
- `drivers/pi/extensions/**` (no Pi extension scaffolding; that's Phase 285)
- Any `/paul:*` skill or workflow instruction file (Phases 286–287 own workflow edits)
- Any existing `docs/PALS-*-CONTRACT.md` file (Phase 284 creates ONE new contract; does not modify existing contracts)

## SCOPE LIMITS

- Phase 284 does NOT write production code
- Phase 284 does NOT add new `/paul:*` commands
- Phase 284 does NOT specify Phase 285 bridge function signatures beyond naming (`requestPlanReview`, `requestCodeReview`, `awaitReviewResult` — names only; signatures, types, and bodies are Phase 285's job)
- Phase 284 does NOT draft Phase 286/287 workflow instruction text beyond scoping which sections will be touched
- Phase 284 does NOT install or pin `@plannotator/pi-extension` as a PALS dependency
- Phase 284 does NOT adopt or document Plannotator's `plannotator.json` configuration model (explicitly non-adopted)
- Phase 284 does NOT add or modify any module manifest entry
- Phase 284 does NOT decide specific timeout values for UI abandonment (records the policy decision; specific numeric value can be parameterized at Phase 285 time)

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `test -f docs/PALS-PLANNOTATOR-UI-INTEGRATION-CONTRACT.md` returns 0
- [ ] Contract has ≥ 12 top-level `## ` sections (covers all required topics)
- [ ] Contract documents all six non-adoption items + YAGNI annotate-wrapper decision
- [ ] Contract documents all three advisory sidecar artifact contracts
- [ ] Contract explicitly numbers the 4-layer authority hierarchy
- [ ] Contract references `pals.json` `integrations.plannotator.enabled` opt-in key AND per-invocation prompt posture
- [ ] Contract cites Pi 0.53.0 minimum + 0.74 project version + v2.65 contract-over-research lineage (Phase 279)
- [ ] Contract has at least one inline "Source Evidence" citation for the upstream raw-GitHub URL it depends on per major topic (event surface, result schema, savedPath semantics, async race recovery, multi-iteration behavior, UI-abandoned behavior)
- [ ] Contract's "Out of Scope" mirrors MILESTONE-CONTEXT verbatim list
- [ ] `git status` shows exactly one new file: `docs/PALS-PLANNOTATOR-UI-INTEGRATION-CONTRACT.md` (plus `.paul/` lifecycle updates from `/paul:apply`)
- [ ] No changes to `kernel/**`, `drivers/**`, `pals.json`, or any existing `docs/PALS-*-CONTRACT.md` file
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All 3 tasks completed
- All 3 acceptance criteria satisfied
- `docs/PALS-PLANNOTATOR-UI-INTEGRATION-CONTRACT.md` exists, is internally consistent with v2.66 MILESTONE-CONTEXT commitments, and is ready to serve as authoritative input to Phases 285–287
- Investigation evidence inline-cited (8 open questions from discussion resolved or explicitly deferred-with-reason)
- No source code changes; no `.paul/*` authority changes; no kernel changes; no Pi-extension scaffolding (all reserved for Phase 285+)
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.paul/phases/284-plannotator-integration-contract/284-01-SUMMARY.md`
</output>
