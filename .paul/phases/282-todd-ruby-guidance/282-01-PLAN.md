---
phase: 282-todd-ruby-guidance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/todd/module.yaml
  - modules/todd/references/tdd.md
  - modules/todd/references/tdd-plan-generation.md
  - modules/todd/references/tdd-execution.md
  - modules/ruby/module.yaml
  - modules/ruby/references/refactor-principles.md
  - modules/ruby/references/refactor-patterns.md
  - tests/pi-end-to-end-validation.sh
  - tests/cross-harness-validation.sh
autonomous: true
---

<objective>
## Goal
Add Phase 282 functional-first guidance to TODD and RUBY so agents preferentially test pure input-to-output logic and can safely extract pure cores during refactoring without changing enforcement semantics or imposing a functional architecture.

## Purpose
Phase 279 established the v2.65 contract: functional-first, not functional-only; Pure Core, Explicit Effects; project conventions remain authoritative. Phase 281 added brownfield local-idiom detection slots. Phase 282 now threads that guidance through the existing TODD and RUBY module surfaces named by the contract.

## Output
- TODD references and hook metadata clarify that pure input-to-output business logic, validation, data transformation, state transitions, algorithms, and decision-making are high-value TDD scope.
- TODD TDD planning/execution guidance encourages testing the pure core directly when behavior can be expressed without side effects, while keeping RED/GREEN/REFACTOR gates unchanged.
- RUBY references and hook metadata add an `Extract Pure Core` refactoring pattern that is incremental, behavior-preserving, test-backed, and respectful of local idioms.
- Pi and cross-harness validation scripts assert installed TODD/RUBY module resources preserve the Phase 282 markers.
</objective>

<context>
## Project Context
.paul/STATE.md — Phase 282 ready to plan after Phase 281 completion and merge.
.paul/ROADMAP.md — v2.65 current milestone; Phase 282 focuses on TODD + RUBY guidance.
.paul/PROJECT.md — v2.65 must add practical “functional-first, not functional-only” guidance without overriding project conventions.

## Prior Work
.paul/phases/279-functional-first-guidance-contract/279-01-SUMMARY.md — established `docs/PALS-FUNCTIONAL-FIRST-AGENT-GUIDANCE-CONTRACT.md` as the implementation boundary.
.paul/phases/281-brownfield-convention-detection/281-01-SUMMARY.md — added source-backed brownfield local-idiom and effect-boundary detection slots that TODD/RUBY guidance should respect.

## Source Files
docs/PALS-FUNCTIONAL-FIRST-AGENT-GUIDANCE-CONTRACT.md — authoritative Phase 282 allowed scope and non-goals.
modules/todd/module.yaml — installed TODD hook metadata and refs.
modules/todd/references/tdd.md — TODD overview and candidate heuristics.
modules/todd/references/tdd-plan-generation.md — TDD plan generation guidance.
modules/todd/references/tdd-execution.md — RED/GREEN/REFACTOR execution guidance.
modules/ruby/module.yaml — installed RUBY hook metadata and refs.
modules/ruby/references/refactor-principles.md — RUBY safe refactoring principles.
modules/ruby/references/refactor-patterns.md — RUBY refactoring pattern catalog.
tests/pi-end-to-end-validation.sh — Pi installed-resource validation.
tests/cross-harness-validation.sh — cross-harness installed-resource validation.
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan enforcement: DEAN PASS — planned files are markdown/YAML/shell validation resources only; no dependency manifests or dependency changes in scope.
[dispatch] pre-plan advisory: TODD — existing shell validation scripts are in scope for marker assertions; this plan is `execute`, not TDD, because it changes guidance text rather than product behavior.
[dispatch] pre-plan advisory: RUBY — docs/module-instruction scope is not code debt; no preliminary debt indicators.
[dispatch] pre-plan advisory: DOCS — controlling related doc is `docs/PALS-FUNCTIONAL-FIRST-AGENT-GUIDANCE-CONTRACT.md`; this plan updates module guidance surfaces named by that contract.
[dispatch] pre-plan advisory: IRIS — planned readable files should be checked for review markers only; no TODO/FIXME/HACK/XXX scope intended.
[dispatch] pre-plan advisory: DAVE — no CI config changes; validation remains local shell-script based (`tests/pi-end-to-end-validation.sh`, `tests/cross-harness-validation.sh`, targeted marker checks).
[dispatch] pre-plan advisory: ARCH/GABE/DANA/LUKE/ARIA/OMAR/REED/VERA/PETE — skipped; no runtime architecture, API, data, UI, observability, resilience, privacy, or performance implementation paths in scope.
[dispatch] pre-plan advisory: CODI skipped — markdown/YAML/shell-only phase; no explicit TS/JS selectors or stable codegraph seeds.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | PLAN/APPLY/UNIFY lifecycle management | ✓ |
| /carl | required | Session boundary monitoring | automatic |
| TODD | required | APPLY test/verification discipline | via module dispatch |
| WALT | required | APPLY quality gates and validation | via module dispatch |

**APPLY note:** Required lifecycle/module skills must remain active; no extra user-loaded specialized flow is required for this docs/module-instruction/test-shell plan.
</skills>

<acceptance_criteria>
## AC-1: TODD Pure-Logic TDD Guidance
```gherkin
Given TODD detects or structures TDD work
When a feature's behavior can be expressed as pure input-to-output logic
Then TODD guidance identifies it as high-value TDD scope and encourages direct pure-core tests without requiring side effects, framework calls, I/O, or architecture conversion
```

## AC-2: RUBY Extract Pure Core Refactoring Guidance
```gherkin
Given RUBY reports or documents refactoring opportunities
When code mixes decisions/data transformation with side effects
Then RUBY guidance includes an Extract Pure Core pattern that is incremental, behavior-preserving, test-backed, and advisory rather than mandatory
```

## AC-3: Installed Resources Preserve TODD/RUBY Functional-First Markers
```gherkin
Given Pi and cross-harness installers copy module resources
When validation runs after APPLY
Then installed TODD and RUBY resources contain the Phase 282 markers for pure input-to-output TDD scope and Extract Pure Core refactoring guidance
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Add pure-logic guidance to TODD references and hook metadata</name>
  <files>modules/todd/module.yaml, modules/todd/references/tdd.md, modules/todd/references/tdd-plan-generation.md, modules/todd/references/tdd-execution.md</files>
  <action>
    Update TODD guidance within existing TDD semantics:
    - In `tdd.md`, clarify that pure input-to-output business logic, validation, data transformations, state transitions, algorithms, and decision-making are high-value TDD candidates.
    - Add concise Pure Core, Explicit Effects language: test the pure core directly when behavior can be expressed without side effects; keep I/O/framework/database/filesystem/logging in a thin shell or boundary where project style permits.
    - In `tdd-plan-generation.md`, add planning guidance to describe pure behavior cases as explicit inputs → outputs and to pass state/dependencies explicitly in the feature spec when practical.
    - In `tdd-execution.md`, add RED/GREEN/REFACTOR guidance that RED tests should prefer observable pure behavior over mock-heavy side-effect assertions when an extracted/present pure core exists.
    - In `modules/todd/module.yaml`, update relevant hook descriptions/refs so installed metadata preserves the new pure-logic TDD markers.
    Avoid: changing TODD blocking/enforcement semantics, requiring TDD for all code, adding new hooks, adding dependencies, or making pure-core extraction mandatory.
  </action>
  <verify>Native grep/read checks show `pure input-to-output`, `Pure Core, Explicit Effects`, `test the pure core directly`, and `project conventions remain authoritative` (or equivalent local-idiom preservation wording) in TODD files; `modules/todd/module.yaml` contains installed metadata markers for pure logic TDD guidance.</verify>
  <done>AC-1 satisfied: TODD guidance identifies pure behavior as high-value TDD scope without changing enforcement semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Add Extract Pure Core guidance to RUBY references and hook metadata</name>
  <files>modules/ruby/module.yaml, modules/ruby/references/refactor-principles.md, modules/ruby/references/refactor-patterns.md</files>
  <action>
    Update RUBY guidance within existing advisory refactoring semantics:
    - Add an `Extract Pure Core` pattern to `refactor-patterns.md` for code where business rules, validation, decisions, or data transformations are mixed with I/O, persistence, network calls, filesystem/process/env access, logging, timers, randomness, or framework callbacks.
    - Define the pattern as small-step and behavior-preserving: characterize existing behavior first, extract deterministic logic into a named helper/module/function, pass dependencies/state/time/random/config as explicit inputs where practical, leave side effects in the shell/boundary, and rerun tests after each step.
    - Update `refactor-principles.md` to mention Pure Core, Explicit Effects as an optional refactoring target only when it reduces coupling and improves testability.
    - Update `modules/ruby/module.yaml` so installed metadata/refs surface Extract Pure Core guidance while preserving docs-only/module-instruction `NOT_APPLICABLE` behavior.
    Avoid: broad refactoring, requiring functional programming, forcing class/OOP rewrites, creating blocking RUBY behavior, or replacing readable imperative code with clever functional chains.
  </action>
  <verify>Native grep/read checks show `Extract Pure Core`, `Pure Core, Explicit Effects`, `behavior-preserving`, `test-backed`, and `side effects` markers in RUBY files; `modules/ruby/module.yaml` contains installed metadata markers for Extract Pure Core guidance.</verify>
  <done>AC-2 satisfied: RUBY documents safe advisory pure-core extraction without making it mandatory.</done>
</task>

<task type="auto">
  <name>Task 3: Add installed-resource validation markers and run verification</name>
  <files>tests/pi-end-to-end-validation.sh, tests/cross-harness-validation.sh</files>
  <action>
    Add validation assertions near existing module/installed-resource guardrails:
    - Pi validation should assert installed TODD resources preserve pure input-to-output / pure-core TDD markers and installed RUBY resources preserve Extract Pure Core markers.
    - Cross-harness validation should assert both Claude Code and Pi installed module resources preserve the same TODD/RUBY markers.
    - Prefer semantic grouped marker checks where helpers are already sourced; otherwise use existing `tap_file_contains_all` / `mis_file_has_all` patterns.
    - Run focused marker checks, shell syntax checks, artifact consistency helper, and the two broader validation suites.
    Avoid: weakening existing counts/checks, adding external dependencies, or treating inherited CODI/PROJECT/artifact-consistency baseline failures as Phase 282 regressions when new markers pass.
  </action>
  <verify>`git diff --check`; `bash -n tests/pi-end-to-end-validation.sh tests/cross-harness-validation.sh`; `bash tests/helpers/artifact_consistency.sh`; `bash tests/pi-end-to-end-validation.sh`; `bash tests/cross-harness-validation.sh`; focused marker checks for all planned TODD/RUBY files.</verify>
  <done>AC-3 satisfied and verification evidence is ready for `282-01-SUMMARY.md`.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- `docs/PALS-FUNCTIONAL-FIRST-AGENT-GUIDANCE-CONTRACT.md` — authoritative contract from Phase 279; cite it but do not rewrite it.
- `kernel/workflows/map-codebase.md` and `kernel/templates/codebase/*` — Phase 281 scope already complete.
- `kernel/templates/AGENTS.md` and init workflow generated Engineering Principles — Phase 280 scope already complete.
- `modules/arch/*` — Phase 283 optional advisory/closure scope.
- Runtime source code, package manifests, dependency locks, CI configs, and generated `.paul/codebase/` outputs.

## SCOPE LIMITS
- No new modules, hooks, dependencies, linters, formatters, or runtime tooling.
- No new default blocking gate for functional style.
- No broad refactoring of source code; this plan updates guidance and validation only.
- Preserve project-specific conventions as authoritative; guidance must be advisory and local-idiom-aware.
- Keep changes to TODD/RUBY module resources and validation marker checks named in this plan.
</boundaries>

<verification>
Before declaring plan complete:
- [ ] TODD files contain pure input-to-output and Pure Core, Explicit Effects TDD guidance.
- [ ] RUBY files contain Extract Pure Core and behavior-preserving/test-backed guidance.
- [ ] `tests/pi-end-to-end-validation.sh` contains Pi installed-resource marker assertions for TODD/RUBY Phase 282 markers.
- [ ] `tests/cross-harness-validation.sh` contains Claude Code + Pi marker assertions for TODD/RUBY Phase 282 markers.
- [ ] `git diff --check` result captured.
- [ ] `bash -n tests/pi-end-to-end-validation.sh tests/cross-harness-validation.sh` result captured.
- [ ] `bash tests/helpers/artifact_consistency.sh` result captured.
- [ ] `bash tests/pi-end-to-end-validation.sh` result captured.
- [ ] `bash tests/cross-harness-validation.sh` result captured.
- [ ] All acceptance criteria met.
</verification>

<success_criteria>
- TODD guidance highlights pure input-to-output behavior as high-value TDD scope.
- RUBY guidance includes safe Extract Pure Core refactoring advice.
- Guidance is advisory, behavior-preserving, test-backed, and project-convention-aware.
- Installed-resource validation guards the new TODD/RUBY markers.
- Phase 283 ARCH scope remains untouched.
</success_criteria>

<output>
After completion, create `.paul/phases/282-todd-ruby-guidance/282-01-SUMMARY.md` with changed files, validation results, module dispatch outcomes, inherited baseline notes, and any deviations from this plan.
</output>
