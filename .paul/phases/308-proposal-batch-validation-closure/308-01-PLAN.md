---
phase: 308-proposal-batch-validation-closure
plan: 01
type: execute
wave: 1
depends_on: ["307-02"]
files_modified:
  - .paul/proposals/v2.73-field-harvest-proposal-batch.md
  - .paul/proposals/v2.73-recurring-advisory-finding-lineage.md
  - .paul/proposals/v2.73-review-finding-class-escalation.md
  - .paul/proposals/v2.73-validation-suite-decomposition.md
  - tests/helpers/field_harvest.sh
  - tests/pi-end-to-end-validation.sh
  - tests/cross-harness-validation.sh
autonomous: true
collaboration_level: high
planning_mode: direct-requirements
review_preference: quick-recap
---

<objective>
## Goal

Turn Phase 307's three-deployment harvest into a complete, source-cited, human-routed framework-improvement proposal batch; guard the evidence and authority boundaries with TAP checks; and leave v2.73 green and ready for UNIFY plus milestone closure.

## Purpose

The 375 normalized rows and 408 unparseable units are useful discovery aids, but the 52% corpus caveat means aggregate counts cannot stand in for module value. Phase 308 must inspect source SUMMARYs, account for every harvested `Finding? = yes / Actioned? = no` row, distinguish repeated observations from independent findings, and route only evidence-backed proposals. No proposal is approved or applied by this plan.

## Output

- `.paul/proposals/v2.73-field-harvest-proposal-batch.md` — batch index, full 17-row evidence disposition, ranking, caveats, and human routing boundary.
- Three independently reviewable proposal documents: recurring advisory finding lineage, REV finding-class escalation, and validation-suite decomposition.
- Sourceable proposal-batch guardrails in `tests/helpers/field_harvest.sh`, called by both official TAP suites.
- Fresh validation and closure-readiness evidence for UNIFY.
</objective>

<context>
## Project Context

.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md

## Direct Prior Work

.paul/phases/306-ledger-contract-unify-integration-config-surfacing/306-01-SUMMARY.md
.paul/phases/307-retroactive-field-harvest/307-01-SUMMARY.md
.paul/phases/307-retroactive-field-harvest/307-02-SUMMARY.md

## Harvest Inputs

.paul/field-harvest/HARVEST-ROLLUP.md
.paul/field-harvest/pals-MODULE-LEDGER.md
.paul/field-harvest/quark-MODULE-LEDGER.md
.paul/field-harvest/hybrid-energy-reasoner-MODULE-LEDGER.md
.paul/field-harvest/UNPARSEABLE.md
docs/PALS-MODULE-EFFICACY-LEDGER-CONTRACT.md
docs/PALS-FIELD-HARVEST-NORMALIZATION-CONTRACT.md

## Source SUMMARY Evidence

/Users/maxwellnewman/pi/workspace/hybrid-energy-reasoner/.paul/phases/02-repair-solver/02-01-SUMMARY.md
/Users/maxwellnewman/pi/workspace/hybrid-energy-reasoner/.paul/phases/03-latin-square-and-sudoku-tasks/03-01-SUMMARY.md
/Users/maxwellnewman/pi/workspace/hybrid-energy-reasoner/.paul/phases/04-benchmark-cli/04-01-SUMMARY.md
/Users/maxwellnewman/pi/workspace/hybrid-energy-reasoner/.paul/phases/05-json-task-constructors/05-01-SUMMARY.md
/Users/maxwellnewman/pi/workspace/hybrid-energy-reasoner/.paul/phases/06-constructor-cli-examples/06-01-SUMMARY.md
/Users/maxwellnewman/pi/workspace/hybrid-energy-reasoner/.paul/phases/16-ci-docs-runbook/16-01-SUMMARY.md
/Users/maxwellnewman/claude/quark/.paul/phases/116-example-descriptor-library-formalization/116-01-SUMMARY.md
/Users/maxwellnewman/claude/quark/.paul/phases/117-anthropic-smoke-suite/117-01-SUMMARY.md
/Users/maxwellnewman/claude/quark/.paul/phases/118-pre-release-runner-and-documentation/118-01-SUMMARY.md
/Users/maxwellnewman/claude/quark/.paul/phases/120-canonical-dual-llm-seeding-migration/120-01-SUMMARY.md
/Users/maxwellnewman/claude/quark/.paul/phases/127-resource-confinement-enforcement-and-audit/127-01-SUMMARY.md

## Additional Source-Backed Context

.paul/assessments/2026-06-03-post-v2-70-next-milestone.md
.paul/assessments/2026-06-04-post-v2-71-next-milestone.md
</context>

<module_dispatch>
## Pre-Plan Module Dispatch

[dispatch] pre-plan enforcement: DEAN(50) PASS — no supported dependency manifest or lockfile exists in scope or at the repository root, so there is no dependency-audit scope and no critical/high block.

[dispatch] pre-plan advisory: ARCH(75), SETH(80), TODD(100), IRIS(150), GABE(175), LUKE(185), ARIA(190), DANA(195), DAVE(200), DOCS(200), OMAR(205), REED(210), VERA(215), CODI(220), PETE(225), RUBY(250) completed in installed-registry priority order.

| Module | Result | Context injected into this plan |
|---|---|---|
| ARCH | WARN | No MVC/Clean/3-tier/feature/flat pattern is inferable from the bounded `.paul/proposals` + `tests` paths. Both planned validation callers are standing god files: Pi 2,937 lines and cross-harness 2,324 lines. Keep all new logic in the existing helper and cap caller growth. |
| SETH | PASS | Existing planned shell files contain no secret-like literal or dynamic execution/rendering sink match. New proposal/helper text remains subject to post-apply changed-file inspection. |
| TODD | SKIP conversion | Repository tests are shell TAP suites rather than conventional `*.test.*`/framework tests. The work is documentation/proposal analysis plus bounded guardrail wiring, so `type: execute` is appropriate; the helper checks provide regression coverage without forcing RED/GREEN over prose. |
| IRIS | PASS | No literal TODO/FIXME/HACK/XXX markers in the three existing planned shell files. Source-citation completeness and false aggregation are the review risks carried into AC-1/AC-2. |
| GABE / LUKE / ARIA / DANA | SKIP | No API, UI, accessibility, schema, model, or migration files are planned. |
| DAVE | WARN | No repository CI configuration was detected. `No CI detected. Add .github/workflows/ci.yml.` remains advisory and out of Phase 308 scope; official local TAP suites plus GitHub PR checks remain the required evidence. |
| DOCS | NOTE | Documentation/proposal-only primary scope; drift check is not applicable. Proposals must cite source SUMMARYs rather than nearest-doc inference. |
| OMAR / REED / VERA / PETE | SKIP | No runtime observability, resilience, privacy, request-path, query, rendering, or performance surface is planned. |
| CODI | SKIP | No explicit TS/JS selectors or stable symbol seeds; `codi_seed_candidates = []`, so no blast-radius context is injected. |
| RUBY | WARN | Planned shell callers are 2,937 and 2,324 lines; the existing helper is 319 lines. Extend the helper rather than adding inline validation logic, keep it below 500 lines, and route the caller decomposition as a proposal rather than implementing it here. |

Context outputs: `tdd_candidates=[]`; `review_flags=[source-citation completeness, 52% coverage caveat, 17-row disposition completeness, normalized-row/source mismatch risk]`; `deploy_warning=no CI config detected`; `doc_warnings=documentation-only scope`; `blast_radius=skipped-no-symbols`; `debt_flags=[Pi validation 2937 lines, cross-harness validation 2324 lines]`; `dep_warnings=[]`.

## Post-Plan Module Dispatch

[dispatch] post-plan: TODD(100) PASS — execute plan retained. Existing shell TAP surfaces are explicitly extended in Task 2; no pure business-logic implementation or existing per-file test target justifies TDD conversion or RED/GREEN/REFACTOR restructuring.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|---|---|---|---|
| /paul | required | PLAN/APPLY/UNIFY lifecycle ownership | ✓ current workflow |
| /carl | required | Automatic session-boundary monitoring | automatic extension |
| TODD | required | Before/during helper and TAP changes | ✓ pre-plan; re-dispatch in APPLY |
| WALT | required | Baseline and post-change quality gating | ○ required in APPLY |

**BLOCKING:** APPLY must confirm TODD and WALT dispatch before implementation/validation proceeds. Parent PAUL retains lifecycle and verification authority.
</skills>

<acceptance_criteria>
## AC-1: Complete source-cited evidence disposition

```gherkin
Given the three committed field-harvest ledgers contain 17 rows with Finding? = yes and Actioned? = no
When the v2.73 proposal batch is authored
Then its evidence matrix accounts for all 17 rows exactly once by deployment/phase/module
And every interpretation cites the authoritative source SUMMARY path plus section or line evidence
And normalized rows that overstate, collapse, duplicate, or merely repeat a finding are called out rather than treated as independent proof
```

## AC-2: Human-routed proposals preserve efficacy authority boundaries

```gherkin
Given HARVEST-ROLLUP.md reports 375 rows and 408 unparseable units with a 52% corpus caveat
When the batch and three proposal documents are reviewed
Then each document states its evidence, proposed change, non-goals, risks, validation shape, and explicit human route
And the batch distinguishes recommended, investigate, defer, and no-proposal dispositions without approving implementation
And source SUMMARYs remain authoritative, no roll-up count is presented as module value, no auto-config or automatic demotion is proposed, and WALT/DEAN/TODD/SETH exemptions are preserved wherever scoped
```

## AC-3: Guardrail TAPs protect completeness and routing posture

```gherkin
Given the proposal batch is a warm non-authoritative decision aid derived from field evidence
When both official validation suites run
Then sourceable helper checks prove every harvested unactioned finding key is represented in the batch
And helper checks prove all three proposal files are listed and carry required source-citation, human-routing, non-authority, and no-auto-application markers
And each suite adds exactly two localized Phase 308 TAP assertions while keeping check logic out of the god-file callers
```

## AC-4: Phase 308 closes green and routes normally

```gherkin
Given Phase 307 closed at Pi 362/362 and cross-harness 259/259 with zero failures
When Phase 308 validation completes
Then both suites pass with zero failures and no regression against a fresh APPLY baseline
And shell syntax, artifact consistency, git diff hygiene, helper size, caller-growth caps, and external-repository read-only checks pass
And the proposals remain proposed rather than approved so UNIFY and the normal milestone route retain authority
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Audit all unactioned findings and author the human-routed proposal batch</name>
  <files>.paul/proposals/v2.73-field-harvest-proposal-batch.md, .paul/proposals/v2.73-recurring-advisory-finding-lineage.md, .paul/proposals/v2.73-review-finding-class-escalation.md, .paul/proposals/v2.73-validation-suite-decomposition.md</files>
  <action>
    Read the three ledgers only to enumerate candidate keys, then inspect the source SUMMARY for every `Finding? = yes / Actioned? = no` row before interpreting it. Build an evidence matrix in the batch index with one row per unique deployment/phase/module key and columns for normalized row, source citation, source meaning, cluster, and disposition. Reconcile the expected 17-row total: hybrid-energy-reasoner 6, pals 6, quark 5.

    Treat the roll-up as discovery evidence only. The index must state 375 rows / 408 unparseable / 52% unparseable share and explain that recorded dispatch evidence is not dispatch count or module value. Explicitly inspect suspicious normalization (including quark `120-01` RUBY `BLOCK`) against source instead of accepting status labels mechanically.

    Author three standalone proposals, each with frontmatter (`type: framework-improvement-proposal`, `source_milestone: v2.73`, `status: proposed`, `decision_authority: human`), Evidence, Problem, Proposed Change, Non-Goals, Risks, Validation Shape, and Human Route sections:

    1. **Recurring advisory finding lineage** — use hybrid phases 02–06 DAVE repetition and the later Phase 16 CI resolution as the primary source-backed example. Propose a bounded way to recognize repeated finding identity/lineage during human proposal analysis without changing the five-column ledger schema by default, creating telemetry, or auto-escalating lifecycle state.
    2. **Review finding-class escalation** — use quark Phase 127's repeated REV rounds over persisted `effectiveLimits` trust boundaries as the primary example. Propose that high-risk planning explicitly choose class-wide scope or a recorded deferral when successive reviews expose the same finding class across readers; do not make REV an automatic lifecycle hook or broaden one fix silently.
    3. **Validation-suite decomposition** — use pals Phase 306/307 ARCH evidence, current measured 2,937/2,324-line callers, and the post-v2.70/v2.71 assessments. Recommend this as a next-milestone candidate with contract-first decomposition, explicit TAP ownership/counts, and no implementation in Phase 308.

    The batch index ranks/routes proposals but approves none. Preserve the Phase 306 constraints: source SUMMARY authority, human approval for any config change, no telemetry, no auto-config, no automatic demotion, and WALT/DEAN/TODD/SETH demotion exemptions wherever scoped. Disposition weak, duplicate, already-resolved, or deployment-local observations as `no proposal` or `defer` with reasons rather than manufacturing a batch item.
  </action>
  <verify>The batch evidence matrix contains exactly 17 unique ledger keys and every row has a source SUMMARY citation; each of the three proposal files has all required sections/frontmatter; focused review confirms quark 120-01 is interpreted from source text rather than its normalized BLOCK label; no proposal has approved/accepted status; the batch names the 375/408/52% caveat and all authority/non-goal markers.</verify>
  <done>AC-1 and AC-2 satisfied: all candidate evidence is source-audited, weak/repeated evidence is honestly dispositioned, and three independently human-routable proposals exist without authorizing implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Add bounded proposal-batch guardrails to both TAP suites</name>
  <files>tests/helpers/field_harvest.sh, tests/pi-end-to-end-validation.sh, tests/cross-harness-validation.sh</files>
  <action>
    Extend the existing field-harvest helper rather than creating another helper or adding parsing logic to either god-file caller. Add source-safe functions that return success/failure only, set `FH_LAST_MISSING`, and never print TAP:

    - one check derives every `Finding? = yes / Actioned? = no` deployment/phase/module key from the three committed ledgers, requires exactly 17 unique keys, and verifies every key appears in the batch evidence matrix;
    - one check verifies the batch lists all three proposal paths and that the batch/proposals contain required source-authority, coverage-caveat, human-route, proposed-status, no-auto-config, no-automatic-demotion/application, and enforcement-exemption markers.

    Use stable structural markers and dynamic ledger-key comparison; do not lock full proposal prose or duplicate the 17 keys inside shell. Fail clearly on missing files, duplicate/missing keys, wrong counts, or missing posture markers.

    Source the existing helper (already loaded) and add one localized Phase 308 section to each suite with exactly two caller-owned TAP assertions: evidence completeness and routing/authority posture. Keep additions to each caller at or below 20 lines and keep `tests/helpers/field_harvest.sh` below 500 lines. Do not rewrite prior Phase 307 checks or hard-code total TAP plans; the suites use dynamic counters.
  </action>
  <verify>`bash -n tests/helpers/field_harvest.sh tests/pi-end-to-end-validation.sh tests/cross-harness-validation.sh`; focused shell invocation proves both new helper checks pass, then mutation copies prove each check fails when one evidence key or one authority marker is removed; git diff confirms exactly two new Phase 308 TAP calls per suite, caller growth ≤20 lines each, helper under 500 lines, and prior Phase 307 blocks unchanged except adjacent additive Phase 308 sections.</verify>
  <done>AC-3 satisfied: both suites protect complete source-backed disposition and non-authoritative human routing with four total TAP assertions while logic remains in the bounded helper.</done>
</task>

<task type="auto">
  <name>Task 3: Run official validation and prove v2.73 closure readiness</name>
  <files>tests/helpers/field_harvest.sh, tests/pi-end-to-end-validation.sh, tests/cross-harness-validation.sh</files>
  <action>
    Before Task 2 changes, capture fresh Pi and cross-harness command baselines plus clean/read-only status snapshots for the two external repositories. After Tasks 1–2, run focused helper checks, syntax checks, both full official suites, artifact consistency, and diff hygiene.

    Reconcile observed TAP totals from command output rather than assumed comments. The expected trajectory from the Phase 307 baseline is Pi 362→364 and cross-harness 259→261 because each suite gains exactly two checks; if the fresh baseline differs, explain the observed delta and require zero new failures. Confirm `tests/pi-end-to-end-validation.sh` and `tests/cross-harness-validation.sh` each grew by at most 20 lines, the helper remains under 500 lines, `.paul/MODULE-LEDGER.md` and all `.paul/field-harvest/*` inputs are unchanged, and both external repositories retain their pre-task status/content state.

    Capture evidence for UNIFY: changed-file list, 17/17 disposition result, proposal statuses/routes, validation totals, module reports, artifact consistency, git diff check, GitHub Flow branch/PR/CI state, and any deviations. Phase 308 becomes ready for UNIFY; milestone completion still routes through the normal merge and `/paul:milestone` lifecycle, never through proposal metadata.
  </action>
  <verify>`bash -n tests/helpers/field_harvest.sh tests/pi-end-to-end-validation.sh tests/cross-harness-validation.sh`; `bash tests/pi-end-to-end-validation.sh`; `bash tests/cross-harness-validation.sh`; `bash tests/helpers/artifact_consistency.sh`; `git diff --check`; line-count checks show both callers +≤20 and helper <500; git diff/status checks show ledgers, harvest inputs, and external roots unchanged; all proposal frontmatter remains `status: proposed` / human authority.</verify>
  <done>AC-4 satisfied: official checks are green with reconciled counts, source evidence stayed read-only, proposals remain non-authoritative, and Phase 308 is ready for UNIFY plus normal v2.73 closure routing.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE

- Anything under `/Users/maxwellnewman/claude/quark` or `/Users/maxwellnewman/pi/workspace/hybrid-energy-reasoner` — source SUMMARY evidence is strictly read-only.
- `.paul/MODULE-LEDGER.md` and `.paul/field-harvest/*` — inputs only; no regeneration, status correction, actioned promotion, or hand edit in Phase 308.
- `docs/PALS-MODULE-EFFICACY-LEDGER-CONTRACT.md` and `docs/PALS-FIELD-HARVEST-NORMALIZATION-CONTRACT.md` — settled authorities; proposals may recommend future work but do not amend them.
- `kernel/`, `modules/`, `drivers/`, `pals.json`, and installed runtime files — proposals route future normal loops; no framework/config implementation now.
- Existing Phase 307 TAP semantics — add adjacent Phase 308 checks; do not rewrite prior coverage.

## SCOPE LIMITS

- No telemetry, background collection, auto-config, automatic module ranking, automatic demotion/disable, or lifecycle-state mutation from efficacy evidence.
- No claim that roll-up totals measure dispatch frequency, finding importance, action effectiveness, or module value.
- No default schema expansion merely to make this batch easier; recurring-finding identity remains a proposal.
- No automatic REV lifecycle hook and no implementation of finding-class escalation.
- No validation-suite decomposition implementation; only an evidence-backed future milestone proposal.
- No parser/harvester fix for suspicious historical normalization in this phase; record source-backed interpretation and route a future fix only if justified.
- No proposal approval. `status: proposed` and human decision authority remain explicit.
</boundaries>

<verification>
Before declaring plan complete:
- [ ] Batch matrix accounts for 17/17 unique unactioned finding keys (hybrid 6, pals 6, quark 5)
- [ ] Every evidence row cites an authoritative source SUMMARY path and section/line
- [ ] Quark 120-01 RUBY normalized BLOCK is checked against source wording
- [ ] Three proposal files plus batch index carry required human-route and authority markers
- [ ] No proposal status is approved/accepted/implemented
- [ ] WALT/DEAN/TODD/SETH scoped exemptions remain explicit
- [ ] Two new Phase 308 TAP assertions per official suite, with logic in the helper
- [ ] `bash -n` passes for all three changed shell files
- [ ] Focused helper checks and mutation negatives pass
- [ ] Full Pi suite passes with zero failures and reconciled +2 delta
- [ ] Full cross-harness suite passes with zero failures and reconciled +2 delta
- [ ] `bash tests/helpers/artifact_consistency.sh` prints PASS
- [ ] `git diff --check` is clean
- [ ] Each validation caller grows by ≤20 lines; helper remains <500 lines
- [ ] Forward ledger, field-harvest inputs, and external repositories are unchanged
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All 17 harvested unactioned findings are source-audited and dispositioned without treating repeated rows as independent value evidence.
- Three bounded framework-improvement proposals are independently reviewable and explicitly human-routed.
- Proposal-batch completeness and non-authority posture are regression-locked in both official TAP suites.
- Validation is green with zero failures, bounded god-file growth, and preserved source artifacts.
- Phase 308 is ready for UNIFY and v2.73 can close through the normal GitHub Flow/milestone lifecycle.
</success_criteria>

<output>
After completion, create `.paul/phases/308-proposal-batch-validation-closure/308-01-SUMMARY.md` documenting:
- the 17/17 source-evidence disposition and any normalized/source mismatches;
- proposal list, ranking, statuses, and human routes;
- acceptance-criteria and task results;
- exact files/line deltas and boundary evidence;
- fresh Pi/cross-harness totals and delta reconciliation;
- artifact consistency, diff hygiene, external read-only, module, skill, GitHub Flow, and merge evidence;
- deviations, decisions, lessons, and v2.73 milestone-closure readiness.
</output>
