---
phase: 288-codi-validation-marker-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/pi-end-to-end-validation.sh
autonomous: true
---

<objective>
## Goal
Replace stale literal CODI marker checks in the Pi end-to-end validation suite with semantic helper assertions already used by cross-harness validation, clearing the inherited CODI validation drift without changing product/runtime behavior.

## Purpose
v2.67 starts by making Pi validation prove current CODI behavior contracts instead of brittle Phase 174/176 wording. This preserves artifact-first truth and keeps CODI advisory/non-authoritative while allowing later phases to focus on hot-artifact budgets and closure validation.

## Output
- `tests/pi-end-to-end-validation.sh` sources `tests/helpers/module_instruction_semantics.sh`.
- Inherited CODI literal-marker checks in the Pi e2e suite are replaced with semantic helper calls and TAP diagnostics.
- The Pi e2e suite still reports the existing PROJECT byte-budget failure as out of scope for Phase 289.
</objective>

<context>
## Project Context
.paul/STATE.md — Phase 288 ready to PLAN; v2.67 Validation Hygiene active.
.paul/ROADMAP.md — Phase 288 focus: replace/repair inherited Pi e2e CODI installed-resource marker-drift checks with `tests/helpers/module_instruction_semantics.sh` patterns; preserve behavior and cross-harness parity.

## Source Files
tests/pi-end-to-end-validation.sh
tests/helpers/module_instruction_semantics.sh

## RED Baseline Evidence
`bash tests/pi-end-to-end-validation.sh` currently reports `1..335`, Passed `329`, Failed `6`.
CODI failures in scope:
- `not ok 15` — installed `modules.yaml` CODI source-selector literal marker drift.
- `not ok 205` — installed `references/codi.md` source-selector/value-envelope/safe-setup literal marker drift.
- `not ok 208` — installed `modules.yaml` CODI post-unify instrumentation literal marker drift.
- `not ok 210` — installed `references/codi-instrumentation.md` hotfix-behavior literal marker drift.
- `not ok 212` — installed `modules.yaml` CODI pre-plan skip-log literal marker drift.
Out of scope: `not ok 125` PROJECT byte-budget drift belongs to Phase 289.

## Existing Semantic Helpers
`tests/helpers/module_instruction_semantics.sh` already provides:
- `mis_codi_source_selector_semantics_ok`
- `mis_codi_ref_semantics_ok`
- `mis_codi_post_unify_manifest_ok`
- `mis_codi_pre_plan_taxonomy_manifest_ok`
- `mis_codi_canonical_log_strings_ok`
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: TODD/WALT context — shell validation suite exists; RED baseline captured with `bash tests/pi-end-to-end-validation.sh` showing 5 CODI failures in this phase scope plus 1 PROJECT-budget failure reserved for Phase 289. `tdd_candidates`: `tests/pi-end-to-end-validation.sh` semantic validation assertions.
[dispatch] pre-plan advisory: IRIS — No TODO/FIXME/HACK/XXX markers in planned files.
[dispatch] pre-plan advisory: DAVE — No CI config files detected in repo root/workflows; no CI-file changes planned.
[dispatch] pre-plan advisory: DOCS — Test/validation-only scope; docs drift check not applicable.
[dispatch] pre-plan advisory: CODI — skipped — no TS/JS selectors or extractable symbols in phase scope.
[dispatch] pre-plan advisory: RUBY — validation-shell scope; no preliminary code-debt indicators in planned file.
[dispatch] pre-plan advisory: ARCH/ARIA/DANA/GABE/LUKE/OMAR/PETE/REED/SETH/VERA — skipped/no concerns for shell validation-only scope with no UI/API/data/runtime source files planned.
[dispatch] pre-plan enforcement: DEAN PASS — no dependency manifest/lockfile found, so vulnerability audit scope is unavailable and no critical/high dependency delta can be established for this plan.
[dispatch] post-plan: TODD — Plan remains `execute`; RED baseline is existing validation failure output, and APPLY must preserve test-first evidence by resolving only the validation assertions against existing semantic helpers.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | PLAN/APPLY/UNIFY lifecycle management | ✓ |
| /carl | required | Session boundary monitoring | automatic |
| TODD | required | Before/during implementation validation | ✓ via module dispatch |
| WALT | required | Post-implementation quality validation | pending APPLY |

**BLOCKING:** Required skills must remain active before APPLY proceeds.
</skills>

<acceptance_criteria>
## AC-1: Pi CODI validations use semantic helpers
```gherkin
Given the Pi e2e validation suite checks installed CODI resources
When it validates `modules.yaml`, `references/codi.md`, and `references/codi-instrumentation.md`
Then it uses semantic helper assertions rather than stale literal marker lists for CODI behavior contracts
```

## AC-2: CODI drift failures are cleared without product/runtime changes
```gherkin
Given the current RED baseline has CODI failures 15, 205, 208, 210, and 212
When `bash tests/pi-end-to-end-validation.sh` is rerun after APPLY
Then those CODI failures pass, and any remaining PROJECT byte-budget failure is unchanged and explicitly out of scope
```

## AC-3: Cross-harness parity is preserved
```gherkin
Given cross-harness validation already uses `tests/helpers/module_instruction_semantics.sh`
When this phase updates Pi e2e validation
Then the Pi suite sources the same helper family and does not weaken cross-harness semantic guardrails
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Source semantic helper in Pi e2e validation</name>
  <files>tests/pi-end-to-end-validation.sh</files>
  <action>
    Add a bounded `source "$REPO_ROOT/tests/helpers/module_instruction_semantics.sh"` near the existing TAP/artifact helper sources.
    Include a short comment that Phase 288 reuses semantic CODI instruction assertions so Pi e2e checks behavior groups instead of stale literal phrases.
    Do not modify installer behavior, installed resources, or helper semantics in this task.
  </action>
  <verify>`bash -n tests/pi-end-to-end-validation.sh` passes.</verify>
  <done>AC-1 and AC-3 satisfied: Pi e2e can call the shared semantic helper family.</done>
</task>

<task type="auto">
  <name>Task 2: Replace stale CODI literal-marker checks with helper assertions</name>
  <files>tests/pi-end-to-end-validation.sh</files>
  <action>
    Replace the five inherited CODI literal-marker checks with semantic helper calls:
    - Check 15 area: use `mis_codi_source_selector_semantics_ok "$SKILL_DIR/modules.yaml"` and emit the existing TAP description or a clearer Phase 288 semantic description.
    - Check 205 area: use `mis_codi_ref_semantics_ok "$PI_CODI_REF"`.
    - Check 208 area: use `mis_codi_post_unify_manifest_ok "$SKILL_DIR/modules.yaml"`.
    - Check 210 area: replace the brittle `## Hotfix behavior` literal expectation with helper-backed semantic validation of `codi-instrumentation.md`; if no helper exists for that exact document, use a small inline grouped check requiring outcome taxonomy/read-order/plan-path/hotfix or legacy/no-dispatch semantics without requiring one stale heading string.
    - Check 212 area: split manifest taxonomy from canonical log strings: use `mis_codi_pre_plan_taxonomy_manifest_ok "$SKILL_DIR/modules.yaml"` for manifest semantics and, if canonical log strings still need direct protection, use `mis_codi_canonical_log_strings_ok "$PI_CODI_REF"` against the CODI reference where the literals actually live.
    For each helper failure, report `MIS_LAST_MISSING` in `tap_not_ok` details.
    Avoid broadening scope to PROJECT/ROADMAP budget checks or changing installed CODI content to satisfy old test strings.
  </action>
  <verify>`bash tests/pi-end-to-end-validation.sh` shows CODI checks 15, 205, 208, 210, and 212 passing; any remaining failure is the known PROJECT byte-budget failure for Phase 289.</verify>
  <done>AC-1 and AC-2 satisfied: inherited CODI marker drift is cleared by semantic validation.</done>
</task>

<task type="auto">
  <name>Task 3: Run parity validation and record scoped outcome</name>
  <files>tests/pi-end-to-end-validation.sh</files>
  <action>
    Run both Pi and cross-harness validation commands and inspect TAP output for CODI-related regressions.
    If cross-harness already passes, do not edit it. If a semantic-helper change becomes necessary, stop and return to parent for scope review before editing `tests/helpers/module_instruction_semantics.sh` or `tests/cross-harness-validation.sh`.
    Record in APPLY notes that the PROJECT budget failure remains intentionally unresolved for Phase 289 if it still appears.
  </action>
  <verify>`bash tests/cross-harness-validation.sh` passes or has no new CODI semantic-helper failures; `bash tests/pi-end-to-end-validation.sh` has no CODI marker-drift failures.</verify>
  <done>AC-2 and AC-3 satisfied: Pi and cross-harness validation agree on CODI semantics, with non-CODI budget drift preserved for later phase.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- `modules/codi/**` behavior/instruction content solely to satisfy tests.
- `modules.yaml` installed registry semantics.
- `drivers/pi/install.sh` installer behavior.
- `.paul/PROJECT.md` or `.paul/ROADMAP.md` byte-budget content; Phase 289 owns hot-artifact budget hygiene.
- Product/runtime behavior and CODI advisory/non-authority semantics.

## SCOPE LIMITS
- This phase fixes Pi e2e CODI validation assertions only.
- Do not turn CODI into a blocking lifecycle gate.
- Do not add broad searches, generated indexes, or `.codegraph/` requirements.
- Do not hide or reclassify the known PROJECT budget failure.
</boundaries>

<verification>
Before declaring APPLY complete:
- [ ] `bash -n tests/pi-end-to-end-validation.sh`
- [ ] `bash tests/pi-end-to-end-validation.sh` has no CODI failures 15/205/208/210/212; known PROJECT byte-budget failure may remain.
- [ ] `bash tests/cross-harness-validation.sh` passes or shows no new CODI semantic-helper regression.
- [ ] No product/runtime files changed.
- [ ] All acceptance criteria met.
</verification>

<success_criteria>
- Pi e2e CODI checks validate semantic behavior groups, not stale literal marker phrases.
- Five inherited CODI marker-drift failures are resolved.
- Existing semantic helper patterns stay shared with cross-harness validation.
- Phase 289 PROJECT/ROADMAP budget work remains untouched.
</success_criteria>

<output>
After completion, create `.paul/phases/288-codi-validation-marker-hygiene/288-01-SUMMARY.md` with:
- RED/GREEN validation evidence for Pi e2e CODI failures.
- Cross-harness parity evidence.
- Explicit note that PROJECT byte-budget drift is deferred to Phase 289 if still failing.
- Module execution reports for TODD/WALT/CODI and other relevant hooks.
</output>
