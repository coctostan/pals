---
phase: 269-plan-workflow-instruction-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kernel/workflows/plan-phase.md
  - tests/pi-end-to-end-validation.sh
  - tests/cross-harness-validation.sh
autonomous: false
---

<objective>
## Goal
Rewrite the plan workflow so its actual procedural steps create lean, executable plans from bounded lifecycle context instead of over-reading hot artifacts, over-prompting collaboration choices, or passing validation through marker prose alone.

## Purpose
Phase 268 proved that selective-loading policy text is insufficient when workflow procedure still contains broad or eager read behavior. Phase 269 applies the same instruction-audit posture to `plan-phase.md`: preserve PLAN quality and module dispatch evidence while making the routine planning path STATE/ROADMAP-targeted, context-efficient, concise by default, and semantically validated.

## Output
- `kernel/workflows/plan-phase.md` rewritten for bounded planning preconditions, current-milestone/target-phase loading, concise collaboration/review prompts, narrow prior-context use, and explicit fallback rules.
- Validation strengthened in `tests/pi-end-to-end-validation.sh` and `tests/cross-harness-validation.sh` so plan-workflow compliance requires operational read-order/semantic checks, not only marker words.
- Phase 269 summary after APPLY at `.paul/phases/269-plan-workflow-instruction-audit/269-01-SUMMARY.md`.
</objective>

<context>
## Project Context
- `.paul/PROJECT.md` — active milestone is v2.63 Workflow Instruction Audit + Context-Efficiency Rewrite; Phase 268 completed resume workflow semantic repair and Phase 269 targets plan workflow behavior.
- `.paul/ROADMAP.md` — Phase 269 scope: review `kernel/workflows/plan-phase.md` for planning-time context bloat, over-reading of lifecycle artifacts, excessive ceremony, unclear collaboration prompts, plan-review verbosity, and validation/acceptance criteria that can pass while behavior remains wrong.
- `.paul/STATE.md` — Phase 269 PLAN is being created; PLAN/APPLY/UNIFY loop should move to PLAN complete / APPLY awaiting approval after this artifact is written.

## Prior Work
- `.paul/phases/268-resume-workflow-instruction-audit/268-01-SUMMARY.md` — establishes the reusable pattern for this milestone: operational workflow rewrite, section-by-section approval, semantic guardrails, and validation that rejects broad/eager instructions.

## Source Files
- `kernel/workflows/plan-phase.md` — target workflow. Current audit concerns include broad `Read STATE.md` / `Read PROJECT.md` wording, eager required-reading framing, a collaboration override menu that may create unnecessary ceremony for direct-requirements phases, duplicated context-loading instructions, and validation that can preserve markers without proving lean operational behavior.
- `tests/pi-end-to-end-validation.sh` — Pi/install validation already has resume semantic guardrails and plan marker checks; add localized plan-workflow semantic assertions.
- `tests/cross-harness-validation.sh` — shared/install validation already checks plan marker preservation; add cross-harness plan semantic assertions mirroring the Pi suite.
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: modules.yaml loaded from installed registry (`kernel_version: 2.0.0`).

- ARCH: Planned files are in expected repo layers: `kernel/workflows/` for lifecycle workflow text and `tests/` for shell validation. Current line counts: `kernel/workflows/plan-phase.md` 357, `tests/pi-end-to-end-validation.sh` 2304, `tests/cross-harness-validation.sh` 1535. Existing validation scripts are large; keep additions localized and avoid full-suite restructuring.
- TODD: No `*.test.*`, `*.spec.*`, or `__tests__` tree detected. Existing shell TAP validation files are in scope; Task 1 must add semantic guardrails before the workflow rewrite is accepted.
- IRIS: No TODO/FIXME/HACK/XXX markers found in planned files. Review flag: current plan workflow mixes bounded-loading policy with procedure that can still encourage broad context collection and ceremony.
- DAVE: No `.github/workflows`, Dockerfile, or other CI config found. No deploy/runtime config changes planned; local verification remains `git diff --check`, Pi validation, and cross-harness validation.
- DOCS: Planned files are lifecycle workflow and validation scripts. No README/API docs required; lifecycle truth will be recorded through PLAN/SUMMARY/STATE/ROADMAP.
- CODI: skipped — no extractable symbols in phase scope (markdown/shell validation work; no TS/JS selectors).
- RUBY: Debt flags are instruction-level: repeated context-loading steps, broad required-reading language, collaboration/review prompt ceremony, marker-only validation, and large existing shell validation suites requiring localized edits.

[dispatch] pre-plan enforcement: DEAN PASS — no root package manager/lockfile found and no dependency manifests are modified by this plan; no dependency audit block applies.

## Post-Plan Module Dispatch
[dispatch] post-plan: TODD reviewed plan shape; no TDD conversion. Guardrail-first validation is represented in Task 1 and must run before finalizing APPLY.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | PLAN/APPLY/UNIFY lifecycle loop | ✓ |
| /carl | required | Session boundary management if context pressure rises | automatic |
| TODD | required | Validation guardrail design before/during APPLY | ✓ via module dispatch |
| WALT | required | Quality validation before completion | ✓ via validation commands |

**BLOCKING:** APPLY must remain under PAUL parent authority. Do not use repo-local `pals-implementer` for the core workflow rewrite unless the parent first approves a narrow helper packet; this phase is intentionally instruction-sensitive and interactive.
</skills>

<acceptance_criteria>
## AC-1: Plan workflow loads bounded lifecycle context in the right order
```gherkin
Given `kernel/workflows/plan-phase.md` is used for `/paul:plan`
When a routine plan starts from a ready-to-plan STATE
Then the workflow instructs agents to confirm PLAN readiness from bounded STATE sections, identify the target phase from the current ROADMAP milestone/phase row, and defer broader PROJECT/PRD/prior-summary/source reads until materially needed.
```

## AC-2: Planning posture is concise and proportional
```gherkin
Given the phase already states direct requirements or collaboration metadata
When planning posture is determined
Then the workflow applies project/default metadata without forcing a full multi-option prompt unless the phase is ambiguous, exploratory, high-risk, checkpointed, or the user asks to change collaboration level.
```

## AC-3: Context loading and CODI seed preparation stay bounded
```gherkin
Given the phase scope names only markdown and shell files
When plan context and CODI seed candidates are prepared
Then the workflow keeps CODI optional, avoids codebase-wide fishing, reads only explicitly relevant source files, and does not reflexively chain unrelated prior artifacts or completed-history archives.
```

## AC-4: Review output stays concise while preserving approval gates
```gherkin
Given a PLAN has been created
When the plan is presented for review
Then the workflow defaults to a compact recap, preserves explicit review/apply approval choices, and avoids repeating long templates or background unless requested or needed for blockers/module warnings.
```

## AC-5: Validation catches semantic regressions, not marker prose
```gherkin
Given a future change retains words such as `target phase detail`, `planning.default_collaboration`, `bounded`, and `current milestone`
When the workflow still instructs broad routine reads, eager PRD/project/history loading, or mandatory collaboration menus on every run
Then Pi and cross-harness validation fail with plan-workflow-specific messages.
```

## AC-6: Rewrite is collaborative section-by-section and scope remains bounded
```gherkin
Given Phase 269 is limited to plan workflow instruction repair and guardrails
When APPLY edits `plan-phase.md`
Then the agent presents one section at a time for approval, edits only approved wording, keeps changes limited to planned files plus routine PAUL lifecycle artifacts, and finishes with `git diff --check`, Pi validation, and cross-harness validation passing.
```
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add semantic plan-workflow guardrails before accepting the rewrite</name>
  <files>tests/pi-end-to-end-validation.sh, tests/cross-harness-validation.sh</files>
  <action>
    Add localized TAP assertions that fail when `plan-phase.md` keeps marker prose but regresses operational planning behavior. Mirror the Phase 268 resume semantic guardrail style.

    Required guardrails:
    - Check repo source workflow and installed/shared copies where each validation suite already has those paths available.
    - Require explicit bounded planning preconditions: STATE readiness from bounded slices, current ROADMAP milestone/target phase identification before expansion, and full hot-artifact reads only as fallback for missing/contradictory fields, repair, migration, lifecycle write, or whole-artifact validation.
    - Require planning posture/review semantics to be concise by default: project/default collaboration metadata can be applied without a mandatory full option menu every routine run, while explicit approval/review choices remain preserved.
    - Reject old broad/eager routine instructions, including at minimum standalone `Read STATE.md` / `Read .paul/STATE.md`, unconditional `Read .paul/PROJECT.md first`, unconditional PRD reads, reflexive prior-summary chaining, or completed-history archive reads without concrete dependency.
    - Keep additions local to existing context-diet / workflow prose contract areas; do not restructure full validation scripts.
  </action>
  <verify>`bash tests/pi-end-to-end-validation.sh` and `bash tests/cross-harness-validation.sh` fail before the workflow rewrite or the new assertions demonstrably target existing overly broad plan-workflow text; after Task 2 they pass.</verify>
  <done>AC-1 and AC-5 satisfied: validation rejects broad/eager plan instructions and cannot pass on marker prose alone.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Confirm the section-by-section rewrite protocol before editing `plan-phase.md`.</decision>
  <context>
    APPLY must not perform a bulk rewrite. Before changing workflow text, it must present a compact audit of the current instruction failures and propose a section order. Then it must work one section at a time.

    Required collaboration loop for each section:
    1. Show the section name and the smallest useful current excerpt.
    2. State the concrete problem in that section: over-read, duplicate loading, ceremony, stale context, helper-boundary confusion, validation false positive, or unclear fallback wording.
    3. Propose replacement wording or a focused diff.
    4. Ask for user approval or wording changes.
    5. Edit only the approved section.
    6. Re-read that section to verify wording before moving on.

    Expected section order:
    1. Purpose / required reading / hot-artifact loading.
    2. Preconditions, config, and target-phase identification.
    3. Planning posture and collaboration prompts.
    4. CODI seed preparation and module dispatch.
    5. Scope/context loading and specialized flows.
    6. Plan creation, validation, and post-plan hooks.
    7. Review output, STATE/ROADMAP updates, and error handling.
  </context>
  <options>
    <option id="approve-section-loop">
      <name>Approve section-by-section collaborative rewrite</name>
      <pros>Gives the user wording control and prevents another marker-only or bulk procedural rewrite.</pros>
      <cons>Requires more APPLY turns and pauses between workflow sections.</cons>
    </option>
    <option id="adjust-section-loop">
      <name>Adjust section order or collaboration protocol</name>
      <pros>Allows the user to change how the rewrite proceeds before any workflow mutation.</pros>
      <cons>Pauses execution and may require plan/task wording updates.</cons>
    </option>
  </options>
  <resume-signal>Reply `approve-section-loop` to continue, or describe the protocol/section-order adjustment.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Collaboratively rewrite plan workflow section by section</name>
  <files>kernel/workflows/plan-phase.md</files>
  <action>
    Rewrite the workflow instructions, not just marker policy text, using the approved section-by-section collaboration loop from the checkpoint. Do not edit ahead of the user's approved section.

    Required edits across the approved sections:
    - Keep PLAN quality requirements and module dispatch evidence intact while making routine planning start from bounded lifecycle facts.
    - Replace broad required-reading implications with explicit bounded reads: STATE Current Position / Loop Position / Session Continuity as needed for PLAN readiness; ROADMAP current milestone + target phase detail; PROJECT hot brief only when needed for goal/value/context; PRD only when materially useful.
    - Make full hot-artifact reads explicit fallback only for missing/contradictory fields, repair, migration, lifecycle write, audit, or whole-artifact validation.
    - Consolidate duplicated identify/load/analyze context instructions so planning reads each artifact for a clear reason and does not reflexively chain prior phase summaries or completed-history archives.
    - Change collaboration posture from mandatory option-menu ceremony to concise default behavior: use carried CONTEXT metadata or `pals.json` default automatically for routine direct-requirements phases; prompt only when ambiguity, risk, checkpoints, exploratory mode, or user override requires it.
    - Preserve CODI seed safety: only explicit TS/JS selectors from target phase/context, no codebase-wide fishing, markdown/shell-only phases may skip cleanly.
    - Preserve module dispatch call-site obligations and evidence lines, but keep skip/no-scope output compact by default.
    - Keep plan review concise by default while preserving the four review choices and explicit APPLY approval gate.
    - Avoid harness-specific installed paths and shell-only examples where plain workflow instructions are clearer.
  </action>
  <verify>After each approved section edit, re-read only that section. At the end, semantic guardrails from Task 1 pass and manual inspection confirms broad/eager routine reads and mandatory ceremony are gone from the normal planning path.</verify>
  <done>AC-1, AC-2, AC-3, AC-4, and AC-6 satisfied: routine planning is bounded, proportional, and collaboratively rewritten section by section.</done>
</task>

<task type="auto">
  <name>Task 3: Install/validate and record Phase 269 evidence</name>
  <files>kernel/workflows/plan-phase.md, tests/pi-end-to-end-validation.sh, tests/cross-harness-validation.sh</files>
  <action>
    Run the final verification set and prepare the evidence needed for UNIFY.

    Required checks:
    - `git diff --check`
    - `bash tests/pi-end-to-end-validation.sh`
    - `bash tests/cross-harness-validation.sh`
    - Inspect changed-file scope and confirm only planned files plus routine `.paul/*` lifecycle artifacts changed.

    If validation fails because installed copies do not reflect source changes, follow the existing installer/validation flow already used by the tests rather than editing installed runtime files directly.
  </action>
  <verify>All required commands pass and changed-file scope is bounded.</verify>
  <done>AC-5 and AC-6 satisfied: semantic validation is green and evidence is ready for `269-01-SUMMARY.md`.</done>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- Do not edit installed runtime copies under `~/.pi`, `~/.claude`, or other home-directory install targets directly.
- Do not modify `resume-project.md`, `apply-phase.md`, `unify-phase.md`, or `pause-work.md`; they have dedicated completed or future phases.
- Do not change module manifests, extension TypeScript, driver installers, or project dependencies.
- Do not rewrite the full validation suites; keep tests localized to plan workflow semantics.

## SCOPE LIMITS
- This phase fixes `plan-phase.md` only.
- No broad lifecycle artifact compaction or historical archive rewrite.
- No v2.63 narrative deduplication architecture work.
- No helper delegation unless explicitly approved after the checkpoint; parent APPLY remains authoritative.
</boundaries>

<verification>
Before declaring plan complete:
- [ ] Semantic plan-workflow guardrails added to Pi validation and cross-harness validation.
- [ ] `kernel/workflows/plan-phase.md` no longer contains broad/eager routine planning read instructions.
- [ ] Planning posture and review output are concise by default while approval gates remain explicit.
- [ ] CODI/module dispatch safety and evidence-line requirements remain intact.
- [ ] `git diff --check` passes.
- [ ] `bash tests/pi-end-to-end-validation.sh` passes.
- [ ] `bash tests/cross-harness-validation.sh` passes.
- [ ] Changed-file scope is limited to planned files plus routine PAUL lifecycle artifacts.
</verification>

<success_criteria>
- Plan workflow instructions are operationally context-efficient, not merely policy-compliant.
- Routine direct-requirements planning no longer forces unnecessary collaboration/review ceremony.
- Validation fails on the exact class of marker-only false positive identified in v2.63.
- Phase 269 remains limited to plan workflow and semantic guardrails.
</success_criteria>

<output>
After completion, create `.paul/phases/269-plan-workflow-instruction-audit/269-01-SUMMARY.md` with:
- Audit findings from the original plan workflow.
- Exact semantic guardrails added.
- Section-by-section rewrite decisions.
- Final verification results.
- Any deferred issues for phases 270–272.
</output>
