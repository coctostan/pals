---
phase: 233-validation-evidence-closure
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/PALS-CONTEXTUAL-VERBOSITY-AUDIT.md
  - tests/pi-end-to-end-validation.sh
  - tests/cross-harness-validation.sh
autonomous: true
---

<objective>
## Goal
Close v2.54 Contextual Verbosity Paring by reconciling Phase 230/231/232 evidence, adding localized validation guardrails for the final closure evidence, and preserving command-output truth and lifecycle authority.

## Purpose
Phase 231 and Phase 232 implemented the process/workflow and skill-wrapper verbosity-paring slices. Phase 233 should make the milestone closure auditable without broadening scope: one durable evidence closure in the Phase 230 audit artifact, one localized Pi validation guardrail, one localized cross-harness guardrail, and fresh final validation.

## Output
- `docs/PALS-CONTEXTUAL-VERBOSITY-AUDIT.md` gains a final v2.54 closure evidence section.
- `tests/pi-end-to-end-validation.sh` gains one localized v2.54 closure guardrail and reconciles Pi total 208→209.
- `tests/cross-harness-validation.sh` gains one localized v2.54 closure guardrail and reconciles cross-harness total 122→123.
</objective>

<context>
## Project Context
- `.paul/PROJECT.md` — current focus and v2.54 boundaries.
- `.paul/ROADMAP.md` — Phase 233 focus and milestone closure route.
- `.paul/STATE.md` — lifecycle state; Phase 233 ready for PLAN.
- `docs/PALS-CONTEXTUAL-VERBOSITY-AUDIT.md` — Phase 230 baseline and the durable artifact to close.
- `.paul/phases/231-process-workflow-verbosity-paring/231-01-SUMMARY.md` — Phase 231 workflow/reference implementation evidence.
- `.paul/phases/232-skill-verbosity-pass/232-01-SUMMARY.md` — Phase 232 skill wrapper implementation evidence.

## Source Files
- `docs/PALS-CONTEXTUAL-VERBOSITY-AUDIT.md`
- `tests/pi-end-to-end-validation.sh`
- `tests/cross-harness-validation.sh`
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
- `[dispatch] pre-plan advisory: ARCH PASS` planned files are docs and shell validation suites in established locations; no runtime architecture boundaries or source dependency cycles are in scope.
- `[dispatch] pre-plan advisory: TODD SKIPPED` no app/unit test framework conversion is appropriate; the shell validation suites are the proof path for this evidence-closure plan.
- `[dispatch] pre-plan advisory: IRIS PASS` no TODO/FIXME/HACK/XXX markers are expected or authorized in planned files.
- `[dispatch] pre-plan advisory: DAVE SKIPPED` no CI config files are in planned scope.
- `[dispatch] pre-plan advisory: DOCS PASS` planned work updates the dedicated v2.54 audit/evidence artifact; README update is not required unless validation or closure review reveals a missing user-facing marker.
- `[dispatch] CODI: skipped — no extractable symbols in phase scope` because planned source selectors are Markdown and shell scripts, not TS/JS code symbols.
- `[dispatch] pre-plan advisory: RUBY WARN` validation scripts are known large pre-existing debt; keep changes marker-only and localized, do not refactor validation structure in this plan.
- `[dispatch] pre-plan advisory: ARIA/DANA/GABE/LUKE/OMAR/PETE/REED/VERA skipped — no UI, data, API, UX, observability, performance, resilience, or privacy files in scope`.
- `[dispatch] pre-plan enforcement: SETH PASS` planned docs/shell validation changes must not introduce secrets; run focused secret grep before APPLY completion.
- `[dispatch] pre-plan enforcement: DEAN PASS/SKIP` no dependency manifests or lockfiles are in planned scope; no dependency changes are authorized.

## Post-Plan Module Dispatch
- `[dispatch] post-plan: TODD PASS` plan remains `execute`; shell validation suites provide the required proof path.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | PLAN/APPLY/UNIFY lifecycle management | ✓ |
| TODD | required module | APPLY verification enforcement | module dispatch |
| WALT | required module | APPLY/UNIFY quality gating | module dispatch |

No additional external skill is required before APPLY.
</skills>

<acceptance_criteria>
## AC-1: v2.54 closure evidence is reconciled in the audit artifact
```gherkin
Given Phase 230 created the contextual verbosity audit and Phases 231/232 implemented the planned slices
When Phase 233 APPLY completes
Then `docs/PALS-CONTEXTUAL-VERBOSITY-AUDIT.md` records final v2.54 closure evidence, Phase 231/232 summaries, PR #146/#147, validation counts, command-output truth, and deferred boundaries.
```

## AC-2: Closure guardrails are localized and count-reconciled
```gherkin
Given current validation baselines are Pi 208/208 and cross-harness 122/122
When Phase 233 APPLY updates validation
Then each suite gains exactly one localized v2.54 closure guardrail and expected totals are reconciled to Pi 209/209 and cross-harness 123/123.
```

## AC-3: Required authority and evidence boundaries remain intact
```gherkin
Given verbosity paring must not weaken lifecycle or evidence guarantees
When closure evidence and validation markers are added
Then approval gates, module evidence, validation truth, GitHub Flow gates, parent APPLY authority, delegated APPLY boundaries, and `.paul/*` artifact truth remain explicitly preserved.
```

## AC-4: Final verification is green and scoped
```gherkin
Given this is the final v2.54 implementation phase
When APPLY finishes
Then focused marker checks pass, installer passes, Pi validation passes, cross-harness validation passes, PR checks pass, and Workguard reports no out-of-scope changes.
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Add final v2.54 closure evidence to the audit artifact</name>
  <files>docs/PALS-CONTEXTUAL-VERBOSITY-AUDIT.md</files>
  <action>
    Append a final `Phase 233 / v2.54 Closure Evidence` section to `docs/PALS-CONTEXTUAL-VERBOSITY-AUDIT.md`.

    Include:
    - Phase 230 baseline audit role.
    - Phase 231 implementation evidence: summary path, shared contextual verbosity guidance, compact dispatch guidance, PR #146, validation pass counts.
    - Phase 232 implementation evidence: summary path, 13 Pi skill wrappers, 363→305 wrapper line reduction, validation-protected marker repair, PR #147, validation pass counts.
    - Phase 233 closure plan: localized validation guardrails and final command-output truth.
    - Explicit preservation boundaries: approval gates, module evidence, validation truth, GitHub Flow gates, parent APPLY authority, delegated APPLY boundaries, `.paul/*` truth, and full authoritative read fallback.
    - Deferred boundaries: no broad validation refactor, no runtime/UI/helper/GitHub Flow/install/dependency/CI behavior change, no CODI manifest compaction, no deep init/map-codebase compaction.

    Avoid changing the original audit findings or rewriting prior sections; append closure evidence only.
  </action>
  <verify>Focused grep finds `v2.54 closure`, `Phase 231`, `Phase 232`, `PR #146`, `PR #147`, `363→305`, `command-output truth`, `.paul/* truth`, and deferred-boundary markers in `docs/PALS-CONTEXTUAL-VERBOSITY-AUDIT.md`.</verify>
  <done>AC-1 and AC-3 satisfied: the audit artifact carries final closure evidence without weakening preserved authority.</done>
</task>

<task type="auto">
  <name>Task 2: Add localized v2.54 closure validation guardrails</name>
  <files>tests/pi-end-to-end-validation.sh, tests/cross-harness-validation.sh</files>
  <action>
    Add one marker-only validation guardrail to each suite:
    - Pi suite: verify the audit artifact contains v2.54 closure evidence, PR #146/#147, Pi validation 208/208, Pi 208→209 reconciliation, command-output truth, and Pi-supported runtime / lifecycle authority markers.
    - Cross-harness suite: verify the audit artifact contains v2.54 closure evidence, shared invariant boundaries, cross-harness validation 122/122, cross-harness 122→123 reconciliation, command-output truth, and no weakening of `.paul/*` authority.

    Update expected TAP totals and comments:
    - `tests/pi-end-to-end-validation.sh`: 208 → 209.
    - `tests/cross-harness-validation.sh`: 122 → 123.

    Keep checks localized near existing validation-classification / closure guardrails. Do not refactor the large shell scripts or alter unrelated validation categories.
  </action>
  <verify>Focused grep confirms `1..209`, `208→209`, `v2.54 closure`, `PR #146`, `PR #147` in the Pi suite and `1..123`, `122→123`, `v2.54 closure`, `Shared Invariant`, `command-output truth` in the cross-harness suite.</verify>
  <done>AC-2 and AC-3 satisfied: validation count changes are localized and explicitly reconciled.</done>
</task>

<task type="auto">
  <name>Task 3: Run final v2.54 closure validation and scope checks</name>
  <files>docs/PALS-CONTEXTUAL-VERBOSITY-AUDIT.md, tests/pi-end-to-end-validation.sh, tests/cross-harness-validation.sh</files>
  <action>
    Run focused marker checks, install, full validation, PR checks, and Workguard scope review:
    - Focused grep checks from Tasks 1 and 2.
    - Secret scan over planned files.
    - `PALS_ROOT="$PWD" bash drivers/pi/install.sh`.
    - `bash tests/pi-end-to-end-validation.sh` expecting 209/209.
    - `bash tests/cross-harness-validation.sh` expecting 123/123.
    - Workguard report with allowed paths limited to planned docs/tests plus `.paul/*` lifecycle artifacts.

    Record command-output evidence for UNIFY. Fresh command output is authoritative; copied counts in docs or summaries are explanatory only.
  </action>
  <verify>Installer, Pi validation 209/209, cross-harness validation 123/123, focused marker checks, secret scan, PR checks, and Workguard scope report all pass.</verify>
  <done>AC-1 through AC-4 satisfied with final evidence ready for milestone closure.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- Runtime TypeScript or extension behavior.
- Kernel workflow/reference behavior except through validation-only marker checks.
- Pi skill wrappers beyond evidence references; Phase 232 already closed that slice.
- Module manifests/references, including CODI complexity contracts.
- Dependency manifests, lockfiles, install scripts, CI config, helper-agent definitions.
- GitHub Flow behavior, guided workflow behavior, delegated APPLY authority, checkpoint/approval behavior.

## SCOPE LIMITS
- Phase 233 is evidence/validation closure only.
- Validation-suite edits must be marker-only and localized; do not refactor shell scripts or broadly reorganize categories.
- Documentation edits should close v2.54 evidence in the audit artifact; do not create duplicate closure surfaces unless validation proves necessary.
- Do not hide or compress away required lifecycle proof just to make output shorter.
</boundaries>

<verification>
Before declaring APPLY complete:
- [ ] Focused audit marker grep passes.
- [ ] Focused validation-suite marker grep passes.
- [ ] Secret scan over planned files passes.
- [ ] `PALS_ROOT="$PWD" bash drivers/pi/install.sh` passes.
- [ ] `bash tests/pi-end-to-end-validation.sh` passes with 209/209.
- [ ] `bash tests/cross-harness-validation.sh` passes with 123/123.
- [ ] GitHub Flow PR checks pass before merge.
- [ ] Workguard reports no out-of-scope changes.
- [ ] All acceptance criteria are met.
</verification>

<success_criteria>
- v2.54 closure evidence is durable, source-cited, and bounded to the audit artifact.
- Validation suites include exactly one localized closure guardrail each and count changes are reconciled.
- Fresh installer, Pi validation, and cross-harness validation are green.
- No runtime, module, dependency, CI, GitHub Flow, helper delegation, lifecycle authority, or broad validation-refactor scope is introduced.
</success_criteria>

<output>
After completion, create `.paul/phases/233-validation-evidence-closure/233-01-SUMMARY.md` with closure evidence, validation count reconciliation, module execution reports, PR/CI evidence, Workguard scope evidence, deferred boundaries, and v2.54 milestone closure handoff.
</output>
