---
phase: 230-baseline-verbosity-audit
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/PALS-CONTEXTUAL-VERBOSITY-AUDIT.md
autonomous: true
---

<objective>
## Goal
Create a baseline verbosity audit artifact for v2.54 that identifies where PALS agent output, workflows, skills, module surfaces, and lifecycle artifacts are wastefully verbose versus necessarily evidentiary.

## Purpose
v2.54 should reduce agentic verbosity without weakening PALS lifecycle truth. Phase 230 establishes the evidence base before Phase 231 changes process/workflow guidance and Phase 232 changes skills.

## Output
- New `docs/PALS-CONTEXTUAL-VERBOSITY-AUDIT.md` with:
  - audit scope and rubric
  - baseline inventory of high-verbosity surfaces
  - findings grouped by workflow/process, skills, modules, and lifecycle artifacts
  - keep/pare/defer recommendations for Phases 231-232
  - explicit boundaries preserving required evidence, approvals, validation truth, GitHub Flow safety, and parent-owned APPLY
</objective>

<context>
## Project Context
- `.paul/PROJECT.md` — current focus, constraints, and active lifecycle guarantees
- `.paul/ROADMAP.md` — v2.54 milestone and Phase 230 scope
- `.paul/STATE.md` — current lifecycle position
- `.paul/SPECIAL-FLOWS.md` — required project-level skills and module expectations

## Prior Work
- v2.44 context optimization reduced hot-path read cost and added anti-regrowth guardrails.
- v2.53 closed Pi integration efficiency/effectiveness exploration with artifact-slice targeting/deduplication evidence.
- Phase 230 must not assume those prior results are enough; it should audit the current verbosity problem directly.

## Source Files to Inspect Read-Only During APPLY
- `kernel/workflows/*.md`
- `kernel/references/*.md`
- `drivers/pi/skills/*/SKILL.md`
- `modules/*/module.yaml`
- `modules/*/references/*.md`
- `.paul/STATE.md`, `.paul/ROADMAP.md`, `.paul/PROJECT.md`, `.paul/SPECIAL-FLOWS.md`
- `README.md`, `drivers/pi/skill-map.md`, `docs/*.md` where needed for docs/readme verbosity surfaces

## Files Modified by This Plan
- `docs/PALS-CONTEXTUAL-VERBOSITY-AUDIT.md`
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
- `[dispatch] pre-plan advisory: ARCH` root structure observed as markdown-first repo with `docs/`, `drivers/`, `kernel/`, `modules/`, and `tests/`; planned output belongs in `docs/`; no boundary concerns for a docs-only audit artifact.
- `[dispatch] pre-plan advisory: TODD` no `*.test.*`, `*.spec.*`, or `__tests__` files found; validation will rely on focused artifact checks plus existing shell validation suites.
- `[dispatch] pre-plan advisory: IRIS` planned file is new documentation; anti-pattern scan has no existing target. APPLY should avoid TODO/FIXME/HACK placeholders in the audit artifact.
- `[dispatch] pre-plan advisory: DAVE` no CI config files detected in scope; no CI changes planned.
- `[dispatch] pre-plan advisory: DOCS` documentation catalog exists under `docs/`; planned artifact is documentation and should be self-contained.
- `[dispatch] pre-plan advisory: CODI skipped — no extractable symbols in phase scope` because this is a markdown audit plan with no TS/JS source selectors.
- `[dispatch] pre-plan advisory: RUBY` docs-only audit; no code complexity target. Note existing verbosity/read-cost debt is the subject of the audit, not an implementation change in this phase.
- `[dispatch] pre-plan advisory: ARIA/DANA/GABE/LUKE/OMAR/PETE/REED/VERA skipped` no UI, data, API, UX, observability, performance, resilience, or privacy source changes planned.
- `[dispatch] pre-plan enforcement: DEAN PASS/SKIP` no dependency manifest or lockfile found, and no dependency changes planned.
- `[dispatch] pre-plan enforcement: SETH PASS` no source or secret-bearing file changes planned; audit artifact must not copy secrets or environment values.

## Post-Plan Module Dispatch
- `[dispatch] post-plan: TODD` plan remains `execute`; no test files cover the planned docs artifact, so no TDD restructuring is required.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | PLAN/APPLY/UNIFY lifecycle | ✓ |
| /carl | required | Session boundary management | automatic |
| TODD | required | Validation/test enforcement during APPLY | via module dispatch |
| WALT | required | Quality gating during APPLY/UNIFY | via module dispatch |

</skills>

<acceptance_criteria>
## AC-1: Audit scope is explicit and bounded
```gherkin
Given v2.54 is focused on contextual verbosity paring
When the Phase 230 audit artifact is created
Then it names the audited surfaces and excludes runtime/process changes from this phase
```

## AC-2: Wasteful versus required verbosity is distinguished
```gherkin
Given PALS must preserve lifecycle truth and evidence
When the audit classifies verbosity findings
Then each finding distinguishes paring candidates from output required for approvals, module evidence, validation truth, GitHub Flow safety, or parent-owned APPLY
```

## AC-3: Phase 231 and Phase 232 inputs are actionable
```gherkin
Given later phases will change workflows/process and skills
When the audit is complete
Then it provides prioritized keep/pare/defer recommendations for workflow/process work and skill work
```

## AC-4: Verification and scope evidence are green
```gherkin
Given this is a documentation-only audit phase
When final verification runs
Then focused artifact checks pass and changed-file scope remains limited to the audit artifact plus PALS lifecycle artifacts
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Create the verbosity audit rubric and inventory</name>
  <files>docs/PALS-CONTEXTUAL-VERBOSITY-AUDIT.md</files>
  <action>
    Create the audit artifact with concise sections for scope, rubric, and baseline inventory.
    Include the major audit surfaces: `kernel/workflows/*.md`, `kernel/references/*.md`, `drivers/pi/skills/*/SKILL.md`, `modules/*/module.yaml`, `modules/*/references/*.md`, lifecycle artifacts, README/docs, and validation output expectations.
    Define at least three categories: required evidence, useful context, and wasteful/redundant verbosity.
    Do not edit workflows, skills, modules, runtime files, validation scripts, or lifecycle mechanics in this task.
  </action>
  <verify>Focused checks find headings for Scope, Rubric, Baseline Inventory, and Preservation Boundaries in `docs/PALS-CONTEXTUAL-VERBOSITY-AUDIT.md`.</verify>
  <done>AC-1 and AC-2 satisfied: audit scope is bounded and classification rubric separates waste from required evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Classify findings and recommendations</name>
  <files>docs/PALS-CONTEXTUAL-VERBOSITY-AUDIT.md</files>
  <action>
    Add findings grouped by: workflow/process guidance, Pi skills, module surfaces, lifecycle artifacts, and validation/evidence output.
    For each group, list concrete examples or patterns observed during APPLY and classify them as keep, pare, or defer.
    Add prioritized Phase 231 inputs for process/workflow verbosity paring and Phase 232 inputs for skill verbosity updates.
    Preserve this boundary: do not recommend suppressing required approval gates, blocking evidence, module dispatch proof, fresh validation command truth, GitHub Flow readiness, or parent APPLY verification.
  </action>
  <verify>Focused checks find `Workflow / Process Findings`, `Skill Findings`, `Module Findings`, `Lifecycle / Evidence Findings`, `Phase 231 Inputs`, and `Phase 232 Inputs` in the audit artifact.</verify>
  <done>AC-2 and AC-3 satisfied: findings are grouped and later-phase inputs are actionable.</done>
</task>

<task type="auto">
  <name>Task 3: Verify audit artifact and scope</name>
  <files>docs/PALS-CONTEXTUAL-VERBOSITY-AUDIT.md</files>
  <action>
    Run focused marker checks for the audit artifact.
    Run existing validation suites if feasible: `bash tests/pi-end-to-end-validation.sh` and `bash tests/cross-harness-validation.sh`.
    Run changed-file scope checks and confirm source changes are limited to `docs/PALS-CONTEXTUAL-VERBOSITY-AUDIT.md` plus `.paul/*` lifecycle artifacts created by PLAN/APPLY/UNIFY.
    Record command results for UNIFY.
  </action>
  <verify>Focused marker checks pass; validation suites pass or any skip/failure is explicitly recorded with reason; Workguard or git scope check shows no unplanned source files.</verify>
  <done>AC-4 satisfied: verification evidence is green or clearly explained, and scope remains bounded.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- `kernel/workflows/*.md`
- `kernel/references/*.md`
- `drivers/pi/skills/*/SKILL.md`
- `modules/**`
- `drivers/pi/extensions/**`
- `tests/**`
- dependency manifests or lockfiles
- GitHub Flow configuration or lifecycle authority rules

## SCOPE LIMITS
- This phase creates an audit artifact only.
- Do not pare or rewrite workflow/process guidance yet; that belongs to Phase 231.
- Do not pare or rewrite skills yet; that belongs to Phase 232.
- Do not reduce required evidence for approvals, module dispatch, validation truth, GitHub Flow safety, parent-owned APPLY, or `.paul/*` lifecycle authority.
</boundaries>

<verification>
Before declaring APPLY complete:
- [ ] `docs/PALS-CONTEXTUAL-VERBOSITY-AUDIT.md` exists.
- [ ] Focused marker checks pass for scope, rubric, findings, preservation boundaries, Phase 231 inputs, and Phase 232 inputs.
- [ ] `bash tests/pi-end-to-end-validation.sh` passes, or any non-run/non-pass is recorded with reason.
- [ ] `bash tests/cross-harness-validation.sh` passes, or any non-run/non-pass is recorded with reason.
- [ ] Changed-file scope is limited to `docs/PALS-CONTEXTUAL-VERBOSITY-AUDIT.md` plus PALS lifecycle artifacts.
- [ ] All acceptance criteria are met.
</verification>

<success_criteria>
- One audit artifact exists and is self-contained.
- Wasteful verbosity candidates are separated from required evidence.
- Phase 231 and Phase 232 have concrete starting inputs.
- No workflow, skill, module, runtime, validation, dependency, or GitHub Flow behavior changes occur in this phase.
</success_criteria>

<output>
After completion, create `.paul/phases/230-baseline-verbosity-audit/230-01-SUMMARY.md` with acceptance results, verification commands, findings summary, deferred scope, and next-phase handoff.
</output>
