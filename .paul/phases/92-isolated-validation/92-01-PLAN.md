---
phase: 92-isolated-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
---

<objective>
## Goal
Validate PALS GitHub Flow end-to-end on the isolated `gh-flow-test` repo by exercising branch creation, push, PR creation, and merge — proving the v2.12/v2.13 workflow mechanics work in real use.

## Purpose
The GitHub Flow implementation was built across v2.12 (enforcement) and v2.13 (hardening) but has never been run against a real GitHub repo. This plan validates that the workflow steps described in `apply-phase.md` (preflight/postflight) and `unify-phase.md` (merge gate) actually work with `gh` CLI operations.

## Output
- Validation results documenting which GitHub Flow operations succeed/fail
- Any bugs or workflow issues documented for Phase 94 fixes
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md

## Source Files
pals.json (GitHub Flow config)
/Users/maxwellnewman/claude/gh-flow-test/pals.json (test project config)
</context>

<acceptance_criteria>

## AC-1: Feature Branch Creation
```gherkin
Given the gh-flow-test repo is on main with github-flow config
When a new feature branch is created following preflight logic
Then the branch exists locally and can be pushed to origin
```

## AC-2: PR Creation via gh CLI
```gherkin
Given a feature branch with at least one commit pushed to origin
When gh pr create is executed with appropriate parameters
Then a PR is created on GitHub linking the feature branch to main
```

## AC-3: Merge via gh CLI
```gherkin
Given an open PR with no required reviews or CI checks
When gh pr merge is executed with squash method
Then the PR is merged, the branch is deleted on remote, and local main is updated
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Exercise GitHub Flow preflight (branch creation & validation)</name>
  <files>(gh-flow-test repo — no pals source files modified)</files>
  <action>
    In the gh-flow-test repo:
    1. Read pals.json to confirm `workflow: "github-flow"` and extract config
    2. Detect current branch (`git rev-parse --abbrev-ref HEAD`) — should be `main`
    3. Detect base branch from config (`base_branch: "main"`)
    4. Create a feature branch: `git checkout -b feature/92-test-validation`
    5. Make a small change (add a test file)
    6. Commit and push: `git push -u origin feature/92-test-validation`
    7. Record results (success/failure, any errors)
  </action>
  <verify>
    Run in gh-flow-test:
    - `git rev-parse --abbrev-ref HEAD` returns `feature/92-test-validation`
    - `git ls-remote --heads origin feature/92-test-validation` shows the remote branch exists
  </verify>
  <done>AC-1 satisfied: Feature branch created locally and pushed to origin</done>
</task>

<task type="auto">
  <name>Task 2: Exercise GitHub Flow postflight (PR creation) and merge gate</name>
  <files>(gh-flow-test repo — no pals source files modified)</files>
  <action>
    In the gh-flow-test repo, on the feature branch:
    1. Create PR: `gh pr create --base main --head feature/92-test-validation --title "Phase 92: Test Validation" --body "PALS GitHub Flow validation test"`
    2. Record PR number and URL
    3. Check PR status: `gh pr status`
    4. Since ci_checks is false, skip CI wait
    5. Merge PR: `gh pr merge --squash --delete-branch`
    6. Verify merge: `git checkout main && git pull origin main`
    7. Verify remote branch deleted: `git ls-remote --heads origin feature/92-test-validation` should return empty
    8. Record all results
  </action>
  <verify>
    Run in gh-flow-test:
    - `gh pr list --state merged` shows the test PR
    - `git log --oneline -1` on main shows the squash-merged commit
    - `git ls-remote --heads origin feature/92-test-validation` returns empty (branch deleted)
  </verify>
  <done>AC-2 and AC-3 satisfied: PR created, merged with squash, branch cleaned up</done>
</task>

<task type="auto">
  <name>Task 3: Document validation results</name>
  <files>.paul/phases/92-isolated-validation/validation-results.md</files>
  <action>
    Create a validation results file documenting:
    1. Each GitHub Flow operation tested (branch create, push, PR create, PR merge, branch delete)
    2. Pass/fail status for each
    3. Any errors, warnings, or unexpected behavior
    4. Whether the pals.json config was read correctly
    5. Any issues that need fixing in Phase 94
  </action>
  <verify>File exists at .paul/phases/92-isolated-validation/validation-results.md with all operations documented</verify>
  <done>All ACs documented with pass/fail evidence</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- No kernel workflow files modified (this is validation only)
- No pals source code changes
- pals.json on the pals repo (read-only for this phase)

## SCOPE LIMITS
- Only test on gh-flow-test repo (pals repo tested in Phase 93)
- ci_checks validation deferred (no CI configured on test repo)
- No resume/pause/status workflow testing (Phase 93)

</boundaries>

<verification>
Before declaring plan complete:
- [ ] Feature branch created and pushed successfully
- [ ] PR created via gh CLI
- [ ] PR merged with squash method
- [ ] Remote branch deleted after merge
- [ ] Local main updated with merged changes
- [ ] All results documented in validation-results.md
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All 3 core GitHub Flow operations (branch/PR/merge) work via gh CLI
- Results documented with pass/fail evidence
- Any issues identified for Phase 94
</success_criteria>

<output>
After completion, create `.paul/phases/92-isolated-validation/92-01-SUMMARY.md`
</output>
