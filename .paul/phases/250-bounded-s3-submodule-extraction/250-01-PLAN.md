---
phase: 250-bounded-s3-submodule-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - drivers/pi/extensions/guided-workflow-detection.ts
  - drivers/pi/extensions/pals-hooks.ts
  - tests/pi-end-to-end-validation.sh
autonomous: true
---

<objective>
## Goal
Extract the S3 `guided-workflow-detection` subsystem from `drivers/pi/extensions/pals-hooks.ts` into `drivers/pi/extensions/guided-workflow-detection.ts` without changing guided workflow behavior.

## Purpose
This continues the v2.58 sibling Pi extension extraction wave after Phase 249 established the S3 contract. The extraction reduces `pals-hooks.ts` responsibility while preserving option parsing, merge-gate routing detection, no inferred merge intent, transcript-visible canonical reply separation, and guided workflow authority boundaries.

## Output
A new sibling Pi extension helper module owns the S3 detection functions/constants; `pals-hooks.ts` imports and delegates to it; Pi validation gains one bounded guardrail proving the extraction boundary and loader-compat invariant.
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md
.paul/SPECIAL-FLOWS.md

## Prior Work
.paul/phases/249-s3-extraction-contract-bounded-submodule-plan/249-01-SUMMARY.md
docs/PI-NATIVE-GUIDED-WORKFLOW-DETECTION-EXTRACTION-CONTRACT.md
docs/PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md
docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md

## Source Files
drivers/pi/extensions/pals-hooks.ts
drivers/pi/extensions/artifact-slice-rendering.ts
drivers/pi/extensions/workflow-resource-capsule-rendering.ts
drivers/pi/extensions/module-activity-parsing.ts
tests/pi-end-to-end-validation.sh

## CODI Seed Candidates
`detectGuidedWorkflowMoment`, `parseGuidedWorkflowOptions`, `isMergeGateRoutingPrompt`, `makeGuidedWorkflowSignature`, `GUIDED_WORKFLOW_LOOKBACK`
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: TODD | IRIS | DAVE | DOCS | CODI | RUBY evaluated for planned files.

- TODD: No `*.test.*`, `*.spec.*`, package test framework, or TDD candidate covering the planned TS extraction was detected. Existing shell validation suites remain the verification path; plan type stays `execute`.
- IRIS: No `TODO`, `FIXME`, `HACK`, or `XXX` markers found in `drivers/pi/extensions/pals-hooks.ts` or `tests/pi-end-to-end-validation.sh`.
- DAVE: No CI config found in scope (`.github/workflows`, Dockerfile, GitLab CI, Jenkinsfile absent). Advisory only: do not add CI in this extraction plan.
- DOCS: Related contract docs exist for the extraction (`docs/PI-NATIVE-GUIDED-WORKFLOW-DETECTION-EXTRACTION-CONTRACT.md`, extraction baseline, modularization contract). Phase 251 owns user-facing docs surfacing, so docs edits are out of scope here.
- RUBY: Planned files are marked for post-unify debt/size awareness; avoid broad rewrites and keep `pals-hooks.ts` diff primarily import/delegation plus removed S3 bodies.

[dispatch] CODI: pre-plan advisory → impact × 5 symbols → 4 resolved, 1 unresolved, 11 total call-sites, injected blast_radius

### Blast Radius (CODI)
**detectGuidedWorkflowMoment**
```text
drivers/pi/extensions/pals-hooks.ts:1167 maybePresentGuidedWorkflow behavioral depth:1
drivers/pi/extensions/pals-hooks.ts:997 palsHooks behavioral depth:2
```

**parseGuidedWorkflowOptions**
```text
drivers/pi/extensions/pals-hooks.ts:579 detectGuidedWorkflowMoment behavioral depth:1
drivers/pi/extensions/pals-hooks.ts:1167 maybePresentGuidedWorkflow behavioral depth:2
drivers/pi/extensions/pals-hooks.ts:997 palsHooks behavioral depth:3
```

**isMergeGateRoutingPrompt**
```text
drivers/pi/extensions/pals-hooks.ts:579 detectGuidedWorkflowMoment behavioral depth:1
drivers/pi/extensions/pals-hooks.ts:1167 maybePresentGuidedWorkflow behavioral depth:2
drivers/pi/extensions/pals-hooks.ts:997 palsHooks behavioral depth:3
```

**makeGuidedWorkflowSignature**
```text
drivers/pi/extensions/pals-hooks.ts:579 detectGuidedWorkflowMoment behavioral depth:1
drivers/pi/extensions/pals-hooks.ts:1167 maybePresentGuidedWorkflow behavioral depth:2
drivers/pi/extensions/pals-hooks.ts:997 palsHooks behavioral depth:3
```

**Unresolved:**
- `GUIDED_WORKFLOW_LOOKBACK` — Symbol not found by codegraph (constant remains contract-bound by source/marker checks).

[dispatch] pre-plan enforcement: DEAN PASS — no package manager manifest/lockfile found (`package.json`, pnpm/npm/yarn locks absent), so dependency audit has no applicable command and no critical/high dependency finding blocks plan creation.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | PLAN/APPLY/UNIFY lifecycle work | ✓ current plan skill loaded |
| TODD | required | APPLY validation/test enforcement | ○ module overlay during APPLY |
| WALT | required | APPLY/UNIFY quality gating | ○ module overlay during APPLY/UNIFY |

**BLOCKING:** Required skills/modules must be honored before APPLY proceeds.
</skills>

<acceptance_criteria>

## AC-1: S3 sibling module exists and owns detection boundary
```gherkin
Given the Phase 249 S3 extraction contract
When Phase 250 extraction is complete
Then `drivers/pi/extensions/guided-workflow-detection.ts` exists and exports the S3 functions/constants named in the contract with a no-op default Pi extension factory.
```

## AC-2: `pals-hooks.ts` delegates without behavior drift
```gherkin
Given existing guided workflow prompts, options, merge-gate routing prompts, and explicit canonical reply handling
When the S3 functions are imported from the sibling module
Then `pals-hooks.ts` keeps the same call behavior and does not extract or alter S4 canonical-reply delivery, S6 lifecycle UI, S7 context injection, or S8 command routing.
```

## AC-3: Validation proves the extraction boundary
```gherkin
Given the source extraction and one bounded Pi validation guardrail
When install, Pi validation, cross-harness validation, artifact consistency, and diff whitespace checks run
Then the commands pass, any validation count delta is reconciled, and no installed runtime copy or installer/manifest file is edited directly.
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Extract guided-workflow-detection sibling module</name>
  <files>drivers/pi/extensions/guided-workflow-detection.ts, drivers/pi/extensions/pals-hooks.ts</files>
  <action>
    Create `drivers/pi/extensions/guided-workflow-detection.ts` following the S5/S1/S2 sibling pattern:
    - Add a module contract header citing Phase 250, `docs/PI-NATIVE-GUIDED-WORKFLOW-DETECTION-EXTRACTION-CONTRACT.md`, `docs/PI-NATIVE-GUIDED-WORKFLOW-EVIDENCE-CONTRACT.md`, and `Authority: Derived aid only`.
    - Move/export the S3-owned constants `GUIDED_WORKFLOW_LOOKBACK` and `GUIDED_WORKFLOW_SIGNATURE_BYTES` into the sibling module as single-defined contract markers.
    - Move/export these S3 functions with behavior preserved: `summarizeWorkflowPrompt`, `extractNextActionSummary`, `isValidOptionId`, `parseGuidedWorkflowOptions`, `detectExplicitCanonicalResponse`, `isMergeGateRoutingPrompt`, `extractMergeGateRoutingSummary`, `makeGuidedWorkflowSignature`, `detectGuidedWorkflowMoment`.
    - Define/export the S3-owned `GuidedWorkflowOption` and `GuidedWorkflowMoment` types from the sibling if needed by `pals-hooks.ts`.
    - Import only required helpers/types from `./pals-hooks` (for example `compactWhitespace`, `PalsStateSnapshot`, and if necessary an exported `extractLoopSignature` helper). Keep helper imports minimal; do not duplicate helpers.
    - End the new sibling with the exact loader-compat marker phrase `No-op Pi extension factory` and `export default function (_pi: unknown): void {}`.
    - In `pals-hooks.ts`, import the moved functions/types/constants from `./guided-workflow-detection`; keep `collectRecentAssistantTexts` using imported `GUIDED_WORKFLOW_LOOKBACK`; keep `maybePresentGuidedWorkflow` calling imported `detectGuidedWorkflowMoment`; do not change S4 `sendCanonicalWorkflowResponse`, `presentGuidedWorkflowMoment`, `loadGuidedWorkflowConfig`, or `shouldAutoPresent` behavior.
    Avoid: installer/uninstaller/driver.yaml edits, S4/S6/S7/S8 extraction, canonical reply rewrites, config semantic changes, telemetry/hidden state, or direct edits under `~/.pi/agent/extensions/`.
  </action>
  <verify>Focused source checks: all 10 S3 identifiers are present in repo source; `pals-hooks.ts` imports from `./guided-workflow-detection`; `pals-hooks.ts` no longer declares the moved constants/functions inline; S4 delivery identifiers remain in `pals-hooks.ts`.</verify>
  <done>AC-1 and AC-2 satisfied: S3 has a loader-compatible sibling module and `pals-hooks.ts` delegates without S4/S6/S7/S8 scope creep.</done>
</task>

<task type="auto">
  <name>Task 2: Add bounded Pi validation guardrail for S3 extraction</name>
  <files>tests/pi-end-to-end-validation.sh</files>
  <action>
    Add one localized Pi-supported-runtime guardrail near the existing Phase 239/243 extraction guardrails:
    - Define/use `EXT_GUIDED_WORKFLOW_DETECTION="$REPO_ROOT/drivers/pi/extensions/guided-workflow-detection.ts"`.
    - Assert the new sibling exists and exports `GUIDED_WORKFLOW_LOOKBACK`, `GUIDED_WORKFLOW_SIGNATURE_BYTES`, all S3 functions, and the `No-op Pi extension factory` marker.
    - Assert `pals-hooks.ts` imports from `./guided-workflow-detection`.
    - Assert `pals-hooks.ts` no longer declares the moved `GUIDED_WORKFLOW_*` constants or S3 functions inline.
    - Assert S4 canonical-reply delivery markers remain in `pals-hooks.ts` (`sendCanonicalWorkflowResponse`, `presentGuidedWorkflowMoment`, `loadGuidedWorkflowConfig`, `shouldAutoPresent`, `pi.sendUserMessage`, `notify-only mode never sends a canonical reply`).
    Keep this to one TAP block/check family if possible. Expected Pi validation count may increase from 223 to 224; do not edit cross-harness validation in this phase unless command output proves a shared-invariant guardrail is necessary.
  </action>
  <verify>`bash tests/pi-end-to-end-validation.sh` passes; expected count baseline is 223/223 and a planned +1 guardrail should reconcile as 224/224 if added.</verify>
  <done>AC-3 satisfied for the Pi validation proof: extraction boundary is command-verifiable with bounded count reconciliation.</done>
</task>

<task type="auto">
  <name>Task 3: Run installation and full validation evidence</name>
  <files>drivers/pi/extensions/guided-workflow-detection.ts, drivers/pi/extensions/pals-hooks.ts, tests/pi-end-to-end-validation.sh</files>
  <action>
    Run the required verification set and record exact command output for SUMMARY:
    - `PALS_ROOT="$PWD" bash drivers/pi/install.sh` — verify the installed extension count includes the new sibling (expected 5 files if no other extension files changed).
    - `bash tests/pi-end-to-end-validation.sh` — reconcile any count delta from the Phase 249 baseline 223/223.
    - `bash tests/cross-harness-validation.sh` — expected Phase 249 baseline 131/131 unless an approved shared-invariant guardrail is added.
    - `bash tests/helpers/artifact_consistency.sh` — must PASS.
    - `git diff --check` — must be clean.
    If validation output reveals an unplanned count change or scope drift, stop and reconcile in-place before marking APPLY complete.
  </action>
  <verify>All listed commands pass with command-output truth captured for SUMMARY.md.</verify>
  <done>AC-3 satisfied: install, Pi validation, cross-harness validation, artifact consistency, and diff whitespace evidence are green and reconciled.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `drivers/pi/install.sh`, `drivers/pi/uninstall.sh`, `drivers/pi/driver.yaml` for this extraction; Phase 241 generalized install source-set already covers sibling `*.ts` files.
- S4 canonical-reply delivery behavior: `sendCanonicalWorkflowResponse`, `presentGuidedWorkflowMoment`, `loadGuidedWorkflowConfig`, `shouldAutoPresent` must remain behaviorally unchanged and in `pals-hooks.ts`.
- S6 lifecycle UI, S7 context injection, S8 command routing, CARL session boundary, module activity parsing, artifact-slice rendering, workflow/resource capsule rendering.
- Dependency manifests, CI config, kernel workflows, module registry, Claude Code driver, Agent SDK driver.
- Installed runtime copies under `~/.pi/agent/extensions/` except through `PALS_ROOT="$PWD" bash drivers/pi/install.sh`.

## SCOPE LIMITS
- Source extraction only: no user-facing docs surfacing; Phase 251 owns README/skill-map/contract surfacing.
- At most one localized Pi validation guardrail unless command output proves a minimal repair is necessary.
- No telemetry, hidden runtime state, generated lifecycle ledger, cache-as-truth, alternate validation/module/lifecycle/GitHub Flow ledger, auto-approval, auto-continue, skipped checkpoints, UI-only lifecycle decisions, or inferred merge intent.
- `pals-implementer` may be used only for a bounded task packet; parent APPLY owns diff review, verification, module gates, fallback, GitHub Flow gates, and `.paul/*` lifecycle writes.
</boundaries>

<verification>
Before declaring plan complete:
- [ ] Focused source checks prove all S3 identifiers are present in repo source and S4/S6/S7/S8 remain out of scope.
- [ ] `PALS_ROOT="$PWD" bash drivers/pi/install.sh` passes and reports the new sibling in the installed extension count.
- [ ] `bash tests/pi-end-to-end-validation.sh` passes; reconcile expected 223→224 if one guardrail is added.
- [ ] `bash tests/cross-harness-validation.sh` passes; expected baseline 131/131 unless explicitly reconciled.
- [ ] `bash tests/helpers/artifact_consistency.sh` passes.
- [ ] `git diff --check` is clean.
- [ ] All acceptance criteria met.
</verification>

<success_criteria>
- `guided-workflow-detection.ts` exists, is loader-compatible, and owns the S3 detection functions/constants.
- `pals-hooks.ts` is smaller and delegates to the sibling with no guided workflow behavior drift.
- S4 canonical replies, S6 lifecycle UI, S7 context injection, and S8 command routing remain untouched.
- Validation command output passes and count deltas are explicitly reconciled.
- No out-of-scope files are changed beyond normal `.paul/*` lifecycle artifacts.
</success_criteria>

<output>
After completion, create `.paul/phases/250-bounded-s3-submodule-extraction/250-01-SUMMARY.md` with AC results, focused source evidence, install output, validation output/count reconciliation, module reports, GitHub Flow evidence, and any deviations.
</output>
