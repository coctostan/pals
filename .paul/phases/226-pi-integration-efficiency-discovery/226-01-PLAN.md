---
phase: 226-pi-integration-efficiency-discovery
plan: 01
type: research
wave: 1
depends_on: []
files_modified:
  - docs/PI-INTEGRATION-EFFICIENCY-EFFECTIVENESS-DISCOVERY.md
autonomous: true
---

# Phase 226 Plan 01: Pi Integration Efficiency Discovery

<objective>

## Goal

Create a source-cited discovery artifact that identifies the highest-value Pi integration efficiency and effectiveness hotspots after v2.45-v2.52, without authorizing runtime, workflow, validation, dependency, GitHub Flow, helper-delegation, or install-surface changes.

## Purpose

v2.53 is exploratory. Before selecting a candidate improvement or implementation spike, PALS needs a bounded evidence pass over existing Pi-native architecture docs, recent milestone results, and current Pi adapter surfaces so Phase 227 can design improvements from observed friction rather than assumptions.

## Output

- New discovery artifact: `docs/PI-INTEGRATION-EFFICIENCY-EFFECTIVENESS-DISCOVERY.md`
- No runtime/source behavior changes.
- No validation-count changes expected.

</objective>

<context>

## Project Context

- `.paul/PROJECT.md` — current product posture, active v2.53 focus, constraints, and active requirements summary.
- `.paul/ROADMAP.md` — v2.53 phase routing and Phase 226 discovery scope.
- `.paul/STATE.md` — lifecycle truth, current decisions, validation baselines, and session continuity.

## Primary Source Inputs

- `docs/PI-NATIVE-CONTEXT-EFFICIENCY-ARCHITECTURE.md` — context-cost surface model, guardrails, read/citation contract, and earlier spike backlog.
- `docs/PI-NATIVE-ARCHITECTURE-PROPOSAL.md` — promoted/rejected Pi-native patterns and authority boundaries.
- `docs/PI-NATIVE-IMPLEMENTATION-PLAN.md` — prior build sequence, acceptance-criteria families, validation classes, and command-output evidence rules.
- `drivers/pi/extensions/README.md` — current Pi extension surfaces: command wrappers, guided workflow UX, artifact slices, workflow/resource capsules, module visibility, CARL, shortcuts, and install posture.
- `drivers/pi/skill-map.md` — current command/skill/workflow mapping and Pi adapter surface inventory.
- `drivers/pi/extensions/pals-hooks.ts` — read-only implementation input for current constants/surface inventory only; do not edit in this plan.

## Recent Prior Work

- `.paul/phases/225-evidence-reconciliation-milestone-closure/225-01-SUMMARY.md` — v2.52 closure evidence, validation baselines, deferred validation-script size debt, and command-output truth posture.
- Recent milestone summaries or docs for v2.47-v2.52 may be read selectively only when they explain a candidate hotspot or validation/evidence pattern.

</context>

<module_dispatch>

## Pre-Plan Module Dispatch

[dispatch] pre-plan advisory: modules.yaml loaded from installed PALS registry (`kernel_version: 2.0.0`). Planned APPLY file set is docs-only: `docs/PI-INTEGRATION-EFFICIENCY-EFFECTIVENESS-DISCOVERY.md`.

- TODD: Project-specific test framework files were not detected for this docs-only discovery artifact; shell validation suites remain the proof path. TDD conversion is not recommended for this research plan.
- IRIS: Planned file does not exist yet; no TODO/FIXME/HACK/XXX findings are present pre-create.
- DAVE: No CI config was detected in the bounded scan; no CI changes are planned.
- DOCS: Documentation catalog includes existing Pi-native docs and validation reports. New discovery artifact belongs under `docs/` and should cite related Pi-native source docs.
- CODI: skipped — no extractable symbols in phase scope. This plan modifies no TS/JS source file; `drivers/pi/extensions/pals-hooks.ts` is read-only context only.
- RUBY: No preliminary debt indicators in the planned docs-only file. Existing validation-script size debt remains separate future driver-quality scope.
- ARCH: Detected repo pattern: modular kernel/driver/modules documentation-heavy architecture (high confidence). Planned docs file is in the expected `docs/` layer. No circular dependency risk; no god-file check is possible before file creation.
- ARIA/LUKE: No UI files in planned changes — skipping.
- DANA/GABE/OMAR/PETE/REED/VERA: No data, API, runtime observability, performance, resilience, or privacy-relevant source files in planned changes — skipping.

[dispatch] pre-plan enforcement: DEAN detected no package manager lock/manifest requiring audit in the repo root; no vulnerability baseline is present or needed for this docs-only plan. SETH found no pre-create secret surface in planned files. PASS — no enforcement block.

## Post-Plan Module Dispatch

[dispatch] post-plan: TODD reviewed plan type `research`; shell validation and marker checks are appropriate because the plan creates a discovery document rather than implementation code. No RED/GREEN/REFACTOR restructuring required.

</module_dispatch>

<skills>

## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | Required | PLAN/APPLY/UNIFY lifecycle management | ✓ |
| /carl | Required | Session boundary management | active via Pi extension |
| TODD | Required | Test/verification enforcement during APPLY | active via module dispatch |
| WALT | Required | Quality gating during APPLY/UNIFY | active via module dispatch |

</skills>

<acceptance_criteria>

## AC-1: Current Pi integration surfaces are inventoried

```gherkin
Given v2.53 Phase 226 is a discovery phase
When the discovery artifact is created
Then it lists the current Pi integration surfaces relevant to efficiency and effectiveness, with source citations to existing docs and/or repo files
```

## AC-2: Efficiency and effectiveness hotspots are ranked

```gherkin
Given the current Pi integration surface inventory and prior Pi-native architecture evidence
When the discovery artifact analyzes opportunity areas
Then it ranks candidate hotspots by expected context-cost reduction, workflow reliability impact, implementation risk, and evidence readiness
```

## AC-3: Authority and scope boundaries remain intact

```gherkin
Given `.paul/*`, PLAN/APPLY/UNIFY, command-output truth, module evidence, GitHub Flow gates, parent-owned APPLY, and explicit approval/checkpoint behavior are protected invariants
When Phase 226 completes
Then the discovery artifact does not authorize runtime, workflow, validation-script, dependency, CI, helper-delegation, GitHub Flow, install-surface, or lifecycle-authority changes
```

## AC-4: Phase 227 receives actionable design inputs

```gherkin
Given Phase 227 will design candidate improvements
When Phase 226 completes
Then the discovery artifact provides a recommended shortlist, open questions, non-goals, and evidence requirements suitable for Phase 227 planning
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Inventory current Pi integration surfaces and evidence inputs</name>
  <files>docs/PI-INTEGRATION-EFFICIENCY-EFFECTIVENESS-DISCOVERY.md</files>
  <action>
    Create the discovery document with sections for status, source inputs, current Pi integration surface inventory, and non-negotiable boundaries.
    Use only source-cited evidence from existing docs, `.paul/*` lifecycle artifacts, and read-only Pi adapter files.
    Include at minimum these surface families: artifact slices, workflow/resource capsules, guided workflow UX, live module visibility, CARL/session continuity, command/shortcut routing, helper delegation, validation/report surfacing, GitHub Flow/status surfacing, and install-surface posture.
    Avoid treating docs, copied validation counts, runtime widgets, injected context, helper reports, or UI state as authoritative proof.
  </action>
  <verify>grep -n "Current Pi Integration Surfaces\|Artifact slices\|Workflow/resource capsules\|Guided workflow UX\|CARL\|Authority Boundaries" docs/PI-INTEGRATION-EFFICIENCY-EFFECTIVENESS-DISCOVERY.md</verify>
  <done>AC-1 and AC-3 satisfied: current surfaces are inventoried with authority boundaries and source citations.</done>
</task>

<task type="auto">
  <name>Task 2: Rank efficiency and effectiveness hotspots</name>
  <files>docs/PI-INTEGRATION-EFFICIENCY-EFFECTIVENESS-DISCOVERY.md</files>
  <action>
    Add a ranked hotspot analysis table.
    For each candidate, include: observed friction, likely Pi integration improvement, expected efficiency gain, expected effectiveness/reliability gain, implementation risk, evidence readiness, and why it is or is not a good Phase 228 spike candidate.
    Consider at least these candidates: context payload deduplication or better slice targeting, module-dispatch evidence lens, guided workflow prompt/reply reliability, CARL bootstrap/resume precision, shortcut/next-action routing, helper report inspection ergonomics, GitHub Flow status lens, and validation/report classification surfacing.
    Keep implementation ideas at discovery granularity; do not specify code edits beyond likely future surfaces.
  </action>
  <verify>grep -n "Ranked Hotspots\|Expected efficiency gain\|Expected effectiveness\|Evidence readiness\|Phase 228" docs/PI-INTEGRATION-EFFICIENCY-EFFECTIVENESS-DISCOVERY.md</verify>
  <done>AC-2 satisfied: candidate hotspots are ranked with risk and evidence-readiness reasoning.</done>
</task>

<task type="auto">
  <name>Task 3: Produce Phase 227 handoff and verify scope safety</name>
  <files>docs/PI-INTEGRATION-EFFICIENCY-EFFECTIVENESS-DISCOVERY.md</files>
  <action>
    Add a Phase 227 handoff section with recommended shortlist, design questions, evidence requirements, and explicit non-goals.
    State that Phase 226 authorizes discovery documentation only.
    Preserve deferred boundaries: no runtime behavior change, no validation-script change, no dependency/CI change, no GitHub Flow automation change, no helper-delegation semantic change, no install-surface change, no lifecycle-authority change, and no `.paul/*` authority change.
    Include validation commands for APPLY: focused grep checks and, if no source/runtime files changed, no full Pi/cross-harness suite requirement unless APPLY discovers source-impacting edits.
  </action>
  <verify>grep -n "Phase 227 Handoff\|Recommended shortlist\|Evidence requirements\|Non-goals\|discovery documentation only" docs/PI-INTEGRATION-EFFICIENCY-EFFECTIVENESS-DISCOVERY.md && git diff --name-only</verify>
  <done>AC-3 and AC-4 satisfied: Phase 227 has actionable inputs and scope boundaries are reviewable from the artifact and changed-file list.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE

- `drivers/pi/extensions/pals-hooks.ts`
- `drivers/pi/extensions/README.md`
- `drivers/pi/skill-map.md`
- `drivers/pi/install.sh`
- `drivers/pi/uninstall.sh`
- `kernel/workflows/**`
- `kernel/references/**`
- `modules/**`
- `tests/pi-end-to-end-validation.sh`
- `tests/cross-harness-validation.sh`
- `pals.json`
- Any installed runtime copy under `~/.pi/**`

## SCOPE LIMITS

- This plan creates one discovery documentation artifact only.
- No runtime, workflow, validation-script, dependency, CI, GitHub Flow automation, helper-agent, install-surface, or lifecycle-authority change is approved.
- Do not treat v2.45-v2.52 docs as current pass/fail proof; command-output truth remains authoritative.
- Do not reopen frozen Claude Code / Agent SDK support parity.
- Do not address validation-script size debt except as a deferred hotspot or non-goal.

</boundaries>

<verification>

Before declaring APPLY complete:

- [ ] `grep -n "Current Pi Integration Surfaces\|Ranked Hotspots\|Phase 227 Handoff\|Authority Boundaries" docs/PI-INTEGRATION-EFFICIENCY-EFFECTIVENESS-DISCOVERY.md`
- [ ] `grep -n "discovery documentation only\|No runtime behavior change\|No validation-script change\|command-output truth\|\.paul/\*" docs/PI-INTEGRATION-EFFICIENCY-EFFECTIVENESS-DISCOVERY.md`
- [ ] `git diff --name-only` shows only the approved discovery artifact plus expected `.paul/*` lifecycle artifacts for PLAN/APPLY/UNIFY.
- [ ] If APPLY changes any runtime/source/test/install file despite this plan, stop and request plan revision before continuing.
- [ ] All acceptance criteria met.

</verification>

<success_criteria>

- `docs/PI-INTEGRATION-EFFICIENCY-EFFECTIVENESS-DISCOVERY.md` exists and is source-cited.
- The artifact inventories current Pi integration surfaces relevant to efficiency and effectiveness.
- The artifact ranks hotspots and identifies a Phase 227/228 candidate shortlist.
- The artifact states explicit boundaries and non-goals preserving lifecycle authority and command-output truth.
- No source/runtime/test/install/dependency/CI files are modified.

</success_criteria>

<output>

After completion, create `.paul/phases/226-pi-integration-efficiency-discovery/226-01-SUMMARY.md` with AC results, discovery artifact path, focused grep evidence, changed-file scope, module reports, and Phase 227 handoff.

</output>
