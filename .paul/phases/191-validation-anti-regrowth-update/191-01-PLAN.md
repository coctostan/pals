---
phase: 191-validation-anti-regrowth-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/pi-end-to-end-validation.sh
  - tests/cross-harness-validation.sh
  - .paul/STATE.md
  - .paul/ROADMAP.md
autonomous: true
---

<objective>
## Goal
Refresh the PALS validation suites so Phase 188-190 context-slimming gains are protected by durable anti-regrowth checks without relying on brittle long exact-prose assertions.

## Purpose
v2.44 reduced recurring context cost by slimming wrappers, archiving completed ROADMAP history, centralizing GitHub Flow mechanics, hoisting module-dispatch taxonomy, and making APPLY references conditional. Phase 191 locks those gains into validation so future edits preserve artifact-first truth, module evidence, GitHub Flow safety, archive authority, and parent-owned APPLY semantics.

## Output
- Updated `tests/pi-end-to-end-validation.sh` with source/installed anti-regrowth guardrails for active-window ROADMAP and hot workflow/read-budget properties.
- Updated `tests/cross-harness-validation.sh` with cross-driver equivalents that verify Claude Code, Pi, and repo-source surfaces keep the same context-diet contracts.
- Lifecycle updates in `.paul/STATE.md` and `.paul/ROADMAP.md`; APPLY/UNIFY will create `.paul/phases/191-validation-anti-regrowth-update/191-01-SUMMARY.md`.
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md
.paul/phases/189-hot-path-read-audit/189-01-HOT-PATH-READ-AUDIT.md
.paul/phases/190-artifact-structure-simplification/190-02-SUMMARY.md

## Source Files
tests/pi-end-to-end-validation.sh
tests/cross-harness-validation.sh

## Planning Posture
- Collaboration level: high (project default from `pals.json`).
- Planning mode: direct-requirements — Phase 191 scope is already defined by ROADMAP and handoff context.
- No checkpoint is required; the work is validation-script-only plus lifecycle artifacts.
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: ARCH PASS — repo layout is flat/kernel-driver-module-test oriented; planned code changes are existing shell validation suites plus lifecycle markdown. No circular dependency risk. Planned file line counts: `tests/pi-end-to-end-validation.sh` 1237, `tests/cross-harness-validation.sh` 899, `.paul/STATE.md` 97, `.paul/ROADMAP.md` 68; validation scripts are large existing harnesses but this plan should keep additions localized.

[dispatch] pre-plan advisory: TODD — root repo has no first-party `*.test.*` / `*.spec.*` framework files or package manager manifest; canonical verification is the two shell validation suites (`bash tests/pi-end-to-end-validation.sh`, `bash tests/cross-harness-validation.sh`). Ignore installed `.pi/npm/node_modules` test files as generated dependency noise.

[dispatch] pre-plan advisory: IRIS PASS — no TODO/FIXME/HACK/XXX markers found in planned files.

[dispatch] pre-plan advisory: SETH PASS — no hardcoded secret patterns found in planned files.

[dispatch] pre-plan advisory: DAVE — no CI config is present; this plan changes local validation suites only and does not add CI configuration.

[dispatch] pre-plan advisory: DOCS — markdown catalog exists; this plan updates validation harnesses and lifecycle artifacts, not user-facing docs. README updates are out of scope unless validation text directly changes user setup guidance.

[dispatch] pre-plan advisory: ARIA, DANA, GABE, LUKE, VERA skipped — no UI, schema/data, API, UX, or privacy files in planned changes.

[dispatch] pre-plan advisory: OMAR, PETE, REED skipped/warn-low — planned shell validation changes are not production runtime observability/performance/resilience surfaces; keep shell helpers bounded and fail messages actionable.

[dispatch] CODI: skipped — no extractable symbols in phase scope.

[dispatch] pre-plan advisory: RUBY — keep validation-script additions factored into small shell helper functions and avoid repeating near-identical blocks where a loop over repo/installed surfaces will do.

[dispatch] pre-plan enforcement: DEAN PASS/SKIP — no package manager or dependency manifest detected; no dependency audit command applies and no dependency files are planned.

[dispatch] post-plan: TODD — execute plan remains appropriate; validation suites themselves are the proof mechanism, no TDD conversion candidate.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | PLAN/APPLY/UNIFY lifecycle management | ✓ |
| /carl | required | Session boundary monitoring | active via Pi extension |
| TODD | required | Validation/test discipline during APPLY | via module dispatch |
| WALT | required | Quality gating after implementation | via module dispatch |

**BLOCKING:** Required skills/modules must be honored during APPLY; this PLAN has already loaded `/paul` and surfaced TODD/WALT obligations through dispatch.
</skills>

<acceptance_criteria>

## AC-1: Active lifecycle artifacts stay compact and authoritative
```gherkin
Given Phase 190 moved completed ROADMAP history to an authoritative archive
When the validation suites run after Phase 191 changes
Then they fail if live `.paul/ROADMAP.md` regrows beyond an active-window budget or loses the completed-history archive pointer
```

## AC-2: Hot workflow/read-cost budgets are validated across source and installed surfaces
```gherkin
Given hot PLAN/APPLY/UNIFY workflows and shared references define recurring lifecycle read cost
When Pi and cross-harness validation install and inspect PALS artifacts
Then they verify source and installed hot surfaces stay under documented line ceilings and preserve shared-reference ownership markers
```

## AC-3: Anti-regrowth checks avoid brittle exact-prose overfitting
```gherkin
Given prior validation caught important semantic marker loss but can become fragile when it checks long wording verbatim
When new guardrails are added
Then they prefer structural properties, line-count budgets, archive-link presence, reference ownership, and bounded occurrence counts over long exact prose matches
```

## AC-4: Existing validation remains green
```gherkin
Given the current baseline passes Pi 177/177 and cross-harness 90/90
When Phase 191 APPLY is complete
Then both `bash tests/pi-end-to-end-validation.sh` and `bash tests/cross-harness-validation.sh` pass with updated totals and no regression in existing guardrail categories
```
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add source-level context-diet anti-regrowth helpers and checks</name>
  <files>tests/pi-end-to-end-validation.sh, tests/cross-harness-validation.sh</files>
  <action>
    Add or reuse small TAP helpers for validation properties that are not single marker strings:
    - file line count at-or-below a named ceiling;
    - total line count across a list of files;
    - occurrence count at-or-below a named ceiling for duplicate mechanics such as repeated inline GitHub Flow `jq -r '.git...` extraction;
    - archive-pointer presence for active-window artifacts.

    Apply those helpers to repo-source surfaces:
    - `.paul/ROADMAP.md` must remain an active-window artifact (target ceiling: 120 lines unless APPLY discovers a better documented threshold) and must point to `.paul/archive/roadmap/ROADMAP-HISTORY-v0-v2.43.md` plus `.paul/MILESTONES.md`.
    - `kernel/workflows/plan-phase.md`, `kernel/workflows/apply-phase.md`, and `kernel/workflows/unify-phase.md` must remain under the existing Phase 186 hot-workflow ceiling of 1711 total lines.
    - The post-190 hot workflow/reference source set (`plan-phase.md`, `apply-phase.md`, `unify-phase.md`, `module-dispatch.md`, `git-strategy.md`, and live `.paul/ROADMAP.md`) must remain under a named ceiling derived from the current 1901-line baseline with a small buffer; document the ceiling in the script near the constants.
    - GitHub Flow duplication must remain bounded by counting repeated inline `jq -r` `.git` extraction snippets in `kernel/workflows/*.md`; allow current legitimate residual uses but fail on broad stanza regrowth.

    Avoid adding long exact-prose assertions for whole paragraphs. Short semantic anchors such as section names, file paths, and role markers are allowed when they represent stable contracts.
  </action>
  <verify>Run targeted shell checks introduced by the task, then run `bash tests/cross-harness-validation.sh` enough to confirm the new source-level guardrails emit passing TAP lines.</verify>
  <done>AC-1, AC-2, and AC-3 are satisfied for repo-source validation surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Extend installed Pi and cross-harness guardrails without increasing brittleness</name>
  <files>tests/pi-end-to-end-validation.sh, tests/cross-harness-validation.sh</files>
  <action>
    Extend the installed-artifact sections so guardrails cover both generated Pi artifacts and cross-harness parity:
    - In Pi validation, verify installed hot workflows still keep the existing hot workflow line ceiling, shared GitHub Flow and module-dispatch ownership markers, and the conditional APPLY/reference guardrails already protected by Phase 190.
    - In cross-harness validation, apply equivalent checks to Claude Code installed, Pi installed, and repo-source kernel surfaces via loops rather than copy/pasted blocks where possible.
    - Preserve existing exact marker checks that protect critical semantics (`gh pr checks`, `gh pr merge`, `modules.yaml NOT FOUND`, parent-owned APPLY, Module Execution Reports), but do not add new long prose string dependencies where a structural check is enough.
    - Keep TAP descriptions clear enough that a future failure identifies whether the regression is line-budget growth, archive-link loss, duplicated GitHub Flow mechanics, or semantic marker loss.
  </action>
  <verify>`bash tests/pi-end-to-end-validation.sh` passes and reports the new anti-regrowth checks; `bash tests/cross-harness-validation.sh` passes and reports cross-harness equivalents.</verify>
  <done>AC-2, AC-3, and AC-4 are satisfied across installed Pi, installed Claude Code, and repo-source validation surfaces.</done>
</task>

<task type="auto">
  <name>Task 3: Run final validation and prepare lifecycle artifacts</name>
  <files>.paul/STATE.md, .paul/ROADMAP.md, .paul/phases/191-validation-anti-regrowth-update/191-01-SUMMARY.md</files>
  <action>
    After Tasks 1-2 pass:
    - Run the full validation suite: `bash tests/pi-end-to-end-validation.sh` and `bash tests/cross-harness-validation.sh`.
    - Record final TAP totals in the Phase 191 SUMMARY during UNIFY.
    - Update `.paul/STATE.md` and `.paul/ROADMAP.md` through APPLY/UNIFY lifecycle transitions only; do not use validation updates as an excuse to rewrite unrelated project history.
    - Preserve archive authority and do not modify cold archives unless a validation check exposes a broken link that must be repaired.
  </action>
  <verify>Both full validation commands exit 0; lifecycle artifacts point to the plan/summary and show the loop moving to UNIFY after APPLY.</verify>
  <done>AC-4 is satisfied and the PLAN/APPLY/UNIFY trace is ready for reconciliation.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- Do not modify `drivers/pi/extensions/pals-hooks.ts`; R5 Pi adapter modularization remains deferred.
- Do not move or delete authoritative archives under `.paul/archive/*`.
- Do not weaken existing critical semantic checks for PLAN/APPLY/UNIFY, module evidence, GitHub Flow merge gates, or parent-owned APPLY.
- Do not add telemetry, hidden runtime state, or Pi-owned lifecycle persistence.
- Do not introduce package dependencies or CI configuration.

## SCOPE LIMITS
- This plan updates validation guardrails only; it does not perform another workflow-prose compaction wave.
- Anti-regrowth thresholds should protect current gains with a documented buffer; they should not create churn for harmless one-line edits.
- Prefer helper functions and loops over duplicated validation blocks.
- Treat Claude Code wrappers as existing supported validation surfaces only; do not expand Claude-specific product scope.
</boundaries>

<verification>
Before declaring APPLY complete:
- [ ] `bash tests/pi-end-to-end-validation.sh` passes.
- [ ] `bash tests/cross-harness-validation.sh` passes.
- [ ] New TAP output includes active ROADMAP budget/archive-link guardrails.
- [ ] New TAP output includes source and installed hot workflow/read-budget guardrails.
- [ ] New checks are structural or short-anchor based, not long paragraph exact-prose overfits.
- [ ] Existing critical semantic marker checks still pass.
- [ ] All acceptance criteria met.
</verification>

<success_criteria>
- Both validation suites pass with updated totals.
- Future ROADMAP history regrowth, hot workflow/reference budget growth, or broad duplicated GitHub Flow stanza regrowth would fail validation.
- Validation remains readable and maintainable: helpers are named, thresholds are documented, and failure messages point to the regressed contract.
- PALS lifecycle state remains artifact-first and traceable through PLAN/APPLY/UNIFY.
</success_criteria>

<output>
After completion, create `.paul/phases/191-validation-anti-regrowth-update/191-01-SUMMARY.md` with validation totals, changed guardrails, deviations, and module execution reports.
</output>
