---
phase: 104-skip-hardening-dean-adversarial
plan: 02
type: fix
wave: 1
depends_on: []
files_modified:
  - kernel/workflows/apply-phase.md
  - kernel/workflows/plan-phase.md
autonomous: true
---

<objective>
## Fix
Replace single-loop dispatch with two explicit steps: advisory_module_dispatch (IRIS, RUBY, DOCS, SKIP — never blocks) then enforcement_module_dispatch (TODD, WALT, DEAN — can block). The prose-based "DO NOT stop" fix failed because models follow structure, not qualifiers.
</objective>

<tasks>
<task type="auto">
  <name>Fix: Split post-apply dispatch into advisory + enforcement steps</name>
  <files>kernel/workflows/apply-phase.md, kernel/workflows/plan-phase.md</files>
  <action>
    In apply-phase.md, replace the single `post_apply_hooks` step with two steps:

    1. `<step name="advisory_module_dispatch" priority="after-tasks">`
       - Read modules.yaml
       - Run ONLY advisory modules (those whose hook description does NOT contain "block"):
         IRIS, RUBY, DOCS, SKIP (sorted by priority)
       - Collect and display all annotations
       - This step NEVER blocks

    2. `<step name="enforcement_module_dispatch" priority="after-advisory">`
       - Run ONLY enforcement modules (those whose hook description contains "block"):
         TODD, WALT, DEAN (sorted by priority)
       - If any returns action: block, surface with advisory context already visible
       - Offer fix/override/stop

    Apply same pattern to post-task dispatch (split TODD enforcement from advisory).
    Apply same pattern to pre-plan dispatch in plan-phase.md.
    
    Keep pre-apply dispatch as-is (TODD+WALT baselines need to run before tasks).
  </action>
  <verify>
    grep -c "advisory_module_dispatch\|enforcement_module_dispatch" kernel/workflows/apply-phase.md should return 2+
  </verify>
  <done>Fix verified working</done>
</task>
</tasks>

<verification>
- [ ] Fix applied and verified
- [ ] No regressions introduced
</verification>
