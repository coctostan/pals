---
phase: 251-documentation-validation-surfacing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - drivers/pi/extensions/README.md
  - drivers/pi/skill-map.md
  - docs/PI-NATIVE-GUIDED-WORKFLOW-DETECTION-EXTRACTION-CONTRACT.md
  - docs/PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md
  - docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md
  - tests/pi-end-to-end-validation.sh
  - tests/cross-harness-validation.sh
autonomous: true
---

<objective>
## Goal
Surface the completed S3 `guided-workflow-detection` sibling extraction across user-facing Pi docs, extraction contract docs, and localized validation guardrails without reopening Phase 250 runtime behavior.

## Purpose
Phase 250 moved S3 detection into `drivers/pi/extensions/guided-workflow-detection.ts` and proved the runtime boundary with one Pi validation guardrail. Phase 251 makes that outcome discoverable and durable: users should see the new sibling in Pi docs/skill-map surfaces, maintainers should see the Phase 250 outcome in extraction contracts, and validation should protect the documentation/contract surfacing from drift.

## Output
Updated README/Pi docs/skill-map and extraction contract docs that name the S3 sibling and its S3/S4 boundary; localized Pi and cross-harness validation guardrails that prove the S3 surfacing and shared-invariant documentation are present; validation counts reconciled in the Phase 251 SUMMARY.
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md
.paul/SPECIAL-FLOWS.md

## Prior Work
.paul/phases/250-bounded-s3-submodule-extraction/250-01-SUMMARY.md
docs/PI-NATIVE-GUIDED-WORKFLOW-DETECTION-EXTRACTION-CONTRACT.md
docs/PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md
docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md

## Documentation Files
README.md
drivers/pi/extensions/README.md
drivers/pi/skill-map.md

## Validation Files
tests/pi-end-to-end-validation.sh
tests/cross-harness-validation.sh
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: TODD | IRIS | DAVE | DOCS | CODI | RUBY evaluated for planned files.

- TODD: No package test framework, `*.test.*`, or `*.spec.*` files cover this docs/validation surfacing phase. Existing shell validation suites remain the proof path; plan type stays `execute`.
- IRIS: Planned docs/scripts should be checked for accidental TODO/FIXME/HACK/XXX introduction during APPLY; no blocker at PLAN.
- DAVE: No CI config is in scope and CI config remains out of scope. Use existing validation commands only.
- DOCS: This is an intentional documentation surfacing phase. Related docs exist (`README.md`, `drivers/pi/extensions/README.md`, `drivers/pi/skill-map.md`, extraction contracts); update only the approved S3 extraction surfaces.
- CODI: skipped — no extractable TS/JS source selectors in phase scope. This phase is docs/shell-validation surfacing only; no codegraph impact call is needed.
- RUBY: Advisory only — validation scripts are large; add localized guardrails near the existing Phase 247/250 blocks instead of broad restructuring.

[dispatch] pre-plan enforcement: DEAN PASS — no dependency manifest/lockfile change is planned; no package manager audit applies to this docs/validation surfacing plan.

[dispatch] post-plan: TODD PASS — plan type remains `execute`; no existing test framework or file-level test candidates require TDD restructuring for this docs/validation surfacing plan.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | PLAN/APPLY/UNIFY lifecycle work | ✓ current plan skill loaded |
| TODD | required | APPLY validation/test enforcement | ○ module overlay during APPLY |
| WALT | required | APPLY/UNIFY quality gating | ○ module overlay during APPLY/UNIFY |

**BLOCKING:** Required skills/modules must be honored before APPLY proceeds.
</skills>

<acceptance_criteria>

## AC-1: User-facing Pi docs surface the S3 sibling
```gherkin
Given Phase 250 extracted `drivers/pi/extensions/guided-workflow-detection.ts`
When Phase 251 documentation surfacing is complete
Then README/Pi extension docs/skill-map name the S3 sibling alongside prior siblings and explain that S3 detection is extracted while S4 canonical reply delivery remains in `pals-hooks.ts`.
```

## AC-2: Extraction contract docs record the Phase 250 outcome
```gherkin
Given the Phase 249/250 S3 extraction contract and baseline docs
When Phase 251 documentation surfacing is complete
Then the extraction-target baseline, modularization contract, and guided-workflow-detection extraction contract record the completed Phase 250 outcome, validation evidence, install count, count reconciliation, and preserved S4/S6/S7/S8 deferrals without changing runtime scope.
```

## AC-3: Validation protects surfacing without broad suite refactors
```gherkin
Given existing Phase 247/250 sibling extraction guardrails
When Phase 251 validation updates are complete
Then Pi validation has localized guardrails for user-facing S3 surfacing and cross-harness validation has localized shared-invariant guardrails for contract surfacing, with count deltas explicitly reconciled.
```
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Surface S3 sibling in user-facing Pi docs</name>
  <files>README.md, drivers/pi/extensions/README.md, drivers/pi/skill-map.md</files>
  <action>
    Update user-facing documentation to make the Phase 250 S3 extraction discoverable:
    - In `README.md`, add bounded wording that the Pi extension install surface now includes `guided-workflow-detection.ts` as the S3 detection sibling, while S4 canonical reply delivery remains in `pals-hooks.ts`.
    - In `drivers/pi/extensions/README.md`, update the command/guided workflow and extension source-set sections to name `guided-workflow-detection.ts` alongside `pals-hooks.ts`, `module-activity-parsing.ts`, `artifact-slice-rendering.ts`, and `workflow-resource-capsule-rendering.ts`.
    - In `drivers/pi/skill-map.md`, update the extension path/source-set wording so the installed `drivers/pi/extensions/*.ts` set lists `guided-workflow-detection.ts` and preserves the derived-aid/no-lifecycle-authority boundary.
    - Keep user-facing docs concise; do not add new runtime semantics, commands, shortcuts, settings, helper behavior, or validation classifications.
    Avoid: editing runtime TypeScript, installer/uninstaller/driver.yaml, dependencies, CI config, or `.paul/*` lifecycle artifacts in this task.
  </action>
  <verify>Focused doc checks: `README.md`, `drivers/pi/extensions/README.md`, and `drivers/pi/skill-map.md` contain `guided-workflow-detection.ts`; Pi docs also contain `pals-hooks.ts` and S4/canonical reply boundary language.</verify>
  <done>AC-1 satisfied: user-facing surfaces name the S3 sibling and preserve the S3 detection vs S4 delivery boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile extraction contract docs with Phase 250 outcome</name>
  <files>docs/PI-NATIVE-GUIDED-WORKFLOW-DETECTION-EXTRACTION-CONTRACT.md, docs/PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md, docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md</files>
  <action>
    Update contract/baseline docs to record the completed Phase 250 outcome:
    - In `docs/PI-NATIVE-GUIDED-WORKFLOW-DETECTION-EXTRACTION-CONTRACT.md`, add a Phase 250 outcome section noting `guided-workflow-detection.ts`, retained S4 canonical reply delivery in `pals-hooks.ts`, install evidence of 5 Pi extension files, Pi validation 224/224, cross-harness 131/131, artifact consistency PASS, `git diff --check` clean, and PR #165 merge evidence.
    - In `docs/PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md`, update S3 current state and add/adjust a Phase 250 outcome section so S3 is no longer described as still inline/deferred. Preserve S4/S6/S7/S8 deferrals.
    - In `docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md`, add/adjust Phase 250 outcome notes tying S3 extraction back to the original S1-S8 inventory and preserving the S3/S4 split.
    - Keep these docs as derived aids only; `.paul/*`, validation command output, GitHub Flow command evidence, and module reports remain authoritative.
    Avoid: re-litigating candidate selection, weakening forbidden scope, changing runtime files, or broad doc rewrites unrelated to S3 surfacing.
  </action>
  <verify>Focused contract checks: the three docs contain `Phase 250`, `guided-workflow-detection.ts`, `pals-hooks.ts`, `S4`, and validation evidence markers (`224/224`, `131/131`, or equivalent count-reconciliation wording) where appropriate.</verify>
  <done>AC-2 satisfied: contract docs record the Phase 250 outcome and preserve authority/deferral boundaries.</done>
</task>

<task type="auto">
  <name>Task 3: Add localized validation guardrails and run full evidence</name>
  <files>tests/pi-end-to-end-validation.sh, tests/cross-harness-validation.sh</files>
  <action>
    Add localized validation guardrails for the Phase 251 surfacing work:
    - In `tests/pi-end-to-end-validation.sh`, add a Phase 251 Pi-supported-runtime block near the Phase 247/250 sibling extraction surfacing blocks that asserts user-facing Pi docs name `guided-workflow-detection.ts` and preserve the S3 detection / S4 canonical reply delivery boundary.
    - In `tests/cross-harness-validation.sh`, add a Phase 251 Shared Invariant block near the Phase 247 shared-invariant guardrail that asserts the extraction contract/baseline docs name `guided-workflow-detection.ts`, record Phase 250 outcome/evidence, and preserve S4 deferral / no inferred merge intent boundaries.
    - Reconcile expected validation counts from the Phase 250 baseline: Pi validation may increase from 224 to 225 or more if the guardrail requires multiple TAP assertions; cross-harness may increase from 131 to 132 or more if the shared-invariant guardrail requires multiple TAP assertions. Keep additions minimal and explain count deltas in SUMMARY.
    - Run the required verification set and capture command-output truth for SUMMARY.
    Avoid: broad validation-suite refactors, changing existing unrelated assertions, weakening checks, or adding generated ledgers/caches.
  </action>
  <verify>`bash tests/pi-end-to-end-validation.sh`, `bash tests/cross-harness-validation.sh`, `bash tests/helpers/artifact_consistency.sh`, and `git diff --check` pass; focused marker checks for new docs/validation surfacing pass.</verify>
  <done>AC-3 satisfied: surfacing is command-verifiable and count deltas are reconciled.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Runtime TypeScript behavior: `drivers/pi/extensions/guided-workflow-detection.ts`, `drivers/pi/extensions/pals-hooks.ts`, and other sibling runtime modules are out of scope except for read-only verification.
- `drivers/pi/install.sh`, `drivers/pi/uninstall.sh`, and `drivers/pi/driver.yaml` — Phase 241 generalized source-set already covers sibling `.ts` modules.
- S4 canonical-reply delivery behavior: `sendCanonicalWorkflowResponse`, `presentGuidedWorkflowMoment`, `loadGuidedWorkflowConfig`, `shouldAutoPresent`.
- S6 lifecycle UI, S7 context injection, S8 command routing, CARL, installer/uninstaller behavior, dependency manifests, CI config, kernel workflows, module registry, Claude Code driver, Agent SDK driver.
- Installed runtime copies under `~/.pi/agent/extensions/` except through normal installer verification if APPLY chooses to run install.

## SCOPE LIMITS
- Documentation and validation surfacing only: no runtime behavior changes, no config semantics changes, no telemetry, hidden runtime state, generated lifecycle ledger, cache-as-truth, alternate validation/module/lifecycle/GitHub Flow ledger, auto-approval, auto-continue, skipped checkpoints, UI-only lifecycle decisions, or inferred merge intent.
- Keep validation additions localized near existing Phase 247/250 sibling extraction guardrails.
- Do not broaden Phase 251 into Phase 252 evidence closure or next-roadmap recommendation; Phase 252 owns final v2.58 evidence closure.
</boundaries>

<verification>
Before declaring plan complete:
- [ ] Focused doc checks prove `guided-workflow-detection.ts` appears in README/Pi docs/skill-map and S3/S4 boundary language remains visible.
- [ ] Focused contract checks prove Phase 250 outcome/evidence is recorded in extraction contract docs.
- [ ] `bash tests/pi-end-to-end-validation.sh` passes and any count delta from 224/224 is reconciled.
- [ ] `bash tests/cross-harness-validation.sh` passes and any count delta from 131/131 is reconciled.
- [ ] `bash tests/helpers/artifact_consistency.sh` passes.
- [ ] `git diff --check` is clean.
- [ ] All acceptance criteria met.
</verification>

<success_criteria>
- User-facing Pi docs name `guided-workflow-detection.ts` alongside the prior sibling extension modules.
- Contract docs record Phase 250 extraction outcome and validation evidence without weakening authority boundaries.
- S3 detection / S4 canonical reply delivery separation remains explicit.
- Validation guardrails protect the new surfacing with localized checks and green command output.
- No source/runtime/install/dependency/CI scope creep beyond docs and validation scripts.
</success_criteria>

<output>
After completion, create `.paul/phases/251-documentation-validation-surfacing/251-01-SUMMARY.md` with AC results, files changed, validation output/count reconciliation, module reports, GitHub Flow evidence, and any deviations.
</output>
