---
phase: 248-evidence-closure-next-roadmap-decision-v2-57
plan: 01
type: execute
wave: 1
depends_on:
  - phase: 246-s2-workflow-resource-capsule-rendering-extraction
    provides: S2 `workflow-resource-capsule-rendering.ts` sibling extraction; triple-confirmed sibling-module pattern; loader-compat invariant promoted into extraction AC; PR #161 merged 2026-05-02.
  - phase: 247-documentation-validation-surfacing-v2-57
    provides: S2 extraction surfaced across README/Pi docs/skill-map/two contract docs; Pi 223/223 and cross-harness 130/130; PR #162 merged 2026-05-02.
files_modified:
  - .paul/MILESTONES.md
  - .paul/PROJECT.md
  - .paul/ROADMAP.md
  - tests/cross-harness-validation.sh
autonomous: true
risk_tier: normal
collaboration_level: high
planning_mode: direct-requirements
review_preference: tbd
---

# Phase 248 Plan 01: Evidence Closure + Next Roadmap Decision (v2.57)

<objective>
## Goal
Close v2.57 Pi Extension Submodule Extraction Wave — S2 Workflow/Resource Capsule Rendering by aggregating Phase 246 and Phase 247 evidence, recording the next-roadmap recommendation for v2.58, marking v2.57 complete in live lifecycle artifacts, and adding one localized shared-invariant closure guardrail.

## Purpose
v2.57 intentionally used a compact 3-phase shape: extract S2, surface docs/validation evidence, then close with evidence reconciliation. Phase 248 should make the closure durable without reopening runtime/source behavior, installer behavior, dependency/CI surfaces, helper delegation, validation-suite decomposition, or lifecycle authority. The next-roadmap recommendation is recorded for `/paul:milestone`; it does not approve v2.58 work.

## Output
- `.paul/MILESTONES.md` records v2.57 as complete and updates the current/future milestone pointer to v2.58 selection pending.
- `.paul/PROJECT.md` marks v2.57 complete, summarizes Phase 246/247 outcomes, and records the v2.58 recommendation.
- `.paul/ROADMAP.md` marks Phase 248/v2.57 complete and moves v2.57 into the completed milestone summary.
- `tests/cross-harness-validation.sh` gains exactly one localized closure guardrail for v2.57 shared milestone evidence, moving cross-harness validation from 130 to 131.
- UNIFY creates `.paul/phases/248-evidence-closure-next-roadmap-decision-v2-57/248-01-SUMMARY.md` with validation, scope, module, and GitHub Flow evidence.
</objective>

<context>
## Project Context
- `.paul/STATE.md` — Phase 248 ready to plan; v2.57 2 of 3 phases complete; post-Phase-247 validation: Pi 223/223, cross-harness 130/130, artifact consistency PASS, install 4 files.
- `.paul/PROJECT.md` — current focus says Phase 248 is next/final; active milestone table lists v2.57 as In Progress (2 of 3 phases).
- `.paul/ROADMAP.md` — current milestone table lists Phase 248 as Not started; Phase 246/247 are complete.
- `.paul/MILESTONES.md` — live status points to Phase 248 as current; completed milestones table ends at v2.56.

## Prior Work
- `.paul/phases/246-s2-workflow-resource-capsule-rendering-extraction/246-01-SUMMARY.md` — S2 extraction evidence: `workflow-resource-capsule-rendering.ts` created, `pals-hooks.ts` shrank, loader-compat invariant promoted, Pi 221/221, cross-harness 129/129, install 4 files, PR #161.
- `.paul/phases/247-documentation-validation-surfacing-v2-57/247-01-SUMMARY.md` — S2 docs/validation surfacing evidence: README/Pi docs/skill-map + two contract docs updated, Pi 223/223, cross-harness 130/130, install unchanged at 4 files, PR #162.

## Source Files
- `.paul/MILESTONES.md`
- `.paul/PROJECT.md`
- `.paul/ROADMAP.md`
- `tests/cross-harness-validation.sh`

## CODI Seed Candidates
No TS/JS source selectors are in this closure plan. The phase names `workflow-resource-capsule-rendering` and `guided-workflow-detection` are milestone evidence/recommendation labels here, not planned symbol changes. CODI may skip cleanly.
</context>

<module_dispatch>
## Pre-Plan Module Dispatch

Planning collaboration level: high (project default from `pals.json`). Planning mode: direct-requirements from STATE/ROADMAP.

- `[dispatch] pre-plan advisory: modules.yaml loaded` from installed PALS registry, kernel version `2.0.0`; `pals.json` schema version matches.
- `[dispatch] pre-plan advisory: TODD` found no repo-local `*.test.*`, `*.spec.*`, or `__tests__` tree. Existing TAP shell suites remain the proof path; plan type stays `execute`, not `tdd`.
- `[dispatch] pre-plan advisory: IRIS` scanned planned files for `TODO|FIXME|HACK|XXX`; no findings.
- `[dispatch] pre-plan advisory: DAVE` found no `.github/workflows`, `.gitlab-ci.yml`, Dockerfile, docker-compose, or Jenkinsfile in repo scope; no CI file is modified by this plan. GitHub Flow PR checks remain merge-gate evidence during UNIFY.
- `[dispatch] pre-plan advisory: DOCS` found repo docs under `docs/`; this plan updates lifecycle milestone artifacts only and does not create a new product/contract doc.
- `[dispatch] pre-plan advisory: CODI skipped — no extractable symbols in phase scope`; planned files are Markdown lifecycle artifacts plus one shell validation guardrail, not TS/JS source changes.
- `[dispatch] pre-plan advisory: RUBY` noted `tests/cross-harness-validation.sh` is an existing large validation script (~1313 lines). Size debt is pre-existing/future driver-quality scope; this plan must add one localized block only.
- `[dispatch] pre-plan advisory: ARCH` detected the expected flat docs/drivers/kernel/modules/tests lifecycle repo structure. Planned files stay in expected lifecycle/test layers; no circular dependency risk.
- `[dispatch] pre-plan advisory: ARIA/LUKE/DANA/GABE/OMAR/PETE/REED/VERA` skipped — no UI, design-token, data, API, observability, performance, resilience, or privacy implementation surface in planned files.
- `[dispatch] pre-plan enforcement: DEAN PASS/SKIP` no dependency manifest/lockfile is in planned scope; no dependency change is introduced. Frozen legacy `drivers/agent-sdk/requirements.txt` exists outside scope; `python3 -m pip_audit` is unavailable in this environment.
- `[dispatch] pre-plan enforcement: SETH PASS` no hardcoded-secret patterns were found in planned lifecycle artifacts; no auth/input-validation surface is modified.

## Post-Plan Module Dispatch

- `[dispatch] post-plan: TODD` execute plan is appropriate; no RED/GREEN/REFACTOR restructuring suggested. Shell validation and focused marker checks are the proof path.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | All PLAN/APPLY/UNIFY lifecycle work | ✓ |
| /carl | required | Session-boundary monitoring via Pi extension | active |
| TODD | required | Test/validation enforcement during APPLY | registry-loaded |
| WALT | required | Quality gating and validation during APPLY/UNIFY | registry-loaded |

**BLOCKING:** Required skills/modules must remain active before APPLY proceeds.
</skills>

<acceptance_criteria>
## AC-1: v2.57 closure is durable in MILESTONES.md
```gherkin
Given v2.57 has completed Phase 246 S2 extraction and Phase 247 documentation/validation surfacing
When `.paul/MILESTONES.md` is read after APPLY
Then the completed milestones table contains a v2.57 row naming Phases 246, 247, and 248; the live status pointer says no active milestone and v2.58 selection is pending via `/paul:milestone`; the current/future pointer records v2.57 complete with Pi 218→223 (+5), cross-harness 129→130 (+1), install 3→4 (+1), and the footer trail includes `v2.57 milestone closure`.
```

## AC-2: PROJECT.md and ROADMAP.md reflect v2.57 complete and preserve the next-roadmap recommendation
```gherkin
Given Phase 248 is the final v2.57 phase
When `.paul/PROJECT.md` and `.paul/ROADMAP.md` are read after APPLY
Then PROJECT marks v2.57 complete (3 of 3 phases, closed via the Phase 248 PR placeholder/actual PR), records a decision row recommending v2.58 default focus S3 `guided-workflow-detection` as a conditional secondary candidate contingent on continued S2 stability, and preserves alternatives; ROADMAP marks Phase 248 complete, marks the current milestone complete, and adds/updates a completed milestone summary for v2.57 without deleting prior completed-milestone history.
```

## AC-3: Cross-harness closure guardrail is localized and count-reconciled
```gherkin
Given cross-harness validation currently passes 130/130 after Phase 247
When Phase 248 adds closure evidence protection
Then `tests/cross-harness-validation.sh` gains exactly one localized `tap_file_contains_all` block titled `Phase 248 sibling extraction wave closure (v2.57 Shared Invariant)` that asserts `.paul/MILESTONES.md` contains `v2.57 Pi Extension Submodule Extraction Wave`, `Phase 246`, `Phase 247`, `Phase 248`, `workflow-resource-capsule-rendering.ts`, and `v2.58`; the suite plan total moves from `1..130` to `1..131`; the suite passes 131/131 with zero failures.
```

## AC-4: Verification is green and scope remains bounded
```gherkin
Given APPLY has completed the planned closure edits
When final verification runs
Then `PALS_ROOT="$PWD" bash drivers/pi/install.sh` reports 4 Pi extension files, Pi validation remains 223/223, cross-harness validation passes 131/131, artifact_consistency PASSes, `git diff --check` is clean, and changed non-lifecycle files are limited to `tests/cross-harness-validation.sh`.
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Aggregate v2.57 closure evidence in MILESTONES.md</name>
  <files>.paul/MILESTONES.md</files>
  <action>
    Update `.paul/MILESTONES.md` in four bounded locations only:
    - Add a completed-milestones row immediately after the v2.56 row for `v2.57 Pi Extension Submodule Extraction Wave — S2 Workflow/Resource Capsule Rendering`, completed 2026-05-02, stats `3 phases, 3 plans`, detail naming Phase 246 S2 extraction, Phase 247 surfacing, Phase 248 closure, SUMMARY paths, PRs #161/#162 and the Phase 248 PR placeholder/actual PR.
    - Update the Live Status `Current milestone` row to state no active milestone after v2.57 closure and v2.58 selection pending via `/paul:milestone`, with the recorded default recommendation `S3 guided-workflow-detection` contingent on continued S2 stability.
    - Add a v2.57 closure paragraph in Current / Future Milestone Pointer: Phase 246 S2 extraction, Phase 247 docs/validation surfacing, Phase 248 closure; validation reconciliation Pi 218→223 (+5), cross-harness 129→130 (+1 before this closure guardrail), install 3→4 (+1).
    - Append `updated for v2.57 milestone closure: 2026-05-02` to the footer trail.
    Preserve existing v2.55/v2.56 rows and archive pointers verbatim.
  </action>
  <verify>Focused checks find `v2.57 Pi Extension Submodule Extraction Wave`, `Phase 246`, `Phase 247`, `Phase 248`, `workflow-resource-capsule-rendering.ts`, `v2.57 milestone closure`, and `v2.58` in `.paul/MILESTONES.md`; existing `v2.56 Pi Extension Submodule Extraction Wave` count is preserved.</verify>
  <done>AC-1 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Close v2.57 in PROJECT.md and ROADMAP.md with next-roadmap recommendation</name>
  <files>.paul/PROJECT.md, .paul/ROADMAP.md</files>
  <action>
    Update `.paul/PROJECT.md`:
    - Mark the v2.57 milestone row complete (3 of 3 phases; closed 2026-05-02 via the Phase 248 PR placeholder/actual PR).
    - Update Current focus / Current milestone pointer prose so Phase 248 is no longer described as pending after APPLY.
    - Append one Key Decisions row: `2026-05-02: Close v2.57 S2 workflow/resource capsule rendering wave; recommend v2.58 default focus S3 guided-workflow-detection as a conditional secondary candidate contingent on continued S2 stability; alternatives remain local context-cost evidence, demo project preset, and broader validation-script size-debt work requiring separate planning.`
    - Update the footer to Phase 248 closure.

    Update `.paul/ROADMAP.md`:
    - Change current milestone status from In Progress to Complete and Phase 248 status from Not started/Planning to Complete with 2026-05-02 completion date.
    - Add or update a completed milestone row/summary for v2.57 naming Phase 246, 247, 248, PRs #161/#162 and Phase 248 PR placeholder/actual PR, and validation/install reconciliation.
    - Preserve existing v2.56/v2.55 completed milestone summaries and archive links.
  </action>
  <verify>Focused checks find `Complete (3 of 3 phases)`, `Phase 248`, `v2.58`, `guided-workflow-detection`, `continued S2 stability`, and Phase 248 closure footer text in `.paul/PROJECT.md`; ROADMAP shows v2.57 complete and Phase 248 complete.</verify>
  <done>AC-2 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Add shared closure guardrail and run official verification</name>
  <files>tests/cross-harness-validation.sh</files>
  <action>
    Add exactly one localized closure `tap_file_contains_all` block to `tests/cross-harness-validation.sh`, immediately before the summary echo block or next to the Phase 247 shared-invariant block:
    - Title: `Phase 248 sibling extraction wave closure (v2.57 Shared Invariant)`.
    - Target: `$REPO_ROOT/.paul/MILESTONES.md`.
    - Required phrases: `v2.57 Pi Extension Submodule Extraction Wave`, `Phase 246`, `Phase 247`, `Phase 248`, `workflow-resource-capsule-rendering.ts`, and `v2.58`.
    - Update the TAP plan total from `1..130` to `1..131`.
    Do not modify `tests/pi-end-to-end-validation.sh`, `tests/helpers/**`, existing Phase 246/247 validation blocks, helper functions, or unrelated test text.

    Run official verification:
    - `PALS_ROOT="$PWD" bash drivers/pi/install.sh`
    - `bash tests/pi-end-to-end-validation.sh`
    - `bash tests/cross-harness-validation.sh`
    - `bash tests/helpers/artifact_consistency.sh`
    - `git diff --check`
    - changed-file scope check against this plan's allowed files plus standard `.paul/*` lifecycle artifacts.
  </action>
  <verify>Cross-harness total is 131 with zero failures; Pi validation remains 223 with zero failures; install reports `[ok] Pi extensions installed: 4 files`; artifact_consistency PASS; `git diff --check` clean; changed non-lifecycle file set is exactly `tests/cross-harness-validation.sh`.</verify>
  <done>AC-3 and AC-4 satisfied.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- `drivers/pi/extensions/pals-hooks.ts`, `drivers/pi/extensions/workflow-resource-capsule-rendering.ts`, `drivers/pi/extensions/artifact-slice-rendering.ts`, `drivers/pi/extensions/module-activity-parsing.ts`.
- `drivers/pi/install.sh`, `drivers/pi/uninstall.sh`, `drivers/pi/driver.yaml`.
- `README.md`, `drivers/pi/extensions/README.md`, `drivers/pi/skill-map.md`, and all `docs/*.md` contract docs; Phase 247 already surfaced the S2 outcome.
- `tests/pi-end-to-end-validation.sh` and `tests/helpers/**`.
- `kernel/**`, `modules/**`, `.pi/agents/**`, package/dependency manifests, lockfiles, CI configuration, GitHub Flow automation, helper-agent contracts, APPLY workflow semantics, guided workflow behavior, module dispatch semantics.
- Existing v2.55/v2.56 milestone rows/summaries except for surrounding pointer context required to add v2.57.

## SCOPE LIMITS
- Evidence aggregation + lifecycle artifact closure + one cross-harness shared-invariant guardrail only.
- No runtime/source extraction, S3 implementation, install-surface change, dependency/CI change, validation-helper refactor, validation-suite decomposition, local context-cost measurement, demo project preset, or lifecycle-authority behavior change.
- The v2.58 recommendation is recorded, not approved. Final v2.58 selection happens via `/paul:milestone` after v2.57 closes.
- If APPLY discovers the recommended v2.58 focus is ambiguous or unsupported by the closure evidence, stop for guidance rather than inventing a roadmap.
</boundaries>

<verification>
Before declaring APPLY complete:
- [ ] Focused marker checks pass for `.paul/MILESTONES.md`, `.paul/PROJECT.md`, `.paul/ROADMAP.md`, and `tests/cross-harness-validation.sh`.
- [ ] `PALS_ROOT="$PWD" bash drivers/pi/install.sh` reports `[ok] Pi extensions installed: 4 files`.
- [ ] `bash tests/pi-end-to-end-validation.sh` passes with `# Total:  223` and `# Failed: 0`.
- [ ] `bash tests/cross-harness-validation.sh` passes with `# Total:  131` and `# Failed: 0`.
- [ ] `bash tests/helpers/artifact_consistency.sh` reports `artifact_consistency_check: PASS`.
- [ ] `git diff --check` is clean.
- [ ] Changed-file scope is limited to `.paul/MILESTONES.md`, `.paul/PROJECT.md`, `.paul/ROADMAP.md`, `tests/cross-harness-validation.sh`, and normal `.paul/*` lifecycle artifacts including this PLAN/SUMMARY.
- [ ] Workguard report allows `.paul/**` and `tests/cross-harness-validation.sh` with no out-of-scope drift.
</verification>

<success_criteria>
- AC-1 through AC-4 all PASS.
- v2.57 closure evidence is durable in MILESTONES/PROJECT/ROADMAP.
- Cross-harness validation increases only 130→131 for the Phase 248 closure guardrail; Pi validation remains 223/223.
- Install surface remains 4 files; no runtime/installer/docs/dependency/CI/helper/lifecycle-authority behavior changes occur.
- v2.58 recommendation is recorded as a recommendation only; `/paul:milestone` remains the approval route.
</success_criteria>

<output>
After completion, create `.paul/phases/248-evidence-closure-next-roadmap-decision-v2-57/248-01-SUMMARY.md` recording AC results, focused marker evidence, installer evidence, Pi 223/223, cross-harness 131/131, artifact consistency, validation count reconciliation across v2.57, changed-file scope, module reports, GitHub Flow/PR evidence, v2.58 recommendation and alternatives, and milestone-closure routing.
</output>
