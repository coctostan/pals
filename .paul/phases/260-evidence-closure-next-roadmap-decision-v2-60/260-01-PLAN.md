---
phase: 260-evidence-closure-next-roadmap-decision-v2-60
plan: 01
type: execute
wave: 1
depends_on:
  - phase: 257-s6-extraction-contract-bounded-submodule-plan
    provides: S6 `lifecycle-ui` extraction contract (`docs/PI-NATIVE-LIFECYCLE-UI-EXTRACTION-CONTRACT.md`, 183 LOC) defining 8 functions, 2 single-defined runtime UI-element identifiers (`PALS_STATUS_ID = "pals-lifecycle"`, `PALS_WIDGET_ID = "pals-lifecycle"`), type-only back-imports for `PalsStateSnapshot` (`./pals-hooks`) and `RecentModuleActivity` (`./module-activity-parsing`), value-import only `compactWhitespace`, every existing `syncLifecycleUi(ctx)` call site preserved by call shape, install surface 6 → 7, no-UI-only-lifecycle-decisions invariant; Pi 231/231; cross-harness 136/136; PR #172 squash-merged 2026-05-06 as `800552a1`.
  - phase: 258-bounded-s6-submodule-extraction
    provides: `drivers/pi/extensions/lifecycle-ui.ts` sibling extraction (123 LOC; 8 S6 functions + 2 single-defined S6 constants) with type-only back-imports ×2, TAP-assertion repoint ×2 (5 in-flight reshape + 1 net new), `inline → export` promotion ×4 at 4-symbol scaling, helper-import tightening, all 5 `syncLifecycleUi(ctx)` call sites preserved by call shape, no-UI-only-lifecycle-decisions invariant verified shipped (no `.paul/*` writes, no `pi.sendUserMessage`, no S4 identifier invocations in the new sibling); `pals-hooks.ts` reduced 903 → 832 LOC; install 6 → 7; Pi 231 → 232; cross-harness 136/136 unchanged; PR #173 squash-merged 2026-05-06 as `1531e47a`. Sibling-extraction recipe ratified a 6th time (S5 + S1 + S2 + S3 + S7 + S6).
  - phase: 259-documentation-validation-surfacing
    provides: README/Pi docs/skill-map naming `lifecycle-ui.ts` + Phase 258 split paragraph naming `pals-lifecycle` byte sequence; `## Phase 258 Outcome` appended to modularization contract + extraction-target baseline; pre-existing `## Status After Phase 258` placeholder filled in `docs/PI-NATIVE-LIFECYCLE-UI-EXTRACTION-CONTRACT.md`; 3 bounded `tap_file_contains_all` Pi surfacing assertions; install 7 unchanged; Pi 232 → 235 (+3 reconciled exactly); cross-harness 136/136 unchanged; PR #174 squash-merged 2026-05-07 as `b879b372`. Sibling-surfacing recipe ratified a 5th time (Phase 244 / 247 / 251 / 255 / 259) — durable across the full active S* set.
files_modified:
  - .paul/MILESTONES.md
  - .paul/PROJECT.md
  - .paul/ROADMAP.md
  - tests/cross-harness-validation.sh
autonomous: true
risk_tier: normal
collaboration_level: medium
planning_mode: direct-requirements
review_preference: tbd
---

# Phase 260 Plan 01: Evidence Closure + Next Roadmap Decision (v2.60)

<objective>
## Goal
Close v2.60 Pi Extension Submodule Extraction Wave — S6 Lifecycle UI Extraction by aggregating Phases 257–260 evidence, marking v2.60 complete in lifecycle artifacts, recording a recommended-not-approved next-roadmap candidate for v2.61, and adding exactly one localized cross-harness shared-invariant closure guardrail to `tests/cross-harness-validation.sh` (cross-harness 136 → 137). Mirrors the Phase 245 (v2.56) / Phase 248 (v2.57) / Phase 252 (v2.58) / Phase 256 (v2.59) closure shape one-to-one.

## Purpose
v2.60 extracted and surfaced the S6 `lifecycle-ui` sibling while preserving S4 canonical reply delivery in `pals-hooks.ts`, the exact runtime UI-element identifier byte sequence (`pals-lifecycle` for both `PALS_STATUS_ID` and `PALS_WIDGET_ID`), the loop-badge / action-label / module-activity render shapes, the 5 preserved `syncLifecycleUi(ctx)` call sites, and the **no-UI-only-lifecycle-decisions** invariant (UI is a render of authoritative `.paul/*` and module-activity inputs, never an authority for lifecycle decisions). Phase 260 makes the milestone closure durable without reopening runtime TypeScript, installer behavior, dependency/CI surfaces, validation-suite structure, or lifecycle authority. The next-milestone recommendation is recorded for `/paul:milestone`; it does not approve or start the next milestone.

This phase mirrors the Phase 245 / 248 / 252 / 256 closure shape one-to-one (3 tasks, 4 ACs, +1 cross-harness shared-invariant closure guardrail, no runtime change, autonomous). The fifth-time-ratified sibling-surfacing recipe (Phase 244 / 247 / 251 / 255 / 259) and the sixth-time-ratified sibling-extraction recipe (S5 + S1 + S2 + S3 + S7 + S6) are documented as durable patterns in PROJECT.md and the closure paragraph in MILESTONES.md.

## Output
- `.paul/MILESTONES.md` records v2.60 complete with Phases 257–260, PR #172/#173/#174 plus the Phase 260 PR placeholder/actual PR, validation count reconciliation, and the next-roadmap recommendation.
- `.paul/PROJECT.md` marks v2.60 complete and records a v2.61 recommendation as pending `/paul:milestone` approval.
- `.paul/ROADMAP.md` marks Phase 260/v2.60 complete and moves v2.60 into the completed milestone summary while preserving the next-phase route and prior completed-milestone history.
- `tests/cross-harness-validation.sh` gains exactly one localized closure guardrail for v2.60 shared milestone evidence, moving cross-harness validation from 136 to 137.
- UNIFY creates `.paul/phases/260-evidence-closure-next-roadmap-decision-v2-60/260-01-SUMMARY.md` with validation, scope, module, count, GitHub Flow, and next-roadmap evidence.
</objective>

<context>
## Project Context
- `.paul/STATE.md` — Phase 260 ready to plan; v2.60 progress 3 of 4 phases complete; post-Phase-259 baseline on main: install 7, Pi 235/235, cross-harness 136/136, artifact_consistency PASS, `git diff --check` clean.
- `.paul/PROJECT.md` — Active milestone summary lists v2.60 with 3 of 4 phases complete and Phase 260 next; current milestone pointer says no active milestone after v2.59 closure (stale — the prior closure pointer was preserved verbatim from v2.59 closure; Phase 260 will refresh it for v2.60 closure).
- `.paul/ROADMAP.md` — current milestone table lists Phase 259 ✅ Complete and Phase 260 Not started; the completed-milestones table at line 113 already lists v2.59 as the latest completed milestone.
- `.paul/MILESTONES.md` — Live Status pointer says "**Current milestone:** v2.60 Pi Extension Submodule Extraction Wave — S6 Lifecycle UI Extraction (created 2026-05-06 ... Phase 257 ready to plan)" (created on milestone creation and not refreshed mid-milestone). Phase 260 closure must replace this with v2.60 closed and v2.61 pending via `/paul:milestone`.
- `.paul/SPECIAL-FLOWS.md` — required lifecycle skills: /paul, /carl, TODD, WALT.

## Prior Work
- `.paul/phases/257-s6-extraction-contract-bounded-submodule-plan/257-01-SUMMARY.md` — S6 extraction contract: `docs/PI-NATIVE-LIFECYCLE-UI-EXTRACTION-CONTRACT.md` created (183 LOC), S4/S8 deferrals preserved, no-UI-only-lifecycle-decisions invariant defined, Pi 231/231 unchanged, cross-harness 136/136 unchanged, PR #172.
- `.paul/phases/258-bounded-s6-submodule-extraction/258-01-SUMMARY.md` — runtime extraction: `lifecycle-ui.ts` (123 LOC) created with 8 functions + 2 markers single-located; `pals-hooks.ts` reduced 903 → 832 LOC; type-only back-import ×2, TAP-assertion repoint ×2, `inline → export` promotion ×4 patterns ratified; install 6 → 7, Pi 231 → 232, cross-harness 136/136 unchanged, PR #173.
- `.paul/phases/259-documentation-validation-surfacing/259-01-SUMMARY.md` — surfacing: README/Pi docs/skill-map naming the new sibling, `## Phase 258 Outcome` / `## Status After Phase 258` appended/filled in three contract docs (pure additions; prior sections byte-unchanged), 3 bounded Pi `tap_file_contains_all` surfacing assertions, sibling-surfacing recipe ratified a fifth time (Phase 244 / 247 / 251 / 255 / 259), Pi 232 → 235, cross-harness 136/136 unchanged, install 7 unchanged, PR #174.
- `.paul/phases/256-evidence-closure-next-roadmap-decision-v2-59/256-01-PLAN.md` and `256-01-SUMMARY.md` — most-recent closure precedent (v2.59); Phase 260 mirrors this shape one-to-one with v2.60 numbers and the v2.61 recommendation.
- `.paul/phases/252-evidence-closure-next-roadmap-decision/252-01-SUMMARY.md`, `.paul/phases/248-evidence-closure-next-roadmap-decision-v2-57/248-01-SUMMARY.md`, and `.paul/phases/245-evidence-closure-next-roadmap-decision/245-01-SUMMARY.md` — earlier closure precedents establishing the +1 cross-harness shared-invariant closure guardrail recipe.

## Source Files
- `.paul/MILESTONES.md` — Live Status table (line 9 `Current milestone` row), Completed Milestones table (line 80 v2.59 row, append v2.60 row immediately after), Current / Future Milestone Pointer paragraphs (line 92 v2.59 paragraph, line 94 `**Current milestone:**` line, line 96 `**Next-roadmap candidates after v2.60**` line), footer trail (line 104). Phase 260 must update the v2.60 `Current milestone` Status cell, append a Completed Milestones row for v2.60, append a v2.60 closure paragraph in the pointer section after the v2.59 closure paragraph (before the current `**Current milestone:**` line), update the `**Current milestone:**` line to read no active milestone after v2.60 closure with v2.61 selection pending, append a Next-Roadmap Recommendation block, and append the v2.60 closure footer entry.
- `.paul/PROJECT.md` — Active milestone summary (line 13), Active milestones table (lines 60–64 with v2.59 as the latest closed milestone row, append v2.60 row), Current milestone pointer (line 66), Key Decisions table (current latest entry at line 153 dated 2026-05-07 for Phase 259 closure), footer (line 187). Phase 260 marks v2.60 complete (4 of 4 phases) in the Active milestone summary, appends one v2.60 row to the Active milestones table, updates the Current milestone pointer to no active milestone (v2.61 selection pending), appends one Key Decisions row with the v2.60 closure recommendation, and refreshes the footer.
- `.paul/ROADMAP.md` — current milestone table (line 40 row 260 Not started), Phase 260 detail (line 54), latest-completed-milestone summary (line 58 v2.59 narrative), completed-milestones table (line 113 v2.59 row), footer (line 117). Phase 260 marks itself complete, marks the entire v2.60 milestone complete, demotes v2.59 from "Latest completed milestone" narrative to a row inside the completed-milestones table at line 113 (preserving byte-for-byte the existing v2.59 entry that is already there), promotes v2.60 to the new "Latest completed milestone" summary, and refreshes the footer.
- `tests/cross-harness-validation.sh` — current baseline 136/136 (unchanged across v2.60). Existing closure-guardrail blocks at lines 1316 (v2.56 / Phase 245), 1332 (v2.57 / Phase 248), 1348 (v2.58 / Phase 252), 1368 (v2.59 / Phase 256). Phase 260 will add one new `Phase 260 sibling extraction wave closure (v2.60 Shared Invariant)` block immediately after the Phase 256 v2.59 block (around line 1387) and before the SUMMARY block (line 1388), modeled byte-for-byte on the Phase 256 closure block at lines 1368–1386. Expected count delta: 136 → 137.

## CODI Seed Candidates
No TS/JS source selectors are planned. The terms `lifecycle-ui`, `pals-context-injection`, `guided-workflow-detection`, `pals-hooks.ts`, `pals-lifecycle`, and S1–S8 are milestone evidence/recommendation labels in lifecycle artifacts, not planned code changes. CODI may skip cleanly per the planning workflow guardrail (markdown/shell-only phases may record no seeds).
</context>

<module_dispatch>
## Pre-Plan Module Dispatch

Planning collaboration level: medium (per-run override of project-default `high`; matches v2.60 STATE/ROADMAP-recorded mode and the precedent set by Phases 257/258/259 within v2.60). Planning mode: direct-requirements from STATE/ROADMAP.

[dispatch] config-version: `pals.json` schema_version 2.0.0 matches installed `modules.yaml` kernel_version 2.0.0 — no migration needed.

### Advisory Modules
- [dispatch] TODD: pre-plan advisory → no `*.test.*`/`*.spec.*`/`__tests__` tree in scope. The new bounded TAP closure assertion in `tests/cross-harness-validation.sh` carries source-shape coverage for the closure milestone. Plan type stays `execute`, not `tdd`.
- [dispatch] IRIS: pre-plan advisory → no `TODO`/`FIXME`/`HACK`/`XXX` markers will be introduced. Preserved-phrase audit pre-recorded: `Authority: Derived aid only`, `Pi is the supported runtime`, `S4 canonical reply delivery`, `Derived aid only`, `before_agent_start`, `pals-lifecycle`, `lifecycle-ui.ts`, `pals-context-injection.ts`, `guided-workflow-detection.ts`, `pals-hooks.ts`, `no-UI-only-lifecycle-decisions`. Existing closure-guardrail block titles (`Phase 245 sibling extraction wave closure (v2.56 Shared Invariant)`, `Phase 248 sibling extraction wave closure (v2.57 Shared Invariant)`, `Phase 252 sibling extraction wave closure (v2.58 Shared Invariant)`, `Phase 256 sibling extraction wave closure (v2.59 Shared Invariant)`) must remain byte-unchanged.
- [dispatch] DAVE: pre-plan advisory → no CI / Dockerfile / installer / driver.yaml changes. GitHub Flow PR checks remain merge-gate evidence during UNIFY.
- [dispatch] DOCS: pre-plan advisory → updates lifecycle closure artifacts (`.paul/MILESTONES.md`, `.paul/PROJECT.md`, `.paul/ROADMAP.md`) plus one shared validation guardrail. No product/runtime docs touched.
- [dispatch] RUBY: pre-plan advisory → `tests/cross-harness-validation.sh` is an existing large validation script (1409 lines after Phase 256). Size debt is pre-existing/future validation-architecture scope; this plan adds one localized closure block only and does not refactor the suite. Broader cross-harness validation-script size-debt remains queued/deferred per v2.59 closure decision.
- [dispatch] CODI: pre-plan advisory → SKIP-cleanly. Markdown/shell-only phase; no `.ts/.tsx/.js/.jsx` symbols in scope; planning workflow guardrail allows clean skip when seeds are not seedable.
- [dispatch] ARCH: pre-plan advisory → flat docs/drivers/kernel/modules/tests lifecycle repo structure. Planned files stay in expected lifecycle/test layers; no circular dependency risk.
- [dispatch] ARIA / DANA / GABE / LUKE / OMAR / PETE / REED / VERA: pre-plan advisory → SKIP-cleanly (no UI / data / API / observability / performance / resilience / privacy implementation surface in planned files).

### Blast Radius (CODI)
[dispatch] CODI: pre-plan advisory → markdown / shell-only phase; no `.ts/.tsx/.js/.jsx` symbols in scope. Skipping cleanly per planning workflow guardrail.

### Enforcement Modules
- [dispatch] DEAN: pre-plan enforcement → no root package manifest / lockfile detected; no dependency files modified. PASS.
- [dispatch] SETH: pre-plan enforcement → no auth/input-validation surface in planned files; no hardcoded-secret patterns in lifecycle artifacts. PASS.

## Post-Plan Module Dispatch

- [dispatch] post-plan: TODD → execute plan is appropriate; no RED/GREEN/REFACTOR restructuring suggested. Shell validation and focused marker checks are the proof path.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | All planning/applying/unifying lifecycle work | ✓ |
| /carl | required | Automatic session boundary monitoring | automatic |
| TODD | required | Test/validation enforcement during APPLY (advisory for closure phase with bounded TAP guardrails) | ✓ module dispatch recorded |
| WALT | required | APPLY/UNIFY quality gating and validation | pending APPLY |

**BLOCKING:** Required lifecycle/module checks must be honored before APPLY proceeds. Parent APPLY remains authoritative for verification, module enforcement, fallback, GitHub Flow gates, and `.paul/*` lifecycle writes.
</skills>

<acceptance_criteria>
## AC-1: v2.60 closure is durable in MILESTONES.md
```gherkin
Given v2.60 has completed Phase 257 contract, Phase 258 runtime extraction, Phase 259 documentation/validation surfacing, and Phase 260 evidence closure
When `.paul/MILESTONES.md` is read after APPLY
Then the Completed Milestones table contains a v2.60 row naming Phases 257, 258, 259, and 260, the four PRs (`#172`, `#173`, `#174`, and the Phase 260 PR placeholder/actual PR), and the four corresponding SUMMARY paths; the Live Status `Current milestone` row says no active milestone after v2.60 closure and v2.61 selection is pending via `/paul:milestone` with the recorded default recommendation; the Current / Future Milestone Pointer section gains a v2.60 closure paragraph naming `lifecycle-ui.ts`, preserved S4 `pals-hooks.ts` delivery, the no-UI-only-lifecycle-decisions invariant verified shipped, the type-only back-import + TAP-assertion repoint + `inline → export` promotion patterns each ratified ×2 (Phase 254 → Phase 258), the 5th ratification of the sibling-surfacing recipe (Phase 244 / 247 / 251 / 255 / 259), the 6th application of the sibling-extraction recipe (S5 + S1 + S2 + S3 + S7 + S6), validation reconciliation Pi 231 → 235 (+4: +1 from Phase 258 source extraction, +3 from Phase 259 surfacing), cross-harness 136 → 137 (+1 after this closure guardrail), install 6 → 7 (+1), and the v2.61 recommendation; and the footer trail appends `updated for v2.60 milestone closure: 2026-05-07`.
```

## AC-2: PROJECT.md and ROADMAP.md reflect v2.60 complete and preserve next-roadmap recommendation
```gherkin
Given Phase 260 is the final v2.60 phase
When `.paul/PROJECT.md` and `.paul/ROADMAP.md` are read after APPLY
Then PROJECT marks v2.60 complete (4 of 4 phases; closed via the Phase 260 PR placeholder/actual PR), updates the Active milestone summary so Phase 260 is no longer described as pending, appends one v2.60 row to the Active milestones table (after the line-64 v2.59 row), updates the Current milestone pointer prose so it reads no active milestone after v2.60 closure (v2.61 selection pending), appends one Key Decisions row recording the v2.60 closure recommendation (default focus: a moderate-risk next candidate informed by the v2.60 evidence — see "Next-Roadmap Recommendation" below — alternatives preserved), and refreshes the footer to 2026-05-07; ROADMAP marks Phase 260 complete with completion date 2026-05-07, marks the v2.60 milestone complete, demotes v2.59 from "Latest completed milestone" narrative to its existing factual row inside the completed-milestones table at line 113 (which is already byte-identical to the v2.59 closure narrative — preserve byte-unchanged), promotes v2.60 to the new "Latest completed milestone" summary naming Phases 257–260 and PRs #172/#173/#174 + Phase 260 PR placeholder/actual PR, preserves existing v2.55/v2.56/v2.57/v2.58 completed-milestone summaries and archive links byte-unchanged, appends a v2.60 row to the completed-milestones table after the line-113 v2.59 row, and refreshes the footer to 2026-05-07.
```

## AC-3: Cross-harness closure guardrail is localized and count-reconciled
```gherkin
Given cross-harness validation currently passes 136/136 after v2.59 closure / Phase 256 (unchanged across v2.60 Phases 257–259)
When Phase 260 adds closure evidence protection
Then `tests/cross-harness-validation.sh` gains exactly one localized `tap_file_contains_all` block titled `Phase 260 sibling extraction wave closure (v2.60 Shared Invariant)`, modeled byte-for-byte on the existing Phase 256 closure block at lines 1368–1386, that asserts `.paul/MILESTONES.md` contains `v2.60 Pi Extension Submodule Extraction Wave`, `Phase 257`, `Phase 258`, `Phase 259`, `Phase 260`, `lifecycle-ui.ts`, `pals-context-injection.ts` (preserved S7 sibling), `pals-hooks.ts` (preserved S4 host), and `v2.61`; the suite plan total moves from `1..136` to `1..137`; the suite passes 137/137 with zero failures; the existing Phase 245 / 248 / 252 / 256 closure blocks remain byte-unchanged.
```

## AC-4: Verification is green and scope remains bounded
```gherkin
Given APPLY has completed the planned closure edits
When final verification runs
Then `PALS_ROOT="$PWD" bash drivers/pi/install.sh` reports `[ok] Pi extensions installed: 7 files` (unchanged from Phase 259 baseline), `bash tests/pi-end-to-end-validation.sh` passes `1..235` / Passed 235 / Failed 0 unchanged, `bash tests/cross-harness-validation.sh` passes `1..137` / Passed 137 / Failed 0 (with exactly +1 from baseline 136), `bash tests/helpers/artifact_consistency.sh` returns `artifact_consistency_check: PASS`, `git diff --check` returns clean, and changed non-`.paul/` files are limited to `tests/cross-harness-validation.sh`. No `drivers/pi/extensions/*.ts`, `drivers/pi/install.sh`, `drivers/pi/uninstall.sh`, `drivers/pi/driver.yaml`, `tests/pi-end-to-end-validation.sh`, `tests/helpers/**`, `pals.json`, dependency, CI, kernel, module-registry, Claude Code driver, Agent SDK driver, or installed-runtime-copy file is in the diff.
```
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Aggregate v2.60 closure evidence in MILESTONES.md</name>
  <files>.paul/MILESTONES.md</files>
  <action>
    Update `.paul/MILESTONES.md` in bounded closure locations only, mirroring the Phase 256 v2.59 closure shape one-to-one:

    - **Live Status (line 9):** Replace the `Current milestone` row's Status cell so it reads no active milestone after v2.60 closure (closed 2026-05-07 with PR #172 squash-merged 2026-05-06 as `800552a1`, PR #173 squash-merged 2026-05-06 as `1531e47a`, PR #174 squash-merged 2026-05-07 as `b879b372`, and Phase 260 PR placeholder/actual PR), and v2.61 selection is pending via `/paul:milestone` with the recorded default recommendation (see Next-Roadmap Recommendation below). Preserve the Pointer cell content unchanged.

    - **Completed Milestones table (after line 80 v2.59 row):** Append a new row immediately after the v2.59 row for `v2.60 Pi Extension Submodule Extraction Wave — S6 Lifecycle UI Extraction`, completed 2026-05-07, stats `4 phases, 4 plans`, detail naming Phase 257 contract, Phase 258 S6 extraction, Phase 259 docs/validation surfacing, Phase 260 closure, the four SUMMARY paths, and PRs `#172` (Phase 257) `800552a1`, `#173` (Phase 258) `1531e47a`, `#174` (Phase 259) `b879b372`, and the Phase 260 PR placeholder/actual PR.

    - **Current / Future Milestone Pointer (after the v2.59 paragraph at line 92, before the current `**Current milestone:**` line 94):** Insert a new v2.60 closure paragraph: "v2.60 Pi Extension Submodule Extraction Wave — S6 Lifecycle UI Extraction is **complete** (closed 2026-05-07; Phase 260 PR placeholder/actual PR squash-merged). All 4 phases shipped: Phase 257 S6 Extraction Contract + Bounded Submodule Plan (PR #172 `800552a1`), Phase 258 Bounded S6 Submodule Extraction extracting `drivers/pi/extensions/lifecycle-ui.ts` (123 LOC; 8 functions + 2 single-defined runtime UI-element identifiers `PALS_STATUS_ID`/`PALS_WIDGET_ID` both `pals-lifecycle`) while preserving S4 canonical reply delivery in `pals-hooks.ts` and verifying the no-UI-only-lifecycle-decisions invariant shipped (no `.paul/*` writes, no `pi.sendUserMessage`, no S4 identifier invocations in the new sibling) (PR #173 `1531e47a`), Phase 259 Documentation + Validation Surfacing across README/Pi docs/skill-map and three contract docs (PR #174 `b879b372`), and Phase 260 Evidence Closure + Next Roadmap Decision. v2.60 milestone closure: Pi 231 → 235 (+4 across the wave: +1 from Phase 258 S6 source-shape + repointed assertions, +3 from Phase 259 surfacing); cross-harness 136 → 137 (+1 from this Phase 260 closure guardrail); install surface 6 → 7 (+1 sibling). Patterns durable: the sibling-surfacing recipe (Phase 244 / 247 / 251 / 255 / 259 — fifth ratification, durable across the full active S* set), the sibling-extraction recipe (S5 + S1 + S2 + S3 + S7 + S6 — sixth application via Phase 258), the type-only back-import pattern (Phase 254 → Phase 258, ratified ×2: `PalsStateSnapshot` + `RecentModuleActivity`), the TAP-assertion repoint pattern (Phase 254 → Phase 258, ratified ×2: 5 in-flight reshape + 1 net new), and the `inline → export` promotion pattern (Phase 254 → Phase 258, ratified ×2 at 4-symbol scaling). Scope preserved the exact runtime UI-element identifier byte sequence `pals-lifecycle` (single-located in `lifecycle-ui.ts`), the loop-badge / action-label / module-activity render shapes, all 5 `syncLifecycleUi(ctx)` call sites preserved by call shape, the no-UI-only-lifecycle-decisions invariant (now repeated verbatim in `drivers/pi/extensions/README.md` and `docs/PI-NATIVE-LIFECYCLE-UI-EXTRACTION-CONTRACT.md` so future code-graph drift surfaces as a doc-vs-code reconciliation gap), S4 canonical reply delivery, transcript-visible canonical reply separation, no-inferred-merge-intent boundary, `.paul/*` authority, and command-output validation truth."

    - **Current milestone (line 94):** Update the prose so it reflects v2.60 closure (no longer "Phase 257 ready to plan" or "0 of 4 phases complete"); replace with: "**Current milestone:** None active. v2.61 selection is pending via `/paul:milestone`. The v2.60 closure recorded a default recommendation as a candidate (see Next-Roadmap Recommendation below); alternatives remain S4 canonical reply delivery (explicitly deferred — integration point), S8 command routing extraction (highest user-visible compatibility surface; requires careful scoping of the `/paul-*` command registration boundary), full Claude Code / Agent SDK driver removal (still blocked per `docs/PI-NATIVE-LEGACY-RETENTION-INSTALL-SURFACE-CLEANUP-CONTRACT.md`), broader cross-harness validation-script size-debt work, local context-cost evidence, and demo project preset, each requiring separate planning."

    - **Next-Roadmap Recommendation (replace existing line 96 `**Next-roadmap candidates after v2.60 (advisory, not approved):**` block in place):** "**Next-roadmap recommendation (recorded, not approved):** v2.61 default focus = a bounded continuation of the Pi-extension modularization wave. Two viable shapes for the next milestone, ranked by precedent strength: (a) S8 command routing extraction (next sibling in the modularization contract S1–S8 inventory after S1/S2/S3/S5/S6/S7 shipped, isolating `/paul-*` command registration paths from `pals-hooks.ts`; highest user-visible compatibility surface; requires careful scoping of the command registration boundary so existing `/paul:plan`, `/paul:apply`, `/paul:unify`, etc. routing is preserved exactly); (b) broader cross-harness validation-script size-debt work (the cross-harness suite is now 1410 lines after this closure guardrail and growing every milestone closure; suite decomposition would unblock future closure recipes from monolithic-suite drift). S4 canonical reply delivery remains explicitly deferred regardless of next-milestone choice — it's the integration point for every other sibling and not a candidate for isolation. Other tracked alternates: full Claude Code / Agent SDK driver removal (still blocked per `docs/PI-NATIVE-LEGACY-RETENTION-INSTALL-SURFACE-CLEANUP-CONTRACT.md`), local context-cost evidence, and demo project preset — each requires separate planning. Final v2.61 selection happens via `/paul:milestone` after v2.60 closes."

    - **Footer trail (line 104):** Append `updated for v2.60 milestone closure: 2026-05-07` to the existing trail (semicolon-separated, preserving the format of prior trail entries).

    Preserve existing v2.55/v2.56/v2.57/v2.58/v2.59 completed-milestone rows and pointer paragraphs verbatim; preserve archive pointer (line 10); preserve the Phase artifacts pointer (line 11).
  </action>
  <verify>Focused checks find `v2.60 Pi Extension Submodule Extraction Wave`, `Phase 257`, `Phase 258`, `Phase 259`, `Phase 260`, `lifecycle-ui.ts`, `pals-context-injection.ts`, `pals-hooks.ts`, `v2.60 milestone closure`, `v2.61`, `Next-roadmap recommendation`, and `no-UI-only-lifecycle-decisions` in `.paul/MILESTONES.md`; existing `v2.59 Pi Extension Submodule Extraction Wave`, `v2.58 Pi Extension Submodule Extraction Wave`, `v2.57 Pi Extension Submodule Extraction Wave`, `v2.56 Pi Extension Submodule Extraction Wave` rows and pointer paragraphs are byte-unchanged; `git diff --check` clean for this file.</verify>
  <done>AC-1 satisfied: MILESTONES.md durably records v2.60 closure with all four phases, the four PRs, the patterns ratified, the count reconciliation, and the v2.61 recommendation.</done>
</task>

<task type="auto">
  <name>Task 2: Close v2.60 in PROJECT.md and ROADMAP.md with next-roadmap recommendation</name>
  <files>.paul/PROJECT.md, .paul/ROADMAP.md</files>
  <action>
    Update `.paul/PROJECT.md` (mirroring the Phase 256 v2.59 closure shape):

    - **Active milestone summary (line 13):** Update the prose so v2.60 reads as "complete (4 of 4 phases; closed 2026-05-07 via PR #174 `b879b372` and the Phase 260 PR placeholder/actual PR)" instead of "3 of 4 phases complete ... Phase 260 (Evidence Closure + Next-Roadmap Decision — the v2.60 milestone closure phase) is next." Preserve the milestone description, scope, the recipe-ratification notes (5th sibling-surfacing-recipe ratification, 6th sibling-extraction-recipe application, type-only back-import / TAP-assertion repoint / `inline → export` promotion patterns each ratified ×2), the no-UI-only-lifecycle-decisions invariant repetition, and the out-of-scope deferrals verbatim.

    - **Active milestones table (after line 64 v2.59 row):** Append one new row for v2.60: `| v2.60 Pi Extension Submodule Extraction Wave — S6 Lifecycle UI Extraction | Complete (4 of 4 phases) — closed 2026-05-07 via Phase 260 closure (Phase 260 via PR placeholder/actual PR; Phases 257/258/259 via PRs #172 `800552a1` / #173 `1531e47a` / #174 `b879b372`) | Extracted S6 `lifecycle-ui` from `drivers/pi/extensions/pals-hooks.ts` into a sibling Pi extension module (`drivers/pi/extensions/lifecycle-ui.ts`, 123 LOC; eight S6 functions + two single-defined runtime UI-element identifiers `PALS_STATUS_ID`/`PALS_WIDGET_ID` both `pals-lifecycle`) while preserving S4 canonical reply delivery and verifying the no-UI-only-lifecycle-decisions invariant shipped. Patterns durable: sibling-surfacing recipe ratified a 5th time (Phase 244 / 247 / 251 / 255 / 259) — durable across the full active S* set; sibling-extraction recipe ratified a 6th time (S5 + S1 + S2 + S3 + S7 + S6); type-only back-import + TAP-assertion repoint + `inline → export` promotion patterns each ratified ×2 (Phase 254 → Phase 258). Surfaced the outcome across README/Pi docs/skill-map and three contract docs. Final validation: Pi 231→235 (+4: +1 from Phase 258 + +3 from Phase 259), cross-harness 136→137 (+1 after the Phase 260 closure guardrail); install surface 6→7 (+1). |`

    - **Current milestone pointer (line 66):** Update the prose so it no longer reads "None active after v2.59 closure"; replace with: "Current milestone pointer: **None active after v2.60 closure.** v2.61 selection is pending via `/paul:milestone` with a recorded default recommendation in `.paul/MILESTONES.md`. v2.60 closure baseline (post-Phase-260-merge on main): Pi 235/235 unchanged from Phase 259 + new closure guardrail bringing cross-harness to 137/137, artifact_consistency PASS, install 7 Pi extension files. Final 4-phase wave: Phase 257 (PR #172 `800552a1`), Phase 258 (PR #173 `1531e47a`), Phase 259 (PR #174 `b879b372`), Phase 260 (placeholder/actual PR)."

    - **Key Decisions table (after the existing Phase 259 closure decision row at line 155, before the line 157 row dated 2026-05-02):** Append one new row with date `2026-05-07`, decision text recording v2.60 closure: "Close v2.60 Pi Extension Submodule Extraction Wave — S6 Lifecycle UI Extraction. Patterns ratified durable: sibling-surfacing recipe (Phase 244 / 247 / 251 / 255 / 259 — fifth ratification; durable across the full active S* set), sibling-extraction recipe (S5 + S1 + S2 + S3 + S7 + S6 — sixth application via Phase 258), type-only back-import + TAP-assertion repoint + `inline → export` promotion patterns each ratified ×2 (Phase 254 → Phase 258), no-UI-only-lifecycle-decisions invariant verified shipped and repeated verbatim in `drivers/pi/extensions/README.md` and `docs/PI-NATIVE-LIFECYCLE-UI-EXTRACTION-CONTRACT.md` so future code-graph drift surfaces as a doc-vs-code reconciliation gap. Recommend v2.61 default focus = a bounded continuation of the modularization wave (S8 command routing extraction or broader cross-harness validation-script size-debt work; S8 is the higher-precedent next candidate and the last unclaimed sibling from S1–S8). Alternatives preserved: S4 canonical reply delivery (explicitly deferred — integration point), full Claude Code / Agent SDK driver removal (still blocked), local context-cost evidence, and demo project preset. Final v2.61 selection happens via `/paul:milestone` after v2.60 closes." Trigger column: `Phase 260 Plan 260-01 UNIFY`. Outcome column: "v2.60 closed 4 of 4 phases (PRs #172 `800552a1` / #173 `1531e47a` / #174 `b879b372` / Phase 260 PR placeholder-actual). Pi 231 → 235 (+4); cross-harness 136 → 137 (+1 closure guardrail); install 6 → 7 (+1 sibling). Patterns documented in MILESTONES.md and PROJECT.md for v2.61 reference."

    - **Footer (line 187):** Replace with "*Last updated: 2026-05-07 after v2.60 closure via PR #174 (`b879b372`) and the Phase 260 PR placeholder/actual PR. v2.55/v2.56/v2.57/v2.58/v2.59 remain closed; v2.60 closed 4 of 4 phases (PRs #172 `800552a1` / #173 `1531e47a` / #174 `b879b372` / Phase 260 PR); v2.61 selection pending via `/paul:milestone`.*"

    Update `.paul/ROADMAP.md`:

    - **Current milestone table (line 40):** Change Phase 260 row from "TBD | Not started | -" to "1/1 | ✅ Complete | 2026-05-07".

    - **Phase 260 detail (lines 54–56):** Replace `Plans: TBD (defined during /paul:plan)` with `Plans: .paul/phases/260-evidence-closure-next-roadmap-decision-v2-60/260-01-PLAN.md + 260-01-SUMMARY.md — ✅ Complete: aggregated v2.60 closure evidence in MILESTONES.md / PROJECT.md / ROADMAP.md and added one localized cross-harness shared-invariant closure guardrail (136 → 137). AC-1–AC-4 PASS (install 7 unchanged, Pi 235/235 unchanged, cross-harness 1..137/0, artifact_consistency PASS, git diff --check clean). Phase 260 PR placeholder/actual PR squash-merged 2026-05-07; v2.60 closed 4 of 4 phases.`

    - **Latest completed milestone (lines 58–60 v2.59 narrative):** Demote v2.59 from the "Latest completed milestone" narrative header to a row inside the existing completed-milestones table (which already has a v2.59 row at line 113; preserve that row byte-unchanged — Phase 259 surfacing already kept it consistent with the v2.60 STATE narrative). The current "Latest completed milestone: v2.59" narrative at lines 58–60 should be removed in favor of the new v2.60 narrative below; if there is any v2.59 detail in the lines-58–60 narrative not already preserved in the line-113 row, fold it into a one-line entry there before promoting v2.60. Promote v2.60 to the new "Latest completed milestone" summary: `### Latest completed milestone: v2.60 Pi Extension Submodule Extraction Wave — S6 Lifecycle UI Extraction` followed by `Result: Phases 257–260 complete. v2.60 closed after Phase 260 PR placeholder/actual PR squash-merged 2026-05-07. Shipped S6 \`lifecycle-ui\` sibling extraction (\`drivers/pi/extensions/lifecycle-ui.ts\`, 123 LOC; eight S6 functions + two single-defined runtime UI-element identifiers \`PALS_STATUS_ID\`/\`PALS_WIDGET_ID\` both \`pals-lifecycle\`) while preserving S4 canonical reply delivery in \`pals-hooks.ts\` (903 → 832 LOC) and verifying the no-UI-only-lifecycle-decisions invariant shipped, README/Pi docs/skill-map plus three contract docs surfacing, and three bounded Pi surfacing TAP guardrails plus one cross-harness closure guardrail. Patterns ratified: sibling-surfacing recipe (5th ratification: Phase 244 / 247 / 251 / 255 / 259 — durable across the full active S* set), sibling-extraction recipe (6th application: S5 + S1 + S2 + S3 + S7 + S6), type-only back-import + TAP-assertion repoint + \`inline → export\` promotion patterns each ratified ×2 (Phase 254 → Phase 258). Final validation: Pi 235/235, cross-harness 137/137, artifact_consistency PASS, install surface 7 Pi extension files. PRs: #172 (Phase 257) \`800552a1\`, #173 (Phase 258) \`1531e47a\`, #174 (Phase 259) \`b879b372\`, Phase 260 PR placeholder/actual PR.` followed by `Per-phase detail (Phases 257–260 with PR numbers and durations) lives in \`.paul/MILESTONES.md\` and in the completed-milestone table further below.`

    - **Completed milestones table (after line 113 v2.59 row):** Append a new v2.60 row: `| v2.60 Pi Extension Submodule Extraction Wave — S6 Lifecycle UI Extraction | 2026-05-07 | 257-260 | Completed the S6 extraction wave: Phase 257 created the S6 extraction contract + bounded submodule plan (PR #172 `800552a1`), Phase 258 extracted `lifecycle-ui.ts` as the seventh sibling Pi extension module (123 LOC; eight S6 functions + two single-defined runtime UI-element identifiers `PALS_STATUS_ID`/`PALS_WIDGET_ID` both `pals-lifecycle`) while preserving S4 canonical reply delivery in `pals-hooks.ts` (903 → 832 LOC) and verifying the no-UI-only-lifecycle-decisions invariant shipped (PR #173 `1531e47a`), Phase 259 surfaced S6 across README/Pi docs/skill-map and three contract docs with three bounded Pi surfacing guardrails — fifth ratification of the sibling-surfacing recipe and durable across the full active S* set (PR #174 `b879b372`), and Phase 260 reconciled milestone evidence, added the shared-invariant closure guardrail, and recorded the v2.61 recommendation. Validation passes Pi 235/235 and cross-harness 137/137; install surface 7 files. | active artifacts |`

    - **Footer (line 117):** Replace with "*Last updated: 2026-05-07 — v2.60 Pi Extension Submodule Extraction Wave closed via Phase 260 PR placeholder/actual PR (squash-merged); 4 of 4 phases complete (PRs #172 `800552a1` / #173 `1531e47a` / #174 `b879b372` / Phase 260 PR). Cross-harness moved 136 → 137 from the Phase 260 closure guardrail; Pi remains 235/235; install remains 7 files. v2.61 selection pending via `/paul:milestone` with recorded default recommendation in `.paul/MILESTONES.md`. v2.55/v2.56/v2.57/v2.58/v2.59 remain closed.*"

    Avoid: editing v2.55/v2.56/v2.57/v2.58/v2.59 prior closure narratives or completed-milestone rows (must remain factually unchanged), editing prior `## Phase NNN Outcome` / `## Status After Phase NNN` sections in any docs, editing `.paul/STATE.md` (UNIFY owns the live STATE update), editing the prior Phase 245 / 248 / 252 / 256 closure-guardrail blocks in `tests/cross-harness-validation.sh` (Task 3 owns only the new v2.60 closure block).
  </action>
  <verify>Focused checks find `Complete (4 of 4 phases)`, `v2.60 closure`, `Phase 260`, `lifecycle-ui.ts`, `v2.61`, `no-UI-only-lifecycle-decisions`, and the 2026-05-07 footer text in `.paul/PROJECT.md`; ROADMAP shows row 260 ✅ Complete, the v2.60 milestone closure summary names Phases 257–260 and the 4 PRs, the v2.59 completed-milestone-table row at line 113 is byte-unchanged (no double-listing), and the new footer points to v2.60 closure. `git diff --check` clean for both files.</verify>
  <done>AC-2 satisfied: PROJECT.md and ROADMAP.md durably reflect v2.60 closure and preserve the v2.61 recommendation as recorded-not-approved.</done>
</task>

<task type="auto">
  <name>Task 3: Add shared closure guardrail and run official verification</name>
  <files>tests/cross-harness-validation.sh</files>
  <action>
    Add exactly one localized closure `tap_file_contains_all` block to `tests/cross-harness-validation.sh`, immediately after the existing Phase 256 v2.59 closure block (around line 1387) and before the SUMMARY block (line 1388), modeled byte-for-byte on the Phase 256 closure block at lines 1368–1386:

    - Header comment block: `# Phase 260 sibling extraction wave closure (v2.60 Shared Invariant)` with the standard `# ════` separator before and after the descriptive prose, mirroring the Phase 256 block exactly. Descriptive prose: "Phase 260 closes v2.60 by aggregating Phases 257–260 evidence into MILESTONES.md. Closure guardrail: .paul/MILESTONES.md must continue to name the v2.60 milestone, the four phases, the S6 sibling basename, the preserved S7 sibling, the preserved S4 host module, and the v2.61 recommendation marker so the shared invariant across docs/contract/lifecycle does not regress."
    - Title: `Phase 260 sibling extraction wave closure (v2.60 Shared Invariant)`
    - Target: `$REPO_ROOT/.paul/MILESTONES.md`
    - Required phrases (in this order to mirror the Phase 256 block): `v2.60 Pi Extension Submodule Extraction Wave`, `Phase 257`, `Phase 258`, `Phase 259`, `Phase 260`, `lifecycle-ui.ts`, `pals-context-injection.ts`, `pals-hooks.ts`, `v2.61`.

    No TAP-plan-total update is required — the suite uses `echo "1..$TOTAL"` driven by a runtime counter, so adding one assertion automatically moves the plan total from 136 to 137.

    Do not modify `tests/pi-end-to-end-validation.sh`, `tests/helpers/**`, the existing Phase 245 / 248 / 252 / 256 closure blocks, helper functions, or unrelated test text. Do not touch the Phase 258 `EXT_LIFECYCLE_UI` TAP block or the Phase 258 5-in-place TAP repoints to `$EXT_LIFECYCLE_UI` in `tests/pi-end-to-end-validation.sh`. Do not touch the Phase 259 surfacing TAP triple in `tests/pi-end-to-end-validation.sh`.

    Run official verification:
    - `PALS_ROOT="$PWD" bash drivers/pi/install.sh` — verify `[ok] Pi extensions installed: 7 files` (unchanged from Phase 259 baseline).
    - `bash tests/pi-end-to-end-validation.sh` — expect `1..235` / Passed 235 / Failed 0 unchanged.
    - `bash tests/cross-harness-validation.sh` — expect `1..137` / Passed 137 / Failed 0.
    - `bash tests/helpers/artifact_consistency.sh` — expect `artifact_consistency_check: PASS`.
    - `git diff --check` — expect clean.
    - changed-file scope check against this plan's allowed files plus standard `.paul/*` lifecycle artifacts.

    If the assertion run reveals any unrelated count shift (i.e., not exactly `+1` from baseline 136), stop and reconcile in-flight (revert / split with explicit justification) rather than silently expanding.
  </action>
  <verify>`bash tests/cross-harness-validation.sh` returns `1..137` / Passed 137 / Failed 0; `bash tests/pi-end-to-end-validation.sh` returns `1..235` / Passed 235 / Failed 0 unchanged; install reports 7 files; artifact_consistency PASS; `git diff --check` clean. If the count is anything other than `+1` from baseline 136, stop and reconcile from command output before APPLY finalize.</verify>
  <done>AC-3 + AC-4 satisfied: the v2.60 closure surface is command-verifiable with bounded count reconciliation; install + Pi + cross-harness + artifact_consistency + diff-whitespace all green.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Any source file under `drivers/pi/extensions/*.ts`, including the Phase 258 sibling `lifecycle-ui.ts` (this phase is docs/test-only — no source LOC delta).
- `drivers/pi/install.sh`, `drivers/pi/uninstall.sh`, `drivers/pi/driver.yaml`.
- `README.md`, `drivers/pi/extensions/README.md`, `drivers/pi/skill-map.md`, and `docs/*.md` contract docs (Phase 259 already surfaced the S6 outcome; Phase 260 is closure, not surfacing).
- `tests/pi-end-to-end-validation.sh` and `tests/helpers/**` (the Phase 258 `EXT_LIFECYCLE_UI` TAP block, the Phase 258 5-in-place TAP repoints to `$EXT_LIFECYCLE_UI`, and the Phase 259 surfacing TAP triple all remain in their post-Phase-259 form).
- Existing Phase 245 / Phase 248 / Phase 252 / Phase 256 closure-guardrail blocks in `tests/cross-harness-validation.sh` (must remain byte-unchanged).
- Existing v2.55 / v2.56 / v2.57 / v2.58 / v2.59 completed-milestone rows and pointer paragraphs in `.paul/MILESTONES.md` and the v2.59 completed-milestones-table row at `.paul/ROADMAP.md` line 113 (must remain byte-unchanged; only add new v2.60 entries).
- `kernel/**`, `modules/**`, `.pi/agents/**`, dependency manifests, lockfiles, CI configuration, GitHub Flow automation, helper-agent contracts, APPLY workflow semantics, guided workflow runtime behavior, module dispatch semantics, `pals.json`.
- S4 canonical reply delivery (`sendCanonicalWorkflowResponse`, `presentGuidedWorkflowMoment`, `loadGuidedWorkflowConfig`, `shouldAutoPresent`), S8 command routing, CARL session boundary, module-activity-parsing, artifact-slice-rendering, workflow-resource-capsule-rendering, guided-workflow-detection, pals-context-injection, lifecycle-ui — preserved deferrals and shipped siblings.
- The exact runtime UI-element identifier byte sequence `pals-lifecycle` (`PALS_STATUS_ID` and `PALS_WIDGET_ID` both single-defined as `"pals-lifecycle"`) and the loop-badge / action-label / module-activity render-string shapes (must continue to live single-defined in `drivers/pi/extensions/lifecycle-ui.ts`).
- The no-UI-only-lifecycle-decisions invariant — UI is a render of authoritative `.paul/*` and module-activity inputs, never an authority for lifecycle decisions; no `.paul/*` writes, no `pi.sendUserMessage`, and no S4 identifier invocations may be added to `lifecycle-ui.ts`.

## SCOPE LIMITS
- Evidence aggregation + lifecycle artifact closure + one cross-harness shared-invariant guardrail only.
- No runtime/source extraction, S4/S8 implementation, install-surface change, dependency/CI change, validation-helper refactor, validation-suite decomposition, local context-cost measurement, demo project preset, or lifecycle-authority behavior change.
- The v2.61 recommendation is recorded, not approved. Final v2.61 selection happens via `/paul:milestone` after v2.60 closes.
- At most one new bounded cross-harness shared-invariant closure assertion (Task 3). If command output proves additional bounded repair is necessary, reconcile in-flight with explicit justification rather than expanding silently.
- No telemetry, hidden runtime state, generated lifecycle ledger, cache-as-truth, alternate validation/module/lifecycle/GitHub Flow ledger, auto-approval, auto-continue, skipped checkpoints, UI-only lifecycle decisions, or inferred merge intent.
- No payload-string drift in the runtime: `PALS_STATUS_ID` and `PALS_WIDGET_ID` keep their exact `"pals-lifecycle"` byte sequence in `lifecycle-ui.ts`; the loop-badge / action-label / module-activity render shapes remain unchanged (closure phase does not touch them).
- No claude-code/Agent-SDK driver changes (frozen since v2.52; full removal still blocked on cross-harness validation decomposition per `docs/PI-NATIVE-LEGACY-RETENTION-INSTALL-SURFACE-CLEANUP-CONTRACT.md`).
- If APPLY discovers the recommended v2.61 focus is ambiguous or unsupported by closure evidence, stop for guidance rather than inventing a roadmap.
- `pals-implementer` may be used only for a bounded task packet covering Task 1 or Task 2; parent APPLY owns diff review, official verification, module gates, fallback, GitHub Flow gates, and `.paul/*` lifecycle writes. Inline parent APPLY remains the recommended default for a 3-task closure phase (mirroring Phase 256 / 252 / 248 / 245 precedent).

</boundaries>

<verification>
Before declaring APPLY complete:
- [ ] `.paul/MILESTONES.md` contains the new v2.60 Completed Milestones row, the v2.60 closure paragraph in Current / Future Milestone Pointer, the updated `Current milestone:` line stating no active milestone after v2.60 closure, the recorded v2.61 default recommendation, and the appended footer trail entry.
- [ ] `.paul/PROJECT.md` Active milestone summary reads v2.60 complete (4 of 4 phases), Active milestones table contains the new v2.60 row, Current milestone pointer reads no active milestone after v2.60 closure, the new Phase 260 closure Key Decisions row exists, and the footer is dated 2026-05-07.
- [ ] `.paul/ROADMAP.md` row 260 is `1/1 ✅ Complete 2026-05-07`, the v2.60 milestone closure summary names Phases 257–260 and PRs #172 / #173 / #174 / Phase 260 PR placeholder/actual PR, the v2.59 completed-milestone-table row at line 113 is byte-unchanged, the new v2.60 row is appended after it, and the footer is dated 2026-05-07.
- [ ] One new `Phase 260 sibling extraction wave closure (v2.60 Shared Invariant)` `tap_file_contains_all` block exists in `tests/cross-harness-validation.sh` immediately after the Phase 256 block; existing Phase 245 / 248 / 252 / 256 closure blocks are byte-unchanged.
- [ ] `bash tests/cross-harness-validation.sh` returns `1..137` / Passed 137 / Failed 0.
- [ ] `bash tests/pi-end-to-end-validation.sh` returns `1..235` / Passed 235 / Failed 0 unchanged.
- [ ] `bash tests/helpers/artifact_consistency.sh` returns `artifact_consistency_check: PASS`.
- [ ] `PALS_ROOT="$PWD" bash drivers/pi/install.sh` reports `[ok] Pi extensions installed: 7 files`.
- [ ] `git diff --check` returns clean.
- [ ] No `drivers/pi/extensions/*.ts` / `drivers/pi/install.sh` / `drivers/pi/uninstall.sh` / `drivers/pi/driver.yaml` / `tests/pi-end-to-end-validation.sh` / `tests/helpers/**` / `pals.json` / dependency / CI / kernel / module-registry / Claude Code driver / Agent SDK driver / installed-runtime-copy file is in the diff.
- [ ] Workguard report allows `tests/cross-harness-validation.sh` and `.paul/**` with no out-of-scope drift.
</verification>

<success_criteria>
- v2.60 closure evidence is durable in `.paul/MILESTONES.md`, `.paul/PROJECT.md`, and `.paul/ROADMAP.md`, and one localized cross-harness shared-invariant closure guardrail protects it (136 → 137).
- The final v2.60 validation/install count reconciliation is explicit: Pi 231 → 235 (+4 across the wave: +1 from Phase 258 + +3 from Phase 259), cross-harness 136 → 137 (+1 closure guardrail), install 6 → 7 (+1 sibling).
- The v2.61 recommendation is recorded as a recommendation only and preserves alternatives; it does not approve the next milestone.
- The five-times-ratified sibling-surfacing recipe (Phase 244 / 247 / 251 / 255 / 259), the six-times-applied sibling-extraction recipe (S5 + S1 + S2 + S3 + S7 + S6), and the type-only back-import + TAP-assertion repoint + `inline → export` promotion patterns each ratified ×2 (Phase 254 → Phase 258) are documented as durable patterns in MILESTONES.md and the PROJECT.md Key Decisions table.
- The no-UI-only-lifecycle-decisions invariant verification (no `.paul/*` writes, no `pi.sendUserMessage`, no S4 identifier invocations in `lifecycle-ui.ts`) is recorded as durable closure evidence.
- No runtime TypeScript, install/uninstall/manifest, dependency, CI, validation-helper, or lifecycle-authority scope creep occurs.
- All four Phase 260 ACs are satisfied.
- v2.60 closes cleanly, ready for `/paul:milestone` to decide v2.61.
</success_criteria>

<output>
After completion, create `.paul/phases/260-evidence-closure-next-roadmap-decision-v2-60/260-01-SUMMARY.md` with:
- Acceptance criteria results.
- Files modified with line deltas.
- Focused phrase-preservation evidence (preserved boundary phrases still present; closure rows / paragraphs / guardrails added; existing closure blocks byte-unchanged; existing milestone rows byte-unchanged).
- Validation command output and count reconciliation (cross-harness 136 → 137 with +1 closure guardrail; Pi 235/235 unchanged; install 7 unchanged).
- Module execution reports (pre-plan / pre-apply / post-task / post-apply / pre-unify / post-unify).
- GitHub Flow evidence (branch, commit, PR, CI, mergeStateStatus, merge result).
- Workguard scope evidence (allowed paths: `.paul/MILESTONES.md`, `.paul/PROJECT.md`, `.paul/ROADMAP.md`, `tests/cross-harness-validation.sh`, `.paul/**`; final changed-file set).
- Final v2.60 milestone closure narrative (validation count reconciliation across all four phases, durable patterns, preserved deferrals, no-UI-only-lifecycle-decisions invariant verification).
- Recorded v2.61 recommendation with explicit alternatives and the `/paul:milestone` approval boundary.
</output>
