---
phase: 249-s3-extraction-contract-bounded-submodule-plan
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/PI-NATIVE-GUIDED-WORKFLOW-DETECTION-EXTRACTION-CONTRACT.md
  - docs/PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md
  - docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md
autonomous: true
---

<objective>
## Goal
Create the v2.58 Phase 249 S3 `guided-workflow-detection` extraction contract and bounded Phase 250 handoff before any runtime/source extraction work begins.

## Purpose
PALS is continuing the Pi extension sibling-module extraction wave after S1 and S2 shipped. S3 is larger and contract-sensitive because it owns guided workflow moment detection, option parsing, canonical-response detection, merge-gate routing detection, and duplicate-guidance signatures. This plan turns the v2.57 recommendation into an executable contract boundary while preserving `.paul/*` authority, transcript-visible canonical replies, no inferred merge intent, and command-output validation truth.

## Output
- New docs-only contract: `docs/PI-NATIVE-GUIDED-WORKFLOW-DETECTION-EXTRACTION-CONTRACT.md`.
- Updated extraction target baseline: `docs/PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md` records the Phase 249 S3 approval and Phase 250 handoff.
- Updated modularization contract: `docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md` points S3 extraction work to the Phase 249 contract while preserving S4/S6/S7/S8 deferrals.
</objective>

<context>
## Project Context
- `.paul/PROJECT.md` — v2.58 active; Phase 249 ready; S3 scope and preservation boundaries.
- `.paul/ROADMAP.md` — current milestone and Phase 249 focus.
- `.paul/STATE.md` — current loop position: PLAN ready for Phase 249.
- `.paul/SPECIAL-FLOWS.md` — required lifecycle skills: /paul, /carl, TODD, WALT.

## Prior Work
- `.paul/phases/248-evidence-closure-next-roadmap-decision-v2-57/248-01-SUMMARY.md` — v2.57 closure recommended S3 `guided-workflow-detection` only as the v2.58 default focus, not as pre-approved implementation.
- `docs/PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md` — current S1/S2/S5 extraction state, S3 conditional-secondary classification, S3 markers, multi-module extraction rule, forbidden scope, and validation expectations.
- `docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md` — original S1-S8 inventory; S3 cited identifiers; guided-workflow evidence contract dependency.
- `docs/PI-NATIVE-GUIDED-WORKFLOW-EVIDENCE-CONTRACT.md` — canonical reply schema, guided UI safety, no auto-approval, no auto-continue, no skipped checkpoints, no UI-only lifecycle decisions, no inferred merge intent, and validation classes.

## Source Files (read-only context for this plan)
- `drivers/pi/extensions/pals-hooks.ts` — S3 still inline; observed top-level identifiers: `detectGuidedWorkflowMoment`, `parseGuidedWorkflowOptions`, `detectExplicitCanonicalResponse`, `isMergeGateRoutingPrompt`, `extractMergeGateRoutingSummary`, `makeGuidedWorkflowSignature`, `summarizeWorkflowPrompt`, `extractNextActionSummary`, `isValidOptionId`, `GUIDED_WORKFLOW_LOOKBACK`, and `GUIDED_WORKFLOW_SIGNATURE_BYTES`.
- `drivers/pi/extensions/artifact-slice-rendering.ts` — S1 sibling-module precedent: module-level contract header, single-defined markers/constants, helper imports from `./pals-hooks`, no-op default extension factory.
- `drivers/pi/extensions/workflow-resource-capsule-rendering.ts` — S2 sibling-module precedent: module-level contract header, single-defined markers/constants, helper imports from `./pals-hooks`, no-op default extension factory.
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] config-version: `pals.json` schema_version 2.0.0 matches installed `modules.yaml` kernel_version 2.0.0 — no migration needed.
[dispatch] planning posture: direct-requirements | collaboration high (from current milestone / project default).

### Advisory Modules
- [dispatch] TODD: pre-plan advisory → native file discovery found no `*.test.*` or `*.spec.*`; validation shell scripts exist under `tests/`. Docs-only contract plan remains `type: execute`; no TDD conversion required.
- [dispatch] IRIS: pre-plan advisory → existing planned docs contain no `TODO`, `FIXME`, `HACK`, or `XXX` markers; new contract file must avoid unresolved placeholders.
- [dispatch] DAVE: pre-plan advisory → no CI config discovered in `.github/workflows` and no Dockerfile discovered; warn only. Phase verification relies on repo validation scripts plus GitHub Flow evidence in APPLY/UNIFY.
- [dispatch] DOCS: pre-plan advisory → docs catalog exists under `docs/`; all planned modified files are docs. No doc drift concern for this docs-only contract plan.
- [dispatch] RUBY: pre-plan advisory → planned files are docs; existing extraction docs are below 500-line god-file threshold (`PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md` 169 lines, `PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md` 287 lines). New contract should stay compact and bounded.
- [dispatch] ARCH: pre-plan advisory → planned files are documentation under `docs/`, the expected documentation layer for this contract. No source architecture boundary concerns; no circular dependency risk because no runtime imports change in Phase 249.
- [dispatch] ARIA/DANA/GABE/LUKE/OMAR/PETE/REED/SETH/VERA: pre-plan advisory → no UI, data schema, API, UX component, observability runtime, performance-sensitive source, resilience runtime, secret-bearing config, or privacy-data files in planned changes. SETH secret scan over existing planned docs found no hardcoded secrets.

### Blast Radius (CODI)
[dispatch] CODI: pre-plan advisory → impact × 5 symbols → 5 resolved, 0 unresolved, 14 total call-sites, injected blast_radius

**detectGuidedWorkflowMoment**
```
drivers/pi/extensions/pals-hooks.ts:1167:71df  maybePresentGuidedWorkflow  behavioral  depth:1  [fan-in:1, fan-out:5, roles:none, coverage:untested, co-change:0.00, chain-confidence:0.90]
drivers/pi/extensions/pals-hooks.ts:997:cdd1  palsHooks  behavioral  depth:2  [fan-in:0, fan-out:21, roles:entry-point, coverage:untested, co-change:0.00, chain-confidence:0.90]
```

**parseGuidedWorkflowOptions**
```
drivers/pi/extensions/pals-hooks.ts:579:51db  detectGuidedWorkflowMoment  behavioral  depth:1  [fan-in:1, fan-out:9, roles:none, coverage:untested, co-change:0.00, chain-confidence:0.90]
drivers/pi/extensions/pals-hooks.ts:1167:71df  maybePresentGuidedWorkflow  behavioral  depth:2  [fan-in:1, fan-out:5, roles:none, coverage:untested, co-change:0.00, chain-confidence:0.90]
drivers/pi/extensions/pals-hooks.ts:997:cdd1  palsHooks  behavioral  depth:3  [fan-in:0, fan-out:21, roles:entry-point, coverage:untested, co-change:0.00, chain-confidence:0.90]
```

**isMergeGateRoutingPrompt**
```
drivers/pi/extensions/pals-hooks.ts:579:51db  detectGuidedWorkflowMoment  behavioral  depth:1  [fan-in:1, fan-out:9, roles:none, coverage:untested, co-change:0.00, chain-confidence:0.90]
drivers/pi/extensions/pals-hooks.ts:1167:71df  maybePresentGuidedWorkflow  behavioral  depth:2  [fan-in:1, fan-out:5, roles:none, coverage:untested, co-change:0.00, chain-confidence:0.90]
drivers/pi/extensions/pals-hooks.ts:997:cdd1  palsHooks  behavioral  depth:3  [fan-in:0, fan-out:21, roles:entry-point, coverage:untested, co-change:0.00, chain-confidence:0.90]
```

**detectExplicitCanonicalResponse**
```
drivers/pi/extensions/pals-hooks.ts:579:51db  detectGuidedWorkflowMoment  behavioral  depth:1  [fan-in:1, fan-out:9, roles:none, coverage:untested, co-change:0.00, chain-confidence:0.90]
drivers/pi/extensions/pals-hooks.ts:1167:71df  maybePresentGuidedWorkflow  behavioral  depth:2  [fan-in:1, fan-out:5, roles:none, coverage:untested, co-change:0.00, chain-confidence:0.90]
drivers/pi/extensions/pals-hooks.ts:997:cdd1  palsHooks  behavioral  depth:3  [fan-in:0, fan-out:21, roles:entry-point, coverage:untested, co-change:0.00, chain-confidence:0.90]
```

**makeGuidedWorkflowSignature**
```
drivers/pi/extensions/pals-hooks.ts:579:51db  detectGuidedWorkflowMoment  behavioral  depth:1  [fan-in:1, fan-out:9, roles:none, coverage:untested, co-change:0.00, chain-confidence:0.90]
drivers/pi/extensions/pals-hooks.ts:1167:71df  maybePresentGuidedWorkflow  behavioral  depth:2  [fan-in:1, fan-out:5, roles:none, coverage:untested, co-change:0.00, chain-confidence:0.90]
drivers/pi/extensions/pals-hooks.ts:997:cdd1  palsHooks  behavioral  depth:3  [fan-in:0, fan-out:21, roles:entry-point, coverage:untested, co-change:0.00, chain-confidence:0.90]
```

### Enforcement Modules
- [dispatch] DEAN: pre-plan enforcement → no root package manifest/lockfile detected; `drivers/agent-sdk/requirements.txt` exists but is outside planned files and no dependency files are modified. PASS: no dependency audit block for docs-only Phase 249.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | All planning/applying/unifying lifecycle work | ✓ |
| /carl | required | Automatic session boundary monitoring | automatic |
| TODD | required | Before/during code implementation; advisory here because this plan is docs-only | ✓ module dispatch recorded |
| WALT | required | APPLY/UNIFY quality gating and validation | pending APPLY |

**BLOCKING:** Required lifecycle/module checks must be honored before APPLY proceeds. Parent APPLY remains authoritative for verification, module enforcement, fallback, GitHub Flow gates, and `.paul/*` lifecycle writes.
</skills>

<acceptance_criteria>
## AC-1: S3 extraction contract exists and is bounded
```gherkin
Given v2.58 targets S3 guided-workflow detection only after S1/S2 stability
When Phase 249 APPLY completes
Then `docs/PI-NATIVE-GUIDED-WORKFLOW-DETECTION-EXTRACTION-CONTRACT.md` exists and defines the S3 extraction purpose, evidence base, cited identifiers, allowed extraction boundary, forbidden scope, Phase 250 handoff, and acceptance checklist
```

## AC-2: Guided workflow safety invariants are preserved
```gherkin
Given S3 owns guided workflow moment detection, option parsing, merge-gate routing detection, and duplicate-guidance signatures
When the Phase 249 contract and updated extraction docs are reviewed
Then they explicitly preserve option parsing semantics, transcript-visible canonical replies, no auto-approval, no auto-continue, no skipped checkpoints, no UI-only lifecycle decisions, no inferred merge intent, `.paul/*` authority, module evidence, GitHub Flow evidence, and command-output validation truth
```

## AC-3: Phase 250 source-change boundary is executable
```gherkin
Given Phase 250 will perform the bounded runtime extraction
When Phase 249 APPLY finishes
Then the docs name the exact Phase 250 allowed source targets, expected sibling-module shape, marker/function preservation checks, validation commands, installer behavior, and prohibited files/surfaces so APPLY can proceed without guessing
```

## AC-4: Phase 249 remains docs-only and validation-backed
```gherkin
Given Phase 249 is a contract and bounded plan before source changes
When verification runs
Then no runtime/source/test/install/dependency/CI files are modified, `git diff --check` is clean, Pi and cross-harness validation remain green, and any validation-count changes are reconciled from command output
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Create S3 guided workflow detection extraction contract</name>
  <files>docs/PI-NATIVE-GUIDED-WORKFLOW-DETECTION-EXTRACTION-CONTRACT.md</files>
  <action>
    Create a new docs-only contract for Phase 249.
    - State that this artifact is a derived aid only and does not modify runtime behavior.
    - Define the S3 subsystem boundary around `detectGuidedWorkflowMoment`, `parseGuidedWorkflowOptions`, `detectExplicitCanonicalResponse`, `isMergeGateRoutingPrompt`, `extractMergeGateRoutingSummary`, `makeGuidedWorkflowSignature`, `summarizeWorkflowPrompt`, `extractNextActionSummary`, `isValidOptionId`, `GUIDED_WORKFLOW_LOOKBACK`, and `GUIDED_WORKFLOW_SIGNATURE_BYTES`.
    - Cite `docs/PI-NATIVE-GUIDED-WORKFLOW-EVIDENCE-CONTRACT.md` as the governing safety contract for canonical replies, no auto-approval, no auto-continue, checkpoint blocking, and merge-gate routing.
    - Define allowed Phase 250 extraction shape: a sibling module such as `drivers/pi/extensions/guided-workflow-detection.ts`, explicit imports from `./pals-hooks` only where needed, single-defined or explicitly re-exported constants/markers, and the same no-op default-exported Pi extension factory loader-compat invariant used by S1/S2/S5 siblings.
    - Define forbidden scope: S4 canonical reply delivery (`sendCanonicalWorkflowResponse`, `presentGuidedWorkflowMoment`, `loadGuidedWorkflowConfig`, `shouldAutoPresent`), S6 lifecycle UI, S7 context injection, S8 command routing, install/uninstall/driver manifest edits, dependencies, CI, broad validation-suite refactors, telemetry, hidden state, and lifecycle authority changes.
    - Include a Phase 250 handoff with required marker/source checks, install verification, validation commands, summary count reconciliation, and GitHub Flow evidence requirements.
    - Keep prose specific enough that Phase 250 can implement from it, but do not write runtime code in this contract.
  </action>
  <verify>Use native `read`/`grep` or shell `git diff --check`; verify the new contract contains the named S3 identifiers, forbidden-scope list, Phase 250 handoff, and acceptance checklist.</verify>
  <done>AC-1 and AC-2 satisfied: S3 contract exists, cites governing evidence, and protects guided workflow safety invariants without runtime changes.</done>
</task>

<task type="auto">
  <name>Task 2: Update extraction baseline and modularization contract with Phase 249 handoff</name>
  <files>docs/PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md, docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md</files>
  <action>
    Update the existing extraction docs to reflect the Phase 249 planning boundary.
    - In `PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md`, record that v2.58 Phase 249 promotes S3 from conditional recommendation to bounded contract target, while Phase 250 remains the first source-changing step.
    - Preserve existing S1/S2/S5 outcome text and do not rewrite completed-history evidence.
    - Add or update S3 validation expectations: exact marker checks for S3 functions/constants, explicit separation from S4 canonical reply delivery, no inferred merge intent, repo-source-only changes, install source-set proof, Pi validation, cross-harness validation, artifact consistency, and `git diff --check`.
    - In `PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md`, add a concise Phase 249 outcome/handoff note linking to the new S3 contract and preserving S4/S6/S7/S8 deferrals.
    - Avoid approving S4 canonical reply delivery, command routing, lifecycle UI, context injection, installer/manifest edits, dependency/CI changes, or broad validation-suite decomposition.
  </action>
  <verify>Use native `grep` for `Phase 249`, `guided-workflow-detection`, `S4 canonical`, `no inferred merge intent`, and the new contract path in both updated docs; run `git diff --check`.</verify>
  <done>AC-2 and AC-3 satisfied: existing extraction docs route Phase 250 through the bounded S3 contract without broadening scope.</done>
</task>

<task type="auto">
  <name>Task 3: Verify docs-only scope and command-output validation</name>
  <files>docs/PI-NATIVE-GUIDED-WORKFLOW-DETECTION-EXTRACTION-CONTRACT.md, docs/PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md, docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md</files>
  <action>
    Prove the Phase 249 contract change is safe and reviewable.
    - Confirm changed files are docs-only plus normal lifecycle artifacts; no `drivers/pi/extensions/*.ts`, `tests/*.sh`, installer, dependency, CI, module registry, or installed runtime copies are modified.
    - Run focused marker checks showing S3 identifiers and safety boundaries remain visible in docs.
    - Run `bash tests/pi-end-to-end-validation.sh` and preserve TAP count output.
    - Run `bash tests/cross-harness-validation.sh` and preserve TAP count output.
    - Run `bash tests/helpers/artifact_consistency.sh`.
    - Run `git diff --check`.
    - If validation counts differ from the current expected baseline (Pi 223/223, cross-harness 131/131), reconcile the delta in the APPLY notes/SUMMARY from command output before UNIFY.
  </action>
  <verify>`git status --short`, focused marker checks, Pi validation, cross-harness validation, artifact consistency, and `git diff --check` all pass or any deviation is explicitly reconciled.</verify>
  <done>AC-4 satisfied: Phase 249 remains docs-only and validation-backed.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- `drivers/pi/extensions/pals-hooks.ts`
- `drivers/pi/extensions/guided-workflow-detection.ts` (do not create until Phase 250)
- Existing sibling runtime modules: `drivers/pi/extensions/module-activity-parsing.ts`, `drivers/pi/extensions/artifact-slice-rendering.ts`, `drivers/pi/extensions/workflow-resource-capsule-rendering.ts`
- `drivers/pi/install.sh`, `drivers/pi/uninstall.sh`, `drivers/pi/driver.yaml`, installed runtime copies under `~/.pi/agent/extensions/`
- `tests/pi-end-to-end-validation.sh`, `tests/cross-harness-validation.sh`, `tests/helpers/artifact_consistency.sh`
- Dependency manifests, CI configuration, module registry files, kernel workflow sources, Claude Code driver, Agent SDK driver

## SCOPE LIMITS
- Phase 249 is docs-only contract/handoff work. It must not extract S3 source code.
- Do not modify S4 canonical reply delivery, Pi message mutation, `sendCanonicalWorkflowResponse`, `presentGuidedWorkflowMoment`, `loadGuidedWorkflowConfig`, or `shouldAutoPresent`.
- Do not approve S6 lifecycle UI, S7 context injection, or S8 command routing extraction.
- Do not add telemetry, hidden state, runtime ledgers, cache-as-truth, auto-approval, auto-continue, skipped checkpoints, UI-only lifecycle decisions, or inferred merge intent.
- Do not broaden this plan into validation-suite decomposition, installer changes, dependency/CI updates, or product UX changes.
</boundaries>

<verification>
Before declaring APPLY complete:
- [ ] New S3 contract exists and contains the named S3 identifiers, allowed Phase 250 shape, forbidden scope, validation expectations, and acceptance checklist.
- [ ] Existing extraction docs reference Phase 249 / S3 handoff and the new contract path.
- [ ] Focused marker checks show `GUIDED_WORKFLOW_LOOKBACK`, `GUIDED_WORKFLOW_SIGNATURE_BYTES`, `parseGuidedWorkflowOptions`, `isMergeGateRoutingPrompt`, `detectExplicitCanonicalResponse`, `makeGuidedWorkflowSignature`, `no inferred merge intent`, and `S4 canonical` in the expected docs/source context.
- [ ] `bash tests/pi-end-to-end-validation.sh` passes; expected current baseline is Pi 223/223 unless command output proves a reconciled count change.
- [ ] `bash tests/cross-harness-validation.sh` passes; expected current baseline is cross-harness 131/131 unless command output proves a reconciled count change.
- [ ] `bash tests/helpers/artifact_consistency.sh` passes.
- [ ] `git diff --check` passes.
- [ ] Changed-file scope remains docs-only plus lifecycle artifacts; no runtime/source/test/install/dependency/CI files are modified.
</verification>

<success_criteria>
- Phase 249 creates a clear, docs-only S3 extraction contract that is sufficient input for Phase 250.
- Guided workflow safety invariants are explicitly protected: transcript-visible canonical replies, no auto-approval, no auto-continue, no skipped checkpoints, no UI-only lifecycle decisions, no inferred merge intent, `.paul/*` authority, module evidence, GitHub Flow evidence, and command-output validation truth.
- Existing extraction docs are updated without rewriting completed S1/S2/S5 history or approving deferred S4/S6/S7/S8 work.
- All verification commands pass or deviations are reconciled from fresh command output.
</success_criteria>

<output>
After completion, create `.paul/phases/249-s3-extraction-contract-bounded-submodule-plan/249-01-SUMMARY.md` with:
- Acceptance criteria results.
- Files created/modified.
- Focused marker-check evidence.
- Validation command output and count reconciliation.
- Module execution reports.
- GitHub Flow evidence.
- Deferred scope and Phase 250 handoff.
</output>
