---
phase: 94-fixes-and-hardening
plan: 01
type: execute
wave: 1
depends_on: ["93-01"]
files_modified:
  - kernel/workflows/roadmap-management.md
  - drivers/pi/skills/paul-status/SKILL.md
autonomous: true
---

<objective>
## Goal
Add GitHub Flow git-aware display to the status workflow so `/paul:status` surfaces branch, PR, CI state, and git-aware routing — matching the parity already present in resume and pause workflows.

## Purpose
Phase 93 found that `roadmap-management.md` (status) has zero GitHub Flow awareness despite v2.13 claiming "Status Parity." This fix closes the gap so users get consistent git-aware information regardless of whether they use `/paul:status` or `/paul:resume`.

## Output
- Updated `roadmap-management.md` with a `show_status` operation including git-aware display
- Updated `paul-status/SKILL.md` to reference git state collection
- Reinstalled to deployed location
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/STATE.md

## Prior Work
.paul/phases/93-live-validation/93-01-SUMMARY.md

## Source Files
kernel/workflows/roadmap-management.md
drivers/pi/skills/paul-status/SKILL.md
</context>

<acceptance_criteria>

## AC-1: Status Workflow Has Git-Aware Display
```gherkin
Given a github-flow project with pals.json configured
When the status workflow is executed
Then it reads git state (branch, PR, CI, behind count) and displays it in the status dashboard
```

## AC-2: Status Routing Matches Resume
```gherkin
Given the same git state (e.g., PR open + CI failing)
When status and resume are both consulted
Then they suggest the same git-aware next action
```

## AC-3: Deployed Copy Updated
```gherkin
Given the source files are updated
When the install script is run
Then the deployed copies at ~/.pi/agent/skills/pals/ match the source
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add show_status operation with git-aware display to roadmap-management.md</name>
  <files>kernel/workflows/roadmap-management.md</files>
  <action>
    Add a new `## Operation: show-status` section to roadmap-management.md (before the existing add-phase section) with:

    1. A `check_git_state` step identical to resume-project.md's:
       - Read pals.json, resolve GIT_WORKFLOW via 3-tier resolution
       - If github-flow: detect branch, base, ahead/behind, PR state, CI state
       - If not github-flow: skip git state surfacing

    2. A `determine_next_action` step with the same git-aware routing table as resume:
       - PR open + CI failing → "Fix CI failures"
       - BEHIND_COUNT > 0 → "Rebase from base" (takes precedence)
       - PR open + CI passing + reviews pending → "Get PR reviewed"
       - PR open + CI passing + ready → "Merge PR"
       - No PR + loop complete → Normal routing
       - Fall through to loop-position routing

    3. A `display_status` step showing the dashboard:
       - Project name, milestone, phase, progress bars
       - Loop position (PLAN/APPLY/UNIFY markers)
       - Git State block (if github-flow): branch, base, PR, CI, sync
       - Single next action
       - Last activity

    Keep the existing add-phase and remove-phase operations unchanged.
    Update the purpose/description to mention show-status.
  </action>
  <verify>
    grep -c "github-flow\|check_git_state\|show-status" kernel/workflows/roadmap-management.md
    should return 10+ matches
  </verify>
  <done>AC-1 and AC-2 satisfied: Status workflow has git-aware display matching resume routing</done>
</task>

<task type="auto">
  <name>Task 2: Update paul-status SKILL.md to reference git state</name>
  <files>drivers/pi/skills/paul-status/SKILL.md</files>
  <action>
    Update the skill to:
    1. Add `pals.json` to the read list (for git config detection)
    2. Add git-aware display to the "Display status" section:
       - "If pals.json has `git.workflow: github-flow`, also display Git State block"
    3. Add to Key Behavior:
       - "Surface git/PR/CI state for github-flow projects (mirrors resume routing)"
    4. Reference the `show-status` operation in roadmap-management.md
  </action>
  <verify>
    grep -c "github-flow\|pals.json\|git" drivers/pi/skills/paul-status/SKILL.md
    should return 3+ matches
  </verify>
  <done>AC-1 supported: Skill entry point references git-aware display</done>
</task>

<task type="auto">
  <name>Task 3: Reinstall to deployed location and verify</name>
  <files>(deployment — no source changes)</files>
  <action>
    1. Copy updated files to deployed location:
       - kernel/workflows/roadmap-management.md → ~/.pi/agent/skills/pals/workflows/roadmap-management.md
       - drivers/pi/skills/paul-status/SKILL.md → ~/.pi/agent/skills/pals/paul-status/SKILL.md
    2. Verify deployed copies match source:
       - diff kernel/workflows/roadmap-management.md ~/.pi/agent/skills/pals/workflows/roadmap-management.md
       - diff drivers/pi/skills/paul-status/SKILL.md ~/.pi/agent/skills/pals/paul-status/SKILL.md
  </action>
  <verify>
    Both diff commands should return empty (files match)
  </verify>
  <done>AC-3 satisfied: Deployed copies match source</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- resume-project.md (already correct)
- pause-work.md (already correct)
- apply-phase.md (already correct)
- unify-phase.md (already correct)

## SCOPE LIMITS
- Only add show-status to roadmap-management.md — do not restructure existing operations
- Mirror resume's git-aware routing exactly — no new routing logic
- No new config fields in pals.json

</boundaries>

<verification>
Before declaring plan complete:
- [ ] roadmap-management.md has show-status operation with git-aware display
- [ ] paul-status SKILL.md references git state
- [ ] Deployed copies match source (diff returns empty)
- [ ] Existing add-phase and remove-phase operations unchanged
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- Status workflow displays git state for github-flow projects
- Routing matches resume-project.md's git-aware routing table
- Deployed copies updated and verified
</success_criteria>

<output>
After completion, create `.paul/phases/94-fixes-and-hardening/94-01-SUMMARY.md`
</output>
