---
phase: 187-regression-anti-regrowth-guardrails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/pi-end-to-end-validation.sh
  - tests/cross-harness-validation.sh
autonomous: true
---

# Plan: Phase 187 Plan 187-01 — Regression + Anti-Regrowth Guardrails

<objective>
## Goal
Add lightweight regression checks to the existing validation scripts so PALS hot-path artifacts do not silently regrow past the Phase 186 pre-compression baseline or lose the semantic evidence markers that preserve PLAN/APPLY/UNIFY, module dispatch, GitHub Flow, and artifact-authority behavior.

## Purpose
v2.43 reduced repeated hot-path context by archiving cold history, compacting active artifacts, and centralizing shared workflow/module-dispatch prose. This plan protects those gains without adding brittle exact line-count checks, new dependencies, or hidden runtime state.

## Output
- `tests/pi-end-to-end-validation.sh` gains Pi-installed-source guardrails for hot workflow line-count envelope and required semantic markers.
- `tests/cross-harness-validation.sh` gains cross-harness source/installed parity guardrails for the same context-diet contract.
</objective>

<context>
## Project Context
- `.paul/PROJECT.md` — v2.43 context-diet focus and constraints.
- `.paul/ROADMAP.md` — Phase 187 scope: regression + anti-regrowth guardrails.
- `.paul/STATE.md` — current lifecycle truth.
- `.paul/phases/186-workflow-module-prose-compression/186-01-SUMMARY.md` — baseline: hot workflow total moved from 1711 to 1632 lines; `module-dispatch.md` owns shared dispatch mechanics.

## Source Files
- `tests/pi-end-to-end-validation.sh`
- `tests/cross-harness-validation.sh`
- `kernel/workflows/plan-phase.md`
- `kernel/workflows/apply-phase.md`
- `kernel/workflows/unify-phase.md`
- `kernel/references/module-dispatch.md`

## Baseline Facts
- Current line counts observed during PLAN:
  - `kernel/workflows/plan-phase.md`: 349
  - `kernel/workflows/apply-phase.md`: 694
  - `kernel/workflows/unify-phase.md`: 589
  - Hot workflow total: 1632
  - Phase 186 pre-compression hot workflow baseline: 1711
  - `kernel/references/module-dispatch.md`: 87
- Guardrails should use the 1711 hot-workflow total as a practical ceiling, not the exact current 1632 count.
- No TS/JS source selectors are in this plan, so CODI has no extractable symbols.
</context>

<module_dispatch>
## Pre-Plan Module Dispatch

[dispatch] pre-plan advisory: ARCH(p75) → arch_context; SETH(p80) → sec_warnings; TODD(p100) → tdd_candidates; IRIS(p150) → review_flags; ARIA/GABE/LUKE/OMAR/PETE/REED/VERA → scope skips; DAVE(p200) → deploy_warning; DOCS(p200) → doc_warnings; CODI(p220) skipped — no extractable symbols in phase scope; RUBY(p250) → debt_flags

### Advisory Findings
- **ARCH:** Detected pattern: kernel + drivers + modules + tests layout (high confidence). Planned files are in the expected `tests/` validation layer. Existing validation scripts are large (`1162` and `795` lines), so additions should stay localized and avoid broad restructuring.
- **SETH:** No hardcoded secret or auth/input-validation concerns found in planned shell files.
- **TODD:** No repo-local `*.test.*` / `*.spec.*` test files found; existing shell validation suites are the project’s regression harness. TDD assessment: SKIP for RED/GREEN/REFACTOR; use standard `execute` plan because this is validation-script integration work.
- **IRIS:** No `TODO`, `FIXME`, `HACK`, or `XXX` markers found in planned files.
- **DAVE:** No CI workflow config detected. This plan will strengthen local validation scripts only; CI generation remains out of scope unless later requested.
- **DOCS:** Related validation docs exist (`docs/VALIDATION-REPORT.md`, `docs/PI-VALIDATION-REPORT.md`). No docs update is required unless APPLY changes user-facing validation behavior beyond adding checks.
- **CODI:** No `.ts`, `.tsx`, `.js`, or `.jsx` files in the plan context; no symbols extracted.
- **RUBY:** Debt risk is file-size creep in already-large shell validation scripts. Keep new checks concise and helper-driven.

[dispatch] pre-plan enforcement: DEAN(p50) → PASS/SKIP — no dependency manifest or lock file detected, no baseline file present, no dependency audit applicable

## Post-Plan Module Dispatch

[dispatch] post-plan: TODD(p100) → 0 modifications; plan type remains `execute`
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | PLAN/APPLY/UNIFY lifecycle work | ✓ |
| /carl | required | Session-boundary awareness | automatic |
| TODD | required | Validation/test discipline during APPLY | dispatch-managed |
| WALT | required | Quality gating during APPLY/UNIFY | dispatch-managed |

**BLOCKING:** Required lifecycle/module behavior must remain visible in APPLY and UNIFY evidence.
</skills>

<acceptance_criteria>
## AC-1: Hot Workflow Anti-Regrowth Guard
```gherkin
Given Phase 186 established a 1711-line pre-compression baseline for PLAN/APPLY/UNIFY workflows
When the Pi validation suite runs after this plan
Then it fails if the installed hot workflow total grows beyond 1711 lines
And it does not fail on harmless small movement below that ceiling
```

## AC-2: Semantic Marker Regression Guard
```gherkin
Given workflow behavior is defined by semantic obligations rather than exact prose
When Pi and cross-harness validation run
Then they verify required markers for targeted planning reads, module-dispatch evidence, parent-owned APPLY, post-unify persistence, and GitHub Flow merge gates remain present
```

## AC-3: Cross-Harness Guardrail Parity
```gherkin
Given PALS installs through both Claude Code and Pi adapters
When cross-harness validation installs both adapters in temporary homes
Then both installed kernels are checked for the same context-diet guardrail markers
And the repo source surfaces are checked so future installer drift is caught
```

## AC-4: Existing Validation Remains Green
```gherkin
Given no functional product behavior should change in this phase
When the existing validation scripts are run
Then `bash tests/pi-end-to-end-validation.sh` and `bash tests/cross-harness-validation.sh` both pass with zero failures
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Add Pi validation anti-regrowth checks</name>
  <files>tests/pi-end-to-end-validation.sh</files>
  <action>
    Add a focused context-diet regression section to the Pi validation suite.
    - Reuse existing TAP helpers where possible; add only small helper functions if needed.
    - Check installed hot workflows under `$SKILL_DIR/workflows/` after the temp Pi install.
    - Fail if installed `plan-phase.md + apply-phase.md + unify-phase.md` line total is greater than `1711`.
    - Check required semantic markers rather than exact prose, including:
      - `target phase detail`, `planning.default_collaboration`, `Would you like to see the plan?` in `plan-phase.md`.
      - `parent owns verification, module gates, fallback, and state/report writes`, checkpoint/delegation markers in `apply-phase.md`.
      - `post-unify`, `Module Execution Reports`, `MERGE GATE`, `gh pr checks`, `gh pr merge`, and `modules.yaml NOT FOUND` in `unify-phase.md`.
      - `Workflow Call-Site Contract`, `Durable Evidence Requirements`, and `Project Config vs Installed Registry` in `module-dispatch.md`.
    - Keep thresholds practical: do not pin exact current line counts, do not introduce new dependencies, and do not make checks sensitive to harmless wording changes.
  </action>
  <verify>`bash tests/pi-end-to-end-validation.sh` exits 0 and includes the new guardrail TAP checks in its output.</verify>
  <done>AC-1, AC-2, and AC-4 satisfied for the Pi-installed validation path.</done>
</task>

<task type="auto">
  <name>Task 2: Add cross-harness context-diet parity checks</name>
  <files>tests/cross-harness-validation.sh</files>
  <action>
    Add matching cross-harness guardrails after both Claude Code and Pi temp installs are available.
    - Check both installed workflow sets stay under the 1711 hot-workflow ceiling.
    - Check both installed `module-dispatch.md` files retain the shared dispatch/evidence contract markers.
    - Check repo source surfaces retain the same context-diet contract so installer parity and source drift are both covered.
    - Keep the section small and aligned with existing TAP style; avoid duplicating large marker lists if a concise helper can express the checks clearly.
  </action>
  <verify>`bash tests/cross-harness-validation.sh` exits 0 and reports the new cross-harness guardrail checks.</verify>
  <done>AC-2, AC-3, and AC-4 satisfied for cross-harness installs and repo source.</done>
</task>

<task type="auto">
  <name>Task 3: Run full validation and record results for UNIFY</name>
  <files>tests/pi-end-to-end-validation.sh, tests/cross-harness-validation.sh</files>
  <action>
    Run both validation scripts from the repository root after Tasks 1 and 2.
    - Capture final pass/fail counts for the APPLY report.
    - If a new check is brittle or fails due to harmless wording, adjust it toward semantic markers rather than weakening the guardrail entirely.
    - Do not change unrelated docs, workflows, drivers, modules, or `.paul/archive/*` history as part of implementation.
  </action>
  <verify>`bash tests/pi-end-to-end-validation.sh` and `bash tests/cross-harness-validation.sh` both exit 0.</verify>
  <done>AC-4 satisfied and APPLY has concrete validation evidence for UNIFY.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- Do not modify `kernel/workflows/plan-phase.md`, `kernel/workflows/apply-phase.md`, `kernel/workflows/unify-phase.md`, or `kernel/references/module-dispatch.md` in this plan.
- Do not modify module manifests or installed runtime files directly.
- Do not add dependencies, CI generation, package manifests, or new tooling.
- Do not rewrite the validation scripts broadly; add localized guardrails only.
- Do not edit `.paul/archive/*` historical authority files.

## SCOPE LIMITS
- This plan protects the Phase 186 shape; it does not perform another compression wave.
- Guardrails should validate durable semantic obligations, not exact wording or current exact line counts.
- CI workflow generation remains deferred even though DAVE noted no CI config.
</boundaries>

<verification>
Before declaring APPLY complete:
- [ ] `bash tests/pi-end-to-end-validation.sh`
- [ ] `bash tests/cross-harness-validation.sh`
- [ ] Confirm hot workflow ceiling check uses `1711` as the fail threshold, not exact current `1632`.
- [ ] Confirm semantic marker checks cover PLAN targeted-read/review menu, APPLY parent/delegation/checkpoint safety, UNIFY post-unify/merge-gate safety, and shared module-dispatch evidence.
- [ ] All acceptance criteria met.
</verification>

<success_criteria>
- Existing validation remains green with the new checks included.
- Future hot workflow regrowth above the Phase 186 pre-compression baseline fails local validation.
- Future loss of key lifecycle/module/merge-gate markers fails local validation.
- The implementation stays limited to the two validation scripts.
</success_criteria>

<output>
After completion, create `.paul/phases/187-regression-anti-regrowth-guardrails/187-01-SUMMARY.md` with validation counts, guardrail details, module reports, and any deviations.
</output>
