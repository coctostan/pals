---
phase: 286-plan-review-workflow-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kernel/workflows/plan-phase.md
  - kernel/workflows/apply-phase.md
  - tests/pi-end-to-end-validation.sh
  - tests/cross-harness-validation.sh
autonomous: true
---

<objective>
## Goal
Integrate optional Plannotator plan-review handling into the PAUL planning workflow instructions, including per-invocation prompt rules, plan-review feedback sidecars, PLAN regeneration on denial, APPLY advisory-note reading, and marker validation.

## Purpose
Phase 286 wires the Phase 285 Plannotator bridge into PAUL workflow semantics without transferring lifecycle authority to Plannotator. `/paul:plan` should offer a user-controlled rich review surface after a plan is created, preserve feedback in advisory sidecars, and continue to route through PAUL-owned PLAN/APPLY/UNIFY state.

## Output
- Updated `kernel/workflows/plan-phase.md` with an opt-in end-of-PLAN Plannotator review step after plan creation/review and before lifecycle APPLY routing.
- Updated `kernel/workflows/apply-phase.md` with a bounded required-reading note for `PLAN-REVIEW-NOTES.md` when present.
- Updated Pi and cross-harness validation marker checks proving the workflow contract: per-invocation prompt, decline zero-overhead, approval notes, denial feedback, PAUL regeneration, `savedPath` non-adoption, and advisory-only sidecars.
</objective>

<context>
## Project Context
.paul/STATE.md — Phase 286 ready to PLAN after Phase 285 completion.
.paul/ROADMAP.md — Phase 286 focus: edit `/paul:plan` workflow instructions for an end-of-PLAN per-invocation prompt; approval writes optional `PLAN-REVIEW-NOTES.md`; denial writes `PLAN-FEEDBACK-{iteration}.md` and re-enters PLAN through PAUL rules; add one-line APPLY required-reading note.
.paul/PROJECT.md — v2.66 active; Phases 284–285 completed the contract and bridge helper surface; Phase 286 owns plan-review workflow integration.

docs/PALS-PLANNOTATOR-UI-INTEGRATION-CONTRACT.md — authoritative for advisory sidecar contracts, `savedPath` policy, opt-in prompt, failure modes, and feedback incorporation protocol.

## Prior Work
.paul/phases/284-plannotator-integration-contract/284-01-SUMMARY.md — established contract-over-research authority and sidecar paths.
.paul/phases/285-paul-plannotator-bridge/285-01-SUMMARY.md — added `drivers/pi/extensions/plannotator-bridge.ts` with `requestPlanReview`, `requestCodeReview`, and `awaitReviewResult`; workflow integration remains deferred to this phase.

## Source Files
- kernel/workflows/plan-phase.md — target workflow instruction for plan-review prompt, bridge call semantics, sidecar creation, denial/replan behavior, and lifecycle routing.
- kernel/workflows/apply-phase.md — target workflow instruction for reading `PLAN-REVIEW-NOTES.md` as advisory context only.
- tests/pi-end-to-end-validation.sh — Pi-supported runtime/installed workflow marker checks.
- tests/cross-harness-validation.sh — shared workflow marker checks.
- drivers/pi/extensions/plannotator-bridge.ts — bridge helper surface referenced by workflow instructions; not modified by this plan unless marker evidence proves a typo/contract mismatch.
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: TODD — no conventional unit-test framework detected; Phase 286 is workflow-instruction and shell-marker-validation scope. Use existing validation scripts, not TDD, unless APPLY identifies a focused helper-test opportunity.
[dispatch] pre-plan advisory: IRIS — planned files are markdown workflow instructions and shell validation scripts. Review for TODO/FIXME/HACK/XXX markers during APPLY; no preliminary blocker.
[dispatch] pre-plan advisory: DAVE — no CI config changes planned; use local validation scripts and GitHub Flow PR checks as evidence.
[dispatch] pre-plan advisory: DOCS — workflow-instruction scope is process documentation; no separate README/docs update required unless APPLY discovers a discovery-surface drift.
[dispatch] pre-plan advisory: CODI — skipped-no-symbols; no TS/JS source changes are planned and `plannotator-bridge.ts` is referenced but not modified.
[dispatch] pre-plan advisory: RUBY — NOT_APPLICABLE for workflow/reference-only and shell-marker validation scope.
[dispatch] pre-plan advisory: ARCH/ARIA/LUKE/DANA/GABE/OMAR/PETE/REED/SETH/VERA — no app UI/API/data/runtime source changes in scope; preserve optional/advisory absence, no hidden persistence, no unbounded retries, no lifecycle writes.
[dispatch] pre-plan enforcement: DEAN PASS — no dependency manifests/lockfiles or dependency changes in scope.
[dispatch] post-plan: TODD no TDD conversion suggested; execute scope remains workflow-marker validation.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | All planning/applying/unifying lifecycle work | ✓ |
| /carl | required | Session boundary management via Pi runtime | ✓ |
| TODD | required | Before/during implementation; non-TDD workflow-marker scope unless APPLY changes that | ✓ (module dispatch) |
| WALT | required | After implementation for quality/validation gates | ○ (APPLY) |

**BLOCKING:** APPLY must dispatch WALT validation before completion and must record TODD as skipped or applied with evidence.
</skills>

<acceptance_criteria>
## AC-1: PLAN workflow offers opt-in Plannotator plan review without changing PAUL authority
```gherkin
Given `/paul:plan` has created a PLAN.md through PAUL rules
When Plannotator plan review is available and the user reaches the review surface
Then the workflow prompts per invocation, declining emits no event and writes no sidecar, approval proceeds through PAUL-owned routing, and Plannotator never approves lifecycle work by itself
```

## AC-2: PLAN workflow records feedback sidecars and regenerates through PAUL on denial
```gherkin
Given Plannotator returns plan-review feedback
When the result is approved with notes or denied
Then approval-with-notes writes advisory `PLAN-REVIEW-NOTES.md`, denial writes `PLAN-FEEDBACK-{iteration}.md`, `savedPath` is recorded only as advisory evidence, and any revised PLAN is regenerated through `/paul:plan` rather than copied from Plannotator output
```

## AC-3: APPLY workflow and validation preserve advisory-only plan-review evidence
```gherkin
Given `PLAN-REVIEW-NOTES.md` may exist beside an approved plan
When `/paul:apply` loads the approved plan context
Then APPLY reads those notes only as advisory context when present, never treats them as lifecycle truth, and validation scripts prove the new PLAN/APPLY workflow markers across Pi and cross-harness installs
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Add Plannotator plan-review step to PLAN workflow</name>
  <files>kernel/workflows/plan-phase.md</files>
  <action>
    Add a new bounded step after post-plan hooks / compact plan review and before required lifecycle state update. The step should be named clearly, e.g. `optional_plannotator_plan_review`, and must:
    - Require project-level availability plus per-invocation prompt: `Run Plannotator review? [y/n]`.
    - State that decline is zero overhead: no bridge call, no event emitted, no sidecar written, normal PAUL review/apply routing continues.
    - Use the Phase 285 bridge helper surface by name (`requestPlanReview` / `awaitReviewResult`) without importing Plannotator internals or adding new installed-runtime writes.
    - On unavailable/error/timeout/abandoned, record compact advisory evidence and continue through PAUL-owned routing unless the user explicitly made the review blocking.
    - On `approved: true` with feedback, write `.paul/phases/{phase-slug}/PLAN-REVIEW-NOTES.md` with phase/plan, `reviewId`, ISO timestamp, approval state, feedback, optional advisory `savedPath`, and advisory-only statement.
    - On `approved: false`, write `.paul/phases/{phase-slug}/PLAN-FEEDBACK-{iteration}.md` with phase/plan, iteration, `reviewId`, ISO timestamp, feedback, optional advisory `savedPath`, bounded savedPath-read note if applicable, and advisory-input statement; then re-enter PAUL planning with that feedback as required reading instead of proceeding to APPLY.
    - Explicitly state: never copy/adopt `savedPath` as PLAN.md, ignore `agentSwitch` and `permissionMode`, never let Plannotator select agents/skills/permission mode, and `.paul/STATE.md` / PLAN.md remain authoritative.
    - Preserve existing 4-option plan review menu semantics and existing module dispatch evidence.
    Avoid: editing `drivers/pi/extensions/plannotator-bridge.ts`, changing `pals.json`, adding auto-review, adding workflow side effects outside `.paul/phases/{phase-slug}/` sidecars, or changing APPLY/UNIFY state directly from Plannotator results.
  </action>
  <verify>grep `kernel/workflows/plan-phase.md` for `Run Plannotator review? [y/n]`, `requestPlanReview`, `PLAN-REVIEW-NOTES.md`, `PLAN-FEEDBACK-{iteration}.md`, `savedPath`, `agentSwitch`, `permissionMode`, `advisory`, and `re-enter PAUL planning`; run shell syntax checks after validation edits.</verify>
  <done>AC-1 and AC-2 satisfied: the PLAN workflow has explicit opt-in review semantics, sidecar outputs, denial/replan routing, and non-authority guardrails.</done>
</task>

<task type="auto">
  <name>Task 2: Add APPLY advisory notes loading rule</name>
  <files>kernel/workflows/apply-phase.md</files>
  <action>
    Add a bounded required-reading or plan-section loading note that says: if `.paul/phases/{phase-slug}/PLAN-REVIEW-NOTES.md` exists beside the approved plan, APPLY may read it as advisory context only. The note must state:
    - PLAN.md and `.paul/STATE.md` remain authoritative.
    - PLAN-REVIEW-NOTES.md never overrides tasks, acceptance criteria, boundaries, module gates, GitHub Flow, or checkpoints.
    - APPLY must not treat approval-with-notes as extra user approval for changes outside the plan.
    Keep the change compact and local to APPLY context loading; do not add a Plannotator prompt to APPLY in this phase.
  </action>
  <verify>grep `kernel/workflows/apply-phase.md` for `PLAN-REVIEW-NOTES.md`, `advisory context only`, `PLAN.md`, `.paul/STATE.md`, and `never overrides`.</verify>
  <done>AC-3 satisfied for APPLY semantics: APPLY can use plan-review notes as advisory evidence without expanding authority.</done>
</task>

<task type="auto">
  <name>Task 3: Add workflow marker validation for Phase 286</name>
  <files>tests/pi-end-to-end-validation.sh, tests/cross-harness-validation.sh</files>
  <action>
    Add focused marker checks in both validation scripts for the Phase 286 workflow integration:
    - PLAN workflow contains the per-invocation prompt, decline zero-overhead wording, `requestPlanReview`, `awaitReviewResult`, `PLAN-REVIEW-NOTES.md`, `PLAN-FEEDBACK-{iteration}.md`, `savedPath` non-adoption, ignored `agentSwitch`/`permissionMode`, advisory-only sidecars, and PAUL re-planning/regeneration wording.
    - APPLY workflow contains the `PLAN-REVIEW-NOTES.md` advisory context-only rule and never-overrides authority wording.
    - Keep checks marker-based; do not require Plannotator installation or browser/UI execution.
    - Preserve existing validation counts and inherited-failure posture; do not fix unrelated CODI/context-budget failures in this phase.
  </action>
  <verify>bash -n tests/pi-end-to-end-validation.sh && bash -n tests/cross-harness-validation.sh && bash tests/helpers/artifact_consistency.sh && bash tests/cross-harness-validation.sh && bash tests/pi-end-to-end-validation.sh</verify>
  <done>AC-3 satisfied for validation: Pi and cross-harness scripts protect Phase 286 workflow markers without requiring Plannotator runtime execution.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- `drivers/pi/extensions/plannotator-bridge.ts` unless a direct marker typo prevents workflow integration validation.
- `pals.json` Plannotator defaults or project-level enablement values.
- PALS module registry (`modules.yaml`) or module manifests.
- Installed runtime copies under `~/.pi/agent/extensions/*` or `~/.pi/agent/skills/pals/*`.
- Plannotator internals, `--plan` mode, `plannotator_submit_plan`, `plannotator.json`, archive/history/note-export behavior, or `/paul:annotate` wrappers.
- `/paul:apply` or `/paul:unify` code-review prompts; Phase 287 owns code-review workflow integration.

## SCOPE LIMITS
- This phase integrates only the plan-review surface for `/paul:plan` and APPLY advisory-note reading.
- Sidecars are advisory evidence only and live under `.paul/phases/{phase-slug}/`.
- Declining Plannotator review must be zero overhead.
- Denied Plannotator review may inform PAUL re-planning, but the revised PLAN must be generated by PAUL rules and never copied from `savedPath`.
- No new dependencies, no browser automation, no runtime Plannotator invocation required for validation, and no cross-harness driver support.
</boundaries>

<verification>
Before declaring APPLY complete:
- [ ] `git diff --check`
- [ ] `bash -n tests/pi-end-to-end-validation.sh`
- [ ] `bash -n tests/cross-harness-validation.sh`
- [ ] `bash tests/helpers/artifact_consistency.sh`
- [ ] `bash tests/cross-harness-validation.sh`
- [ ] `bash tests/pi-end-to-end-validation.sh` with inherited failures classified separately from new Phase 286 checks
- [ ] Focused marker grep evidence for plan-review prompt, sidecars, `savedPath` non-adoption, ignored fields, advisory APPLY note, and PAUL re-planning
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- `/paul:plan` workflow instructions include opt-in Plannotator plan review with safe decline, sidecars, denial/replan handling, and non-authority guardrails.
- `/paul:apply` workflow instructions read `PLAN-REVIEW-NOTES.md` only as advisory context when present.
- Pi and cross-harness validation scripts enforce the new workflow markers.
- No runtime Plannotator installation, workflow auto-approval, lifecycle authority transfer, dependency changes, module-registry changes, or code-review integration are introduced.
</success_criteria>

<output>
After completion, create `.paul/phases/286-plan-review-workflow-integration/286-01-SUMMARY.md` documenting implementation, validation output, inherited validation failures if any, and handoff notes for Phase 287 code-review workflow integration.
</output>
