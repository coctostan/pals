---
phase: 290-validation-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .paul/archive/roadmap/v2.67-validation-hygiene.md
  - .paul/phases/290-validation-closure/VALIDATION-RESULTS.md
  - .paul/PROJECT.md
  - .paul/ROADMAP.md
  - .paul/MILESTONES.md
autonomous: true
---

<objective>
## Goal
Run final v2.67 validation closure and record durable evidence that the inherited Pi e2e CODI marker drift and hot-artifact budget drift are fully cleared, with Pi e2e and cross-harness validation green.

## Purpose
Phase 288 replaced brittle CODI installed-resource marker checks with semantic helper validation, and Phase 289 brought PROJECT/ROADMAP hot artifacts back under budget. Phase 290 closes v2.67 by proving the full validation baseline is green, reconciling durable lifecycle artifacts, and preserving the evidence needed to complete the milestone without changing product/runtime behavior or weakening validation thresholds.

## Output
- `.paul/phases/290-validation-closure/VALIDATION-RESULTS.md` records final validation commands, counts, and relevant state.
- `.paul/archive/roadmap/v2.67-validation-hygiene.md` records compact milestone closure evidence for v2.67.
- `.paul/PROJECT.md`, `.paul/ROADMAP.md`, and `.paul/MILESTONES.md` are updated only as needed to reflect v2.67 closure posture and Phase 290 evidence.
</objective>

<context>
## Project Context
`.paul/STATE.md` — Phase 290 ready to PLAN after Phase 289 merge gate; Phase 290 must perform final v2.67 validation closure without product/runtime behavior or CODI authority changes.
`.paul/ROADMAP.md` — v2.67 milestone is 2/3 complete; Phase 290 focus is final validation closure and durable history reconciliation.
`.paul/PROJECT.md` — active milestone posture says v2.67 has cleared Phase 288 CODI marker drift and Phase 289 PROJECT/ROADMAP hot-artifact budget drift; Phase 290 owns final validation closure.

## Prior Work
`.paul/phases/288-codi-validation-marker-hygiene/288-01-SUMMARY.md` — Pi e2e CODI installed-resource marker drift cleared via shared semantic helper assertions; cross-harness stayed green.
`.paul/phases/289-hot-artifact-budget-hygiene/289-01-SUMMARY.md` — PROJECT budget drift cleared; PROJECT 24,347 bytes, ROADMAP 11,360 bytes / 79 lines; Pi e2e 335/335 and cross-harness 246/246.

## Source Files
`.paul/PROJECT.md`
`.paul/ROADMAP.md`
`.paul/MILESTONES.md`
`.paul/archive/roadmap/v2.67-validation-hygiene.md`
`.paul/phases/290-validation-closure/VALIDATION-RESULTS.md`
`tests/helpers/artifact_consistency.sh`
`tests/pi-end-to-end-validation.sh`
`tests/cross-harness-validation.sh`
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: ARCH/ARIA/DANA/GABE/LUKE/OMAR/PETE/REED/SETH/VERA skipped — planned changes are lifecycle markdown/evidence artifacts only; no source/API/UI/data/runtime files in scope.
[dispatch] pre-plan advisory: TODD/WALT context — existing shell/TAP validation suites are the acceptance surface for this phase: `tests/helpers/artifact_consistency.sh`, `tests/pi-end-to-end-validation.sh`, and `tests/cross-harness-validation.sh`. No RED/GREEN test-first restructure is needed because Phase 290 verifies already-shipped validation hygiene rather than introducing new behavior.
[dispatch] pre-plan advisory: IRIS — no TODO/FIXME/HACK/XXX markers found in planned hot lifecycle artifacts.
[dispatch] pre-plan advisory: DAVE — no CI config files detected in repo; GitHub Flow merge gate relies on available PR checks plus local validation evidence.
[dispatch] pre-plan advisory: DOCS — documentation/lifecycle-artifact scope; update durable milestone/history pointers only, and keep archive links explicit.
[dispatch] pre-plan advisory: CODI — skipped-no-symbols; markdown/lifecycle/shell-validation closure has no TS/JS source selectors.
[dispatch] pre-plan advisory: RUBY — NOT_APPLICABLE; planned files are lifecycle markdown/evidence artifacts, not readable source files with refactor candidates.
[dispatch] pre-plan enforcement: DEAN PASS — no dependency manifest or lockfile is modified by this plan; dependency audit delta is out of scope and no critical/high dependency delta can be introduced by planned files.
[dispatch] post-plan: TODD — Plan remains `execute`; no TDD restructure suggested because planned changes are lifecycle markdown/evidence artifacts and existing validation suites are verification commands, not new pure logic implementation.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | PLAN/APPLY/UNIFY lifecycle management | ✓ |
| /carl | required | Session boundary monitoring | automatic |
| TODD | required | Before/during implementation validation | ✓ via module dispatch |
| WALT | required | Post-implementation quality validation | pending APPLY |

**BLOCKING:** Required skills must remain active before APPLY proceeds.
</skills>

<acceptance_criteria>
## AC-1: Full validation baseline is green
```gherkin
Given Phase 288 and Phase 289 have cleared the known v2.67 inherited failures
When Phase 290 runs final local validation closure
Then `git diff --check`, artifact consistency, Pi e2e validation, and cross-harness validation all pass without remaining inherited CODI or hot-artifact budget failures
```

## AC-2: Durable v2.67 closure evidence is recorded
```gherkin
Given v2.67 validation hygiene spans Phases 288 through 290
When final validation passes
Then `.paul/phases/290-validation-closure/VALIDATION-RESULTS.md` and `.paul/archive/roadmap/v2.67-validation-hygiene.md` record commands, pass counts, resolved baseline issues, and phase-level evidence links
```

## AC-3: Hot lifecycle artifacts remain compact and authoritative
```gherkin
Given PROJECT, ROADMAP, MILESTONES, and STATE are hot lifecycle artifacts with v2.62 budget expectations
When Phase 290 updates closure posture
Then PROJECT, ROADMAP, and MILESTONES remain compact, aligned, and point to cold archive evidence without duplicating detailed history or changing lifecycle authority
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Capture final validation baseline</name>
  <files>.paul/phases/290-validation-closure/VALIDATION-RESULTS.md</files>
  <action>
    Run the final Phase 290 validation commands and create `VALIDATION-RESULTS.md` with command, exit status, pass/fail counts, and short interpretation for each command.
    Include at minimum: `git diff --check`, `bash tests/helpers/artifact_consistency.sh`, `bash tests/pi-end-to-end-validation.sh`, and `bash tests/cross-harness-validation.sh`.
    Record whether CODI marker drift and PROJECT/ROADMAP hot-artifact budget drift remain cleared.
    Do not weaken validation thresholds, edit validation scripts to hide failures, or change product/runtime source behavior.
  </action>
  <verify>`git diff --check`, `bash tests/helpers/artifact_consistency.sh`, `bash tests/pi-end-to-end-validation.sh`, and `bash tests/cross-harness-validation.sh` all pass, and `VALIDATION-RESULTS.md` records the observed counts.</verify>
  <done>AC-1 and AC-2 satisfied: final validation baseline is green and command evidence is recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Record v2.67 closure archive</name>
  <files>.paul/archive/roadmap/v2.67-validation-hygiene.md, .paul/PROJECT.md, .paul/ROADMAP.md, .paul/MILESTONES.md</files>
  <action>
    Create `.paul/archive/roadmap/v2.67-validation-hygiene.md` with compact milestone closure evidence for Phases 288, 289, and 290.
    Update PROJECT/ROADMAP/MILESTONES only where necessary to point to the v2.67 closure archive and reflect the current closure posture.
    Keep detailed phase evidence in SUMMARY/VALIDATION-RESULTS/archive files and keep hot artifacts concise.
    Preserve current lifecycle authority, GitHub Flow routing, module semantics, and archive links.
  </action>
  <verify>`bash tests/helpers/artifact_consistency.sh` passes and targeted reads show PROJECT/ROADMAP/MILESTONES reference v2.67 closure consistently without large duplicated narrative.</verify>
  <done>AC-2 and AC-3 satisfied: v2.67 closure evidence is durable and hot artifacts remain compact/authoritative.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run closure validation after artifact updates</name>
  <files>.paul/phases/290-validation-closure/VALIDATION-RESULTS.md, .paul/archive/roadmap/v2.67-validation-hygiene.md, .paul/PROJECT.md, .paul/ROADMAP.md, .paul/MILESTONES.md</files>
  <action>
    Re-run the full closure validation suite after lifecycle/archive updates.
    Update `VALIDATION-RESULTS.md` if post-update counts or command evidence differ from Task 1.
    Stop and classify any new failure before making further changes; do not reclassify failures by weakening tests or narrowing assertions.
  </action>
  <verify>`git diff --check`; `bash tests/helpers/artifact_consistency.sh`; `bash tests/pi-end-to-end-validation.sh`; `bash tests/cross-harness-validation.sh` all pass after updates.</verify>
  <done>AC-1, AC-2, and AC-3 satisfied: validation remains green after durable closure writes.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- Product/runtime source behavior.
- Validation thresholds or assertions in `tests/helpers/artifact_consistency.sh`, `tests/pi-end-to-end-validation.sh`, or `tests/cross-harness-validation.sh` to hide failures.
- CODI advisory/non-authority semantics or installed module instruction semantics.
- GitHub Flow merge-gate behavior or `.paul/*` lifecycle authority.

## SCOPE LIMITS
- This phase closes v2.67 validation hygiene only; do not start the next milestone.
- Do not add dependencies, CI configuration, new modules, or new runtime features.
- Do not duplicate detailed historical narratives in hot artifacts; place detailed closure evidence in the v2.67 archive and phase-local validation file.
- If validation exposes a new unrelated failure, stop and classify before broadening scope.
</boundaries>

<verification>
Before declaring APPLY complete:
- [ ] `git diff --check` passes.
- [ ] `bash tests/helpers/artifact_consistency.sh` passes.
- [ ] `bash tests/pi-end-to-end-validation.sh` passes with all checks green.
- [ ] `bash tests/cross-harness-validation.sh` passes with all checks green.
- [ ] `.paul/phases/290-validation-closure/VALIDATION-RESULTS.md` records final command evidence and counts.
- [ ] `.paul/archive/roadmap/v2.67-validation-hygiene.md` records compact v2.67 closure evidence and links to phase summaries/results.
- [ ] PROJECT/ROADMAP/MILESTONES remain compact, aligned, and authoritative.
- [ ] All acceptance criteria met.
</verification>

<success_criteria>
- v2.67 validation hygiene closes with full local validation green.
- CODI marker drift and hot-artifact budget drift remain cleared.
- Durable closure evidence exists outside hot artifact narratives.
- No product/runtime behavior, validation threshold, module semantics, or lifecycle authority changes are introduced.
</success_criteria>

<output>
After completion, create `.paul/phases/290-validation-closure/290-01-SUMMARY.md` with:
- final validation command results and pass counts;
- resolved v2.67 baseline issues from Phases 288 and 289;
- archive and hot-artifact updates;
- explicit note whether v2.67 is ready for milestone completion;
- module execution reports for WALT/SKIP/CODI/RUBY as applicable.
</output>
