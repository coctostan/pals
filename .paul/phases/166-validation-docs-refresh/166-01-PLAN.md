---
phase: 166-validation-docs-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - drivers/pi/extensions/README.md
  - drivers/pi/skill-map.md
  - tests/pi-end-to-end-validation.sh
  - tests/cross-harness-validation.sh
autonomous: true
---

<objective>
## Goal
Validate the explicit implementer-config and broadened delegated-APPLY contract in fresh-install / fresh-project-facing surfaces, then refresh the user-facing docs and validation suites so the generated `pals.json` shape and Pi operating model stay in sync.

## Purpose
Phase 164 aligned the canonical config contract and Phase 165 broadened bounded delegation. Phase 166 closes the loop by ensuring users and validation suites see the same truth: fresh init/config guidance includes `agents.implementer`, Pi docs describe `pals-implementer` as a helper rather than a second lifecycle owner, and future drift is caught automatically.

## Output
Updated primary docs and Pi adapter docs, plus stronger Pi and cross-harness validation coverage that fails if generated-config or bounded-delegation markers drift.
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md

## Prior Work (only if genuinely needed)
.paul/phases/164-implementer-config-contract-alignment/164-01-SUMMARY.md
.paul/phases/165-delegated-apply-expansion/165-01-SUMMARY.md

## Source Files
README.md
kernel/workflows/init-project.md
kernel/references/pals-json-schema.md
drivers/pi/extensions/README.md
drivers/pi/skill-map.md
tests/pi-end-to-end-validation.sh
tests/cross-harness-validation.sh
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: TODD(100) → no conventional `.test` / `.spec` suite or package-managed test framework was detected at repo root; treat this as docs + shell-validation work and verify via the existing validation scripts instead of TDD restructuring | IRIS(150) → no `TODO` / `FIXME` / `HACK` / `XXX` markers were found in the candidate README / Pi-doc / validation files | DAVE(200) → no CI/CD config files were detected, so there is no pipeline-specific planning constraint for this phase | DOCS(200) → 88 non-`.paul` markdown docs exist and the planned files already sit in the primary README / Pi adapter doc surfaces, so doc ownership for this phase is explicit | RUBY(250) → candidate files are bounded docs and shell-validation surfaces; keep assertions semantic and avoid brittle line-count coupling

[dispatch] pre-plan enforcement: DEAN(50) → skip; no supported package-manager manifest was present at repo root, so dependency-audit blocking does not apply
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | Entire PLAN → APPLY → UNIFY lifecycle | ✓ |
| /carl | required | Automatic session-boundary handling during execution | ✓ |
| TODD | required | Before/during implementation verification expectations | ✓ |
| WALT | required | After implementation validation and UNIFY quality tracking | ✓ |

**BLOCKING:** Required skills MUST remain available before APPLY proceeds.

## Skill Invocation Checklist
- [x] /paul loaded
- [x] /carl active via installed Pi runtime
- [x] TODD module overlay available
- [x] WALT module overlay available
</skills>

<acceptance_criteria>
## AC-1: Fresh config guidance matches the canonical implementer contract
```gherkin
Given a user reads the primary PALS docs or the init-generated config guidance for a new project
When they inspect the documented `pals.json` shape
Then `agents.implementer` appears alongside the other current top-level sections with defaults and wording that matches the canonical init/schema contract
```

## AC-2: Pi delegated-APPLY docs keep helper-agent boundaries explicit
```gherkin
Given a Pi user reads the adapter-facing documentation
When they inspect how delegated APPLY is described
Then the docs clearly distinguish `/paul-*` wrappers, canonical `/skill:paul-*` entry points, the helper `pals-implementer`, and the rule that parent APPLY keeps verification, module gates, fallback, checkpoints, and `.paul/*` lifecycle authority
```

## AC-3: Validation catches config and delegation drift across doc and installed surfaces
```gherkin
Given future changes touch init/config/delegation surfaces
When the Pi and cross-harness validation suites run
Then they fail if the generated-config markers or bounded delegated-APPLY contract drift from the canonical shared workflow and documentation surfaces
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Refresh the primary README with the canonical config and delegation contract</name>
  <files>README.md</files>
  <action>
    Update the main README so its user-facing configuration and operating-model sections match the current shared workflow contract.
    - Add the explicit `agents.implementer` block to the documented `pals.json` example near the existing top-level sections.
    - Keep the generated-config story aligned with `kernel/workflows/init-project.md` and `kernel/references/pals-json-schema.md`, including `planning`, `preferences`, and `guided_workflow` as current top-level sections.
    - Tighten the delegated APPLY wording so it stays consistent with Phase 165: `pals-implementer` is a bounded helper surface, not a second APPLY owner.
    Avoid: changing actual workflow semantics in README, inventing new config fields, or rewriting unrelated install/module sections.
  </action>
  <verify>grep -n '"agents"\|"implementer"\|guided_workflow\|pals-implementer\|parent APPLY remains authoritative' README.md</verify>
  <done>AC-1 and AC-2 satisfied: the primary user-facing doc shows the canonical config shape and bounded delegation model without ambiguity.</done>
</task>

<task type="auto">
  <name>Task 2: Align Pi adapter docs with the fresh-install helper-agent model</name>
  <files>drivers/pi/extensions/README.md, drivers/pi/skill-map.md</files>
  <action>
    Refresh the Pi adapter documentation so fresh-install readers see the same three-surface runtime model and delegated-APPLY contract everywhere.
    - Keep `~/.pi/agent/skills/pals/`, `~/.pi/agent/extensions/`, and `~/.pi/agent/agents/` clearly separated by role.
    - Ensure the docs explain that `pals-implementer` is available after install as a helper agent only for eligible bounded repo-local work.
    - Re-state that parent APPLY keeps verification, module gates, fallback, checkpoints, and `.paul/*` lifecycle authority.
    Avoid: adding Pi-only lifecycle semantics, implying helper agents can start PLAN/APPLY/UNIFY directly, or duplicating large README sections verbatim.
  </action>
  <verify>grep -n 'pals-implementer\|three distinct PALS runtime surfaces\|\.paul/\*\|parent APPLY' drivers/pi/extensions/README.md drivers/pi/skill-map.md</verify>
  <done>AC-2 satisfied: Pi-facing docs preserve the helper-agent boundary and install-surface model consistently.</done>
</task>

<task type="auto">
  <name>Task 3: Extend validation to guard fresh-config and delegation contract drift</name>
  <files>tests/pi-end-to-end-validation.sh, tests/cross-harness-validation.sh</files>
  <action>
    Strengthen the validation suites so future contract drift is caught automatically.
    - Add Pi validation assertions for the installed init/schema/apply surfaces and the repo docs that now describe the explicit implementer config and bounded delegation model.
    - Add cross-harness assertions that keep the shared installed init/schema/apply contract aligned while using only milestone-critical doc markers from repo-facing documentation.
    - Keep assertions semantic and contract-oriented rather than fragile line-count checks.
    Avoid: turning the validation suites into generic markdown linting, depending on local mutable runtime state outside the temp-HOME install flow, or asserting wording that is unrelated to the implementer-config / delegated-APPLY contract.
  </action>
  <verify>bash tests/pi-end-to-end-validation.sh && bash tests/cross-harness-validation.sh</verify>
  <done>AC-3 satisfied: validation fails if the fresh-config or bounded-delegation contract drifts across the planned surfaces.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- `kernel/workflows/apply-phase.md`, `kernel/workflows/plan-phase.md`, and other shared lifecycle semantics unless validation exposes a real contract bug that this plan cannot avoid
- `.pi/agents/pals-implementer.md` and `drivers/pi/skills/paul-apply/SKILL.md` — Phase 165 already aligned these and this phase is for validation/docs refresh
- `.paul/*` historical phase artifacts other than creating the new Phase 166 plan/state/roadmap updates

## SCOPE LIMITS
- Do not broaden delegated APPLY further or redesign eligibility rules in this phase
- Do not add new `pals.json` fields; align docs/tests to the existing canonical contract only
- Do not convert historical validation reports into a new reporting system; keep this phase focused on active docs and executable validation suites
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `grep -n '"agents"\|"implementer"\|guided_workflow\|pals-implementer' README.md drivers/pi/extensions/README.md drivers/pi/skill-map.md`
- [ ] `bash tests/pi-end-to-end-validation.sh`
- [ ] `bash tests/cross-harness-validation.sh`
- [ ] All acceptance criteria are satisfied without changing lifecycle ownership or config semantics
</verification>

<success_criteria>
- All planned docs describe the current explicit implementer-config and helper-agent model consistently
- Pi validation covers the fresh-config / delegated-APPLY contract on installed and repo-facing surfaces
- Cross-harness validation keeps the shared installed init/schema/apply contract aligned with the refreshed documentation markers
- No new lifecycle semantics or config fields are introduced
</success_criteria>

<output>
After completion, create `.paul/phases/166-validation-docs-refresh/166-01-SUMMARY.md`
</output>
