---
phase: 169-supporting-ux-alignment-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - drivers/pi/extensions/README.md
  - drivers/pi/skill-map.md
  - drivers/pi/skills/paul-help/SKILL.md
  - tests/pi-end-to-end-validation.sh
  - drivers/pi/extensions/pals-hooks.ts
autonomous: true
---

<objective>
## Goal
Align the Pi-facing docs, discovery surfaces, and validation contract around the calmer lifecycle presentation shipped in Phase 168 so the documented and tested adapter behavior now matches the strict milestone / phase / loop / next-action hierarchy.

## Purpose
Phase 168 intentionally stopped at the runtime render-path change and preserved older docs/validation expectations so the bounded UI polish could land cleanly. Phase 169 closes that loop: it updates the surrounding Pi contract surfaces so users, validation, and adjacent runtime wording all describe the same calmer lifecycle model without reopening lifecycle ownership, adding new persistent surfaces, or broadening into a Pi redesign.

## Output
Updated Pi-facing documentation and validation guidance for the calmer lifecycle surface, plus any strictly necessary adjacent wording cleanup in `drivers/pi/extensions/pals-hooks.ts`, with the final contract verified by the Pi end-to-end validation suite.
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md
.paul/SPECIAL-FLOWS.md

## Prior Work (only if genuinely needed)
- `.paul/phases/167-ux-audit-target-design/167-ux-audit-design-report.md` — separates Phase 168 runtime implementation from Phase 169 docs/validation follow-up and recommends semantic guardrails that avoid brittle snapshots
- `.paul/phases/167-ux-audit-target-design/pi-lifecycle-ux-design.md` — defines the accepted 4-line hierarchy and deliberate removals from the persistent panel
- `.paul/phases/168-core-lifecycle-ui-polish/168-01-SUMMARY.md` — records what actually changed in the runtime, what was intentionally deferred, and the carry-forward scope for this phase

## Source Files
Primary contract-alignment targets:
- `drivers/pi/extensions/README.md`
- `drivers/pi/skill-map.md`
- `drivers/pi/skills/paul-help/SKILL.md`
- `tests/pi-end-to-end-validation.sh`

Conditional adjacent wording target:
- `drivers/pi/extensions/pals-hooks.ts` — only if source inspection shows nearby user-facing wording that now drifts from the calmer lifecycle contract

Notes:
- `.paul/PRD.md` is absent in this repo.
- Keep all work in repo source files only; never edit installed runtime copies under `~/.pi`.
- The accepted Phase 168 runtime shape already keeps the widget centered on `PALS Milestone`, `Phase`, `Loop`, and `Next action`; this phase should document and validate that shape rather than redesign it.
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: TODD(100) → no conventional `*.test` / `*.spec` infrastructure detected for this scope; existing validation is shell-script-based (`tests/pi-end-to-end-validation.sh`) so no TDD restructure is suggested | IRIS(150) → no `TODO` / `FIXME` / `HACK` / `XXX` markers found in the planned files | DAVE(200) → no repo-local CI config found under `.github/workflows` / Docker / Jenkins; if CI needs to be codified later, generate a bounded workflow separately rather than folding it into this phase | DOCS(200) → 88 markdown docs found; `drivers/pi/extensions/README.md` is the main drift surface because it still describes quick-action presentation that Phase 168 removed from the persistent panel | RUBY(250) → debt awareness only; `drivers/pi/extensions/pals-hooks.ts` (1331 lines) and `tests/pi-end-to-end-validation.sh` (996 lines) are already large, so this phase should prefer precise edits over additional accretion | ARIA(190) → no UI component files in planned changes | DANA(195) → no data/schema files in planned changes | GABE(175) → no API files in planned changes | LUKE(185) → no UI styling/component files in planned changes | OMAR(205) → observability relevance is low; no console logging found in planned files and no route-entry health-surface changes are in scope | PETE(225) → no heavy-import or obvious performance-sensitive scope detected | REED(210) → only incidental timeout references exist in `pals-hooks.ts`; no retry / shutdown / error-boundary work is planned | VERA(215) → no privacy-relevant files or PII-oriented changes detected.

[dispatch] pre-plan enforcement: DEAN(50) → PASS; `npm audit --json` reports `0 critical`, `1 high`, `5 moderate`, and no `.paul/dean-baseline.json` is present, so planning is allowed because there are no critical vulnerabilities to block on | ARCH(75) → PASS; detected layered driver/kernel/modules/tests structure with no circular-dependency concern for the planned docs/validation scope, but large-file warnings remain for `drivers/pi/extensions/pals-hooks.ts` and `tests/pi-end-to-end-validation.sh` | SETH(80) → PASS; no hardcoded secret markers found in the planned files, and no auth/input-validation scope was identified.

Additional planning context:
- Planning posture: `medium` collaboration, `direct-requirements` mode.
- Phase 168 deliberately kept the direct `renderLoopBadge(state.loop)` status wiring intact; this phase should preserve that contract unless a directly related doc/validation update requires explicitly describing it.
- Validation needs semantic guardrails for the calmer surface, but those guardrails should remain structural and maintainable rather than exact-string snapshots of every rendered line.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | Entire PLAN → APPLY → UNIFY lifecycle for this alignment/validation phase | ✓ |
| /carl | required | Automatic session-boundary handling while the phase moves across APPLY / UNIFY | ✓ |
| TODD | required | During implementation to keep validation edits disciplined and regression-aware | ✓ |
| WALT | required | During verification and UNIFY quality tracking | ✓ |

**BLOCKING:** Required skills MUST remain available before APPLY proceeds.

## Skill Invocation Checklist
- [x] /paul loaded
- [x] /carl active via installed Pi runtime
- [x] TODD module overlay available
- [x] WALT module overlay available
</skills>

<acceptance_criteria>
## AC-1: Pi-facing docs describe the calmer lifecycle contract instead of the pre-Phase-168 presentation
```gherkin
Given the Phase 168 runtime change is already shipped
When a user reads the Pi adapter discovery docs and help surfaces
Then the lifecycle surface is described as centered on milestone, phase, loop, and next action
And shortcuts are presented as available entry points rather than persistent panel lines
And the docs do not claim the old `Actions:` / `More:` panel structure still exists
```

## AC-2: Validation protects the calmer lifecycle contract with semantic guardrails
```gherkin
Given the Pi end-to-end validation suite is the durable contract surface for adapter behavior
When the lifecycle-related checks are updated for Phase 169
Then they still verify the core milestone / phase / loop / next-action contract and approved wiring
And they add bounded guardrails against reintroducing the old noisy persistent elements
And they avoid brittle snapshot-style assertions that would overfit cosmetic formatting
```

## AC-3: Any adjacent runtime wording and the final verification results stay aligned with the bounded Pi model
```gherkin
Given the docs and validation are aligned to the calmer lifecycle presentation
When any necessary nearby wording in `drivers/pi/extensions/pals-hooks.ts` is reviewed and the verification suite is run
Then user-facing wording remains consistent with the calmer lifecycle language
And the direct runtime behavior from Phase 168 is not broadened into new persistent surfaces or lifecycle-state ownership changes
And `bash tests/pi-end-to-end-validation.sh` passes with the updated contract
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Refresh Pi discovery surfaces to match the implemented calm lifecycle presentation</name>
  <files>drivers/pi/extensions/README.md, drivers/pi/skill-map.md, drivers/pi/skills/paul-help/SKILL.md</files>
  <action>
    Update the Pi-facing documentation and discovery copy so it accurately describes the runtime shipped in Phase 168.
    At minimum:
    - describe the persistent lifecycle surface as the calm milestone / phase / loop / next-action hierarchy
    - keep shortcut guidance, but frame shortcuts as functional entry points rather than persistent widget rows
    - keep `/paul-*` vs canonical `/skill:paul-*`, module-overlay, helper-agent, and installation-boundary guidance intact
    - remove or rewrite any statements that still describe the old `Actions:` / `More:` presentation or imply that shortcut clutter remains in the main persistent panel
    Avoid: inventing new surfaces, reopening live-module-visibility design, or broadening this into unrelated README cleanup.
  </action>
  <verify>grep -q 'milestone, phase, loop, and next action' drivers/pi/extensions/README.md && grep -q 'Ctrl+Alt+N' drivers/pi/extensions/README.md && ! grep -q 'secondary "More" line' drivers/pi/extensions/README.md && grep -q 'shortcut' drivers/pi/skill-map.md && grep -q 'canonical' drivers/pi/skills/paul-help/SKILL.md</verify>
  <done>AC-1 satisfied: the Pi-facing docs/help surfaces now describe the calmer lifecycle contract and no longer claim the old persistent quick-action panel shape.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten the Pi validation contract around the calmer lifecycle hierarchy</name>
  <files>tests/pi-end-to-end-validation.sh</files>
  <action>
    Update `tests/pi-end-to-end-validation.sh` so the lifecycle checks protect the intended Phase 168/169 contract semantically.
    At minimum:
    - keep existing guardrails for field presence, approved event wiring, canonical routing, helper-agent boundaries, and installation/discovery surfaces
    - add bounded checks that guard against reintroducing the old noisy persistent elements (`PALS Lifecycle`, `Actions:`, `More:`) where those checks materially protect the intended contract
    - align README/discovery-surface expectations to the updated wording from Task 1
    - prefer structural/semantic assertions over fragile full-output snapshots or cosmetic exact-line matching
    Avoid: deleting high-value existing coverage, weakening helper-agent or authoritative-artifact guardrails, or turning this into a general test-suite refactor.
  </action>
  <verify>bash tests/pi-end-to-end-validation.sh</verify>
  <done>AC-2 satisfied: the validation suite protects the calmer lifecycle contract and still passes without brittle presentation snapshots.</done>
</task>

<task type="auto">
  <name>Task 3: Apply only necessary adjacent runtime wording cleanup and complete bounded verification</name>
  <files>drivers/pi/extensions/pals-hooks.ts, drivers/pi/extensions/README.md, drivers/pi/skill-map.md, drivers/pi/skills/paul-help/SKILL.md, tests/pi-end-to-end-validation.sh</files>
  <action>
    Inspect `drivers/pi/extensions/pals-hooks.ts` for any nearby user-facing wording that now drifts from the calmer lifecycle contract after Tasks 1-2.
    If drift exists, make the smallest wording-only update needed so orientation/help text matches the documented lifecycle hierarchy while preserving Phase 168 behavior.
    Then run the bounded verification for this phase:
    - bundle the extension from repo source to catch syntax/runtime-shape regressions
    - run `bash tests/pi-end-to-end-validation.sh`
    - confirm the updated contract still preserves direct `renderLoopBadge(state.loop)` status wiring, approved adapter events, and the additive Pi model
    Avoid: changing the actual lifecycle render hierarchy unless a wording fix cannot be achieved otherwise, removing shortcut functionality, or broadening into lifecycle-authority / CARL / guided-workflow redesign.
  </action>
  <verify>npx -y esbuild drivers/pi/extensions/pals-hooks.ts --bundle --platform=node --format=cjs --outfile=/tmp/pals-hooks-p169.js --external:@mariozechner/pi-tui && bash tests/pi-end-to-end-validation.sh</verify>
  <done>AC-3 satisfied: any adjacent runtime wording is aligned, the bounded Pi model is preserved, and the updated contract verifies cleanly.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- `.paul/*` lifecycle authority, command routing semantics, or CARL session-boundary behavior
- the core 4-line runtime hierarchy shipped in Phase 168, except for strictly necessary wording-only cleanup in `drivers/pi/extensions/pals-hooks.ts`
- installed runtime files under `~/.pi/agent/...`
- helper-agent boundaries for `pals-implementer`, module-overlay semantics, or the authoritative `/skill:paul-*` model
- `README.md` or unrelated docs unless a direct Phase 169 drift is proven by source search and is necessary to keep the Pi lifecycle contract accurate

## SCOPE LIMITS
- This phase aligns docs, discovery surfaces, validation, and any directly adjacent wording only
- Do not add new widgets, dashboards, module panels, or persistent shortcut rows
- Do not reopen Phase 168 runtime design decisions or turn this into a broader Pi UX redesign
- Do not redesign live module visibility; at most, clarify its bounded/non-authoritative posture if a touched doc already mentions it
- Keep edits precise because `drivers/pi/extensions/pals-hooks.ts` and `tests/pi-end-to-end-validation.sh` are already large files
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `grep -q 'milestone, phase, loop, and next action' drivers/pi/extensions/README.md`
- [ ] `! grep -q 'secondary "More" line' drivers/pi/extensions/README.md`
- [ ] `grep -q 'shortcut' drivers/pi/skill-map.md`
- [ ] `grep -q 'canonical' drivers/pi/skills/paul-help/SKILL.md`
- [ ] `npx -y esbuild drivers/pi/extensions/pals-hooks.ts --bundle --platform=node --format=cjs --outfile=/tmp/pals-hooks-p169.js --external:@mariozechner/pi-tui`
- [ ] `bash tests/pi-end-to-end-validation.sh`
- [ ] Lifecycle-related validation still checks approved adapter events, canonical routing, and additive Pi boundaries
- [ ] All acceptance criteria are satisfied without adding new persistent UI surfaces or moving lifecycle truth out of shared artifacts
</verification>

<success_criteria>
- Pi-facing docs and help surfaces accurately describe the calm 4-line lifecycle presentation
- The validation suite now protects the calmer lifecycle contract instead of only the older broader presence checks
- Any needed adjacent runtime wording is aligned without reopening the underlying render implementation
- The updated contract passes verification and remains bounded to the existing additive Pi model
</success_criteria>

<output>
After completion, create `.paul/phases/169-supporting-ux-alignment-validation/169-01-SUMMARY.md`
</output>
