---
phase: 63-guided-workflow-ux
plan: 02
type: fix
wave: 1
depends_on: []
files_modified:
  - drivers/pi/install.sh
  - drivers/claude-code/install.sh
  - kernel/workflows/plan-phase.md
  - kernel/workflows/apply-phase.md
  - kernel/workflows/unify-phase.md
  - kernel/workflows/fix-loop.md
  - tests/pi-end-to-end-validation.sh
  - tests/cross-harness-validation.sh
  - docs/MODULE-SPEC.md
  - docs/HOOK-CONTRACT.md
  - kernel/references/module-dispatch.md
autonomous: true
---

<objective>
## Fix
Fix module dispatch so enabled modules are actually consulted during PLAN/APPLY/UNIFY, reconciling conceptual `kernel/modules.yaml` workflow wording with the real installed `modules.yaml` registries.
</objective>

<tasks>
<task type="auto">
  <name>Fix: make installed module registries carry hook metadata and align lifecycle workflows to use it</name>
  <files>
    drivers/pi/install.sh
    drivers/claude-code/install.sh
    kernel/workflows/plan-phase.md
    kernel/workflows/apply-phase.md
    kernel/workflows/unify-phase.md
    kernel/workflows/fix-loop.md
    tests/pi-end-to-end-validation.sh
    tests/cross-harness-validation.sh
    docs/MODULE-SPEC.md
    docs/HOOK-CONTRACT.md
    kernel/references/module-dispatch.md
  </files>
  <action>
    Audit how installers generate `modules.yaml`, extend the installed registry with the hook metadata lifecycle workflows actually need, and update shared lifecycle workflows/docs/tests so they read hook details from the installed registry instead of depending on conceptual access to source `module.yaml` manifests.
  </action>
  <verify>
    Run targeted validation proving installed `modules.yaml` now includes hook metadata/refs and that Pi + cross-harness validation still pass.
  </verify>
  <done>Fix verified working</done>
</task>
</tasks>

<verification>
- [ ] Fix applied and verified
- [ ] No regressions introduced
</verification>
