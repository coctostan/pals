---
phase: 185-artifact-archival-hot-path-compaction
plan: 02
type: execute
wave: 2
depends_on: ["185-01"]
files_modified:
  - .paul/archive/INDEX.md
  - .paul/archive/state/STATE-HISTORY-v0-v2.43.md
  - .paul/STATE.md
autonomous: true
---

<objective>
## Goal
Compact `.paul/STATE.md` by archiving accumulated historical decisions, fixes, deviations, and skill-audit history into a linked cold archive while preserving current lifecycle routing, current blockers, current git state, and session continuity in the hot state artifact.

## Purpose
Plan 185-01 proved the archive-link pattern on lower-risk `MILESTONES.md` and `PROJECT.md`. This plan applies the same pattern to the most sensitive hot-path artifact, `STATE.md`, without changing PALS lifecycle semantics or hiding any project truth outside `.paul/*` artifacts.

## Output
Create/update:
- `.paul/archive/state/STATE-HISTORY-v0-v2.43.md`
- `.paul/archive/INDEX.md`
- `.paul/STATE.md`

This plan intentionally does **not** compact `.paul/ROADMAP.md`; roadmap active-window/history compaction remains a follow-on Phase 185 plan after `STATE.md` archival proves safe.
</objective>

<context>
## Project Context
- `.paul/PROJECT.md` — compact hot-path project brief; confirms artifact authority and current v2.43 context-diet goals.
- `.paul/ROADMAP.md` — Phase 185 remains in progress; `STATE.md` and `ROADMAP.md` compaction are pending after 185-01.
- `.paul/STATE.md` — current lifecycle state and the target hot artifact for this plan.
- `.paul/archive/INDEX.md` — archive catalog created by 185-01; must be extended rather than bypassed.

## Prior Work
- `.paul/phases/185-artifact-archival-hot-path-compaction/185-01-SUMMARY.md` — archive-link convention proven for `MILESTONES.md` and `PROJECT.md`; next wave should use it for `STATE.md` decision/fix/audit history.

## Source Files
No application source files are modified. This plan touches PALS lifecycle/archive markdown artifacts only.
</context>

<module_dispatch>
## Pre-Plan Module Dispatch

Installed registry loaded from `modules.yaml` (`kernel_version: 2.0.0`). Planning collaboration level: high (project default). Planning mode: direct-requirements continuation from 185-01 UNIFY; user selected continuation to the next Phase 185 plan.

[dispatch] pre-plan advisory: ARCH(75) → no application architecture changes; markdown lifecycle/archive layering should keep hot state small and cold history indexed | SETH(80) → skipped/pass; planned markdown artifacts only and no secret-bearing config planned | TODD(100) → no TDD restructuring surface for markdown-only artifact migration; existing validation scripts remain the verification surface | IRIS(150) → no TODO/FIXME/HACK/XXX findings in planned hot/archive files | GABE(175) → skipped, no API files | LUKE(185) → skipped, no UI files | ARIA(190) → skipped, no UI files | DANA(195) → skipped, no data/schema files | DAVE(200) → CI config unchanged; existing validation scripts should run after compaction | DOCS(200) → lifecycle documentation changes only; archive index must be updated | OMAR(205) → skipped, no runtime observability files | REED(210) → skipped, no resilience/runtime files | VERA(215) → skipped/pass; no PII logging/storage scope | CODI(220) → skipped — no extractable symbols in phase scope | PETE(225) → skipped, no performance-sensitive source files | RUBY(250) → debt target acknowledged: compact hot `STATE.md` while accepting larger cold archive file under `.paul/archive/`.

[dispatch] pre-plan enforcement: DEAN(50) → skipped/pass; no dependency manifest is planned for modification and no package-manager audit target is present.

[dispatch] post-plan: TODD(100) → no TDD restructuring; plan type remains `execute` because work is deterministic artifact migration/compaction, not research.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | PLAN/APPLY/UNIFY lifecycle | ✓ |
| /carl | required | Session boundary monitoring | automatic |
| TODD | required | Test/validation enforcement | N/A for markdown-only artifact migration; validation scripts required during APPLY |
| WALT | required | APPLY/UNIFY validation and quality history | pending APPLY/UNIFY |

</skills>

<acceptance_criteria>

## AC-1: State history archive exists and is authoritative
```gherkin
Given `.paul/STATE.md` contains long accumulated decisions, fixes, deviations, and skill audits
When Plan 185-02 completes
Then `.paul/archive/state/STATE-HISTORY-v0-v2.43.md` exists with metadata: Source, Archived on, Scope, Still authoritative?, Return link
And it preserves the historical accumulated context moved out of the hot state file
```

## AC-2: Live STATE.md stays lifecycle-complete but compact
```gherkin
Given `STATE.md` is the authoritative hot lifecycle artifact
When Plan 185-02 completes
Then it keeps Project Reference, Current Position, Loop Position, current Blockers/Concerns, Git State, and Session Continuity inline
And historical accumulated context is replaced with concise summaries and explicit links to `.paul/archive/state/STATE-HISTORY-v0-v2.43.md`
And the live file is materially smaller than before this plan
```

## AC-3: Archive index catalogs the new state archive
```gherkin
Given `.paul/archive/INDEX.md` is the single archive entry point
When Plan 185-02 completes
Then it includes `.paul/archive/state/STATE-HISTORY-v0-v2.43.md` with source, scope, authoritative status, and return link
And no parallel unindexed archive entry point is introduced
```

## AC-4: Lifecycle routing and validation remain intact
```gherkin
Given `STATE.md` controls PALS resume/status routing
When Plan 185-02 completes
Then PLAN/APPLY/UNIFY loop markers, next action, git state, and resume file still parse correctly
And existing Pi/cross-harness validation remains green
And `.paul/ROADMAP.md` is not structurally compacted by this plan except for normal PAUL lifecycle updates
```
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Archive historical STATE accumulated context</name>
  <files>.paul/archive/state/STATE-HISTORY-v0-v2.43.md</files>
  <action>
    Create `.paul/archive/state/STATE-HISTORY-v0-v2.43.md` and any needed parent directory.
    The archive file must:
    - begin with metadata: Source, Archived on, Scope, Still authoritative?, Return link
    - preserve the historical content removed from `.paul/STATE.md`, including long Decisions, Fixes, Deviations, and Skill Audit sections
    - retain enough headings/table structure that old decisions remain findable from search
    - state that the live `.paul/STATE.md` remains the hot lifecycle routing artifact

    Avoid: summarizing away decision/fix/deviation details; moving current position, current loop state, current git state, or session continuity out of the live state file.
  </action>
  <verify>test -f .paul/archive/state/STATE-HISTORY-v0-v2.43.md && grep -n "Source: .paul/STATE.md\|Still authoritative\|Decisions\|Fixes\|Deviations\|Skill Audit" .paul/archive/state/STATE-HISTORY-v0-v2.43.md</verify>
  <done>AC-1 satisfied: state history exists as an authoritative archive preserving removed historical context.</done>
</task>

<task type="auto">
  <name>Task 2: Compact live STATE.md around current lifecycle routing</name>
  <files>.paul/STATE.md</files>
  <action>
    Rewrite `.paul/STATE.md` as a compact hot-path lifecycle state file.
    It must keep inline:
    - Project Reference and core value/current focus
    - Current Position
    - Loop Position visual marker
    - a compact Accumulated Context summary with archive links
    - current Blockers/Concerns
    - Git State
    - Session Continuity

    Replace long historical tables with concise summaries and explicit links to `.paul/archive/state/STATE-HISTORY-v0-v2.43.md`.
    Preserve the current route: Plan 185-02 created/ready for APPLY after normal plan lifecycle update.

    Avoid: removing current next action, hiding lifecycle state in the archive, changing command/workflow semantics, or deleting historical content before Task 1 has archived it.
  </action>
  <verify>grep -n "Current Position\|Loop Position\|Git State\|Session Continuity\|STATE-HISTORY-v0-v2.43.md" .paul/STATE.md && test "$(wc -l < .paul/STATE.md)" -lt 180</verify>
  <done>AC-2 satisfied: live state remains route-complete and materially smaller with archive links.</done>
</task>

<task type="auto">
  <name>Task 3: Register state archive and run lifecycle validation</name>
  <files>.paul/archive/INDEX.md, .paul/archive/state/STATE-HISTORY-v0-v2.43.md, .paul/STATE.md</files>
  <action>
    Update `.paul/archive/INDEX.md` to catalog `state/STATE-HISTORY-v0-v2.43.md`.
    Then validate the archive split and lifecycle safety:
    - archive index links to the new state archive
    - compact `STATE.md` links back to the state archive
    - state archive contains metadata and preserved history headings
    - no `.paul/archive/roadmap` directory or `ROADMAP.md` structural compaction is introduced in this plan
    - existing validation suites still pass:
      - `bash tests/pi-end-to-end-validation.sh`
      - `bash tests/cross-harness-validation.sh`

    Avoid: adding future empty archive directories or changing roadmap structure in this plan.
  </action>
  <verify>grep -n "STATE-HISTORY-v0-v2.43.md" .paul/archive/INDEX.md .paul/STATE.md && test ! -d .paul/archive/roadmap && bash tests/pi-end-to-end-validation.sh && bash tests/cross-harness-validation.sh</verify>
  <done>AC-3 and AC-4 satisfied: archive index includes the state archive, scope stayed bounded, and validation remains green.</done>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- Do not structurally compact `.paul/ROADMAP.md` in this plan except for normal PAUL lifecycle updates.
- Do not edit `kernel/workflows/*`, module manifests, drivers, tests, or source code as part of state archival.
- Do not delete historical decisions, fixes, deviations, or skill audits unless they have first been preserved in the state archive.
- Do not replace `.paul/STATE.md` with a pointer-only stub; it must remain the hot lifecycle routing artifact.

## SCOPE LIMITS
- This is the second Phase 185 implementation wave: `STATE.md` historical accumulated-context archival and compaction only.
- `ROADMAP.md` active-window/history compaction is deferred to a later Phase 185 plan.
- Workflow/module prose compression belongs to Phase 186.
- Do not use telemetry, hidden runtime state, Pi-owned persistence, or local memory as a substitute for explicit `.paul/*` artifacts.
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `.paul/archive/state/STATE-HISTORY-v0-v2.43.md` exists and preserves historical STATE accumulated context
- [ ] `.paul/STATE.md` remains lifecycle-route-complete and links to the state archive
- [ ] `.paul/archive/INDEX.md` catalogs the state archive
- [ ] No `ROADMAP.md` structural compaction happened in this plan
- [ ] `bash tests/pi-end-to-end-validation.sh` passes
- [ ] `bash tests/cross-harness-validation.sh` passes
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- Phase 185 has now proven the archive-link convention on the sensitive hot `STATE.md` artifact
- `STATE.md` is materially smaller while keeping current lifecycle routing inline
- Historical decisions/fixes/deviations/skill audits remain findable and authoritative in `.paul/archive/state/STATE-HISTORY-v0-v2.43.md`
- PALS lifecycle routing and validation remain green
- Follow-on Phase 185 work can safely address `ROADMAP.md` active-window/history compaction using the same archive-link pattern
</success_criteria>

<output>
After completion, create `.paul/phases/185-artifact-archival-hot-path-compaction/185-02-SUMMARY.md`.
</output>
