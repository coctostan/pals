---
phase: 182-bounded-instrumentation-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/codi/module.yaml
  - modules/codi/references/codi-instrumentation.md
  - kernel/workflows/unify-phase.md
autonomous: true
---

<objective>
## Goal
Add the smallest post-unify contract repair that (1) makes the 113-01 hook-entry/iteration failure observable and (2) stops 112-01-style Blast Radius plans from collapsing into `no-dispatch-found`, while keeping v2.41 artifacts untouched.

## Purpose
Phase 181 already isolated the two owning layers: 112-01 is a parser-too-strict / wording-drift problem on the post-unify measurement surface, and 113-01 lives in the UNIFY dispatch path with the sub-sub-cause intentionally deferred to a bounded Phase 182 probe. This phase lands the minimum honest-measurement repair under the fresh v2.42 SHA freeze so Phase 183 can validate the repaired instrument on a new natural quark phase instead of re-arguing old evidence.

## Output
- Updated `modules/codi/module.yaml` post-unify contract with a pre-STEP-1 hook-entry guard and a distinct degraded-injection classification path for Blast Radius-preserving-but-countless plans.
- Updated `modules/codi/references/codi-instrumentation.md` so the row schema, outcome taxonomy, and hotfix/read-order guidance stay aligned with the repaired manifest contract.
- Updated `kernel/workflows/unify-phase.md` only if the opening probe proves a workflow-side iteration/selection wording correction is still required for CODI(220) to be enumerated reliably.
- No retroactive edit to any v2.41 artifact, quark row, or `.paul/CODI-HISTORY.md` history entry.
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md
.paul/SPECIAL-FLOWS.md

## Prior Work (only if genuinely needed)
.paul/phases/181-root-cause-analysis/181-01-ANALYSIS.md — authoritative ownership map, Phase 182 authorization envelope, and the exact 112-01 / 113-01 evidence.
.paul/phases/181-root-cause-analysis/181-01-SUMMARY.md — concise handoff with the Phase 182 next-action sentence and preserved milestone invariants.
.paul/proposals/v2.42-post-unify-instrumentation-fix.md — bounded milestone premise and explicit out-of-scope list.

## Source Files
modules/codi/module.yaml
modules/codi/references/codi-instrumentation.md
kernel/workflows/unify-phase.md
modules/codi/references/codi.md
drivers/pi/install.sh
tests/pi-end-to-end-validation.sh
tests/cross-harness-validation.sh
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
Dispatched against the installed `modules.yaml` registry for the candidate Phase 182 scope: `modules/codi/module.yaml`, `modules/codi/references/codi-instrumentation.md`, and `kernel/workflows/unify-phase.md` only if the opening probe proves a workflow-side dispatch wording fix is still needed. `modules/codi/references/codi.md`, `drivers/pi/install.sh`, and the two validation suites are read-only verification/deploy surfaces.

**Planning posture used for this run:** `high` collaboration (project default) + `direct-requirements` mode (inferred from Phase 181's bounded authorization envelope and the explicit Phase 182 next-action handoff).

**High-collaboration shaping notes:**
- Assumption: `modules/codi/references/codi-instrumentation.md` moves in lockstep with any new outcome token or field semantics because it is the post-unify hook's contract reference, even though Phase 181 named the manifest as the primary edit surface.
- Alternative intentionally deferred: optional `kernel/workflows/plan-phase.md` wording-guard work stays out of this plan unless APPLY finds a concrete repo-side paraphrase path that parser widening alone cannot safely absorb.
- Primary risk to manage: `kernel/workflows/unify-phase.md` is already 614 lines. If it needs a real edit, keep it limited to the `post_unify_hooks` iteration/selection wording and avoid broader workflow churn.

**Advisory (non-blocking):**
- `[dispatch] ARCH (p75) → pass with structural warning` — the repo remains a layered modules/kernel/drivers layout and the candidate changes stay inside the expected module/workflow layer. If `kernel/workflows/unify-phase.md` is touched, keep the diff surgical because it is already a large workflow file.
- `[dispatch] TODD (p100) → skip` — no standard `.test` / `.spec` infrastructure was detected for this repo, and the phase is contract/workflow repair rather than a test-first feature slice. Use `type: execute` with explicit verification commands.
- `[dispatch] IRIS (p150) → pass` — no TODO/FIXME/HACK/XXX markers were found in the candidate repo-source files.
- `[dispatch] ARIA / LUKE / DANA / GABE / OMAR / PETE / REED / VERA → skip` — no UI, data, API, observability, performance, resilience, or privacy surfaces are in scope for the bounded instrumentation repair.
- `[dispatch] DAVE (p200) → warn` — no CI config was detected in the repo. Useful background only; CI generation remains unrelated to this phase.
- `[dispatch] DOCS (p200) → pass` — the instrumentation reference itself is in scope, so the hook contract can stay documented without opening a broader README / CHANGELOG sweep.
- `[dispatch] CODI (p220) → skipped — no extractable symbols in phase scope` — the planned implementation surfaces are yaml / markdown / workflow files with no bounded TS/JS source selectors, so a codegraph call here would be an overreach rather than useful context.
- `[dispatch] RUBY (p250) → pass with debt note` — the only debt signal is workflow-file size. Treat any `kernel/workflows/unify-phase.md` change as a localized contract edit, not a refactor campaign.

**Enforcement (blocking-capable):**
- `[dispatch] DEAN (p50) → skip` — no supported dependency-audit manifest exists at the repo root, and `pip-audit` is unavailable for the nested `drivers/agent-sdk/requirements.txt` file, so no meaningful blocking delta/baseline audit can be computed during this plan.
- `[dispatch] SETH (p80) → pass` — no secrets or auth-sensitive patterns were found in the candidate file set.
- `[dispatch] ARCH circular-dep → pass` — no import-graph or runtime dependency changes are planned; if the workflow file is touched, keep the change isolated to `post_unify_hooks` wording only.

**Accumulated context_inject:**
- `arch_context.structural_warnings` → if `kernel/workflows/unify-phase.md` changes, keep the diff isolated to the post-unify dispatch section.
- `deploy_context.coverage_gaps` → CI absence is acknowledged but remains out of scope for this bounded repair.
- `debt_flags` → large workflow size is caution only; verification should rely on targeted greps plus existing validation suites, not broader cleanup.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | Entire PLAN → APPLY → UNIFY lifecycle for Phase 182 | ✓ |
| /carl | required | Session-boundary handling during planning, APPLY, and UNIFY | ✓ |
| TODD | required | APPLY verification discipline for bounded contract/workflow edits | ✓ |
| WALT | required | Validation and quality gates before UNIFY | ✓ |

**BLOCKING:** Required lifecycle flows remain in force for APPLY. In this environment, `/carl` is provided by the installed Pi runtime path and TODD/WALT are enforced as module overlays during APPLY/UNIFY rather than separate callable skills.

## Skill Invocation Checklist
- [x] /paul loaded
- [x] /carl active via installed runtime/extension path
- [x] TODD available via installed module overlay
- [x] WALT available via installed module overlay
</skills>

<acceptance_criteria>
## AC-1: The 113-01 omission path becomes observable and bounded to the surviving owner
```gherkin
Given Phase 181 isolated 113-01 to the UNIFY dispatch layer with the sub-sub-cause intentionally deferred
When Phase 182 lands the opening probe and any strictly necessary workflow-side correction
Then CODI's post-unify contract emits a hook-entry trace as soon as STEP 1 begins
  And any workflow edit is limited to `post_unify_hooks` iteration / selection wording in `kernel/workflows/unify-phase.md`
  And installer / registry generation surfaces remain untouched
```

## AC-2: Blast Radius-preserving parser drift is distinguished from CODI-absent cases
```gherkin
Given a PLAN or SUMMARY region that contains `### Blast Radius (CODI)` but not the canonical success-log line
When the CODI post-unify classifier evaluates that region
Then it records a distinct degraded injected outcome instead of collapsing to `no-dispatch-found`
  And it preserves `blast_radius = y` plus the symbol order recoverable from Blast Radius headings
  And true hotfix / no-PLAN / CODI-absent cases still land on `no-dispatch-found`
```

## AC-3: Repo and installed Pi surfaces reflect the bounded repair without rewriting history
```gherkin
Given the repo-source changes for Phase 182 are applied
When `PALS_ROOT="$(pwd)" bash drivers/pi/install.sh`, `bash tests/pi-end-to-end-validation.sh`, and `bash tests/cross-harness-validation.sh` run
Then the installed Pi surface contains the new hook-entry and degraded-classification markers
  And both validation suites pass
  And no v2.41 artifact or historical `.paul/CODI-HISTORY.md` row is edited
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Probe the 113-01 omission path and land the minimal hook-entry visibility guard</name>
  <files>modules/codi/module.yaml, kernel/workflows/unify-phase.md</files>
  <action>
    Start from `.paul/phases/181-root-cause-analysis/181-01-ANALYSIS.md` Section 2 / Block D and resolve the surviving workflow-side ambiguity before broad edits.
    - In `modules/codi/module.yaml`, add a minimal pre-STEP-1 trace that fires as soon as the post-unify hook body begins, before plan-path resolution or outcome classification. Keep STEP 5 append-row behavior and STEP 7 final dispatch logging intact; the new trace exists so future row absence means "not iterated", not "iterated but invisible".
    - Re-check `kernel/workflows/unify-phase.md`'s `post_unify_hooks` step against the Phase 181 omission pattern. If the workflow wording still leaves room for CODI(220) to vanish from iteration, tighten only the iteration / selection language around `installed_modules.*.hook_details.post-unify` and priority ordering. If the current wording is already sufficient, leave the file untouched rather than inventing workflow churn.
    - Preserve the fresh v2.42 SHA discipline: do not read this task as permission to replay or repair quark 113-01. The goal is to make the surviving owner explicit and future observations legible.
    - Avoid: installer scripts, installed `modules.yaml`, `modules/codi/references/codi-instrumentation.md` taxonomy edits before the probe result is known, `kernel/workflows/plan-phase.md`, `.paul/CODI-HISTORY.md`, or any pre-plan CODI behavior.
  </action>
  <verify>grep -n 'hook body entered\|hook_details.post-unify.priority\|installed_modules.\*\.hook_details.post-unify' modules/codi/module.yaml kernel/workflows/unify-phase.md</verify>
  <done>AC-1 satisfied: hook entry is now observable, and any workflow-side correction stays bounded to the `post_unify_hooks` iteration path.</done>
</task>

<task type="auto">
  <name>Task 2: Widen the post-unify classifier so Blast Radius-preserving drift records a distinct degraded injected outcome</name>
  <files>modules/codi/module.yaml, modules/codi/references/codi-instrumentation.md</files>
  <action>
    Repair the 112-01 parser-miss without reopening CODI's advisory behavior or altering historical artifacts.
    - In `modules/codi/module.yaml`, keep the canonical success-log line as the primary `injected` path, but widen STEP 2 / STEP 3 / STEP 4 / STEP 5 so a PLAN or SUMMARY region with `### Blast Radius (CODI)` plus bold symbol headings is treated as a degraded injected outcome instead of falling through to `no-dispatch-found`.
    - Introduce one explicit degraded token (for example `injected-degraded`) that means "CODI ran and blast_radius survived, but the exact success log was not preserved." Preserve `no-dispatch-found` for true hotfix / no-PLAN / CODI-absent cases.
    - Update field extraction semantics so degraded outcomes still set `blast_radius = y` and preserve symbol order from Blast Radius headings, while R / U / K remain `—` when the canonical success counts are unavailable.
    - Update `modules/codi/references/codi-instrumentation.md` in lockstep so the row schema, outcome taxonomy, data-source read order, hotfix behavior, and worked example remain consistent with the manifest contract.
    - Preserve Phase 171 / 176 invariants: the pre-plan success / skip strings remain verbatim, Blast Radius markdown format and extractor rules remain untouched, the classifier-safety token stays absent, and no retroactive v2.41 repair is attempted.
    - Avoid: `modules/codi/references/codi.md`, PLAN-template changes, pre-plan `impact` call semantics, installer/registry work, or any v0.2 / v0.3 surface.
  </action>
  <verify>grep -n 'injected-degraded\|### Blast Radius (CODI)\|no-dispatch-found\|blast_radius' modules/codi/module.yaml modules/codi/references/codi-instrumentation.md</verify>
  <done>AC-2 satisfied: wording-drifted injected outcomes are distinct from CODI-absent cases while hotfix/no-PLAN fallback remains intact.</done>
</task>

<task type="auto">
  <name>Task 3: Reinstall the active Pi surface and prove the bounded repair on repo and installed artifacts</name>
  <files>modules/codi/module.yaml, modules/codi/references/codi-instrumentation.md, kernel/workflows/unify-phase.md</files>
  <action>
    Prove the bounded Phase 182 repair without widening the scope into validation-script rewrites or historical artifact edits.
    - Run targeted repo-side proof greps first so the new hook-entry trace, degraded-injected token, and preserved `no-dispatch-found` fallback are visible in source.
    - Refresh the active Pi runtime surface with `PALS_ROOT="$(pwd)" bash drivers/pi/install.sh`; this phase changes shared workflow/module artifacts consumed from the installed Pi skill tree, so the live runtime must be redeployed before the next PALS phase.
    - Confirm the installed Pi copies carry the same markers in `$HOME/.pi/agent/skills/pals/modules.yaml` and `$HOME/.pi/agent/skills/pals/references/codi-instrumentation.md`. If Task 1 required a workflow edit, confirm the installed `workflows/unify-phase.md` copy reflects it as well.
    - Run `bash tests/pi-end-to-end-validation.sh` and `bash tests/cross-harness-validation.sh` after reinstall.
    - Avoid: modifying validation scripts, installer sources, README / CHANGELOG, or any historical `.paul/*` trial artifact in this phase.
  </action>
  <verify>PI_SKILL_DIR="$HOME/.pi/agent/skills/pals" && grep -n 'injected-degraded\|hook body entered\|no-dispatch-found' modules/codi/module.yaml modules/codi/references/codi-instrumentation.md && PALS_ROOT="$(pwd)" bash drivers/pi/install.sh && grep -n 'injected-degraded\|hook body entered\|no-dispatch-found' "$PI_SKILL_DIR/modules.yaml" "$PI_SKILL_DIR/references/codi-instrumentation.md" && bash tests/pi-end-to-end-validation.sh && bash tests/cross-harness-validation.sh</verify>
  <done>AC-3 satisfied: repo and installed Pi surfaces align on the bounded repair, validation passes, and v2.41 history remains untouched.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- `.paul/CODI-HISTORY.md`, any v2.41 artifact under `.paul/phases/179-*`, `.paul/phases/180-*`, or quark trial artifacts — this phase repairs the instrument for future runs only.
- `modules/codi/references/codi.md`, the pre-plan portion of `modules/codi/module.yaml`, and any Blast Radius markdown / extractor-rule / classifier-token behavior — Phase 182 is post-unify measurement repair only.
- `drivers/pi/install.sh`, `install.sh`, generated `modules.yaml`, README.md, CHANGELOG.md, and validation scripts — use them only as deploy / verification surfaces in this plan.
- Any module or workflow surface outside CODI post-unify and the narrowly scoped `post_unify_hooks` section in `kernel/workflows/unify-phase.md`.

## SCOPE LIMITS
- No retroactive row repair, no synthesized `113-01` row, and no reinterpretation of the v2.41 verdict.
- No `symbol_graph`, `trace`, new PLAN template sections, or broader module-system redesign.
- No installer / registry alignment work unless APPLY proves a real workflow-side wording gap requires a bounded `kernel/workflows/unify-phase.md` correction.
- Verification uses the existing install + validation commands; this plan does not become a test-suite refactor or a documentation sweep.
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `grep -n 'hook body entered\|injected-degraded\|no-dispatch-found' modules/codi/module.yaml modules/codi/references/codi-instrumentation.md`
- [ ] If `kernel/workflows/unify-phase.md` changed, its diff is limited to the `post_unify_hooks` step and remains grep-verifiable against `hook_details.post-unify` / priority-order wording.
- [ ] `PALS_ROOT="$(pwd)" bash drivers/pi/install.sh`
- [ ] Installed Pi surface shows the same new markers under `$HOME/.pi/agent/skills/pals/`
- [ ] `bash tests/pi-end-to-end-validation.sh`
- [ ] `bash tests/cross-harness-validation.sh`
- [ ] No v2.41 artifact or historical `.paul/CODI-HISTORY.md` row was edited
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- The repaired post-unify contract can distinguish degraded injected outcomes from CODI-absent cases
- Future silent non-entry is observable from the hook-entry trace and, if needed, the bounded workflow wording correction
- The active Pi installed surface is refreshed and ready for Phase 183 natural validation
</success_criteria>

<output>
After completion, create `.paul/phases/182-bounded-instrumentation-fix/182-01-SUMMARY.md`
</output>
