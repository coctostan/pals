---
phase: 70-visibility-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - drivers/pi/extensions/pals-hooks.ts
  - drivers/pi/extensions/README.md
  - drivers/pi/skill-map.md
  - tests/pi-end-to-end-validation.sh
autonomous: true
---

<objective>
## Goal
Make module participation legible in Pi's live lifecycle surfaces by surfacing a bounded, derived summary of recent module activity from canonical PALS dispatch/reporting signals.

## Purpose
Phase 68 and Phase 69 established that the module system is real and that the durable evidence path now exists. Phase 70 should turn that proof into a clearer day-to-day Pi experience: users should be able to tell which modules most recently fired while PLAN/APPLY/UNIFY flows are running, without introducing Pi-only lifecycle truth, hidden telemetry, or another execution ledger.

## Output
- Updated `drivers/pi/extensions/pals-hooks.ts` with bounded dispatch-derived module visibility for the existing status/widget surfaces
- Updated Pi adapter docs describing the module-visibility UX and its authoritative-source guardrails
- Expanded `tests/pi-end-to-end-validation.sh` coverage for the live module-visibility contract
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md
.paul/SPECIAL-FLOWS.md

## Prior Work (only if genuinely needed)
- `.paul/phases/68-execution-audit/module-gap-report.md` — identifies the Phase 70 problem as missing live module visibility in Pi's status/widget surfaces
- `.paul/phases/69-gap-fixes/69-01-SUMMARY.md` — confirms the durable module evidence/reporting path is already hardened and should not be reopened here
- `.paul/phases/59-always-visible-lifecycle-surface/59-01-SUMMARY.md` — existing status/widget lifecycle surfaces that Phase 70 will extend
- `.paul/phases/63-guided-workflow-ux/63-01-SUMMARY.md` — recent session-message parsing patterns already used in the extension
- `.paul/phases/64-polish-validation-ux-readability/64-01-SUMMARY.md` — current status/widget/readability guardrails and validation baseline

## Source Files
drivers/pi/extensions/pals-hooks.ts
drivers/pi/extensions/README.md
drivers/pi/skill-map.md
tests/pi-end-to-end-validation.sh
kernel/workflows/plan-phase.md
kernel/workflows/apply-phase.md
kernel/workflows/unify-phase.md
/Users/maxwellnewman/.pi/agent/skills/pals/modules.yaml
/opt/homebrew/lib/node_modules/@mariozechner/pi-coding-agent/docs/extensions.md
/opt/homebrew/lib/node_modules/@mariozechner/pi-coding-agent/docs/tui.md
/opt/homebrew/lib/node_modules/@mariozechner/pi-coding-agent/examples/extensions/send-user-message.ts
/opt/homebrew/lib/node_modules/@mariozechner/pi-coding-agent/examples/extensions/plan-mode/index.ts

## Notes
- Planning posture for this run: `medium` collaboration fallback (`pals.json` has no `planning.default_collaboration`), `direct-requirements` mode — the audit plus Phase 69 already narrowed the problem, so keep this plan execution-focused.
- `.paul/PRD.md` is absent; `PROJECT.md`, `ROADMAP.md`, the audit, and the Phase 69 summary provide sufficient context for this phase.
- Repo-local `kernel/modules.yaml` is still absent; use the installed Pi registry only as background for expected hook stages/modules, not as a reason to reopen installer or registry generation work.
- Treat canonical shared workflow output (especially `[dispatch] ...` lines and durable module-report wording) plus `.paul/STATE.md` as the only inputs for live visibility. Do not create Pi-owned module truth or a new telemetry store.
- If the work surfaces broader questions about long-lived module history, richer proof, or module consolidation, capture them for Phase 71 instead of solving them here.
</context>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | Driving the PLAN → APPLY → UNIFY lifecycle for this phase | ✓ |
| /carl | required | Before changing Pi adapter behavior that shapes user-facing workflow/context guidance | ○ |
| TODD | required | Before and during extension/test changes in APPLY | ○ |
| WALT | required | Before final validation and completion checks in APPLY/UNIFY | ○ |

**BLOCKING:** Required skills MUST be loaded before APPLY proceeds.
Run each skill command or confirm already loaded.

## Skill Invocation Checklist
- [x] /paul loaded
- [ ] /carl loaded or explicitly deemed unnecessary for APPLY
- [ ] TODD loaded or confirmed for APPLY
- [ ] WALT loaded or confirmed for APPLY/UNIFY
</skills>

<acceptance_criteria>
## AC-1: Pi live lifecycle surfaces show recent module participation
```gherkin
Given a PALS workflow running in Pi emits canonical module dispatch/reporting output such as `[dispatch] pre-plan: ...`, `[dispatch] post-apply: ...`, or `[dispatch] post-unify: ...`
When the Pi extension refreshes its persistent lifecycle status/widget in that same session
Then the user can see a bounded summary of the latest module stage and participating modules derived from shared workflow output plus `.paul/STATE.md`
```

## AC-2: Module visibility stays derived-only, compact, and non-authoritative
```gherkin
Given the module-visibility UX is active in Pi
When workflow output is missing, stale, or unrelated to PALS module execution
Then the extension degrades cleanly, avoids stale or misleading module claims, and does not persist Pi-owned module state, hidden telemetry, or another lifecycle truth source
```

## AC-3: Pi docs and validation describe and enforce the visibility contract
```gherkin
Given the updated Pi extension docs and validation suite
When a maintainer reviews the adapter contract or runs `bash tests/pi-end-to-end-validation.sh`
Then the docs describe module visibility as an adapter-only derivation from shared workflow/reporting signals, and the validation suite fails if those visibility surfaces or guardrails regress
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Add a bounded recent-module-activity model to the Pi extension</name>
  <files>drivers/pi/extensions/pals-hooks.ts</files>
  <action>
    Extend `drivers/pi/extensions/pals-hooks.ts` so the adapter can derive a small recent-module-activity snapshot from canonical PALS workflow output already present in the session.
    - Reuse the extension's existing recent-assistant-text/session inspection patterns to look for stable markers such as `[dispatch] pre-plan:`, `[dispatch] post-plan:`, `[dispatch] pre-apply:`, `[dispatch] post-task(Task N):`, `[dispatch] post-apply:`, `[dispatch] pre-unify:`, and `[dispatch] post-unify:`
    - Parse the newest relevant dispatch line into a compact structured summary (stage + participating modules + high-signal outcomes/counts where they are explicitly present)
    - Keep the derivation bounded and explicit: only use canonical workflow markers or clearly durable module-report phrasing already emitted by shared workflows; do not infer module execution from vague assistant narration or registry presence alone
    - Keep this model ephemeral and derived-only inside the adapter; do not use `appendEntry`, hidden persistence, or a Pi-owned module history log
    - Structure the code so module-activity parsing stays clearly separate from lifecycle-state parsing and guided workflow response logic
    Avoid: building a telemetry subsystem, inventing new workflow markers in TypeScript, or claiming modules ran when only install metadata exists.
  </action>
  <verify>npx -y esbuild drivers/pi/extensions/pals-hooks.ts --bundle --platform=node --format=cjs --outfile=/tmp/pals-hooks-p70.js --external:@mariozechner/pi-tui && grep -n "extractRecentModuleActivity\|renderModuleActivity\|\[dispatch\]\|Module Execution Reports" drivers/pi/extensions/pals-hooks.ts</verify>
  <done>AC-1 satisfied in part: the extension has a bounded, dispatch-derived model for recent module participation instead of leaving module visibility entirely to manual narration.</done>
</task>

<task type="auto">
  <name>Task 2: Surface module participation in the existing Pi lifecycle UI and document the contract</name>
  <files>drivers/pi/extensions/pals-hooks.ts, drivers/pi/extensions/README.md, drivers/pi/skill-map.md</files>
  <action>
    Extend the existing lifecycle status/widget surfaces so recent module activity becomes visible without replacing the current artifact-first model.
    - Add a compact module-activity segment to the persistent lifecycle status when a recent dispatch-derived snapshot exists
    - Add one or more bounded widget lines that make the latest module stage legible (for example the latest stage plus module names/outcomes) while staying within the existing setStatus/setWidget patterns documented by Pi
    - Clear or hide the module-activity surface cleanly when no trustworthy recent signal is present so stale information does not linger
    - Update `drivers/pi/extensions/README.md` and `drivers/pi/skill-map.md` only as needed to explain that live module visibility is derived from shared workflow output plus shared artifacts, remains adapter-only, and does not create Pi-owned lifecycle truth
    - Keep the UI additive and lightweight; reuse the current status/widget surfaces instead of introducing a custom footer, overlay dashboard, or larger shortcut layer
    Avoid: broad discovery-surface rewrites, custom persistent module ledgers, or UX that suggests Pi can authoritatively prove execution independently of shared workflow/report artifacts.
  </action>
  <verify>grep -n "Modules:\|module activity\|dispatch-derived\|authoritative\|shared workflow" drivers/pi/extensions/pals-hooks.ts drivers/pi/extensions/README.md drivers/pi/skill-map.md</verify>
  <done>AC-1 and AC-2 satisfied: Pi's existing lifecycle UI surfaces recent module participation in a bounded way, and the docs describe that visibility as derived-only and artifact-safe.</done>
</task>

<task type="auto">
  <name>Task 3: Extend Pi validation for the live module-visibility contract</name>
  <files>tests/pi-end-to-end-validation.sh</files>
  <action>
    Expand `tests/pi-end-to-end-validation.sh` so the Phase 70 module-visibility UX is regression-protected.
    - Add checks that the extension contains a bounded parser/renderer path for canonical module dispatch markers and wires that path into the existing lifecycle status/widget surfaces
    - Add doc-contract checks that `drivers/pi/extensions/README.md` and `drivers/pi/skill-map.md` describe module visibility as derived from shared workflow/reporting signals rather than Pi-owned state
    - Preserve the prior guardrails from Phases 59, 63, 64, and 69: existing lifecycle UI, guided workflow behavior, bounded injection, and durable evidence-path checks must continue to pass
    - Prefer resilient contract checks over brittle copy-locked assertions, and keep the proof static/deterministic rather than attempting flaky live-agent execution
    - Run the suite and resolve any failures caused by the new assertions
    Avoid: validation that implies registry presence alone proves execution, or tests that only pass if the adapter starts storing hidden module state.
  </action>
  <verify>bash tests/pi-end-to-end-validation.sh</verify>
  <done>AC-3 satisfied: the Pi validation suite enforces the new live module-visibility contract and still passes with prior adapter/evidence guardrails intact.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- Shared `.paul/*` artifacts, shared kernel workflow semantics, or the durable Phase 69 reporting contract except for normal lifecycle artifact updates
- Module enablement policy, module priorities, `modules.yaml` generation, or install/uninstall behavior
- Non-Pi drivers or cross-harness UX surfaces
- The canonical `/skill:paul-*` ownership model or the bounded activation/injection architecture established in earlier Pi phases

## SCOPE LIMITS
- This plan adds bounded live module visibility to Pi's existing status/widget/doc/test surfaces only
- Derive module activity from current-session shared workflow output and shared artifacts; do not add telemetry stores, `appendEntry`-based module history, or Pi-owned lifecycle state
- Prefer compact additive visibility over new overlays, custom footers, or expanded shortcut inventories
- Defer broader proof/visibility product questions (richer telemetry, long-lived execution ledgers, module consolidation/recommendation logic) to Phase 71
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `npx -y esbuild drivers/pi/extensions/pals-hooks.ts --bundle --platform=node --format=cjs --outfile=/tmp/pals-hooks-p70.js --external:@mariozechner/pi-tui` exits 0
- [ ] `grep -n "\[dispatch\]\|Module Execution Reports\|module activity\|Modules:" drivers/pi/extensions/pals-hooks.ts` shows bounded module-visibility parsing/rendering
- [ ] `grep -n "module visibility\|shared workflow\|authoritative" drivers/pi/extensions/README.md drivers/pi/skill-map.md` confirms the docs describe the adapter layer accurately
- [ ] `bash tests/pi-end-to-end-validation.sh` passes
- [ ] `drivers/pi/extensions/pals-hooks.ts` does not introduce `appendEntry(` or another Pi-owned module-state persistence path
- [ ] All acceptance criteria are met
</verification>

<success_criteria>
- Users can tell which modules most recently fired from Pi's live lifecycle surfaces instead of inferring it only from freeform narration
- Live module visibility remains derived from shared workflow/reporting output and `.paul/STATE.md`, with no Pi-owned lifecycle truth
- Pi adapter docs and validation clearly encode the new visibility contract
- Phase 71 can focus on longer-term proof/visibility recommendations rather than patching a still-opaque live UX
</success_criteria>

<output>
After completion, create `.paul/phases/70-visibility-ux/70-01-SUMMARY.md`
</output>
