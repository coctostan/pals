---
phase: 190-artifact-structure-simplification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - kernel/references/git-strategy.md
  - kernel/references/module-dispatch.md
  - kernel/workflows/apply-phase.md
  - kernel/workflows/unify-phase.md
  - kernel/workflows/transition-phase.md
  - kernel/workflows/roadmap-management.md
  - kernel/workflows/pause-work.md
autonomous: true
---

<objective>
## Goal
Centralize the remaining duplicated workflow prose identified by Phase 189 recommendations R2, R3, and R4: GitHub Flow stanzas, module-dispatch priority/taxonomy wording, and unconditional APPLY-only reference loading.

## Purpose
Phase 190 already removed ROADMAP history from the hot path. The next largest avoidable cost is workflow prose that repeats shared mechanics inline. This plan mirrors the Phase 186 module-dispatch pattern: shared mechanics live in references, while workflows retain short call-site obligations, visible `[dispatch] ...` evidence requirements, GitHub Flow gates, and blocking semantics.

## Output
- `kernel/references/git-strategy.md` carries the shared GitHub Flow resolution, preflight, postflight, merge-gate, status/routing, pause/WIP, and milestone/tagging recipes.
- `kernel/references/module-dispatch.md` carries hook taxonomy and priority-list guidance so workflows do not duplicate registry mechanics.
- `kernel/workflows/apply-phase.md`, `kernel/workflows/unify-phase.md`, `kernel/workflows/transition-phase.md`, `kernel/workflows/roadmap-management.md`, and `kernel/workflows/pause-work.md` replace duplicated mechanics with concise call-site contracts.
- `apply-phase.md` marks `references/checkpoints.md` and `references/subagent-criteria.md` as conditional reads with explicit trigger rules.
</objective>

<context>
## Project Context
- `.paul/PROJECT.md` — v2.44 must reduce recurring read cost without weakening PLAN/APPLY/UNIFY, module evidence, GitHub Flow safety, archive authority, or parent-owned APPLY boundaries.
- `.paul/ROADMAP.md` — Phase 190 Plan 190-02 is the workflow centralization wave for R2 + R3 + R4; R5 Pi adapter split is deferred.
- `.paul/STATE.md` — Plan 190-01 is complete; the loop is ready for Plan 190-02.
- Planning mode: `direct-requirements`, collaboration `high`. The Phase 189 audit and Phase 190 roadmap are the requirement source; this plan stays within R2/R3/R4 only.

## Prior Work
- `.paul/phases/189-hot-path-read-audit/189-01-HOT-PATH-READ-AUDIT.md` — F2/R2 identify duplicated GitHub Flow stanzas; F7/R4 identify conditional APPLY references; F8/R3 identifies module-dispatch priority/taxonomy duplication.
- `.paul/phases/190-artifact-structure-simplification/190-01-SUMMARY.md` — Phase 190 Plan 190-01 completed R1 and recorded the archive-pattern precedent for reducing hot-path read cost while preserving authority.
- `kernel/references/module-dispatch.md` — Phase 186 precedent: centralize shared mechanics while preserving call-site obligations inline.

## Source Files
- `kernel/references/git-strategy.md`
- `kernel/references/module-dispatch.md`
- `kernel/workflows/apply-phase.md`
- `kernel/workflows/unify-phase.md`
- `kernel/workflows/transition-phase.md`
- `kernel/workflows/roadmap-management.md`
- `kernel/workflows/pause-work.md`
- `kernel/workflows/complete-milestone.md` (read-only candidate check: only modify if a duplicated GitHub Flow stanza is actually present; current scan suggests it likely needs no change)
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: ARCH PASS — planned files are existing kernel workflow/reference markdown files; this mirrors Phase 186 reference-centralization architecture; no source dependency boundary risk.

[dispatch] pre-plan advisory: ARIA, DANA, GABE, LUKE, OMAR, PETE, REED, VERA skipped — no UI, data schema, API endpoint, observability runtime, performance-sensitive runtime, resilience runtime, or privacy surface in scope.

[dispatch] pre-plan advisory: SETH PASS — grep found no hardcoded secret/auth patterns in target kernel markdown scope.

[dispatch] pre-plan advisory: TODD — no root test framework or `*.test.*`/`*.spec.*` files detected; canonical validation is `tests/pi-end-to-end-validation.sh` and `tests/cross-harness-validation.sh`.

[dispatch] pre-plan advisory: IRIS PASS — no TODO/FIXME/HACK/XXX markers in the target workflow/reference files planned for modification.

[dispatch] pre-plan advisory: DAVE — no CI config changes in scope; repository has no workflow YAML changes planned.

[dispatch] pre-plan advisory: DOCS PASS — this is internal workflow/reference documentation refactoring; no user-facing README/API docs update required.

[dispatch] CODI: skipped — no extractable symbols in phase scope; target files are markdown workflow/reference artifacts, not TS/JS symbols.

[dispatch] pre-plan advisory: RUBY — reduction target is duplicated prose in hot/warm workflow markdown; verify with line-count deltas and semantic marker checks rather than code complexity.

[dispatch] pre-plan enforcement: DEAN PASS/SKIP — no dependency manifest or lockfile is modified by this plan.

[dispatch] post-plan: TODD — no TDD conversion candidate for workflow/reference markdown centralization.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | PLAN/APPLY/UNIFY lifecycle work | ✓ loaded via `/skill:paul-plan` |
| /carl | required | Session boundary management | automatic Pi extension |
| TODD | required | APPLY gates and task verification | module dispatch |
| WALT | required | APPLY/UNIFY quality validation | module dispatch |

**BLOCKING:** Required skills MUST be loaded before APPLY proceeds.
</skills>

<acceptance_criteria>

## AC-1: GitHub Flow mechanics centralized without weakening gates
```gherkin
Given the existing GitHub Flow behavior in workflow files
When duplicate resolution, preflight, postflight, status/routing, pause/WIP, or merge-gate recipe prose is compacted
Then `kernel/references/git-strategy.md` contains the shared recipe and every affected workflow keeps an inline call-site contract that says what it must check, what blocks, and what evidence to display
```

## AC-2: Module dispatch taxonomy is reference-owned while evidence stays inline
```gherkin
Given `apply-phase.md` and `unify-phase.md` currently restate hook priority/taxonomy details
When module-dispatch prose is centralized
Then `kernel/references/module-dispatch.md` owns the shared hook taxonomy/priority guidance and workflows still retain `[dispatch] ...` evidence requirements plus SKIPPED/modules.yaml warning behavior at each call site
```

## AC-3: APPLY conditional references are self-describing
```gherkin
Given APPLY may have no checkpoints and may not consider delegation
When the APPLY workflow references optional material
Then `references/checkpoints.md` is load-only-if the approved plan contains `checkpoint:*` tasks, and `references/subagent-criteria.md` is load-only-if delegation is considered for an eligible auto task, with both triggers encoded inline
```

## AC-4: Hot/warm workflow read cost decreases while validation remains green
```gherkin
Given the pre-plan line counts for the planned workflow/reference files are recorded in this plan
When APPLY completes the centralization work
Then the total line count of modified workflow files decreases or equivalent duplicate-prose reduction is documented, required semantic guardrail markers remain present, and both canonical validation suites pass
```
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Centralize GitHub Flow shared mechanics</name>
  <files>kernel/references/git-strategy.md, kernel/workflows/apply-phase.md, kernel/workflows/unify-phase.md, kernel/workflows/transition-phase.md, kernel/workflows/roadmap-management.md, kernel/workflows/pause-work.md</files>
  <action>
    Move duplicated GitHub Flow mechanics into `kernel/references/git-strategy.md` and compact workflow steps into call-site contracts.
    - In `git-strategy.md`, add or reorganize concise sections for: 3-tier workflow resolution, APPLY preflight responsibilities, APPLY postflight responsibilities, UNIFY merge-gate responsibilities, status/resume/roadmap routing state, pause/WIP continuity, and phase/milestone transition/tagging behavior.
    - In `apply-phase.md`, replace long inline preflight/postflight bash blocks with short obligations: resolve workflow mode, create/use feature branch, update base, rebase if behind, require clean tree handling, push/create PR/surface CI, and emit preflight/postflight summaries. Reference `git-strategy.md` for the exact bash recipe.
    - In `unify-phase.md`, replace long inline merge-gate recipe blocks with a compact ordered gate contract: resolve config, ensure PR exists, CI passes, reviews/code-review requirements are handled, PR is merged, base is synced, branch cleanup/state update is recorded. Reference `git-strategy.md` for commands.
    - In `transition-phase.md`, `roadmap-management.md`, and `pause-work.md`, replace repeated 3-tier resolution and git-state recipes with concise call-site obligations plus `git-strategy.md` pointer.
    - Inspect `complete-milestone.md`; only modify it if an actual duplicated GitHub Flow stanza is found. If unchanged, record in APPLY/UNIFY that it was checked and did not need R2 edits.
    - Preserve behavior: no weakening of CI failure blocks, PR requirements, review requirements, base sync, branch cleanup, or git-aware routing.
  </action>
  <verify>grep -R "references/git-strategy.md" kernel/workflows/apply-phase.md kernel/workflows/unify-phase.md kernel/workflows/transition-phase.md kernel/workflows/roadmap-management.md kernel/workflows/pause-work.md && grep -q "GitHub Flow" kernel/references/git-strategy.md && grep -q "CI failure is blocking" kernel/workflows/unify-phase.md</verify>
  <done>AC-1 satisfied: shared recipe is reference-owned and each affected workflow keeps explicit gate/check obligations.</done>
</task>

<task type="auto">
  <name>Task 2: Hoist module-dispatch taxonomy out of workflows</name>
  <files>kernel/references/module-dispatch.md, kernel/workflows/apply-phase.md, kernel/workflows/unify-phase.md</files>
  <action>
    Compact module-dispatch call sites without removing visible dispatch evidence requirements.
    - Extend `kernel/references/module-dispatch.md` with shared lifecycle hook taxonomy: advisory vs enforcement classification, hook ordering, priority source, required evidence-line convention, stale/missing registry warning convention, and durable evidence destinations.
    - In `apply-phase.md`, reduce `pre_apply_hooks`, `post-task`, `advisory_module_dispatch`, and `enforcement_module_dispatch` prose so they list only hook name, local context/output, blocking behavior, and exact `[dispatch] ...` evidence/warning line expectations.
    - In `unify-phase.md`, reduce `pre_unify_hooks` and `post_unify_hooks` prose the same way.
    - Do not remove mandatory STOP/DO NOT SKIP emphasis where the workflow depends on it.
    - Do not collapse `module-dispatch.md` back into workflow files; the reference remains the shared mechanics source.
  </action>
  <verify>grep -q "Workflow Call-Site Contract" kernel/references/module-dispatch.md && grep -q "\[dispatch\] post-apply advisory" kernel/workflows/apply-phase.md && grep -q "\[dispatch\] post-unify" kernel/workflows/unify-phase.md && grep -q "modules.yaml NOT FOUND" kernel/workflows/apply-phase.md kernel/workflows/unify-phase.md</verify>
  <done>AC-2 satisfied: shared hook taxonomy lives in the reference and workflow dispatch evidence remains inline.</done>
</task>

<task type="auto">
  <name>Task 3: Encode APPLY conditional reference triggers and validate guardrails</name>
  <files>kernel/workflows/apply-phase.md, kernel/references/git-strategy.md, kernel/references/module-dispatch.md</files>
  <action>
    Make APPLY optional reference loading explicit and run full validation.
    - In the `<references>` block of `apply-phase.md`, keep `references/loop-phases.md` and `references/module-dispatch.md` as normal references.
    - Mark `references/checkpoints.md` as load-only-if the approved PLAN contains any `checkpoint:*` task.
    - Mark `references/subagent-criteria.md` as load-only-if APPLY actually considers delegating an eligible auto task to `pals-implementer`.
    - Keep the Phase 155 parent-owned APPLY contract inline enough that delegation cannot hide lifecycle ownership: parent owns `.paul/*`, official verification, module gates, fallback judgment, and state/report writes.
    - Run line-count and semantic guardrail checks before final validation; document any expected line-count exception in SUMMARY if exact net reduction is smaller than expected but duplicate prose is still centralized.
  </action>
  <verify>grep -q "checkpoint" kernel/workflows/apply-phase.md && grep -q "load-only-if" kernel/workflows/apply-phase.md && grep -q "parent" kernel/workflows/apply-phase.md && bash tests/pi-end-to-end-validation.sh && bash tests/cross-harness-validation.sh</verify>
  <done>AC-3 and AC-4 satisfied: optional references are conditional, parent-owned delegation guardrails remain visible, and canonical validation passes.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Do not change runtime behavior, workflow semantics, module dispatch contracts, GitHub Flow gates, or Pi context injection logic.
- Do not remove `[dispatch] ...` evidence requirements, `modules.yaml NOT FOUND` warnings, SKIPPED warning behavior, or mandatory post-apply/post-unify dispatch stops.
- Do not weaken GitHub Flow branch/PR/CI/review/merge/base-sync/cleanup gates.
- Do not edit `drivers/pi/extensions/pals-hooks.ts` or split the Pi adapter in this plan; R5 is deferred.
- Do not edit Claude Code command wrappers or expand Claude-specific support surface.
- Do not delete or rewrite archive history under `.paul/archive/*`.
- Do not add dependencies or dependency manifests.

## SCOPE LIMITS
- This plan is workflow/reference markdown centralization only.
- No product decision on Claude Code support tier is included.
- No anti-regrowth test update is included; Phase 191 owns validation budget updates.
- If APPLY discovers a GitHub Flow or module-dispatch behavior ambiguity that would require semantics changes, stop and re-plan instead of silently changing behavior.
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `grep -R "references/git-strategy.md" kernel/workflows/apply-phase.md kernel/workflows/unify-phase.md kernel/workflows/transition-phase.md kernel/workflows/roadmap-management.md kernel/workflows/pause-work.md`
- [ ] `grep -q "Workflow Call-Site Contract" kernel/references/module-dispatch.md`
- [ ] `grep -q "\[dispatch\] post-apply advisory" kernel/workflows/apply-phase.md`
- [ ] `grep -q "\[dispatch\] post-unify" kernel/workflows/unify-phase.md`
- [ ] `grep -q "load-only-if" kernel/workflows/apply-phase.md`
- [ ] `grep -q "CI failure is blocking" kernel/workflows/unify-phase.md`
- [ ] `bash tests/pi-end-to-end-validation.sh`
- [ ] `bash tests/cross-harness-validation.sh`
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- Shared GitHub Flow recipes are discoverable in `git-strategy.md`; workflow files retain only local obligations and blocking semantics.
- Shared module-dispatch taxonomy is discoverable in `module-dispatch.md`; workflow files retain visible evidence and warning requirements.
- APPLY no longer implies unconditional checkpoint/subagent reference reads.
- Hot/warm workflow line counts are reduced or a measured duplicate-prose reduction/deviation is documented.
- Pi 177/177 and cross-harness 90/90 validation remain green.
</success_criteria>

<output>
After completion, create `.paul/phases/190-artifact-structure-simplification/190-02-SUMMARY.md` documenting changed files, line-count deltas, validation results, module reports, checked-but-unchanged files, and any deviations.
</output>
