---
phase: 271-unify-workflow-instruction-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kernel/workflows/unify-phase.md
  - tests/pi-end-to-end-validation.sh
  - tests/cross-harness-validation.sh
autonomous: false
---

<objective>
## Goal
Rewrite the unify workflow so reconciliation closes the PLAN/APPLY/UNIFY loop with bounded evidence loading, concise durable summaries, correct source-of-truth boundaries, and semantic validation that cannot pass on marker prose alone.

## Purpose
Phase 271 applies the v2.63 instruction-audit pattern to `unify-phase.md`. The current workflow preserves important closure gates, module evidence persistence, GitHub Flow merge safety, and transition routing, but it still contains broad STATE/PLAN/diff reading instructions, duplicated summary/state/roadmap narrative paths, large merge-gate templates, and validation guardrails that can be strengthened to prove operational behavior rather than marker retention.

## Output
- `kernel/workflows/unify-phase.md` rewritten for bounded UNIFY state/plan loading, compact reconciliation evidence, concise SUMMARY finalization, post-unify module persistence, GitHub Flow merge gate routing, and phase transition handoff.
- Semantic guardrails in `tests/pi-end-to-end-validation.sh` and `tests/cross-harness-validation.sh` requiring bounded reconciliation behavior and rejecting marker-only UNIFY compliance.
- Phase 271 summary after APPLY at `.paul/phases/271-unify-workflow-instruction-audit/271-01-SUMMARY.md`.
</objective>

<context>
## Project Context
- `.paul/STATE.md` — Phase 271 ready to plan; PLAN/APPLY/UNIFY loop is open.
- `.paul/ROADMAP.md` — Phase 271 scope: review `kernel/workflows/unify-phase.md` for reconciliation and summary behavior that duplicates narrative across artifacts, over-expands module evidence, weakens source-of-truth boundaries, or lets closure pass with prose markers instead of semantic evidence.
- `.paul/phases/270-apply-workflow-instruction-audit/270-01-SUMMARY.md` — reusable pattern: semantic guardrails first, then collaborative section-by-section workflow rewrite with explicit user approval before mutation.

## Source Files
- `kernel/workflows/unify-phase.md` — target workflow. Current audit concerns include broad `Read STATE.md` / PLAN / diff instructions, eager full reconciliation templates, possible narrative duplication across SUMMARY/STATE/ROADMAP, large merge-gate blocks, and marker-oriented durable evidence checks.
- `tests/pi-end-to-end-validation.sh` — Pi/install validation currently checks durable summary evidence path markers; add semantic UNIFY workflow assertions.
- `tests/cross-harness-validation.sh` — shared/kernel validation currently checks durable summary path markers; add matching semantic UNIFY workflow assertions.
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: modules.yaml loaded from installed registry (`kernel_version: 2.0.0`).

- ARCH: Planned files are in expected repo layers: `kernel/workflows/` for lifecycle workflow text and `tests/` for shell validation. Current line counts: `kernel/workflows/unify-phase.md` 529, `tests/pi-end-to-end-validation.sh` 2394, `tests/cross-harness-validation.sh` 1613. `unify-phase.md` is over the 500-line hot-workflow/god-file threshold; compaction is a direct plan goal. Validation scripts are large existing suites; keep additions localized.
- TODD: No `*.test.*`, `*.spec.*`, or `__tests__` tree detected. Existing shell TAP validation files are in scope; Task 1 must add semantic guardrails before accepting the workflow rewrite.
- IRIS: No TODO/FIXME/HACK/XXX markers found in planned files. Review flags: broad reconciliation reads, duplicate narrative persistence, large merge-gate templates, and marker-only durable-evidence validation.
- DAVE: No `.github/workflows`, Dockerfile, or docker-compose config found. No deploy/runtime config changes planned; local verification remains `git diff --check`, Pi validation, and cross-harness validation.
- DOCS: Planned files are lifecycle workflow and validation scripts. No README/API docs required; lifecycle truth will be recorded through PLAN/SUMMARY/STATE/ROADMAP.
- CODI: skipped — no extractable symbols in phase scope (markdown/shell validation work; no TS/JS selectors).
- RUBY: Debt flags are instruction-level and size-related: `unify-phase.md` is 529 lines with repeated templates and broad gate instructions that should move toward compact operational rules.

[dispatch] pre-plan enforcement: DEAN PASS — no root package manager/lockfile found and no dependency manifests are modified by this plan; no dependency audit block applies.

## Post-Plan Module Dispatch
[dispatch] post-plan: TODD reviewed plan shape; no TDD conversion. Guardrail-first validation is represented in Task 1 and must run before finalizing APPLY.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | PLAN/APPLY/UNIFY lifecycle loop | ✓ |
| /carl | required | Session boundary management if context pressure rises | automatic |
| TODD | required | Validation guardrail design before/during APPLY | ✓ via module dispatch |
| WALT | required | Quality validation before completion | ✓ via validation commands |

**BLOCKING:** APPLY must remain under PAUL parent authority. Do not use repo-local `pals-implementer` for the core workflow rewrite unless the parent first approves a narrow helper packet; this phase is instruction-sensitive and interactive.
</skills>

<acceptance_criteria>
## AC-1: UNIFY starts from bounded state, plan, and execution evidence
```gherkin
Given `kernel/workflows/unify-phase.md` is used for `/paul:unify`
When UNIFY determines whether normal or retroactive reconciliation applies
Then it loads only the required STATE loop/session fields, approved PLAN sections, execution annotations, and targeted changed-file evidence needed to reconcile the loop.
```

## AC-2: Summary and lifecycle writes avoid duplicate narrative while preserving authority
```gherkin
Given APPLY is complete and UNIFY creates or finalizes a SUMMARY
When UNIFY records results across SUMMARY, STATE, ROADMAP, and module histories
Then SUMMARY remains the durable reconciliation artifact, STATE/ROADMAP receive compact routing/status updates, and narrative duplication is avoided unless a lifecycle invariant requires it.
```

## AC-3: Module evidence persistence is compact and mandatory
```gherkin
Given pre-unify or post-unify hooks are registered or skipped
When UNIFY dispatches and finalizes module evidence
Then it preserves visible `[dispatch] ...` evidence, records side effects in `Module Execution Reports`, warns on missing dispatch evidence, and avoids over-expanded module transcripts in hot artifacts.
```

## AC-4: Merge gate and transition routing stay safe without verbose ceremony
```gherkin
Given github-flow merge gating or phase transition applies
When UNIFY reaches PR/CI/review/merge/sync/transition routing
Then it follows mandatory gate order, blocks on unsafe states, records compact evidence, and does not skip transition routing after the last plan.
```

## AC-5: Validation catches semantic UNIFY regressions, not marker prose
```gherkin
Given a future change preserves strings like `finalize_summary`, `module_reports`, or `Module Execution Reports`
When the workflow reintroduces broad/eager STATE/PLAN/diff reads, duplicate narrative writes, optional post-unify dispatch, marker-only module evidence, or transition skipping
Then Pi and cross-harness validation fail with UNIFY-specific messages.
```

## AC-6: Rewrite is collaborative, bounded, and green
```gherkin
Given Phase 271 is limited to unify workflow instruction repair and guardrails
When APPLY edits `unify-phase.md`
Then the agent presents one section at a time for approval, edits only approved wording, keeps changes limited to planned files plus routine PAUL lifecycle artifacts, and finishes with `git diff --check`, Pi validation, and cross-harness validation passing.
```
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add semantic unify-workflow guardrails before accepting the rewrite</name>
  <files>tests/pi-end-to-end-validation.sh, tests/cross-harness-validation.sh</files>
  <action>
    Add localized TAP assertions that fail when `unify-phase.md` keeps marker prose but regresses operational UNIFY behavior.

    Required guardrails:
    - Check repo source workflow and installed/shared copies where each validation suite already has those paths available.
    - Require bounded UNIFY entry behavior: named steps or explicit wording for bounded STATE/session/PLAN/execution-evidence loading rather than broad eager reads.
    - Require SUMMARY-first reconciliation semantics: SUMMARY carries detailed planned-vs-actual evidence; STATE and ROADMAP receive compact routing/status updates.
    - Require mandatory post-unify dispatch and summary finalization evidence, including warnings when dispatch evidence is missing.
    - Require GitHub Flow merge gate and phase transition routing to remain mandatory and ordered when applicable.
    - Reject broad/eager routine instructions, such as standalone `Read STATE.md`, full PLAN reads without section limits, broad `git diff --stat`/full diff gathering where targeted changed-file evidence is sufficient, and marker-only durable-evidence checks.
    - Keep additions local to existing durable module evidence / context-diet / workflow guardrail areas; do not restructure full validation scripts.
  </action>
  <verify>`bash tests/pi-end-to-end-validation.sh` and `bash tests/cross-harness-validation.sh` fail before the workflow rewrite or the new assertions demonstrably target existing broad/verbose unify-workflow text; after Task 2 they pass.</verify>
  <done>AC-1, AC-2, AC-3, AC-4, and AC-5 satisfied: validation rejects broad/eager UNIFY instructions and cannot pass on marker prose alone.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Confirm the section-by-section rewrite protocol before editing `unify-phase.md`.</decision>
  <context>
    UNIFY must not perform a bulk rewrite. Before changing workflow text, APPLY must present a compact audit of the current instruction failures and propose a section order. Then it must work one section at a time.

    Required collaboration loop for each section:
    1. Show the section name and smallest useful current excerpt.
    2. State the concrete problem: over-read, duplicated narrative, over-expanded module evidence, source-of-truth ambiguity, merge-gate verbosity, transition risk, validation false positive, or unnecessary ceremony.
    3. Propose replacement wording or a focused diff.
    4. Ask for user approval or wording changes.
    5. Edit only the approved section.
    6. Re-read that section before moving on.

    Expected section order:
    1. Purpose / required reading / hot-artifact loading.
    2. Retroactive vs normal UNIFY entry and bounded evidence loading.
    3. Plan-vs-actual reconciliation, pre-unify hooks, and skill audit.
    4. SUMMARY drafting/finalization and STATE/ROADMAP update boundaries.
    5. Post-unify dispatch and module evidence persistence.
    6. GitHub Flow merge gate and PR/CI/review/merge/sync routing.
    7. Phase completion, transition routing, output, and error handling.
  </context>
  <options>
    <option id="approve-section-loop">
      <name>Approve section-by-section collaborative rewrite</name>
      <pros>Preserves wording control and avoids a marker-only/bulk rewrite.</pros>
      <cons>Requires more APPLY turns and pauses between workflow sections.</cons>
    </option>
    <option id="adjust-section-loop">
      <name>Adjust section order or collaboration protocol</name>
      <pros>Allows the user to change how the rewrite proceeds before mutation.</pros>
      <cons>Pauses execution and may require plan/task wording updates.</cons>
    </option>
  </options>
  <resume-signal>Reply `approve-section-loop` to continue, or describe the protocol/section-order adjustment.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Collaboratively rewrite unify workflow section by section</name>
  <files>kernel/workflows/unify-phase.md</files>
  <action>
    Rewrite the workflow instructions, not just marker text, using the approved section-by-section collaboration loop from the checkpoint. Do not edit ahead of the user's approved section.

    Required edits across approved sections:
    - Preserve PLAN/APPLY/UNIFY closure authority, SUMMARY creation, module dispatch evidence, GitHub Flow merge safety, and mandatory phase transition routing.
    - Replace broad STATE/PLAN/diff reads with bounded STATE loop/session fields, section-limited PLAN extraction, APPLY annotation use, and targeted changed-file evidence.
    - Make SUMMARY the detailed reconciliation artifact while keeping STATE/ROADMAP updates compact and routing-focused.
    - Keep pre/post-unify module dispatch mandatory and visible, but record compact reports and side effects instead of large transcripts in hot artifacts.
    - Compress merge-gate templates while preserving PR existence, CI, review, merge, sync, branch cleanup, and blocking semantics.
    - Shorten output/error templates while preserving blockers for missing APPLY completion, missing PLAN, ambiguous execution evidence, and transition requirements.
    - Avoid harness-specific installed paths and shell-only examples where workflow instructions are clearer as platform-agnostic steps.
  </action>
  <verify>After each approved section edit, re-read only that section. At the end, semantic guardrails from Task 1 pass and manual inspection confirms broad/eager routine reads, duplicate narrative instructions, optional post-unify dispatch, and transition-skipping paths are gone from the normal UNIFY path.</verify>
  <done>AC-1 through AC-6 satisfied: UNIFY remains safe and authoritative while becoming bounded and concise.</done>
</task>

<task type="auto">
  <name>Task 3: Install/validate and record Phase 271 evidence</name>
  <files>kernel/workflows/unify-phase.md, tests/pi-end-to-end-validation.sh, tests/cross-harness-validation.sh</files>
  <action>
    Run the final verification set and prepare evidence for UNIFY.

    Required checks:
    - `git diff --check`
    - `bash tests/pi-end-to-end-validation.sh`
    - `bash tests/cross-harness-validation.sh`
    - Inspect changed-file scope and confirm only planned files plus routine `.paul/*` lifecycle artifacts changed.

    If validation fails because installed copies do not reflect source changes, follow the existing installer/validation flow already used by the tests rather than editing installed runtime files directly.
  </action>
  <verify>All required commands pass and changed-file scope is bounded.</verify>
  <done>AC-5 and AC-6 satisfied: semantic validation is green and evidence is ready for `271-01-SUMMARY.md`.</done>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- Do not edit installed runtime copies under `~/.pi`, `~/.claude`, or other home-directory install targets directly.
- Do not modify `resume-project.md`, `plan-phase.md`, `apply-phase.md`, or `pause-work.md`; they have dedicated completed or future phases.
- Do not change module manifests, extension TypeScript, driver installers, project dependencies, or GitHub Flow configuration.
- Do not rewrite the full validation suites; keep tests localized to unify workflow semantics.

## SCOPE LIMITS
- This phase fixes `unify-phase.md` only.
- No broad lifecycle artifact compaction, historical archive rewrite, or single-source-of-truth architecture redesign beyond instruction wording needed for UNIFY.
- No redesign of module dispatch contracts outside the unify workflow text and validation guardrails.
- No helper delegation unless explicitly approved after the checkpoint; parent APPLY remains authoritative.
</boundaries>

<verification>
Before declaring plan complete:
- [ ] Semantic unify-workflow guardrails added to Pi validation and cross-harness validation.
- [ ] `kernel/workflows/unify-phase.md` no longer contains broad/eager routine UNIFY read instructions.
- [ ] SUMMARY-first reconciliation, compact STATE/ROADMAP routing updates, mandatory module dispatch, GitHub Flow merge gates, and transition routing remain explicit.
- [ ] Module evidence and merge-gate instructions are materially shorter and less duplicative.
- [ ] `git diff --check` passes.
- [ ] `bash tests/pi-end-to-end-validation.sh` passes.
- [ ] `bash tests/cross-harness-validation.sh` passes.
- [ ] Changed-file scope is limited to planned files plus routine PAUL lifecycle artifacts.
</verification>

<success_criteria>
- Unify workflow instructions are operationally context-efficient, not merely marker-compliant.
- SUMMARY/STATE/ROADMAP authority boundaries are clearer and less duplicative.
- Validation fails on broad UNIFY reads, optional or missing module dispatch, duplicate narrative persistence, transition skipping, or marker-only durable-evidence compliance.
- Phase 271 remains limited to unify workflow and semantic guardrails.
</success_criteria>

<output>
After completion, create `.paul/phases/271-unify-workflow-instruction-audit/271-01-SUMMARY.md` with:
- Audit findings from the original unify workflow.
- Exact semantic guardrails added.
- Section-by-section rewrite decisions.
- Final verification results.
- Any deferred issues for Phase 272.
</output>
