---
phase: 67-onboarding-prd-depth
plan: 02
type: execute
wave: 1
depends_on: ["67-01"]
files_modified:
  - kernel/workflows/init-project.md
  - kernel/references/brownfield-onboarding.md
  - kernel/templates/PROJECT.md
  - kernel/templates/PRD.md
  - kernel/commands/paul/init.md
  - drivers/pi/skills/paul-init/SKILL.md
  - kernel/workflows/plan-phase.md
  - kernel/workflows/discuss-phase.md
  - kernel/workflows/discuss-milestone.md
  - kernel/workflows/phase-assumptions.md
  - kernel/commands/paul/plan.md
  - kernel/commands/paul/discuss.md
  - kernel/commands/paul/discuss-milestone.md
  - kernel/commands/paul/assumptions.md
  - kernel/commands/paul/research.md
  - kernel/commands/paul/research-phase.md
  - drivers/pi/skills/paul-plan/SKILL.md
  - drivers/pi/skills/paul-discuss/SKILL.md
  - docs/ARTIFACT-SPEC.md
  - docs/ADAPTER-CONTRACT.md
  - docs/KERNEL-MANIFEST.md
  - tests/cross-harness-validation.sh
autonomous: true
---

<objective>
## Goal
Implement the first execution slice of Phase 67 by turning the approved layered artifact recommendation into shared workflow, template, command, skill, and validation behavior: `PROJECT.md` becomes the compact hot-path brief, `PRD.md` becomes the selectively-read deeper product-definition artifact, and onboarding/planning surfaces use the two-layer model consistently.

## Purpose
Phase 67 research is complete and the core design question is settled. This plan converts that decision into portable PALS behavior so onboarding captures real product definition, brownfield and greenfield flows converge on one durable schema, and planning/discovery workflows can read deeper product context without bloating the hot path.

## Output
- Updated onboarding workflow and brownfield guidance that create and populate layered durable artifacts
- Updated `PROJECT.md` template plus new `PRD.md` template
- Updated planning/discussion/assumptions/research surfaces that read `PRD.md` selectively
- Updated shared artifact/adapter docs and validation to codify the new portable contract
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md
.paul/SPECIAL-FLOWS.md

## Prior Work (only if genuinely needed)
- `.paul/phases/67-onboarding-prd-depth/67-01-SUMMARY.md` — reconciled the research-first plan and recorded the layered `PROJECT.md` + `PRD.md` recommendation
- `.paul/phases/67-onboarding-prd-depth/recommended-artifact-model.md` — source of truth for the approved content model, read model, and file model
- `.paul/phases/66-collaborative-planning-model/66-01-SUMMARY.md` — collaboration level, planning mode, and review-menu behavior that must be preserved while the artifact model deepens
- `docs/ARTIFACT-SPEC.md` — current normative artifact contract that must be extended without breaking portability

## Source Files
- kernel/workflows/init-project.md
- kernel/references/brownfield-onboarding.md
- kernel/templates/PROJECT.md
- kernel/workflows/plan-phase.md
- kernel/workflows/discuss-phase.md
- kernel/workflows/discuss-milestone.md
- kernel/workflows/phase-assumptions.md
- kernel/commands/paul/init.md
- kernel/commands/paul/plan.md
- kernel/commands/paul/discuss.md
- kernel/commands/paul/discuss-milestone.md
- kernel/commands/paul/assumptions.md
- kernel/commands/paul/research.md
- kernel/commands/paul/research-phase.md
- drivers/pi/skills/paul-init/SKILL.md
- drivers/pi/skills/paul-plan/SKILL.md
- drivers/pi/skills/paul-discuss/SKILL.md
- docs/ARTIFACT-SPEC.md
- docs/ADAPTER-CONTRACT.md
- docs/KERNEL-MANIFEST.md
- tests/cross-harness-validation.sh

## Notes
- Preserve `.paul/*` as the only lifecycle truth; do not introduce Pi-only or harness-specific artifact semantics
- Keep `PROJECT.md` intentionally concise and treat `PRD.md` as selective depth, not a second competing truth model
- Brownfield onboarding remains evidence-first, but it now needs an explicit bridge from current-system facts to future product intent
- Migration for existing repos must stay additive/backward-safe; do not require destructive splitting of legacy `PROJECT.md` files just to pass validation
- Preserve Phase 66 behavior: collaboration defaults, per-run override, exploratory/direct-requirements mode, and the 4-option review flow
</context>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | For the PLAN → APPLY → UNIFY lifecycle work in this implementation plan | ○ |

**BLOCKING:** Required skills MUST be loaded before APPLY proceeds.
Run each skill command or confirm already loaded.

## Skill Invocation Checklist
- [ ] /paul loaded (run command or confirm)
</skills>

<acceptance_criteria>
## AC-1: Onboarding creates layered durable artifacts from distinct product-definition inputs
```gherkin
Given greenfield or brownfield onboarding with the Phase 67 layered model approved
When init-related workflows, templates, and guidance are updated
Then onboarding captures distinct product-definition concepts instead of overlapping shallow prompts, writes a compact `PROJECT.md` plus deeper `PRD.md`, and uses brownfield evidence to enrich current-state and dependency context without forking the durable schema
```

## AC-2: Planning and discovery surfaces treat `PRD.md` as selective depth, not hot-path default context
```gherkin
Given the new layered durable model
When planning, discussion, assumptions, and research surfaces need deeper product context
Then they explicitly read `PRD.md` where user/problem framing, deferred scope, assumptions, or open questions matter, while keeping `PROJECT.md` as the default hot-path brief and avoiding kitchen-sink reads in orientation-only workflows
```

## AC-3: Shared docs and validation codify the new artifact contract without portability regression
```gherkin
Given shared markdown artifacts are the portable runtime for PALS
When artifact specs, adapter/template docs, and validation are updated
Then the `PROJECT.md` + `PRD.md` contract is documented, new template inventory is reflected, legacy migration remains additive, and shared Pi/cross-harness validation still passes
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Implement layered onboarding artifact creation and brownfield bridging</name>
  <files>kernel/workflows/init-project.md, kernel/references/brownfield-onboarding.md, kernel/templates/PROJECT.md, kernel/templates/PRD.md, kernel/commands/paul/init.md, drivers/pi/skills/paul-init/SKILL.md</files>
  <action>
    Rework onboarding so the conversation no longer stops at the overlapping “core value” / “what are you building?” pair.
    Update init to gather distinct product-definition inputs appropriate to the collaboration level and planning mode:
    - identity / framing
    - target users and needs
    - problem / opportunity and why now
    - must-have vs deferred vs out-of-scope distinctions
    - constraints, assumptions, open questions, and success signals

    Keep greenfield and brownfield on the same durable schema, but let brownfield onboarding explicitly pull map-codebase findings into current-state, dependency, and constraint fields while still asking about future product direction.

    Update `kernel/templates/PROJECT.md` so it is clearly the compact hot-path landing brief.
    Add `kernel/templates/PRD.md` for deeper product-definition depth.
    Ensure init guidance, command docs, and Pi skill docs all describe the new artifact pair consistently.

    Avoid:
    - changing module-selection, git-configuration, or collaboration-default behavior beyond what the new intake needs
    - stuffing deep rationale back into `PROJECT.md`
    - making brownfield onboarding just a codebase dump without product intent
  </action>
  <verify>grep -q 'PRD.md' kernel/workflows/init-project.md && grep -q '## Scope Snapshot' kernel/templates/PROJECT.md && grep -q '## Problem / Opportunity' kernel/templates/PRD.md && grep -q 'PRD.md' kernel/commands/paul/init.md && grep -q 'PRD.md' drivers/pi/skills/paul-init/SKILL.md</verify>
  <done>AC-1 satisfied: onboarding and artifact templates now create a layered `PROJECT.md` + `PRD.md` model from distinct product-definition inputs</done>
</task>

<task type="auto">
  <name>Task 2: Update planning and discovery surfaces for selective `PRD.md` reads</name>
  <files>kernel/workflows/plan-phase.md, kernel/workflows/discuss-phase.md, kernel/workflows/discuss-milestone.md, kernel/workflows/phase-assumptions.md, kernel/commands/paul/plan.md, kernel/commands/paul/discuss.md, kernel/commands/paul/discuss-milestone.md, kernel/commands/paul/assumptions.md, kernel/commands/paul/research.md, kernel/commands/paul/research-phase.md, drivers/pi/skills/paul-plan/SKILL.md, drivers/pi/skills/paul-discuss/SKILL.md</files>
  <action>
    Teach shared planning/discovery surfaces to treat `PROJECT.md` as the first-stop brief and `PRD.md` as selective deeper context.

    Update the relevant workflows and command/skill guidance so they:
    - keep reading `PROJECT.md` for the hot-path project brief
    - add `PRD.md` only where deeper product framing, assumptions, deferred scope, or open questions materially help the workflow
    - preserve the Phase 66 collaboration model and 4-option review flow exactly
    - make it explicit that not every workflow should load both artifacts

    Cover at least phase planning, phase discussion, milestone discussion, assumptions review, and research surfaces that currently rely only on `PROJECT.md` for deeper context.

    Avoid:
    - adding `PRD.md` to resume/help/status/pause orientation flows
    - duplicating the full PRD back into `PROJECT.md`
    - using vague wording like “read the project files” instead of naming when `PRD.md` is actually needed
  </action>
  <verify>grep -q '.paul/PRD.md' kernel/workflows/plan-phase.md && grep -q '.paul/PRD.md' kernel/workflows/discuss-phase.md && grep -q '.paul/PRD.md' kernel/workflows/phase-assumptions.md && grep -q '.paul/PRD.md' kernel/commands/paul/research-phase.md && ! grep -q 'PRD.md' kernel/workflows/resume-project.md</verify>
  <done>AC-2 satisfied: planning/discovery surfaces use `PRD.md` selectively while `PROJECT.md` remains the hot-path brief</done>
</task>

<task type="auto">
  <name>Task 3: Codify the layered artifact contract, migration posture, and validation</name>
  <files>docs/ARTIFACT-SPEC.md, docs/ADAPTER-CONTRACT.md, docs/KERNEL-MANIFEST.md, tests/cross-harness-validation.sh</files>
  <action>
    Extend the shared artifact and adapter contract to include `PRD.md` as a first-class durable artifact.
    Update the docs so they clearly define:
    - `PROJECT.md` responsibilities as the compact landing brief
    - `PRD.md` responsibilities as deeper product-definition context
    - which workflows/adapters should treat the new artifact as readable shared state
    - additive migration expectations for legacy repos that do not yet have `PRD.md`

    Update cross-harness validation so it checks the new shared artifact semantics and template inventory without forcing destructive migration of existing repos.
    The validation should prove the layered model is portable and that the repo still passes its shared validation suites after the docs/workflow/template changes.

    Avoid:
    - declaring a harness-specific exception path
    - making `PRD.md` a Pi-only or adapter-only concept
    - tightening validation in a way that instantly breaks legacy repos with no migration path
  </action>
  <verify>grep -q 'PRD.md' docs/ARTIFACT-SPEC.md && grep -q 'PRD.md' docs/ADAPTER-CONTRACT.md && grep -q 'templates/PRD.md' docs/KERNEL-MANIFEST.md && grep -q 'PRD.md' tests/cross-harness-validation.sh</verify>
  <done>AC-3 satisfied: the layered artifact model is documented and validated as shared portable behavior</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- `drivers/pi/extensions/pals-hooks.ts` or other Pi runtime code unless the shared markdown/template changes prove a runtime update is truly required
- install/uninstall scripts or driver manifests in this plan
- live project artifacts in `.paul/` except normal PLAN/STATE/ROADMAP lifecycle updates for creating this plan
- unrelated tracked working tree changes outside the files listed in this plan

## SCOPE LIMITS
- This plan implements shared workflow/template/command/skill/doc/test changes only; it does not redesign the lifecycle model or add a new source-of-truth artifact family
- Keep migration additive: do not require destructive splitting of existing legacy `PROJECT.md` files
- Do not add a new temporary onboarding/discovery artifact unless the updated init flow cannot land results cleanly in `PROJECT.md` + `PRD.md`
- If secondary documentation outside the listed files is found to be stale, note it for a follow-on plan rather than turning this into a broad docs sweep
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `kernel/templates/PRD.md` exists and defines the deeper product-definition sections from the Phase 67 recommendation
- [ ] `kernel/workflows/init-project.md` creates/populates layered `PROJECT.md` + `PRD.md` outputs with brownfield-aware bridging
- [ ] `kernel/workflows/plan-phase.md`, `kernel/workflows/discuss-phase.md`, and `kernel/workflows/phase-assumptions.md` explicitly reference `.paul/PRD.md` only where deeper context is needed
- [ ] `docs/ARTIFACT-SPEC.md`, `docs/ADAPTER-CONTRACT.md`, and `docs/KERNEL-MANIFEST.md` reflect the new shared artifact/template contract
- [ ] `bash tests/cross-harness-validation.sh` passes
- [ ] `bash tests/pi-end-to-end-validation.sh` passes
- [ ] All acceptance criteria are satisfied
</verification>

<success_criteria>
- Init/onboarding now produces a compact `PROJECT.md` plus deeper `PRD.md` from distinct product-definition prompts
- Greenfield and brownfield flows converge on one durable schema while preserving evidence-first brownfield onboarding
- Planning/discussion/assumptions/research surfaces use `PRD.md` selectively instead of making every workflow read both artifacts
- Shared docs and validation codify the new layered artifact model without breaking portability or Pi parity
</success_criteria>

<output>
After completion, create `.paul/phases/67-onboarding-prd-depth/67-02-SUMMARY.md`
</output>
