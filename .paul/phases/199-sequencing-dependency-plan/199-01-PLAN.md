---
phase: 199-sequencing-dependency-plan
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/PI-NATIVE-SEQUENCING-DEPENDENCY-PLAN.md
autonomous: true
---

<objective>
## Goal

Create a source-cited sequencing and dependency plan for v2.46 that turns the Phase 198 workstream map into actionable ordering options, dependency edges, proof points, risk gates, split/merge recommendations, and handoff inputs for Phase 200 validation/evidence strategy and Phase 201 final implementation-plan proposal.

## Purpose

Phase 199 is still implementation planning, not implementation hardening. Its job is to test the default v2.45/v2.46 workstream order against dependencies, risk, validation burden, implementation size, and authority boundaries before any build milestone starts.

## Output

Create `docs/PI-NATIVE-SEQUENCING-DEPENDENCY-PLAN.md` with:
- source inputs and planning-only boundary,
- sequencing principles and candidate ordering options,
- dependency matrix across the six Phase 198 workstreams,
- per-workstream prerequisites, proof points, risk gates, and split/merge recommendations,
- Phase 200 and Phase 201 handoff notes.
</objective>

<context>
## Project Context

- `.paul/PROJECT.md` — current product posture: Pi-supported runtime, artifact-first lifecycle truth, GitHub Flow safety, module evidence, parent-owned APPLY, and v2.46 implementation-planning focus.
- `.paul/STATE.md` — current lifecycle position: Phase 199 ready to plan after Phase 198 completion.
- `.paul/ROADMAP.md` — current v2.46 milestone route and Phase 199 scope.
- `pals.json` — project GitHub Flow, guided workflow, and planning defaults.

## Direct Source Inputs

- `docs/PI-NATIVE-IMPLEMENTATION-WORKSTREAMS.md` — primary Phase 198 workstream map and Phase 199 handoff source.
- `docs/PI-NATIVE-IMPLEMENTATION-FINDINGS.md` — Phase 197 source for promoted order, workstream seeds, open planning questions, and Phase 199 handoff recommendations.
- `docs/PI-NATIVE-ARCHITECTURE-PROPOSAL.md` — v2.45 migration roadmap, implementation roadmap, legacy guidance, validation strategy, and non-goals.
- `docs/PI-NATIVE-CONTEXT-EFFICIENCY-ARCHITECTURE.md` — recommended sequence, read/citation contract, validation redesign requirements, and rejected designs.
- `docs/PI-RUNTIME-CAPABILITY-MAP.md` — runtime capability classifications and validation redesign notes.
- `.paul/phases/198-implementation-workstream-design/198-01-SUMMARY.md` — immediate prior phase outcome and decisions.

## Planned Source Files

- `docs/PI-NATIVE-SEQUENCING-DEPENDENCY-PLAN.md` — new Phase 199 planning artifact to be created during APPLY.
</context>

<module_dispatch>
## Pre-Plan Module Dispatch

[dispatch] pre-plan advisory: modules.yaml loaded from installed PALS registry (kernel_version 2.0.0).

| Module | Result |
|---|---|
| TODD | No repo-local unit test framework covers this new planning document; use marker greps plus existing Pi and cross-harness validation scripts. Plan remains `execute`, not TDD. |
| IRIS | Planned file does not yet exist; no TODO/FIXME/HACK/XXX findings in planned file scope. |
| DAVE | No CI config detected in repository; preserve warning, but do not broaden this planning-doc phase into CI generation. |
| DOCS | Documentation catalog includes Phase 197-198 and v2.45/v2.46 source docs; planned output is a docs artifact and should cite nearby source docs. |
| CODI | Skipped — no extractable TS/JS symbols or source selectors in this markdown-only phase scope. |
| RUBY | Planned artifact is documentation; keep it structured and bounded with tables/matrices so sequencing logic remains reviewable. |
| ARIA / LUKE / GABE / DANA / OMAR / PETE / REED / VERA | No UI, API, data, observability, performance, resilience, or privacy-sensitive implementation files are in planned scope. |
| ARCH | Repository structure is documentation/kernel/driver/module/test oriented; planned docs file is in the expected documentation layer. No architectural concerns in planned files. |

[dispatch] pre-plan enforcement: DEAN checked package-manager manifests; no root dependency manifest or lockfile found, so no audit command applies and no dependency block is raised. SETH found no planned security-sensitive source files; no security block is raised.

## Post-Plan Module Dispatch

[dispatch] post-plan: TODD reviewed plan type. No existing tests cover this docs-only planned artifact; plan remains `execute` rather than TDD.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | PLAN/APPLY/UNIFY lifecycle management | ✓ |
| /carl | required | Automatic session-boundary monitoring | automatic |
| TODD | required | APPLY test/validation discipline | module dispatch |
| WALT | required | APPLY/UNIFY quality validation | module dispatch |

Required lifecycle skills are active through the current `/skill:paul-plan` workflow and installed module dispatch.
</skills>

<acceptance_criteria>
## AC-1: Sequencing source inputs and boundary are explicit
```gherkin
Given Phase 198 produced a planning-only workstream map
When `docs/PI-NATIVE-SEQUENCING-DEPENDENCY-PLAN.md` is created
Then it cites relevant source inputs and states that Phase 199 does not approve runtime, workflow, validation-script, dependency, GitHub Flow, or legacy-surface changes
```

## AC-2: Workstream ordering is tested against dependencies and risk
```gherkin
Given six candidate workstreams have different readiness, dependency, validation, and authority risks
When the sequencing plan evaluates candidate order
Then it includes sequencing principles, default order, viable alternatives, dependency matrix, prerequisites, risk gates, and split/merge recommendations
```

## AC-3: Phase 200 and Phase 201 handoffs are actionable
```gherkin
Given Phase 200 owns validation/evidence strategy and Phase 201 owns final implementation-plan proposal
When Phase 199 completes
Then the sequencing artifact provides validation/evidence questions for Phase 200 and roadmap/proposal inputs for Phase 201 without finalizing build milestones prematurely
```

## AC-4: Validation remains green
```gherkin
Given this plan creates documentation and should not change runtime behavior
When APPLY completes
Then marker checks pass and the existing Pi plus cross-harness validation suites remain green
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Create the sequencing source map and planning boundary</name>
  <files>docs/PI-NATIVE-SEQUENCING-DEPENDENCY-PLAN.md</files>
  <action>
    Create `docs/PI-NATIVE-SEQUENCING-DEPENDENCY-PLAN.md` with a concise title, status table, purpose, planning-only boundary, source-input table, and sequencing principles.
    Include at minimum these source paths: `docs/PI-NATIVE-IMPLEMENTATION-WORKSTREAMS.md`, `docs/PI-NATIVE-IMPLEMENTATION-FINDINGS.md`, `docs/PI-NATIVE-ARCHITECTURE-PROPOSAL.md`, `docs/PI-NATIVE-CONTEXT-EFFICIENCY-ARCHITECTURE.md`, `docs/PI-RUNTIME-CAPABILITY-MAP.md`, and `.paul/phases/198-implementation-workstream-design/198-01-SUMMARY.md`.
    State explicitly that this artifact provides sequencing/dependency planning only and does not approve runtime, workflow, validation-script, dependency, GitHub Flow, or legacy-surface changes.
    Preserve the non-negotiable boundaries: `.paul/*` authority, PLAN/APPLY/UNIFY ownership, module evidence, GitHub Flow gates, parent-owned APPLY, citation/freshness/full-read fallback, and no hidden Pi state/telemetry/auto-approval.
  </action>
  <verify>grep -n "Source Inputs\|Planning-Only Boundary\|Sequencing Principles\|PI-NATIVE-IMPLEMENTATION-WORKSTREAMS\|PI-NATIVE-IMPLEMENTATION-FINDINGS\|PI-NATIVE-ARCHITECTURE-PROPOSAL" docs/PI-NATIVE-SEQUENCING-DEPENDENCY-PLAN.md</verify>
  <done>AC-1 satisfied: the sequencing artifact exists, cites source inputs, and preserves the planning-only boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Define dependency matrix, ordering options, and risk gates</name>
  <files>docs/PI-NATIVE-SEQUENCING-DEPENDENCY-PLAN.md</files>
  <action>
    Add a dependency and sequencing analysis for the six Phase 198 workstreams:
    - artifact-slice contract hardening,
    - workflow/resource capsule design,
    - guided workflow evidence hardening,
    - delegated APPLY packet/report compression,
    - Pi-native validation classification,
    - legacy retention and install-surface cleanup.
    Include a dependency matrix, default recommended order, at least two viable ordering alternatives with trade-offs, per-workstream prerequisites/proof points/risk gates, and split/merge recommendations.
    Treat artifact-slice contract hardening as the likely first implementation candidate, guided workflow evidence as a possible early parallel/near-term workstream, workflow capsules as dependent on citation/fallback and blocking-semantics rules, helper packet compression as dependent on report schema clarity, validation classification as required before weakening parity, and legacy cleanup as last/deferred.
    Do not finalize the implementation roadmap or choose build milestone scope; Phase 201 owns the final implementation plan proposal.
  </action>
  <verify>grep -n "Dependency Matrix\|Recommended Order\|Ordering Alternatives\|Risk Gates\|Split / Merge Recommendations\|Artifact-Slice Contract Hardening\|Guided Workflow Evidence Hardening\|Pi-Native Validation Classification\|Legacy Retention" docs/PI-NATIVE-SEQUENCING-DEPENDENCY-PLAN.md</verify>
  <done>AC-2 satisfied: workstream ordering is tested against dependencies, alternatives, risk gates, and split/merge cautions.</done>
</task>

<task type="auto">
  <name>Task 3: Add Phase 200/201 handoff and validate</name>
  <files>docs/PI-NATIVE-SEQUENCING-DEPENDENCY-PLAN.md</files>
  <action>
    Add Phase 200 and Phase 201 handoff sections.
    For Phase 200, identify validation/evidence questions, validation-class dependencies, command-output evidence requirements, and checks that must remain green before implementation hardening.
    For Phase 201, identify implementation-roadmap inputs, candidate milestone splits, readiness criteria, non-goals, and unresolved decisions that must be carried into the final executable implementation plan proposal.
    Add a short sequencing-readiness checklist for when a workstream can be promoted into a future build milestone proposal.
    Run marker checks plus existing Pi and cross-harness validation scripts.
  </action>
  <verify>grep -n "Phase 200 Handoff\|Phase 201 Handoff\|Validation / Evidence Questions\|Implementation Roadmap Inputs\|Sequencing Readiness Checklist" docs/PI-NATIVE-SEQUENCING-DEPENDENCY-PLAN.md && bash tests/pi-end-to-end-validation.sh && bash tests/cross-harness-validation.sh</verify>
  <done>AC-3 and AC-4 satisfied: Phase 200/201 handoffs are actionable and validation remains green.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE

- Do not edit `drivers/pi/extensions/pals-hooks.ts`, `drivers/pi/extensions/README.md`, `drivers/pi/skill-map.md`, or Pi installer behavior.
- Do not edit `tests/pi-end-to-end-validation.sh` or `tests/cross-harness-validation.sh`.
- Do not edit shared workflow semantics, module manifests, installed runtime copies, dependency manifests, GitHub Flow behavior, Claude Code wrappers, Agent SDK surfaces, or portability docs.
- Do not modify `.paul/STATE.md`, `.paul/ROADMAP.md`, `.paul/QUALITY-HISTORY.md`, or `.paul/CODI-HISTORY.md` during APPLY tasks except through normal UNIFY/transition workflow steps after implementation is complete.

## SCOPE LIMITS

- This plan creates a sequencing/dependency planning documentation artifact only.
- Do not implement artifact-slice hardening, workflow capsules, guided UI changes, helper-agent packet changes, validation classification, or legacy cleanup.
- Do not rewrite validation scripts or validation reports; Phase 200 owns validation/evidence strategy.
- Do not choose the final implementation milestone roadmap; Phase 201 owns final implementation-plan proposal.
- Do not add dependencies, telemetry, hidden Pi memory, persistent runtime caches, auto-approval, helper-owned APPLY, merge-gate bypass, or lifecycle ledger.
</boundaries>

<verification>
Before declaring APPLY complete:
- [ ] `grep -n "Source Inputs\|Planning-Only Boundary\|Sequencing Principles\|Dependency Matrix\|Phase 200 Handoff\|Phase 201 Handoff\|Sequencing Readiness Checklist" docs/PI-NATIVE-SEQUENCING-DEPENDENCY-PLAN.md`
- [ ] `grep -n "Artifact-Slice Contract Hardening\|Workflow/Resource Capsule Design\|Guided Workflow Evidence Hardening\|Delegated APPLY Packet/Report Compression\|Pi-Native Validation Classification\|Legacy Retention" docs/PI-NATIVE-SEQUENCING-DEPENDENCY-PLAN.md`
- [ ] `bash tests/pi-end-to-end-validation.sh`
- [ ] `bash tests/cross-harness-validation.sh`
- [ ] All acceptance criteria are satisfied.
</verification>

<success_criteria>
- `docs/PI-NATIVE-SEQUENCING-DEPENDENCY-PLAN.md` exists and is concise, source-cited, and clearly planning-only.
- Workstream sequencing is evaluated with dependency matrix, recommended order, alternatives, proof points, risk gates, and split/merge recommendations.
- Phase 200 has actionable validation/evidence strategy inputs.
- Phase 201 has actionable final implementation-plan proposal inputs without Phase 199 prematurely choosing final build scope.
- No runtime, workflow, validation-script, dependency, legacy-surface, or lifecycle-authority behavior changes are made.
- Existing validation remains green.
</success_criteria>

<output>
After APPLY completion, create `.paul/phases/199-sequencing-dependency-plan/199-01-SUMMARY.md` documenting changed files, acceptance-criteria results, validation output, module reports, deviations, decisions, and follow-on recommendations for Phase 200.
</output>
