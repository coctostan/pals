---
phase: 73-walt-report-clarity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/maxwellnewman/.pi/agent/skills/pals/references/quality-delta.md
  - /Users/maxwellnewman/.pi/agent/skills/pals/references/quality-runner.md
  - /Users/maxwellnewman/.pi/agent/skills/pals/templates/SUMMARY.md
  - /Users/maxwellnewman/.pi/agent/skills/pals/workflows/unify-phase.md
autonomous: true
---

<objective>
## Goal
Reinforce WALT's summary language, validation interpretation, and quality-delta wording so that readers can clearly see what WALT contributed across baseline capture, regression gating, and post-unify quality history.

## Purpose
Phase 71 identified WALT as the strongest proof-bearing module and recommended reinforcing consistency of summary language and validation interpretation. WALT already has the best evidence path — this plan makes it read more clearly by adding a `module_report: walt` label, uncommenting the template subsection, and adding WALT-specific unify reconciliation guidance.

## Output
- Updated `quality-delta.md` with explicit `module_report: walt` carry-forward label
- Updated `quality-runner.md` report format with module_report annotation
- Updated `SUMMARY.md` template with uncommented WALT Quality subsection
- Updated `unify-phase.md` with WALT-specific reconciliation guidance
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md

## Prior Work
.paul/phases/72-todd-report-clarity/72-01-SUMMARY.md

## Source Files
/Users/maxwellnewman/.pi/agent/skills/pals/references/quality-delta.md
/Users/maxwellnewman/.pi/agent/skills/pals/references/quality-runner.md
/Users/maxwellnewman/.pi/agent/skills/pals/templates/SUMMARY.md
/Users/maxwellnewman/.pi/agent/skills/pals/workflows/unify-phase.md
/Users/maxwellnewman/.pi/agent/skills/pals/modules.yaml
</context>

<acceptance_criteria>

## AC-1: WALT report output includes module_report label for carry-forward
```gherkin
Given WALT runs its post-apply quality gate and produces a test/lint/coverage report
When the report format is rendered in quality-runner.md and quality-delta.md
Then the output includes a `module_report: walt` label indicating this is durable evidence for Module Execution Reports
```

## AC-2: SUMMARY.md template provides a concrete WALT Quality subsection
```gherkin
Given a user reads the SUMMARY.md template
When they look at the Module Execution Reports section
Then there is an uncommented WALT Quality subsection with Before/After/Delta/Trajectory columns alongside the already-uncommented TODD subsection
```

## AC-3: Unify workflow includes WALT-specific reconciliation guidance
```gherkin
Given UNIFY runs after an APPLY phase where WALT captured quality baselines
When the reconcile step processes WALT annotations
Then the workflow instructs the agent to populate the WALT Quality subsection using before/after metrics, delta, trajectory indicators, and overall verdict
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add module_report label to WALT report surfaces</name>
  <files>/Users/maxwellnewman/.pi/agent/skills/pals/references/quality-runner.md, /Users/maxwellnewman/.pi/agent/skills/pals/references/quality-delta.md</files>
  <action>
    In `quality-runner.md`, in the `<report_format>` section after the report block (after line ~94):
    - Add a line: "Label this output as `module_report: walt` so the unify phase can carry test/lint gate results into `## Module Execution Reports` as durable WALT evidence."
    - Keep it to 1-2 lines — parallel to the TODD pattern from Phase 72

    In `quality-delta.md`, in the `<metric_format>` section after the SUMMARY.md Quality table (after line ~37):
    - Add: "Label this quality delta as `module_report: walt` for durable inclusion in the SUMMARY.md `## Module Execution Reports` section."
    - Add the compact format note: "One-liner (for dispatch log): `WALT: {overall_verdict} — Tests {before}→{after}, Cov {before}→{after}, Lint {before}→{after}`"
    - Keep both files under 150-line cap
  </action>
  <verify>
    Read quality-runner.md report_format section and confirm `module_report: walt` label is present.
    Read quality-delta.md metric_format section and confirm `module_report: walt` label and one-liner format are present.
    Confirm both files remain under 150 lines.
  </verify>
  <done>AC-1 satisfied: WALT report output includes module_report label for carry-forward</done>
</task>

<task type="auto">
  <name>Task 2: Uncomment WALT subsection in SUMMARY.md template and add unify reconciliation guidance</name>
  <files>/Users/maxwellnewman/.pi/agent/skills/pals/templates/SUMMARY.md, /Users/maxwellnewman/.pi/agent/skills/pals/workflows/unify-phase.md</files>
  <action>
    In `SUMMARY.md` template, in the Module Execution Reports section:
    - Convert the existing commented WALT Quality example (currently starting with `<!-- Example subsection (from a quality gating module`) into an uncommented concrete subsection
    - Use the heading: `### Quality (WALT)`
    - Add a guidance comment: `<!-- Populated from WALT module_report carried forward from quality-delta results. Omit if WALT gate was SKIP (no quality tools detected). -->`
    - Keep the table format: Metric / Before / After / Delta / Trajectory
    - Keep the `**Overall:**` verdict line
    - The TODD subsection (already uncommented in Phase 72) should remain above it

    In `unify-phase.md`, in the `create_summary` step (after the TODD guidance added in Phase 72):
    - Add WALT-specific guidance:
      - "For plans where WALT captured quality baselines: populate the WALT subsection (`### Quality (WALT)`) using before/after metrics, delta values, and trajectory indicators from the quality-delta `module_report: walt`. Include the overall verdict line."
      - "If WALT gate was SKIP (no quality tools detected): omit the WALT subsection entirely rather than showing empty metrics."
    - Keep this to 2-3 lines
  </action>
  <verify>
    Read SUMMARY.md template Module Execution Reports section and confirm:
    1. WALT subsection is uncommented with heading `### Quality (WALT)`
    2. Table has Metric/Before/After/Delta/Trajectory columns
    3. TODD subsection from Phase 72 remains intact above it
    Read unify-phase.md create_summary step and confirm:
    1. WALT reconciliation guidance exists after the TODD guidance
    2. SKIP handling guidance is present
  </verify>
  <done>AC-2 and AC-3 satisfied: Template has concrete WALT subsection; unify workflow has WALT-specific reconciliation guidance</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Quality detection logic in quality-detection.md
- Regression detection logic in quality-runner.md
- Gating rules in quality-runner.md
- Quality history append logic in quality-history.md
- modules.yaml registry entries
- apply-phase.md dispatch logic
- Phase 69/70 `module_reports` / `side_effects` contract
- TODD subsection in SUMMARY.md template (Phase 72 work)
- TODD guidance in unify-phase.md (Phase 72 work)

## SCOPE LIMITS
- Report wording and template clarity only — no architectural changes
- No new hooks or dispatch points
- No changes to the layered proof model
- Do not touch SKIP or RUBY report surfaces (Phases 74-75)

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `quality-runner.md` report_format has `module_report: walt` label
- [ ] `quality-delta.md` metric_format has `module_report: walt` label and one-liner format
- [ ] `SUMMARY.md` template has uncommented WALT Quality subsection alongside TODD
- [ ] `unify-phase.md` has WALT-specific reconciliation guidance after TODD guidance
- [ ] All modified reference files remain under the 150-line cap
- [ ] No changes to detection, gating, or dispatch logic
- [ ] Phase 72 TODD work remains intact
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- WALT's quality story is legible end-to-end: baseline → gate → delta → carry-forward → summary
</success_criteria>

<output>
After completion, create `.paul/phases/73-walt-report-clarity/73-01-SUMMARY.md`
</output>
