---
phase: 100-module-e2e-test
plan: 02
type: fix
wave: 1
depends_on: []
files_modified:
  - kernel/workflows/plan-phase.md
  - kernel/workflows/apply-phase.md
  - kernel/workflows/unify-phase.md
autonomous: true
---

<objective>
## Fix
Add modules.yaml to `<required_reading>` in plan-phase.md, apply-phase.md, and unify-phase.md. The model reads everything in required_reading but skips the embedded dispatch substeps. This exploits the pattern the model already follows.
</objective>

<tasks>
<task type="auto">
  <name>Fix: Add modules.yaml to required_reading in 3 workflow files</name>
  <files>kernel/workflows/plan-phase.md, kernel/workflows/apply-phase.md, kernel/workflows/unify-phase.md</files>
  <action>Add "modules.yaml (installed module registry — MUST read if it exists; drives hook dispatch)" to required_reading section of each file.</action>
  <verify>grep "modules.yaml" in required_reading sections of all 3 files</verify>
  <done>Fix applied</done>
</task>
</tasks>

<verification>
- [ ] All 3 files have modules.yaml in required_reading
- [ ] Reinstall needed for deployed copies
</verification>
