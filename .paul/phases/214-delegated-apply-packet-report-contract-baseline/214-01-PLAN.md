---
phase: 214-delegated-apply-packet-report-contract-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/PI-NATIVE-DELEGATED-APPLY-PACKET-REPORT-CONTRACT.md
autonomous: true
---

<objective>
## Goal
Create a source-grounded Delegated APPLY Packet/Report Contract baseline that defines how bounded `pals-implementer` delegation can reduce parent APPLY context cost without weakening parent authority.

## Purpose
v2.50 needs a contract before runtime helper compression begins. This plan establishes the packet fields, report schema, allowed/forbidden file rules, fallback triggers, validation classes, and Phase 215-217 handoffs that later implementation must follow.

## Output
- New contract artifact: `docs/PI-NATIVE-DELEGATED-APPLY-PACKET-REPORT-CONTRACT.md`
- No runtime behavior, validation script, dependency, CI, GitHub Flow, installed-runtime, helper-agent, or `.paul/*` lifecycle-authority changes during APPLY, except normal lifecycle writes by PALS workflows.
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md
.paul/SPECIAL-FLOWS.md

## Source Inputs
- `docs/PI-NATIVE-IMPLEMENTATION-PLAN.md` — names Delegated APPLY Packet/Report Contract as the fourth Pi-native build milestone and requires Helper Delegation proof, structured reports, parent official verification, file-scope diff checks, and no helper-owned `.paul/*` writes.
- `docs/PI-NATIVE-ARTIFACT-SLICE-CONTRACT.md` — preserves derived-aid, source-citation, freshness, bounds, full-read fallback, and command-output-truth boundaries.
- `docs/PI-NATIVE-GUIDED-WORKFLOW-EVIDENCE-CONTRACT.md` — preserves no auto-approval, no auto-continue, no skipped checkpoints, no UI-only lifecycle decisions, and transcript/artifact-visible user intent.
- `docs/PI-NATIVE-WORKFLOW-RESOURCE-CAPSULE-CONTRACT.md` — preserves orientation-only context aids, full authoritative read fallback, blocking-semantics exclusions, and parent-owned APPLY boundaries.

## Source Files
- `docs/PI-NATIVE-DELEGATED-APPLY-PACKET-REPORT-CONTRACT.md` (new)
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: modules.yaml loaded from installed registry; advisory modules evaluated for planned docs-only file `docs/PI-NATIVE-DELEGATED-APPLY-PACKET-REPORT-CONTRACT.md`.

### Advisory Findings
- ARCH: Detected pattern: layered docs/kernel/drivers/modules repository (medium confidence). Planned file is a docs artifact in `docs/`, which is the expected layer for a contract baseline. Structural warning: `docs/` currently contains 28 markdown files (>25 directory-count warning). No circular dependency risk in planned files.
- TODD: No test files/framework markers detected in root manifests; no TDD conversion candidate for a docs-only contract baseline.
- IRIS: No TODO/FIXME/HACK/XXX findings in planned file because it is new.
- DAVE: No CI config detected in `.github/workflows`, GitLab, Docker, Compose, or Jenkins surfaces. Suggestion remains out of scope: add `.github/workflows/ci.yml` in a separate approved plan if desired.
- DOCS: Documentation catalog exists under `docs/`; planned source is itself a docs artifact. No nearby-doc drift for this baseline.
- CODI: skipped — no extractable symbols in phase scope.
- RUBY: No preliminary debt indicators in planned file; post-unify can use line-count/complexity fallback if needed.
- ARIA: No UI files in planned changes — skipping.
- DANA: No data files in planned changes — skipping.
- GABE: No API files in planned changes — skipping.
- LUKE: No UI files in planned changes — skipping.
- OMAR: No observability-relevant files in scope — skipping.
- PETE: No performance-sensitive files in planned changes.
- REED: No resilience-relevant files in scope — skipping.
- SETH: No security concerns in planned file; no secrets detected.
- VERA: No privacy-relevant files in scope — skipping.

[dispatch] pre-plan enforcement: DEAN PASS_WITH_WARNING — no root package manager lock/manifest detected; nested `drivers/agent-sdk/requirements.txt` exists, but `python3 -m pip_audit -r drivers/agent-sdk/requirements.txt --format json` reported `No module named pip_audit`. No critical/high vulnerability evidence was produced and no dependency changes are in scope.

## Post-Plan Module Dispatch
[dispatch] post-plan: TODD reviewed plan type `execute`; no existing tests cover the planned docs-only file and no RED-GREEN-REFACTOR conversion was suggested.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | All planning, applying, and unifying work | ✓ |
| /carl | required | Automatic session boundary monitoring | automatic |
| TODD | required | Before/during implementation and post-plan/post-task checks | module dispatch |
| WALT | required | APPLY/UNIFY quality gates and validation | module dispatch |

**BLOCKING:** Required skills MUST be loaded or active before APPLY proceeds.
</skills>

<acceptance_criteria>
## AC-1: Contract baseline is source-grounded and complete
```gherkin
Given v2.50 needs a delegated APPLY packet/report contract before runtime hardening
When the contract artifact is created
Then it cites relevant Pi-native source contracts and defines packet fields, report fields, source inputs, scope boundaries, and phase handoffs without relying on hidden state or unstated assumptions
```

## AC-2: Parent APPLY authority is preserved
```gherkin
Given delegated `pals-implementer` work may reduce parent context cost
When the contract defines helper eligibility, allowed files, forbidden files, fallback triggers, and report review rules
Then parent APPLY remains responsible for official verification, module enforcement, file-scope checks, checkpoints, fallback judgment, result classification, and `.paul/*` lifecycle writes
```

## AC-3: Validation and handoff requirements are executable
```gherkin
Given Phase 215 will harden runtime delegation behavior and later phases will surface/reconcile evidence
When the contract names validation classes, marker-check families, command-output evidence, failure conditions, and Phase 215-217 handoffs
Then later plans can implement and verify helper delegation without broad validation classification, legacy cleanup, dependency, CI, GitHub Flow, or lifecycle-authority scope creep
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Create the delegated APPLY packet/report contract baseline</name>
  <files>docs/PI-NATIVE-DELEGATED-APPLY-PACKET-REPORT-CONTRACT.md</files>
  <action>
    Create the new contract artifact with these sections:
    - Status and purpose/boundary for v2.50 Phase 214.
    - Source inputs citing `docs/PI-NATIVE-IMPLEMENTATION-PLAN.md`, `docs/PI-NATIVE-ARTIFACT-SLICE-CONTRACT.md`, `docs/PI-NATIVE-GUIDED-WORKFLOW-EVIDENCE-CONTRACT.md`, and `docs/PI-NATIVE-WORKFLOW-RESOURCE-CAPSULE-CONTRACT.md`.
    - Delegation eligibility rules for bounded `pals-implementer` use, including when parent APPLY must stay inline.
    - Parent-owned task packet schema: plan/task identifiers, objective, files allowed, files forbidden, context sources, verification expectation, boundaries, fallback triggers, checkpoint status, module caveats, and no lifecycle-write authority.
    - Structured helper report schema: task id, files touched, summary, verification attempted, changed-file list, uncertainty/deviation flags, fallback recommendation, and parent verification requirements.
    - Allowed/forbidden file-scope rules, especially forbidding helper-owned `.paul/*` lifecycle writes and installed/runtime absolute paths.
    - Full-read fallback triggers and failure conditions.
    - Validation classes and marker-check families for Helper Delegation, Shared Invariant, Artifact / Process, Pi-Supported Runtime, Runtime Lens, Guided UI Safety, GitHub Flow Safety, and Frozen Legacy Parity where relevant.
    - Command-output evidence and Phase 215, 216, and 217 handoff requirements.
    Avoid: implementing runtime behavior, editing helper-agent definitions, changing validation scripts, adding dependencies, changing CI/GitHub Flow behavior, or rewriting prior contract docs.
  </action>
  <verify>test -f docs/PI-NATIVE-DELEGATED-APPLY-PACKET-REPORT-CONTRACT.md && grep -n "Parent-owned task packet schema\|Structured helper report schema\|Fallback\|Helper Delegation\|no helper-owned .paul" docs/PI-NATIVE-DELEGATED-APPLY-PACKET-REPORT-CONTRACT.md</verify>
  <done>AC-1 and AC-2 satisfied: contract exists, is source-grounded, and defines packet/report authority boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Validate contract markers and no-scope-creep boundaries</name>
  <files>docs/PI-NATIVE-DELEGATED-APPLY-PACKET-REPORT-CONTRACT.md</files>
  <action>
    Review and tighten the contract so it contains explicit validation/evidence and deferral markers:
    - Validation classes include Helper Delegation and Shared Invariant.
    - Marker-check families mention parent-owned packet, structured helper report, changed-file diff check, fallback decision, parent-run official verification, and no helper-owned `.paul/*` writes.
    - Failure conditions block helper lifecycle writes, helper-owned module/validation results, helper expansion outside allowed files, missing parent verification, hidden helper state, auto-approval/auto-continue, GitHub Flow bypass, and command-output replacement.
    - Phase 215 handoff targets runtime delegation hardening only after this contract; Phase 216 owns docs/validation surfacing; Phase 217 owns evidence reconciliation and milestone closure.
    - Explicit deferrals preserve broad validation classification, legacy cleanup, dependencies, CI config, GitHub Flow automation, runtime implementation in Phase 214, and lifecycle authority.
    Avoid: broadening the contract into a runtime implementation spec with code-level changes or changing existing docs beyond this new artifact.
  </action>
  <verify>grep -n "Helper Delegation\|Shared Invariant\|changed-file diff\|parent-run official verification\|Phase 215\|Phase 216\|Phase 217\|Explicit Deferrals\|Failure Conditions" docs/PI-NATIVE-DELEGATED-APPLY-PACKET-REPORT-CONTRACT.md && bash tests/pi-end-to-end-validation.sh && bash tests/cross-harness-validation.sh</verify>
  <done>AC-2 and AC-3 satisfied: contract has executable validation markers, explicit handoffs, and no-scope-creep boundaries with validation passing.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- `drivers/pi/extensions/pals-hooks.ts`
- `drivers/pi/extensions/README.md`
- `drivers/pi/skill-map.md`
- `tests/pi-end-to-end-validation.sh`
- `tests/cross-harness-validation.sh`
- `kernel/workflows/*`
- `modules/**`
- `pals.json`
- Dependency manifests, CI configuration, installed runtime copies, generated runtime outputs, and GitHub Flow automation
- `.paul/*` lifecycle artifacts except normal PLAN/APPLY/UNIFY lifecycle writes by the parent workflow

## SCOPE LIMITS
- Phase 214 is contract-baseline/docs-only work.
- Do not implement runtime delegation hardening; Phase 215 owns that.
- Do not surface docs/skill-map/validation script updates; Phase 216 owns that.
- Do not reconcile validation counts or close the milestone; Phase 217 owns that.
- Do not classify broad validation suites, clean legacy surfaces, add dependencies, change CI, or modify GitHub Flow behavior.
- Do not let `pals-implementer` write `.paul/*`, own official verification, satisfy module gates, complete checkpoints, approve APPLY/UNIFY, or make fallback decisions.
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `test -f docs/PI-NATIVE-DELEGATED-APPLY-PACKET-REPORT-CONTRACT.md`
- [ ] `grep -n "Parent-owned task packet schema\|Structured helper report schema\|Helper Delegation\|Shared Invariant\|changed-file diff\|parent-run official verification\|no helper-owned .paul" docs/PI-NATIVE-DELEGATED-APPLY-PACKET-REPORT-CONTRACT.md`
- [ ] `bash tests/pi-end-to-end-validation.sh`
- [ ] `bash tests/cross-harness-validation.sh`
- [ ] `git diff --stat` confirms changed source scope is limited to the new contract artifact plus normal `.paul/*` lifecycle artifacts.
- [ ] All acceptance criteria met.
</verification>

<success_criteria>
- `docs/PI-NATIVE-DELEGATED-APPLY-PACKET-REPORT-CONTRACT.md` exists and is source-grounded.
- The contract defines parent-owned packet fields, structured helper report fields, allowed/forbidden file rules, fallback triggers, validation classes, command-output evidence, and handoffs for Phases 215-217.
- The contract explicitly preserves parent APPLY authority, module evidence, GitHub Flow gates, checkpoints/approvals, `.paul/*` lifecycle writes, and command-output truth.
- Validation commands pass or any failure is documented with a parent-owned fallback decision.
- No out-of-scope runtime, validation-script, dependency, CI, GitHub Flow, legacy cleanup, or lifecycle-authority change is introduced.
</success_criteria>

<output>
After completion, create `.paul/phases/214-delegated-apply-packet-report-contract-baseline/214-01-SUMMARY.md`.
</output>
