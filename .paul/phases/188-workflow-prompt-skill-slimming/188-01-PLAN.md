---
phase: 188-workflow-prompt-skill-slimming
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kernel/commands/paul/plan.md
  - kernel/commands/paul/apply.md
  - kernel/commands/paul/unify.md
  - kernel/commands/paul/resume.md
  - kernel/commands/paul/milestone.md
  - drivers/pi/skills/paul-plan/SKILL.md
  - drivers/pi/skills/paul-apply/SKILL.md
  - drivers/pi/skills/paul-unify/SKILL.md
  - drivers/pi/skills/paul-resume/SKILL.md
  - drivers/pi/skills/paul-milestone/SKILL.md
autonomous: true
---

<objective>
## Goal
Slim the highest-frequency PALS command prompts and matching Pi skill wrappers for PLAN/APPLY/UNIFY/resume/milestone so they delegate repeated mechanics to canonical workflows instead of restating them inline.

## Purpose
v2.44 continues the context diet by reducing recurring prompt/skill read cost while preserving artifact-first truth, module dispatch evidence, GitHub Flow safety, and parent-owned APPLY semantics.

## Output
A smaller, consistent pointer-style wrapper set for core lifecycle commands and Pi skills, plus validation evidence that existing PALS behavior markers still pass.
</objective>

<context>
## Project Context
- `.paul/PROJECT.md` — v2.44 active; context optimization must preserve archive links, historical authority, module evidence, GitHub Flow safety, and parent-owned APPLY.
- `.paul/ROADMAP.md` — Phase 188 focus: reduce recurring prompt, skill, and workflow read cost without weakening lifecycle semantics.
- `.paul/STATE.md` — ready for Phase 188 PLAN.
- Planning mode: exploratory; collaboration level: high.
- Strategic direction: Pi is likely the primary supported harness going forward; Claude Code command wrappers should be treated as legacy/reference thin pointers, not a surface for new Claude-specific polish.

## Prior Work
- `.paul/phases/187-regression-anti-regrowth-guardrails/187-01-SUMMARY.md` — existing validation now protects hot workflow line envelopes and semantic lifecycle/module markers.

## Source Files
- `kernel/commands/paul/plan.md`
- `kernel/commands/paul/apply.md`
- `kernel/commands/paul/unify.md`
- `kernel/commands/paul/resume.md`
- `kernel/commands/paul/milestone.md`
- `drivers/pi/skills/paul-plan/SKILL.md`
- `drivers/pi/skills/paul-apply/SKILL.md`
- `drivers/pi/skills/paul-unify/SKILL.md`
- `drivers/pi/skills/paul-resume/SKILL.md`
- `drivers/pi/skills/paul-milestone/SKILL.md`

## Baseline
Current selected-wrapper total is 406 lines:
- Claude command wrappers: 313 lines across 5 files.
- Pi skill wrappers: 93 lines across 5 files.
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: ARCH detected repo-organized kernel/drivers/modules/tests structure; planned files are markdown prompt/skill wrappers in expected command/driver locations. Current selected-wrapper total is 406 lines; no circular dependency risk.

[dispatch] pre-plan advisory: ARIA, DANA, GABE, LUKE, OMAR, PETE, REED, VERA skipped — planned files are markdown prompt/skill wrappers with no UI, data, API, observability, performance-sensitive runtime, resilience, or privacy surface.

[dispatch] pre-plan advisory: SETH passed — no hardcoded secret patterns found in planned files.

[dispatch] pre-plan advisory: TODD found no project test framework from root manifests; existing shell validation suites remain the relevant verification path for markdown workflow artifacts.

[dispatch] pre-plan advisory: IRIS found no TODO/FIXME/HACK/XXX markers in planned files.

[dispatch] pre-plan advisory: DAVE found no CI workflow config; no CI generation is in scope for this prompt/skill slimming plan.

[dispatch] pre-plan advisory: DOCS found markdown documentation catalog; planned changes are documentation/prompt artifacts themselves, so no separate nearby docs update is required.

[dispatch] CODI: skipped — no extractable symbols in phase scope.

[dispatch] pre-plan advisory: RUBY flags prompt-wrapper line-count debt in `kernel/commands/paul/apply.md` and `kernel/commands/paul/unify.md`; slimming should prefer shared workflow pointers over duplicating checkpoint/reconcile mechanics.

[dispatch] pre-plan enforcement: DEAN PASS/SKIP — no dependency manifest or lock file is modified by this plan; dependency audit baseline is not affected.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | PLAN/APPLY/UNIFY lifecycle work | ✓ loaded via `/skill:paul-plan` |
| /carl | required | Automatic session boundary monitoring | automatic |
| TODD | required | During APPLY verification gates | module dispatch |
| WALT | required | During APPLY/UNIFY quality gates | module dispatch |

**BLOCKING:** APPLY must preserve parent-owned verification and module enforcement even if delegated execution is considered.
</skills>

<acceptance_criteria>
## AC-1: Wrapper Line-Cost Reduction
```gherkin
Given the selected command and Pi skill wrappers currently total 406 lines
When Phase 188 Plan 188-01 is applied
Then the selected wrapper total is reduced to 325 lines or fewer without deleting canonical workflow pointers
```

## AC-2: Lifecycle Semantics Preserved
```gherkin
Given the wrappers route PLAN/APPLY/UNIFY/resume/milestone behavior to canonical workflows
When the wrappers are slimmed
Then they still preserve explicit approval, parent-owned APPLY, checkpoint handling, mandatory UNIFY, single next-action routing, collaboration mode markers, and milestone create/complete routing
```

## AC-3: Pi Skill Wrappers Stay Thin and Complete
```gherkin
Given Pi skills are the active harness-facing command wrappers
When matching skill wrappers are normalized
Then each changed Pi skill keeps frontmatter, canonical workflow references, and only command-local notes that matter at invocation time
```

## AC-4: Existing Validation Remains Green
```gherkin
Given v2.43 added context-diet regression guardrails
When wrapper slimming is complete
Then `tests/pi-end-to-end-validation.sh` and `tests/cross-harness-validation.sh` pass without weakening existing marker checks
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Slim core Claude command wrappers to canonical pointers</name>
  <files>kernel/commands/paul/plan.md, kernel/commands/paul/apply.md, kernel/commands/paul/unify.md, kernel/commands/paul/resume.md, kernel/commands/paul/milestone.md</files>
  <action>
    Rewrite the five command prompt wrappers into a consistent thin-wrapper shape:
    - Keep YAML frontmatter and allowed tools intact unless an allowed-tool entry is plainly unused by the wrapper.
    - Keep `<objective>`, `<execution_context>`, `<context>`, `<process>`, and `<success_criteria>` sections.
    - Replace duplicated workflow mechanics with short "Follow workflow: ..." routing plus only command-local obligations.
    - Preserve exact semantic markers required by existing validation where relevant: `default_collaboration`, `low / medium / high`, `exploratory`, and `direct-requirements` must remain discoverable across planning command/workflow surfaces.
    - Preserve APPLY approval/checkpoint/parent verification meaning, UNIFY mandatory close-loop meaning, resume exactly-one-next-action meaning, and milestone create/complete routing.
    - Bias Claude command wrappers toward minimal legacy/reference routing, while Pi skill wrappers retain the stronger active user-facing notes needed for current Pi operation.
    Avoid: editing canonical workflow files in this task; those are authoritative and out of scope for this wrapper-only slice.
  </action>
  <verify>`wc -l kernel/commands/paul/plan.md kernel/commands/paul/apply.md kernel/commands/paul/unify.md kernel/commands/paul/resume.md kernel/commands/paul/milestone.md` shows a materially lower command-wrapper total, and `grep -R 'default_collaboration\|exploratory\|direct-requirements' kernel/commands/paul kernel/workflows drivers/pi/skills` still finds the planning markers.</verify>
  <done>AC-1 and AC-2 satisfied for Claude command wrappers.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize matching Pi skill wrappers without adding prose</name>
  <files>drivers/pi/skills/paul-plan/SKILL.md, drivers/pi/skills/paul-apply/SKILL.md, drivers/pi/skills/paul-unify/SKILL.md, drivers/pi/skills/paul-resume/SKILL.md, drivers/pi/skills/paul-milestone/SKILL.md</files>
  <action>
    Review the five Pi skill wrappers against the slim command wrappers:
    - Keep existing frontmatter names and descriptions.
    - Keep canonical workflow references repo-relative to the installed skill layout.
    - Remove only redundant explanatory prose; keep command-local notes that prevent semantic loss.
    - Ensure `paul-plan` still mentions shared collaboration defaults, exploratory/direct-requirements mode, selective PRD reading, and review routing.
    - Ensure `paul-apply` still states explicit approval, optional delegated execution boundaries, parent-owned verification/module gates, and UNIFY routing.
    Avoid: making Pi skills mirror every command-wrapper sentence; the goal is thinner wrappers with equivalent routing, not duplicate prose.
  </action>
  <verify>`wc -l drivers/pi/skills/paul-plan/SKILL.md drivers/pi/skills/paul-apply/SKILL.md drivers/pi/skills/paul-unify/SKILL.md drivers/pi/skills/paul-resume/SKILL.md drivers/pi/skills/paul-milestone/SKILL.md` stays at or below the current 93-line total while preserving canonical workflow path references.</verify>
  <done>AC-2 and AC-3 satisfied for Pi skill wrappers.</done>
</task>

<task type="auto">
  <name>Task 3: Verify context-diet markers and full validation</name>
  <files>kernel/commands/paul/plan.md, kernel/commands/paul/apply.md, kernel/commands/paul/unify.md, kernel/commands/paul/resume.md, kernel/commands/paul/milestone.md, drivers/pi/skills/paul-plan/SKILL.md, drivers/pi/skills/paul-apply/SKILL.md, drivers/pi/skills/paul-unify/SKILL.md, drivers/pi/skills/paul-resume/SKILL.md, drivers/pi/skills/paul-milestone/SKILL.md</files>
  <action>
    Run targeted shape checks and the existing validation suites:
    - Compute selected-wrapper total and confirm it is <= 325 lines.
    - Confirm planning markers still exist across shared workflow/command/skill surfaces.
    - Run Pi and cross-harness validation scripts.
    - If validation fails because an exact marker moved, restore the marker in the thinnest appropriate wrapper or workflow-adjacent surface; do not weaken tests in this plan.
  </action>
  <verify>`bash tests/pi-end-to-end-validation.sh` and `bash tests/cross-harness-validation.sh` both pass; selected-wrapper total is <= 325 lines.</verify>
  <done>AC-1, AC-2, AC-3, and AC-4 satisfied with validation evidence ready for UNIFY.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- `kernel/workflows/*.md` — canonical workflow semantics are out of scope for this first v2.44 slice.
- `kernel/references/module-dispatch.md` and `modules.yaml` registry semantics.
- Validation scripts, unless APPLY discovers a false-negative marker issue that cannot be fixed by preserving wrapper text. Default is no validation-script edits.
- Installed runtime copies outside the repo.

## SCOPE LIMITS
- Do not redesign command taxonomy, add/remove commands, or change command names.
- Do not alter GitHub Flow, checkpoint, module dispatch, or parent-owned APPLY behavior.
- Do not expand Claude-specific support surface; only reduce and preserve existing routing semantics.
- Do not archive additional `.paul/*` history in this plan; artifact-structure simplification belongs to Phase 190.
- Do not add new dependencies or CI generation.
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `wc -l` over the 10 planned wrapper files shows total <= 325 lines.
- [ ] `grep -R 'default_collaboration' kernel/workflows kernel/commands/paul drivers/pi/skills` succeeds.
- [ ] `grep -R 'exploratory' kernel/workflows kernel/commands/paul drivers/pi/skills` succeeds.
- [ ] `grep -R 'direct-requirements' kernel/workflows kernel/commands/paul drivers/pi/skills` succeeds.
- [ ] `bash tests/pi-end-to-end-validation.sh` passes.
- [ ] `bash tests/cross-harness-validation.sh` passes.
- [ ] All acceptance criteria met.
</verification>

<success_criteria>
- The selected wrapper set is at least 20% smaller, with total line count <= 325.
- Command wrappers route through canonical workflows instead of duplicating detailed workflow mechanics.
- Pi skill wrappers remain thin and semantically complete.
- Existing context-diet and cross-harness validation remains green.
</success_criteria>

<output>
After completion, create `.paul/phases/188-workflow-prompt-skill-slimming/188-01-SUMMARY.md` documenting line-count delta, semantic markers preserved, validation results, module execution reports, and any deviations.
</output>
