---
phase: 144-readme-documentation-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CHANGELOG.md
autonomous: true
---

<objective>
## Goal
Full refresh of README.md and CHANGELOG.md to reflect the current state of PALS after 144 phases of evolution.

## Purpose
README is stale: lists 8 pals instead of 18 modules, wrong git clone URL, ghost commands, missing pals.json sections, references deprecated ~/.pals/ install path, and doesn't mention features shipped since v2.12 (collaborative planning, E2E testing, guided workflow config, init overhaul, module dispatch hardening). CHANGELOG hasn't been updated since v2.12.

## Output
- README.md fully rewritten to match current reality
- CHANGELOG.md updated with v2.13 through v2.30 entries
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md

## Source Files
README.md
CHANGELOG.md
pals.json (current config schema)
drivers/pi/extensions/pals-hooks.ts (Pi extension reference)

## Audit Findings (13 issues)
1. 8 pals listed → 18 modules installed (ARCH, ARIA, DANA, GABE, LUKE, OMAR, PETE, REED, SETH, VERA missing)
2. Git clone URL: maxwellnewman/pals → coctostan/pals
3. /paul:progress listed → actual command is /paul:status
4. /paul:handoff listed → not a real command
5. /paul:discuss-milestone separate → actual is /paul:discuss or /paul:milestone
6. pals.json example missing guided_workflow section (v2.30)
7. pals.json example missing planning section
8. /carl:manager referenced → CARL is Pi-extension-only session boundary now
9. No mention of E2E testing (v2.29)
10. No mention of collaborative planning (v2.6)
11. ~/.pals/ install path → actual is ~/.pi/ and ~/.claude/
12. CHANGELOG missing v2.13 through v2.30
13. Several ghost commands in command tables
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: TODD(100) → skip | IRIS(150) → skip | DOCS(200) → README.md needs update (confirmed by audit) | RUBY(250) → skip
[dispatch] pre-plan enforcement: DEAN(50) → skip | SETH(80) → PASS
</module_dispatch>

<acceptance_criteria>

## AC-1: Module Roster Accurate
```gherkin
Given the README module table
When a reader counts the listed modules
Then all 18 installed modules are represented (8 core pals + 10 expert modules)
```

## AC-2: Commands Match Reality
```gherkin
Given the README command tables
When compared against actual /paul:* skill definitions
Then every listed command exists, ghost commands are removed, and /paul:status replaces /paul:progress
```

## AC-3: Configuration Example Current
```gherkin
Given the README pals.json example
When compared against the current pals.json schema
Then git, planning, preferences, and guided_workflow sections are all shown
```

## AC-4: Install Paths Correct
```gherkin
Given the README install section
When a user follows the instructions
Then paths reference the correct locations (no ~/.pals/)
```

## AC-5: CHANGELOG Updated Through v2.30
```gherkin
Given the CHANGELOG
When a reader looks for recent versions
Then entries exist for v2.13 through v2.30 with key changes noted
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md</name>
  <files>README.md</files>
  <action>
    Rewrite README.md with the following corrections and improvements:

    **Header + Quick Start:**
    - Fix git clone URL to `coctostan/pals`
    - Keep the same concise tone

    **Architecture:**
    - Update module row to show all 18 modules (group as "8 core pals + 10 expert modules")
    - Keep 3-layer diagram

    **The Pals — split into two tables:**
    - "Core Pals" table: PAUL, CARL, TODD, WALT, DEAN, IRIS, SKIP, DAVE, RUBY (existing 8+PAUL)
    - "Expert Modules" table: ARCH, ARIA, DANA, GABE, LUKE, OMAR, PETE, REED, SETH, VERA
    - Brief 1-line description for each expert module (architecture, accessibility, data, API conventions, UX, observability, performance, resilience, security, privacy)
    - Update CARL description: "Pi: autonomous session boundary manager. Claude Code: context rule injection."

    **Commands — fix ghost commands:**
    - Remove /paul:progress → replace with /paul:status
    - Remove /paul:handoff (not a real command)
    - Remove /paul:discuss-milestone (use /paul:discuss)
    - Remove /paul:assumptions, /paul:discover, /paul:research, /paul:research-phase, /paul:consider-issues (not real Pi skills)
    - Remove /paul:complete-milestone, /paul:add-phase, /paul:remove-phase (handled by /paul:milestone)
    - Remove /paul:verify, /paul:plan-fix (not real commands)
    - Remove /paul:flows, /paul:map-codebase (not real Pi commands)
    - Keep accurate commands: init, plan, apply, unify, status, resume, pause, fix, milestone, discuss, help
    - Keep module commands: coverage, quality, deps, review, knowledge, deploy, refactor, config

    **Configuration:**
    - Update pals.json example to include ALL current sections: modules, git, integrations, planning, preferences, guided_workflow
    - Show the guided_workflow.auto_present section with a note about v2.30
    - Update text about module count to 18

    **Install:**
    - Fix paths: remove ~/.pals/ references
    - Describe what gets installed: ~/.claude/ for Claude Code, ~/.pi/agent/skills/pals/ + ~/.pi/agent/extensions/ for Pi
    - Fix git clone URL

    **New section: "Key Features" (after Architecture, before The Pals):**
    - Collaborative Planning (low/medium/high collaboration levels)
    - GitHub Flow enforcement (strict branch/PR/CI gating)
    - Guided Workflow UX (Pi: per-type auto-approve config)
    - E2E Test Protocol (reusable testing methodology)
    - 18 modules across architecture, security, accessibility, performance, data, API, UX, observability, resilience, privacy

    **CARL section:**
    - Remove /carl:manager reference
    - Pi: session boundary manager via extension events (automatic)
    - Claude Code: context rule injection via hooks

    Avoid: Making the README excessively long. Keep it scannable. Use tables and brief descriptions.
    Avoid: Documenting every workflow detail — point to docs/ for deep dives.
  </action>
  <verify>
    # Check module count
    grep -c 'ARCH\|ARIA\|DANA\|GABE\|LUKE\|OMAR\|PETE\|REED\|SETH\|VERA' README.md
    # Check no ghost commands
    grep -c 'paul:progress\|paul:handoff\|paul:discuss-milestone' README.md
    # Check correct URL
    grep 'coctostan/pals' README.md | head -2
    # Check guided_workflow mentioned
    grep 'guided_workflow' README.md | head -2
  </verify>
  <done>AC-1, AC-2, AC-3, AC-4 satisfied: Modules accurate, commands match reality, config current, paths correct</done>
</task>

<task type="auto">
  <name>Task 2: Update CHANGELOG.md with v2.13–v2.30</name>
  <files>CHANGELOG.md</files>
  <action>
    Add entries for v2.13 through v2.30 at the top of the changelog (below the header, above v2.12). Each entry should follow Keep a Changelog format with Added/Changed/Fixed sections.

    Key milestones to cover (use ROADMAP.md completed milestones for reference):
    - v2.13 (2026-03-23): GitHub Flow Hardening — branch/staging correctness, routing validation, status parity, docs
    - v2.14 (2026-03-24): GitHub Flow Validation — isolated + live validation, fixes
    - v2.15 (2026-03-24): PALS E2E Validation via Pi — pi-verify built through PALS
    - v2.16 (2026-03-24): Module Dispatch Validation — E2E module test
    - v2.17 (2026-03-24): Module Dispatch Refinement — blocking-suppresses-advisory fix
    - v2.18 (2026-03-24): Module Hardening — tool-backed output
    - v2.19 (2026-03-24): Module Excellence — all modules A/A+
    - v2.20-v2.22 (2026-03-24): Expertise Expansion I-III — 10 new expert modules (ARCH, SETH, PETE, GABE, LUKE, ARIA, DANA, OMAR, REED, VERA)
    - v2.23 (2026-03-25): pi-verify v1.0 shipped
    - v2.24 (2026-03-25): Module Dispatch Integrity — debug + fix
    - v2.25 (2026-03-25): E2E Adversarial Testing
    - v2.26 (2026-03-26): Init/Onboarding Overhaul — 3 init flows
    - v2.27 (2026-03-26): Module Dispatch Hardening — imperative wording
    - v2.28 (2026-03-26): DEAN Baseline Management
    - v2.29 (2026-03-26): E2E Test Protocol — reusable protocol + report template
    - v2.30 (2026-03-26): Pi Guided Workflow UX & Documentation — auto-approve config, menu parser hardening, label fixes, README/docs refresh

    Keep entries concise — 2-5 bullet points per version. Group minor versions (e.g., v2.20-v2.22 can share one entry for "Expertise Expansion").

    Avoid: Exhaustive per-phase detail. Summarize at the milestone level.
  </action>
  <verify>
    grep -c '## \[2\.' CHANGELOG.md
    grep 'v2.30\|2.30' CHANGELOG.md | head -2
  </verify>
  <done>AC-5 satisfied: CHANGELOG covers v2.13 through v2.30</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- drivers/pi/extensions/pals-hooks.ts (no code changes)
- kernel/ workflow files
- pals.json
- .paul/ artifacts (except STATE.md/ROADMAP.md via workflow steps)

## SCOPE LIMITS
- README + CHANGELOG only — not docs/*.md files
- Don't rewrite architecture docs
- Keep README scannable — link to docs/ for depth

</boundaries>

<verification>
Before declaring plan complete:
- [ ] All 18 modules listed in README
- [ ] Zero ghost commands (progress, handoff, discuss-milestone)
- [ ] Git clone URL is coctostan/pals
- [ ] pals.json example shows all 6 sections
- [ ] No ~/.pals/ references
- [ ] CHANGELOG has v2.13 through v2.30 entries
- [ ] guided_workflow mentioned in README
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- README accurately represents current PALS
- CHANGELOG is current through v2.30
</success_criteria>

<output>
After completion, create `.paul/phases/144-readme-documentation-update/144-01-SUMMARY.md`
</output>
