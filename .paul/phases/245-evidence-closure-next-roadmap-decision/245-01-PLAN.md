---
phase: 245-evidence-closure-next-roadmap-decision
plan: 01
type: execute
wave: 1
depends_on:
  - phase: 242-extension-extraction-target-baseline
    provides: `docs/PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md` approved-wave selection prose; PR #157 merged 2026-05-01.
  - phase: 243-bounded-submodule-extraction-wave
    provides: S1 `artifact-slice-rendering` extracted to `drivers/pi/extensions/artifact-slice-rendering.ts`; helpers exported from `pals-hooks.ts`; canonical default-arg cycle-avoidance pattern; PR #158 merged 2026-05-02.
  - phase: 244-documentation-validation-surfacing
    provides: S1 outcome surfaced in 5 docs (`README.md`, `drivers/pi/extensions/README.md`, `drivers/pi/skill-map.md`, `docs/PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md`, `docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md`); two closure guardrails (`Phase 244 sibling extraction surfacing (Pi-supported runtime)` and `Phase 244 sibling extraction surfacing (Shared Invariant)`); Pi `1..216/0`, cross-harness `1..128/0`; PR #159 merged 2026-05-02.
files_modified:
  - drivers/pi/extensions/module-activity-parsing.ts
  - drivers/pi/extensions/artifact-slice-rendering.ts
  - tests/pi-end-to-end-validation.sh
  - tests/cross-harness-validation.sh
  - .paul/MILESTONES.md
  - .paul/PROJECT.md
autonomous: true
risk_tier: risky
collaboration_level: high
planning_mode: direct-requirements
review_preference: tbd
---

# Phase 245 Plan 01: Evidence Closure + Next Roadmap Decision (v2.56)

<objective>
**Goal:** Close v2.56 Pi Extension Submodule Extraction Wave by (a) hotfixing the Phase 239/243 sibling-module Pi extension-loader regression (Pi auto-loads every `~/.pi/agent/extensions/*.ts` and expects each to default-export a factory function `(pi) => void`; the two helper siblings `module-activity-parsing.ts` and `artifact-slice-rendering.ts` only export named symbols and therefore fail Pi's extension loader at session start), (b) aggregating Phase 242–244 evidence into `MILESTONES.md` and `PROJECT.md`, (c) recording exactly one recommended next milestone (v2.57) focus, and (d) binding the new invariant with one new Pi e2e guardrail and one new cross-harness closure guardrail.

**Purpose:** Phase 245 mirrors the Phase 241 v2.55 closure shape with one scope addition: surface and fix a real product-reality bug discovered post-Phase-244. Pi runtime reports `Failed to load extension ".../artifact-slice-rendering.ts": Extension does not export a valid factory function` and the same for `module-activity-parsing.ts` at session start. The behavior of `pals-hooks.ts` (the only real extension) is unaffected because its named imports work fine; but every Pi session that loads PALS shows two loader errors. This is exactly the kind of evidence gap a closure phase should catch. The canonical fix is the same idiom Pi uses elsewhere for helper-only modules: append a no-op default-exported factory to each sibling, leaving named imports intact and behavior unchanged. Phase 245 then reconciles all four phases (242 baseline, 243 S1 extraction, 244 surfacing, 245 closure-with-loader-hotfix) into the milestones log, marks v2.56 complete, and routes the v2.57 decision to a future `/paul:milestone` invocation.

**Output:** No-op default-exported factory appended to `drivers/pi/extensions/module-activity-parsing.ts` and `drivers/pi/extensions/artifact-slice-rendering.ts` (named exports preserved verbatim; behavior unchanged); updated `.paul/MILESTONES.md` (v2.56 row added to Completed Milestones table; current-milestone pointer advanced; updated-for footer extended); updated `.paul/PROJECT.md` (Active Milestones table marks v2.56 as Complete; Key Decisions row appended; v2.57 recommendation surfaced); exactly one new Pi e2e `tap_file_contains_all` block titled `Phase 245 sibling extension loader-compat (Pi-supported runtime)` asserting both siblings carry a default-exported factory; exactly one new cross-harness `tap_file_contains_all` block titled `Phase 245 sibling extraction wave closure (v2.56 Shared Invariant)` asserting `MILESTONES.md` names v2.56 + Phases 242–245.
</objective>

<context>
**Project files:**
- `.paul/STATE.md` — Phase 245 ready to plan; v2.56 is 3 of 4 phases complete (Phases 242 + 243 + 244 closed via PR #157 / #158 / #159 respectively).
- `.paul/PROJECT.md` — v2.56 Active Milestones row currently reads "In Progress (3 of 4 phases)"; Key Decisions table already records Phase 242/243/244 entries.
- `.paul/ROADMAP.md` — Phase 245 row: "Reconcile v2.56 evidence, close the milestone, and decide the next roadmap focus." Phase 244 row already marked Complete.
- `.paul/MILESTONES.md` — Compacted milestones log; current-milestone pointer (line ~9) and Current/Future Milestone Pointer paragraph (line ~80) both still describe v2.56 as in-progress with Phase 244 as the next planning target; needs full closure update.
- `.paul/phases/241-evidence-closure-next-roadmap-decision/241-01-SUMMARY.md` — direct precedent (v2.55 closure phase). Same shape: aggregate evidence, record recommendation, no new source surface.
- `.paul/phases/242-extension-extraction-target-baseline/242-01-SUMMARY.md`, `.paul/phases/243-bounded-submodule-extraction-wave/243-01-SUMMARY.md`, `.paul/phases/244-documentation-validation-surfacing/244-01-SUMMARY.md` — read-only inputs for the v2.56 milestone aggregation.

**Source-set context (read-only orientation; no source edits in this plan):**
- `drivers/pi/extensions/pals-hooks.ts`, `drivers/pi/extensions/module-activity-parsing.ts`, `drivers/pi/extensions/artifact-slice-rendering.ts` — current 3-sibling Pi extension source set (unchanged this plan).
- `tests/pi-end-to-end-validation.sh` (216 assertions), `tests/cross-harness-validation.sh` (128 assertions) — current baselines; only cross-harness is touched (+1 closure guardrail).

**Precedent phase (Evidence Closure shape):**
- Phase 241 (v2.55 closer). Phase 241 also did install-surface generalization in the same plan, which Phase 245 does NOT need because Phase 244 already shipped the equivalent surfacing for v2.56. Phase 245 is therefore strictly lighter than Phase 241: pure aggregation + one closure guardrail.

**v2.56 evidence to aggregate (from prior SUMMARYs):**
- Phase 242: Extension extraction target baseline doc (`docs/PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md`); Pi 211→212; cross-harness 126→127; PR #157.
- Phase 243: S1 `artifact-slice-rendering` extracted to its own sibling Pi extension module (drivers/pi/extensions/artifact-slice-rendering.ts, 169 LOC); pals-hooks.ts shrank by ~135 LOC; helpers exported; default-arg cycle-avoidance pattern; Pi 212→214 (+2 vs +1 cosmetic); cross-harness unchanged at 127; install 2→3 files; PR #158.
- Phase 244: S1 outcome surfaced in README.md + drivers/pi/extensions/README.md + drivers/pi/skill-map.md + 2 contract docs; closure guardrails added (Pi +2, cross-harness +1); Pi 214→216; cross-harness 127→128; install unchanged at 3 files; PR #159.
- Phase 245 (this plan): pure aggregation + 1 cross-harness closure guardrail; Pi unchanged at 216; cross-harness 128→129; install unchanged at 3 files; PR #160 (expected).

**v2.57 recommendation — assumptions/alternatives surfaced for `high` collaboration:**
- **Recommended (one):** Continue the sibling-extraction wave with Plan 243-02 (S2 `workflow-resource-capsule-rendering`), since (a) it is structurally near-identical to S1 per `docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md`, (b) the helpers are already exported, (c) the canonical cycle-avoidance pattern is now named, and (d) `pals-hooks.ts` is still ~1415 LOC. The simplest "do more of what worked" path is to extract S2 next.
- **Alternatives** (not recommended for v2.57 by default; surface for the next `/paul:milestone` decision):
  - Local context-cost evidence (deferred since Phase 240/241).
  - Demo project preset for first-run UX (deferred since Phase 240/241).
  - S3 `guided-workflow-detection` (conditional secondary candidate per `docs/PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md`).
- The plan **records** the recommendation; final v2.57 selection happens via `/paul:milestone` after PR #160 merges.

**Anchors (verbatim phrases that should be searchable after closure):**
- `v2.56 Pi Extension Submodule Extraction Wave` (milestone name).
- `v2.56 closure marker (Shared Invariant)` (cross-harness closure-guardrail title).
- `Phase 245 sibling extraction wave closure` (cross-harness anchor sentence in the new guardrail).
- `Phase 242 + Phase 243 + Phase 244 + Phase 245` (the four phases that must remain referenced in `MILESTONES.md`).

**Pre-plan dispatch (advisory):**
- TODD: no spec/unit tests; existing TAP shell suites carry validation. Plan stays `execute`. Pi e2e gains +1 guardrail asserting the loader-compat invariant.
- IRIS/RUBY: 2 trivial source appends (no-op factory) + lifecycle-doc + 2 validation lines; no complexity introduced.
- DOCS: aggregation-only on `MILESTONES.md`/`PROJECT.md`; no broken cross-references introduced.
- DEAN/SETH/VERA/OMAR/PETE/REED/GABE/LUKE/ARIA/DANA: no in-scope surface.
- ARCH: appended no-op default export per sibling; named exports unchanged; import graph unchanged. PASS expected.
- DAVE: install surface unchanged at 3 files (Phase 241 source-set rule); no installer/`driver.yaml` edits.
- CODI: 2 symbol additions (default export per sibling), but neither is referenced from any other file; skip-no-symbols expected; if CODI does reach the new symbols it should report 0 call-sites.
- WALT: pre-apply baseline expected Pi 216/216, cross-harness 128/128. Post-apply expected: Pi 217/217 (+1; new loader-compat guardrail), cross-harness 129/129 (+1; v2.56 closure guardrail). Real-world fix evidence: after install, Pi session start no longer emits `Failed to load extension "…/module-activity-parsing.ts"` or `Failed to load extension "…/artifact-slice-rendering.ts"`.

**Pre-plan dispatch (enforcement):**
- DEAN p50: not applicable (no `package.json` change).
- Module dispatch evidence to be recorded in APPLY per `references/module-dispatch.md`.
</context>

<acceptance_criteria>

**AC-0: Sibling Pi extension loader-compat hotfix**

Given Pi auto-loads every `*.ts` file under `~/.pi/agent/extensions/` and expects each to default-export a factory function `(pi) => void`, and the two helper siblings `module-activity-parsing.ts` and `artifact-slice-rendering.ts` currently only export named symbols (causing Pi session start to emit `Failed to load extension ".../module-activity-parsing.ts": Extension does not export a valid factory function` and the same for `artifact-slice-rendering.ts`),
When `drivers/pi/extensions/module-activity-parsing.ts` and `drivers/pi/extensions/artifact-slice-rendering.ts` are read after this plan,
Then each file ends with a no-op default-exported factory function (e.g., `export default function (_pi: unknown): void {}`) preceded by a JSDoc comment explaining that the module is a helper consumed by `pals-hooks.ts` and the default export exists only to satisfy Pi's extension loader; every previously-existing named export and import in both files is preserved verbatim; `pals-hooks.ts` is not modified by this AC; after `PALS_ROOT="$PWD" bash drivers/pi/install.sh`, fresh Pi sessions no longer emit the two `Failed to load extension` errors for these basenames; install reports `[ok] Pi extensions installed: 3 files` (unchanged).

**AC-1: MILESTONES.md records v2.56 closure**

Given Phase 245 is the final v2.56 closure phase and `.paul/MILESTONES.md` currently describes v2.56 as in-progress (line ~9 + line ~80 + footer ~90),
When `.paul/MILESTONES.md` is read after this plan,
Then a new row for v2.56 Pi Extension Submodule Extraction Wave is appended to the Completed Milestones table (immediately after the v2.55 row), naming all four phases (242 baseline, 243 S1 extraction, 244 documentation/validation surfacing, 245 evidence closure) and referencing PRs #157/#158/#159/#160 plus this SUMMARY path; the current-milestone pointer (line ~9) is updated from "v2.56 ... active" to "no active milestone — v2.57 selection pending via /paul:milestone (recommended focus: continue sibling-extraction wave with Plan 243-02 S2 `workflow-resource-capsule-rendering`)"; the Current/Future Milestone Pointer paragraph is updated to mark v2.56 complete and surface the v2.57 recommendation; and the footer's "updated for ..." trail is extended with "updated for v2.56 milestone closure: 2026-05-02".

**AC-2: PROJECT.md reflects v2.56 complete and surfaces the v2.57 recommendation**

Given `.paul/PROJECT.md` currently lists v2.56 as "In Progress (3 of 4 phases)" in the Active Milestones table and Phase 244 in the Key Decisions table,
When `.paul/PROJECT.md` is read after this plan,
Then the Active Milestones row for v2.56 reads "Complete (4 of 4 phases) — closed 2026-05-02 via PR #160" (or the matching merged PR number); a new row in the Key Decisions table records the Phase 245 closure decision (recommended v2.57 focus + alternatives + the rule that final v2.57 selection happens via `/paul:milestone`); the Current Milestone pointer paragraph notes v2.56 is closed and v2.57 is pending; and the "Last updated" footer is bumped to "2026-05-02 after Phase 245 Evidence Closure UNIFY + transition (Phase 245 / PR #160); v2.56 Pi Extension Submodule Extraction Wave 4 of 4 phases complete".

**AC-3: Cross-harness suite carries exactly one new localized closure guardrail asserting v2.56 milestone closure**

Given Phase 245 is the durable closer for v2.56 and `MILESTONES.md` is the shared invariant truth across docs/contract,
When `bash tests/cross-harness-validation.sh` runs after this plan,
Then `tests/cross-harness-validation.sh` contains exactly one new `tap_file_contains_all` block titled `Phase 245 sibling extraction wave closure (v2.56 Shared Invariant)` that asserts the literal phrases `v2.56 Pi Extension Submodule Extraction Wave`, `Phase 242`, `Phase 243`, `Phase 244`, and `Phase 245` all appear in `.paul/MILESTONES.md`; the suite runs green at `1..129/0` (planned +1, single file under assertion); `bash tests/helpers/artifact_consistency.sh` PASSes; `git diff --check` is clean.

**AC-4: Pi e2e suite gains exactly the loader-compat guardrail and nothing else; only the two helper siblings are touched in the Pi runtime surface**

Given Phase 245 includes the sibling loader-compat hotfix (AC-0) and Phase 244 already shipped the Phase 243-outcome surfacing guardrail,
When `bash tests/pi-end-to-end-validation.sh` runs after this plan,
Then the Pi e2e suite contains exactly the new `Phase 245 sibling extension loader-compat (Pi-supported runtime)` blocks added by Task 3.1 (expected +2 due to multi-file split, precedent-aligned with Phase 244); the suite runs green at `1..218/0`; the only Pi runtime files modified are `drivers/pi/extensions/module-activity-parsing.ts` and `drivers/pi/extensions/artifact-slice-rendering.ts` (each gaining only the appended no-op default export and its JSDoc); `drivers/pi/extensions/pals-hooks.ts`, `drivers/pi/install.sh`, `drivers/pi/uninstall.sh`, `drivers/pi/driver.yaml`, `kernel/`, `modules/`, `package.json`, `tests/helpers/`, `.github/`, and `pals.json` are NOT modified.

**AC-5: Strict scope and v2.56 closure evidence**

Given Phase 245 is documentation-aggregation + sibling loader-compat hotfix + 2 validation lines,
When `git diff --name-only main..HEAD` is reviewed at the merge gate,
Then the changed-file set equals exactly: `drivers/pi/extensions/module-activity-parsing.ts`, `drivers/pi/extensions/artifact-slice-rendering.ts`, `tests/pi-end-to-end-validation.sh`, `tests/cross-harness-validation.sh`, `.paul/MILESTONES.md`, `.paul/PROJECT.md`, plus the standard lifecycle artifacts (`.paul/STATE.md`, `.paul/ROADMAP.md`, `.paul/phases/245-evidence-closure-next-roadmap-decision/245-01-PLAN.md`, `.paul/phases/245-evidence-closure-next-roadmap-decision/245-01-SUMMARY.md`); no `.paul/*` artifact authority is reassigned; no new contract docs are authored; `pals-hooks.ts`/installer/uninstaller/`driver.yaml`/`kernel/`/`modules/`/`pals.json`/`tests/helpers/`/`.github/` are NOT modified; install surface remains unchanged at `[ok] Pi extensions installed: 3 files`; and the SUMMARY records v2.56 closure evidence (Phase 242–245 outcomes, validation count reconciliation across all four phases including Pi 216→217 and cross-harness 128→129, install reports, the loader-compat hotfix evidence, and the v2.57 recommendation).
</acceptance_criteria>

<tasks>

<task id="0" name="Hotfix sibling Pi extension loader-compat (no-op default factory)">
  <files>
    <file>drivers/pi/extensions/module-activity-parsing.ts</file>
    <file>drivers/pi/extensions/artifact-slice-rendering.ts</file>
  </files>
  <action>
    Append exactly one no-op default-exported factory function block to the END of each file (after all existing named exports). The block MUST be:

    ```ts
    /**
     * No-op Pi extension factory.
     *
     * This module is a helper consumed by `drivers/pi/extensions/pals-hooks.ts`;
     * the default export exists only to satisfy Pi's extension loader, which
     * scans every `*.ts` file in `~/.pi/agent/extensions/` for a default-
     * exported factory function. Real extension behavior lives in
     * `pals-hooks.ts`. This factory MUST remain a no-op so it never mutates Pi
     * state, registers tools, subscribes to events, or owns lifecycle authority.
     *
     * Authority: Derived aid only.
     */
    export default function (_pi: unknown): void {}
    ```

    Do NOT modify any existing import, named export, type, or function in either file. Do NOT modify `pals-hooks.ts` (its named imports of `compactWhitespace`, `MAX_WIDGET_MODULE_DETAILS`, `renderArtifactSlices`, etc. continue to work because the no-op default does not shadow named exports). Do NOT add any runtime imports for Pi types — use `unknown` for the parameter so neither sibling acquires a new transitive dependency.

    After both files are amended, run `PALS_ROOT="$PWD" bash drivers/pi/install.sh` to deploy the updated siblings to `~/.pi/agent/extensions/` and confirm `[ok] Pi extensions installed: 3 files`.
  </action>
  <verify>
    `grep -c "^export default function" drivers/pi/extensions/module-activity-parsing.ts` = 1; `grep -c "^export default function" drivers/pi/extensions/artifact-slice-rendering.ts` = 1; `grep -c "^export default function" drivers/pi/extensions/pals-hooks.ts` = 1 (preservation — unchanged); `PALS_ROOT="$PWD" bash drivers/pi/install.sh 2>&1 | grep -c "\[ok\] Pi extensions installed: 3 files"` = 1; existing named-export count in each sibling unchanged from pre-edit count.
  </verify>
  <done>AC-0.</done>
</task>

<task id="1" name="Aggregate v2.56 closure evidence into MILESTONES.md">
  <files>
    <file>.paul/MILESTONES.md</file>
  </files>
  <action>
    1. Append a new row to the Completed Milestones table immediately after the existing `v2.55 Maintainability & Product Reality Hardening` row:
       - Milestone: `v2.56 Pi Extension Submodule Extraction Wave`
       - Completed: `2026-05-02`
       - Duration: `1 day` (or actual span)
       - Phases: `4 phases, 4 plans`
       - Notable artifacts: `docs/PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md`, `drivers/pi/extensions/artifact-slice-rendering.ts`, `.paul/phases/242-extension-extraction-target-baseline/242-01-SUMMARY.md`, `.paul/phases/243-bounded-submodule-extraction-wave/243-01-SUMMARY.md`, `.paul/phases/244-documentation-validation-surfacing/244-01-SUMMARY.md`, `.paul/phases/245-evidence-closure-next-roadmap-decision/245-01-SUMMARY.md`. Reference PRs #157, #158, #159, #160 (the merged Phase 245 PR; record the actual merged number during APPLY).
    2. Update the current-milestone pointer (the row near line 9 that currently reads `| Current milestone | v2.56 ... active ...`):
       - From: in-progress description naming Phase 244 as next.
       - To: `no active milestone — v2.57 selection pending via /paul:milestone (recommended focus: continue sibling-extraction wave with Plan 243-02 S2 \`workflow-resource-capsule-rendering\`)`. Reference PRs #157/#158/#159/#160 in the Source column.
    3. Update the Current/Future Milestone Pointer paragraph (line ~80–82):
       - Add a new closing-paragraph entry naming v2.56 Pi Extension Submodule Extraction Wave **complete** with all 4 phases shipped (242 baseline / 243 S1 extraction / 244 documentation/validation surfacing / 245 evidence closure), then state v2.57 is pending with the recommended focus + alternatives.
    4. Append `; updated for v2.56 milestone closure: 2026-05-02` to the footer trail (line ~90).
    5. Preserve all existing rows, columns, and prose verbatim — append/update only the four locations above. Do NOT touch the v2.55 row, archived-milestones index, or any prior milestone entry.
  </action>
  <verify>
    `grep -c "v2.56 Pi Extension Submodule Extraction Wave" .paul/MILESTONES.md` ≥ 3 (table row + pointer + paragraph); `grep -c "Phase 242" .paul/MILESTONES.md` ≥ 1; `grep -c "Phase 243" .paul/MILESTONES.md` ≥ 1; `grep -c "Phase 244" .paul/MILESTONES.md` ≥ 1; `grep -c "Phase 245" .paul/MILESTONES.md` ≥ 1; `grep -c "v2.56 milestone closure" .paul/MILESTONES.md` ≥ 1; `grep -c "v2.55 Maintainability & Product Reality Hardening" .paul/MILESTONES.md` unchanged (preservation).
  </verify>
  <done>AC-1.</done>
</task>

<task id="2" name="Mark v2.56 complete in PROJECT.md and surface v2.57 recommendation">
  <files>
    <file>.paul/PROJECT.md</file>
  </files>
  <action>
    1. Update the Active Milestones table row for v2.56: from `In Progress (3 of 4 phases)` → `Complete (4 of 4 phases) — closed 2026-05-02 via PR #160` (use the actual merged PR number).
    2. Update the Current Milestone pointer paragraph (line ~63): note v2.56 is closed; surface that v2.57 selection is pending via `/paul:milestone` with the recommended focus (continue sibling-extraction wave with Plan 243-02 S2 `workflow-resource-capsule-rendering`).
    3. Append a new row to the Key Decisions table recording the Phase 245 closure decision: "2026-05-02: v2.56 Pi Extension Submodule Extraction Wave closes with the sibling-module pattern repeated and validated (S5 + S1); the recommended v2.57 focus is to continue the wave with S2 `workflow-resource-capsule-rendering` (Plan 243-02), with alternatives (local context-cost evidence, demo project preset, S3 conditional extraction) preserved for the `/paul:milestone` decision". Source column: Phase 245 Plan 245-01 UNIFY. Impact column: lists the v2.56 outcomes and the v2.57 routing rule.
    4. Update the "Last updated" footer to `2026-05-02 after Phase 245 Evidence Closure UNIFY + transition (Phase 245 / PR #160); v2.56 Pi Extension Submodule Extraction Wave 4 of 4 phases complete`.
    5. Preserve all existing Active Milestones rows, Key Decisions rows, validated-requirement rows, and constraint prose verbatim — append/update only the four locations above.
  </action>
  <verify>
    `grep -c "Complete (4 of 4 phases)" .paul/PROJECT.md` = 1 (the v2.56 row); `grep -c "v2.57" .paul/PROJECT.md` ≥ 1; `grep -c "Phase 245 Evidence Closure UNIFY" .paul/PROJECT.md` ≥ 1; `grep -c "Plan 243-02" .paul/PROJECT.md` ≥ 1; existing Key Decisions row count increases by exactly 1.
  </verify>
  <done>AC-2.</done>
</task>

<task id="3" name="Add closure guardrails: Pi e2e loader-compat + cross-harness v2.56 milestone closure">
  <files>
    <file>tests/pi-end-to-end-validation.sh</file>
    <file>tests/cross-harness-validation.sh</file>
  </files>
  <action>
    1. Append exactly one new `tap_file_contains_all` block to `tests/pi-end-to-end-validation.sh` titled `Phase 245 sibling extension loader-compat (Pi-supported runtime)`. The block asserts that BOTH sibling helper files contain the literal phrase `export default function` (the canonical Pi extension factory marker). Because `tap_file_contains_all` takes a single file per call, emit the assertion as TWO `tap_file_contains_all` calls under the same shared title (precedent: Phase 244's multi-file surfacing) — one for `drivers/pi/extensions/module-activity-parsing.ts` and one for `drivers/pi/extensions/artifact-slice-rendering.ts`. Each block MUST also assert the shared phrase `No-op Pi extension factory` so the canonical-comment marker stays single-defined. Place the new blocks adjacent to the existing Phase 244 `Phase 244 sibling extraction surfacing (Pi-supported runtime)` blocks. Pi suite count delta: +2 (precedent-aligned with Phase 243/244 multi-file split).

    2. Append exactly one new `tap_file_contains_all` block to `tests/cross-harness-validation.sh` titled `Phase 245 sibling extraction wave closure (v2.56 Shared Invariant)` immediately before the SUMMARY echo block. The block asserts that `.paul/MILESTONES.md` contains all five literal phrases: `v2.56 Pi Extension Submodule Extraction Wave`, `Phase 242`, `Phase 243`, `Phase 244`, and `Phase 245`. Cross-harness suite count delta: +1.

    3. Do NOT modify any existing assertion, helper, or count outside the three new blocks. Do NOT edit `tests/helpers/`.
  </action>
  <verify>
    `grep -c "Phase 245 sibling extension loader-compat (Pi-supported runtime)" tests/pi-end-to-end-validation.sh` = 2 (one block per asserted file under shared title; OR 1 if a single block can target two files — record actual count); `grep -c "Phase 245 sibling extraction wave closure (v2.56 Shared Invariant)" tests/cross-harness-validation.sh` = 1; `bash tests/pi-end-to-end-validation.sh` exits 0 with `# Total:  218` (planned +2; expected actual due to multi-file split, mirroring Phase 244's multi-file pattern); `bash tests/cross-harness-validation.sh` exits 0 with `# Total:  129`; `bash tests/helpers/artifact_consistency.sh` reports `artifact_consistency_check: PASS`; `git diff --check` clean.
  </verify>
  <done>AC-0 (Pi guardrail half), AC-3, AC-4 (revised: Pi expected at 218, not 216), AC-5 (changed-file scope on the test side).</done>
</task>

</tasks>

<boundaries>

**DO NOT CHANGE:**
- `drivers/pi/extensions/pals-hooks.ts` (the only real Pi extension; behavior preserved verbatim).
- The existing imports, named exports, types, or function bodies in `drivers/pi/extensions/module-activity-parsing.ts` and `drivers/pi/extensions/artifact-slice-rendering.ts` (the loader-compat hotfix appends a no-op default export only).
- `drivers/pi/install.sh`, `drivers/pi/uninstall.sh`, `drivers/pi/driver.yaml`.
- `kernel/`, `modules/`, `pals.json`, `package.json`, `package-lock.json`, `.github/`, `tests/helpers/`.
- Any `docs/*.md` file (no contract docs touched; Phase 244 already surfaced the contract-doc closure markers).
- `README.md`, `drivers/pi/extensions/README.md`, `drivers/pi/skill-map.md` (Phase 244 already surfaced these).
- The existing v2.55 row in `.paul/MILESTONES.md` Completed Milestones table.
- The existing Active Milestones rows in `.paul/PROJECT.md` other than the v2.56 row.
- The existing Key Decisions rows in `.paul/PROJECT.md` (one new row appended only).
- Any archived-milestones file under `.paul/archive/`.
- The existing Phase 244 `Phase 244 sibling extraction surfacing (...)` blocks in either validation suite (Phase 245 only adds new blocks; it never modifies, renames, or removes Phase 244's blocks).

**SCOPE LIMITS:**
- Documentation aggregation + sibling loader-compat hotfix + 2 validation guardrails. No new contract docs. No installer/uninstaller/`driver.yaml`/dep/CI/GitHub-Flow/helper-delegation/lifecycle-authority changes.
- The v2.57 recommendation is **recorded**, not approved. Final v2.57 selection happens via `/paul:milestone` after PR #160 merges.
- S2 `workflow-resource-capsule-rendering` extraction is **not** initiated by this plan (the recommendation surfaces it for v2.57 only).
- Plan 243-02 is referenced as the natural next plan if v2.57 selects S2; it is not authored by this plan.
- Pi suite delta: +2 (multi-file `tap_file_contains_all` split, precedent-aligned with Phase 243/244). Cross-harness suite delta: +1 (single file under assertion). Total validation delta: Pi 216→218, cross-harness 128→129; install unchanged at 3 files.
</boundaries>

<verification>
1. **MILESTONES.md aggregation (AC-1):**
   - `grep -c "v2.56 Pi Extension Submodule Extraction Wave" .paul/MILESTONES.md` ≥ 3 (table row + pointer + paragraph).
   - `grep -c "Phase 242\|Phase 243\|Phase 244\|Phase 245" .paul/MILESTONES.md` each ≥ 1.
   - `grep -c "v2.56 milestone closure" .paul/MILESTONES.md` ≥ 1.
   - `grep -c "v2.55 Maintainability & Product Reality Hardening" .paul/MILESTONES.md` unchanged.

2. **PROJECT.md closure (AC-2):**
   - `grep -c "Complete (4 of 4 phases)" .paul/PROJECT.md` = 1.
   - `grep -c "v2.57" .paul/PROJECT.md` ≥ 1.
   - `grep -c "Plan 243-02" .paul/PROJECT.md` ≥ 1.
   - `grep -c "Phase 245 Evidence Closure UNIFY" .paul/PROJECT.md` ≥ 1.

3. **Validation counts (AC-3, AC-4):**
   - `bash tests/pi-end-to-end-validation.sh` → `1..218`, `# Total: 218`, `# Failed: 0` (+2: loader-compat split across two siblings).
   - `bash tests/cross-harness-validation.sh` → `1..129`, `# Total: 129`, `# Failed: 0` (+1: v2.56 closure marker).
   - `bash tests/helpers/artifact_consistency.sh` → `artifact_consistency_check: PASS`.

4. **Install surface unchanged (AC-5):**
   - `PALS_ROOT="$PWD" bash drivers/pi/install.sh` reports `[ok] Pi extensions installed: 3 files` (count unchanged from Phase 243/244; only the file contents of two siblings change).
   - Fresh Pi session start no longer emits `Failed to load extension ".../module-activity-parsing.ts"` or `Failed to load extension ".../artifact-slice-rendering.ts"`.
   - `git diff --name-only main...HEAD` lists exactly the six files in `files_modified` (plus the standard `.paul/*` lifecycle artifacts).
   - `git diff --check` clean.

5. **Module dispatch evidence (per `references/module-dispatch.md`):**
   - WALT pre-apply baseline: Pi 216/216, cross-harness 128/128. WALT post-apply: Pi 218/218 (+2; loader-compat split), cross-harness 129/129 (+1; v2.56 closure marker). Validation count reconciliation: +2 Pi (Phase 245 sibling extension loader-compat under shared title across two helper siblings; precedent-aligned with Phase 244's multi-file split), +1 cross-harness (Phase 245 v2.56 milestone-closure guardrail).
   - CODI: 2 symbol additions (one no-op default export per sibling); both have 0 call-sites; skipped-no-call-sites or skipped-no-symbols evidence expected.
   - ARCH: PASS — the no-op default exports do not import any new module and do not register hooks/tools/UI; import graph unchanged.
   - DAVE: PASS — install surface unchanged at 3 files; no installer/`driver.yaml` edits.
   - DOCS: PASS — aggregation applied to `MILESTONES.md` + `PROJECT.md`.
   - DEAN/SETH/IRIS/RUBY/PETE/REED/VERA/OMAR/LUKE/ARIA/GABE/DANA: no in-scope surface; advisory PASS.
</verification>

<success_criteria>
<!-- success_criteria recap below replaces the prior single-line summary -->
- AC-0, AC-1, AC-2, AC-3, AC-4, AC-5 all PASS.
- Validation: Pi `1..218 / # Failed: 0` (+2: loader-compat), cross-harness `1..129 / # Failed: 0` (+1: v2.56 closure), artifact_consistency PASS, `git diff --check` clean, install unchanged at 3 files, fresh Pi session start no longer emits the two `Failed to load extension` errors.
- Changed-file scope matches the six approved files (2 sibling sources + 2 test scripts + 2 lifecycle docs) plus `.paul/*` lifecycle artifacts; no drift.
- Phase 245 transitions to milestone-completion routing: v2.56 closes; v2.57 selection is deferred to `/paul:milestone` with the recommendation recorded but unapproved.
</success_criteria>

<output>
SUMMARY at `.paul/phases/245-evidence-closure-next-roadmap-decision/245-01-SUMMARY.md` recording AC-0–AC-5 results, sibling loader-compat hotfix evidence (no-op default factory in both helpers; fresh Pi session no longer emits the two `Failed to load extension` errors), focused grep evidence, installer verification (3 files), Pi 218/218, cross-harness 129/129, full v2.56 phase-by-phase aggregation (242 baseline → 243 S1 extraction → 244 surfacing → 245 closure-with-loader-hotfix), validation count reconciliation across all four phases (Pi 211→212→214→216→218 net +7; cross-harness 126→127→127→128→129 net +3), recorded v2.57 recommendation + alternatives, deferred items not done in v2.56, module reports, and PR merge-gate readiness for v2.56 milestone closure.
</output>

<module_dispatch>
Pre-plan advisory: TODD/IRIS/RUBY/DOCS/DEAN/CODI/ARCH/SETH/DAVE/WALT — all PASS or no-surface; AC-0 surfaces a real Pi runtime regression introduced in Phase 239 and not caught in Phase 243/244 because neither validation suite checked "every `~/.pi/agent/extensions/*.ts` carries a default-exported factory function"; the Phase 245 Pi e2e guardrail closes that gap. Pre-plan enforcement: DEAN p50 not applicable (no `package.json` change). Post-plan: TODD did not request restructure. Full dispatch evidence to be recorded in APPLY transcript per `references/module-dispatch.md`.
</module_dispatch>
