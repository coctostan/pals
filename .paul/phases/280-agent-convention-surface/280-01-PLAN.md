---
phase: 280-agent-convention-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kernel/templates/AGENTS.md
  - kernel/workflows/init-project.md
  - tests/pi-end-to-end-validation.sh
  - tests/cross-harness-validation.sh
autonomous: true
---

<objective>
## Goal
Add compact “functional-first, not functional-only” Engineering Principles to generated agent convention surfaces so new PALS projects receive practical default guidance without overriding project-specific conventions.

## Purpose
Phase 280 implements the agent-convention slice of `docs/PALS-FUNCTIONAL-FIRST-AGENT-GUIDANCE-CONTRACT.md`: make the generated `AGENTS.md` and init workflow carry lightweight, editable engineering guidance while keeping local project conventions authoritative.

## Output
- `kernel/templates/AGENTS.md` includes an editable `## Engineering Principles` section before `## Project Conventions`.
- `kernel/workflows/init-project.md` generates the same guidance for new projects and explicitly preserves Project Conventions authority.
- `tests/pi-end-to-end-validation.sh` and `tests/cross-harness-validation.sh` assert the installed template/workflow keep the required guidance markers.
</objective>

<context>
## Project Context
.paul/STATE.md — Phase 280 ready to plan after Phase 279 contract completion.
.paul/ROADMAP.md — v2.65 current milestone; Phase 280 focuses on generated AGENTS/init surfaces.
.paul/PROJECT.md — v2.65 uses contract-over-research authority; no new module or blocking FP gate expected.

## Prior Work
.paul/phases/279-functional-first-guidance-contract/279-01-SUMMARY.md — establishes `docs/PALS-FUNCTIONAL-FIRST-AGENT-GUIDANCE-CONTRACT.md` as authoritative for v2.65 implementation.

## Source Files
- docs/PALS-FUNCTIONAL-FIRST-AGENT-GUIDANCE-CONTRACT.md — Phase 280 allowed scope and expected guidance shape.
- kernel/templates/AGENTS.md — generated `AGENTS.md` template surface.
- kernel/workflows/init-project.md — init workflow that writes root `AGENTS.md`.
- tests/pi-end-to-end-validation.sh — Pi install validation markers.
- tests/cross-harness-validation.sh — cross-harness installed-resource parity markers.
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan enforcement: DEAN PASS — dependency audit tool unavailable (`python3 -m pip_audit` missing); no planned dependency manifest/package changes; no critical/high vulnerability evidence produced.
[dispatch] pre-plan advisory: TODD — no `*.test.*`/`*.spec.*` files detected; shell validation scripts are in scope and should receive marker assertions rather than TDD conversion.
[dispatch] pre-plan advisory: ARCH — markdown/workflow/test-shell scope only; no architecture layer concerns detected for `kernel/templates`, `kernel/workflows`, or `tests`.
[dispatch] pre-plan advisory: SETH — planned files contain existing literal “secrets/API keys/credentials” safety text only; no new secret material or auth/authz implementation planned.
[dispatch] pre-plan advisory: IRIS — no `TODO`, `FIXME`, `HACK`, or `XXX` markers in `kernel/templates/AGENTS.md` or `kernel/workflows/init-project.md`.
[dispatch] pre-plan advisory: GABE/DANA/LUKE/ARIA — skipped; no API, data model/migration, or UI files in scope.
[dispatch] pre-plan advisory: DAVE — no CI config present; validation remains local shell-script based (`tests/pi-end-to-end-validation.sh`, `tests/cross-harness-validation.sh`).
[dispatch] pre-plan advisory: DOCS — documentation/instruction-surface scope; `docs/PALS-FUNCTIONAL-FIRST-AGENT-GUIDANCE-CONTRACT.md` is the controlling related doc.
[dispatch] pre-plan advisory: OMAR/REED/VERA/PETE/RUBY — no observability/resilience/privacy/performance/debt implementation paths in scope; inspect only for accidental broadening during APPLY.
[dispatch] pre-plan advisory: CODI skipped — no explicit TS/JS selectors or stable code seeds in this markdown/shell plan.

## Post-Plan Module Dispatch
[dispatch] post-plan: TODD PASS — plan type is `execute`; no TDD candidates covering the planned files, so no RED-GREEN-REFACTOR rewrite suggested.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | PLAN/APPLY/UNIFY lifecycle management | ✓ |
| /carl | required | Session boundary monitoring | automatic |
| TODD | required | APPLY test/verification discipline | via module dispatch |
| WALT | required | APPLY quality gates and validation | via module dispatch |

**APPLY note:** Required lifecycle/module skills must remain active; no extra user-loaded specialized flow is required for this docs/workflow/test-shell plan.
</skills>

<acceptance_criteria>
## AC-1: Generated AGENTS Template Carries Advisory Engineering Principles
```gherkin
Given the PALS `kernel/templates/AGENTS.md` template
When a new project uses the generated `AGENTS.md` content
Then it includes a compact `## Engineering Principles` section that says project conventions remain authoritative and frames functional guidance as functional-first, not functional-only
```

## AC-2: Init Workflow Generates the Same Editable Guidance
```gherkin
Given `kernel/workflows/init-project.md` creates root `AGENTS.md`
When the `create_agents_md` step is followed
Then the generated markdown includes the same Engineering Principles section before Project Conventions and keeps Project Conventions explicitly user-owned/editable
```

## AC-3: Validation Guards the Installed Convention Surface
```gherkin
Given the Pi and cross-harness validation scripts install or inspect PALS runtime resources
When validation runs after APPLY
Then both scripts assert required Engineering Principles markers for the installed AGENTS template and init workflow so regressions are caught
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Add Engineering Principles to generated AGENTS surfaces</name>
  <files>kernel/templates/AGENTS.md, kernel/workflows/init-project.md</files>
  <action>
    Add a compact `## Engineering Principles` section immediately before `## Project Conventions` in both the template and the init workflow’s generated markdown block.
    Include these contract-backed ideas in concise bullets: functional-first, not functional-only; prefer boring/readable/testable code; project conventions remain authoritative; prefer pure functions for business logic/data transformation/validation/decisions; keep side effects explicit and near boundaries; prefer immutable data and explicit state/dependency passing where practical; keep changes minimal/local; do not add dependencies or architecture changes without approval.
    Update nearby init workflow prose so step 3 describes adding default Engineering Principles and leaving Project Conventions as user-owned guidance.
    Avoid: presenting functional style as a mandatory architecture, moving Project Conventions ahead of its authority statement, or touching brownfield/TODD/RUBY/ARCH module guidance in this phase.
  </action>
  <verify>Native read/grep checks show `## Engineering Principles`, `Functional-first, not functional-only`, and `Project Conventions remain authoritative` in both `kernel/templates/AGENTS.md` and `kernel/workflows/init-project.md`.</verify>
  <done>AC-1 and AC-2 satisfied: generated AGENTS surfaces carry advisory functional-first guidance while preserving local convention authority.</done>
</task>

<task type="auto">
  <name>Task 2: Add validation markers for installed resources</name>
  <files>tests/pi-end-to-end-validation.sh, tests/cross-harness-validation.sh</files>
  <action>
    Add focused TAP assertions near the existing init-workflow/installed-resource checks.
    In Pi validation, assert the installed AGENTS template and init workflow contain the core markers: `## Engineering Principles`, `Functional-first, not functional-only`, `Project Conventions remain authoritative`, and `Keep side effects explicit and near boundaries`.
    In cross-harness validation, assert both Claude Code and Pi installed AGENTS templates/init workflows contain the same markers.
    Avoid: weakening existing validation counts, replacing broad sections, or adding dependency/tool requirements.
  </action>
  <verify>`bash tests/pi-end-to-end-validation.sh` and `bash tests/cross-harness-validation.sh` report the new TAP checks as passing or expose only pre-existing baseline failures unrelated to these markers.</verify>
  <done>AC-3 satisfied: installed convention-surface regressions are covered by validation scripts.</done>
</task>

<task type="auto">
  <name>Task 3: Run focused and full phase verification</name>
  <files>kernel/templates/AGENTS.md, kernel/workflows/init-project.md, tests/pi-end-to-end-validation.sh, tests/cross-harness-validation.sh</files>
  <action>
    Run focused marker checks first, then the project validation scripts.
    Record exact command results for UNIFY, including any baseline-inherited failures already documented in STATE/ROADMAP.
    If validation failures are caused by the new markers or edited files, fix them before APPLY completes.
    Avoid: marking APPLY complete on failing new checks or expanding scope into Phase 281+ surfaces.
  </action>
  <verify>`bash tests/pi-end-to-end-validation.sh`; `bash tests/cross-harness-validation.sh`; focused grep/read evidence for marker presence in all four planned files.</verify>
  <done>All ACs satisfied and verification evidence is ready for `280-01-SUMMARY.md`.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- docs/PALS-FUNCTIONAL-FIRST-AGENT-GUIDANCE-CONTRACT.md — authoritative contract from Phase 279; cite it but do not rewrite it.
- kernel/commands/paul/map-codebase.md and kernel/templates/codebase/* — Phase 281 scope.
- modules/todd/* and modules/ruby/* — Phase 282 scope.
- modules/arch/* — Phase 283 optional advisory/closure scope.
- pals.json dependency/module configuration — no new module or gate for functional style.

## SCOPE LIMITS
- No new dependencies, linters, formatters, CI config, or runtime tooling.
- No broad refactors or style rewrites outside the generated AGENTS/init convention surface.
- Engineering Principles must remain advisory and editable, not a blocking quality gate.
- Validation changes are limited to marker-preservation checks for this phase’s installed resources.
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `kernel/templates/AGENTS.md` contains the required Engineering Principles markers.
- [ ] `kernel/workflows/init-project.md` contains the same generated AGENTS markers and updated create_agents_md prose.
- [ ] `tests/pi-end-to-end-validation.sh` contains Pi installed-resource marker assertions.
- [ ] `tests/cross-harness-validation.sh` contains cross-harness marker assertions for both installed drivers.
- [ ] `bash tests/pi-end-to-end-validation.sh` result captured.
- [ ] `bash tests/cross-harness-validation.sh` result captured.
- [ ] All acceptance criteria met.
</verification>

<success_criteria>
- Generated PALS `AGENTS.md` surfaces include compact functional-first Engineering Principles.
- Project-specific conventions remain explicitly authoritative and user-editable.
- Init workflow and template stay aligned.
- Validation guards the installed convention-surface markers without new dependencies or module gates.
- Phase 281/282/283 surfaces remain untouched.
</success_criteria>

<output>
After completion, create `.paul/phases/280-agent-convention-surface/280-01-SUMMARY.md` with changed files, validation results, module dispatch outcomes, and any deviations from this plan.
</output>
