---
phase: 285-paul-plannotator-bridge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - drivers/pi/extensions/plannotator-bridge.ts
  - drivers/pi/extensions/README.md
  - drivers/pi/skill-map.md
  - tests/pi-end-to-end-validation.sh
autonomous: true
---

<objective>
## Goal
Create the repo-source Pi extension bridge for optional Plannotator review requests, with helper exports for plan/code review, async result recovery, graceful unavailable/timeout handling, and marker validation for the new installed-resource surface.

## Purpose
Phase 285 turns the Phase 284 contract into a small, testable Pi runtime bridge without changing PAUL workflows yet. The bridge gives Phases 286–287 a bounded adapter they can call while preserving `.paul/*` lifecycle authority, PALS-owned opt-in prompts, and advisory-only Plannotator output.

## Output
- New sibling extension `drivers/pi/extensions/plannotator-bridge.ts` following the existing Pi extension sibling pattern and loader-compat invariant.
- Documentation updates naming the new sibling in the Pi extension source set and clarifying that Plannotator is optional/advisory and not lifecycle truth.
- Pi validation marker checks proving the bridge helper names, channels, graceful fallback posture, non-adoption boundaries, and no-op extension factory are present.
</objective>

<context>
## Project Context
.paul/STATE.md — Phase 285 PLAN target; Phase 284 complete and merged; Phase 285 should probe unresolved Plannotator details before implementation.
.paul/ROADMAP.md — Phase 285 focus: new sibling Pi extension `drivers/pi/extensions/plannotator-bridge.ts`, helper functions `requestPlanReview(planPath)`, `requestCodeReview(diffType)`, `awaitReviewResult(reviewId, timeoutMs)`, extension-presence graceful fallback, installed-resource marker checks, no workflow instruction changes.
.paul/PROJECT.md — core value remains minimal kernel, modular pals, universal drivers; Pi surfaces are derived aids only.

docs/PALS-PLANNOTATOR-UI-INTEGRATION-CONTRACT.md — authoritative for consumed event/result fields, non-adoption, sidecars, `savedPath`, opt-in, validation, and failure modes.

## Prior Work
.paul/phases/284-plannotator-integration-contract/284-01-SUMMARY.md — Phase 284 deferred extension-presence probing, exact `savedPath` format, and UI-abandoned timeout behavior to Phase 285. It also recorded the durable decision that Plannotator is an integration, not a PALS module, and no module-registry entry belongs to this milestone.

## Source Files
- drivers/pi/extensions/pals-hooks.ts — active Pi extension entry precedent and shared helper import pattern.
- drivers/pi/extensions/command-routing.ts — sibling extension + no-op factory precedent.
- drivers/pi/extensions/README.md — Pi extension install/source-set documentation.
- drivers/pi/skill-map.md — Pi adapter command/skill/source-set map.
- tests/pi-end-to-end-validation.sh — Pi-supported runtime marker checks.
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: TODD — no `*.test.*`, `*.spec.*`, `__tests__`, package manifest, Makefile, Cargo manifest, or known test framework detected in repo source. Treat Phase 285 as marker-validation-driven execute scope, not TDD by default.
[dispatch] pre-plan advisory: IRIS — planned files are new source/docs/test-script surfaces; no planned readable file content exists yet for `drivers/pi/extensions/plannotator-bridge.ts`; existing docs/test targets should be checked for TODO/FIXME/HACK/XXX during APPLY if touched.
[dispatch] pre-plan advisory: DAVE — no `.github/workflows`, Dockerfile, docker-compose, GitLab CI, or Jenkinsfile found; no CI generation is in Phase 285 scope. Use existing local validation scripts as command-output truth.
[dispatch] pre-plan advisory: DOCS — planned source file has related docs `drivers/pi/extensions/README.md` and `drivers/pi/skill-map.md`; update both so docs do not drift from the new bridge sibling.
[dispatch] pre-plan advisory: CODI — skipped-no-symbols at plan time; `plannotator-bridge.ts` is new and no stable top-level repo symbol exists before implementation.
[dispatch] pre-plan advisory: RUBY — no preliminary debt indicators in planned files; new bridge must stay small and avoid becoming lifecycle authority.
[dispatch] pre-plan advisory: ARCH — planned source is one sibling under `drivers/pi/extensions/`, matching the existing Pi extension sibling pattern; no cross-layer concerns identified.
[dispatch] pre-plan advisory: ARIA/LUKE — no UI files in planned changes.
[dispatch] pre-plan advisory: DANA/GABE/OMAR/PETE/REED/SETH/VERA — no data/API/observability/performance/resilience/security/privacy concerns beyond standard optional integration guardrails; bridge must avoid secret storage, hidden persistence, unbounded retries, and lifecycle writes.
[dispatch] pre-plan enforcement: DEAN PASS — no dependency manifests or lockfiles detected (`package.json`, `pnpm-lock.yaml`, `package-lock.json`, `yarn.lock`, `go.mod`, `Cargo.toml`, `requirements.txt`, `Pipfile`, `Gemfile` absent), so no dependency audit applies and no dependency changes are planned.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | All planning/applying/unifying lifecycle work | ✓ |
| /carl | required | Session boundary management via Pi runtime | ✓ |
| TODD | required | Before/during code implementation; marker-validation scope is non-TDD unless APPLY chooses otherwise | ✓ (module dispatch) |
| WALT | required | After implementation for quality/validation gates | ○ (APPLY) |

**BLOCKING:** APPLY must dispatch WALT validation before completion and must record TODD as skipped or applied with evidence.
</skills>

<acceptance_criteria>
## AC-1: Bridge sibling exposes contracted helpers and event surface
```gherkin
Given Phase 284's Plannotator contract is authoritative
When Phase 285 creates `drivers/pi/extensions/plannotator-bridge.ts`
Then the file exports `requestPlanReview`, `requestCodeReview`, and `awaitReviewResult`, uses the `plannotator:request` and `plannotator:review-result` channel strings, and preserves the no-op Pi extension factory loader-compat marker
```

## AC-2: Optional integration fails gracefully without lifecycle authority
```gherkin
Given Plannotator may be absent, unavailable, errored, or abandoned
When a bridge request cannot complete through Pi events
Then the bridge returns a typed unavailable/error/timeout/abandoned result instead of throwing by default, does not write `.paul/*`, does not adopt `savedPath`, ignores `agentSwitch` and `permissionMode`, and bounds any status retry/timeout behavior
```

## AC-3: Installed-resource docs and validation surface the new bridge
```gherkin
Given Pi installs every repo-owned `drivers/pi/extensions/*.ts` sibling
When Phase 285 adds the bridge sibling
Then `drivers/pi/extensions/README.md`, `drivers/pi/skill-map.md`, and `tests/pi-end-to-end-validation.sh` name the bridge, its helper/event markers, optional/advisory authority boundary, and validation classification without changing workflow instructions
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Implement bounded Plannotator bridge sibling</name>
  <files>drivers/pi/extensions/plannotator-bridge.ts</files>
  <action>
    Create a new sibling Pi extension module following existing sibling conventions:
    - Define explicit string constants for `PLANNOTATOR_REQUEST_CHANNEL = "plannotator:request"` and `PLANNOTATOR_REVIEW_RESULT_CHANNEL = "plannotator:review-result"`.
    - Export TypeScript types for handled/unavailable/error responses and normalized plan/code review results; keep `savedPath` advisory and `annotations` opaque.
    - Export helper functions with the contracted names: `requestPlanReview`, `requestCodeReview`, and `awaitReviewResult`. Use dependency injection or an options object for the Pi event surface so future workflow integration can call the helpers without hidden global lifecycle state.
    - For plan review, read the PALS-authored plan file from repo/workspace path, send only `planContent`, optional `planFilePath`, and `origin`, prepare/listen for `plannotator:review-result` before emitting when possible, and verify returned `reviewId` matches before consuming the result.
    - For code review, send only contract-approved payload fields (`diffType`, optional default branch/VCS/cwd/PR context) and consume only `approved` and `feedback` by default.
    - Implement bounded graceful handling for absent listener/no callback, `unavailable`, `error`, timeout, `review-status: pending/missing`, and UI-abandoned classification. Avoid unbounded loops and avoid throwing by default for optional integration absence.
    - Include comments/markers stating that `savedPath` is advisory only, `agentSwitch`/`permissionMode` are ignored, no `.paul/*` lifecycle artifacts are written, and Plannotator cannot approve or route PAUL lifecycle work.
    - End with `// No-op Pi extension factory` and `export default function (_pi: unknown): void {}`.
    Avoid: modifying `pals-hooks.ts` or workflow instruction files in this phase; those are Phase 286/287 integration surfaces.
  </action>
  <verify>grep markers in `drivers/pi/extensions/plannotator-bridge.ts` for `requestPlanReview`, `requestCodeReview`, `awaitReviewResult`, `plannotator:request`, `plannotator:review-result`, `review-status`, `savedPath`, `agentSwitch`, `permissionMode`, `.paul/*`, and `No-op Pi extension factory`; run `bash -n tests/pi-end-to-end-validation.sh` after test edits.</verify>
  <done>AC-1 and AC-2 satisfied: helper/event markers exist, fallback behavior is represented in source, and no lifecycle-authority writes are introduced.</done>
</task>

<task type="auto">
  <name>Task 2: Surface bridge in Pi docs and adapter map</name>
  <files>drivers/pi/extensions/README.md, drivers/pi/skill-map.md</files>
  <action>
    Update the Pi extension README and skill map where they enumerate repo-owned `drivers/pi/extensions/*.ts` siblings:
    - Add `plannotator-bridge.ts` to current sibling lists and path strategy/install-boundary prose.
    - Add a compact Plannotator bridge note explaining that it is optional/advisory, consumes only the Phase 284 contract surface, writes no `.paul/*` lifecycle truth, never adopts `savedPath`, ignores `agentSwitch`/`permissionMode`, and is not a workflow instruction change yet.
    - Preserve existing derived-aid/no hidden Pi state/no UI-only lifecycle decisions language.
    Avoid: adding new `/paul:*` commands, Plannotator module entries, workflow prompts, or `pals.json` defaults in this phase.
  </action>
  <verify>grep `drivers/pi/extensions/README.md` and `drivers/pi/skill-map.md` for `plannotator-bridge.ts`, `savedPath`, `agentSwitch`, `permissionMode`, `advisory`, and `.paul/*` authority language.</verify>
  <done>AC-3 satisfied for docs: install/source-set documentation and adapter map include the new bridge without expanding lifecycle authority.</done>
</task>

<task type="auto">
  <name>Task 3: Add Pi marker validation for bridge contract</name>
  <files>tests/pi-end-to-end-validation.sh</files>
  <action>
    Add focused marker checks to `tests/pi-end-to-end-validation.sh` near the existing Pi extension structural validity section:
    - Define `EXT_PLANNOTATOR_BRIDGE="$REPO_ROOT/drivers/pi/extensions/plannotator-bridge.ts"` with the other extension sibling variables.
    - Assert the bridge file contains helper names, event channels, `review-status`, optional/unavailable/error/timeout markers, advisory `savedPath`, ignored `agentSwitch`/`permissionMode`, no `.paul/*` lifecycle authority marker, and no-op extension factory marker.
    - Assert `drivers/pi/extensions/README.md` and `drivers/pi/skill-map.md` mention `plannotator-bridge.ts` and the advisory/non-authoritative boundary.
    - Keep checks marker-based and local to Phase 285; do not require Plannotator to be installed and do not run browser/UI flows in validation.
  </action>
  <verify>bash -n tests/pi-end-to-end-validation.sh && bash tests/pi-end-to-end-validation.sh (record pass/fail counts; known inherited failures may remain if identical to baseline) && bash tests/cross-harness-validation.sh</verify>
  <done>AC-3 satisfied for validation: marker checks protect the new bridge surface while preserving existing validation-script syntax and command-output truth.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- Installed runtime copies under `~/.pi/agent/extensions/*` or `~/.pi/agent/skills/pals/*`.
- `/paul:plan`, `/paul:apply`, `/paul:unify`, or other workflow instruction files; Phase 286/287 own workflow prompt/sidecar integration.
- `pals.json` Plannotator defaults or project-level enablement; user-driven opt-in remains documented but not configured in this phase.
- PALS module registry (`modules.yaml`) or module manifests; Plannotator is an integration, not a PALS module.
- Plannotator `--plan` mode, `plannotator_submit_plan`, `plannotator.json`, internal phase machine, archive/history/note-export behavior, or `/paul:annotate` wrappers.

## SCOPE LIMITS
- This plan creates the bridge helper surface and marker/docs validation only; it does not invoke Plannotator during PAUL workflows.
- `savedPath` may be returned/recorded by helper types as advisory data only; do not parse, copy, or adopt it as a PALS artifact in this phase.
- Empirical installed-package/resource inspection is allowed only as read-only evidence for safe bridge behavior; do not edit installed resources.
- No new dependencies, no CI generation, no browser automation, and no cross-harness driver support.
</boundaries>

<verification>
Before declaring APPLY complete:
- [ ] `git diff --check`
- [ ] `bash -n tests/pi-end-to-end-validation.sh`
- [ ] `bash -n tests/cross-harness-validation.sh`
- [ ] `bash tests/pi-end-to-end-validation.sh` with result counts recorded; any inherited failures classified with before/after evidence
- [ ] `bash tests/cross-harness-validation.sh`
- [ ] Focused marker grep evidence for bridge helper/channel/non-authority markers and docs mentions
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- `drivers/pi/extensions/plannotator-bridge.ts` exists and exposes the contracted helper/event surface with graceful optional-integration semantics.
- Pi docs and skill map include the bridge sibling and preserve advisory/derived-aid authority boundaries.
- Validation scripts include bridge marker checks and remain syntactically valid.
- No workflow instructions, `.paul/*` lifecycle authority semantics, module registry, dependencies, or installed runtime files are changed during APPLY.
</success_criteria>

<output>
After completion, create `.paul/phases/285-paul-plannotator-bridge/285-01-SUMMARY.md` documenting implementation, validation output, known inherited validation failures if any, and handoff notes for Phase 286 workflow integration.
</output>
