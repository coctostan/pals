---
phase: 225-evidence-reconciliation-milestone-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/PI-NATIVE-LEGACY-RETENTION-INSTALL-SURFACE-CLEANUP-CONTRACT.md
  - tests/pi-end-to-end-validation.sh
  - tests/cross-harness-validation.sh
autonomous: true
---

<objective>
## Goal
Reconcile final v2.52 Legacy Retention / Install-Surface Cleanup evidence and prepare the milestone for closure by recording the Phase 222-224 evidence chain, PR #137/#138/#139 handoff, final validation-count rationale, and closure-specific command checks.

## Purpose
v2.52 cleaned install-surface posture only where validation classification made it safe. The milestone now needs a bounded closure pass that preserves command-output truth, proves no Shared Invariant coverage was lost, and makes the final evidence chain explicit before UNIFY closes the milestone.

## Output
- Updated cleanup contract closure evidence in `docs/PI-NATIVE-LEGACY-RETENTION-INSTALL-SURFACE-CLEANUP-CONTRACT.md`.
- Exactly one localized v2.52 closure guardrail added to `tests/pi-end-to-end-validation.sh` and exactly one to `tests/cross-harness-validation.sh`.
- Expected validation count reconciliation: Pi validation `204→205`; cross-harness validation `120→121`.
- UNIFY output after APPLY: `.paul/phases/225-evidence-reconciliation-milestone-closure/225-01-SUMMARY.md`.
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md

## Current milestone and phase inputs
.paul/phases/222-legacy-install-surface-cleanup-contract-baseline/222-01-SUMMARY.md
.paul/phases/223-safe-cleanup-implementation/223-01-SUMMARY.md
.paul/phases/224-documentation-validation-surfacing/224-01-SUMMARY.md

## Source Files
docs/PI-NATIVE-LEGACY-RETENTION-INSTALL-SURFACE-CLEANUP-CONTRACT.md
tests/pi-end-to-end-validation.sh
tests/cross-harness-validation.sh
</context>

<module_dispatch>
## Pre-Plan Module Dispatch

Planning collaboration level: high (project default from `pals.json`).
Planning mode: direct-requirements; the handoff and ROADMAP route explicitly to Phase 225 evidence reconciliation and milestone closure.

- `[dispatch] pre-plan advisory: modules.yaml loaded` from `/Users/maxwellnewman/.pi/agent/skills/pals/modules.yaml` with kernel version `2.0.0`; `pals.json` schema version matches.
- `[dispatch] pre-plan advisory: TODD` found no repo-local `*.test.*`, `*.spec.*`, or `__tests__` directories outside ignored tool/vendor state, but the project has shell validation suites `tests/pi-end-to-end-validation.sh` and `tests/cross-harness-validation.sh`; `execute` plan remains appropriate.
- `[dispatch] pre-plan advisory: IRIS` scanned planned files for `TODO|FIXME|HACK|XXX`; no findings.
- `[dispatch] pre-plan advisory: DAVE` found no repo CI config outside ignored tool/vendor state; this plan does not modify CI. GitHub Flow PR checks remain merge-gate evidence during UNIFY.
- `[dispatch] pre-plan advisory: DOCS` found 103 repo docs outside `.paul/*`; planned docs work is localized to the cleanup contract near related Pi-native contracts.
- `[dispatch] pre-plan advisory: CODI skipped — no extractable symbols in phase scope`; planned source selectors are Markdown and shell validation scripts, not TS/JS symbols.
- `[dispatch] pre-plan advisory: RUBY` noted planned file sizes: cleanup contract 182 lines, Pi validation script 1693 lines, cross-harness script 1277 lines. Validation-script size debt is pre-existing/future driver-quality scope; this plan must keep edits localized.
- `[dispatch] pre-plan advisory: ARCH` detected a documentation/process repo with `docs/`, `drivers/`, `kernel/`, `modules/`, and `tests/` layers. Planned files are in expected docs/tests layers. ARCH warns validation scripts are existing large files; no broad refactor is approved.
- `[dispatch] pre-plan advisory: ARIA/LUKE/DANA/GABE/OMAR/PETE/REED/VERA` skipped or found no relevant UI, design, data, API, observability, performance, resilience, or privacy implementation surface in the planned files.
- `[dispatch] pre-plan enforcement: DEAN PASS` no dependency manifests/lockfiles were found for an applicable package-manager audit, and this plan does not modify dependencies.
- `[dispatch] pre-plan enforcement: SETH PASS` secret-pattern scan over planned files found no hardcoded secret markers.

## Post-Plan Module Dispatch

- `[dispatch] post-plan: TODD` execute plan is appropriate; shell validation suites and focused marker checks are the proof path. No RED/GREEN/REFACTOR restructuring suggested.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | PLAN/APPLY/UNIFY lifecycle management | ✓ |
| /carl | required | Session boundary monitoring via Pi extension | active |
| TODD | required | Test/validation enforcement during APPLY | registry-loaded |
| WALT | required | Quality gating and validation during APPLY/UNIFY | registry-loaded |

**BLOCKING:** Required skills/modules must remain active before APPLY proceeds.
</skills>

<acceptance_criteria>

## AC-1: v2.52 closure evidence is reconciled in the cleanup contract
```gherkin
Given v2.52 has completed Phases 222, 223, and 224 with PR #137, PR #138, and PR #139 merged
When Phase 225 updates the cleanup contract
Then the contract records the final evidence chain, validation count progression, command-output truth boundary, module/GitHub Flow evidence expectations, and remaining deferred boundaries for milestone closure
```

## AC-2: Closure guardrails are localized and count-reconciled
```gherkin
Given Pi validation currently passes 204/204 and cross-harness validation currently passes 120/120 after Phase 224
When Phase 225 adds closure checks
Then each suite gains exactly one localized v2.52 closure guardrail, with Pi validation expected to move 204→205 and cross-harness validation expected to move 120→121 for evidence-reconciliation coverage only
```

## AC-3: Command-output truth and protected boundaries remain intact
```gherkin
Given docs, reports, summaries, helper reports, widgets, and copied validation counts are non-authoritative
When Phase 225 records closure evidence
Then fresh focused marker checks, installer output, Pi validation output, cross-harness validation output, and GitHub Flow evidence remain the authoritative proof path, with no runtime, installer, dependency, CI, helper-delegation, GitHub Flow automation, or lifecycle-authority behavior changes
```

## AC-4: Final verification is green and scoped
```gherkin
Given the Phase 225 APPLY changes are complete
When verification runs
Then focused marker checks pass, `PALS_ROOT="$PWD" bash drivers/pi/install.sh` passes, Pi validation passes 205/205, cross-harness validation passes 121/121, and changed files remain limited to approved contract/test files plus normal `.paul/*` lifecycle artifacts
```
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add v2.52 closure evidence to the cleanup contract</name>
  <files>docs/PI-NATIVE-LEGACY-RETENTION-INSTALL-SURFACE-CLEANUP-CONTRACT.md</files>
  <action>
    Add a bounded Phase 225 / v2.52 closure evidence section to the cleanup contract.
    - Record the Phase 222 baseline contract evidence and PR #137.
    - Record the Phase 223 Pi-first install/uninstall implementation evidence, stable Pi 203/203 and cross-harness 119/119 validation, and PR #138.
    - Record the Phase 224 docs/report/validation surfacing evidence, Pi 204/204 and cross-harness 120/120 validation, and PR #139.
    - State that Phase 225 closure adds evidence-reconciliation guardrails only, with expected count movement Pi 204→205 and cross-harness 120→121.
    - Preserve the command-output truth boundary: copied counts and docs explain posture but do not prove current pass/fail status.
    - Preserve remaining deferred boundaries: no broader legacy removal, validation cleanup, runtime behavior change, installer behavior change, dependency change, CI change, helper-delegation change, GitHub Flow automation change, or lifecycle-authority change.
  </action>
  <verify>grep -n "Phase 225\|v2.52 closure\|PR #137\|PR #138\|PR #139\|204→205\|120→121\|command-output truth\|deferred" docs/PI-NATIVE-LEGACY-RETENTION-INSTALL-SURFACE-CLEANUP-CONTRACT.md</verify>
  <done>AC-1 and AC-3 satisfied: final cleanup closure evidence is explicit and non-authoritative boundaries remain intact.</done>
</task>

<task type="auto">
  <name>Task 2: Add localized v2.52 closure guardrails to validation suites</name>
  <files>tests/pi-end-to-end-validation.sh, tests/cross-harness-validation.sh</files>
  <action>
    Add exactly one localized closure guardrail to each validation suite.
    - In `tests/pi-end-to-end-validation.sh`, add one check that proves the cleanup contract exposes v2.52 closure evidence, PR #137/#138/#139, command-output truth, and the Pi count reconciliation `204→205`.
    - In `tests/cross-harness-validation.sh`, add one check that proves shared closure evidence remains classified as Shared Invariant / Artifact / Process / Frozen Legacy Parity-safe where applicable, exposes PR #137/#138/#139, command-output truth, and the cross-harness count reconciliation `120→121`.
    - Update each TAP plan total by exactly one: Pi `1..204` to `1..205`; cross-harness `1..120` to `1..121`.
    - Do not reorganize, weaken, delete, or reclassify existing validation checks.
  </action>
  <verify>grep -n "1..205\|204→205\|v2.52 closure\|PR #137\|PR #138\|PR #139" tests/pi-end-to-end-validation.sh && grep -n "1..121\|120→121\|v2.52 closure\|PR #137\|PR #138\|PR #139" tests/cross-harness-validation.sh</verify>
  <done>AC-2 and AC-3 satisfied: each suite gains exactly one localized closure guardrail and count movement is reconciled.</done>
</task>

<task type="auto">
  <name>Task 3: Run final validation and scope checks for closure readiness</name>
  <files>docs/PI-NATIVE-LEGACY-RETENTION-INSTALL-SURFACE-CLEANUP-CONTRACT.md, tests/pi-end-to-end-validation.sh, tests/cross-harness-validation.sh</files>
  <action>
    Run the full proof path for Phase 225 APPLY.
    - Run focused marker greps from Tasks 1 and 2.
    - Run `PALS_ROOT="$PWD" bash drivers/pi/install.sh` to preserve Pi install/output proof.
    - Run `bash tests/pi-end-to-end-validation.sh` and confirm 205/205 with zero failures.
    - Run `bash tests/cross-harness-validation.sh` and confirm 121/121 with zero failures.
    - Run changed-file scope checks and confirm non-`.paul/` source changes are limited to the approved cleanup contract and validation suites.
    - Capture enough command-output evidence for UNIFY to record module, validation, count reconciliation, and GitHub Flow closure evidence.
  </action>
  <verify>PALS_ROOT="$PWD" bash drivers/pi/install.sh && bash tests/pi-end-to-end-validation.sh && bash tests/cross-harness-validation.sh && git diff --name-only</verify>
  <done>AC-4 satisfied: validation is green, count changes are expected, and scope remains bounded.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `install.sh` and `uninstall.sh` runtime behavior.
- `drivers/pi/**`, `drivers/claude-code/**`, and `drivers/agent-sdk/**` runtime/source behavior.
- `kernel/**`, `modules/**`, `.pi/agents/pals-implementer.md`, dependency manifests/lockfiles, CI configuration, GitHub Flow automation, and helper-delegation semantics.
- Existing validation checks except for adding exactly one localized closure guardrail per suite and updating the TAP totals accordingly.
- `.paul/archive/**` historical evidence.

## SCOPE LIMITS
- Evidence reconciliation and milestone closure only; no additional legacy cleanup or install-surface behavior change is approved.
- Validation count changes are limited to Pi 204→205 and cross-harness 120→121 for closure guardrails only.
- Docs/report/copy-count evidence remains explanatory and non-authoritative; fresh command output remains pass/fail truth.
- UNIFY may update `.paul/*` lifecycle artifacts and histories, but APPLY source edits stay limited to the listed contract and validation files.
</boundaries>

<verification>
Before declaring plan complete:
- [ ] Focused cleanup-contract marker grep passes for Phase 225/v2.52 closure evidence, PR #137/#138/#139, command-output truth, deferred boundaries, and count reconciliation.
- [ ] Focused validation-suite marker greps pass for `1..205`, `204→205`, `1..121`, `120→121`, closure evidence, and PR #137/#138/#139.
- [ ] `PALS_ROOT="$PWD" bash drivers/pi/install.sh` passes.
- [ ] `bash tests/pi-end-to-end-validation.sh` passes 205/205 with zero failures.
- [ ] `bash tests/cross-harness-validation.sh` passes 121/121 with zero failures.
- [ ] Changed-file scope is limited to approved contract/test files plus normal `.paul/*` lifecycle artifacts.
- [ ] All acceptance criteria met.
</verification>

<success_criteria>
- Final v2.52 closure evidence is source-cited and command-output-backed.
- Pi validation passes 205/205 and cross-harness validation passes 121/121 after exactly one localized closure guardrail is added to each suite.
- No runtime, installer, dependency, CI, helper delegation, GitHub Flow automation, lifecycle-authority, or broader legacy cleanup behavior changes occur.
- Phase 225 SUMMARY can close v2.52 and hand off future driver-quality/validation-script size debt as deferred scope.
</success_criteria>

<output>
After completion, create `.paul/phases/225-evidence-reconciliation-milestone-closure/225-01-SUMMARY.md` during UNIFY with AC results, command-output evidence, validation count reconciliation (Pi 204→205, cross-harness 120→121), module execution reports, GitHub Flow PR/CI/merge evidence, scope checks, deferred boundaries, and milestone closure status.
</output>
