---
phase: 02-fork-restructure
plan: 03
type: execute
wave: 1
depends_on: ["02-01"]
files_modified:
  - commands/pals/*.md (25 files)
autonomous: true
---

<objective>
## Goal
Migrate all 25 PAUL skill commands into commands/pals/, rewriting /paul: → /pals: namespace and @-references to ~/.pals/ paths. Skip deprecated status.md.

## Purpose
Completes Phase 2 — after this, all PAUL framework files are in the PALS repo with updated references. The skill commands are the user-facing entry points that invoke workflows.

## Output
- 25 skill command files in commands/pals/
- All /paul: → /pals: in command text and cross-references
- All @-references updated to ~/.pals/ paths
- status.md excluded (deprecated, replaced by progress.md)
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md

## Prior Work
@.paul/phases/02-fork-restructure/02-01-SUMMARY.md — directory structure + workflows
@.paul/phases/02-fork-restructure/02-02-SUMMARY.md — references, templates, rules

## Source
@.paul/phases/01-architecture-design/pals-structure-design.md — section 4.5 defines file list
</context>

<skills>
No specialized flows required — file migration work.
</skills>

<acceptance_criteria>

## AC-1: All Skill Commands Migrated
```gherkin
Given the 26 source command files at ~/.claude/commands/paul/
When the migration task completes
Then 25 files exist under ~/claude/pals/commands/pals/
And status.md is NOT present (deprecated)
```

## AC-2: Namespace and References Rewritten
```gherkin
Given the 25 copied command files
When inspecting their contents
Then no references to ~/.claude/paul-framework/ remain
And no references to /paul: remain (replaced with /pals:)
And all @-references point to ~/.pals/ paths
```

## AC-3: Command Cross-References Consistent
```gherkin
Given the copied command files
When checking internal cross-references
Then /pals:plan, /pals:apply, /pals:unify etc. are used consistently
And no orphan /paul: references exist
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Copy, rename namespace, and rewrite skill commands</name>
  <files>commands/pals/*.md (25 files)</files>
  <action>
    1. Copy all 26 files from ~/.claude/commands/paul/ to ~/claude/pals/commands/pals/
    2. Delete status.md from destination (deprecated — replaced by progress.md)
    3. Apply replacements to all 25 remaining files:
       - @~/.claude/paul-framework/{dir}/ → @~/.pals/{dir}/
       - ~/.claude/paul-framework/ → ~/.pals/
       - /paul: → /pals: (namespace rename in command text and cross-refs)
    4. Do NOT touch CARL commands (Phase 4)
  </action>
  <verify>
    1. ls ~/claude/pals/commands/pals/*.md | wc -l → 25
    2. ls ~/claude/pals/commands/pals/status.md → should not exist
    3. grep -rl "paul-framework" ~/claude/pals/commands/pals/ → empty
    4. grep -rl "/paul:" ~/claude/pals/commands/pals/ → empty
    5. grep -rl "/pals:" ~/claude/pals/commands/pals/ | wc -l → > 0
    6. Spot-check: plan.md, apply.md, help.md for content integrity
  </verify>
  <done>AC-1, AC-2, AC-3 satisfied</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- workflows/, references/, templates/, rules/ (already migrated)
- .paul/ directory (project state)
- Source files at ~/.claude/commands/paul/ (read-only)
- commands/carl/ directory (Phase 4)

## SCOPE LIMITS
- PAUL commands only — no CARL commands
- No install script or symlink wiring (Phase 5)
- No TODD/WALT merging (Phase 3)

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `ls ~/claude/pals/commands/pals/*.md | wc -l` returns 25
- [ ] `test ! -f ~/claude/pals/commands/pals/status.md` passes
- [ ] `grep -rl "paul-framework" ~/claude/pals/commands/pals/` returns nothing
- [ ] `grep -rl "/paul:" ~/claude/pals/commands/pals/` returns nothing
- [ ] Spot-check plan.md, apply.md, help.md for integrity
</verification>

<success_criteria>
- 25 command files migrated (status.md excluded)
- Zero old path references
- Zero old namespace references
- Phase 2 fully complete after this plan
</success_criteria>

<output>
After completion, create `.paul/phases/02-fork-restructure/02-03-SUMMARY.md`
</output>
