---
phase: 255-documentation-validation-surfacing-v2-59
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - drivers/pi/extensions/README.md
  - drivers/pi/skill-map.md
  - docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md
  - docs/PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md
  - docs/PI-NATIVE-PALS-CONTEXT-INJECTION-EXTRACTION-CONTRACT.md
  - tests/pi-end-to-end-validation.sh
autonomous: true
---

<objective>
## Goal
Surface the Phase 254 S7 `pals-context-injection` sibling Pi extension extraction across the user-facing Pi docs (root README, Pi extensions README, Pi skill-map), the modularization-contract docs (modularization contract, extraction-target baseline, S7 extraction contract), and the Pi validation suite, without changing any source files, runtime behavior, or installed-runtime artifacts. The surfacing must continue to honor every preserved deferral (S4 canonical reply delivery, S6 lifecycle UI, S8 command routing, full Claude Code / Agent SDK driver removal, broader cross-harness size-debt) and every consumer-visible authority/activation tag and message-stream housekeeping invariant established by Phase 253 + Phase 254.

## Purpose
This is the v2.59 documentation + validation surfacing phase, mirroring the Phase 244 (S1) / Phase 247 (S2) / Phase 251 (S3) precedents one-to-one. After Phase 254 closed PR #169 and added `drivers/pi/extensions/pals-context-injection.ts` to the source set, the user-facing docs still describe "5 siblings"; the modularization contract docs still terminate at the Phase 253 outcome section; the S7 contract doc still terminates at the Phase 253 status section; and the Pi validation suite still holds no surfacing TAP guardrails for S7. Phase 255 closes those gaps so Phase 256 can run a clean evidence-closure pass.

## Output
- Updated user-facing surfaces naming `pals-context-injection.ts` alongside the prior siblings: `README.md` (Pi-supported-runtime preamble, extension source-set list, default-supported-target list), `drivers/pi/extensions/README.md` (extension source-set narrative, sibling list, source-set rule), `drivers/pi/skill-map.md` (extensions surface, source-set narrative).
- Updated contract docs adding bounded `## Phase 254 Outcome` sections: `docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md`, `docs/PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md`. Updated `docs/PI-NATIVE-PALS-CONTEXT-INJECTION-EXTRACTION-CONTRACT.md` adding `## Status After Phase 254`.
- Updated `tests/pi-end-to-end-validation.sh` adding three bounded surfacing TAP assertions (modeled on the Phase 251 `tap_file_contains_all` blocks) proving each user-facing surface names `pals-context-injection.ts` and preserves the "S4 canonical reply delivery" / "Authority: Derived aid only" / "Pi is the supported runtime" boundary phrasings.
- All preserved markers, deferrals, and authority phrasings remain unchanged everywhere they currently appear.
</objective>

<context>
## Project Context
- `.paul/PROJECT.md` — v2.59 active, milestone 2 of 4 phases complete; Phase 254 closure decision row records the type-only back-import + TAP repoint patterns.
- `.paul/ROADMAP.md` — v2.59 milestone, Phase 255 = Documentation + Validation Surfacing.
- `.paul/STATE.md` — current loop position: ready for Phase 255 PLAN.
- `.paul/SPECIAL-FLOWS.md` — required lifecycle skills: /paul, /carl, TODD, WALT.

## Prior Work
- `.paul/phases/254-bounded-s7-submodule-extraction/254-01-SUMMARY.md` — Phase 254 closure naming the new sibling, the LOC delta (947 → 903), the byte-sequence single-location evidence, the bounded TAP guardrail (227 → 228), the TAP repoint pattern, and the post-merge baseline (Pi 228, cross-harness 135, install 6).
- `.paul/phases/253-s7-extraction-contract-bounded-submodule-plan/253-01-SUMMARY.md` — Phase 253 contract closure that authorized Phase 254 + Phase 255 surfacing scope.
- `.paul/phases/251-documentation-validation-surfacing/251-01-PLAN.md` and `.paul/phases/251-documentation-validation-surfacing/251-01-SUMMARY.md` — most-recent surfacing precedent (S3 / Phase 250). Phase 255 mirrors its task structure (user-facing surfaces → contract docs → TAP guardrails + validation), one task at a time, three TAP assertions.
- `.paul/phases/247-sibling-extraction-surfacing/247-01-SUMMARY.md` and `.paul/phases/244-sibling-extraction-surfacing/244-01-SUMMARY.md` — earlier S2 + S1 surfacing precedents; same shape.
- `docs/PI-NATIVE-PALS-CONTEXT-INJECTION-EXTRACTION-CONTRACT.md` — Phase 253 S7 contract; defines the markers and deferrals that the surfacing must continue to honor.
- `docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md` — current modularization contract; ends at Phase 253 Outcome. Phase 255 must append a bounded Phase 254 Outcome section.
- `docs/PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md` — current target baseline; ends at Phase 253 Outcome. Phase 255 must append a bounded Phase 254 Outcome section.
- `docs/PI-NATIVE-LEGACY-RETENTION-INSTALL-SURFACE-CLEANUP-CONTRACT.md` — preserved deferral (Claude Code / Agent SDK driver removal still blocked); Phase 255 surfacing must continue to honor this.
- `docs/PI-NATIVE-GUIDED-WORKFLOW-EVIDENCE-CONTRACT.md` — guided-workflow safety contract Phase 255 surfacing must continue to honor (transcript-visible canonical replies; no auto-approval / auto-continue / skipped checkpoints / UI-only lifecycle decisions / inferred merge intent).

## Source Files (read-only context for this plan)
- `README.md` — root README. Lines 25 and 284 carry the Pi-supported-runtime preamble + default-supported-target list naming the current siblings (`pals-hooks.ts`, `module-activity-parsing.ts`, `artifact-slice-rendering.ts`, `workflow-resource-capsule-rendering.ts`, `guided-workflow-detection.ts`). Phase 255 appends `pals-context-injection.ts` and the Phase 254 phrase. Line 32 carries the S3 detection / S4 canonical reply delivery boundary phrasing — surfacing must keep it intact.
- `drivers/pi/extensions/README.md` — Pi extension docs. Lines 20 (sibling list), 58 (Phase 250 prose narrating S3 detection / S4 canonical reply delivery split), 199 (table row for the Pi extension(s) entry naming each sibling), 208 (source-set rule narrative naming each sibling). All four sibling-naming places must add `pals-context-injection.ts` and a Phase 254 sentence; the S3/S4 boundary prose must remain intact.
- `drivers/pi/skill-map.md` — Pi skill map. Lines 9 (extensions surface naming each sibling), 20 (S3 detection / S4 canonical reply delivery boundary phrasing), 96 (source-set rule narrative naming each sibling). Sibling-naming places must add `pals-context-injection.ts` and a Phase 254 sentence; the S3/S4 boundary phrasing must remain intact.
- `docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md` — has `## Phase 243 Outcome`, `## Phase 246 Outcome`, `## Phase 250 Outcome`, `## Phase 253 Outcome` sections. Phase 255 appends `## Phase 254 Outcome` after the Phase 253 outcome.
- `docs/PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md` — has `## Phase 243 Outcome`, `## Phase 246 Outcome`, `## Phase 250 Outcome`, `## Phase 253 Outcome` sections. Phase 255 appends `## Phase 254 Outcome` after the Phase 253 outcome.
- `docs/PI-NATIVE-PALS-CONTEXT-INJECTION-EXTRACTION-CONTRACT.md` — ends at `## Status After Phase 253`. Phase 255 appends `## Status After Phase 254`.
- `tests/pi-end-to-end-validation.sh` — current baseline 228/228 (after Phase 254). Phase 251 Phase-251-surfacing block (lines 1827–1856) shows the exact `tap_file_contains_all` shape Phase 255 will mirror with three new assertions for S7 surfacing (`README.md`, `drivers/pi/extensions/README.md`, `drivers/pi/skill-map.md`). Expected count delta: 228 → 231 (+3).

</context>

<module_dispatch>
## Pre-Plan Module Dispatch

[dispatch] config-version: `pals.json` schema_version 2.0.0 matches installed `modules.yaml` kernel_version 2.0.0 — no migration needed.
[dispatch] planning posture: direct-requirements | collaboration high (carried from milestone).

### Advisory Modules
- [dispatch] TODD: pre-plan advisory → docs/surfacing phase; no `*.test.*` / `*.spec.*` files; the new bounded TAP surfacing assertions in `tests/pi-end-to-end-validation.sh` carry source-shape coverage for the docs surface. No TDD conversion required.
- [dispatch] IRIS: pre-plan advisory → no `TODO`/`FIXME`/`HACK`/`XXX` markers will be introduced; preserved-phrase audit pre-recorded for: `Authority: Derived aid only`, `Pi is the supported runtime`, `S4 canonical reply delivery`, `Derived aid only`, `before_agent_start`.
- [dispatch] DAVE: pre-plan advisory → no CI / Dockerfile / installer / driver.yaml changes. Surfacing is docs + one test file only.
- [dispatch] DOCS: pre-plan advisory → six doc surfaces to update (README.md, drivers/pi/extensions/README.md, drivers/pi/skill-map.md, docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md, docs/PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md, docs/PI-NATIVE-PALS-CONTEXT-INJECTION-EXTRACTION-CONTRACT.md) plus one test file. All within precedented surfacing-phase scope. No drift risk if the precedent is followed verbatim.
- [dispatch] RUBY: pre-plan advisory → no source LOC delta; the current `pals-hooks.ts` size-debt remains queued behind future S4/S6/S8 sibling extractions. Broader `tests/cross-harness-validation.sh` size-debt remains deferred per v2.58 closure.
- [dispatch] ARCH: pre-plan advisory → no symbol or import graph change; doc surfacing only.
- [dispatch] ARIA / DANA / GABE / LUKE / OMAR / PETE / REED / SETH / VERA: pre-plan advisory → no UI / data-schema / API / UX / observability / performance / resilience / secret / privacy changes.

### Blast Radius (CODI)
[dispatch] CODI: pre-plan advisory → markdown / shell-only phase; no `.ts/.tsx/.js/.jsx` symbols in scope. Skipping cleanly per planning workflow guardrail (markdown/config-only phases may record no seeds; CODI may skip cleanly).

### Enforcement Modules
- [dispatch] DEAN: pre-plan enforcement → no root package manifest / lockfile detected; no dependency files modified. PASS.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | All planning/applying/unifying lifecycle work | ✓ |
| /carl | required | Automatic session boundary monitoring | automatic |
| TODD | required | Before/during code implementation; advisory for docs/surfacing with bounded TAP guardrails | ✓ module dispatch recorded |
| WALT | required | APPLY/UNIFY quality gating and validation | pending APPLY |

**BLOCKING:** Required lifecycle/module checks must be honored before APPLY proceeds. Parent APPLY remains authoritative for verification, module enforcement, fallback, GitHub Flow gates, and `.paul/*` lifecycle writes.
</skills>

<acceptance_criteria>
## AC-1: User-facing Pi docs name the new S7 sibling and preserve every prior boundary phrase
```gherkin
Given Phase 254 added `drivers/pi/extensions/pals-context-injection.ts`
When `README.md`, `drivers/pi/extensions/README.md`, and `drivers/pi/skill-map.md` are reviewed
Then each user-facing sibling-list surface names `pals-context-injection.ts` alongside `pals-hooks.ts`, `module-activity-parsing.ts`, `artifact-slice-rendering.ts`, `workflow-resource-capsule-rendering.ts`, and `guided-workflow-detection.ts`; each surface still preserves the literal "Pi is the supported runtime", "Authority: Derived aid only" / "Derived aid only", "S4 canonical reply delivery", "before_agent_start", and the v2.59 phrase "Phase 254 S7 PALS context injection sibling extraction `pals-context-injection.ts`" appears in at least the root README and the Pi extensions README; and no S4 / S6 / S8 / Claude Code / Agent SDK driver phrasing is changed
```

## AC-2: Modularization contract docs add bounded Phase 254 Outcome sections; S7 contract doc adds Status After Phase 254
```gherkin
Given Phase 253 outcome sections currently terminate the modularization contract docs
When `docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md` and `docs/PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md` are reviewed
Then each appends a `## Phase 254 Outcome` section after its existing `## Phase 253 Outcome`, naming the new sibling `drivers/pi/extensions/pals-context-injection.ts`, the LOC delta (947 → 903), the marker single-location evidence (`[PALS_STATE_AUTHORITY=.paul/STATE.md]` and `[PALS_ACTIVATION_SIGNAL]` exactly once each in the new sibling), the type-only back-import pattern, and the TAP-assertion repoint pattern; the existing Phase 243 / Phase 246 / Phase 250 / Phase 253 Outcome sections are byte-unchanged; `docs/PI-NATIVE-PALS-CONTEXT-INJECTION-EXTRACTION-CONTRACT.md` appends `## Status After Phase 254` after the existing `## Status After Phase 253`, recording the same closure facts plus the install/Pi-validation reconciled count (`5 → 6` install, `227 → 228` Pi e2e); and S4 / S6 / S8 / Claude Code / Agent SDK driver deferrals remain explicitly preserved
```

## AC-3: Bounded Pi validation surfacing guardrails prove the docs surface
```gherkin
Given Phase 255 surfacing must be command-verifiable
When `bash tests/pi-end-to-end-validation.sh` runs
Then three new TAP assertions modeled on the Phase 251 `EXT_GUIDED_WORKFLOW_DETECTION` surfacing block exist immediately after the Phase 251 surfacing block (around line 1856), each `tap_file_contains_all` asserts that one of `README.md`, `drivers/pi/extensions/README.md`, or `drivers/pi/skill-map.md` names `pals-context-injection.ts` alongside the prior siblings AND preserves the appropriate boundary phrasing ("S4 canonical reply delivery" / "Authority: Derived aid only" / "Derived aid only"); the suite passes with the count reconciled from command output (expected baseline 228 → 231 with exactly +3 assertions); cross-harness validation remains 135/135 unchanged (no shared-invariant surfacing guardrail planned for S7)
```

## AC-4: Install + full validation green; docs/test-only changes; no scope drift
```gherkin
Given Phase 255 is a bounded docs + validation surfacing phase
When `PALS_ROOT="$PWD" bash drivers/pi/install.sh`, `bash tests/pi-end-to-end-validation.sh`, `bash tests/cross-harness-validation.sh`, `bash tests/helpers/artifact_consistency.sh`, and `git diff --check` all run
Then install reports `[ok] Pi extensions installed: 6 files` (unchanged from Phase 254 baseline); Pi validation passes at the reconciled baseline 228 → 231 (with exactly the +3 surfacing assertions; otherwise stop and reconcile in-flight from command output); cross-harness validation passes at 135/135 unchanged; artifact_consistency returns PASS; `git diff --check` returns clean; no source files under `drivers/pi/extensions/*.ts`, `drivers/pi/install.sh`, `drivers/pi/uninstall.sh`, `drivers/pi/driver.yaml`, `tests/cross-harness-validation.sh`, `tests/helpers/artifact_consistency.sh`, dependency / lockfile, CI config, kernel, module registry, Claude Code driver, Agent SDK driver, or installed-runtime-copy file is in the diff
```
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Surface S7 across user-facing Pi docs</name>
  <files>README.md, drivers/pi/extensions/README.md, drivers/pi/skill-map.md</files>
  <action>
    Append `pals-context-injection.ts` (and a bounded "Phase 254 S7 PALS context injection sibling extraction" sentence) to every place each surface currently lists the prior five siblings, mirroring exactly how Phase 251 added `guided-workflow-detection.ts`:

    - `README.md` lines 25 and 284: extend the comma-separated sibling list naming `pals-hooks.ts`, `module-activity-parsing.ts`, `artifact-slice-rendering.ts`, `workflow-resource-capsule-rendering.ts`, `guided-workflow-detection.ts` to also name `pals-context-injection.ts`; keep "the Phase 250 S3 detection sibling `guided-workflow-detection.ts`" phrasing intact and add an analogous "the Phase 254 S7 PALS context injection sibling `pals-context-injection.ts`" clause; preserve the source-set rule reference to `docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md`. Do NOT change the S3/S4 boundary sentence at line 32 or any other line.

    - `drivers/pi/extensions/README.md` lines 20, 58, 199, 208: same sibling-list extension. The Phase 250 narrative paragraph at line 58 keeps the "S3 guided workflow detection now lives in `guided-workflow-detection.ts`; S4 canonical reply delivery (...) remains in `pals-hooks.ts`" prose intact; immediately follow it with one new short paragraph naming the Phase 254 split: "Phase 254 split S7 PALS context injection into `pals-context-injection.ts`; S4 canonical reply delivery (`sendCanonicalWorkflowResponse`, `presentGuidedWorkflowMoment`, `loadGuidedWorkflowConfig`, `shouldAutoPresent`) remains in `pals-hooks.ts`. The split preserves the literal byte sequences `[PALS_STATE_AUTHORITY=.paul/STATE.md]` and `[PALS_ACTIVATION_SIGNAL]`, the legacy header `## PALS Context (auto-injected)`, the custom-message-type identity `pals-context`, and the `before_agent_start`/`context` Pi event surface without changing runtime semantics." Update the line-199 table row source-set list and the line-208 source-set narrative the same way (add `pals-context-injection.ts` after `guided-workflow-detection.ts`).

    - `drivers/pi/skill-map.md` lines 9, 20, 96: extend each sibling list to include `pals-context-injection.ts`. The line-20 boundary clause "S3 detection lives in `guided-workflow-detection.ts`, while S4 canonical reply delivery remains in `pals-hooks.ts`" keeps its exact wording; immediately after the existing clause (within the same bullet or as a co-located sub-clause) add: "S7 PALS context injection lives in `pals-context-injection.ts`, while S4 canonical reply delivery remains in `pals-hooks.ts`". Preserve the bullet's existing `Authority: Derived aid only` posture.

    Avoid: any change to source files under `drivers/pi/extensions/*.ts`, `drivers/pi/install.sh`, `drivers/pi/uninstall.sh`, `drivers/pi/driver.yaml`; any change to the S4 / S6 / S8 phrasing or the Claude Code / Agent SDK driver phrasing; any change to the "Authority: Derived aid only" / "Pi is the supported runtime" / "before_agent_start" / "S4 canonical reply delivery" preserved phrases other than where they currently appear; any reformatting of unrelated lines.
  </action>
  <verify>Focused `grep` checks: `pals-context-injection.ts` appears in `README.md`, `drivers/pi/extensions/README.md`, and `drivers/pi/skill-map.md`; each of the prior five sibling basenames (`pals-hooks.ts`, `module-activity-parsing.ts`, `artifact-slice-rendering.ts`, `workflow-resource-capsule-rendering.ts`, `guided-workflow-detection.ts`) still appears in each of the three files; the literal phrases `S4 canonical reply delivery`, `Authority: Derived aid only` (or `Derived aid only` in skill-map), `Pi is the supported runtime`, and `before_agent_start` still appear in the files where they currently appear; `git diff --check` is clean for these three files.</verify>
  <done>AC-1 satisfied: each user-facing surface names the new sibling alongside the prior five siblings and preserves every prior boundary phrase verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: Append Phase 254 Outcome to modularization contract docs and Status After Phase 254 to S7 contract</name>
  <files>docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md, docs/PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md, docs/PI-NATIVE-PALS-CONTEXT-INJECTION-EXTRACTION-CONTRACT.md</files>
  <action>
    Append three bounded outcome / status sections, modeled byte-for-byte on the existing precedents in each doc:

    - `docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md`: append `## Phase 254 Outcome` after the existing `## Phase 253 Outcome` section. Mirror the brevity and structure of the existing Phase 250 Outcome section. Record: new sibling `drivers/pi/extensions/pals-context-injection.ts` (88 LOC, 6 single-defined exact-string constants + 6 exported functions + loader-compat no-op default factory carrying the literal `No-op Pi extension factory` marker); `pals-hooks.ts` reduced 947 → 903 LOC by importing the six S7 functions and six S7 constants from `./pals-context-injection` and exporting `ActivationState` for the sibling's type-import; S4 canonical-reply identifiers and shared helpers retained inline; the type-only back-import pattern (sibling type-imports `ActivationState` and `PalsStateSnapshot` from `./pals-hooks` while value-importing only `renderArtifactSlices` from `./artifact-slice-rendering` and `renderWorkflowResourceCapsules` from `./workflow-resource-capsule-rendering`; the back-import is erased at runtime so the dependency graph stays acyclic); the TAP-assertion repoint pattern (when a constant *definition* moves into a sibling, pre-existing TAP definition-string assertions are repointed to imported-identifier assertions in the same plan-scope file as a bounded, no-net-count-change reconciliation); the validation gates Phase 254 ratified (Pi 227 → 228, install 5 → 6, cross-harness 135/135 unchanged, artifact_consistency PASS, `git diff --check` clean); and the still-deferred surfaces (S4 canonical reply delivery, S6 lifecycle UI, S8 command routing).

    - `docs/PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md`: append `## Phase 254 Outcome` after the existing `## Phase 253 Outcome` section. Mirror the brevity and structure of the existing Phase 250 Outcome section. Record: post-Phase-254 baseline (Pi 228/228, cross-harness 135/135, artifact_consistency PASS, install 6 Pi extension files); the new sibling enters the source set governed by `drivers/pi/install.sh` lines 88–112; six S7 markers single-located in the new sibling; the still-deferred surfaces (S4 / S6 / S8); broader cross-harness validation-script size-debt still deferred per v2.58 closure.

    - `docs/PI-NATIVE-PALS-CONTEXT-INJECTION-EXTRACTION-CONTRACT.md`: append `## Status After Phase 254` after the existing `## Status After Phase 253` section. Record: contract closed for Phase 254; six S7 functions extracted with no signature change; six S7 constants single-defined as exact-string markers; loader-compat invariant satisfied; helper-import tightening applied; transcript-visible authority/activation byte sequences preserved exactly; legacy-header migration preserved; install reports `[ok] Pi extensions installed: 6 files`; Pi e2e 228/228 (227 → 228 with the bounded EXT_PALS_CONTEXT_INJECTION TAP block + the in-flight repoint of pre-existing assertion 133); cross-harness 135/135 unchanged; PR #169 squash-merged 2026-05-04 as `af1c1793`; Phase 255 owns user-facing-doc surfacing (this phase); Phase 256 will own evidence closure + next-roadmap decision; S4 / S6 / S8 deferrals preserved; full Claude Code / Agent SDK driver removal still blocked per `PI-NATIVE-LEGACY-RETENTION-INSTALL-SURFACE-CLEANUP-CONTRACT.md`.

    Avoid: editing any prior `## Phase NNN Outcome` or `## Status After Phase NNN` section (must remain byte-unchanged); editing any S7 marker definition or function signature; touching the Forbidden Scope (Phase 254) section or the Acceptance Checklist; reformatting unrelated lines.
  </action>
  <verify>`grep -c '^## Phase 254 Outcome'` returns exactly 1 in each of `docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md` and `docs/PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md`; `grep -c '^## Status After Phase 254'` returns exactly 1 in `docs/PI-NATIVE-PALS-CONTEXT-INJECTION-EXTRACTION-CONTRACT.md`; the prior `## Phase 250 Outcome` and `## Phase 253 Outcome` sections still exist (count ≥ 1) in each contract doc; `git diff --check` is clean for these three files.</verify>
  <done>AC-2 satisfied: contract docs carry bounded Phase 254 Outcome / Status After Phase 254 sections that mirror the prior precedent exactly.</done>
</task>

<task type="auto">
  <name>Task 3: Add bounded Pi surfacing TAP guardrails and run install + full validation</name>
  <files>tests/pi-end-to-end-validation.sh</files>
  <action>
    Add three new bounded surfacing TAP assertions immediately after the existing Phase 251 surfacing block (around line 1856), modeled byte-for-byte on the Phase 251 `tap_file_contains_all` triple at lines 1832–1855:

    - Block A (root README): `tap_file_contains_all "Phase 255 sibling extraction surfacing (Pi-supported runtime): user-facing docs name pals-context-injection.ts and preserve S7/S4 boundary" "$REPO_ROOT/README.md" 'pals-context-injection.ts' 'pals-hooks.ts' 'S4 canonical reply delivery'`
    - Block B (Pi extensions README): `tap_file_contains_all "Phase 255 sibling extraction surfacing (Pi-supported runtime): drivers/pi/extensions/README.md names pals-context-injection.ts alongside extracted siblings" "$REPO_ROOT/drivers/pi/extensions/README.md" 'pals-hooks.ts' 'module-activity-parsing.ts' 'artifact-slice-rendering.ts' 'workflow-resource-capsule-rendering.ts' 'guided-workflow-detection.ts' 'pals-context-injection.ts' 'S4 canonical reply delivery'`
    - Block C (Pi skill-map): `tap_file_contains_all "Phase 255 sibling extraction surfacing (Pi-supported runtime): drivers/pi/skill-map.md names pals-context-injection.ts and derived-only boundary" "$REPO_ROOT/drivers/pi/skill-map.md" 'pals-context-injection.ts' 'pals-hooks.ts' 'Derived aid only'`

    Use exactly three `tap_file_contains_all` assertions. The expected count delta is `Pi 228 → 231`. If the assertion run reveals additional gaps that demand more assertions or an unrelated count shift, stop and reconcile in-flight (revert / split with explicit justification) rather than silently expanding.

    Then run the full validation set and capture command output for SUMMARY:
    - `PALS_ROOT="$PWD" bash drivers/pi/install.sh` — verify `[ok] Pi extensions installed: 6 files` (unchanged from Phase 254 baseline; no source extraction in this phase).
    - `bash tests/pi-end-to-end-validation.sh` — expect 1..231 / Passed 231 / Failed 0.
    - `bash tests/cross-harness-validation.sh` — expect 1..135 / Passed 135 / Failed 0 unchanged (no shared-invariant surfacing guardrail planned).
    - `bash tests/helpers/artifact_consistency.sh` — expect `artifact_consistency_check: PASS`.
    - `git diff --check` — expect clean.

    Avoid: modifying `tests/cross-harness-validation.sh`, `tests/helpers/artifact_consistency.sh`, or any source file under `drivers/`; adding more than three new TAP assertions; changing the EXT_PALS_CONTEXT_INJECTION block from Phase 254 (the source-shape guardrail stays as-is).
  </action>
  <verify>`bash tests/pi-end-to-end-validation.sh` returns 1..231 / Passed 231 / Failed 0; install reports 6 files; cross-harness 135/135 unchanged; artifact_consistency PASS; `git diff --check` clean. If the count is anything other than `+3` from baseline 228, stop and reconcile from command output before APPLY finalize.</verify>
  <done>AC-3 + AC-4 satisfied: the surface is command-verifiable with bounded count reconciliation; install + Pi + cross-harness + artifact_consistency + diff-whitespace all green.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Any source file under `drivers/pi/extensions/*.ts`, including the new `pals-context-injection.ts` from Phase 254 (this phase is docs/test-only — no source LOC delta).
- `drivers/pi/install.sh`, `drivers/pi/uninstall.sh`, `drivers/pi/driver.yaml` (Phase 241 generalized source-set rule already covers the new sibling).
- S4 canonical reply delivery (`sendCanonicalWorkflowResponse`, `presentGuidedWorkflowMoment`, `loadGuidedWorkflowConfig`, `shouldAutoPresent`), S6 lifecycle UI, S8 command routing, CARL session boundary, module-activity-parsing, artifact-slice-rendering, workflow-resource-capsule-rendering, guided-workflow-detection — preserved deferrals.
- Existing `## Phase 243 Outcome`, `## Phase 246 Outcome`, `## Phase 250 Outcome`, `## Phase 253 Outcome` sections in the modularization contract docs (must remain byte-unchanged).
- Existing `## Status After Phase 253` section in `docs/PI-NATIVE-PALS-CONTEXT-INJECTION-EXTRACTION-CONTRACT.md` (must remain byte-unchanged).
- The S3/S4 boundary sentence in `README.md` line 32 and the S3-detection-vs-S4-canonical-reply-delivery clauses in `drivers/pi/extensions/README.md` line 58 and `drivers/pi/skill-map.md` line 20 (the S7 surfacing must be additive, not a rewrite).
- `tests/cross-harness-validation.sh`, `tests/helpers/artifact_consistency.sh`, `pals.json`, dependency manifests, CI config, kernel workflows, module registry, Claude Code driver, Agent SDK driver, installed runtime copies under `~/.pi/agent/extensions/`.
- The Phase 254 EXT_PALS_CONTEXT_INJECTION TAP block in `tests/pi-end-to-end-validation.sh` (the source-shape guardrail from Phase 254 — Phase 255 only adds three new surfacing TAP assertions; it does not modify the existing block).
- The Phase 254 repoint of pre-existing assertion 133 (it must remain in its post-Phase-254 form).

## SCOPE LIMITS
- Surfacing only: no source extraction, no installer change, no driver / module / lifecycle / kernel change.
- At most three new bounded Pi surfacing TAP assertions (Task 3). If command output proves additional bounded repair is necessary, reconcile in-flight with explicit justification rather than expanding silently.
- No telemetry, hidden runtime state, generated lifecycle ledger, cache-as-truth, alternate validation/module/lifecycle/GitHub Flow ledger, auto-approval, auto-continue, skipped checkpoints, UI-only lifecycle decisions, or inferred merge intent.
- No payload-string drift: `STATE_AUTHORITY_TAG`, `ACTIVATION_SIGNAL_TAG`, `LEGACY_PALS_CONTEXT_HEADER`, `PALS_CONTEXT_CUSTOM_TYPE`, `PRIMARY_INJECTION_EVENT`, `SUPPORTING_CONTEXT_EVENT` keep their exact byte sequences in their single source-of-truth (`pals-context-injection.ts`).
- No claude-code/Agent-SDK driver changes (frozen since v2.52; full removal still blocked on cross-harness validation decomposition per `PI-NATIVE-LEGACY-RETENTION-INSTALL-SURFACE-CLEANUP-CONTRACT.md`).
- `pals-implementer` may be used only for a bounded task packet covering Task 1 or Task 2; parent APPLY owns diff review, official verification, module gates, fallback, GitHub Flow gates, and `.paul/*` lifecycle writes. Inline parent APPLY remains the recommended default for a 3-task surfacing wave.

</boundaries>

<verification>
Before declaring APPLY complete:
- [ ] `pals-context-injection.ts` named in `README.md`, `drivers/pi/extensions/README.md`, and `drivers/pi/skill-map.md` alongside the prior five siblings.
- [ ] `S4 canonical reply delivery`, `Authority: Derived aid only` / `Derived aid only`, `Pi is the supported runtime`, `before_agent_start` literal phrases preserved in their original locations.
- [ ] `## Phase 254 Outcome` appended exactly once in each of `docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md` and `docs/PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md`.
- [ ] `## Status After Phase 254` appended exactly once in `docs/PI-NATIVE-PALS-CONTEXT-INJECTION-EXTRACTION-CONTRACT.md`.
- [ ] Existing `## Phase 243 Outcome`, `## Phase 246 Outcome`, `## Phase 250 Outcome`, `## Phase 253 Outcome`, and `## Status After Phase 253` sections still exist and are byte-unchanged.
- [ ] Three new `Phase 255 sibling extraction surfacing` TAP assertions in `tests/pi-end-to-end-validation.sh` (one per surface).
- [ ] `bash tests/pi-end-to-end-validation.sh` returns 1..231 / Passed 231 / Failed 0.
- [ ] `bash tests/cross-harness-validation.sh` returns 1..135 / Passed 135 / Failed 0 unchanged.
- [ ] `bash tests/helpers/artifact_consistency.sh` returns `artifact_consistency_check: PASS`.
- [ ] `PALS_ROOT="$PWD" bash drivers/pi/install.sh` reports `[ok] Pi extensions installed: 6 files`.
- [ ] `git diff --check` returns clean.
- [ ] No `drivers/pi/extensions/*.ts` / `drivers/pi/install.sh` / `drivers/pi/uninstall.sh` / `drivers/pi/driver.yaml` / `tests/cross-harness-validation.sh` / `tests/helpers/artifact_consistency.sh` / `pals.json` / dependency / CI / kernel / module-registry / Claude Code driver / Agent SDK driver / installed-runtime-copy file is in the diff.
</verification>

<success_criteria>
- Phase 254's S7 `pals-context-injection` extraction is fully surfaced across user-facing Pi docs (root README, Pi extensions README, Pi skill-map), the modularization contract docs (modularization contract + extraction-target baseline), the S7 extraction contract, and the Pi validation suite.
- The Phase 244 (S1) / Phase 247 (S2) / Phase 251 (S3) surfacing recipe is applied a fourth time, ratifying it for any remaining S* surfacing.
- Validation: Pi 228 → 231 (+3 surfacing assertions), cross-harness 135/135, artifact_consistency PASS, install 6 Pi extension files unchanged, `git diff --check` clean.
- All four Phase 255 ACs are satisfied.
- Phase 256 (Evidence Closure + Next Roadmap Decision) inherits a clean baseline to close v2.59.
</success_criteria>

<output>
After completion, create `.paul/phases/255-documentation-validation-surfacing-v2-59/255-01-SUMMARY.md` with:
- Acceptance criteria results.
- Files modified with line deltas.
- Focused phrase-preservation evidence (preserved boundary phrases still present; sibling lists extended; outcome / status sections appended; existing outcome sections byte-unchanged).
- Validation command output and count reconciliation (Pi 228 → 231 with +3 surfacing assertions; cross-harness 135 unchanged; install 6 unchanged).
- Module execution reports (pre-plan / pre-apply / post-task / post-apply / pre-unify / post-unify).
- GitHub Flow evidence (branch, commit, PR, CI, mergeStateStatus, merge result).
- Workguard scope evidence (allowed paths: `README.md`, `drivers/pi/extensions/README.md`, `drivers/pi/skill-map.md`, `docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md`, `docs/PI-NATIVE-EXTENSION-EXTRACTION-TARGET-BASELINE.md`, `docs/PI-NATIVE-PALS-CONTEXT-INJECTION-EXTRACTION-CONTRACT.md`, `tests/pi-end-to-end-validation.sh`, `.paul/**`; final changed-file set).
- Deferred scope (S4/S6/S8 still deferred; broader cross-harness validation-script size-debt deferred; Claude Code / Agent SDK driver changes still blocked) and Phase 256 handoff.
</output>
