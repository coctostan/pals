---
phase: 209-evidence-reconciliation-milestone-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/PI-NATIVE-GUIDED-WORKFLOW-EVIDENCE-CONTRACT.md
  - tests/pi-end-to-end-validation.sh
  - tests/cross-harness-validation.sh
autonomous: true
---

<objective>
## Goal
Reconcile v2.48 Guided Workflow Evidence Hardening validation/evidence so the guided workflow runtime markers, Pi/cross-harness command-output proof, module evidence, GitHub Flow evidence, and lifecycle metadata reflect the current implemented state and close the milestone.

## Purpose
Phase 206 defined the guided workflow evidence contract baseline; Phase 207 hardened the runtime in `drivers/pi/extensions/pals-hooks.ts`; Phase 208 surfaced the contract in user/Pi docs, schema reference, and validation markers (Pi 192/192, cross-harness 108/108, PR #123 merged). Phase 209 closes v2.48 by:
- Updating the guided workflow evidence contract's command-output evidence baselines to the current post-Phase-207/208 counts while preserving Phase 206 historical context.
- Adding focused durable marker checks (no new validation classes) so the v2.48 closure baseline stays command-verifiable.
- Producing complete UNIFY evidence (acceptance results, command output, module reports, GitHub Flow proof, decisions, deviations) so the next Pi-native build milestone can start from a clean v2.48 closure record.

This mirrors the Phase 205 closure pattern that closed v2.47.

## Output
- Updated baseline wording in `docs/PI-NATIVE-GUIDED-WORKFLOW-EVIDENCE-CONTRACT.md`.
- Focused marker additions in `tests/pi-end-to-end-validation.sh` and `tests/cross-harness-validation.sh` that protect the v2.48 closure evidence (no new validation classes, no harness restructuring).
- Completion evidence in `.paul/phases/209-evidence-reconciliation-milestone-closure/209-01-SUMMARY.md` after APPLY/UNIFY, including milestone-closure handoff to the next Pi-native build milestone (Workflow/Resource Capsule Contract per `docs/PI-NATIVE-IMPLEMENTATION-PLAN.md`).
- Lifecycle metadata updates (`.paul/STATE.md`, `.paul/ROADMAP.md`, `.paul/PROJECT.md`, `.paul/MILESTONES.md`) reflecting v2.48 closure — produced via normal lifecycle writes during APPLY/UNIFY, not part of source-file scope here.
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md
.paul/phases/208-documentation-validation-surfacing/208-01-SUMMARY.md
.paul/phases/205-validation-evidence-reconciliation/205-01-PLAN.md
.paul/phases/205-validation-evidence-reconciliation/205-01-SUMMARY.md

## Source Files
docs/PI-NATIVE-GUIDED-WORKFLOW-EVIDENCE-CONTRACT.md
tests/pi-end-to-end-validation.sh
tests/cross-harness-validation.sh
</context>

<module_dispatch>
## Pre-Plan Module Dispatch

[dispatch] pre-plan advisory: TODD, IRIS, DAVE, DOCS, CODI, RUBY, ARCH, SETH, ARIA/LUKE/DANA/GABE/OMAR/PETE/REED/VERA scope probes completed; advisory only.

- TODD: No repo-local `*.test.*`, `*.spec.*`, or `__tests__` test files in planned scope; validation is shell-suite based. Plan remains `execute`, not `tdd`.
- IRIS: No `TODO|FIXME|HACK|XXX` markers found in planned files.
- DAVE: No repo-local CI workflow under `.github/workflows/` outside ignored runtime/vendor paths; keep validation command-output local. No deploy step needed.
- DOCS: Documentation catalog includes `docs/PI-NATIVE-GUIDED-WORKFLOW-EVIDENCE-CONTRACT.md`; planned doc edits are limited to baseline reconciliation and have nearby project docs already aligned (Phase 208 surfaced README/Pi extension README/skill-map/schema reference).
- CODI: NO MAGIC INFERENCE. Phase scope is Markdown + shell-suite reconciliation only; no `.ts/.tsx/.js/.jsx` source files appear in `<files>`. CODI may skip cleanly with `[dispatch] CODI: skipped — no extractable symbols in phase scope`. No `codi_seed_candidates` recorded.
- RUBY: Planned files include two large shell validation scripts (`tests/pi-end-to-end-validation.sh` 1477 lines and `tests/cross-harness-validation.sh` 1103 lines) and one 191-line doc. Keep changes localized; do not broaden harness structure or refactor existing helpers.
- ARCH: Detected pattern: flat / kernel-and-driver repository (medium confidence). Planned files are in expected docs/test locations. `wc -l`: `docs/PI-NATIVE-GUIDED-WORKFLOW-EVIDENCE-CONTRACT.md` 191, `tests/pi-end-to-end-validation.sh` 1477, `tests/cross-harness-validation.sh` 1103. Existing validation scripts are large; add focused checks only.
- SETH: No hardcoded secret markers found in planned files.
- ARIA/LUKE/DANA/GABE/OMAR/PETE/REED/VERA: No UI, data, API, observability, performance-import, resilience, or privacy-relevant planned file types/patterns detected.

[dispatch] pre-plan enforcement: DEAN PASS — no package manager manifests or lockfiles detected at repo root/maxdepth 2 outside ignored runtime/vendor paths, so dependency audit is not applicable and no critical/high dependency block exists.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | PLAN/APPLY/UNIFY lifecycle work | ✓ |
| /carl | required | Session boundary monitoring | automatic |
| TODD | required | Before/during implementation; shell-suite validation substitutes for TDD conversion here | via module dispatch |
| WALT | required | APPLY/UNIFY quality validation | via module dispatch |

**BLOCKING:** Required skills MUST be loaded or active before APPLY proceeds.
</skills>

<acceptance_criteria>
## AC-1: Guided workflow evidence contract baselines reflect current v2.48 state
```gherkin
Given Phase 207 hardened the Pi guided workflow runtime and Phase 208 surfaced docs/settings/validation markers with Pi 192/192 and cross-harness 108/108
When `docs/PI-NATIVE-GUIDED-WORKFLOW-EVIDENCE-CONTRACT.md` is reviewed after this plan
Then the command-output evidence section states the current post-Phase-207/208 baseline as Pi 192/192 and cross-harness 108/108, preserves the Phase 205/206 starting baseline (Pi 188/188, cross-harness 104/104) as historical context, and continues to require any future count changes to record old count, new count, reason, validation class affected, and shared-invariant preservation rationale in SUMMARY.
```

## AC-2: Guided workflow closure markers are durable in Pi validation
```gherkin
Given Phase 207/208 already added Guided UI Safety and Pi-Supported Runtime markers in `tests/pi-end-to-end-validation.sh`
When the Pi validation suite runs after this plan
Then it explicitly protects the canonical reply contract surfaces (`sendCanonicalWorkflowResponse`, `merge-gate-routing`, `canonical transcript`, `explicit confirm/select`, `notify-only`, no auto-approval / no auto-continue / no skipped checkpoints / no UI-only lifecycle decisions / no inferred merge intent, and display-only `guided_workflow.auto_present`) using existing helper style with no new validation classes and no harness restructuring.
```

## AC-3: Cross-harness shared-invariant markers protect v2.48 evidence boundaries
```gherkin
Given guided workflow runtime is Pi-supported but `.paul/*`, module reports, GitHub Flow evidence, installed `modules.yaml`, and command output remain shared authority
When the cross-harness validation suite runs after this plan
Then it explicitly protects Shared Invariant and GitHub Flow Safety markers for canonical transcript replies, route-assistance-only behavior, no CI/review bypass, no inferred merge intent, full authoritative reads, command-output truth, and parent-owned APPLY without modifying frozen Claude Code / Agent SDK behavior or adding new validation classes.
```

## AC-4: v2.48 milestone closure produces complete reconciliation evidence
```gherkin
Given Phase 209 is the final v2.48 phase
When APPLY/UNIFY completes
Then `.paul/phases/209-evidence-reconciliation-milestone-closure/209-01-SUMMARY.md` records acceptance-criteria results, focused-grep output, fresh Pi/cross-harness TAP totals (with old/new count reconciliation when counts changed), changed-file scope review, module reports, GitHub Flow evidence (branch, base, ahead/behind, PR URL/state, CI state, merge-gate result), decisions, deviations, and a milestone-closure handoff naming the next recommended Pi-native build milestone (Workflow/Resource Capsule Contract per `docs/PI-NATIVE-IMPLEMENTATION-PLAN.md`).
```

## AC-5: Scope stays limited to v2.48 evidence reconciliation and milestone closure
```gherkin
Given Phase 209 is a milestone-closing reconciliation phase
When APPLY completes
Then no runtime behavior in `drivers/pi/extensions/pals-hooks.ts`, installer behavior, dependency manifests/lockfiles, CI configuration, workflow/resource capsule behavior, delegated APPLY packet/report compression, broad Pi-native validation classification, legacy retention / install-surface cleanup, frozen Claude Code / Agent SDK parity surfaces, or `.paul/*` lifecycle authority semantics are changed.
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Reconcile guided workflow evidence contract baselines</name>
  <files>docs/PI-NATIVE-GUIDED-WORKFLOW-EVIDENCE-CONTRACT.md</files>
  <action>
    Update the contract's command-output evidence section so v2.48 closure baselines are explicit while Phase 206 historical context is preserved:
    - Keep the existing "Phase 205/Phase 206 starting baseline is Pi 188/188 / cross-harness 104/104" wording as historical context (rename if needed to make the historical role explicit).
    - Add the current post-Phase-207/208 baseline: Pi 192/192 and cross-harness 108/108, naming the Phase 207 runtime hardening and Phase 208 docs/validation surfacing as the source.
    - Preserve the existing requirement that any future count change must record old count, new count, reason, validation class affected, and shared-invariant preservation rationale in SUMMARY.
    - Do NOT rewrite the contract as if Phase 206 itself implemented runtime/docs changes; keep Phase 206 framed as planning-only.
    - Do NOT add new validation classes, capsule/helper-compression language, legacy cleanup language, or lifecycle-authority language.
  </action>
  <verify>grep -n "188/188\|104/104\|192/192\|108/108\|old count\|new count\|command output\|planning-only\|Phase 207\|Phase 208" docs/PI-NATIVE-GUIDED-WORKFLOW-EVIDENCE-CONTRACT.md</verify>
  <done>AC-1 satisfied: the contract documents both the historical Phase 205/206 starting baseline (Pi 188/188, cross-harness 104/104) and the current post-Phase-207/208 baseline (Pi 192/192, cross-harness 108/108), and the count-change reconciliation requirement is preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Add durable v2.48 closure marker checks in Pi and cross-harness validation</name>
  <files>tests/pi-end-to-end-validation.sh, tests/cross-harness-validation.sh</files>
  <action>
    Add focused marker checks that make the v2.48 guided workflow closure evidence command-verifiable, using the existing helper style (`tap_file_contains_all`, direct `grep -Fq`, or small grouped checks). Do NOT introduce new validation classes, do NOT refactor either harness, and do NOT broaden into CI creation, runtime behavior changes, installer changes, legacy parity changes, or validation classification redesign.

    In `tests/pi-end-to-end-validation.sh`:
    - Verify `docs/PI-NATIVE-GUIDED-WORKFLOW-EVIDENCE-CONTRACT.md` records the current post-Phase-207/208 baseline (`192/192`, `108/108`) and the count-change reconciliation requirement (old count / new count / reason / validation class / shared-invariant rationale).
    - Reinforce that the existing Guided UI Safety / Pi-Supported Runtime checks cover the closure-relevant marker cluster: `sendCanonicalWorkflowResponse`, `merge-gate-routing`, `canonical transcript`, `explicit confirm/select`, `notify-only`, and the no-auto / no-UI-only / no-inferred-merge family. (Add only what is missing — do not duplicate existing checks.)

    In `tests/cross-harness-validation.sh`:
    - Verify the contract's v2.48 closure baselines are surfaced for shared review (`192/192`, `108/108`) without requiring frozen Claude Code / Agent SDK parity changes.
    - Reinforce that Shared Invariant and GitHub Flow Safety markers cover canonical transcript replies, route-assistance-only behavior, no CI/review bypass, full authoritative reads, command-output truth, and parent-owned APPLY. (Add only what is missing.)

    All additions must be local, idempotent, and protected by structural marker matching (no long exact-prose assertions).
  </action>
  <verify>bash tests/pi-end-to-end-validation.sh && bash tests/cross-harness-validation.sh</verify>
  <done>AC-2 and AC-3 satisfied: both validation suites pass green and visibly protect the v2.48 closure baselines and guided workflow shared-invariant boundaries; if pass counts change, old/new counts and rationale are captured for SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 3: Run final v2.48 closure evidence and scope checks</name>
  <files>docs/PI-NATIVE-GUIDED-WORKFLOW-EVIDENCE-CONTRACT.md, tests/pi-end-to-end-validation.sh, tests/cross-harness-validation.sh</files>
  <action>
    Produce APPLY evidence for UNIFY and milestone closure:
    - Run focused marker grep over the planned files for v2.48 closure markers: `192/192`, `108/108`, `188/188`, `104/104`, `canonical transcript`, `merge-gate-routing`, `sendCanonicalWorkflowResponse`, `explicit confirm/select`, `notify-only`, `no auto-approval`, `no auto-continue`, `no skipped checkpoints`, `no UI-only`, `no inferred merge`, `display-only`, `Guided UI Safety`, `Pi-Supported Runtime`, `Shared Invariant`, `GitHub Flow Safety`, `command output`, `Module Execution Reports`, `parent-owned APPLY`.
    - Run installer verification (`PALS_ROOT="$PWD" bash drivers/pi/install.sh`).
    - Run both validation suites and capture TAP totals.
    - Run `git diff --stat` and confirm only the approved evidence-reconciliation surfaces changed outside normal `.paul/*` lifecycle artifacts; keep the milestone closure free of runtime/installer/dependency/CI/legacy/capsule/classification changes.
  </action>
  <verify>grep -n "192/192\|108/108\|188/188\|104/104\|canonical transcript\|merge-gate-routing\|sendCanonicalWorkflowResponse\|explicit confirm/select\|notify-only\|no auto-approval\|no auto-continue\|no skipped checkpoints\|no UI-only\|no inferred merge\|display-only\|Guided UI Safety\|Pi-Supported Runtime\|Shared Invariant\|GitHub Flow Safety\|command output\|parent-owned APPLY" docs/PI-NATIVE-GUIDED-WORKFLOW-EVIDENCE-CONTRACT.md tests/pi-end-to-end-validation.sh tests/cross-harness-validation.sh && git diff --stat</verify>
  <done>AC-4 and AC-5 satisfied: closure evidence is command-backed (focused grep, installer, Pi/cross-harness TAP, git diff --stat) and changed files stay inside approved evidence-reconciliation/milestone-closure scope.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- `drivers/pi/extensions/pals-hooks.ts` runtime behavior — Phase 207 owns runtime hardening; Phase 209 must not modify it.
- `drivers/pi/install.sh`, installed runtime copies outside the repo, dependency manifests/lockfiles, and CI workflow configuration.
- Kernel workflow semantics, module source behavior, GitHub Flow automation, lifecycle approval/checkpoint semantics, or `.paul/*` authority outside normal lifecycle artifact updates during APPLY/UNIFY.
- Frozen Claude Code, Agent SDK, and portability/legacy parity surfaces.
- Existing harness helper structure — no refactor of `tests/pi-end-to-end-validation.sh` or `tests/cross-harness-validation.sh`.

## SCOPE LIMITS
- This plan reconciles guided workflow evidence baselines, adds focused durable closure markers, and produces v2.48 milestone-closure evidence. Nothing else.
- Do NOT add workflow/resource capsules, delegated APPLY packet/report compression, broad Pi-native validation classification, legacy retention / install-surface cleanup, telemetry, hidden Pi state, or helper-owned APPLY behavior.
- Do NOT introduce new validation classes; reuse the named classes already in the contract (Guided UI Safety, Pi-Supported Runtime, Shared Invariant, GitHub Flow Safety, Runtime Lens, Artifact / Process).
- Lifecycle metadata writes (`.paul/STATE.md`, `.paul/ROADMAP.md`, `.paul/PROJECT.md`, `.paul/MILESTONES.md`) are produced via normal APPLY/UNIFY lifecycle writes, not as bespoke source-file edits in `<files>`.
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `grep -n "192/192\|108/108\|188/188\|104/104\|canonical transcript\|merge-gate-routing\|sendCanonicalWorkflowResponse\|explicit confirm/select\|notify-only\|no auto-approval\|no auto-continue\|no skipped checkpoints\|no UI-only\|no inferred merge\|display-only\|Guided UI Safety\|Pi-Supported Runtime\|Shared Invariant\|GitHub Flow Safety\|command output\|parent-owned APPLY" docs/PI-NATIVE-GUIDED-WORKFLOW-EVIDENCE-CONTRACT.md tests/pi-end-to-end-validation.sh tests/cross-harness-validation.sh`
- [ ] `PALS_ROOT="$PWD" bash drivers/pi/install.sh`
- [ ] `bash tests/pi-end-to-end-validation.sh`
- [ ] `bash tests/cross-harness-validation.sh`
- [ ] `git diff --stat` confirms only approved repo-source evidence-reconciliation surfaces changed outside `.paul/*` lifecycle artifacts.
- [ ] All acceptance criteria met.
- [ ] Pre-merge GitHub Flow evidence captured: branch, ahead/behind base, PR URL/state, CI/check state, and merge-gate result during UNIFY.
</verification>

<success_criteria>
- Guided workflow evidence contract documents current v2.48 baselines (Pi 192/192, cross-harness 108/108) while preserving Phase 205/206 historical baselines.
- Pi validation passes with a TAP summary and zero failures, and includes durable closure markers for the canonical reply contract.
- Cross-harness validation passes with a TAP summary and zero failures, and includes durable Shared Invariant / GitHub Flow Safety markers for v2.48 closure.
- v2.48 closes with `.paul/STATE.md`, `.paul/ROADMAP.md`, `.paul/PROJECT.md`, and `.paul/MILESTONES.md` reflecting milestone completion via normal lifecycle writes.
- The next Pi-native build milestone is named in the SUMMARY handoff (Workflow/Resource Capsule Contract per `docs/PI-NATIVE-IMPLEMENTATION-PLAN.md`).
- No runtime, dependency, CI, legacy, capsule, classification, or lifecycle-authority changes are introduced.
</success_criteria>

<output>
After completion, create `.paul/phases/209-evidence-reconciliation-milestone-closure/209-01-SUMMARY.md` with acceptance-criteria results, focused-grep output, fresh Pi/cross-harness TAP totals (with old/new count reconciliation when counts changed), changed-file scope review, module reports (pre-plan dispatch carried forward, pre-unify dispatch, post-unify dispatch including WALT quality history, CODI history, SKIP knowledge capture, RUBY debt note), GitHub Flow evidence (branch, base, ahead/behind, PR URL/state, CI state, merge-gate result), decisions/deviations, and a milestone-closure handoff naming the next recommended Pi-native build milestone (Workflow/Resource Capsule Contract).
</output>
