---
phase: 01-architecture-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [".paul/phases/01-architecture-design/architecture-audit.md"]
autonomous: true
---

<objective>
## Goal
Audit and map every file across PAUL, CARL, TODD, and WALT — producing a complete inventory with file purposes, dependencies, and component boundaries.

## Purpose
Before we can design a unified PALS structure, we need to know exactly what exists. This audit prevents missing files during migration and reveals hidden dependencies between components.

## Output
- `.paul/phases/01-architecture-design/architecture-audit.md` — complete file inventory organized by component
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@.paul/MILESTONE-CONTEXT.md

## Source Locations
@~/.claude/paul-framework/ (PAUL core — workflows, references, templates, rules)
@~/.claude/commands/paul/ (PAUL skill commands)
@~/.claude/commands/carl/ (CARL skill commands)
@~/.carl/ (CARL core — manifest, domains, hooks, global rules)
@~/.claude/paul-framework/workflows/apply-phase-tdd.md (TODD — TDD apply workflow)
@~/.claude/paul-framework/workflows/plan-phase-tdd.md (TODD — TDD plan workflow)
@~/.claude/paul-framework/references/tdd-detection.md (TODD — TDD detection heuristics)
@~/.claude/paul-framework/references/tdd.md (TODD — TDD reference)
@~/.claude/paul-framework/workflows/quality-gate.md (WALT — quality gate workflow)
@~/.claude/paul-framework/references/quality-*.md (WALT — quality references)
</context>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | Lifecycle management (already active) | ● |
| /carl | required | Context rules (hooks active) | ● |

No blocking skills — this is an audit/research plan.
</skills>

<acceptance_criteria>

## AC-1: Complete PAUL Inventory
```gherkin
Given the PAUL framework at ~/.claude/paul-framework/
When the audit scans all subdirectories (workflows, references, templates, rules)
Then every .md file is listed with its purpose, category, and cross-references
```

## AC-2: Complete CARL Inventory
```gherkin
Given the CARL system at ~/.carl/ and ~/.claude/commands/carl/
When the audit scans all configuration and command files
Then every file is listed with its purpose and integration points (hooks, manifest, domains)
```

## AC-3: Complete TODD/WALT Inventory
```gherkin
Given TODD/WALT files embedded within the PAUL framework
When the audit identifies all TDD and quality-related files
Then each file is categorized as TODD or WALT with its role in the lifecycle
```

## AC-4: Dependency Map
```gherkin
Given the complete file inventory
When cross-references (@-references, imports, hook wiring) are analyzed
Then a dependency map shows which files reference which, and which components depend on each other
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Audit PAUL framework files</name>
  <files>.paul/phases/01-architecture-design/architecture-audit.md</files>
  <action>
    Scan ~/.claude/paul-framework/ recursively:
    - List every file in workflows/, references/, templates/, rules/
    - For each file: record path, purpose (from first few lines), category
    - Note all @-references to other files (cross-dependencies)
    - List all ~/.claude/commands/paul/ skill commands with their workflow targets

    Use subagents to scan directories in parallel for speed.
  </action>
  <verify>Every .md file in ~/.claude/paul-framework/ and ~/.claude/commands/paul/ appears in the audit</verify>
  <done>AC-1 satisfied: Complete PAUL inventory with purposes and cross-references</done>
</task>

<task type="auto">
  <name>Task 2: Audit CARL and TODD/WALT files</name>
  <files>.paul/phases/01-architecture-design/architecture-audit.md</files>
  <action>
    Scan CARL:
    - ~/.carl/ — manifest, context, global rules, domains, commands, sessions structure
    - ~/.claude/commands/carl/ — skill commands
    - Claude Code hooks that inject CARL rules (check ~/.claude/settings or hooks config)

    Scan TODD/WALT (within PAUL framework):
    - Identify all TDD-related files (plan-phase-tdd, apply-phase-tdd, tdd-detection, tdd.md)
    - Identify all quality-related files (quality-gate, quality-*.md references)
    - Categorize each as TODD or WALT

    Append to architecture-audit.md under CARL and TODD/WALT sections.
  </action>
  <verify>Every file in ~/.carl/ and all quality/tdd files in paul-framework/ appear in the audit</verify>
  <done>AC-2 and AC-3 satisfied: Complete CARL and TODD/WALT inventories</done>
</task>

<task type="auto">
  <name>Task 3: Build dependency map and component boundaries</name>
  <files>.paul/phases/01-architecture-design/architecture-audit.md</files>
  <action>
    Analyze cross-references from Tasks 1-2:
    - Map @-references between files (which workflow references which template/reference)
    - Identify hook integration points (CARL → Claude Code hooks → PAUL workflows)
    - Map command → workflow connections (skill commands → workflow files)
    - Document component boundaries: what's purely PAUL, purely CARL, shared, TODD overlay, WALT overlay

    Add "Dependency Map" and "Component Boundaries" sections to architecture-audit.md.
    Include a summary table showing component interaction matrix.
  </action>
  <verify>Dependency map covers all four components and shows inter-component connections</verify>
  <done>AC-4 satisfied: Dependency map shows cross-component references and boundaries</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- ~/.claude/paul-framework/* (source files — read only, do not modify)
- ~/.carl/* (source files — read only, do not modify)
- ~/.claude/commands/* (source files — read only, do not modify)
- .paul/PROJECT.md, .paul/ROADMAP.md (project config — not part of this plan)

## SCOPE LIMITS
- This plan is audit/inventory only — no design decisions
- Do not propose the unified structure yet (that's Plan 01-02)
- Do not modify any source files
- Do not create any code — output is documentation only

</boundaries>

<verification>
Before declaring plan complete:
- [ ] architecture-audit.md exists with all four component sections
- [ ] Every .md file in paul-framework/ is listed
- [ ] Every file in ~/.carl/ (excluding sessions) is listed
- [ ] All TODD files identified and categorized
- [ ] All WALT files identified and categorized
- [ ] Dependency map shows cross-component references
- [ ] Component boundaries section defines what belongs to each PAL
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- architecture-audit.md is comprehensive enough to inform Phase 1 Plan 2 (structure design)
</success_criteria>

<output>
After completion, create `.paul/phases/01-architecture-design/01-01-SUMMARY.md`
</output>
