---
phase: 215-runtime-delegation-packet-report-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kernel/workflows/apply-phase.md
  - .pi/agents/pals-implementer.md
autonomous: true
---

<objective>
## Goal
Harden repo-source APPLY delegation behavior and the repo-local `pals-implementer` contract so every delegated APPLY task is parent-packeted, report-structured, file-scoped, fallback-safe, and parent-verified.

## Purpose
Phase 214 defined the Delegated APPLY Packet/Report Contract. Phase 215 now applies that contract to runtime delegation sources so helper work can reduce parent context cost without weakening parent APPLY authority, module gates, changed-file checks, checkpoints, GitHub Flow safety, `.paul/*` lifecycle ownership, or command-output truth.

Planning posture: direct-requirements, high collaboration level from `pals.json` project default.

## Output
- Hardened `kernel/workflows/apply-phase.md` delegation path with explicit parent-owned task packet markers, report requirements, fallback triggers, changed-file diff checks, and parent-run official verification language.
- Hardened `.pi/agents/pals-implementer.md` helper prompt/report contract with allowed/forbidden file scope, stop/fallback behavior, complete report fields, and no helper-owned lifecycle/validation/module/GitHub Flow authority.
- No docs/skill-map/validation surfacing beyond task-local focused marker checks; Phase 216 owns user-facing docs and validation-suite marker additions.
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md
.paul/SPECIAL-FLOWS.md

## Contract Inputs
- `docs/PI-NATIVE-DELEGATED-APPLY-PACKET-REPORT-CONTRACT.md` — Phase 214 baseline requiring parent-owned task packets, structured helper reports, allowed/forbidden files, fallback triggers, parent-run official verification, changed-file diff checks, no helper-owned `.paul/*` writes, no helper-owned validation/module/GitHub Flow authority, and no hidden helper state.
- `.paul/phases/214-delegated-apply-packet-report-contract-baseline/214-01-SUMMARY.md` — confirms Phase 214 completed the contract baseline and hands Phase 215 runtime hardening to APPLY/helper-agent sources.
- `kernel/references/subagent-criteria.md` — defines bounded subagent eligibility, parent-verifiable delegation, and fallback preference when uncertain.

## Source Files
- `kernel/workflows/apply-phase.md`
- `.pi/agents/pals-implementer.md`
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: modules.yaml loaded from installed registry; advisory modules evaluated for planned runtime/helper files `kernel/workflows/apply-phase.md` and `.pi/agents/pals-implementer.md`.

### Advisory Findings
- ARCH: Detected pattern: layered docs/kernel/drivers/modules repository (medium confidence). Planned files are in expected layers: lifecycle workflow source under `kernel/workflows/` and repo-local Pi agent source under `.pi/agents/`. Advisory debt: `kernel/workflows/apply-phase.md` is 543 lines (>500 god-file warning), so APPLY edits must be narrow and avoid broad workflow rewrites; `.pi/agents/pals-implementer.md` is 62 lines. Directory-count warnings exist for `docs/`, `kernel/commands/paul`, and `.pi/prompt-assembler/tools`, but they are outside this plan's modified files. No circular dependency risk identified.
- TODD: No root test framework markers were found in root manifests. Existing lifecycle validation is shell-based (`tests/pi-end-to-end-validation.sh`, `tests/cross-harness-validation.sh`), so no RED-GREEN-REFACTOR conversion candidate was identified for these markdown/runtime-source edits.
- IRIS: No TODO/FIXME/HACK/XXX findings in planned files.
- DAVE: No CI config detected in `.github/workflows`, GitLab, Docker, Compose, or Jenkins surfaces. Suggestion remains out of scope: add `.github/workflows/ci.yml` in a separate approved plan if desired.
- DOCS: Documentation catalog exists under `docs/`; planned runtime/helper changes are contract-backed by `docs/PI-NATIVE-DELEGATED-APPLY-PACKET-REPORT-CONTRACT.md`. User-facing docs and validation surfacing are intentionally deferred to Phase 216.
- CODI: skipped — no extractable symbols in phase scope. The planned source selectors are markdown workflow/agent files, not `.ts/.tsx/.js/.jsx` code selectors.
- RUBY: Preliminary debt warning mirrors ARCH: `kernel/workflows/apply-phase.md` is already >500 lines. Keep edits localized to delegation paragraphs and avoid structural refactor in this phase.
- ARIA: No UI files in planned changes — skipping.
- DANA: No data files or migrations in planned changes — skipping.
- GABE: No API files in planned changes — skipping.
- LUKE: No UI files in planned changes — skipping.
- OMAR: No new observability surface planned. Existing retry/timeout wording in `kernel/workflows/apply-phase.md` is workflow guidance, not runtime logging/health behavior.
- PETE: No performance-sensitive runtime code, database query, rendering, or heavy import surface in planned changes.
- REED: Existing retry/timeout wording appears in workflow guidance only; no runtime retry loop or external-call resilience surface is planned.
- SETH: No hardcoded secret patterns detected in planned files; no auth/injection surface in scope.
- VERA: No privacy-relevant schema/model/user-data surface in planned changes; one generic word match (`address issues`) is non-PII workflow prose.

[dispatch] pre-plan enforcement: DEAN PASS — no root package manager lock/manifest detected for audit, no dependency files are in planned scope, and no dependency changes are allowed by this plan.

## Post-Plan Module Dispatch
[dispatch] post-plan: TODD reviewed plan type `execute`; no existing test framework covers these planned markdown/runtime-source changes, so no TDD conversion was suggested. Shell validation commands remain required in task and final verification.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | All planning, applying, and unifying work | ✓ |
| /carl | required | Automatic session boundary monitoring | automatic |
| TODD | required | Before/during implementation and post-plan/post-task checks | module dispatch |
| WALT | required | APPLY/UNIFY quality gates and validation | module dispatch |

**BLOCKING:** Required skills MUST be loaded or active before APPLY proceeds.
</skills>

<acceptance_criteria>
## AC-1: Parent-owned delegation packet is explicit
```gherkin
Given an approved autonomous APPLY task is considered for repo-local `pals-implementer` delegation
When `kernel/workflows/apply-phase.md` describes the delegation path
Then the parent workflow requires a parent-owned task packet with packet identity, plan path, task objective/action, allowed files, forbidden files, context sources, fallback triggers, parent-run official verification, module caveats, checkpoint status, report schema, and an authority notice before dispatch
```

## AC-2: Helper report acceptance is structured and fallback-safe
```gherkin
Given a delegated helper returns from a bounded APPLY task
When parent APPLY reviews the result
Then the workflow requires a complete structured report, compares `files_changed` and actual `git diff --name-only` output against allowed files, falls back inline on incomplete/unverifiable/out-of-scope/non-equivalent results, and reruns official verification before marking task PASS
```

## AC-3: Helper agent cannot own lifecycle authority
```gherkin
Given the repo-local `pals-implementer` receives a delegated task packet
When it edits and reports on bounded repo-local work
Then it stays inside allowed files, avoids `.paul/*` lifecycle writes and installed absolute paths, stops on ambiguity/checkpoints/scope creep, reports fallback recommendations, and never claims official verification, module gate, checkpoint, GitHub Flow, APPLY/UNIFY, or lifecycle-state authority
```

## AC-4: Phase boundaries and validation truth are preserved
```gherkin
Given Phase 216 owns docs/skill-map/validation surfacing and Phase 217 owns evidence reconciliation
When Phase 215 runtime/helper hardening is complete
Then source changes are limited to the APPLY workflow and repo-local helper-agent source, focused markers prove contract coverage, parent-run validation remains command-output truth, and existing validation suites pass without validation-count changes unless explicitly reconciled
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Harden APPLY workflow delegation packet and acceptance rules</name>
  <files>kernel/workflows/apply-phase.md</files>
  <action>
    Update only the delegation-related portions of `kernel/workflows/apply-phase.md` to align with `docs/PI-NATIVE-DELEGATED-APPLY-PACKET-REPORT-CONTRACT.md`:
    - Add or tighten explicit marker language for `Parent-owned task packet`, `Allowed files:`, `Forbidden files:`, `Fallback triggers:`, `Parent-run official verification:`, and `Authority: Derived aid only; no helper-owned .paul/* lifecycle writes.`
    - Expand the parent-owned packet fields so the APPLY workflow names packet id, plan path, task name, objective/action, allowed files, forbidden files, context sources, verification to attempt, parent official verification, module caveats, checkpoint status, fallback triggers, report schema, and authority notice.
    - Expand structured helper report acceptance to require status, packet id, task name, files changed, summary, commands run, verification attempted/results, deviations, concerns, fallback recommendation, and parent review required.
    - Make parent acceptance explicit: inspect helper report, compare `git diff --name-only` and report `files_changed` against `allowed_files`, inspect content diffs, rerun the official `<verify>` in the parent session, run module hooks, and classify task status only by parent judgment.
    - Tighten fallback triggers for missing/malformed reports, blocked/fallback-recommended results, out-of-scope file changes, unverifiable checks, checkpoint/human-decision needs, `.paul/*` lifecycle writes, GitHub Flow/CI/review/merge questions, hidden helper state, or non-equivalent results.
    Avoid: broad APPLY workflow redesign, checkpoint semantics rewrites, GitHub Flow automation changes, validation script changes, docs surfacing, dependency changes, or edits outside the delegation path.
  </action>
  <verify>grep -n "Parent-owned task packet\|Allowed files:\|Forbidden files:\|Fallback triggers:\|Parent-run official verification:\|Authority: Derived aid only; no helper-owned .paul/\* lifecycle writes\|changed-file diff\|structured helper report" kernel/workflows/apply-phase.md</verify>
  <done>AC-1 and AC-2 satisfied: APPLY delegation requires parent-owned packets, structured report review, file-scope checks, fallback, and parent-run official verification.</done>
</task>

<task type="auto">
  <name>Task 2: Harden repo-local pals-implementer prompt and report contract</name>
  <files>.pi/agents/pals-implementer.md</files>
  <action>
    Update `.pi/agents/pals-implementer.md` so the helper agent contract mirrors the Phase 214 packet/report rules:
    - Require the parent packet to provide `packet_id`, `plan_path`, `task_name`, task objective/action, `allowed_files`, `forbidden_files`, context sources, verification to attempt, parent official verification, fallback triggers, and report schema.
    - State that allowed files are repo-relative and explicit; forbidden files include `.paul/*` lifecycle artifacts, installed absolute paths, dependency manifests/lockfiles, CI config, GitHub Flow automation, generated install outputs, and any packet-protected files unless explicitly allowed.
    - Strengthen stop/fallback behavior for ambiguity, missing packet fields, scope creep, checkpoint/human decision/action, out-of-scope files, verification uncertainty/failure, `.paul/*` lifecycle needs, GitHub Flow/CI/review/merge questions, hidden-state needs, or non-equivalent outcomes.
    - Make the output report include `status`, `packet_id`, `task_name`, `files_changed`, `summary`, `commands_run`, `verification_attempted`, `verification_results`, `deviations`, `concerns`, `fallback_recommended`, and `parent_review_required`.
    - Explicitly forbid claiming official verification, module gate satisfaction, checkpoint completion, APPLY/UNIFY completion, GitHub Flow readiness, validation pass/fail authority, or lifecycle truth.
    Avoid: changing the agent into an autonomous lifecycle owner, adding hidden persistence/telemetry, expanding tools, changing model settings, or authorizing `.paul/*` lifecycle writes.
  </action>
  <verify>grep -n "packet_id\|allowed_files\|forbidden_files\|fallback_recommended\|parent_review_required\|no helper-owned\|official verification\|module gate\|GitHub Flow" .pi/agents/pals-implementer.md</verify>
  <done>AC-3 satisfied: repo-local helper stays packet-bounded, file-scoped, fallback-safe, and non-authoritative.</done>
</task>

<task type="auto">
  <name>Task 3: Verify contract markers, install from repo source, and run validation</name>
  <files></files>
  <action>
    Run parent-owned verification after Tasks 1 and 2:
    - Run focused marker greps for Helper Delegation runtime markers across `kernel/workflows/apply-phase.md` and `.pi/agents/pals-implementer.md`.
    - Run `PALS_ROOT="$PWD" bash drivers/pi/install.sh` so repo-source workflow/agent changes are propagated through the supported install path if the installer manages those resources.
    - Run `bash tests/pi-end-to-end-validation.sh` and `bash tests/cross-harness-validation.sh`.
    - Run `git diff --name-only` and confirm changed source scope is limited to `kernel/workflows/apply-phase.md` and `.pi/agents/pals-implementer.md` plus normal `.paul/*` lifecycle artifacts.
    Avoid: treating helper-attempted checks as official validation, editing validation scripts, adding Phase 216 docs/skill-map surfacing, or reconciling validation counts before Phase 217 unless counts unexpectedly change and must be documented as a deviation.
  </action>
  <verify>grep -n "Parent-owned task packet\|structured helper report\|Allowed files:\|Forbidden files:\|Fallback triggers:\|Parent-run official verification:\|no helper-owned .paul" kernel/workflows/apply-phase.md .pi/agents/pals-implementer.md && PALS_ROOT="$PWD" bash drivers/pi/install.sh && bash tests/pi-end-to-end-validation.sh && bash tests/cross-harness-validation.sh && git diff --name-only</verify>
  <done>AC-4 satisfied: runtime/helper markers are present, install/validation commands pass, and changed-file scope remains bounded.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- `docs/PI-NATIVE-DELEGATED-APPLY-PACKET-REPORT-CONTRACT.md` except reading it as authoritative input.
- `drivers/pi/extensions/pals-hooks.ts`
- `drivers/pi/extensions/README.md`
- `drivers/pi/skill-map.md`
- `README.md`
- `tests/pi-end-to-end-validation.sh`
- `tests/cross-harness-validation.sh`
- `kernel/workflows/plan-phase.md`
- `kernel/workflows/unify-phase.md`
- `kernel/references/module-dispatch.md`
- `kernel/references/git-strategy.md`
- `modules/**`
- `pals.json`
- Dependency manifests, lockfiles, CI configuration, GitHub Flow automation, generated runtime outputs, and installed absolute paths outside the repo
- `.paul/*` lifecycle artifacts except normal PLAN/APPLY/UNIFY lifecycle writes by the parent workflow

## SCOPE LIMITS
- Phase 215 is runtime/helper-agent hardening only for delegated APPLY packet/report behavior.
- Do not add docs/skill-map/user-facing validation surfacing; Phase 216 owns that.
- Do not reconcile validation count changes or close v2.50; Phase 217 owns that.
- Do not broadly refactor `kernel/workflows/apply-phase.md`; keep edits localized to delegation packet/report/fallback/file-scope/verification language.
- Do not introduce helper-owned `.paul/*` writes, helper-owned official verification, helper-owned module gates, helper-owned checkpoint completion, helper-owned GitHub Flow authority, hidden helper state, telemetry, dependencies, CI config, or lifecycle-authority changes.
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `grep -n "Parent-owned task packet\|Allowed files:\|Forbidden files:\|Fallback triggers:\|Parent-run official verification:\|Authority: Derived aid only; no helper-owned .paul/\* lifecycle writes\|changed-file diff\|structured helper report" kernel/workflows/apply-phase.md`
- [ ] `grep -n "packet_id\|allowed_files\|forbidden_files\|fallback_recommended\|parent_review_required\|official verification\|module gate\|GitHub Flow" .pi/agents/pals-implementer.md`
- [ ] `PALS_ROOT="$PWD" bash drivers/pi/install.sh`
- [ ] `bash tests/pi-end-to-end-validation.sh`
- [ ] `bash tests/cross-harness-validation.sh`
- [ ] `git diff --name-only` confirms changed source scope is limited to `kernel/workflows/apply-phase.md`, `.pi/agents/pals-implementer.md`, and normal `.paul/*` lifecycle artifacts.
- [ ] All acceptance criteria met.
</verification>

<success_criteria>
- APPLY workflow delegation requires a parent-owned task packet with explicit allowed/forbidden files, fallback triggers, parent-run official verification, changed-file diff checks, and derived-only authority notice.
- Helper reports are structured enough for parent inspection and cannot replace parent-run validation, module gates, task status classification, checkpoints, GitHub Flow gates, or lifecycle writes.
- Repo-local `pals-implementer` is explicitly packet-bounded, file-scoped, fallback-safe, and forbidden from lifecycle/validation/module/GitHub Flow authority.
- Focused marker checks, install command, Pi validation, and cross-harness validation pass or any deviation is documented before UNIFY.
- No Phase 216 docs/validation surfacing, Phase 217 reconciliation, broad workflow refactor, dependency/CI/GitHub Flow change, hidden state, telemetry, or lifecycle-authority change is introduced.
</success_criteria>

<output>
After completion, create `.paul/phases/215-runtime-delegation-packet-report-hardening/215-01-SUMMARY.md`.
</output>
