---
phase: 77-core-implementation
plan: 01
type: execute
wave: 1
depends_on: ["76-01"]
files_modified:
  - /Users/maxwellnewman/.pi/agent/extensions/pals-hooks.ts
autonomous: true
---

<objective>
## Goal
Implement production CARL session boundary manager logic in pals-hooks.ts: command context stashing on every /paul-* invocation, phase-completion detection via STATE.md loop position changes, context usage ratio check against configurable thresholds, and autonomous session creation + bootstrap + auto-resume when thresholds are exceeded.

## Purpose
This is the core CARL implementation. Phase 76 validated the APIs; this phase wires them into the PALS lifecycle so session boundaries are managed automatically. After this phase, CARL will detect phase completions, check context pressure, and create fresh sessions when needed — replacing the manual pause/resume cycle.

## Output
Production CARL logic in pals-hooks.ts:
- Command context stashing on all /paul-* commands
- CARL config loading from pals.json
- Phase-completion detection in agent_end
- Context usage ratio computation
- Decision model (continue vs fresh session)
- Session creation with bootstrap prompt from STATE.md
- Auto-resume via sendUserMessage
- Mid-phase safety valve monitoring in turn_end
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md

## Research & Design
.paul/phases/carl-research/carl-pi-design.md
.paul/phases/carl-research/gsd2-session-mechanisms.md

## Phase 76 Findings
.paul/phases/76-api-validation-prototype/api-validation-report.md

## Source Files
/Users/maxwellnewman/.pi/agent/extensions/pals-hooks.ts
</context>

<acceptance_criteria>

## AC-1: Command Context Stashing
```gherkin
Given the PALS extension is loaded
When any /paul-* command is invoked
Then the ExtensionCommandContext is stashed in a module-level variable
And the stashed context is available to event handlers (agent_end, turn_end)
```

## AC-2: Phase Completion Detection
```gherkin
Given a PALS project is active
When agent_end fires after a UNIFY completes (loop shows ✓ ✓ ✓)
Then CARL detects the loop-complete state by parsing STATE.md
And CARL evaluates the context usage decision model
```

## AC-3: Session Boundary Decision Model
```gherkin
Given CARL detects a phase completion
When context usage ratio is above continue_threshold (default 0.4)
Then CARL triggers /paul:pause, creates a fresh session with bootstrap, and sends /skill:paul-resume
When context usage ratio is below continue_threshold
Then CARL allows continuation in the same session (no session break)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Implement CARL core logic in pals-hooks.ts</name>
  <files>/Users/maxwellnewman/.pi/agent/extensions/pals-hooks.ts</files>
  <action>
    Replace the Phase 76 test command block (lines ~980-1077) with production CARL logic. Add new code in three areas:

    **A. CARL constants and state (add near existing constants, ~line 30):**
    ```
    const CARL_NEW_SESSION_TIMEOUT_MS = 10_000;
    const CARL_DEFAULT_CONTINUE_THRESHOLD = 0.4;
    const CARL_DEFAULT_SAFETY_CEILING = 0.8;
    const CARL_DEFAULT_STRATEGY = "phase-boundary";
    ```

    **B. CARL config loader (add as new helper function after existing helpers):**
    Create `loadCarlConfig(cwd: string)` that reads `pals.json` from `.paul/pals.json` or `pals.json` in cwd, extracts the `carl` section, and returns `{ session_strategy, continue_threshold, safety_ceiling }` with defaults.

    **C. CARL type and state for loop-change detection:**
    Create a type `CarlState` with `{ stashedCmdCtx, dispatching, previousLoopSignature, pauseAtNextBoundary }`. Initialize inside `palsHooks()`.

    **D. Command context stashing (modify existing command registration loop):**
    In the `for (const cmd of COMMANDS)` loop that registers `/paul-*` commands, stash `ctx` into `carlState.stashedCmdCtx` before calling `routeCommand`. This ensures every command invocation refreshes the stashed context.

    **E. CARL phase-completion handler (add to existing `agent_end` handler):**
    After `maybePresentGuidedWorkflow`, add CARL logic:
    1. Parse STATE.md loop position
    2. Compute a "loop signature" string (e.g., "✓✓✓" or "✓○○")
    3. Compare against `carlState.previousLoopSignature`
    4. If signature changed to all-complete (✓✓✓) AND strategy is not "manual":
       a. Read `ctx.getContextUsage()` and compute ratio
       b. If ratio >= continue_threshold OR strategy is "always-fresh":
          - Call `/paul:pause` equivalent (build handoff content inline)
          - Call `carlState.stashedCmdCtx.newSession({ setup })` with bootstrap from STATE.md
          - Call `pi.sendUserMessage("/skill:paul-resume")` for auto-resume
       c. If ratio < continue_threshold AND strategy is "phase-boundary":
          - Notify user: "CARL: context usage at X% — continuing in same session"
          - Do nothing (allow natural flow)
    5. Update `carlState.previousLoopSignature`

    **F. Mid-phase safety valve (add to existing `turn_end` handler):**
    After `syncLifecycleUi`, add:
    1. If strategy is "manual": skip
    2. Read `ctx.getContextUsage()`, compute ratio
    3. If ratio >= safety_ceiling AND not already flagged:
       - Set `carlState.pauseAtNextBoundary = true`
       - Notify user: "CARL: context pressure at X% — will pause at next task boundary"
    4. The `pauseAtNextBoundary` flag is consumed in `agent_end` to trigger session break even if loop is not complete

    **G. Remove Phase 76 test command:**
    Delete the entire `/paul-carl-test` registration block and its associated state variables (`carlTestDispatching`, `stashedCmdCtx`, `CARL_NEW_SESSION_TIMEOUT_MS` local const).

    **Implementation constraints:**
    - Keep CARL logic self-contained within clearly commented blocks
    - Reuse existing helpers: `parsePalsState`, `readFileOr`, `renderLoopBadge`
    - The bootstrap message for fresh sessions should use the same compact format from Phase 76's test command (milestone, phase, loop, next action)
    - All session creation must use the GSD2-proven patterns: Promise.race timeout, reentrancy guard
    - If stashedCmdCtx is undefined when CARL needs it (e.g., no /paul-* command was run), notify the user and skip the session break gracefully
    - Do NOT modify any existing guided workflow detection, activation model, or context injection logic
  </action>
  <verify>
    1. `grep -c "carl" /Users/maxwellnewman/.pi/agent/extensions/pals-hooks.ts` should show multiple CARL-related lines
    2. `grep "paul-carl-test" /Users/maxwellnewman/.pi/agent/extensions/pals-hooks.ts` should return 0 matches (test command removed)
    3. `grep "stashedCmdCtx" /Users/maxwellnewman/.pi/agent/extensions/pals-hooks.ts` should show stashing in command loop and usage in agent_end
    4. `grep "CARL_DEFAULT" /Users/maxwellnewman/.pi/agent/extensions/pals-hooks.ts` should show 3 constants
  </verify>
  <done>AC-1, AC-2, AC-3 satisfied: CARL core logic implemented with stashing, detection, decision model, session creation, and safety valve</done>
</task>

<task type="auto">
  <name>Task 2: Verify extension structural integrity</name>
  <files>/Users/maxwellnewman/.pi/agent/extensions/pals-hooks.ts</files>
  <action>
    Verify the modified pals-hooks.ts is structurally sound:
    1. Check that the file parses without TypeScript errors (balanced braces, valid syntax)
    2. Verify all existing exports are preserved (default export of palsHooks function)
    3. Verify no existing event handlers were accidentally removed or broken
    4. Count existing handlers to confirm: session_start, before_agent_start, turn_end, agent_end, context — all 5 must still be present
    5. Verify the COMMANDS array is unchanged (11 commands)
    6. Verify all keyboard shortcuts are still registered (5 shortcuts)

    If any structural issues are found, fix them immediately.
  </action>
  <verify>
    Run these checks:
    - `grep -c 'pi.on(' /Users/maxwellnewman/.pi/agent/extensions/pals-hooks.ts` should be 5
    - `grep -c 'pi.registerCommand(' /Users/maxwellnewman/.pi/agent/extensions/pals-hooks.ts` should be 11 (no more test command)
    - `grep -c 'registerQuickActionShortcut(' /Users/maxwellnewman/.pi/agent/extensions/pals-hooks.ts` should be 5
    - `grep 'export default' /Users/maxwellnewman/.pi/agent/extensions/pals-hooks.ts` should show palsHooks
  </verify>
  <done>AC-1, AC-2, AC-3 structurally verified: extension is sound with all existing functionality preserved plus CARL additions</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Existing guided workflow detection logic (detectGuidedWorkflowMoment and related functions)
- Existing activation model (markActivation, getActiveActivation, consumeActivationTurn)
- Existing context injection logic (buildPalsContextPayload, shouldInjectPalsContext)
- Existing UI rendering (renderLifecycleStatus, renderLifecycleWidget, syncLifecycleUi)
- Existing COMMANDS array (11 commands, their names, descriptions, skills, guidance)
- Existing keyboard shortcuts (5 shortcuts)
- Any .paul/ workflow, template, or reference files

## SCOPE LIMITS
- No pals.json carl config creation (Phase 78)
- No AGENTS.md template (Phase 78)
- No dead carl/ file removal (Phase 78)
- No pause/resume workflow modifications (Phase 78)
- No documentation updates (Phase 79)
- No validation suite updates (Phase 79)
- CARL reads config from pals.json but does NOT write it — config creation belongs to Phase 78

</boundaries>

<verification>
Before declaring plan complete:
- [ ] CARL constants defined (CARL_DEFAULT_CONTINUE_THRESHOLD, CARL_DEFAULT_SAFETY_CEILING, CARL_DEFAULT_STRATEGY)
- [ ] loadCarlConfig helper reads pals.json carl section with defaults
- [ ] Command context stashed on every /paul-* command invocation
- [ ] agent_end detects phase completion via loop signature change
- [ ] agent_end computes context ratio and applies decision model
- [ ] Session creation uses timeout + reentrancy guard
- [ ] Bootstrap message injects STATE.md summary
- [ ] Auto-resume sends /skill:paul-resume after session creation
- [ ] turn_end monitors safety ceiling and sets pause flag
- [ ] Phase 76 test command removed
- [ ] All 5 event handlers preserved
- [ ] All 11 commands preserved (no test command)
- [ ] Export default palsHooks preserved
</verification>

<success_criteria>
- CARL core logic implemented in pals-hooks.ts
- Phase-completion detection works via STATE.md loop signature
- Decision model applies configurable thresholds
- Session creation + bootstrap + auto-resume chain complete
- Mid-phase safety valve monitors context pressure
- No regressions to existing extension functionality
- Phase 76 test command cleanly removed
</success_criteria>

<output>
After completion, create `.paul/phases/77-core-implementation/77-01-SUMMARY.md`
</output>
