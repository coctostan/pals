---
phase: 295-context-offload-measurement-next-target-ranking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/PI-NATIVE-CONTEXT-OFFLOAD-MEASUREMENT-NEXT-TARGET-RANKING.md
  - drivers/pi/extensions/README.md
  - drivers/pi/skill-map.md
  - tests/pi-end-to-end-validation.sh
  - tests/cross-harness-validation.sh
autonomous: true
---

<objective>
## Goal
Measure the context offload achieved by the completed S4 extraction, rank the next safe TypeScript runtime offload candidates, and preserve that recommendation in docs plus validation without implementing another target.

## Purpose
v2.69 intentionally ends with evidence, not another extraction. Phase 293 defined the S4 boundary and Phase 294 executed it. Phase 295 should convert those results into a durable measurement/ranking artifact so the next milestone can choose the highest-value context-bloat reduction target without relying on chat memory or broad source rereads.

## Output
- New report: `docs/PI-NATIVE-CONTEXT-OFFLOAD-MEASUREMENT-NEXT-TARGET-RANKING.md`.
- Pi docs updated to point at the Phase 295 report and state that the next target ranking is recommendation-only.
- Validation guardrails updated to assert the report exists, names the S4 offload result, preserves recommendation-only wording, and records ranked next-target candidates.
</objective>

<context>
## Project Context
.paul/STATE.md — Phase 295 is ready to plan after Phase 294 UNIFY and PR #213 merge.
.paul/ROADMAP.md — Phase 295 focus: measure remaining agent-context burden after S4 and rank next safe TypeScript offload candidates; no implementation.
.paul/PROJECT.md — current value remains minimal kernel, modular pals, universal drivers; Pi runtime assistance must reduce context burden without weakening `.paul/*` lifecycle truth.

## Direct Prior Work
- `.paul/phases/294-s4-canonical-reply-delivery-extraction/294-01-SUMMARY.md` — S4 extraction result, exact moved symbols, validation counts, and PR #213 evidence.
- `docs/PI-NATIVE-GUIDED-WORKFLOW-CANONICAL-REPLY-DELIVERY-EXTRACTION-CONTRACT.md` — S4 contract plus Phase 294 status.
- `docs/PI-NATIVE-EXTENSION-MODULARIZATION-CONTRACT.md` — original S1–S8 inventory and classification baseline.

## Source Context
Current measured extension file sizes before PLAN creation:

| File | Lines | Bytes |
| --- | ---: | ---: |
| `drivers/pi/extensions/pals-hooks.ts` | 546 | 19,210 |
| `drivers/pi/extensions/guided-workflow-delivery.ts` | 148 | 5,855 |
| `drivers/pi/extensions/guided-workflow-detection.ts` | 356 | 13,236 |
| `drivers/pi/extensions/pals-context-injection.ts` | 88 | 4,054 |
| `drivers/pi/extensions/lifecycle-ui.ts` | 123 | 5,177 |
| `drivers/pi/extensions/command-routing.ts` | 230 | 8,270 |
| `drivers/pi/extensions/workflow-resource-capsule-rendering.ts` | 192 | 9,097 |
| `drivers/pi/extensions/artifact-slice-rendering.ts` | 183 | 8,233 |
| `drivers/pi/extensions/module-activity-parsing.ts` | 172 | 5,974 |

During APPLY, recompute metrics from fresh repo state and, where possible, compare against pre-S4 `pals-hooks.ts` from the Phase 293/294 boundary using git history. Do not treat the table above as final validation truth.
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan enforcement: DEAN PASS — planned files contain docs/validation scripts only; no dependency manifests or lockfiles in scope.
[dispatch] pre-plan advisory: ARCH skipped — no in-scope runtime source files are modified; Phase 295 is measurement/docs/validation only.
[dispatch] pre-plan advisory: TODD PASS — no code implementation or pure-core behavior selected; repo shell validation remains the verification strategy.
[dispatch] pre-plan advisory: SETH PASS — no auth/secret/runtime input handling changes planned; recommendation-only report must not introduce hidden lifecycle authority.
[dispatch] pre-plan advisory: IRIS PASS — new report and doc updates should not introduce TODO/FIXME/HACK/XXX markers.
[dispatch] pre-plan advisory: GABE/DANA/LUKE/ARIA/VERA skipped — no API, data, UI component, or privacy data files in scope.
[dispatch] pre-plan advisory: DAVE WARN — no CI workflow config in changed set; use existing repo shell validation and PR Socket checks.
[dispatch] pre-plan advisory: DOCS PASS — docs-only/reporting scope; update Pi extension README and skill-map to surface the Phase 295 ranking report.
[dispatch] pre-plan advisory: OMAR/REED/PETE PASS — report may rank observability/resilience/performance-sensitive candidates only from source-backed evidence; no runtime behavior changes.
[dispatch] pre-plan advisory: CODI skipped — no TS/JS behavior-change seed selected; Phase 295 ranks candidates but does not alter runtime symbols.
[dispatch] pre-plan advisory: RUBY PASS — measurement may cite file sizes and candidate debt signals, but must not perform refactors or invent complexity metrics.

## Post-Plan Module Dispatch
[dispatch] post-plan: TODD PASS — plan type remains `execute`; no TDD conversion because scope is docs/report/validation only and no code behavior is implemented.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | PLAN/APPLY/UNIFY lifecycle management | ✓ |
| /carl | required | Session boundary management during lifecycle transitions | automatic |
| TODD | required | Before/during code implementation; satisfied by no-code recommendation scope plus shell validation | n/a |
| WALT | required | APPLY quality/validation gates | ○ |

**BLOCKING:** Required skills must be satisfied before APPLY proceeds. WALT validation gates remain mandatory during APPLY.
</skills>

<acceptance_criteria>
## AC-1: Context-Offload Metrics Recorded
```gherkin
Given Phase 294 extracted S4 delivery from `pals-hooks.ts`
When Phase 295 APPLY completes
Then the new report records fresh measured line/byte counts for current Pi extension siblings and compares the S4 offload against a source-backed pre-S4 baseline or explicitly documents why a baseline could not be reconstructed.
```

## AC-2: Next Target Ranking Is Evidence-Backed
```gherkin
Given multiple TypeScript runtime surfaces remain possible future offload targets
When the report ranks candidates
Then each ranked candidate includes source-backed rationale, risk/authority constraints, validation impact, expected context-offload value, and a clear recommendation without authorizing implementation.
```

## AC-3: Recommendation-Only Boundary Preserved and Surfaced
```gherkin
Given Phase 295 is not allowed to implement another runtime extraction
When docs and validation are updated
Then Pi docs and validation guardrails name the Phase 295 report, assert recommendation-only wording, and do not require or imply any new runtime sibling implementation.
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Create context-offload measurement and ranking report</name>
  <files>docs/PI-NATIVE-CONTEXT-OFFLOAD-MEASUREMENT-NEXT-TARGET-RANKING.md</files>
  <action>
    Create `docs/PI-NATIVE-CONTEXT-OFFLOAD-MEASUREMENT-NEXT-TARGET-RANKING.md`.

    The report must include:
    - Status/header: Phase 295 recommendation-only artifact; not runtime authority.
    - Evidence base: Phase 293 contract, Phase 294 SUMMARY, S4 extraction contract, current `drivers/pi/extensions/*.ts` file metrics, and validation count sources.
    - Measurement method: exact commands or reproducible `git show`/working-tree measurement steps used to compute line/byte counts. Use fresh values during APPLY; if comparing to the Phase 293/294 boundary, prefer `git show 17578fb0:drivers/pi/extensions/pals-hooks.ts` or the nearest source-backed pre-S4 commit and name the commit used.
    - Measurement table: current line/byte counts for `pals-hooks.ts` and all extracted Pi extension siblings, plus S4-specific delta if reconstructable.
    - Remaining-burden analysis: identify what still lives in `pals-hooks.ts` (runtime registration/orchestration, CARL/session-boundary helpers, shared helpers, lifecycle event wiring) and why some pieces should stay inline.
    - Ranked next-target candidates with at least 3 candidates and at most 5. Each candidate must include expected context-offload value, risk, authority constraints, validation burden, dependencies, and recommended next action.
    - Explicit winner recommendation for the next milestone, plus runner-up and “do not extract yet” notes.
    - Non-goals: no implementation in Phase 295, no `.paul/*` runtime writes, no telemetry, no hidden state, no auto-approval/auto-continue/skipped checkpoints, no UI-only lifecycle decisions, no inferred merge intent, no changes to current S4/S3 behavior.

    Candidate set should be source-backed and may include:
    - CARL/session-boundary helper extraction from `pals-hooks.ts` if it remains compact and authority-safe.
    - Shared filesystem/state helper extraction only if it reduces import cycles and does not create lifecycle authority.
    - Runtime registration/orchestration cleanup only as a low-priority/defer candidate because it is the supported entry point.
    - Validation helper/script decomposition only if it reduces Pi e2e maintenance burden without weakening TAP count truth.
    - Any remaining doc/contract consolidation needed before future runtime work.

    Avoid: implementing any ranked target, editing TypeScript runtime files, changing validation semantics beyond report guardrails, inventing metrics, or presenting recommendations as approved scope.
  </action>
  <verify>Docs/source checks confirm the report contains `recommendation-only`, `S4`, `guided-workflow-delivery.ts`, `pals-hooks.ts`, `Ranked Candidates`, `Validation Impact`, and `No implementation in Phase 295`; fresh measurement commands are quoted or summarized with values.</verify>
  <done>AC-1 and AC-2 satisfied: measurement and ranking are durable, source-backed, and recommendation-only.</done>
</task>

<task type="auto">
  <name>Task 2: Surface the Phase 295 report in Pi docs</name>
  <files>drivers/pi/extensions/README.md, drivers/pi/skill-map.md</files>
  <action>
    Update Pi-facing docs to mention the Phase 295 context-offload measurement/ranking report.

    In `drivers/pi/extensions/README.md`:
    - Add a compact note near the modular extension/source-set or guided workflow discussion that Phase 295 measured the S4 offload and ranked future TypeScript offload candidates in `docs/PI-NATIVE-CONTEXT-OFFLOAD-MEASUREMENT-NEXT-TARGET-RANKING.md`.
    - State the report is recommendation-only and does not authorize another runtime extraction.

    In `drivers/pi/skill-map.md`:
    - Add the same report reference near the Pi extension/helper surface discussion.
    - Preserve the current command/skill mapping and guided workflow no-auto/no-hidden-authority boundaries.

    Avoid: changing command behavior descriptions, shortcut lists, runtime install semantics, or claiming Phase 295 implemented a second extraction.
  </action>
  <verify>Focused grep finds `PI-NATIVE-CONTEXT-OFFLOAD-MEASUREMENT-NEXT-TARGET-RANKING.md` and `recommendation-only` in both docs.</verify>
  <done>AC-3 satisfied: human-facing Pi docs surface the report and its non-implementation boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Add bounded validation guardrails and run full validation</name>
  <files>tests/pi-end-to-end-validation.sh, tests/cross-harness-validation.sh</files>
  <action>
    Add bounded validation guardrails for the Phase 295 report.

    In `tests/pi-end-to-end-validation.sh`:
    - Add one focused check that verifies the report exists and contains the key markers: `recommendation-only`, `S4`, `guided-workflow-delivery.ts`, `pals-hooks.ts`, `Ranked Candidates`, and `No implementation in Phase 295`.
    - Add or update a discovery-surface check that the Pi README/skill-map reference the report.

    In `tests/cross-harness-validation.sh`:
    - Add one shared-invariant check that verifies the report preserves recommendation-only wording and does not imply new runtime authority.

    Run required validation and copy exact results to SUMMARY:
    - `git diff --check`
    - `bash tests/helpers/artifact_consistency.sh`
    - `bash tests/pi-end-to-end-validation.sh`
    - `bash tests/cross-harness-validation.sh`

    Avoid: weakening existing guardrails, deleting checks, adding runtime source requirements, or changing validation counts without documenting the fresh output delta.
  </action>
  <verify>All listed validation commands pass; any TAP count deltas are copied from fresh output into SUMMARY.</verify>
  <done>AC-1, AC-2, and AC-3 satisfied with validation evidence ready for UNIFY.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- Runtime TypeScript extension behavior in `drivers/pi/extensions/*.ts`.
- `drivers/pi/install.sh`, `drivers/pi/uninstall.sh`, `drivers/pi/driver.yaml`, dependency manifests/locks, CI configs, Dockerfiles, Claude Code driver, Agent SDK driver, kernel workflow sources, or module registry files.
- `.paul/*` lifecycle authority from runtime code.
- S3/S4 guided workflow behavior, command routing, lifecycle UI, context injection, artifact slices, workflow capsules, module activity parsing, or CARL behavior.

## SCOPE LIMITS
- This plan creates a measurement/ranking report, surfaces it in docs, and adds validation guardrails only.
- No second target implementation, no runtime refactor, no source extraction, no telemetry, no hidden state, no generated ledger, no auto-approval, no auto-continue, no skipped checkpoints, no UI-only lifecycle decisions, and no inferred merge intent.
- Candidate ranking is recommendation-only until a future approved PLAN authorizes implementation.
</boundaries>

<verification>
Before declaring APPLY complete:
- [ ] `docs/PI-NATIVE-CONTEXT-OFFLOAD-MEASUREMENT-NEXT-TARGET-RANKING.md` exists and contains source-backed current metrics plus ranked candidates.
- [ ] Focused grep/source checks confirm `recommendation-only`, `No implementation in Phase 295`, `guided-workflow-delivery.ts`, `pals-hooks.ts`, and `Ranked Candidates` are present.
- [ ] Pi README and skill-map reference the report and preserve recommendation-only wording.
- [ ] `git diff --check`
- [ ] `bash tests/helpers/artifact_consistency.sh`
- [ ] `bash tests/pi-end-to-end-validation.sh`
- [ ] `bash tests/cross-harness-validation.sh`
- [ ] `git diff --name-only` contains only planned docs/validation plus expected `.paul/phases/295-*` PLAN/SUMMARY and lifecycle artifacts.
</verification>

<success_criteria>
- Phase 295 produces a durable, source-backed context-offload measurement and ranking artifact.
- The next recommended target is explicit, but no implementation is performed or implied.
- Validation protects the report and docs without weakening existing Pi/runtime guardrails.
- APPLY evidence is ready for UNIFY and milestone closure.
</success_criteria>

<output>
After completion, create `.paul/phases/295-context-offload-measurement-next-target-ranking/295-01-SUMMARY.md` with:
- Measurement method and final line/byte table.
- Ranked candidates and chosen next recommendation.
- Files changed and validation count reconciliation.
- Module execution reports, including WALT/DEAN/DOCS/CODI/RUBY notes.
- PR URL, CI/check state, and milestone-completion readiness.
</output>
