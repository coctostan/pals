---
phase: 205-validation-evidence-reconciliation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/pi-end-to-end-validation.sh
  - tests/cross-harness-validation.sh
  - docs/PI-NATIVE-ARTIFACT-SLICE-CONTRACT.md
autonomous: true
---

<objective>
## Goal
Reconcile v2.47 artifact-slice validation and evidence so runtime-lens markers, Pi/cross-harness command-output proof, module evidence, and GitHub Flow proof are protected by durable checks and current documentation.

## Purpose
Phase 203 hardened the Pi artifact-slice runtime and Phase 204 surfaced it in docs. Phase 205 closes the milestone by making the validation/evidence contract match the implemented surfaces and by preserving command-output truth before handing off to the next Pi-native build milestone.

## Output
- Updated validation marker checks in `tests/pi-end-to-end-validation.sh` and `tests/cross-harness-validation.sh`.
- Updated evidence/baseline wording in `docs/PI-NATIVE-ARTIFACT-SLICE-CONTRACT.md` if validation counts or evidence requirements are reconciled.
- Completion evidence in `205-01-SUMMARY.md` after APPLY/UNIFY.
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md
.paul/phases/204-documentation-skill-map-surfacing/204-01-SUMMARY.md

## Source Files
docs/PI-NATIVE-ARTIFACT-SLICE-CONTRACT.md
drivers/pi/extensions/pals-hooks.ts
drivers/pi/extensions/README.md
drivers/pi/skill-map.md
tests/pi-end-to-end-validation.sh
tests/cross-harness-validation.sh
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: TODD, IRIS, DAVE, DOCS, CODI, RUBY, ARCH, SETH, and local scope probes completed; advisory only.

- TODD: No repo-local `*.test.*`, `*.spec.*`, or `__tests__` test files detected outside ignored runtime/vendor paths; validation is shell-suite based. Plan remains `execute`, not `tdd`.
- IRIS: No TODO/FIXME/HACK/XXX markers found in planned files.
- DAVE: No repo-local CI config found outside ignored runtime/vendor paths; keep command-output validation local for this plan and do not add CI.
- DOCS: Documentation catalog includes `docs/PI-NATIVE-ARTIFACT-SLICE-CONTRACT.md`; planned validation-script edits have nearby project docs via the artifact-slice contract and validation/evidence strategy.
- CODI: NO MAGIC INFERENCE — only explicit source selectors. Seeds from `drivers/pi/extensions/pals-hooks.ts`: `buildArtifactSlice`, `getArtifactSliceSpecs`, `renderArtifactSlices`, `shouldInjectPalsContext`, `buildPalsContextPayload`.

### Blast Radius (CODI)
**buildArtifactSlice**
```text
indexing-failed (0s ago): Bun.YAML.parse is unavailable in this runtime
drivers/pi/extensions/pals-hooks.ts:293:e4f8  renderArtifactSlices  behavioral  depth:1  [fan-in:1, fan-out:2, roles:none, coverage:untested, co-change:0.00, chain-confidence:0.90]
drivers/pi/extensions/pals-hooks.ts:599:4794  buildPalsContextPayload  behavioral  depth:2  [fan-in:1, fan-out:1, roles:none, coverage:untested, co-change:0.00, chain-confidence:0.90]
drivers/pi/extensions/pals-hooks.ts:1185:cdd1  palsHooks  behavioral  depth:3  [fan-in:0, fan-out:21, roles:entry-point, coverage:untested, co-change:0.00, chain-confidence:0.90]
```

**getArtifactSliceSpecs**
```text
drivers/pi/extensions/pals-hooks.ts:293:e4f8  renderArtifactSlices  behavioral  depth:1  [fan-in:1, fan-out:2, roles:none, coverage:untested, co-change:0.00, chain-confidence:0.90]
drivers/pi/extensions/pals-hooks.ts:599:4794  buildPalsContextPayload  behavioral  depth:2  [fan-in:1, fan-out:1, roles:none, coverage:untested, co-change:0.00, chain-confidence:0.90]
drivers/pi/extensions/pals-hooks.ts:1185:cdd1  palsHooks  behavioral  depth:3  [fan-in:0, fan-out:21, roles:entry-point, coverage:untested, co-change:0.00, chain-confidence:0.90]
```

**renderArtifactSlices**
```text
drivers/pi/extensions/pals-hooks.ts:599:4794  buildPalsContextPayload  behavioral  depth:1  [fan-in:1, fan-out:1, roles:none, coverage:untested, co-change:0.00, chain-confidence:0.90]
drivers/pi/extensions/pals-hooks.ts:1185:cdd1  palsHooks  behavioral  depth:2  [fan-in:0, fan-out:21, roles:entry-point, coverage:untested, co-change:0.00, chain-confidence:0.90]
```

**shouldInjectPalsContext**
```text
drivers/pi/extensions/pals-hooks.ts:1185:cdd1  palsHooks  behavioral  depth:1  [fan-in:0, fan-out:21, roles:entry-point, coverage:untested, co-change:0.00, chain-confidence:0.90]
```

**buildPalsContextPayload**
```text
drivers/pi/extensions/pals-hooks.ts:1185:cdd1  palsHooks  behavioral  depth:1  [fan-in:0, fan-out:21, roles:entry-point, coverage:untested, co-change:0.00, chain-confidence:0.90]
```

[dispatch] CODI: pre-plan advisory → impact × 5 symbols → 5 resolved, 0 unresolved, 10 total call-sites, injected blast_radius
- RUBY: Planned files include two large shell validation scripts (`1403` and `1015` lines) and one 225-line doc. Keep changes localized; do not broaden validation-suite structure.
- ARCH: Detected pattern: flat / kernel-and-driver repository (medium confidence). Planned files are in expected validation/docs locations. `wc -l`: `tests/pi-end-to-end-validation.sh` 1403, `tests/cross-harness-validation.sh` 1015, `docs/PI-NATIVE-ARTIFACT-SLICE-CONTRACT.md` 225. Existing validation scripts are large; add focused checks only.
- SETH: No hardcoded secret markers found in planned files.
- ARIA/LUKE/DANA/GABE/OMAR/PETE/REED/VERA: No UI, data, API, observability, performance-import, resilience, or privacy-relevant planned file types/patterns detected.

[dispatch] pre-plan enforcement: DEAN PASS — no package manager manifests or lockfiles detected at repo root/maxdepth 2 outside ignored runtime/vendor paths, so dependency audit is not applicable and no critical/high dependency block exists.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | PLAN/APPLY/UNIFY lifecycle work | ✓ |
| /carl | required | Session boundary monitoring | automatic |
| TODD | required | Before/during implementation; shell-suite validation substitutes for TDD conversion here | via module dispatch |
| WALT | required | APPLY/UNIFY quality validation | via module dispatch |

**BLOCKING:** Required skills MUST be loaded or active before APPLY proceeds.
</skills>

<acceptance_criteria>
## AC-1: Runtime-lens marker checks are reconciled
```gherkin
Given the implemented artifact-slice runtime emits activation-gated, source-cited, freshness-marked, bounded, fallback-safe, read-only, non-authoritative slices
When the Pi validation suite runs
Then it checks the repo-source extension and Pi docs for the named slice inventory, schema markers, fallback triggers, no-hidden-state boundary, and no Pi-owned lifecycle/module/validation ledger language.
```

## AC-2: Cross-harness validation protects shared evidence invariants
```gherkin
Given artifact slices are Pi-supported runtime aids but `.paul/*`, module reports, GitHub Flow evidence, and command output remain shared authority
When the cross-harness validation suite runs
Then it verifies the repo-source contract/docs/validation surfaces preserve source citation, freshness, bounds, fallback, module evidence, GitHub Flow evidence, command-output truth, and no hidden/Pi-owned ledger markers without requiring frozen legacy behavior changes.
```

## AC-3: Evidence contract matches current validation baselines
```gherkin
Given Phase 203 and Phase 204 completed with Pi validation at 187/187 and cross-harness validation at 100/100
When `docs/PI-NATIVE-ARTIFACT-SLICE-CONTRACT.md` is reviewed after this plan
Then command-output evidence requirements and count-change reconciliation language reflect the current v2.47 baseline and require SUMMARY evidence for any validation-count changes.
```

## AC-4: Scope stays limited to validation/evidence reconciliation
```gherkin
Given Phase 205 is a milestone-closing validation/evidence phase
When APPLY completes
Then no runtime behavior, installer behavior, dependency manifest, CI config, workflow/capsule/helper-compression behavior, legacy cleanup, or lifecycle authority semantics are changed.
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Reconcile artifact-slice marker checks in validation suites</name>
  <files>tests/pi-end-to-end-validation.sh, tests/cross-harness-validation.sh</files>
  <action>
    Add focused marker checks that make the validation/evidence contract durable:
    - In the Pi suite, keep the existing runtime-lens checks and add/adjust checks only if needed so the suite explicitly covers slice names, `Source:`, `Freshness:`, `Bounds:`, `Fallback: full authoritative read`, `Authority:`, `Derived aid only`, activation gating, no hidden persistence, no Pi-owned lifecycle/module/validation ledger, and full-read fallback triggers.
    - In the cross-harness suite, add repo-source shared-invariant checks around `docs/PI-NATIVE-ARTIFACT-SLICE-CONTRACT.md`, `drivers/pi/extensions/README.md`, `drivers/pi/skill-map.md`, and the validation scripts so command-output truth, module evidence, GitHub Flow evidence, and no-authority-replacement boundaries are validated across the repo source.
    - Keep additions local to existing helper style (`tap_file_contains_all`, direct `grep -Fq`, or small grouped checks). Do not refactor the large shell suites or introduce new dependencies.
    - Avoid broadening into CI creation, runtime behavior changes, installer changes, legacy parity changes, or validation classification redesign.
  </action>
  <verify>bash tests/pi-end-to-end-validation.sh && bash tests/cross-harness-validation.sh</verify>
  <done>AC-1 and AC-2 satisfied: both validation suites pass and visibly protect artifact-slice runtime-lens/shared-evidence markers.</done>
</task>

<task type="auto">
  <name>Task 2: Update artifact-slice evidence baseline wording</name>
  <files>docs/PI-NATIVE-ARTIFACT-SLICE-CONTRACT.md</files>
  <action>
    Reconcile the contract's command-output evidence section with the current v2.47 state:
    - Preserve command output as validation truth.
    - Update stale baseline wording where it still describes the pre-Phase-203 Pi count as the current implementation baseline.
    - State that current post-Phase-203/204 evidence is Pi 187/187 and cross-harness 100/100, and that any future count changes must record old count, new count, reason, validation class affected, and shared-invariant preservation rationale in SUMMARY.
    - Keep Phase 202 contract-baseline historical statements clear; do not rewrite the document as if Phase 202 itself implemented runtime changes.
  </action>
  <verify>grep -n "187/187\|100/100\|old count\|new count\|command output" docs/PI-NATIVE-ARTIFACT-SLICE-CONTRACT.md</verify>
  <done>AC-3 satisfied: the evidence contract reflects current baselines while preserving historical Phase 202 boundary language.</done>
</task>

<task type="auto">
  <name>Task 3: Run final evidence and scope checks</name>
  <files>tests/pi-end-to-end-validation.sh, tests/cross-harness-validation.sh, docs/PI-NATIVE-ARTIFACT-SLICE-CONTRACT.md</files>
  <action>
    Produce APPLY evidence for UNIFY:
    - Run marker grep over the planned files for artifact-slice names, schema markers, fallback triggers, command-output truth, module evidence, GitHub Flow evidence, no hidden state, and Pi-owned ledger boundaries.
    - Run both validation suites and capture TAP totals.
    - Run `git diff --stat` and review changed files to confirm only the approved validation/doc surfaces changed outside normal `.paul/*` lifecycle artifacts.
  </action>
  <verify>grep -n "artifact-slice\|current-lifecycle-state\|active-roadmap-phase\|approved-plan-task-packet\|Source:\|Freshness:\|Bounds:\|Fallback:\|Derived aid only\|command output\|Module Execution Reports\|GitHub Flow\|hidden Pi state\|Pi-owned" tests/pi-end-to-end-validation.sh tests/cross-harness-validation.sh docs/PI-NATIVE-ARTIFACT-SLICE-CONTRACT.md && git diff --stat</verify>
  <done>AC-4 satisfied: final evidence is command-backed and changed files stay inside approved validation/evidence reconciliation scope.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- `drivers/pi/extensions/pals-hooks.ts` runtime behavior unless a new explicit plan approves runtime changes.
- `drivers/pi/install.sh`, installed runtime copies outside the repo, dependency manifests/lockfiles, and CI configuration.
- Kernel workflow semantics, module source behavior, GitHub Flow automation, lifecycle approval/checkpoint semantics, or `.paul/*` authority outside normal lifecycle artifact updates.
- Claude Code, Agent SDK, portability/frozen legacy parity surfaces.

## SCOPE LIMITS
- This plan reconciles validation scripts and evidence documentation only.
- Do not add workflow/resource capsules, guided workflow evidence hardening, delegated APPLY packet/report compression, validation classification redesign, legacy cleanup, telemetry, hidden state, or helper-owned APPLY behavior.
- Keep shell-suite edits focused; do not restructure the large validation scripts.
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `grep -n "artifact-slice\|current-lifecycle-state\|active-roadmap-phase\|approved-plan-task-packet\|Source:\|Freshness:\|Bounds:\|Fallback:\|Derived aid only\|command output\|Module Execution Reports\|GitHub Flow\|hidden Pi state\|Pi-owned" tests/pi-end-to-end-validation.sh tests/cross-harness-validation.sh docs/PI-NATIVE-ARTIFACT-SLICE-CONTRACT.md`
- [ ] `bash tests/pi-end-to-end-validation.sh`
- [ ] `bash tests/cross-harness-validation.sh`
- [ ] `git diff --stat` confirms only approved repo-source validation/doc surfaces changed outside `.paul/*` lifecycle artifacts.
- [ ] All acceptance criteria met.
</verification>

<success_criteria>
- Pi validation passes with TAP summary and zero failures.
- Cross-harness validation passes with TAP summary and zero failures.
- Artifact-slice marker/evidence checks cover runtime-lens schema markers, fallback triggers, command-output truth, module/GitHub Flow proof, and non-authority boundaries.
- Evidence contract documents current baselines and count-change reconciliation requirements.
- No runtime, dependency, CI, legacy, workflow-semantics, or lifecycle-authority changes are introduced.
</success_criteria>

<output>
After completion, create `.paul/phases/205-validation-evidence-reconciliation/205-01-SUMMARY.md` with acceptance criteria results, command-output evidence, changed-file scope review, module reports, GitHub Flow evidence, decisions/deviations, and next milestone handoff recommendations.
</output>
