---
phase: 202-slice-inventory-contract-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/PI-NATIVE-ARTIFACT-SLICE-CONTRACT.md
autonomous: true
---

<objective>
## Goal
Create `docs/PI-NATIVE-ARTIFACT-SLICE-CONTRACT.md`, the Phase 202 baseline contract for v2.47 Artifact-Slice Contract Hardening. The artifact must define the named artifact-slice inventory, stable output schema, source/freshness/bounds/fallback rules, validation classes, protected files, and non-goals that must be settled before runtime slice hardening begins.

## Purpose
v2.47 is the first Pi-native implementation build milestone after v2.46. Phase 202 keeps the first step intentionally docs-only so the runtime hardening phases can implement against a reviewed contract instead of rediscovering slice names, schema fields, validation expectations, and authority boundaries during code changes.

## Output
- `docs/PI-NATIVE-ARTIFACT-SLICE-CONTRACT.md` created as the Phase 202 contract baseline.
- The artifact carries source inputs from v2.45/v2.46, named slice inventory, schema and formatting rules, fallback triggers, validation classes, protected boundaries, and a handoff for Phase 203 runtime hardening.
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md

## Prior Work
.paul/phases/195-exploratory-pi-native-spikes/195-01-SUMMARY.md
.paul/phases/201-final-implementation-plan-proposal/201-01-SUMMARY.md

docs/PI-NATIVE-CONTEXT-EFFICIENCY-ARCHITECTURE.md
docs/PI-NATIVE-ARCHITECTURE-PROPOSAL.md
docs/PI-NATIVE-IMPLEMENTATION-WORKSTREAMS.md
docs/PI-NATIVE-SEQUENCING-DEPENDENCY-PLAN.md
docs/PI-NATIVE-VALIDATION-EVIDENCE-STRATEGY.md
docs/PI-NATIVE-IMPLEMENTATION-PLAN.md

## Source Files
docs/PI-NATIVE-ARTIFACT-SLICE-CONTRACT.md
</context>

<module_dispatch>
## Pre-Plan Module Dispatch

Planning posture: `high` collaboration from `pals.json` project default; direct-requirements mode because v2.46 explicitly routed the next milestone and Phase 202 scope is explicit in `.paul/ROADMAP.md`.

[dispatch] pre-plan advisory: modules.yaml loaded from installed registry; docs-only Phase 202 scope reviewed for planned file `docs/PI-NATIVE-ARTIFACT-SLICE-CONTRACT.md`.

| Module / Cohort | Result |
|-----------------|--------|
| TODD | Repo-scoped test discovery excluding installed deps found no first-party `*.test.*`, `*.spec.*`, or `__tests__` entries; plan type remains `execute`, not TDD. Existing validation remains shell-command based. |
| IRIS | Planned output is a new markdown contract artifact; no existing source anti-pattern scan applies before file creation. |
| DAVE | No CI config discovered in repo scope; this docs-only plan does not add CI/CD files. Verification remains marker checks plus existing Pi/cross-harness validation. |
| DOCS | Documentation catalog contains related Pi-native architecture, workstream, sequencing, validation, and implementation-plan artifacts; planned output belongs under `docs/`. |
| CODI | Skipped — no extractable TS/JS symbols or bounded source selectors in this markdown-only contract-baseline phase scope. |
| RUBY | Markdown-only planning artifact; no implementation functions/classes or complexity-bearing source files planned. |
| ARCH | Repository pattern is documentation/kernel/driver/module oriented; planned output is a docs artifact in the expected docs layer. Existing large-file debt in runtime/validation files is not touched. |
| ARIA / LUKE / GABE / DANA / OMAR / PETE / REED / VERA | No UI, API, data, observability, performance, resilience, or privacy implementation files are planned. |

[dispatch] pre-plan enforcement: DEAN PASS — no root dependency manifest or lockfile detected; no dependency audit applies and no critical/high vulnerability block is raised.

## Post-Plan Module Dispatch

[dispatch] post-plan: TODD reviewed plan type; no TDD conversion suggested for docs-only contract-baseline work.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | All PLAN/APPLY/UNIFY lifecycle work | ✓ |
| /carl | required | Automatic session-boundary monitoring at phase boundaries and safety thresholds | automatic |
| TODD | required | APPLY validation/test enforcement | ✓ via module dispatch |
| WALT | required | APPLY/UNIFY quality gating and validation history | ✓ via module dispatch |

**BLOCKING:** Required skills must remain available before APPLY proceeds.
</skills>

<acceptance_criteria>

## AC-1: Source-grounded contract baseline
```gherkin
Given v2.45/v2.46 promoted artifact slices as the first Pi-native implementation pattern
When the Phase 202 contract artifact is created
Then it cites the Phase 195 spike, v2.45 architecture, v2.46 workstream/sequencing/validation/implementation artifacts, and the Phase 201 summary as source inputs
```

## AC-2: Named inventory and schema are explicit
```gherkin
Given Phase 203 will harden runtime artifact-slice behavior
When the contract defines the artifact-slice baseline
Then it names the initial slice inventory and defines required schema fields for source path, slice label, freshness marker, bounds/size limit, and fallback language
```

## AC-3: Authority boundaries and fallback triggers are preserved
```gherkin
Given `.paul/*` artifacts, PLAN/APPLY/UNIFY, module evidence, GitHub Flow gates, and parent-owned APPLY remain authoritative
When the contract defines slice usage rules
Then it states slices are derived, bounded, disposable, read-only, source-cited aids and requires full authoritative reads for edits, lifecycle writes, stale/ambiguous/contested facts, approved PLAN execution, GitHub Flow gates, and decisions
```

## AC-4: Validation and evidence requirements are executable
```gherkin
Given Phase 200 made command output the validation truth
When the contract defines Phase 203 handoff requirements
Then it declares validation classes, marker-check families, expected Pi/cross-harness command evidence, durable SUMMARY evidence, and failure/block conditions for missing citations, missing freshness, hidden persistence, `.paul/*` writes, or full-read replacement
```

## AC-5: Scope remains docs-only and validation stays green
```gherkin
Given Phase 202 is a contract-baseline phase
When APPLY completes
Then only the new docs contract plus lifecycle artifacts are changed, required marker checks pass, and `bash tests/pi-end-to-end-validation.sh` plus `bash tests/cross-harness-validation.sh` remain green
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Create source-grounded contract frame</name>
  <files>docs/PI-NATIVE-ARTIFACT-SLICE-CONTRACT.md</files>
  <action>
    Create the contract artifact with:
    - status table for v2.47 / Phase 202 and an explicit docs-only contract-baseline boundary;
    - purpose explaining that the artifact authorizes contract definition, not runtime hardening by itself;
    - source inputs table citing Phase 195, Phase 194/196 architecture docs, Phase 198/199/200/201 implementation-planning docs, and `.paul/phases/201-final-implementation-plan-proposal/201-01-SUMMARY.md`;
    - concise settled decisions and non-negotiable authority boundaries.
    Avoid copying large prior sections verbatim; cite source files and preserve only the decision/evidence needed for the contract baseline.
  </action>
  <verify>grep -n "Contract-Baseline Boundary\|Source Inputs\|Settled Decisions\|Non-Negotiable Authority Boundaries" docs/PI-NATIVE-ARTIFACT-SLICE-CONTRACT.md</verify>
  <done>AC-1 and AC-3 satisfied: the artifact is source-grounded, docs-only, and authority-preserving.</done>
</task>

<task type="auto">
  <name>Task 2: Define slice inventory, schema, and fallback rules</name>
  <files>docs/PI-NATIVE-ARTIFACT-SLICE-CONTRACT.md</files>
  <action>
    Add the executable contract sections with:
    - initial named slice inventory for current lifecycle state, active roadmap phase/current milestone, current PLAN task packet when present, recent SUMMARY decisions, and module evidence / validation baseline;
    - stable schema fields for `Source`, `Slice`, `Freshness`, `Bounds`, `Fallback`, and `Authority` / non-authority language;
    - size/bounds expectations and freshness rules for facts that can change during a session;
    - full-read fallback triggers for edits, lifecycle writes, approved PLAN execution, stale/ambiguous/contested facts, GitHub Flow gates, and decisions;
    - explicit examples of acceptable and unacceptable slice wording.
    Keep the contract implementation-ready but avoid designing code APIs beyond the schema and validation expectations needed for Phase 203.
  </action>
  <verify>grep -n "Named Slice Inventory\|Slice Output Schema\|Freshness and Bounds\|Full-Read Fallback Triggers\|Acceptable Slice Wording" docs/PI-NATIVE-ARTIFACT-SLICE-CONTRACT.md</verify>
  <done>AC-2 and AC-3 satisfied: inventory, schema, freshness/bounds rules, and fallback triggers are explicit.</done>
</task>

<task type="auto">
  <name>Task 3: Define validation, evidence, and Phase 203 handoff</name>
  <files>docs/PI-NATIVE-ARTIFACT-SLICE-CONTRACT.md</files>
  <action>
    Add validation and handoff sections with:
    - primary validation classes: Runtime Lens, Pi-Supported Runtime, Shared Invariant, and Artifact / Process;
    - marker-check families for activation gating, `Source:` citations, freshness markers, bounds, fallback language, read-only/no `.paul/*` writes, no hidden persistence, and no lifecycle-authority replacement;
    - command-output evidence requirements for marker greps, `bash tests/pi-end-to-end-validation.sh`, `bash tests/cross-harness-validation.sh`, changed-file diff review, module reports, and GitHub Flow state during UNIFY;
    - block/failure conditions that Phase 203 must not violate;
    - Phase 203 handoff: likely repo surfaces, protected files, implementation entry conditions, and explicit deferrals.
    Preserve the rule that validation command output is truth; Pi summaries, reports, slices, capsules, and helper reports may explain but not replace command evidence.
  </action>
  <verify>grep -n "Validation Classes\|Marker-Check Families\|Command-Output Evidence\|Failure Conditions\|Phase 203 Handoff" docs/PI-NATIVE-ARTIFACT-SLICE-CONTRACT.md && bash tests/pi-end-to-end-validation.sh && bash tests/cross-harness-validation.sh</verify>
  <done>AC-4 and AC-5 satisfied: validation/evidence gates are executable and baseline validation remains green.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Runtime behavior in `drivers/pi/extensions/pals-hooks.ts`, `drivers/pi/install.sh`, `drivers/pi/extensions/README.md`, `drivers/pi/skill-map.md`, or installed runtime copies.
- Validation scripts, validation reports, dependency manifests, CI configuration, GitHub Flow behavior, Claude Code wrappers, Agent SDK surfaces, portability docs, or legacy install surfaces.
- Existing Phase 194-201 source artifacts except for citing them in the new contract artifact.
- `.paul/*` lifecycle authority, PLAN/APPLY/UNIFY semantics, module evidence requirements, GitHub Flow gates, parent-owned APPLY boundaries, approval/checkpoint requirements, archive authority, or full-read fallback rules.

## SCOPE LIMITS
- Docs-only contract-baseline work: create `docs/PI-NATIVE-ARTIFACT-SLICE-CONTRACT.md` only.
- Do not implement or modify artifact-slice runtime behavior in this plan.
- Do not add new slice runtime outputs, workflow/resource capsules, guided workflow behavior, helper packet/report compression, validation classification changes, or legacy cleanup.
- Do not add dependencies, CI configuration, generated install outputs, or historical report rewrites.
- Do not treat this PLAN or the contract artifact as APPLY approval for Phase 203; future runtime hardening still requires normal PLAN approval, APPLY execution, validation, GitHub Flow gates, and UNIFY reconciliation.

</boundaries>

<verification>
Before declaring APPLY complete:
- [ ] `grep -n "Contract-Baseline Boundary\|Source Inputs\|Named Slice Inventory\|Slice Output Schema\|Full-Read Fallback Triggers\|Validation Classes\|Marker-Check Families\|Command-Output Evidence\|Failure Conditions\|Phase 203 Handoff" docs/PI-NATIVE-ARTIFACT-SLICE-CONTRACT.md`
- [ ] `bash tests/pi-end-to-end-validation.sh`
- [ ] `bash tests/cross-harness-validation.sh`
- [ ] `git diff --stat` confirms docs-only product scope plus lifecycle artifacts.
- [ ] All acceptance criteria are met.
</verification>

<success_criteria>
- `docs/PI-NATIVE-ARTIFACT-SLICE-CONTRACT.md` exists and is source-cited to Phase 195 and v2.45/v2.46 planning artifacts.
- Named slice inventory, schema fields, freshness/bounds rules, full-read fallback triggers, and authority boundaries are explicit.
- Validation classes, marker-check families, command-output evidence, failure conditions, and Phase 203 handoff requirements are present.
- Validation command output remains green and is captured during APPLY/UNIFY.
- No runtime, workflow, validation-script, dependency, GitHub Flow, installed runtime, legacy-surface, or lifecycle-authority behavior changes are introduced.
</success_criteria>

<output>
After completion, create `.paul/phases/202-slice-inventory-contract-baseline/202-01-SUMMARY.md` with acceptance-criteria results, verification output, module reports, changed files, decisions, deviations, and Phase 203 runtime-hardening handoff.
</output>
