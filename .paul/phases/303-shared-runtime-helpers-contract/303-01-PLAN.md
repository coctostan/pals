---
phase: 303-shared-runtime-helpers-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["docs/PALS-SHARED-RUNTIME-HELPERS-CONTRACT.md"]
autonomous: true
---

<objective>
## Goal
Author `docs/PALS-SHARED-RUNTIME-HELPERS-CONTRACT.md` — the authoritative, contract-first boundary for v2.72 that inventories the exact pure/derived helper symbols to extract from `drivers/pi/extensions/pals-hooks.ts`, the precise importer map, the allowed helper responsibilities, the authority boundaries (what the new sibling must NOT do), and leaf/cycle-avoidance rules. No code changes in this phase.

## Purpose
Serves the Core Value (minimal kernel, modular runtime surfaces) by making the v2.69 Phase 295 ranked extraction safe and bounded before any code moves. A fixed contract lets Phase 304 extract + repoint deterministically and Phase 305 validate against a written spec — mirroring the v2.69 S4 and v2.70 strategic-assessment contract-first pattern (cites `.paul/STATE.md` Current focus; `.paul/assessments/2026-06-04-post-v2-71-next-milestone.md` Actionable Points).

## Output
- New doc: `docs/PALS-SHARED-RUNTIME-HELPERS-CONTRACT.md` (authoritative spec; no source/code changes).
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md

## Prior Work (only if genuinely needed)
- `.paul/assessments/2026-06-04-post-v2-71-next-milestone.md` — decision basis (Ranked Recommendation + Actionable Points define helper scope and guardrails).
- Sibling-extraction precedent: `docs/` S4 contract pattern (v2.69) and `docs/PALS-STRATEGIC-ASSESSMENT-CONTRACT.md` (v2.70) as document-shape conventions.

## Source Files
drivers/pi/extensions/pals-hooks.ts
</context>

<module_dispatch>
## Pre-Plan Module Dispatch

[dispatch] pre-plan advisory: modules.yaml NOT FOUND — WARNING (this is the PALS kernel source repo; no installed dispatch registry at repo root. Module source lives under `modules/*/module.yaml` but is not an installed runtime registry. Advisory modules TODD/IRIS/DAVE/DOCS/CODI/RUBY could not be dispatched.)
[dispatch] pre-plan enforcement: modules.yaml NOT FOUND — WARNING (DEAN dependency audit not dispatched; no installed registry. This phase modifies one Markdown doc and adds no dependencies, so dependency-audit risk is nil.)

CODI seeds: none — docs-only phase, no TS/JS selectors selected for plan context.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | All PLAN/APPLY/UNIFY lifecycle work (active) | ✓ |
| /carl | required | Automatic session-boundary monitoring | ✓ |
| TODD | required | Before/during code implementation — N/A this phase (docs-only, no code/tests) | ○ (not triggered) |
| WALT | required | After implementation — N/A this phase (no code to quality-gate; full validation in Phase 305) | ○ (not triggered) |

**Note:** TODD/WALT are APPLY-phase code/test/quality gates. Phase 303 produces only a Markdown contract document, so neither is triggered. They apply to Phase 304 (extraction) and Phase 305 (validation).

## Skill Invocation Checklist
- [x] /paul loaded (active lifecycle)
- [x] /carl loaded (passive Pi extension)
</skills>

<acceptance_criteria>

## AC-1: Authoritative contract document exists with required sections
```gherkin
Given v2.72 Phase 303 is the contract-first phase
When docs/PALS-SHARED-RUNTIME-HELPERS-CONTRACT.md is authored
Then it contains Purpose, Source Authority, Doctrine, Helper Inventory, Importer Map, Allowed Responsibilities, Authority Boundaries, Leaf/Cycle-Avoidance Rules, Out of Scope, and Status sections
And it names the exact helper symbols to extract: compactWhitespace, selectBoundedLines, escapeRegExp, extractLoopSignature, readFileOr, getFileFreshness, parsePalsState, and the PalsStateSnapshot type
```

## AC-2: Helper inventory and importer map are factually accurate against source
```gherkin
Given the contract inventories helpers and their importers
When each named helper symbol and import site is checked against drivers/pi/extensions/*.ts
Then every helper named for extraction is currently exported from pals-hooks.ts
And the importer map matches the actual `from "./pals-hooks"` import sites (9 files including pals-hooks itself; the 8 sibling importers)
And no fabricated symbol or importer appears in the contract
```

## AC-3: Authority boundaries and cycle rules are explicit and bounded
```gherkin
Given the new sibling must stay pure/derived and lifecycle-safe
When the Authority Boundaries and Leaf/Cycle-Avoidance sections are read
Then they explicitly forbid .paul/* writes, lifecycle-state ownership, module dispatch, git/validation, approvals, command routing, UI decisions, and canonical user-message delivery
And they require the new module to be a dependency leaf importing only Node fs/path (no imports from pals-hooks or other siblings)
And they explicitly exclude the ctx/event-touching helpers (collectRecentAssistantTexts, extractTextContent) and CARL/activation/routing logic from extraction
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Author the shared-runtime-helpers contract document</name>
  <files>docs/PALS-SHARED-RUNTIME-HELPERS-CONTRACT.md</files>
  <action>
    Create docs/PALS-SHARED-RUNTIME-HELPERS-CONTRACT.md as the authoritative v2.72 implementation boundary. Follow the document shape of docs/PALS-STRATEGIC-ASSESSMENT-CONTRACT.md (Purpose / Source Authority / Doctrine / ... / Status). Single Write per artifact (compute content offline, one Write).

    Required sections:
    - **Purpose:** define v2.72's contract-first boundary for extracting pure/derived filesystem + state helpers from pals-hooks.ts into a leaf shared-runtime-helpers sibling.
    - **Source Authority:** this contract is authoritative for v2.72 planning/APPLY; Phase 304 extracts against it, Phase 305 validates; contract wins on conflict unless the user revises it.
    - **Doctrine:** "Pure leaf helpers, authoritative runtime." Move only pure/derived logic; orchestration and authority stay in pals-hooks.ts.
    - **Helper Inventory:** table of the exact symbols to extract with one-line responsibility and current pals-hooks.ts line ranges — compactWhitespace (155-157), selectBoundedLines (189-198), escapeRegExp (200-202), extractLoopSignature (302-306), readFileOr (147-153), getFileFreshness (181-187), parsePalsState (159-179), and the PalsStateSnapshot type (81-87). Group as: pure string/logic, filesystem, derived state + type.
    - **Importer Map:** list the importers and which helpers each consumes. Importers (8 siblings): guided-workflow-detection (compactWhitespace, extractLoopSignature, PalsStateSnapshot type), artifact-slice-rendering (selectBoundedLines, escapeRegExp, PalsStateSnapshot type), lifecycle-ui (compactWhitespace, parsePalsState, PalsStateSnapshot type, + collectRecentAssistantTexts which stays in pals-hooks), workflow-resource-capsule-rendering (getFileFreshness, selectBoundedLines), command-routing (compactWhitespace, ActivationState/PalsStateSnapshot types), module-activity-parsing (compactWhitespace, MAX_WIDGET_MODULE_DETAILS which stays in pals-hooks), pals-context-injection (PalsStateSnapshot/ActivationState types), guided-workflow-delivery (compactWhitespace, readFileOr). Plus pals-hooks.ts itself as a consumer after extraction. Note which currently-co-imported symbols (collectRecentAssistantTexts, MAX_WIDGET_MODULE_DETAILS, ActivationState) are NOT moving and must keep their existing home.
    - **Allowed Responsibilities:** pure string normalization, regex escaping, bounded line selection, loop-signature derivation, safe file reads, file freshness (mtime), and STATE parsing into a derived snapshot. Imports limited to Node fs/path.
    - **Authority Boundaries:** the sibling must NOT perform .paul/* writes, lifecycle-state ownership, module dispatch, git/validation, approval/continue behavior, command routing, UI decisions, or canonical user-message delivery; no new authority markers.
    - **Leaf / Cycle-Avoidance Rules:** the module is a dependency leaf — it must not import from pals-hooks or any other sibling; back-references use type-only imports erased at runtime where unavoidable; document the acyclic-graph requirement.
    - **Out of Scope for v2.72:** ctx/event-touching helpers (collectRecentAssistantTexts, extractTextContent), CARL session-boundary logic (loadCarlConfig, carlState, buildCarlBootstrapPrompt, formatCarlContextPressure), activation state (markActivation/getActiveActivation/consumeActivationTurn), command routing, and validation-suite decomposition (a separately-ranked future milestone).
    - **Status:** Phase 303 contract authored; extraction pending Phase 304; validation pending Phase 305.

    Escape nothing special (Markdown). Cite the strategic assessment and pals-hooks.ts as provenance. Do NOT edit any .ts source in this phase.
  </action>
  <verify>Read docs/PALS-SHARED-RUNTIME-HELPERS-CONTRACT.md and confirm all required section headings and the 8 named helper symbols + PalsStateSnapshot are present; grep for "## Purpose", "## Helper Inventory", "## Importer Map", "## Authority Boundaries", "## Leaf", "## Out of Scope", "## Status".</verify>
  <done>AC-1 satisfied: authoritative contract exists with all required sections and the exact helper set named.</done>
</task>

<task type="auto">
  <name>Task 2: Verify contract facts against actual source (self-consistency)</name>
  <files>docs/PALS-SHARED-RUNTIME-HELPERS-CONTRACT.md</files>
  <action>
    Cross-check every factual claim in the contract against the real source, and correct the contract (not the source) on any mismatch:
    - For each helper named for extraction, confirm it is `export`ed from drivers/pi/extensions/pals-hooks.ts (grep `export function {name}` / `export type PalsStateSnapshot`).
    - Confirm the importer map matches actual `from "./pals-hooks"` import sites across drivers/pi/extensions/*.ts (expect 8 sibling files + the structural reality that pals-hooks defines them). Reconcile any helper-to-importer pairing that does not match the real imports.
    - Confirm the "not moving" symbols (collectRecentAssistantTexts, extractTextContent, MAX_WIDGET_MODULE_DETAILS, ActivationState, CARL/activation symbols) are correctly excluded and their current home is accurately stated.
    - Do NOT modify any .ts file. Only adjust the contract document to be exactly accurate.
  </action>
  <verify>grep `export (function|type|const)` for each named helper in drivers/pi/extensions/pals-hooks.ts returns a match; grep `from "./pals-hooks"` across drivers/pi/extensions/*.ts matches the contract's importer list; no .ts files appear in `git diff --name-only` except none (docs-only).</verify>
  <done>AC-2 and AC-3 satisfied: inventory/importer map are factually accurate, and authority/cycle/exclusion rules are explicit and bounded.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Any `drivers/pi/extensions/*.ts` source file (extraction is Phase 304, not this phase).
- Any `.paul/*` lifecycle artifact except the standard PLAN/STATE lifecycle writes performed by the workflow.
- `tests/*` validation suites (validation is Phase 305).

## SCOPE LIMITS
- This phase produces exactly one new file: `docs/PALS-SHARED-RUNTIME-HELPERS-CONTRACT.md`.
- No code is moved, created, or repointed; no imports change.
- Helper-set membership is fixed by this contract; broadening it (e.g., pulling ctx/event helpers or CARL logic) is out of scope.
- No validation-suite decomposition (separate future milestone).
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `docs/PALS-SHARED-RUNTIME-HELPERS-CONTRACT.md` exists with all required sections (Purpose, Source Authority, Doctrine, Helper Inventory, Importer Map, Allowed Responsibilities, Authority Boundaries, Leaf/Cycle-Avoidance, Out of Scope, Status).
- [ ] All 8 named helpers + `PalsStateSnapshot` are confirmed `export`ed from `pals-hooks.ts`.
- [ ] Importer map matches actual `from "./pals-hooks"` import sites.
- [ ] `git diff --name-only` shows only the new contract doc (no `.ts` changes).
- [ ] All acceptance criteria met.
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No source/code changes (docs-only phase)
- Contract is factually accurate and bounded; ready for Phase 304 extraction
</success_criteria>

<output>
After completion, create `.paul/phases/303-shared-runtime-helpers-contract/303-01-SUMMARY.md`
</output>
