---
phase: 15-adversarial-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/admin-api.ts
  - src/dashboard.ts
  - src/db-helpers.ts
  - package.json
  - .prettierignore
autonomous: true
---

<objective>
## Goal
Remove the Phase 14 adversarial bait artifacts and restore the repository to a clean product baseline without regressing the existing verify feature set.

## Purpose
This removes intentionally bad code that was only added to trigger PALS modules, so `pi-verify` returns to being a clean verification extension and Phase 16 can build a real user-facing feature on top of a stable baseline.

## Output
- `src/admin-api.ts`, `src/dashboard.ts`, and `src/db-helpers.ts` removed
- `package.json` keywords/version metadata aligned with the clean baseline for v1.2 work
- `.prettierignore` cleaned up if the `.paul/` exclusion was only temporary bait-phase remediation
- Verification evidence showing tests, lint, typecheck, and format all pass after cleanup
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md
.paul/PRD.md
.paul/phases/14-module-bait-implementation/14-01-SUMMARY.md

## Source Files
src/admin-api.ts
src/dashboard.ts
src/db-helpers.ts
src/index.ts
src/commands.ts
src/types.ts
package.json
.prettierignore
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: TODD(p100) → test coverage exists for command parsing and verify command surfaces; no TDD-only plan required for cleanup | IRIS(p150) → review_flags injected from known bait files with intentional anti-patterns | DAVE(p200) → GitHub Actions present, no CI config changes in scope | DOCS(p200) → README/CHANGELOG proximity noted but source cleanup plan does not require doc changes unless behavior changes | RUBY(p250) → debt flags limited to temporary bait files slated for removal

[dispatch] pre-plan enforcement: DEAN(p50) → BLOCK(existing baseline vulnerabilities: critical/high present in dependency tree). User override accepted for planning because the vulnerabilities are pre-existing and Phase 15 scope is cleanup, not dependency expansion.

Additional pre-plan context:
- ARCH: flat `src/` structure remains valid; planned removals do not create boundary risk.
- ARIA/LUKE/GABE/OMAR/PETE/REED/DANA/SETH/VERA findings are all concentrated in the three bait files and are expected to disappear when those files are removed.
- No SPECIAL-FLOWS.md present.
</module_dispatch>

<acceptance_criteria>
## AC-1: Adversarial bait files are removed
```gherkin
Given the repository still contains Phase 14 adversarial bait source files
When Phase 15 cleanup is applied
Then `src/admin-api.ts`, `src/dashboard.ts`, and `src/db-helpers.ts` no longer exist in the repo
```

## AC-2: Repository metadata is restored to a clean baseline
```gherkin
Given Phase 14 changed repo metadata to support module-bait validation
When cleanup is completed
Then `package.json` and `.prettierignore` contain only the entries needed for normal product operation, with no bait-only adjustments left behind
```

## AC-3: Existing product behavior remains healthy after cleanup
```gherkin
Given the bait files were not part of the shipped verify feature set
When verification is run after cleanup
Then tests, lint, typecheck, and format checks pass without the removed files and without new regressions
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Remove the three Phase 14 bait source files</name>
  <files>src/admin-api.ts, src/dashboard.ts, src/db-helpers.ts</files>
  <action>
    Delete the three files that were created solely for adversarial module triggering in v1.1.
    Confirm they are not imported by the shipped extension code before removal.
    Avoid: leaving dead references, stale exports, or comments in source that still describe the removed bait modules as active code.
  </action>
  <verify>rg -n "admin-api|dashboard|db-helpers" src test returns no live references to the removed files, and ls src no longer lists those three files</verify>
  <done>AC-1 satisfied: all three bait files are removed with no remaining code references</done>
</task>

<task type="auto">
  <name>Task 2: Revert bait-specific repository metadata</name>
  <files>package.json, .prettierignore</files>
  <action>
    Revert the package metadata changes that were made only to support the adversarial milestone.
    Remove the Phase 14 keyword additions from `package.json` if they were only used to touch metadata for DEAN comparison.
    Reassess `.prettierignore`: remove the `.paul/` exclusion if it was introduced only as a temporary workaround for the bait milestone and normal formatting can stay clean without it.
    Avoid: changing package scripts, dependencies, exports, or other product metadata that are unrelated to the cleanup scope.
  </action>
  <verify>git diff -- package.json .prettierignore shows only bait-cleanup edits, and prettier --check . / pnpm run check still succeed with the final metadata state</verify>
  <done>AC-2 satisfied: repo metadata contains no bait-only adjustments</done>
</task>

<task type="auto">
  <name>Task 3: Re-establish a clean verification baseline</name>
  <files>src/admin-api.ts, src/dashboard.ts, src/db-helpers.ts, package.json, .prettierignore</files>
  <action>
    Run the project verification suite after cleanup and fix only cleanup-caused regressions.
    Use the existing project commands (`pnpm test`, `pnpm run check`) as the final proof that removal did not break shipped behavior.
    Avoid: expanding scope into the new `/verify diff` feature or unrelated refactors; this task is strictly baseline restoration.
  </action>
  <verify>pnpm test && pnpm run check</verify>
  <done>AC-3 satisfied: the cleaned repo passes the full verification baseline</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- src/index.ts, src/verify.ts, src/commands.ts, src/types.ts unless a direct compile/test failure proves cleanup requires a minimal reference fix
- test/* except for strictly necessary cleanup-related updates caused by removed references
- .paul/* planning/history artifacts other than the standard lifecycle files updated by PAUL
- dependency versions, lockfile contents, and CI config in this plan

## SCOPE LIMITS
- This plan only removes adversarial bait and restores baseline metadata
- Do not start implementing `/verify diff` in this plan
- Do not remediate the pre-existing dependency audit findings in this plan
- Do not rewrite docs unless cleanup changes visible user behavior
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `rg -n "admin-api|dashboard|db-helpers" src test` shows no live references to removed bait modules
- [ ] `pnpm test` passes
- [ ] `pnpm run check` passes
- [ ] `git diff -- package.json .prettierignore` reflects only intended cleanup edits
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All three bait source files are removed
- package metadata and ignore rules no longer contain bait-only changes
- Full verification baseline passes after cleanup
- No unrelated feature work or dependency churn introduced
</success_criteria>

<output>
After completion, create `.paul/phases/15-adversarial-cleanup/15-01-SUMMARY.md`
</output>
