---
phase: 130-real-user-feature-dev
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [".paul/phases/130-real-user-feature-dev/dev-results.md"]
autonomous: false
---

<objective>
## Goal
Drive pi-verify through 2 real PAUL loops via cmux using openai-codex/gpt5.4 (low thinking): one cleanup loop (remove v1.1 bait), one feature loop (add /verify diff). Monitor module dispatch and output quality as in Phase 129, but focus on whether PALS helps during genuine development.

## Purpose
Phase 129 proved modules dispatch (sonnet) or don't (kimi) with adversarial bait. Phase 130 tests whether modules add value during normal development — the kind of work a real user does.

## Output
- `dev-results.md` — assessment of GPT-5.4's PALS compliance, module dispatch, and whether modules helped during real dev
- Raw .paul/ artifacts from pi-verify preserved as evidence
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md

## Target Project
pi-verify at /Users/maxwellnewman/claude/pi-verify
- Currently has v1.1 bait files (admin-api.ts, dashboard.ts, db-helpers.ts) that need cleanup
- MILESTONE-CONTEXT.md placed for v1.2 Cleanup & Diff Check (2 phases: 15 cleanup, 16 feature)
- pals.json has github-flow enabled

## Prior Work
.paul/phases/129-adversarial-testing-setup/adversarial-results.md (Phase 129 results)
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: skip — no pals source files modified in this repo
[dispatch] pre-plan enforcement: skip — no dependency changes
</module_dispatch>

<acceptance_criteria>

## AC-1: GPT-5.4 Cleanup Loop Completes
```gherkin
Given pi-verify with bait files from v1.1 in a cmux pane using openai-codex/gpt5.4
When a PLAN→APPLY→UNIFY loop removes the bait files
Then pi-verify is clean (67 tests passing, no bait files) and loop artifacts are preserved
```

## AC-2: GPT-5.4 Feature Loop Completes
```gherkin
Given clean pi-verify after cleanup loop
When a PLAN→APPLY→UNIFY loop adds /verify diff
Then the feature works (tests added and passing) and loop artifacts are preserved
```

## AC-3: Development Assessment Documented
```gherkin
Given completed loops from GPT-5.4
When results are analyzed for workflow compliance, dispatch, and real development value
Then dev-results.md contains structured assessment with evidence
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Run cleanup PAUL loop with GPT-5.4 on pi-verify</name>
  <files>.paul/phases/130-real-user-feature-dev/dev-results.md</files>
  <action>
    1. Open a cmux workspace, cd to /Users/maxwellnewman/claude/pi-verify, start pi with openai-codex/gpt5.4
    2. Drive /skill:paul-milestone to complete v1.1 and create v1.2 (MILESTONE-CONTEXT.md is pre-placed)
    3. Drive /skill:paul-plan for Phase 15 (cleanup)
    4. Drive /skill:paul-apply — model should remove bait files, revert package.json, verify 67 tests pass
    5. Drive /skill:paul-unify
    6. Monitor: does the model follow PAUL? Does it dispatch modules? Do modules produce useful output during a cleanup task?
    7. Leave all artifacts in place, record observations
  </action>
  <verify>
    Post-hoc: grep .paul/ artifacts for [dispatch] markers, check bait files removed, verify tests pass
  </verify>
  <done>AC-1 satisfied: Cleanup loop completed with evidence</done>
</task>

<task type="auto">
  <name>Task 2: Run feature PAUL loop with GPT-5.4 on pi-verify</name>
  <files>.paul/phases/130-real-user-feature-dev/dev-results.md</files>
  <action>
    1. In the same cmux workspace (or new one if session ended)
    2. Drive /skill:paul-plan for Phase 16 (/verify diff feature)
    3. Drive /skill:paul-apply — model should add diff detection, new subcommand, tests
    4. Drive /skill:paul-unify
    5. Monitor same criteria as Task 1
    6. Key question: did modules HELP the developer? Did TODD catch missing tests? Did WALT flag regressions? Did IRIS/RUBY flag code quality issues?
    7. Leave all artifacts in place, record observations
  </action>
  <verify>
    Post-hoc: verify /verify diff feature exists in source, tests added, dispatch evidence present
  </verify>
  <done>AC-2 satisfied: Feature loop completed with evidence</done>
</task>

<task type="auto">
  <name>Task 3: Analyze results and produce development assessment</name>
  <files>.paul/phases/130-real-user-feature-dev/dev-results.md</files>
  <action>
    Analyze pi-verify's .paul/ artifacts from both loops:

    **Workflow compliance:** Same L1 criteria as Phase 129
    **Module dispatch:** Same L2 criteria — did hooks fire?
    **Development value:** NEW criteria for Phase 130:
    - Did modules catch real issues (not just bait)?
    - Did TODD enforce test coverage for the new feature?
    - Did WALT catch any regressions?
    - Did the developer experience feel helped or hindered by modules?
    - Compare GPT-5.4 module behavior against sonnet-4.6 from Phase 129

    Produce comparison and development value assessment.
  </action>
  <verify>
    dev-results.md exists with workflow, dispatch, and development value sections
  </verify>
  <done>AC-3 satisfied: Development assessment documented</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- pals kernel source files
- pi-verify source from this repo's perspective

## SCOPE LIMITS
- 2 loops only (cleanup + feature)
- Do not fix PALS issues found — document for later
- Focus on development value, not just dispatch presence
- GPT-5.4 with low thinking only

</boundaries>

<verification>
Before declaring plan complete:
- [ ] Both loops attempted
- [ ] Bait files removed after cleanup loop
- [ ] /verify diff feature exists after feature loop
- [ ] dev-results.md has both loop assessments
- [ ] Development value question answered with evidence
</verification>

<success_criteria>
- Both loops attempted with documented outcomes
- pi-verify left in working state with new feature
- Assessment of whether modules help during real development
</success_criteria>

<output>
After completion, create `.paul/phases/130-real-user-feature-dev/130-01-SUMMARY.md`
</output>
