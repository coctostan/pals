---
phase: 268-resume-workflow-instruction-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kernel/workflows/resume-project.md
  - tests/pi-end-to-end-validation.sh
  - tests/cross-harness-validation.sh
autonomous: false
---

<objective>
## Goal
Rewrite the resume workflow so its actual procedural steps enforce context-efficient, STATE-first resume behavior instead of merely containing selective-loading marker prose.

## Purpose
Phase 267 exposed a false-positive closure: `resume-project.md` contains the new bounded-read policy, but still tells agents to detect handoffs first, read broad STATE content, read handoff content eagerly, and treat archived handoffs as routine fallback. This plan fixes the operational instructions and adds validation that catches that class of failure.

## Output
- `kernel/workflows/resume-project.md` rewritten for bounded STATE section loading, correct handoff ordering, archived-handoff fallback discipline, concise reporting, and safer handoff lifecycle cleanup.
- Validation strengthened in `tests/pi-end-to-end-validation.sh` and `tests/cross-harness-validation.sh` so marker-prose presence is not enough.
- Phase 268 summary after APPLY at `.paul/phases/268-resume-workflow-instruction-audit/268-01-SUMMARY.md`.
</objective>

<context>
## Project Context
- `.paul/PROJECT.md` — active milestone is v2.63 Workflow Instruction Audit + Context-Efficiency Rewrite; v2.63 exists because Phase 267 added selective-loading wording without sufficiently rewriting underlying workflow procedures.
- `.paul/ROADMAP.md` — Phase 268 scope: review `kernel/workflows/resume-project.md` and installed/skill-facing copies for actual instruction order and read behavior; fix STATE/handoff ordering, bounded section reads, stale archived-handoff fallback, report verbosity, and contradictory broad-read language.
- `.paul/STATE.md` — Phase 268 ready to plan; PLAN/APPLY/UNIFY open.

## Prior Work
- `.paul/phases/267-validation-guardrails-evidence-closure/267-01-SUMMARY.md` — AC-1/AC-2 passed on selective-loading wording and anti-regrowth markers, but this session proved those validations did not catch operational contradictions.

## Source Files
- `kernel/workflows/resume-project.md` — target workflow. Current observed defects include: `Read .paul/STATE.md`, handoff detection before state loading, archived handoff selection before knowing whether it is needed, eager handoff file reads, and routine orphan-handoff cleanup.
- `tests/pi-end-to-end-validation.sh` — Pi/install validation currently checks only selective-loading marker presence for resume.
- `tests/cross-harness-validation.sh` — shared/install validation currently has no resume-specific semantic guardrail for STATE-first bounded loading or old broad-read absence.
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan advisory: modules.yaml loaded from installed registry (`kernel_version: 2.0.0`).

- ARCH: Markdown workflow plus shell validation files are in expected repo layers (`kernel/workflows/`, `tests/`). Current line counts: `kernel/workflows/resume-project.md` 274, `tests/pi-end-to-end-validation.sh` 2243, `tests/cross-harness-validation.sh` 1474. Existing validation scripts are large; keep additions localized and avoid broad helper rewrites.
- TODD: Existing TAP-style validation files are in scope. Plan must add/adjust failing semantic guardrails before or alongside workflow edits so the old false-positive behavior cannot pass by marker prose alone.
- IRIS: No TODO/FIXME/HACK/XXX markers found in planned files. Review flag: current resume workflow contains contradictory broad/stale-context instructions despite selective-loading markers.
- DAVE: No CI config files found in repo scope; no deploy/runtime config changes planned. Local validation remains `git diff --check`, Pi validation, and cross-harness validation.
- DOCS: Planned files are lifecycle workflow and validation scripts. No README/API docs required; lifecycle truth will be recorded through PLAN/SUMMARY/STATE/ROADMAP.
- CODI: skipped — no extractable symbols in phase scope (markdown/shell validation work; no TS/JS selectors).
- RUBY: Debt flags are instruction-level: broad imperative reads, stale archived-handoff routing, eager context loading, routine cleanup side effects, and validation that checks marker presence instead of semantic behavior.

[dispatch] pre-plan enforcement: DEAN PASS — no root package manager/lockfile found and no dependency manifests are modified by this plan; no dependency audit block applies.

## Post-Plan Module Dispatch
[dispatch] post-plan: TODD reviewed plan shape; no TDD conversion. Guardrail-first validation is represented in Task 1 and must run before finalizing APPLY.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | PLAN/APPLY/UNIFY lifecycle loop | ✓ |
| /carl | required | Session boundary management if context pressure rises | automatic |
| TODD | required | Validation guardrail design before/during APPLY | ✓ via module dispatch |
| WALT | required | Quality validation before completion | ✓ via validation commands |

**BLOCKING:** APPLY must remain under PAUL parent authority. No repo-local `pals-implementer` delegation should be used for the core workflow rewrite unless the parent has already approved a narrow helper packet; this phase is intentionally interactive and instruction-sensitive.
</skills>

<acceptance_criteria>
## AC-1: Resume uses bounded STATE-first loading
```gherkin
Given `kernel/workflows/resume-project.md` is used for `/paul:resume`
When a routine resume starts
Then the workflow instructs agents to load bounded STATE sections first, with explicit small section windows for Current Position, Loop Position, and Session Continuity, before any routine handoff discovery or archived handoff lookup.
```

## AC-2: Handoffs are fallback context, not routine context
```gherkin
Given STATE contains enough current resume context
When no explicit handoff path is supplied
Then the resume workflow does not search archived handoffs or read handoff files by default, and only selects handoffs when STATE points to one, an active handoff is needed to fill missing context, or the user provides a path.
```

## AC-3: Broad-read and eager-context instructions are removed
```gherkin
Given the resume workflow has selective-loading policy text
When validation inspects the actual procedural steps
Then old broad/eager instructions such as `Read .paul/STATE.md`, `Read handoff file content`, and most-recent archived-handoff preference cannot remain in the routine path.
```

## AC-4: Validation catches semantic regressions, not just marker prose
```gherkin
Given a future change adds the words `bounded window`, `full read`, `fallback`, `heading`, `marker`, and `phase row`
When the workflow still tells agents to read STATE broadly or search archived handoffs first
Then Pi and cross-harness validation fail with resume-specific messages.
```

## AC-5: Rewrite is collaborative section-by-section
```gherkin
Given the user requested interactive wording control
When APPLY reaches the resume workflow rewrite
Then the agent presents one workflow section at a time with current problem, proposed replacement, and rationale, waits for user approval or wording changes, and edits only that approved section before moving to the next section.
```

## AC-6: Scope remains limited and verification stays green
```gherkin
Given Phase 268 is limited to resume workflow instruction repair and guardrails
When APPLY completes
Then only planned files plus routine PAUL lifecycle artifacts are changed, `git diff --check` passes, Pi validation passes, and cross-harness validation passes.
```
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add semantic resume guardrails before accepting the rewrite</name>
  <files>tests/pi-end-to-end-validation.sh, tests/cross-harness-validation.sh</files>
  <action>
    Add localized TAP assertions that fail when resume workflow semantics regress. The assertions must go beyond the Phase 267 marker check.

    Required guardrails:
    - Check repo source resume workflow and installed copies where each validation suite already has those paths available.
    - Require explicit STATE-first bounded section behavior. Accept stable markers such as a named step like `load_state_bounded` / `load_state_slices` and phrases for bounded `Current Position`, `Loop Position`, and `Session Continuity` windows.
    - Require handoff detection/fallback to occur after STATE context loading in the source text, using line-order or equivalent semantic checks.
    - Reject the old broad/eager routine instructions, including at minimum:
      - `Read \`.paul/STATE.md\`` as a standalone step.
      - `Read handoff file content` in the routine path.
      - unconditional most-recent archived handoff selection before STATE context is evaluated.
    - Keep additions local to the existing context-diet / workflow prose contract area; do not restructure the full validation scripts.
  </action>
  <verify>`bash tests/pi-end-to-end-validation.sh` and `bash tests/cross-harness-validation.sh` fail before the workflow rewrite or the new assertions demonstrably target existing bad text; after Task 2 they pass.</verify>
  <done>AC-3 and AC-4 satisfied: validation rejects the known bad resume instructions and cannot pass on marker prose alone.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Confirm the section-by-section rewrite protocol before editing `resume-project.md`.</decision>
  <context>
    APPLY must not perform a bulk rewrite. Before changing workflow text, it must present a compact audit of the current instruction failures and propose a section order. Then it must work one section at a time.

    Required collaboration loop for each section:
    1. Show the section name and the smallest useful current excerpt.
    2. State the concrete problem in that section: over-read, stale context, wrong ordering, unnecessary ceremony, broad cleanup, validation false positive, or unclear wording.
    3. Propose replacement wording or a focused diff.
    4. Ask for user approval or wording changes.
    5. Edit only the approved section.
    6. Re-read that section to verify the wording before moving on.

    Expected section order:
    1. Purpose / loop context / required reading.
    2. PAUL existence and bounded STATE loading.
    3. Optional handoff and resume-file context resolution.
    4. Config, git state, and single-action routing.
    5. Report format and verbosity.
    6. Handoff lifecycle cleanup.
    7. Error handling and output contract.
  </context>
  <options>
    <option id="approve-section-loop">
      <name>Approve section-by-section collaborative rewrite</name>
      <pros>Gives the user wording control and prevents another marker-only or bulk procedural rewrite.</pros>
      <cons>Requires more APPLY turns and pauses between workflow sections.</cons>
    </option>
    <option id="adjust-section-loop">
      <name>Adjust section order or collaboration protocol</name>
      <pros>Allows the user to change how the rewrite proceeds before any workflow mutation.</pros>
      <cons>Pauses execution and may require plan/task wording updates.</cons>
    </option>
  </options>
  <resume-signal>Reply `approve-section-loop` to continue, or describe the protocol/section-order adjustment.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Collaboratively rewrite resume workflow section by section</name>
  <files>kernel/workflows/resume-project.md</files>
  <action>
    Rewrite the workflow instructions, not just the policy paragraph, using the approved section-by-section collaboration loop from the checkpoint. Do not edit ahead of the user's approved section.

    Required edits across the approved sections:
    - Update purpose/loop wording so resume restores context from bounded STATE sections and routes from authoritative lifecycle state.
    - Replace `<required_reading>` with section-specific STATE slices and only conditional handoff/resume-file context.
    - Replace `detect_handoffs` before `load_state` with STATE-first order. A suitable structure is: `verify_paul_exists`, `load_state_bounded`, `check_config_version`, `resolve_optional_handoff_context`, `load_needed_resume_context`, `check_git_state`, `determine_single_action`, `report_and_route`, `handoff_lifecycle`.
    - In `load_state_bounded`, explicitly instruct agents to locate section headings/markers and read small bounded windows, e.g. Current Position ≤20 lines, Loop Position ≤12 lines, Session Continuity ≤18 lines, and Git/config fields only as needed. Full STATE read is allowed only for missing/contradictory fields, audit, repair, migration, lifecycle write, or whole-artifact validation.
    - Make handoff handling conditional: explicit user path first; otherwise use STATE `Resume file` only when it names a handoff; otherwise inspect active `.paul/HANDOFF*.md` only if STATE context is missing or contradictory; archived handoffs only if user supplied the path, STATE points there, or no adequate current context exists.
    - Replace eager `Read handoff file content` with bounded handoff section reads (`Current State`, `What's Next`, `Key Decisions`, `Resume Instructions`) and full handoff read only as fallback for malformed/contradictory handoff context.
    - Replace eager resume-file reads with bounded plan/summary/status slices only when STATE says they are needed for routing.
    - Keep report concise and exactly one next action. Do not require loading `.paul/PROJECT.md` merely to fill decorative output if STATE/ROADMAP context already identifies the project/phase.
    - Make lifecycle cleanup narrow: archive only an active handoff that was actually consumed after the user proceeds; do not search/clean orphaned handoffs during routine resume.
    - Avoid harness-specific tool names and avoid shell-only examples where plain workflow instructions are clearer.
  </action>
  <verify>After each approved section edit, re-read only that section. At the end, semantic guardrails from Task 1 pass and manual inspection confirms the old broad/eager routine instructions are gone and STATE-first ordering is explicit.</verify>
  <done>AC-1, AC-2, AC-3, and AC-5 satisfied: routine resume now instructs bounded STATE-first loading and conditional handoff fallback, and the rewrite happened through approved section-level wording.</done>
</task>

<task type="auto">
  <name>Task 3: Install/validate and record Phase 268 evidence</name>
  <files>kernel/workflows/resume-project.md, tests/pi-end-to-end-validation.sh, tests/cross-harness-validation.sh</files>
  <action>
    Run the final verification set and prepare the evidence needed for UNIFY.

    Required checks:
    - `git diff --check`
    - `bash tests/pi-end-to-end-validation.sh`
    - `bash tests/cross-harness-validation.sh`
    - Inspect changed-file scope and confirm only planned files plus routine `.paul/*` lifecycle artifacts changed.

    If validation fails because installed copies do not reflect source changes, follow the existing installer/validation flow already used by the tests rather than editing installed runtime files directly.
  </action>
  <verify>All required commands pass and changed-file scope is bounded.</verify>
  <done>AC-4 and AC-5 satisfied: validations are green and evidence is ready for `268-01-SUMMARY.md`.</done>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- Do not edit installed runtime copies under `~/.pi`, `~/.claude`, or other home-directory install targets directly.
- Do not modify `plan-phase.md`, `apply-phase.md`, `unify-phase.md`, or `pause-work.md`; they have dedicated later phases.
- Do not change module manifests, extension TypeScript, driver installers, or project dependencies.
- Do not rewrite the full validation suites; keep tests localized to resume workflow semantics.

## SCOPE LIMITS
- This phase fixes `resume-project.md` only.
- No broad lifecycle artifact compaction or historical archive rewrite.
- No v2.63 narrative deduplication architecture work.
- No helper delegation unless explicitly approved after the checkpoint; parent APPLY remains authoritative.
</boundaries>

<verification>
Before declaring plan complete:
- [ ] Semantic resume guardrails added to Pi validation and cross-harness validation.
- [ ] `kernel/workflows/resume-project.md` no longer contains the old routine broad/eager read instructions.
- [ ] Workflow order is STATE-first, then conditional handoff/resume context.
- [ ] `git diff --check` passes.
- [ ] `bash tests/pi-end-to-end-validation.sh` passes.
- [ ] `bash tests/cross-harness-validation.sh` passes.
- [ ] Changed-file scope is limited to planned files plus routine PAUL lifecycle artifacts.
</verification>

<success_criteria>
- Resume instructions are operationally context-efficient, not merely policy-compliant.
- Archived handoffs are no longer routine resume context.
- Validation fails on the exact class of Phase 267 false positive observed in this session.
- Phase 268 remains limited to resume workflow and semantic guardrails.
</success_criteria>

<output>
After completion, create `.paul/phases/268-resume-workflow-instruction-audit/268-01-SUMMARY.md` with:
- Audit findings from the original resume workflow.
- Exact semantic guardrails added.
- Final verification results.
- Any deferred issues for phases 269–272.
</output>
