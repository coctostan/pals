---
phase: 216-documentation-validation-surfacing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - drivers/pi/extensions/README.md
  - drivers/pi/skill-map.md
  - .pi/agents/pals-implementer.md
  - tests/pi-end-to-end-validation.sh
  - tests/cross-harness-validation.sh
autonomous: true
---

<objective>
## Goal
Surface the Phase 214/215 delegated APPLY packet/report guarantees in user-facing documentation, Pi documentation, the Pi skill map, the repo-local helper-agent definition, and validation suites.

## Purpose
Phase 215 hardened runtime/helper behavior. Phase 216 makes those guarantees discoverable and test-protected without reopening runtime delegation mechanics or closing v2.50 early.

## Output
- Documentation updates that explain parent-owned task packets, structured helper reports, allowed/forbidden file scope, fallback triggers, parent-run official verification, actual changed-file diff checks, and no helper-owned lifecycle authority.
- Helper-agent documentation/contract wording that is easier for parent APPLY and reviewers to inspect.
- Pi and cross-harness validation marker checks labeled around Helper Delegation and Shared Invariant coverage.
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md
.paul/phases/215-runtime-delegation-packet-report-hardening/215-01-SUMMARY.md
docs/PI-NATIVE-DELEGATED-APPLY-PACKET-REPORT-CONTRACT.md

## Source Files
README.md
drivers/pi/extensions/README.md
drivers/pi/skill-map.md
.pi/agents/pals-implementer.md
tests/pi-end-to-end-validation.sh
tests/cross-harness-validation.sh
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
Planning collaboration level: high (project default from `pals.json`). Planning mode: direct-requirements.

[dispatch] pre-plan advisory: modules.yaml loaded from installed registry; TODD, IRIS, DAVE, DOCS, CODI, RUBY and expert advisory modules considered for planned files.

- TODD: validation infrastructure exists as shell validation suites; no TDD conversion because this is documentation/marker-validation surfacing, not new behavior implementation.
- IRIS: no TODO/FIXME/HACK/XXX patterns found in planned files.
- DAVE: no CI config files discovered in scope; no deployment surface changes planned.
- DOCS: 101 markdown docs found within depth 3; planned docs and validation files are directly documentation/validation surfaces for the delegated APPLY contract.
- CODI: skipped — no extractable symbols in phase scope; planned source selectors are markdown and shell validation files, not `.ts/.tsx/.js/.jsx` code selectors.
- RUBY/ARCH: planned files include large pre-existing validation scripts (`tests/pi-end-to-end-validation.sh`, `tests/cross-harness-validation.sh`); this plan limits edits to localized marker checks and documentation prose.
- ARIA/LUKE/DANA/GABE/OMAR/PETE/REED/VERA: no blocking UI/data/API/observability/performance/resilience/privacy implementation surface in planned changes; README contains existing module descriptions only.

[dispatch] pre-plan enforcement: DEAN found no package manager lock/manifest in repo root for an audit command; no dependency changes planned and no dependency block applies. SETH secret scan over planned files found no hardcoded secret markers.

[dispatch] post-plan: TODD considered plan type `execute`; no TDD restructuring applied.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | Planning, applying, and unifying this lifecycle work | ✓ |
| /carl | required | Session boundary monitoring | active via Pi extension |
| TODD | required | Test/validation enforcement during APPLY | registry-loaded |
| WALT | required | Quality gating and validation during APPLY/UNIFY | registry-loaded |

**BLOCKING:** Required skills/modules must remain available before APPLY proceeds.
</skills>

<acceptance_criteria>
## AC-1: User-facing delegated APPLY guarantees are explicit
```gherkin
Given README readers need to understand delegated APPLY
When they read the lifecycle and install/runtime surface sections
Then they can see that helper delegation requires a parent-owned task packet, structured helper report, explicit allowed/forbidden file scope, fallback triggers, parent-run official verification, actual changed-file diff checks, and no helper-owned `.paul/*` lifecycle authority
```

## AC-2: Pi and helper-agent docs surface parent authority
```gherkin
Given Pi users may see `pals-implementer` as an installed helper agent
When they read Pi extension docs, the Pi skill map, or the helper-agent definition
Then those surfaces explain that `pals-implementer` is bounded to eligible repo-local tasks, returns structured reports, stops on fallback triggers, and cannot own validation, module gates, checkpoints, GitHub Flow, or lifecycle writes
```

## AC-3: Validation protects Helper Delegation and Shared Invariant coverage
```gherkin
Given Phase 216 changes documentation and validation surfaces
When Pi and cross-harness validation suites run
Then they include marker checks for Helper Delegation and Shared Invariant language across docs/helper-agent surfaces and still pass with reconciled counts
```

## AC-4: Phase boundaries remain intact
```gherkin
Given Phase 217 owns milestone evidence reconciliation and closure
When Phase 216 completes
Then it does not close v2.50, does not reopen Phase 215 runtime behavior unless validation reveals a required fix, and preserves parent-owned APPLY, command-output truth, module evidence, checkpoints, GitHub Flow safety, and `.paul/*` authority
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Surface delegated APPLY guarantees in docs and helper-agent wording</name>
  <files>README.md, drivers/pi/extensions/README.md, drivers/pi/skill-map.md, .pi/agents/pals-implementer.md</files>
  <action>
    Update documentation prose only:
    - Expand README delegated APPLY operating model with the packet/report/file-scope/fallback/parent-verification/changed-file-diff/no-helper-lifecycle guarantees from `docs/PI-NATIVE-DELEGATED-APPLY-PACKET-REPORT-CONTRACT.md`.
    - Expand `drivers/pi/extensions/README.md` and `drivers/pi/skill-map.md` so Pi users can distinguish lifecycle entry surfaces from helper-agent surfaces and understand that Helper Delegation is parent-authoritative.
    - Add/adjust `.pi/agents/pals-implementer.md` wording so the helper-agent contract visibly names parent-owned task packet, structured helper report, allowed files, forbidden files, fallback triggers, parent-run official verification, actual changed-file diff review, and `Authority: Derived aid only; no helper-owned .paul/* lifecycle writes`.
    - Avoid changing runtime workflow behavior, install logic, command routing, module dispatch semantics, or lifecycle artifacts beyond normal PLAN/APPLY/UNIFY writes.
  </action>
  <verify>grep -n "Parent-owned task packet\|structured helper report\|Allowed files:\|Forbidden files:\|Fallback triggers:\|Parent-run official verification:\|changed-file diff\|Authority: Derived aid only; no helper-owned .paul/\* lifecycle writes\|Helper Delegation" README.md drivers/pi/extensions/README.md drivers/pi/skill-map.md .pi/agents/pals-implementer.md</verify>
  <done>AC-1 and AC-2 satisfied: docs/helper surfaces expose the delegated APPLY packet/report guarantees without runtime behavior changes.</done>
</task>

<task type="auto">
  <name>Task 2: Add validation surfacing for Helper Delegation and Shared Invariant markers</name>
  <files>tests/pi-end-to-end-validation.sh, tests/cross-harness-validation.sh</files>
  <action>
    Add localized marker checks to existing delegated APPLY / shared invariant validation sections:
    - In Pi validation, protect docs/helper-agent markers for Helper Delegation: parent-owned task packet, structured helper report, allowed/forbidden files, fallback triggers, parent-run official verification, changed-file diff review, and no helper-owned lifecycle authority.
    - In cross-harness validation, protect Shared Invariant markers showing parent-owned APPLY, module evidence, checkpoint/GitHub Flow safety, command-output truth, and `.paul/*` authority remain parent/workflow-owned even when helper delegation is available.
    - Keep additions structural and marker-based; do not add brittle long-prose assertions.
    - Reconcile expected TAP counts only if the suites gain checks, and record old→new counts during APPLY/UNIFY evidence.
  </action>
  <verify>bash tests/pi-end-to-end-validation.sh && bash tests/cross-harness-validation.sh</verify>
  <done>AC-3 satisfied: validation suites protect Phase 216 Helper Delegation and Shared Invariant surfacing and pass.</done>
</task>

<task type="auto">
  <name>Task 3: Run install and focused evidence checks</name>
  <files>README.md, drivers/pi/extensions/README.md, drivers/pi/skill-map.md, .pi/agents/pals-implementer.md, tests/pi-end-to-end-validation.sh, tests/cross-harness-validation.sh</files>
  <action>
    Validate the surfaced docs against installed Pi resources and command-output truth:
    - Run `PALS_ROOT="$PWD" bash drivers/pi/install.sh` so installed Pi skill/agent surfaces are refreshed from repo source.
    - Run focused greps for Helper Delegation / Shared Invariant marker families in changed files.
    - Run both validation suites and capture final TAP counts.
    - Review `git diff --name-only` to confirm changed files stay within this plan plus normal lifecycle artifacts.
  </action>
  <verify>PALS_ROOT="$PWD" bash drivers/pi/install.sh && bash tests/pi-end-to-end-validation.sh && bash tests/cross-harness-validation.sh && git diff --name-only</verify>
  <done>AC-3 and AC-4 satisfied: install/validation evidence is current, counts are reconciled, and scope remains bounded.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- `kernel/workflows/apply-phase.md` unless validation reveals a narrow marker repair that cannot be handled in docs/tests.
- `drivers/pi/extensions/pals-hooks.ts` runtime behavior.
- `drivers/pi/install.sh`, `install.sh`, `uninstall.sh`, or generated installed copies outside the repo.
- Dependency manifests, lockfiles, CI config, GitHub Flow automation, or module manifests.
- Phase 217 milestone-closure evidence beyond normal Phase 216 lifecycle handoff.

## SCOPE LIMITS
- This phase documents and validates existing delegated APPLY guarantees; it does not redesign helper delegation.
- Do not close v2.50; Phase 217 owns final evidence reconciliation and milestone closure.
- Do not delegate `.paul/*` lifecycle writes to `pals-implementer`.
- Helper output, if used later during APPLY, remains review input only; parent APPLY owns verification, module gates, fallback, checkpoints, GitHub Flow decisions, task status, and lifecycle artifacts.
</boundaries>

<verification>
Before declaring plan complete:
- [ ] Focused marker grep passes for delegated APPLY packet/report guarantees in docs/helper-agent surfaces.
- [ ] `PALS_ROOT="$PWD" bash drivers/pi/install.sh` passes.
- [ ] `bash tests/pi-end-to-end-validation.sh` passes and final TAP count is recorded.
- [ ] `bash tests/cross-harness-validation.sh` passes and final TAP count is recorded.
- [ ] `git diff --name-only` shows only approved source files plus normal `.paul/*` lifecycle artifacts.
- [ ] All acceptance criteria met.
</verification>

<success_criteria>
- Docs/helper-agent surfaces clearly explain delegated APPLY as parent-owned Helper Delegation, not a second lifecycle authority.
- Validation suites protect both Pi-supported Helper Delegation markers and cross-harness Shared Invariant markers.
- No runtime delegation behavior is changed unless a narrow validation repair is explicitly justified during APPLY.
- Phase 216 leaves v2.50 open for Phase 217 evidence reconciliation and closure.
</success_criteria>

<output>
After completion, create `.paul/phases/216-documentation-validation-surfacing/216-01-SUMMARY.md`.
</output>
