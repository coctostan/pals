---
phase: 294-s4-canonical-reply-delivery-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - drivers/pi/extensions/guided-workflow-delivery.ts
  - drivers/pi/extensions/pals-hooks.ts
  - tests/pi-end-to-end-validation.sh
  - docs/PI-NATIVE-GUIDED-WORKFLOW-CANONICAL-REPLY-DELIVERY-EXTRACTION-CONTRACT.md
  - drivers/pi/extensions/README.md
  - drivers/pi/skill-map.md
autonomous: true
---

<objective>
## Goal
Extract S4 canonical guided-workflow reply delivery from `pals-hooks.ts` into a dedicated Pi extension sibling while preserving behavior exactly.

## Purpose
Phase 293 defined the S4 offload contract. Phase 294 executes the bounded source extraction so `pals-hooks.ts` sheds the last inline S* delivery surface without weakening transcript-visible canonical replies, explicit user-choice requirements, checkpoint safety, or `.paul/*` lifecycle authority.

## Output
- New sibling `drivers/pi/extensions/guided-workflow-delivery.ts` with the S4 delivery/config surface and loader-compatible no-op factory.
- `drivers/pi/extensions/pals-hooks.ts` imports S4 functions/types from the new sibling and keeps existing call sites functionally identical.
- Pi validation guardrails repointed to assert S4 moved, S3/S4 separation remains visible, and safety markers still exist.
- Docs updated to surface `guided-workflow-delivery.ts` and record Phase 294 outcome.
</objective>

<context>
## Project Context
.paul/STATE.md — Phase 294 is ready to plan after Phase 293 contract completion and PR #212 merge.
.paul/ROADMAP.md — Phase 294 focus: create `guided-workflow-delivery.ts`, move S4 functions out of `pals-hooks.ts`, update docs/validation, preserve behavior exactly.

## Direct Prior Work
- `.paul/phases/293-s4-offload-contract/293-01-SUMMARY.md` — Phase 293 created the contract and recommends the Phase 294 sibling target.
- `docs/PI-NATIVE-GUIDED-WORKFLOW-CANONICAL-REPLY-DELIVERY-EXTRACTION-CONTRACT.md` — authoritative Phase 294 boundary, forbidden scope, and verification handoff.

## Source Context
- `drivers/pi/extensions/pals-hooks.ts` currently owns:
  - `GuidedWorkflowAutoPresent`
  - `GuidedWorkflowConfig`
  - `GUIDED_WORKFLOW_DEFAULTS`
  - `sendCanonicalWorkflowResponse`
  - `presentGuidedWorkflowMoment`
  - `loadGuidedWorkflowConfig`
  - `shouldAutoPresent`
- `drivers/pi/extensions/guided-workflow-detection.ts` owns S3 detection and exports `GuidedWorkflowMoment`; do not move S3 helpers.
- `drivers/pi/install.sh` already installs every `drivers/pi/extensions/*.ts` sibling by source-set rule, so no installer edit is planned.
</context>

<module_dispatch>
## Pre-Plan Module Dispatch
[dispatch] pre-plan enforcement: DEAN PASS — no dependency manifests or lockfiles in planned scope.
[dispatch] pre-plan advisory: ARCH PASS — extension siblings follow the existing flat `drivers/pi/extensions/*.ts` pattern; new sibling belongs in the same directory as S5/S1/S2/S3/S7/S6/S8 siblings.
[dispatch] pre-plan advisory: SETH PASS — no secret/auth material planned; S4 preserves existing user-message routing and must not add hidden state or alternate authority.
[dispatch] pre-plan advisory: TODD PASS — behavior-preserving extraction with existing shell validation coverage; no new pure-core TDD target selected.
[dispatch] pre-plan advisory: IRIS PASS — planned files may contain existing marker comments, but no TODO/FIXME-driven review blocker is expected.
[dispatch] pre-plan advisory: GABE skipped — no API endpoint files in scope.
[dispatch] pre-plan advisory: LUKE/ARIA skipped — no UI component files; Pi `ctx.ui.*` adapter calls are runtime integration, not JSX/HTML UI implementation.
[dispatch] pre-plan advisory: DANA skipped — no data schema/model/migration files.
[dispatch] pre-plan advisory: DAVE WARN — no CI workflow config detected; Phase 294 must rely on repo shell validation and PR Socket checks.
[dispatch] pre-plan advisory: DOCS PASS — source extraction must update Pi extension README, skill-map, and the S4 contract status/outcome.
[dispatch] pre-plan advisory: OMAR/REED PASS — preserve existing notify-only/degraded display behavior and follow-up delivery; no new runtime retry/timeout/logging surface planned.
[dispatch] pre-plan advisory: VERA skipped — no PII/privacy data paths in scope.
[dispatch] pre-plan advisory: PETE PASS — extraction should not add expensive work to hot hooks; move code without extra file reads beyond existing config read behavior.
[dispatch] pre-plan advisory: CODI limited — seed candidates: `sendCanonicalWorkflowResponse`, `presentGuidedWorkflowMoment`, `loadGuidedWorkflowConfig`, `shouldAutoPresent`; blast radius constrained to `pals-hooks.ts`, new sibling, Pi validation, and docs.
[dispatch] pre-plan advisory: RUBY PASS — keep extraction mechanical; avoid broad refactors beyond moving S4 surface.

## Post-Plan Module Dispatch
[dispatch] post-plan: TODD PASS — plan type remains `execute`; existing shell validation is the test strategy for behavior-preserving extraction.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | PLAN/APPLY/UNIFY lifecycle management | ✓ |
| /carl | required | Session boundary management during lifecycle transitions | automatic |
| TODD | required | Before/during code implementation; satisfied by existing shell validation coverage for extraction | n/a |
| WALT | required | APPLY quality/validation gates | ○ |

**BLOCKING:** Required skills must be satisfied before APPLY proceeds. WALT validation gates remain mandatory during APPLY.
</skills>

<acceptance_criteria>
## AC-1: S4 Delivery Surface Extracted
```gherkin
Given `pals-hooks.ts` currently defines S4 canonical reply delivery inline
When Phase 294 APPLY completes
Then `guided-workflow-delivery.ts` exports the S4 delivery/config functions and `pals-hooks.ts` imports them instead of defining them inline.
```

## AC-2: Canonical Reply Behavior Preserved
```gherkin
Given guided workflow moments may use notify, confirm, or select UI affordances
When the extracted S4 code handles a moment
Then canonical replies still send only after explicit mapped user choice, `deliverAs: "followUp"` remains used for non-idle contexts, notify-only mode never sends, and display config remains non-authoritative.
```

## AC-3: Validation and Docs Repointed
```gherkin
Given previous guardrails expected S4 identifiers in `pals-hooks.ts`
When Phase 294 extraction moves S4 ownership
Then validation checks assert the new sibling, absence of inline S4 definitions in `pals-hooks.ts`, preserved S3/S4 separation, install count increase, and docs name the new sibling.
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Extract S4 delivery sibling</name>
  <files>drivers/pi/extensions/guided-workflow-delivery.ts, drivers/pi/extensions/pals-hooks.ts</files>
  <action>
    Create `drivers/pi/extensions/guided-workflow-delivery.ts`.
    Move/export from `pals-hooks.ts` without behavior changes:
    - `GuidedWorkflowAutoPresent`
    - `GuidedWorkflowConfig`
    - `GUIDED_WORKFLOW_DEFAULTS`
    - `sendCanonicalWorkflowResponse`
    - `presentGuidedWorkflowMoment`
    - `loadGuidedWorkflowConfig`
    - `shouldAutoPresent`
    Import only needed helpers/types: value-import `compactWhitespace` and `readFileOr` from `./pals-hooks`; import `join` from `path`; type-import `GuidedWorkflowMoment` from `./guided-workflow-detection` if needed.
    End the new sibling with a default-exported no-op factory preceded by JSDoc containing `No-op Pi extension factory`.
    Update `pals-hooks.ts` to import the moved S4 surface from `./guided-workflow-delivery`, remove the inline S4 definitions, and preserve existing call sites functionally identical.
    Avoid: moving S3 detection helpers, command routing, lifecycle UI, context injection, CARL, artifact slices, workflow capsules, module activity parsing, or any `.paul/*` lifecycle authority into the sibling.
  </action>
  <verify>`grep`/source checks prove `guided-workflow-delivery.ts` exports all S4 functions, contains `No-op Pi extension factory`, and `pals-hooks.ts` imports from `./guided-workflow-delivery` while no longer defining the moved functions inline.</verify>
  <done>AC-1 and AC-2 satisfied: S4 surface moved and behavior-preserving call sites remain wired.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint Pi validation guardrails</name>
  <files>tests/pi-end-to-end-validation.sh</files>
  <action>
    Add `EXT_GUIDED_WORKFLOW_DELIVERY="$REPO_ROOT/drivers/pi/extensions/guided-workflow-delivery.ts"` beside the other extension sibling variables.
    Repoint existing S4 checks so they assert:
    - confirm/select, `pi.sendUserMessage`, `sendCanonicalWorkflowResponse`, `deliverAs: "followUp"`, notify-only no-send, no-auto markers, and display-config comments in `EXT_GUIDED_WORKFLOW_DELIVERY`;
    - S3 detection/no-inferred-merge markers remain in `EXT_GUIDED_WORKFLOW_DETECTION`;
    - `pals-hooks.ts` imports from `./guided-workflow-delivery` and no longer defines the moved S4 functions/types/constants inline;
    - existing S7/S6/S8 guardrails refer to S4 retained-by-import rather than inline ownership where needed.
    Add one bounded Phase 294 TAP guardrail for the new sibling extraction and install/source-set expectation if needed.
    Avoid weakening guardrails or deleting prior safety markers; repoint assertions to the new owner instead.
  </action>
  <verify>`bash tests/pi-end-to-end-validation.sh` passes; any TAP count delta is copied from fresh output into SUMMARY.</verify>
  <done>AC-3 satisfied: validation follows moved ownership and still protects S3/S4 separation plus no-auto boundaries.</done>
</task>

<task type="auto">
  <name>Task 3: Update extraction docs and run full validation</name>
  <files>docs/PI-NATIVE-GUIDED-WORKFLOW-CANONICAL-REPLY-DELIVERY-EXTRACTION-CONTRACT.md, drivers/pi/extensions/README.md, drivers/pi/skill-map.md</files>
  <action>
    Update docs to reflect Phase 294 completion shape:
    - Add a `Status After Phase 294` section to the S4 contract once extraction is implemented.
    - Update `drivers/pi/extensions/README.md` extension sibling lists and guided workflow section to name `guided-workflow-delivery.ts` as S4 owner instead of saying S4 remains inline in `pals-hooks.ts`.
    - Update `drivers/pi/skill-map.md` extension sibling lists and guided workflow UX description to name `guided-workflow-delivery.ts` as S4 owner.
    Run required validation and copy exact results to SUMMARY:
    - `PALS_ROOT="$PWD" bash drivers/pi/install.sh` install file count
    - `git diff --check`
    - `bash tests/helpers/artifact_consistency.sh`
    - `bash tests/pi-end-to-end-validation.sh`
    - `bash tests/cross-harness-validation.sh`
    Avoid: changing README/skill-map unrelated sections or claiming validation count deltas without fresh output.
  </action>
  <verify>Docs grep finds `guided-workflow-delivery.ts` in the S4 contract, extension README, and skill-map; all listed validation commands pass.</verify>
  <done>AC-1, AC-2, and AC-3 satisfied with docs and command-output evidence ready for UNIFY.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- S3 detection behavior in `drivers/pi/extensions/guided-workflow-detection.ts` except type-only references if absolutely necessary.
- S5/S1/S2/S7/S6/S8 sibling behavior.
- `/paul-*` command routing, shortcut bindings, command activation, or command-delivered `pi.sendUserMessage` behavior.
- `.paul/*` lifecycle authority from runtime code.
- Dependency manifests/locks, CI configs, Dockerfiles, Claude Code driver, Agent SDK driver, kernel workflow sources, module registry files.
- Installed runtime copies under `~/.pi/agent/extensions/` except through installer validation output.

## SCOPE LIMITS
- This plan is a mechanical S4 extraction plus validation/docs repoint.
- Do not redesign guided workflow UX, canonical responses, checkpoint semantics, merge-gate routing, or config defaults.
- Do not add telemetry, hidden state, caches, generated ledgers, alternate lifecycle truth, auto-approval, auto-continue, skipped checkpoints, UI-only lifecycle decisions, or inferred merge intent.
</boundaries>

<verification>
Before declaring APPLY complete:
- [ ] `PALS_ROOT="$PWD" bash drivers/pi/install.sh` reports the fresh Pi extension count; expected one more than Phase 293.
- [ ] Focused grep/source checks confirm S4 functions exported from `guided-workflow-delivery.ts` and removed from inline `pals-hooks.ts` definitions.
- [ ] Focused grep/source checks confirm `deliverAs: "followUp"`, `ctx.ui.confirm`, `ctx.ui.select`, `notify-only mode never sends a canonical reply`, `no auto-approval`, `no auto-continue`, `no skipped checkpoints`, `no UI-only lifecycle decisions`, and `no inferred merge intent` remain visible.
- [ ] `git diff --check`
- [ ] `bash tests/helpers/artifact_consistency.sh`
- [ ] `bash tests/pi-end-to-end-validation.sh`
- [ ] `bash tests/cross-harness-validation.sh`
- [ ] `git diff --name-only` contains only planned source/validation/docs plus expected `.paul/phases/294-*` PLAN/SUMMARY and lifecycle artifacts.
</verification>

<success_criteria>
- S4 canonical reply delivery is owned by `guided-workflow-delivery.ts`.
- `pals-hooks.ts` remains the Pi runtime orchestration entry point and imports S4 delivery/config from the sibling.
- Existing guided workflow behavior and safety guardrails remain intact.
- Installer source-set installs the new sibling and validation evidence is copied from fresh command output.
</success_criteria>

<output>
After completion, create `.paul/phases/294-s4-canonical-reply-delivery-extraction/294-01-SUMMARY.md` with:
- Files changed and exact S4 symbols moved.
- Validation command output/count reconciliation.
- Module execution reports, including WALT/DEAN/DAVE/DOCS/CODI/RUBY notes.
- PR URL, CI/check state, and merge-gate readiness.
- Confirmation that Phase 295 should measure context offload and rank next target.
</output>
