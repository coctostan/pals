---
phase: 219-runtime-validation-suite-classification-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/pi-end-to-end-validation.sh
  - tests/cross-harness-validation.sh
autonomous: true
---

<objective>
## Goal
Add explicit validation-classification markers and local guardrail checks to the Pi and cross-harness validation suites so current suite families are distinguishable by support purpose and command-output-backed before any future validation cleanup.

## Purpose
Phase 218 established `docs/PI-NATIVE-VALIDATION-CLASSIFICATION-CONTRACT.md` as the baseline taxonomy and suite inventory. Phase 219 must now harden the executable validation suites themselves so Pi-Supported Runtime, Shared Invariant, Frozen Legacy Parity, Runtime Lens, Guided UI Safety, Helper Delegation, GitHub Flow Safety, and Artifact / Process coverage is visible in command output and protected before Legacy Retention / Install-Surface Cleanup can be considered.

## Output
- `tests/pi-end-to-end-validation.sh` declares its current suite-family classification inventory and adds exactly one localized TAP guardrail for that inventory.
- `tests/cross-harness-validation.sh` declares its current suite-family classification inventory and adds exactly one localized TAP guardrail for that inventory.
- Fresh validation output reconciles the expected count changes from Pi 200→201 and cross-harness 116→117.
</objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md

## Prior Work
.paul/phases/218-validation-classification-baseline/218-01-SUMMARY.md
docs/PI-NATIVE-VALIDATION-CLASSIFICATION-CONTRACT.md

## Source Files
tests/pi-end-to-end-validation.sh
tests/cross-harness-validation.sh
</context>

<module_dispatch>
## Pre-Plan Module Dispatch

Planning posture: high collaboration from `pals.json` default; direct-requirements mode because STATE/ROADMAP and the Phase 218 contract define Phase 219 scope.

- `[dispatch] pre-plan advisory`: TODD, IRIS, DAVE, DOCS, CODI, RUBY, ARCH, SETH, ARIA, LUKE, DANA, GABE, OMAR, PETE, REED, and VERA scope probes completed; advisory only.
- TODD: Existing shell validation suites are the proof path: `tests/pi-end-to-end-validation.sh` and `tests/cross-harness-validation.sh`. No package-level root test framework was detected; `.pi/npm/node_modules` test files are installed dependency noise and not project test candidates.
- IRIS: No TODO/FIXME/HACK/XXX markers found in planned files.
- DAVE: No CI config files detected in bounded repo scan; no CI file is in planned scope.
- DOCS: Docs catalog exists and Phase 218 contract is the source taxonomy. Phase 219 should avoid broad docs/report surfacing; only validation suites are planned files.
- CODI: skipped — no extractable TS/JS source selectors in phase scope. Planned changes are shell-validation markers only.
- RUBY/ARCH: Planned files are pre-existing large validation scripts (`tests/pi-end-to-end-validation.sh` 1611 lines, `tests/cross-harness-validation.sh` 1201 lines). This is known validation-script size debt; additions must be localized and helper/loop based, not a broad refactor.
- SETH/ARIA/LUKE/DANA/GABE/OMAR/PETE/REED/VERA: No security, UI, data, API, observability, performance, resilience, or privacy implementation surface in planned files.
- Source accounting: scratch register created outside repo; plan accounts for ROADMAP Phase 219, Phase 218 contract, Phase 218 summary, and both validation suites.
- `[dispatch] pre-plan enforcement`: DEAN PASS — no dependency manifests or lockfiles are planned; dependency audit not applicable.

## Post-Plan Module Dispatch

- `[dispatch] post-plan`: TODD reviewed plan type `execute`; shell validation suites are the proof path. TDD conversion is not appropriate because this plan hardens existing validation scripts and verifies through focused greps plus full Pi/cross-harness validation.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | Required | PLAN/APPLY/UNIFY lifecycle management | ✓ |
| /carl | Required | Session boundary management | active via Pi extension |
| TODD | Required | Before/during implementation validation planning | ✓ module dispatch recorded |
| WALT | Required | APPLY/UNIFY quality validation | ○ load during APPLY |

**BLOCKING:** Required skills/modules must be active before APPLY proceeds. WALT validation runs during APPLY/UNIFY, not during PLAN creation.
</skills>

<acceptance_criteria>
## AC-1: Pi suite classification inventory is command-checked
```gherkin
Given the Phase 218 contract maps current Pi validation families to validation classes
When APPLY updates tests/pi-end-to-end-validation.sh
Then the Pi suite declares the mapped family→class inventory and adds exactly one TAP guardrail that checks those markers in command output
```

## AC-2: Cross-harness suite classification inventory is command-checked
```gherkin
Given the Phase 218 contract maps current cross-harness validation families to validation classes
When APPLY updates tests/cross-harness-validation.sh
Then the cross-harness suite declares the mapped family→class inventory and adds exactly one TAP guardrail that checks those markers in command output
```

## AC-3: Count-change reconciliation is explicit
```gherkin
Given validation count changes require old count, new count, reason, validation class affected, shared-invariant preservation rationale, and command-output evidence
When APPLY completes
Then Pi validation increases from 200 to 201 and cross-harness validation increases from 116 to 117 only because one localized classification guardrail was added to each suite
```

## AC-4: No validation cleanup or unrelated behavior change occurs
```gherkin
Given Legacy Retention / Install-Surface Cleanup remains deferred
When Phase 219 APPLY completes
Then no validation check is weakened, deleted, relocated, archived, or treated as legacy-only, and no runtime, installer, dependency, CI, GitHub Flow, report, or lifecycle-authority semantics are changed
```

## AC-5: Full validation remains green
```gherkin
Given command-output truth remains authoritative
When APPLY runs focused marker greps and the full validation suites
Then `bash tests/pi-end-to-end-validation.sh` and `bash tests/cross-harness-validation.sh` both pass with reconciled counts
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Add Pi validation classification inventory guardrail</name>
  <files>tests/pi-end-to-end-validation.sh</files>
  <action>
    Add a localized validation-classification section to the Pi validation suite without restructuring existing checks.

    Required implementation shape:
    - Add a new section before the final SUMMARY block named `CATEGORY 7: VALIDATION CLASSIFICATION INVENTORY` with `section "VALIDATION CLASSIFICATION INVENTORY"`.
    - Add a comment block headed exactly `Validation Classification Inventory — Pi Suite`.
    - Include exact family/class marker lines for every current Pi suite family from the Phase 218 contract:
      - `Installer Deployment => Pi-Supported Runtime / Shared Invariant`
      - `Module Execution Evidence => Shared Invariant / Pi-Supported Runtime`
      - `Skill Structural Validity => Pi-Supported Runtime / Shared Invariant`
      - `Thin Wrapper Contract => Shared Invariant / Pi-Supported Runtime`
      - `Delegated APPLY / REV Boundaries => Helper Delegation / Shared Invariant`
      - `Shared Workflow Prose Contract => Shared Invariant / Runtime Lens`
      - `Context-Diet Regression Guardrails => Shared Invariant / Runtime Lens`
      - `CODI Plan-Phase Distribution => Shared Invariant / Helper Delegation`
      - `Extension Structural Validity => Pi-Supported Runtime / Runtime Lens / Guided UI Safety`
      - `Driver Manifest Accuracy => Pi-Supported Runtime / Shared Invariant`
      - `Pi Discovery Surfaces => Pi-Supported Runtime / Runtime Lens / Helper Delegation`
      - `Collaborative Planning Model => Shared Invariant / Guided UI Safety`
      - `UX Readability & Color Enrichment => Pi-Supported Runtime / Guided UI Safety`
    - Add exactly one new `tap_file_contains_all` check named `Pi validation suite declares validation classification inventory` that checks the Pi suite itself for the header plus all mapping markers above.
    - Keep the addition localized; do not alter existing TAP helpers, existing category checks, runtime source checks, installer behavior, or existing check descriptions except where strictly needed for the new section.
  </action>
  <verify>grep -n "Validation Classification Inventory — Pi Suite\|Installer Deployment => Pi-Supported Runtime / Shared Invariant\|Extension Structural Validity => Pi-Supported Runtime / Runtime Lens / Guided UI Safety\|Pi validation suite declares validation classification inventory" tests/pi-end-to-end-validation.sh && bash tests/pi-end-to-end-validation.sh</verify>
  <done>AC-1 partially and AC-3 partially satisfied: Pi suite has explicit classification inventory markers, exactly one new guardrail, and Pi validation passes with expected count 201/201.</done>
</task>

<task type="auto">
  <name>Task 2: Add cross-harness validation classification inventory guardrail</name>
  <files>tests/cross-harness-validation.sh</files>
  <action>
    Add a localized validation-classification section to the cross-harness validation suite without restructuring existing checks.

    Required implementation shape:
    - Add a new section before the final SUMMARY block named `CATEGORY 5: VALIDATION CLASSIFICATION INVENTORY` with `section "VALIDATION CLASSIFICATION INVENTORY"`.
    - Add a comment block headed exactly `Validation Classification Inventory — Cross-Harness Suite`.
    - Include exact family/class marker lines for every current cross-harness suite family from the Phase 218 contract:
      - `Installer Parity => Frozen Legacy Parity / Shared Invariant`
      - `Shared APPLY / REV Boundaries => Shared Invariant / Helper Delegation`
      - `Durable Module Evidence Path => Shared Invariant / Artifact / Process`
      - `Portability Compliance => Shared Invariant / Frozen Legacy Parity`
      - `Shared Collaborative Planning Model => Shared Invariant / Guided UI Safety`
      - `Context-Diet Regression Guardrails => Shared Invariant / Runtime Lens`
      - `CODI Plan-Phase Distribution => Shared Invariant / Helper Delegation`
      - `Artifact Spec Compliance => Shared Invariant / Artifact / Process`
      - `Driver Manifest Completeness => Frozen Legacy Parity / Shared Invariant`
    - Add exactly one new `tap_file_contains_all` check named `Cross-harness validation suite declares validation classification inventory` that checks the cross-harness suite itself for the header plus all mapping markers above.
    - Keep the addition localized; do not alter existing TAP helpers, existing category checks, installer parity logic, portability logic, driver manifest checks, or existing check descriptions except where strictly needed for the new section.
  </action>
  <verify>grep -n "Validation Classification Inventory — Cross-Harness Suite\|Installer Parity => Frozen Legacy Parity / Shared Invariant\|Artifact Spec Compliance => Shared Invariant / Artifact / Process\|Cross-harness validation suite declares validation classification inventory" tests/cross-harness-validation.sh && bash tests/cross-harness-validation.sh</verify>
  <done>AC-2 partially and AC-3 partially satisfied: cross-harness suite has explicit classification inventory markers, exactly one new guardrail, and cross-harness validation passes with expected count 117/117.</done>
</task>

<task type="auto">
  <name>Task 3: Validate count reconciliation and scope boundaries</name>
  <files>tests/pi-end-to-end-validation.sh, tests/cross-harness-validation.sh</files>
  <action>
    Run final focused checks and full validation, then verify scope boundaries.

    Required checks:
    - Confirm both suites include their classification inventory headers.
    - Confirm representative markers for all validation-class groups are present across the two suites: Pi-Supported Runtime, Shared Invariant, Frozen Legacy Parity, Runtime Lens, Guided UI Safety, Helper Delegation, GitHub Flow Safety where already present, and Artifact / Process.
    - Run both full validation suites.
    - Confirm count changes are exactly Pi 200→201 and cross-harness 116→117 due to one new classification guardrail per suite.
    - Confirm `git diff --name-only` includes only `tests/pi-end-to-end-validation.sh`, `tests/cross-harness-validation.sh`, and expected `.paul/*` lifecycle artifacts.
    - Do not edit reports, README, runtime files, installer files, dependency manifests, CI config, GitHub Flow automation, legacy wrappers, or lifecycle authority semantics.
  </action>
  <verify>grep -n "Validation Classification Inventory — Pi Suite\|Validation Classification Inventory — Cross-Harness Suite\|Pi-Supported Runtime\|Shared Invariant\|Frozen Legacy Parity\|Runtime Lens\|Guided UI Safety\|Helper Delegation\|Artifact / Process" tests/pi-end-to-end-validation.sh tests/cross-harness-validation.sh && bash tests/pi-end-to-end-validation.sh && bash tests/cross-harness-validation.sh && git diff --name-only</verify>
  <done>AC-3, AC-4, and AC-5 satisfied: count changes are reconciled, full validation is green, and scope stays limited to validation-suite hardening plus lifecycle artifacts.</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- `docs/PI-VALIDATION-REPORT.md` and `docs/VALIDATION-REPORT.md` — Phase 220 owns broad report surfacing.
- `README.md`, `drivers/**`, `kernel/**`, `modules/**`, `install.sh`, `migrate.sh`, dependency manifests/lockfiles, CI config, generated installed runtime copies, and GitHub Flow automation.
- Legacy wrappers, Claude Code / Agent SDK support-tier semantics, checkpoint/approval behavior, parent-owned APPLY/helper delegation semantics, and `.paul/*` lifecycle authority rules.
- Existing validation checks must not be weakened, deleted, relocated, archived, or treated as legacy-only.

## SCOPE LIMITS
- This plan adds explicit classification inventory markers and one local guardrail per validation suite only.
- This plan does not perform Legacy Retention / Install-Surface Cleanup.
- This plan does not update validation reports, README/docs surfacing, runtime behavior, installer behavior, dependency state, CI, or merge-gate behavior.
- Validation-script large-file debt is acknowledged but not refactored in this plan.
- Lifecycle updates to `.paul/STATE.md`, `.paul/ROADMAP.md`, and `.paul/PROJECT.md` are allowed only through PLAN/APPLY/UNIFY bookkeeping.
</boundaries>

<verification>
Before declaring APPLY complete:
- [ ] Focused marker grep confirms both validation inventory headers and representative family/class markers are present.
- [ ] `bash tests/pi-end-to-end-validation.sh` passes with expected count 201/201.
- [ ] `bash tests/cross-harness-validation.sh` passes with expected count 117/117.
- [ ] Count-change reconciliation records old count, new count, reason, validation class affected, shared-invariant preservation rationale, and command-output evidence.
- [ ] `git diff --name-only` shows only both validation scripts plus expected `.paul/*` lifecycle artifacts.
- [ ] All acceptance criteria are met.
</verification>

<success_criteria>
- Pi validation suite declares and command-checks its Phase 218 classification inventory.
- Cross-harness validation suite declares and command-checks its Phase 218 classification inventory.
- Command-output truth remains authoritative and green.
- Legacy cleanup remains deferred.
- Validation count changes are intentional, localized, and reconciled.
</success_criteria>

<output>
After completion, create `.paul/phases/219-runtime-validation-suite-classification-hardening/219-01-SUMMARY.md` with AC results, changed files, command output, count-change reconciliation, module execution reports, GitHub Flow evidence, and Phase 220 handoff.
</output>
