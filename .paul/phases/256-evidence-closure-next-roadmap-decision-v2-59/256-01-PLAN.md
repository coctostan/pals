---
phase: 256-evidence-closure-next-roadmap-decision-v2-59
plan: 01
type: execute
wave: 1
depends_on:
  - phase: 253-s7-extraction-contract-bounded-submodule-plan
    provides: S7 `pals-context-injection` extraction contract + bounded Phase 254 handoff; preserved authority/activation tags, legacy header, custom message type, injection-event constants; Pi 227/227; cross-harness 135/135; PR #168 squash-merged 2026-05-03 as `f326db54`.
  - phase: 254-bounded-s7-submodule-extraction
    provides: `drivers/pi/extensions/pals-context-injection.ts` sibling extraction (88 LOC) with six S7 functions and six S7 constants single-located as exact-string markers; `pals-hooks.ts` reduced 947 → 903 LOC; type-only back-import + TAP-assertion repoint patterns ratified; install 5 → 6; Pi 227 → 228; cross-harness 135/135 unchanged; PR #169 squash-merged 2026-05-04 as `af1c1793`.
  - phase: 255-documentation-validation-surfacing-v2-59
    provides: README/Pi docs/skill-map naming `pals-context-injection.ts` + Phase 254 split sentence; `## Phase 254 Outcome` appended to modularization contract + extraction-target baseline; `## Status After Phase 254` appended to S7 contract; 3 bounded `tap_file_contains_all` Pi surfacing assertions; install 6 unchanged; Pi 228 → 231; cross-harness 135/135 unchanged; PR #170 squash-merged 2026-05-05 as `0a607c43`.
files_modified:
  - .paul/MILESTONES.md
  - .paul/PROJECT.md
  - .paul/ROADMAP.md
  - tests/cross-harness-validation.sh
autonomous: true
risk_tier: normal
collaboration_level: high
planning_mode: direct-requirements
review_preference: tbd
---

# Phase 256 Plan 01: Evidence Closure + Next Roadmap Decision (v2.59)

<objective>
## Goal
Close v2.59 Pi Extension Submodule Extraction Wave — S7 PALS Context Injection by aggregating Phases 253–256 evidence, marking v2.59 complete in lifecycle artifacts, recording a next-roadmap recommendation for v2.60 (or the next milestone), and adding one localized shared-invariant closure guardrail to `tests/cross-harness-validation.sh` that protects v2.59 milestone evidence.

## Purpose
v2.59 extracted and surfaced the S7 `pals-context-injection` sibling while preserving S4 canonical reply delivery in `pals-hooks.ts`, the exact authority/activation byte sequences (`[PALS_STATE_AUTHORITY=.paul/STATE.md]`, `[PALS_ACTIVATION_SIGNAL]`), the legacy header migration (`## PALS Context (auto-injected)`), the custom-message-type identity (`pals-context`), the injection-event constants (`before_agent_start`/`context`), the `keepOnlyLatestPalsContextMessage` housekeeping behavior, and the no-inferred-merge-intent boundary. Phase 256 makes the milestone closure durable without reopening runtime TypeScript, installer behavior, dependency/CI surfaces, validation-suite structure, or lifecycle authority. The next-milestone recommendation is recorded for `/paul:milestone`; it does not approve or start the next milestone.

This phase mirrors the Phase 245 / 248 / 252 closure shape one-to-one (3 tasks, 4 ACs, +1 cross-harness shared-invariant closure guardrail, no runtime change, autonomous). The ratified sibling-surfacing recipe (Phase 244 / 247 / 251 / 255) and the Phase 254 type-only back-import + TAP-assertion repoint patterns are preserved as durable patterns in PROJECT.md and the closure paragraph in MILESTONES.md.

## Output
- `.paul/MILESTONES.md` records v2.59 complete with Phases 253–256, PR #168/#169/#170 plus the Phase 256 PR placeholder/actual PR, validation count reconciliation, and the next-roadmap recommendation.
- `.paul/PROJECT.md` marks v2.59 complete and records a v2.60 recommendation as pending `/paul:milestone` approval.
- `.paul/ROADMAP.md` marks Phase 256/v2.59 complete and moves v2.59 into the completed milestone summary while preserving the next-phase route and prior completed-milestone history.
- `tests/cross-harness-validation.sh` gains exactly one localized closure guardrail for v2.59 shared milestone evidence, moving cross-harness validation from 135 to 136.
- UNIFY creates `.paul/phases/256-evidence-closure-next-roadmap-decision-v2-59/256-01-SUMMARY.md` with validation, scope, module, count, GitHub Flow, and next-roadmap evidence.
</objective>

<context>
## Project Context
- `.paul/STATE.md` — Phase 256 ready to plan; v2.59 progress 3 of 4 phases complete; post-Phase-255 baseline on main: install 6, Pi 231/231, cross-harness 135/135, artifact_consistency PASS, `git diff --check` clean.
- `.paul/PROJECT.md` — Active milestone summary lists v2.59 with 3 of 4 phases complete and Phase 256 next; current milestone pointer says ready to plan via `/paul:plan`.
- `.paul/ROADMAP.md` — current milestone table lists Phase 255 ✅ Complete and Phase 256 Not started.
- `.paul/MILESTONES.md` — Live Status pointer still references v2.59 with `Phase 253 of 256 ready to plan; 0 of 4 phases complete` (created 2026-05-03 and not refreshed mid-milestone). Phase 256 closure must replace this with v2.59 closed and v2.60 pending via `/paul:milestone`.
- `.paul/SPECIAL-FLOWS.md` — required lifecycle skills: /paul, /carl, TODD, WALT.

## Prior Work
- `.paul/phases/253-s7-extraction-contract-bounded-submodule-plan/253-01-SUMMARY.md` — S7 extraction contract: `docs/PI-NATIVE-PALS-CONTEXT-INJECTION-EXTRACTION-CONTRACT.md` created, S4/S6/S8 deferrals preserved, Pi 227/227 unchanged, cross-harness 135/135 unchanged, PR #168.
- `.paul/phases/254-bounded-s7-submodule-extraction/254-01-SUMMARY.md` — runtime extraction: `pals-context-injection.ts` (88 LOC) created with 6 functions + 6 markers single-located; `pals-hooks.ts` reduced 947 → 903 LOC; type-only back-import + TAP-assertion repoint patterns ratified; install 5 → 6, Pi 227 → 228, cross-harness 135/135 unchanged, PR #169.
- `.paul/phases/255-documentation-validation-surfacing-v2-59/255-01-SUMMARY.md` — surfacing: README/Pi docs/skill-map naming the new sibling, `## Phase 254 Outcome` / `## Status After Phase 254` appended to three contract docs (pure additions; prior sections byte-unchanged), 3 bounded Pi `tap_file_contains_all` surfacing assertions, sibling-surfacing recipe ratified a fourth time (Phase 244 / 247 / 251 / 255), Pi 228 → 231, cross-harness 135/135 unchanged, install 6 unchanged, PR #170.
- `.paul/phases/252-evidence-closure-next-roadmap-decision/252-01-PLAN.md` and `252-01-SUMMARY.md` — most-recent closure precedent (v2.58); Phase 256 mirrors this shape one-to-one with v2.59 numbers and the v2.60 recommendation.
- `.paul/phases/248-evidence-closure-next-roadmap-decision-v2-57/248-01-SUMMARY.md` and `.paul/phases/245-evidence-closure-next-roadmap-decision/245-01-SUMMARY.md` — earlier closure precedents.

## Source Files
- `.paul/MILESTONES.md` — Live Status table (line 9 v2.59 row), Completed Milestones table (line 79 v2.58 row, append v2.59 row immediately after), Current / Future Milestone Pointer paragraphs (line 91 v2.59 paragraph), footer trail (line 99). Phase 256 must update v2.59 paragraph to closure prose, append a Completed Milestones row for v2.59, append a v2.59 closure paragraph in the pointer section, and append the v2.59 closure footer entry.
- `.paul/PROJECT.md` — Active milestone summary (line 13), Active requirements (line 28), Current milestone pointer (line 65), Key Decisions table, footer (line 181). Phase 256 marks v2.59 complete (4 of 4 phases), updates the pointer to no active milestone (v2.60 selection pending), appends one Key Decisions row with the v2.59 closure recommendation, and refreshes the footer.
- `.paul/ROADMAP.md` — current milestone table (line 39 row 255 ✅, row 256 Not started), Phase 256 detail (line 54), latest-completed-milestone summary (line 58 v2.58 narrative), footer (line 116). Phase 256 marks itself complete, marks the entire v2.59 milestone complete, demotes v2.58 from "Latest completed milestone" to a row inside the completed-milestones list, promotes v2.59 to the new "Latest completed milestone" summary, and refreshes the footer.
- `tests/cross-harness-validation.sh` — current baseline 135/135 (unchanged across v2.59). Existing closure-guardrail blocks at lines 1316 (v2.56), 1332 (v2.57), 1348 (v2.58). Phase 256 will add one new `Phase 256 sibling extraction wave closure (v2.59 Shared Invariant)` block immediately after the Phase 252 v2.58 block (around line 1366) and before the SUMMARY block, modeled byte-for-byte on the Phase 252 closure block. Expected count delta: 135 → 136.

## CODI Seed Candidates
No TS/JS source selectors are planned. The terms `pals-context-injection`, `guided-workflow-detection`, `pals-hooks.ts`, and S1–S8 are milestone evidence/recommendation labels in lifecycle artifacts, not planned code changes. CODI may skip cleanly per the planning workflow guardrail (markdown/shell-only phases may record no seeds).
</context>

<module_dispatch>
## Pre-Plan Module Dispatch

Planning collaboration level: high (project default from `pals.json`). Planning mode: direct-requirements from STATE/ROADMAP.

[dispatch] config-version: `pals.json` schema_version 2.0.0 matches installed `modules.yaml` kernel_version 2.0.0 — no migration needed.

### Advisory Modules
- [dispatch] TODD: pre-plan advisory → no `*.test.*`/`*.spec.*`/`__tests__` tree in scope. The new bounded TAP closure assertion in `tests/cross-harness-validation.sh` carries source-shape coverage for the closure milestone. Plan type stays `execute`, not `tdd`.
- [dispatch] IRIS: pre-plan advisory → no `TODO`/`FIXME`/`HACK`/`XXX` markers will be introduced. Preserved-phrase audit pre-recorded: `Authority: Derived aid only`, `Pi is the supported runtime`, `S4 canonical reply delivery`, `Derived aid only`, `before_agent_start`, `pals-context-injection.ts`, `guided-workflow-detection.ts`, `pals-hooks.ts`. Existing closure-guardrail block titles (`Phase 245 sibling extraction wave closure (v2.56 Shared Invariant)`, `Phase 248 sibling extraction wave closure (v2.57 Shared Invariant)`, `Phase 252 sibling extraction wave closure (v2.58 Shared Invariant)`) must remain byte-unchanged.
- [dispatch] DAVE: pre-plan advisory → no CI / Dockerfile / installer / driver.yaml changes. GitHub Flow PR checks remain merge-gate evidence during UNIFY.
- [dispatch] DOCS: pre-plan advisory → updates lifecycle closure artifacts (`.paul/MILESTONES.md`, `.paul/PROJECT.md`, `.paul/ROADMAP.md`) plus one shared validation guardrail. No product/runtime docs touched.
- [dispatch] RUBY: pre-plan advisory → `tests/cross-harness-validation.sh` is an existing large validation script (1389 lines). Size debt is pre-existing/future validation-architecture scope; this plan adds one localized closure block only and does not refactor the suite. Broader cross-harness validation-script size-debt remains queued/deferred per v2.58 closure.
- [dispatch] CODI: pre-plan advisory → SKIP-cleanly. Markdown/shell-only phase; no `.ts/.tsx/.js/.jsx` symbols in scope; planning workflow guardrail allows clean skip when seeds are not seedable.
- [dispatch] ARCH: pre-plan advisory → flat docs/drivers/kernel/modules/tests lifecycle repo structure. Planned files stay in expected lifecycle/test layers; no circular dependency risk.
- [dispatch] ARIA / DANA / GABE / LUKE / OMAR / PETE / REED / VERA: pre-plan advisory → SKIP-cleanly (no UI / data / API / observability / performance / resilience / privacy implementation surface in planned files).

### Blast Radius (CODI)
[dispatch] CODI: pre-plan advisory → markdown / shell-only phase; no `.ts/.tsx/.js/.jsx` symbols in scope. Skipping cleanly per planning workflow guardrail.

### Enforcement Modules
- [dispatch] DEAN: pre-plan enforcement → no root package manifest / lockfile detected; no dependency files modified. PASS.
- [dispatch] SETH: pre-plan enforcement → no auth/input-validation surface in planned files; no hardcoded-secret patterns in lifecycle artifacts. PASS.

## Post-Plan Module Dispatch

- [dispatch] post-plan: TODD → execute plan is appropriate; no RED/GREEN/REFACTOR restructuring suggested. Shell validation and focused marker checks are the proof path.
</module_dispatch>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /paul | required | All planning/applying/unifying lifecycle work | ✓ |
| /carl | required | Automatic session boundary monitoring | automatic |
| TODD | required | Test/validation enforcement during APPLY (advisory for closure phase with bounded TAP guardrails) | ✓ module dispatch recorded |
| WALT | required | APPLY/UNIFY quality gating and validation | pending APPLY |

**BLOCKING:** Required lifecycle/module checks must be honored before APPLY proceeds. Parent APPLY remains authoritative for verification, module enforcement, fallback, GitHub Flow gates, and `.paul/*` lifecycle writes.
</skills>

<acceptance_criteria>
## AC-1: v2.59 closure is durable in MILESTONES.md
```gherkin
Given v2.59 has completed Phase 253 contract, Phase 254 runtime extraction, Phase 255 documentation/validation surfacing, and Phase 256 evidence closure
When `.paul/MILESTONES.md` is read after APPLY
Then the Completed Milestones table contains a v2.59 row naming Phases 253, 254, 255, and 256, the four PRs (`#168`, `#169`, `#170`, and the Phase 256 PR placeholder/actual PR), and the four corresponding SUMMARY paths; the Live Status `Current milestone` row says no active milestone after v2.59 closure and v2.60 selection is pending via `/paul:milestone` with the recorded default recommendation; the Current / Future Milestone Pointer section gains a v2.59 closure paragraph naming `pals-context-injection.ts`, preserved S4 `pals-hooks.ts` delivery, the type-only back-import + TAP-assertion repoint patterns, the 4th ratification of the sibling-surfacing recipe (Phase 244 / 247 / 251 / 255), validation reconciliation Pi 227 → 231 (+4), cross-harness 135 → 136 (+1 after this closure guardrail), install 5 → 6 (+1), and the v2.60 recommendation; and the footer trail appends `updated for v2.59 milestone closure: 2026-05-05`.
```

## AC-2: PROJECT.md and ROADMAP.md reflect v2.59 complete and preserve next-roadmap recommendation
```gherkin
Given Phase 256 is the final v2.59 phase
When `.paul/PROJECT.md` and `.paul/ROADMAP.md` are read after APPLY
Then PROJECT marks v2.59 complete (4 of 4 phases; closed via the Phase 256 PR placeholder/actual PR), updates the Active milestone summary and Current milestone pointer prose so Phase 256 is no longer described as pending, appends one Key Decisions row recording the v2.59 closure recommendation (default focus: a moderate-risk next candidate informed by the v2.59 evidence — see "Next-Roadmap Recommendation" below — alternatives preserved), and refreshes the footer to 2026-05-05; ROADMAP marks Phase 256 complete with completion date 2026-05-05, marks the v2.59 milestone complete, demotes v2.58 from "Latest completed milestone" narrative to a completed-milestone row, promotes v2.59 to the new "Latest completed milestone" summary naming Phases 253–256 and PRs #168/#169/#170 + Phase 256 PR placeholder/actual PR, preserves existing v2.55/v2.56/v2.57 completed-milestone summaries and archive links, and refreshes the footer to 2026-05-05.
```

## AC-3: Cross-harness closure guardrail is localized and count-reconciled
```gherkin
Given cross-harness validation currently passes 135/135 after v2.58 closure / Phase 252 (unchanged across v2.59 Phases 253–255)
When Phase 256 adds closure evidence protection
Then `tests/cross-harness-validation.sh` gains exactly one localized `tap_file_contains_all` block titled `Phase 256 sibling extraction wave closure (v2.59 Shared Invariant)`, modeled byte-for-byte on the existing Phase 252 closure block at lines 1348–1365, that asserts `.paul/MILESTONES.md` contains `v2.59 Pi Extension Submodule Extraction Wave`, `Phase 253`, `Phase 254`, `Phase 255`, `Phase 256`, `pals-context-injection.ts`, `guided-workflow-detection.ts` (preserved S3 sibling), `pals-hooks.ts` (preserved S4 host), and `v2.60`; the suite plan total moves from `1..135` to `1..136`; the suite passes 136/136 with zero failures; the existing Phase 245 / 248 / 252 closure blocks remain byte-unchanged.
```

## AC-4: Verification is green and scope remains bounded
```gherkin
Given APPLY has completed the planned closure edits
When final verification runs
Then `PALS_ROOT="$PWD" bash drivers/pi/install.sh` reports `[ok] Pi extensions installed: 6 files` (unchanged from Phase 255 baseline), `bash tests/pi-end-to-end-validation.sh` passes `1..231` / Passed 231 / Failed 0 unchanged, `bash tests/cross-harness-validation.sh` passes `1..136` / Passed 136 / Failed 0 (with exactly +1 from baseline 135), `bash tests/helpers/artifact_consistency.sh` returns `artifact_consistency_check: PASS`, `git diff --check` returns clean, and changed non-`.paul/` files are limited to `tests/cross-harness-validation.sh`. No `drivers/pi/extensions/*.ts`, `drivers/pi/install.sh`, `drivers/pi/uninstall.sh`, `drivers/pi/driver.yaml`, `tests/pi-end-to-end-validation.sh`, `tests/helpers/**`, `pals.json`, dependency, CI, kernel, module-registry, Claude Code driver, Agent SDK driver, or installed-runtime-copy file is in the diff.
```
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Aggregate v2.59 closure evidence in MILESTONES.md</name>
  <files>.paul/MILESTONES.md</files>
  <action>
    Update `.paul/MILESTONES.md` in bounded closure locations only, mirroring the Phase 252 v2.58 closure shape one-to-one:

    - **Live Status (line 9):** Replace the `Current milestone` row's Status cell so it reads no active milestone after v2.59 closure (closed 2026-05-05 with PR #170 squash-merged 2026-05-05 as `0a607c43` and Phase 256 PR placeholder/actual PR), and v2.60 selection is pending via `/paul:milestone` with the recorded default recommendation (see Next-Roadmap Recommendation below). Preserve the Pointer cell content unchanged.

    - **Completed Milestones table (after line 79 v2.58 row):** Append a new row immediately after the v2.58 row for `v2.59 Pi Extension Submodule Extraction Wave — S7 PALS Context Injection`, completed 2026-05-05, stats `4 phases, 4 plans`, detail naming Phase 253 contract, Phase 254 S7 extraction, Phase 255 docs/validation surfacing, Phase 256 closure, the four SUMMARY paths, and PRs `#168` (Phase 253), `#169` (Phase 254), `#170` (Phase 255), and the Phase 256 PR placeholder/actual PR.

    - **Current / Future Milestone Pointer (after the v2.58 paragraph at line 89, before the current "Current milestone" line 91):** Insert a new v2.59 closure paragraph: "v2.59 Pi Extension Submodule Extraction Wave — S7 PALS Context Injection is **complete** (closed 2026-05-05; Phase 256 PR placeholder/actual PR squash-merged). All 4 phases shipped: Phase 253 S7 Extraction Contract + Bounded Submodule Plan (PR #168 `f326db54`), Phase 254 Bounded S7 Submodule Extraction extracting `drivers/pi/extensions/pals-context-injection.ts` (88 LOC) while preserving S4 canonical reply delivery in `pals-hooks.ts` (PR #169 `af1c1793`), Phase 255 Documentation + Validation Surfacing across README/Pi docs/skill-map and three contract docs (PR #170 `0a607c43`), and Phase 256 Evidence Closure + Next Roadmap Decision. v2.59 milestone closure: Pi 227 → 231 (+4 across the wave: +1 from Phase 254 S7 source-shape + repointed assertion, +3 from Phase 255 surfacing); cross-harness 135 → 136 (+1 from this Phase 256 closure guardrail); install surface 5 → 6 (+1 sibling). Patterns durable: the sibling-surfacing recipe (Phase 244 / 247 / 251 / 255 — fourth ratification), the type-only back-import pattern (Phase 254), and the TAP-assertion repoint pattern (Phase 254). Scope preserved the exact authority/activation byte sequences (`[PALS_STATE_AUTHORITY=.paul/STATE.md]`, `[PALS_ACTIVATION_SIGNAL]`), the legacy header `## PALS Context (auto-injected)`, the custom-message-type identity `pals-context`, the injection-event constants `before_agent_start`/`context`, the `keepOnlyLatestPalsContextMessage` housekeeping behavior, S4 canonical reply delivery, transcript-visible canonical reply separation, no-inferred-merge-intent boundary, `.paul/*` authority, and command-output validation truth."

    - **Current milestone (line 91):** Update the prose so it reflects v2.59 closure (no longer "ready for `/paul:plan` Phase 253" or "0 of 4 phases complete"); replace with: "**Current milestone:** None active. v2.60 selection is pending via `/paul:milestone`. The v2.59 closure recorded a default recommendation as a candidate (see Next-Roadmap Recommendation below); alternatives remain S4 canonical reply delivery, S6 lifecycle UI, S8 command routing, full Claude Code / Agent SDK driver removal (still blocked per `docs/PI-NATIVE-LEGACY-RETENTION-INSTALL-SURFACE-CLEANUP-CONTRACT.md`), broader cross-harness validation-script size-debt work, local context-cost evidence, and demo project preset, each requiring separate planning."

    - **Next-Roadmap Recommendation (append after the new "Current milestone:" line):** "**Next-roadmap recommendation (recorded, not approved):** v2.60 default focus = a bounded continuation of the Pi-extension modularization wave. Two viable shapes for the next milestone, ranked by precedent strength: (a) S6 lifecycle UI extraction (next sibling in the modularization contract S1–S8 inventory after S1/S2/S3/S5/S7 shipped, isolating `lifecycle-ui` rendering paths from `pals-hooks.ts` while preserving S4 canonical reply delivery and the no-UI-only-lifecycle-decisions boundary); (b) S8 command routing extraction (highest user-visible compatibility surface, requires careful scoping of the `/paul-*` command registration boundary). S4 canonical reply delivery remains explicitly deferred regardless of next-milestone choice — it's the integration point for every other sibling and not a candidate for isolation. Final v2.60 selection happens via `/paul:milestone` after v2.59 closes."

    - **Footer trail (line 99):** Append `updated for v2.59 milestone closure: 2026-05-05` to the existing trail (semicolon-separated, preserving the format of prior trail entries).

    Preserve existing v2.55/v2.56/v2.57/v2.58 completed-milestone rows and pointer paragraphs verbatim; preserve archive pointer (line 10); preserve the Phase artifacts pointer (line 11).
  </action>
  <verify>Focused checks find `v2.59 Pi Extension Submodule Extraction Wave`, `Phase 253`, `Phase 254`, `Phase 255`, `Phase 256`, `pals-context-injection.ts`, `pals-hooks.ts`, `v2.59 milestone closure`, `v2.60`, and `Next-roadmap recommendation` in `.paul/MILESTONES.md`; existing `v2.58 Pi Extension Submodule Extraction Wave`, `v2.57 Pi Extension Submodule Extraction Wave`, `v2.56 Pi Extension Submodule Extraction Wave` rows and pointer paragraphs are byte-unchanged; `git diff --check` clean for this file.</verify>
  <done>AC-1 satisfied: MILESTONES.md durably records v2.59 closure with all four phases, the four PRs, the patterns ratified, the count reconciliation, and the v2.60 recommendation.</done>
</task>

<task type="auto">
  <name>Task 2: Close v2.59 in PROJECT.md and ROADMAP.md with next-roadmap recommendation</name>
  <files>.paul/PROJECT.md, .paul/ROADMAP.md</files>
  <action>
    Update `.paul/PROJECT.md` (mirroring the Phase 252 v2.58 closure shape):

    - **Active milestone summary (line 13):** Update the prose so v2.59 reads as "complete (4 of 4 phases; closed 2026-05-05 via PR #170 `0a607c43` and the Phase 256 PR placeholder/actual PR)" instead of "3 of 4 phases complete ... Phase 256 next". Preserve the milestone description, scope, the recipe-ratification note, and the out-of-scope deferrals verbatim.

    - **Current milestone pointer (line 65):** Update the prose so it no longer reads "Phase 256 of 256 ... ready to plan via `/paul:plan`"; replace with: "Current milestone pointer: **None active after v2.59 closure.** v2.60 selection is pending via `/paul:milestone` with a recorded default recommendation in `.paul/MILESTONES.md`. v2.59 closure baseline (post-Phase-256-merge on main): Pi 231/231 unchanged from Phase 255 + new closure guardrail bringing cross-harness to 136/136, artifact_consistency PASS, install 6 Pi extension files. Final 4-phase wave: Phase 253 (PR #168), Phase 254 (PR #169), Phase 255 (PR #170), Phase 256 (placeholder/actual PR)."

    - **Key Decisions table (after the existing Phase 255 closure decision row):** Append one new row with date `2026-05-05`, decision text recording v2.59 closure: "Close v2.59 Pi Extension Submodule Extraction Wave — S7 PALS Context Injection. Patterns ratified durable: sibling-surfacing recipe (Phase 244 / 247 / 251 / 255 — fourth ratification), type-only back-import pattern (Phase 254), TAP-assertion repoint pattern (Phase 254), sibling-module recipe (Phase 239 / 243 / 246 / 250 / 254 — fifth application). Recommend v2.60 default focus = a bounded continuation of the modularization wave (S6 lifecycle UI extraction or S8 command routing extraction; S6 is the higher-precedent next candidate). Alternatives preserved: S4 canonical reply delivery (explicitly deferred — integration point), full Claude Code / Agent SDK driver removal (still blocked), broader cross-harness validation-script size-debt work, local context-cost evidence, and demo project preset. Final v2.60 selection happens via `/paul:milestone` after v2.59 closes." Trigger column: `Phase 256 Plan 256-01 UNIFY`. Outcome column: "v2.59 closed 4 of 4 phases (PRs #168 / #169 / #170 / Phase 256 PR placeholder-actual). Pi 227 → 231 (+4); cross-harness 135 → 136 (+1 closure guardrail); install 5 → 6 (+1 sibling). Patterns documented in MILESTONES.md and PROJECT.md for v2.60 reference."

    - **Footer (line 181):** Replace with "*Last updated: 2026-05-05 after v2.59 closure via PR #170 (`0a607c43`) and the Phase 256 PR placeholder/actual PR. v2.55/v2.56/v2.57/v2.58 remain closed; v2.59 closed 4 of 4 phases (PRs #168 / #169 / #170 / Phase 256 PR); v2.60 selection pending via `/paul:milestone`.*"

    Update `.paul/ROADMAP.md`:

    - **Current milestone table (line 40):** Change Phase 256 row from "TBD | Not started | -" to "1/1 | ✅ Complete | 2026-05-05".

    - **Phase 256 detail (line 54–56):** Replace `Plans: TBD (defined during /paul:plan)` with `Plans: .paul/phases/256-evidence-closure-next-roadmap-decision-v2-59/256-01-PLAN.md + 256-01-SUMMARY.md — ✅ Complete: aggregated v2.59 closure evidence in MILESTONES.md / PROJECT.md / ROADMAP.md and added one localized cross-harness shared-invariant closure guardrail (135 → 136). AC-1–AC-4 PASS (install 6 unchanged, Pi 231/231 unchanged, cross-harness 1..136/0, artifact_consistency PASS, git diff --check clean). Phase 256 PR placeholder/actual PR squash-merged 2026-05-05; v2.59 closed 4 of 4 phases.`

    - **Latest completed milestone (line 58–60 v2.58 narrative):** Demote v2.58 from "Latest completed milestone" header to a completed-milestone row inside the existing completed-milestones list further down the file (or, if no such list exists in this ROADMAP, fold the v2.58 narrative into a one-line entry preserving Phase 249–252 + PRs #164/#165/#166/#167 evidence). Promote v2.59 to the new "Latest completed milestone" summary: `### Latest completed milestone: v2.59 Pi Extension Submodule Extraction Wave — S7 PALS Context Injection` followed by `Result: Phases 253–256 complete. v2.59 closed after Phase 256 PR placeholder/actual PR squash-merged 2026-05-05. Shipped S7 \`pals-context-injection\` sibling extraction (\`drivers/pi/extensions/pals-context-injection.ts\`, 88 LOC) while preserving S4 canonical reply delivery in \`pals-hooks.ts\`, README/Pi docs/skill-map plus three contract docs surfacing, and three bounded Pi surfacing TAP guardrails plus one cross-harness closure guardrail. Patterns ratified: sibling-surfacing recipe (4th ratification: Phase 244 / 247 / 251 / 255), type-only back-import pattern (Phase 254), TAP-assertion repoint pattern (Phase 254). Final validation: Pi 231/231, cross-harness 136/136, artifact_consistency PASS, install surface 6 Pi extension files. PRs: #168 (Phase 253) `f326db54`, #169 (Phase 254) `af1c1793`, #170 (Phase 255) `0a607c43`, Phase 256 PR placeholder/actual PR.` followed by `Per-phase detail (Phases 253–256 with PR numbers and durations) lives in \`.paul/MILESTONES.md\` and in the completed-milestone table further below.`

    - **Footer (line 116):** Replace with "*Last updated: 2026-05-05 — v2.59 Pi Extension Submodule Extraction Wave closed via Phase 256 PR placeholder/actual PR (squash-merged); 4 of 4 phases complete (PRs #168 / #169 / #170 / Phase 256 PR). Cross-harness moved 135 → 136 from the Phase 256 closure guardrail; Pi remains 231/231; install remains 6 files. v2.60 selection pending via `/paul:milestone` with recorded default recommendation in `.paul/MILESTONES.md`. v2.55/v2.56/v2.57/v2.58 remain closed.*"

    Avoid: editing v2.55/v2.56/v2.57/v2.58 prior closure narratives (must remain factually unchanged), editing prior `## Phase NNN Outcome` / `## Status After Phase NNN` sections in any docs, editing `.paul/STATE.md` (UNIFY owns the live STATE update), editing the prior Phase 252 closure-guardrail block in `tests/cross-harness-validation.sh` (Task 3 owns only the new v2.59 closure block).
  </action>
  <verify>Focused checks find `Complete (4 of 4 phases)`, `v2.59 closure`, `Phase 256`, `pals-context-injection.ts`, `v2.60`, and the 2026-05-05 footer text in `.paul/PROJECT.md`; ROADMAP shows row 256 ✅ Complete, the v2.59 milestone closure summary names Phases 253–256 and the 4 PRs, the v2.58 narrative is preserved as factual but demoted, and the new footer points to v2.59 closure. `git diff --check` clean for both files.</verify>
  <done>AC-2 satisfied: PROJECT.md and ROADMAP.md durably reflect v2.59 closure and preserve the v2.60 recommendation as recorded-not-approved.</done>
</task>

<task type="auto">
  <name>Task 3: Add shared closure guardrail and run official verification</name>
  <files>tests/cross-harness-validation.sh</files>
  <action>
    Add exactly one localized closure `tap_file_contains_all` block to `tests/cross-harness-validation.sh`, immediately after the existing Phase 252 v2.58 closure block (around line 1366) and before the SUMMARY block (line 1367), modeled byte-for-byte on the Phase 252 closure block at lines 1348–1365:

    - Header comment block: `# Phase 256 sibling extraction wave closure (v2.59 Shared Invariant)` with the standard `# ════` separator before and after the descriptive prose, mirroring the Phase 252 block exactly. Descriptive prose: "Phase 256 closes v2.59 by aggregating Phases 253–256 evidence into MILESTONES.md. Closure guardrail: .paul/MILESTONES.md must continue to name the v2.59 milestone, the four phases, the S7 sibling basename, the preserved S3 sibling, the preserved S4 host module, and the v2.60 recommendation marker so the shared invariant across docs/contract/lifecycle does not regress."
    - Title: `Phase 256 sibling extraction wave closure (v2.59 Shared Invariant)`
    - Target: `$REPO_ROOT/.paul/MILESTONES.md`
    - Required phrases (in this order to mirror the Phase 252 block): `v2.59 Pi Extension Submodule Extraction Wave`, `Phase 253`, `Phase 254`, `Phase 255`, `Phase 256`, `pals-context-injection.ts`, `guided-workflow-detection.ts`, `pals-hooks.ts`, `v2.60`.

    No TAP-plan-total update is required — the suite uses `echo "1..$TOTAL"` driven by a runtime counter, so adding one assertion automatically moves the plan total from 135 to 136.

    Do not modify `tests/pi-end-to-end-validation.sh`, `tests/helpers/**`, the existing Phase 245 / 248 / 252 closure blocks, helper functions, or unrelated test text. Do not touch the Phase 254 EXT_PALS_CONTEXT_INJECTION TAP block, the Phase 254 repoint of pre-existing assertion 133, or the Phase 255 surfacing TAP triple in `tests/pi-end-to-end-validation.sh`.

    Run official verification:
    - `PALS_ROOT="$PWD" bash drivers/pi/install.sh` — verify `[ok] Pi extensions installed: 6 files` (unchanged from Phase 255 baseline).
    - `bash tests/pi-end-to-end-validation.sh` — expect `1..231` / Passed 231 / Failed 0 unchanged.
    - `bash tests/cross-harness-validation.sh` — expect `1..136` / Passed 136 / Failed 0.
    - `bash tests/helpers/artifact_consistency.sh` — expect `artifact_consistency_check: PASS`.
    - `git diff --check` — expect clean.
    - changed-file scope check against this plan's allowed files plus standard `.paul/*` lifecycle artifacts.

    If the assertion run reveals any unrelated count shift (i.e., not exactly `+1` from baseline 135), stop and reconcile in-flight (revert / split with explicit justification) rather than silently expanding.
  </action>
  <verify>`bash tests/cross-harness-validation.sh` returns `1..136` / Passed 136 / Failed 0; `bash tests/pi-end-to-end-validation.sh` returns `1..231` / Passed 231 / Failed 0 unchanged; install reports 6 files; artifact_consistency PASS; `git diff --check` clean. If the count is anything other than `+1` from baseline 135, stop and reconcile from command output before APPLY finalize.</verify>
  <done>AC-3 + AC-4 satisfied: the v2.59 closure surface is command-verifiable with bounded count reconciliation; install + Pi + cross-harness + artifact_consistency + diff-whitespace all green.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Any source file under `drivers/pi/extensions/*.ts`, including the Phase 254 sibling `pals-context-injection.ts` (this phase is docs/test-only — no source LOC delta).
- `drivers/pi/install.sh`, `drivers/pi/uninstall.sh`, `drivers/pi/driver.yaml`.
- `README.md`, `drivers/pi/extensions/README.md`, `drivers/pi/skill-map.md`, and `docs/*.md` contract docs (Phase 255 already surfaced the S7 outcome; Phase 256 is closure, not surfacing).
- `tests/pi-end-to-end-validation.sh` and `tests/helpers/**` (the Phase 254 EXT_PALS_CONTEXT_INJECTION TAP block, the Phase 254 repoint of pre-existing assertion 133, and the Phase 255 surfacing TAP triple all remain in their post-Phase-255 form).
- Existing Phase 245 / Phase 248 / Phase 252 closure-guardrail blocks in `tests/cross-harness-validation.sh` (must remain byte-unchanged).
- Existing v2.55 / v2.56 / v2.57 / v2.58 completed-milestone rows and pointer paragraphs in `.paul/MILESTONES.md` (must remain byte-unchanged; only add new v2.59 entries).
- `kernel/**`, `modules/**`, `.pi/agents/**`, dependency manifests, lockfiles, CI configuration, GitHub Flow automation, helper-agent contracts, APPLY workflow semantics, guided workflow runtime behavior, module dispatch semantics, `pals.json`.
- S4 canonical reply delivery (`sendCanonicalWorkflowResponse`, `presentGuidedWorkflowMoment`, `loadGuidedWorkflowConfig`, `shouldAutoPresent`), S6 lifecycle UI, S8 command routing, CARL session boundary, module-activity-parsing, artifact-slice-rendering, workflow-resource-capsule-rendering, guided-workflow-detection, pals-context-injection — preserved deferrals and shipped siblings.
- The exact authority/activation byte sequences (`[PALS_STATE_AUTHORITY=.paul/STATE.md]`, `[PALS_ACTIVATION_SIGNAL]`), the legacy header `## PALS Context (auto-injected)`, the custom-message-type identity `pals-context`, and the injection-event constants `before_agent_start`/`context` (must continue to live single-defined in `drivers/pi/extensions/pals-context-injection.ts`).

## SCOPE LIMITS
- Evidence aggregation + lifecycle artifact closure + one cross-harness shared-invariant guardrail only.
- No runtime/source extraction, S4/S6/S8 implementation, install-surface change, dependency/CI change, validation-helper refactor, validation-suite decomposition, local context-cost measurement, demo project preset, or lifecycle-authority behavior change.
- The v2.60 recommendation is recorded, not approved. Final v2.60 selection happens via `/paul:milestone` after v2.59 closes.
- At most one new bounded cross-harness shared-invariant closure assertion (Task 3). If command output proves additional bounded repair is necessary, reconcile in-flight with explicit justification rather than expanding silently.
- No telemetry, hidden runtime state, generated lifecycle ledger, cache-as-truth, alternate validation/module/lifecycle/GitHub Flow ledger, auto-approval, auto-continue, skipped checkpoints, UI-only lifecycle decisions, or inferred merge intent.
- No payload-string drift in the runtime: `STATE_AUTHORITY_TAG`, `ACTIVATION_SIGNAL_TAG`, `LEGACY_PALS_CONTEXT_HEADER`, `PALS_CONTEXT_CUSTOM_TYPE`, `PRIMARY_INJECTION_EVENT`, `SUPPORTING_CONTEXT_EVENT` keep their exact byte sequences in `pals-context-injection.ts` (closure phase does not touch them).
- No claude-code/Agent-SDK driver changes (frozen since v2.52; full removal still blocked on cross-harness validation decomposition per `docs/PI-NATIVE-LEGACY-RETENTION-INSTALL-SURFACE-CLEANUP-CONTRACT.md`).
- If APPLY discovers the recommended v2.60 focus is ambiguous or unsupported by closure evidence, stop for guidance rather than inventing a roadmap.
- `pals-implementer` may be used only for a bounded task packet covering Task 1 or Task 2; parent APPLY owns diff review, official verification, module gates, fallback, GitHub Flow gates, and `.paul/*` lifecycle writes. Inline parent APPLY remains the recommended default for a 3-task closure phase (mirroring Phase 252 / 248 / 245 precedent).

</boundaries>

<verification>
Before declaring APPLY complete:
- [ ] `.paul/MILESTONES.md` contains the new v2.59 Completed Milestones row, the v2.59 closure paragraph in Current / Future Milestone Pointer, the updated `Current milestone:` line stating no active milestone after v2.59 closure, the recorded v2.60 default recommendation, and the appended footer trail entry.
- [ ] `.paul/PROJECT.md` Active milestone summary reads v2.59 complete (4 of 4 phases), Current milestone pointer reads no active milestone, the new Phase 256 closure Key Decisions row exists, and the footer is dated 2026-05-05.
- [ ] `.paul/ROADMAP.md` row 256 is `1/1 ✅ Complete 2026-05-05`, the v2.59 milestone closure summary names Phases 253–256 and PRs #168 / #169 / #170 / Phase 256 PR placeholder/actual PR, the v2.58 narrative is demoted to factual completed-milestone form (not deleted), and the footer is dated 2026-05-05.
- [ ] One new `Phase 256 sibling extraction wave closure (v2.59 Shared Invariant)` `tap_file_contains_all` block exists in `tests/cross-harness-validation.sh` immediately after the Phase 252 block; existing Phase 245 / 248 / 252 closure blocks are byte-unchanged.
- [ ] `bash tests/cross-harness-validation.sh` returns `1..136` / Passed 136 / Failed 0.
- [ ] `bash tests/pi-end-to-end-validation.sh` returns `1..231` / Passed 231 / Failed 0 unchanged.
- [ ] `bash tests/helpers/artifact_consistency.sh` returns `artifact_consistency_check: PASS`.
- [ ] `PALS_ROOT="$PWD" bash drivers/pi/install.sh` reports `[ok] Pi extensions installed: 6 files`.
- [ ] `git diff --check` returns clean.
- [ ] No `drivers/pi/extensions/*.ts` / `drivers/pi/install.sh` / `drivers/pi/uninstall.sh` / `drivers/pi/driver.yaml` / `tests/pi-end-to-end-validation.sh` / `tests/helpers/**` / `pals.json` / dependency / CI / kernel / module-registry / Claude Code driver / Agent SDK driver / installed-runtime-copy file is in the diff.
- [ ] Workguard report allows `tests/cross-harness-validation.sh` and `.paul/**` with no out-of-scope drift.
</verification>

<success_criteria>
- v2.59 closure evidence is durable in `.paul/MILESTONES.md`, `.paul/PROJECT.md`, and `.paul/ROADMAP.md`, and one localized cross-harness shared-invariant closure guardrail protects it (135 → 136).
- The final v2.59 validation/install count reconciliation is explicit: Pi 227 → 231 (+4 across the wave), cross-harness 135 → 136 (+1 closure guardrail), install 5 → 6 (+1 sibling).
- The v2.60 recommendation is recorded as a recommendation only and preserves alternatives; it does not approve the next milestone.
- The four-times-ratified sibling-surfacing recipe (Phase 244 / 247 / 251 / 255) and the Phase 254 type-only back-import + TAP-assertion repoint patterns are documented as durable patterns in MILESTONES.md and the PROJECT.md Key Decisions table.
- No runtime TypeScript, install/uninstall/manifest, dependency, CI, validation-helper, or lifecycle-authority scope creep occurs.
- All four Phase 256 ACs are satisfied.
- v2.59 closes cleanly, ready for `/paul:milestone` to decide v2.60.
</success_criteria>

<output>
After completion, create `.paul/phases/256-evidence-closure-next-roadmap-decision-v2-59/256-01-SUMMARY.md` with:
- Acceptance criteria results.
- Files modified with line deltas.
- Focused phrase-preservation evidence (preserved boundary phrases still present; closure rows / paragraphs / guardrails added; existing closure blocks byte-unchanged; existing milestone rows byte-unchanged).
- Validation command output and count reconciliation (cross-harness 135 → 136 with +1 closure guardrail; Pi 231/231 unchanged; install 6 unchanged).
- Module execution reports (pre-plan / pre-apply / post-task / post-apply / pre-unify / post-unify).
- GitHub Flow evidence (branch, commit, PR, CI, mergeStateStatus, merge result).
- Workguard scope evidence (allowed paths: `.paul/MILESTONES.md`, `.paul/PROJECT.md`, `.paul/ROADMAP.md`, `tests/cross-harness-validation.sh`, `.paul/**`; final changed-file set).
- Final v2.59 milestone closure narrative (validation count reconciliation across all four phases, durable patterns, preserved deferrals).
- Recorded v2.60 recommendation with explicit alternatives and the `/paul:milestone` approval boundary.
</output>
