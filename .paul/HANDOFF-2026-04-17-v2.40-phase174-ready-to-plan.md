# PAUL Handoff

**Date:** 2026-04-17T04:10:00Z
**Status:** paused (ready to plan Phase 174)

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** pals — Project Automation & Lifecycle System
**Core value:** The Linux of Harness Engineering — minimal kernel, modular pals, universal drivers

---

## Current State

**Version:** v2.40 CODI v0.1 — Extractor & Coverage Iteration
**Phase:** 174 of 4 — Source-File Symbol Extraction
**Plan:** Not started (ready to plan)

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ○        ○        ○     [Ready for first PLAN of v2.40]
```

## Git State

| Field | Value |
|-------|-------|
| Branch | `feature/174-source-file-symbol-extraction` |
| Base | `main` |
| PR | none yet (will be created during APPLY postflight) |
| CI | N/A |
| Behind base | 0 commits (1 commit ahead: milestone setup) |

The feature branch has ONE commit (`0bcf194`) — the v2.40 milestone setup (ROADMAP + STATE changes creating the milestone structure). No plan artifacts yet. The branch was intentionally created during `/paul:milestone` because github-flow requires feature branches before APPLY — the milestone setup commit needed somewhere to land.

---

## What Was Done (this session)

- **v2.39 milestone closed.** Phase 173 (Real-World Trial & Gating Decision) shipped with verdict **ITERATE_V0_1**. Full evidence in `.paul/phases/173-real-world-trial-gating-decision/173-01-DECISION.md`. PR #83 merged to main as squash commit `38c439a`.
- **v2.40 milestone created.** ROADMAP promoted v2.40 "CODI v0.1 — Extractor & Coverage Iteration" to Current Milestone with:
  - 4 phases: 174 → 175 → 176 → 177
  - 3 Milestone Invariants (install-required, self-test phasing, trial integrity)
  - v2.39 archived to Completed Milestones section
- **Phase directories created** (all empty, awaiting PLAN): `.paul/phases/{174,175,176,177}-*/`
- **STATE.md updated** to v2.40 Phase 174 ready-to-plan.
- **Feature branch created** `feature/174-source-file-symbol-extraction` from main, milestone setup commit made.

---

## What's In Progress

Nothing mid-flight. Loop position is cleanly at "ready for first PLAN of v2.40." No partial tasks, no mid-APPLY state, no open retroactive flags.

---

## What's Next

**Immediate:** `/paul:plan` for Phase 174 (Source-File Symbol Extraction).

**After that:** approve plan → `/paul:apply` → `/paul:unify` → repeat for Phases 175, 176, 177.

---

## v2.40 Milestone Invariants (CRITICAL CONTEXT)

These are documented in `.paul/ROADMAP.md` Current Milestone section. **Any phase in v2.40 must respect them:**

1. **Nothing takes effect until `bash drivers/pi/install.sh` runs.** The installed surface (`~/.pi/agent/skills/pals/modules.yaml`, `~/.pi/agent/skills/pals/kernel/workflows/*`, `~/.pi/agent/skills/pals/modules/codi/*`) is what CODI dispatches against at plan time. Repo-source edits to `modules/codi/*`, `kernel/workflows/*`, or `drivers/pi/install.sh` itself are **invisible** until the installer regenerates the installed tree. Every v2.40 phase that edits any of those surfaces MUST include `PALS_ROOT="$(pwd)" bash drivers/pi/install.sh` as a verification step in APPLY.

2. **Self-test phasing.** Phase 174 plans-and-applies against the v2.39 (prose-only) extractor because the v2.40 extractor doesn't go live until 174's APPLY + installer re-run complete. Phase 174's own CODI dispatch is therefore **expected CODI_NULL**. The first session where the v2.40 extractor demonstrates itself live is **Phase 175's planning run**. Phase 177's re-trial set is {175-plan, 176-plan, 177-plan-self} plus counterfactual, NOT {174-plan, ...}.

3. **Trial integrity (Phase 177).** Before observing each trial session's dispatch, confirm `~/.pi/agent/skills/pals/modules.yaml` reflects the latest repo-source `modules/codi/module.yaml`. Any session observed against a stale installed manifest is excluded from the trial set.

**Meta-bootstrap quirk for Phase 175:** it edits `drivers/pi/install.sh` itself. APPLY must run the installer TWICE — once to deploy the edit, once to exercise the new probe.

---

## Phase 174 Scope (summary)

**Focus:** Highest-leverage v2.40 iteration. During `prepare_codi_seed_candidates` (kernel/workflows/plan-phase.md), read PLAN's `<context>` source-file list and parse top-level declarations (TS/JS functions, classes, exports; extensible) to seed `impact` calls in addition to the existing prose-based extraction.

**Why this matters:** Phase 173 counterfactual (Session E) established that Phase 168's scope — `drivers/pi/extensions/pals-hooks.ts` — would have yielded **4 resolved-with-call-sites symbols** (`palsHooks` depth 3 fan-out 21, `renderLifecycleStatus`, `renderCompactLoopSummary`, `syncLifecycleUi`) if source-file extraction had been active. v2.39's prose-only extractor missed these because they're not mentioned by name in prose-heavy plan objectives.

**Target files (for plan-time scope):**
- `modules/codi/module.yaml` — manifest description Step 1: extraction rules expansion
- `modules/codi/references/codi.md` — Symbol Extraction Rules section expansion
- `kernel/workflows/plan-phase.md` — `prepare_codi_seed_candidates` step updates

**Expected outcome after Phase 174 APPLY + install:** Phase 175's planning run (next phase after 174) will be the first session where CODI dispatches with the v2.40 extractor active, potentially producing resolved blast_radius from the TS source files it touches.

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state — Phase 174 ready-to-plan |
| `.paul/ROADMAP.md` | v2.40 Current Milestone + Milestone Invariants section |
| `.paul/phases/173-real-world-trial-gating-decision/173-01-DECISION.md` | Phase 173 verdict ITERATE_V0_1 — the spec for v2.40 |
| `.paul/phases/173-real-world-trial-gating-decision/173-01-TRIAL-DATA.md` | Counterfactual evidence (Session E) justifying Phase 174's approach |
| `.paul/phases/173-real-world-trial-gating-decision/173-01-SUMMARY.md` | v2.39 closeout with Key Patterns section (empirical probing, CODI_NULL discipline, etc.) |
| `modules/codi/module.yaml` | Current extractor contract (v2.39 prose-only; Phase 174 will extend) |
| `modules/codi/references/codi.md` | Current CODI reference doc |
| `kernel/workflows/plan-phase.md` | Current `prepare_codi_seed_candidates` step (Phase 172) |
| `pals.json` | `planning.default_collaboration: high`; `modules.codi.enabled: true` |

---

## Recent Decisions (context)

From Phase 173 (2026-04-17), all in `173-01-DECISION.md` + STATE.md Decisions table:

- **CODI_NULL-aware signal scoring** is required for any future CODI trial rubric — without it, Signal 3 falsely attributes planner-discipline gains to CODI.
- **Counterfactual projection** is the only way to score a value envelope when live trial samples are uniformly CODI_NULL.
- **The fix is in the extraction layer, not the seed-coupling layer.** Phase 172's prose-heavy seed coupling worked but extracted the wrong symbols. Source-file symbol extraction (Phase 174) is the correct layer.
- **Empirical probing during PLAN** materially reshaped Phase 173 (7 `impact` probes during plan revision). Continue the pattern for Phase 174 if extractor-layer assumptions need validation against the live tool.

---

## Planning Posture

`pals.json` `planning.default_collaboration: high`. Phase 173 kept high and it paid off (pre-APPLY review caught 3 real concerns including concern 2 which became the trial's structural reframing). Recommend continuing high for Phase 174, especially because it touches the extractor contract — easy to get the extraction rules subtly wrong.

Phase 174 is **direct-requirements** planning: Phase 173's DECISION.md ITERATE_V0_1 scope Task 1 is the spec. No exploratory design needed.

---

## Resume Instructions

1. Read `.paul/STATE.md` for latest position (should say Phase 174 ready-to-plan).
2. Read this handoff for v2.40 context (invariants + Phase 174 scope).
3. Read `.paul/phases/173-real-world-trial-gating-decision/173-01-DECISION.md` section "ITERATE_V0_1 scope — SELECTED" → Task 1 for the Phase 174 spec.
4. Read the target files to see their current state before planning:
   - `modules/codi/module.yaml`
   - `modules/codi/references/codi.md`
   - `kernel/workflows/plan-phase.md` (look for `prepare_codi_seed_candidates`)
5. Run `/paul:plan` (or `/skill:paul-plan`) for Phase 174.

Or just: `/paul:resume` / `/skill:paul-resume` — it will route you to the same place.

---

*Handoff created: 2026-04-17T04:10:00Z*
